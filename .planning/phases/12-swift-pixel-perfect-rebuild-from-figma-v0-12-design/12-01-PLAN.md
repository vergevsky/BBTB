---
phase: 12-swift-pixel-perfect-rebuild-from-figma-v0-12-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BBTB/Packages/DesignSystem/Sources/DesignSystem/DesignSystem.swift
  - BBTB/Packages/DesignSystem/Sources/DesignSystem/DSColor.swift
  - BBTB/Packages/DesignSystem/Sources/DesignSystem/ButtonStyles.swift
  - BBTB/Packages/DesignSystem/Package.swift
  - BBTB/Packages/DesignSystem/Tests/DesignSystemTests/DSColorTests.swift
  - BBTB/Packages/DesignSystem/Tests/DesignSystemTests/DSTokensTests.swift
  - BBTB/Packages/DesignSystem/Tests/DesignSystemSnapshotTests/ButtonStyleSnapshotTests.swift
  - BBTB/Packages/DesignSystem/Tests/DesignSystemSnapshotTests/__Snapshots__/.gitkeep
autonomous: true
requirements: [DS-01, DS-02, DS-03, DS-04, DS-05, DS-06, DS-07, DS-10, DS-15]
tags: [design-system, ios, swiftui, snapshot-testing, foundation]

must_haves:
  truths:
    - "Public `DS.Color` enum существует с 15 семантическими токенами (canvas, surface, surfaceSunken, surfaceHeader, divider, controlIdle, accent, error, textPrimary, textSecondary, textTertiary, textInverse, iconPrimary, iconSecondary, iconMuted), каждый возвращает SwiftUI.Color с авто-переключением Dark/Light."
    - "`DS.Color.accent.cgColor` равен sRGB(0x14,0x66,0x4B) ±0.01 в обоих режимах (Dark и Light — Phase 11 designer-decision: accent одинаков)."
    - "`DS.Typography.expanded(_:weight:)` helper применяет `.system(size:weight:).width(.expanded)` и используется всеми 9 sized presets (displayTimer, titleScreen, titleSection, titleUppercase, labelButton, bodyDefault, bodyCaption, bodyMicro, tipsLight)."
    - "`DS.Typography.Size` enum содержит 7 CGFloat constants: display=48, title=16, labelButton=14, body=12, tips=10, caption=9, micro=8."
    - "Существующие deprecated aliases `DS.Typography.display/title/body/callout/subheadline/caption` сохраняются и внутри возвращают `expanded(...)` Font (~95 call-sites не ломаются)."
    - "`DS.Radius.section=24`, `DS.Radius.sheet=32`, `DS.Blur.pill=4` определены как public static let CGFloat constants."
    - "`DS.ConnectionButtonSize.compactDiameter=280`, `regularDiameter=320`, `compactIcon=112`, `regularIcon=128` (names preserved, values updated)."
    - "`DS.accent` существует как `@available(*, deprecated, renamed: \"DS.Color.accent\")` alias на `DS.Color.accent` (backward compat)."
    - "Public structs `PrimaryButtonStyle: ButtonStyle` и `SecondaryButtonStyle: ButtonStyle` существуют в DesignSystem с public init() и сборкой через DS.Typography.labelButton + DS.Color tokens."
    - "Package.swift содержит url-dependency `https://github.com/pointfreeco/swift-snapshot-testing` from \"1.18.3\" и два test targets: `DesignSystemTests` (unit) + `DesignSystemSnapshotTests` (image)."
    - "`swift test --package-path BBTB/Packages/DesignSystem` (или xcodebuild эквивалент) выполняется зелёным: DS.Color.accent hex assertion + DS.ConnectionButtonSize numeric assertions + DS.Radius/Blur assertions + ButtonStyle snapshot baseline recorded."
  artifacts:
    - path: "BBTB/Packages/DesignSystem/Sources/DesignSystem/DesignSystem.swift"
      provides: "DS namespace + extended Spacing/Radius (with section/sheet)/Blur/ConnectionButtonSize/Typography (with expanded helper + Size constants + 9 presets + deprecated aliases)"
      contains: "DS.Radius.section, DS.Radius.sheet, DS.Blur.pill, DS.Typography.Size.display, DS.Typography.expanded, DS.ConnectionButtonSize.compactDiameter, deprecated DS.accent"
    - path: "BBTB/Packages/DesignSystem/Sources/DesignSystem/DSColor.swift"
      provides: "DS.Color enum (15 semantic tokens) + UIColor/NSColor dynamic provider bridge"
      exports: ["DS.Color.canvas", "DS.Color.surface", "DS.Color.surfaceSunken", "DS.Color.surfaceHeader", "DS.Color.divider", "DS.Color.controlIdle", "DS.Color.accent", "DS.Color.error", "DS.Color.textPrimary", "DS.Color.textSecondary", "DS.Color.textTertiary", "DS.Color.textInverse", "DS.Color.iconPrimary", "DS.Color.iconSecondary", "DS.Color.iconMuted"]
    - path: "BBTB/Packages/DesignSystem/Sources/DesignSystem/ButtonStyles.swift"
      provides: "PrimaryButtonStyle + SecondaryButtonStyle (custom ButtonStyle, Capsule pill, pressed-state scale/opacity, Reduce-Motion fallback)"
      exports: ["PrimaryButtonStyle", "SecondaryButtonStyle"]
    - path: "BBTB/Packages/DesignSystem/Package.swift"
      provides: "swift-snapshot-testing dep + DesignSystemTests + DesignSystemSnapshotTests targets"
      contains: ".package(url: \"https://github.com/pointfreeco/swift-snapshot-testing\", from: \"1.18.3\")"
    - path: "BBTB/Packages/DesignSystem/Tests/DesignSystemTests/DSColorTests.swift"
      provides: "Hex value assertions для DS.Color.accent + error + canvas (Dark+Light)"
    - path: "BBTB/Packages/DesignSystem/Tests/DesignSystemTests/DSTokensTests.swift"
      provides: "Numeric assertions: ConnectionButtonSize 280/320/112/128 + Radius.section=24/sheet=32 + Blur.pill=4 + Typography.Size 7-tuple"
    - path: "BBTB/Packages/DesignSystem/Tests/DesignSystemSnapshotTests/ButtonStyleSnapshotTests.swift"
      provides: "Baseline snapshot tests для PrimaryButtonStyle + SecondaryButtonStyle (proof-of-infrastructure для DS-15)"
  key_links:
    - from: "DSColor.swift dynamic provider"
      to: "SwiftUI Color"
      via: "UIColor(dynamicProvider:) на iOS / NSColor(name:dynamicProvider:) на macOS"
      pattern: "userInterfaceStyle == \\.dark"
    - from: "DS.Typography presets"
      to: "DS.Typography.expanded(_:weight:)"
      via: "shared helper применяет .system(size:weight:).width(.expanded)"
      pattern: "\\.width\\(\\.expanded\\)"
    - from: "Package.swift testTarget"
      to: "SnapshotTesting product"
      via: ".product(name: \"SnapshotTesting\", package: \"swift-snapshot-testing\")"
      pattern: "swift-snapshot-testing"
    - from: "DS.accent deprecated alias"
      to: "DS.Color.accent"
      via: "@available(*, deprecated, renamed:) wrapper"
      pattern: "deprecated.*renamed: \"DS.Color.accent\""
---

<objective>
Plan 12-01 (Foundation) расширяет DesignSystem package новыми токенами и устанавливает snapshot test infrastructure. **Никаких UI изменений** — только tokens (15 DS.Color, 7 Typography.Size + 9 presets + expanded helper, 2 новых Radius, 1 Blur, обновлённые ConnectionButtonSize numerics, deprecated DS.accent alias), 2 кастомных ButtonStyle и тестовая инфраструктура (`swift-snapshot-testing` ≥1.18.3 + два test target'а).

Purpose: Создать атомарную foundation, на которую Plan 12-02 нашивает все UI изменения. Существующие 95 call-sites DS.* и ~207 тестов остаются зелёными (additions only + deprecated alias + numeric updates).

Output: Расширенный `DS` namespace, `DSColor.swift`, `ButtonStyles.swift`, обновлённый `Package.swift` (+ url dep, + 2 test targets), Unit + первый baseline snapshot, готовая infrastructure для Plan 12-02 component тестов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-CONTEXT.md
@.planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-RESEARCH.md
@.planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-PATTERNS.md
@.planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-UI-SPEC.md
@BBTB/Packages/DesignSystem/Tokens/CODE-CONNECT.md
@BBTB/Packages/DesignSystem/Tokens/figma-tokens.json
@BBTB/Packages/DesignSystem/Sources/DesignSystem/DesignSystem.swift
@BBTB/Packages/DesignSystem/Package.swift
@BBTB/Packages/AppFeatures/Package.swift

<interfaces>
<!-- Existing DS namespace (DesignSystem.swift) — preserve as additions-only baseline. -->
<!-- Все 9 существующих presets ниже остаются как deprecated aliases в Task 1. -->

From BBTB/Packages/DesignSystem/Sources/DesignSystem/DesignSystem.swift:
- `public enum DS` (umbrella)
- `public static let accent: Color = .accentColor` (Phase 1 carry-forward — будет deprecated alias на DS.Color.accent)
- `public static let titleFont: Font = .system(.title, design: .rounded).weight(.semibold)` (preserve untouched — отдельный legacy узел)
- `public enum Spacing` с xs/sm/md/lg/xl/xxl/xxxl (preserve as-is — Figma уже совпадает)
- `public enum Radius` с small/card/cardLarge/button (preserve, добавить section + sheet)
- `public enum Typography` с display/title/body/callout/subheadline/caption (refactor internally to expanded(), preserve names)
- `public enum ConnectionButtonSize` с 4 const (update numerics 140→280, 160→320, 56→112, 64→128, names preserved)

From CODE-CONNECT.md §2.1 (15 DS.Color семантических токенов — source of truth):
canvas        Dark #000000 / Light #FFFFFF
surface       Dark #222222 / Light #F4F4F6
surfaceSunken Dark #1A1A1A / Light #ECEDEF
surfaceHeader Dark #333333 / Light #E0E0E5
divider       Dark #333333 / Light #D8D8DD
controlIdle   Dark #222222 / Light #E8E8EC
accent        Dark #14664B / Light #14664B  (одинаков — Phase 11 designer-decision)
error         Dark #661414 / Light #B3261E
textPrimary   Dark #FFFFFF / Light #111113
textSecondary Dark #808080 / Light #6B6B72
textTertiary  Dark #64706F / Light #7A8281
textInverse   Dark #000000 / Light #FFFFFF
iconPrimary   Dark #FFFFFF / Light #111113
iconSecondary Dark #808080 / Light #6B6B72
iconMuted     Dark #CCCCCC / Light #A5A5AC

From CODE-CONNECT.md §2.2 + §3 (numeric + typography):
DS.Radius.section = 24
DS.Radius.sheet   = 32
DS.Blur.pill      = 4
DS.Typography.Size.display=48, title=16, labelButton=14, body=12, tips=10, caption=9, micro=8
9 presets per RESEARCH §2.2: displayTimer (48/Medium), titleScreen (16/Semibold), titleSection (12/Semibold),
titleUppercase (9/Semibold), labelButton (14/Semibold), bodyDefault (12/Regular), bodyCaption (9/Regular),
bodyMicro (8/Regular), tipsLight (10/Light)

From RESEARCH §2.3 dynamic provider sketch:
private static func dynamic(dark: UInt32, light: UInt32) -> SwiftUI.Color  — iOS UIColor / macOS NSColor branch
private static func uiColor(hex: UInt32) -> UIColor   (iOS)
private static func nsColor(hex: UInt32) -> NSColor   (macOS)

From RESEARCH §2.5 ButtonStyle signatures:
public struct PrimaryButtonStyle: ButtonStyle { public init(); public func makeBody(configuration: Configuration) -> some View }
public struct SecondaryButtonStyle: ButtonStyle { public init(); public func makeBody(configuration: Configuration) -> some View }

From BBTB/Packages/DesignSystem/Package.swift (current — extend):
let package = Package(
    name: "DesignSystem",
    platforms: [.iOS(.v18), .macOS(.v15)],
    products: [.library(name: "DesignSystem", targets: ["DesignSystem"])],
    targets: [.target(name: "DesignSystem")]
)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend DS namespace — Radius/Blur/Typography.Size + expanded helper + 9 presets + ConnectionButtonSize numerics + deprecated DS.accent alias</name>
  <files>BBTB/Packages/DesignSystem/Sources/DesignSystem/DesignSystem.swift, BBTB/Packages/DesignSystem/Tests/DesignSystemTests/DSTokensTests.swift</files>
  <read_first>
    - BBTB/Packages/DesignSystem/Sources/DesignSystem/DesignSystem.swift (целиком — текущий 46-строчный файл)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-RESEARCH.md §2.2 (lines 262-301 — DS.Typography.expanded helper + Size + 9 presets) + §3 (DS-02..DS-05 allocation table)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-PATTERNS.md §MOD-1 (lines 30-93 — token shape, existing Spacing/Radius examples)
    - BBTB/Packages/DesignSystem/Tokens/CODE-CONNECT.md §2.2 (lines 203-231) + §3 (lines 235-253)
    - BBTB/Packages/DesignSystem/Tokens/figma-tokens.json (size+radius+blur sections — verify exact values)
  </read_first>
  <behavior>
    - Test 1: `XCTAssertEqual(DS.Radius.section, 24)` + `XCTAssertEqual(DS.Radius.sheet, 32)`.
    - Test 2: `XCTAssertEqual(DS.Blur.pill, 4)`.
    - Test 3: `XCTAssertEqual(DS.Typography.Size.display, 48)` и аналогично для title=16, labelButton=14, body=12, tips=10, caption=9, micro=8.
    - Test 4: `XCTAssertEqual(DS.ConnectionButtonSize.compactDiameter, 280)` + `regularDiameter=320` + `compactIcon=112` + `regularIcon=128`.
    - Test 5: compile-time existence — `let _: Font = DS.Typography.displayTimer; let _ = DS.Typography.titleScreen; ...` для всех 9 presets + проверка что `DS.Typography.body` (deprecated alias) всё ещё компилируется (~95 call-sites guard).
  </behavior>
  <action>
    Extend `DesignSystem.swift` strictly additively per RESEARCH §2.2 code sketch + §3 DS allocation. Все добавления — public; имена сохраняются:

    1. В `DS.Radius` enum добавить `public static let section: CGFloat = 24` (DS-03 / M10) и `public static let sheet: CGFloat = 32` (DS-03 / M9). Существующие small/card/cardLarge/button НЕ трогать.
    2. Добавить новый nested enum `public enum Blur { public static let pill: CGFloat = 4 }` (DS-04 — token из figma-tokens.json).
    3. В `DS.Typography` добавить nested enum `public enum Size { public static let display: CGFloat = 48; title=16; labelButton=14; body=12; tips=10; caption=9; micro=8 }` (DS-02). Все 7 — public static let CGFloat.
    4. Добавить public helper `public static func expanded(_ size: CGFloat, weight: Font.Weight = .regular) -> Font` который возвращает `.system(size: size, weight: weight).width(.expanded)` (DS-06).
    5. Добавить 9 sized presets как public static let Font: displayTimer = expanded(Size.display, weight: .medium); titleScreen = expanded(Size.title, weight: .semibold); titleSection = expanded(Size.body, weight: .semibold); titleUppercase = expanded(Size.caption, weight: .semibold); labelButton = expanded(Size.labelButton, weight: .semibold); bodyDefault = expanded(Size.body, weight: .regular); bodyCaption = expanded(Size.caption, weight: .regular); bodyMicro = expanded(Size.micro, weight: .regular); tipsLight = expanded(Size.tips, weight: .light).
    6. **Migration deprecated aliases (preserve ~95 call-sites):** существующие `DS.Typography.display/title/body/callout/subheadline/caption` сохранить, **но переопределить внутренне** так чтобы они возвращали `expanded(...)` Font: display = expanded(Size.display, weight: .medium); title = expanded(Size.title, weight: .semibold); body = expanded(Size.body, weight: .regular); callout = expanded(Size.body, weight: .regular); subheadline = expanded(Size.body, weight: .medium); caption = expanded(Size.caption, weight: .regular). Это **выполняет M4** (font family switch) для всего call-site corpus без массовых правок (DS-06).
    7. В `DS.ConnectionButtonSize` обновить numerics (имена preserve): `compactDiameter: 140 → 280; regularDiameter: 160 → 320; compactIcon: 56 → 112; regularIcon: 64 → 128` (DS-05 / M1+M2).
    8. Переопределить top-level `DS.accent`: поставить `@available(*, deprecated, renamed: "DS.Color.accent") public static let accent: Color = Color.accent` где `Color.accent` приходит из `DSColor.swift` (Task 2). Это даст backward-compat warning во всех call-sites, не сломав сборку (DS-07 / M5 wired в Plan 12-02). **NB:** Task 2 даёт `DS.Color.accent`; Task 1 ссылается на него — порядок применения через тот же compile unit (одна DesignSystem target), Xcode разрешит forward reference.
    9. Doc-comments: для каждой группы добавить `/// Phase 12 / DS-XX — <описание>. См. CODE-CONNECT.md §2.x.` (например `/// Phase 12 / DS-03 / M9-M10 — sheet+section radius tokens.`).
    10. Dynamic Type comment: над `DS.Typography` enum добавить `/// Dynamic Type intentionally NOT applied per Phase 12 UI-SPEC §3.4. Backlog: v1.x.` (UI-SPEC §3.4 explicit contract).

    Создать `Tests/DesignSystemTests/DSTokensTests.swift` (новый test target — будет объявлен в Task 4). Test class `final class DSTokensTests: XCTestCase` (XCTest, не Swift Testing — consistency per RESEARCH §6.1) с 5 функциями: `test_radius_sectionAndSheet`, `test_blur_pill`, `test_typographySize_sevenConstants`, `test_connectionButtonSize_phase12Values`, `test_typographyPresets_compileCheck`. Каждая использует Russian inline error messages (`XCTAssertEqual(DS.Radius.section, 24, "DS-03 / M10 — section radius должен быть 24pt")`) per PATTERNS §«Shared Patterns 5».
  </action>
  <verify>
    <automated>swift test --package-path BBTB/Packages/DesignSystem --filter DSTokensTests 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    DSTokensTests все 5 функций PASS. `swift build --package-path BBTB/Packages/DesignSystem` SUCCEEDED. `grep -c "deprecated.*DS.Color.accent" BBTB/Packages/DesignSystem/Sources/DesignSystem/DesignSystem.swift` ≥ 1. `grep -Ev '^[[:space:]]*//' BBTB/Packages/DesignSystem/Sources/DesignSystem/DesignSystem.swift | grep -c "static let compactDiameter: CGFloat = 280"` = 1. Существующие AppFeatures regression тесты (`swift test --package-path BBTB/Packages/AppFeatures --filter ConnectionButtonTests`) — зелёные (numeric token change не ломает helper-only тесты).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DSColor.swift — 15 semantic tokens с UIColor/NSColor dynamic provider (Dark+Light wire-only)</name>
  <files>BBTB/Packages/DesignSystem/Sources/DesignSystem/DSColor.swift, BBTB/Packages/DesignSystem/Tests/DesignSystemTests/DSColorTests.swift</files>
  <read_first>
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-RESEARCH.md §2.3 (lines 303-395 — Option B literal pattern + dynamic provider + 15 tokens + migration plan DS.accent)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-PATTERNS.md §MOD-2 (lines 97-133 — analog + dynamic provider sketch) + §«Shared Patterns 3» (#if os(iOS)/macOS pattern)
    - BBTB/Packages/DesignSystem/Tokens/CODE-CONNECT.md §2.1 (lines 181-202 — 15 tokens с точными hex Dark+Light)
    - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift (lines 9, 57-73 — каноничный `#if os(iOS) / #else` example to mirror)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-CONTEXT.md D-05..D-07 (Light wire-only, dynamic provider, system auto-switch — никаких runtime toggles)
  </read_first>
  <behavior>
    - Test 1: `DS.Color.accent` resolved в Dark trait collection возвращает sRGB(0x14/255, 0x66/255, 0x4B/255) ±0.003 — DS-07 / M5 contract.
    - Test 2: `DS.Color.accent` resolved в Light trait collection возвращает то же sRGB(0x14/255, 0x66/255, 0x4B/255) (одинаков per CODE-CONNECT.md §2.1 + designer-decision Phase 11).
    - Test 3: `DS.Color.canvas` Dark = sRGB(0,0,0); Light = sRGB(1,1,1) (D-05 wire-only contract).
    - Test 4: `DS.Color.error` Dark = sRGB(0x66/255, 0x14/255, 0x14/255); Light = sRGB(0xB3/255, 0x26/255, 0x1E/255).
    - Test 5: все 15 свойств существуют и возвращают `SwiftUI.Color` (compile-check + non-nil resolve).
  </behavior>
  <action>
    Создать новый файл `BBTB/Packages/DesignSystem/Sources/DesignSystem/DSColor.swift` по точному sketch из RESEARCH §2.3 (lines 326-383) + добавить top-level `public extension Color` alias для backward compat per RESEARCH §2.3 migration plan:

    1. Header comment: `/// Phase 12 / DS-01 / DS-07 — semantic color tokens из Figma BBTB v3 (15 токенов). См. CODE-CONNECT.md §2.1 + RESEARCH.md §2.3. Dark = pixel-perfect Figma source-of-truth; Light = placeholder per Phase 11 designer-decision (D-06). System auto-switch через UIColor(dynamicProvider:) — D-07 (без in-app toggle).`
    2. `import SwiftUI` + (внутри `#if os(iOS) import UIKit #elseif os(macOS) import AppKit #endif`).
    3. `public extension DS { enum Color { ... } }` с 15 public static let SwiftUI.Color свойствами в порядке CODE-CONNECT.md §2.1: canvas, surface, surfaceSunken, surfaceHeader, divider, controlIdle, accent, error, textPrimary, textSecondary, textTertiary, textInverse, iconPrimary, iconSecondary, iconMuted. Каждый = `dynamic(dark: 0xXXXXXX, light: 0xYYYYYY)` с точными hex values из CODE-CONNECT.md §2.1 (canvas Dark=0x000000 Light=0xFFFFFF; surface Dark=0x222222 Light=0xF4F4F6; surfaceSunken Dark=0x1A1A1A Light=0xECEDEF; surfaceHeader Dark=0x333333 Light=0xE0E0E5; divider Dark=0x333333 Light=0xD8D8DD; controlIdle Dark=0x222222 Light=0xE8E8EC; **accent Dark=0x14664B Light=0x14664B одинаков**; error Dark=0x661414 Light=0xB3261E; textPrimary Dark=0xFFFFFF Light=0x111113; textSecondary Dark=0x808080 Light=0x6B6B72; textTertiary Dark=0x64706F Light=0x7A8281; textInverse Dark=0x000000 Light=0xFFFFFF; iconPrimary Dark=0xFFFFFF Light=0x111113; iconSecondary Dark=0x808080 Light=0x6B6B72; iconMuted Dark=0xCCCCCC Light=0xA5A5AC).
    4. Helper `private static func dynamic(dark: UInt32, light: UInt32) -> SwiftUI.Color` с `#if os(iOS) / #elseif os(macOS) / #endif` ветвями per RESEARCH §2.3 sketch. iOS branch: `SwiftUI.Color(uiColor: UIColor { traits in traits.userInterfaceStyle == .dark ? uiColor(hex: dark) : uiColor(hex: light) })`. macOS branch: `SwiftUI.Color(nsColor: NSColor(name: nil) { appearance in let isDark = appearance.bestMatch(from: [.darkAqua, .vibrantDark]) != nil; return isDark ? nsColor(hex: dark) : nsColor(hex: light) })`.
    5. Hex helpers: `#if os(iOS)` `private static func uiColor(hex: UInt32) -> UIColor { UIColor(red: CGFloat((hex >> 16) & 0xFF)/255, green: CGFloat((hex >> 8) & 0xFF)/255, blue: CGFloat(hex & 0xFF)/255, alpha: 1) }` + `#elseif os(macOS)` analog `nsColor(hex:)` using `NSColor(srgbRed:green:blue:alpha:)`.
    6. **Top-level alias** для Task 1 deprecated DS.accent: после enum закрытия добавить `internal extension SwiftUI.Color { static var accent: SwiftUI.Color { DS.Color.accent } }` (internal — только для Task 1 deprecated alias; не loadbearing для downstream).

    Создать `Tests/DesignSystemTests/DSColorTests.swift`:
    - Class `final class DSColorTests: XCTestCase` + `@MainActor` (UI traits — main actor).
    - Helper `private func components(of color: SwiftUI.Color, traits: UITraitCollection) -> (r: CGFloat, g: CGFloat, b: CGFloat, a: CGFloat)` через `UIColor(color).resolvedColor(with: traits).cgColor.components`. На macOS test использовать `NSColor` resolver через appearance switch. Использовать `#if os(iOS)` ветки в тесте.
    - 5 функций RU error messages: `test_accent_darkHexMatchesFigma`, `test_accent_lightHexMatchesFigma`, `test_canvas_darkAndLight`, `test_error_darkAndLight`, `test_allFifteenTokensResolve` (последний просто компиляция + non-empty array через mirror — guard что 15 tokens существуют).
    - Tolerance: `XCTAssertEqual(red, 0x14/255.0, accuracy: 0.003, "DS-07 / M5 — DS.Color.accent.red Dark должен быть 0x14/255")`.
  </action>
  <verify>
    <automated>swift test --package-path BBTB/Packages/DesignSystem --filter DSColorTests 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    Все 5 функций DSColorTests PASS на iOS Simulator (xcodebuild test через DesignSystem scheme либо SPM CLI если работает на iOS). `grep -c "static let " BBTB/Packages/DesignSystem/Sources/DesignSystem/DSColor.swift` ≥ 15 (15 token + helpers). `grep -E "Dark.*0x14664B|0x14664B.*0x14664B" BBTB/Packages/DesignSystem/Sources/DesignSystem/DSColor.swift | grep -c accent` ≥ 1. DesignSystem package сборка зелёная iOS+macOS (`xcodebuild build -scheme DesignSystem -destination 'platform=iOS Simulator,name=iPhone 16'` + same `-destination 'platform=macOS'`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ButtonStyles.swift — PrimaryButtonStyle + SecondaryButtonStyle с Reduce-Motion fallback</name>
  <files>BBTB/Packages/DesignSystem/Sources/DesignSystem/ButtonStyles.swift</files>
  <read_first>
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-RESEARCH.md §2.5 (lines 476-538 — PrimaryButtonStyle + SecondaryButtonStyle sketch + haptic note)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-UI-SPEC.md §2.3 (lines 61-65 — pressed-state motion contract) + §3.6 (lines 129-135 — tap target ≥44pt) + §3.8 (lines 140-149 — Reduce-Motion fallback)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-PATTERNS.md §«NEW · ButtonStyles.swift» (lines 136-184 — нет existing analog, canonical SwiftUI pattern)
    - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift (lines 93-106 — то, что Plan 12-02 заменит на эти стили — verify signatures соответствуют usage)
  </read_first>
  <behavior>
    - Behavior 1: При `configuration.isPressed = false` view имеет `scaleEffect(1.0)` + `opacity(1.0)`; фон `Capsule().fill(DS.Color.accent)` для Primary / `Capsule().fill(DS.Color.textPrimary)` для Secondary.
    - Behavior 2: При `configuration.isPressed = true` (default motion path) — `scaleEffect(0.97)` + `opacity(0.92)` с `.animation(.easeOut(duration: 0.12), value: configuration.isPressed)`.
    - Behavior 3: При `accessibilityReduceMotion = true` (UI-SPEC §3.8) — заменить animated press на static `scaleEffect(1.0)` + `opacity(0.85)` без `.animation(...)` modifier.
    - Behavior 4: Tap target вертикально достигает ≥44pt: `.padding(.vertical, DS.Spacing.lg)` = 16 + контентный label ≥ 12pt + 16 = ≥44pt (Behaviour gate — UI-SPEC §3.6).
    - **Note:** snapshot test для рендера живёт в Task 5; здесь только структурная корректность (compile + signature + Reduce-Motion gate).
  </behavior>
  <action>
    Создать `BBTB/Packages/DesignSystem/Sources/DesignSystem/ButtonStyles.swift` по RESEARCH §2.5 sketch + UI-SPEC §2.3/§3.8 Reduce-Motion gate:

    1. Header comment: `/// Phase 12 / DS-10 / M7 — Primary/Secondary pill-button styles (Figma Onboarding CTAs). Применяются в Plan 12-02 к OnboardingView. См. RESEARCH §2.5 + UI-SPEC §2.3/§3.8.`
    2. `import SwiftUI`.
    3. `public struct PrimaryButtonStyle: ButtonStyle`:
       - `public init() {}`.
       - Свойство `@Environment(\.accessibilityReduceMotion) private var reduceMotion: Bool` — **первый precedent в проекте** для accessibilityReduceMotion (PATTERNS §«No Analog Found»).
       - `public func makeBody(configuration: Configuration) -> some View` который возвращает: `configuration.label.font(DS.Typography.labelButton).foregroundStyle(DS.Color.textPrimary).frame(maxWidth: .infinity).padding(.vertical, DS.Spacing.lg).background(Capsule().fill(DS.Color.accent)).scaleEffect(reduceMotion ? 1.0 : (configuration.isPressed ? 0.97 : 1.0)).opacity(reduceMotion ? (configuration.isPressed ? 0.85 : 1.0) : (configuration.isPressed ? 0.92 : 1.0)).animation(reduceMotion ? nil : .easeOut(duration: 0.12), value: configuration.isPressed)`.
    4. `public struct SecondaryButtonStyle: ButtonStyle` — идентичная структура за исключением `.foregroundStyle(DS.Color.textInverse)` (тёмный текст) + `.background(Capsule().fill(DS.Color.textPrimary))` (белая pill в Dark mode).
    5. Никаких internal/private helper struct'ов; никаких хардкодов pt/Color — только DS.* токены.
    6. **Haptic NOT inside ButtonStyle** (RESEARCH §2.5 — ButtonStyle.makeBody не имеет hook на tap). Haptic поднимается на consumer-side в Plan 12-02 task OnboardingView через `.sensoryFeedback(.impact(weight: .light), trigger: tapCounter)` (UI-SPEC §2.1 — pitfall: trigger привязывается к локальному `@State tapCounter`, НЕ к ConnectionState).
  </action>
  <verify>
    <automated>swift build --package-path BBTB/Packages/DesignSystem 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    `swift build` SUCCEEDED. `grep -c "struct PrimaryButtonStyle: ButtonStyle" BBTB/Packages/DesignSystem/Sources/DesignSystem/ButtonStyles.swift` = 1. `grep -c "struct SecondaryButtonStyle: ButtonStyle" BBTB/Packages/DesignSystem/Sources/DesignSystem/ButtonStyles.swift` = 1. `grep -c "accessibilityReduceMotion" BBTB/Packages/DesignSystem/Sources/DesignSystem/ButtonStyles.swift` ≥ 2 (по одному на каждый style). `grep -c "DS.Color.accent\|DS.Color.textPrimary\|DS.Color.textInverse\|DS.Typography.labelButton\|DS.Spacing.lg" BBTB/Packages/DesignSystem/Sources/DesignSystem/ButtonStyles.swift` ≥ 6 (все 6 DS.* refs). Никаких хардкодов: `grep -E "\\.font\\(\\.(system|custom)|\\.padding\\([0-9]" BBTB/Packages/DesignSystem/Sources/DesignSystem/ButtonStyles.swift` = 0.
  </done>
</task>

<task type="auto">
  <name>Task 4: Extend Package.swift — swift-snapshot-testing dep (≥1.18.3) + DesignSystemTests + DesignSystemSnapshotTests targets</name>
  <files>BBTB/Packages/DesignSystem/Package.swift, BBTB/Packages/DesignSystem/Tests/DesignSystemSnapshotTests/__Snapshots__/.gitkeep</files>
  <read_first>
    - BBTB/Packages/DesignSystem/Package.swift (текущий 9-line manifest)
    - BBTB/Packages/AppFeatures/Package.swift (lines 1-100 — closest analog: deps + testTarget pattern)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-RESEARCH.md §2.4 (lines 397-475 — swift-snapshot-testing 1.18.3+ rationale + Package.swift sketch + sample test) + §6.5 Wave 0 Gaps (lines 776-784)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-PATTERNS.md §MOD-8 (lines 643-700)
  </read_first>
  <action>
    Перезаписать `BBTB/Packages/DesignSystem/Package.swift` per RESEARCH §2.4 sketch + добавить два test target'а:

    1. Сохранить `// swift-tools-version: 6.0` + `import PackageDescription`.
    2. `let package = Package(name: "DesignSystem", platforms: [.iOS(.v18), .macOS(.v15)], products: [.library(name: "DesignSystem", targets: ["DesignSystem"])], dependencies: [...], targets: [...])`.
    3. `dependencies:` массив: `.package(url: "https://github.com/pointfreeco/swift-snapshot-testing", from: "1.18.3")` — **первая remote-dep в DesignSystem package**. Pin `1.18.3` минимум per Risk #6 (main-thread deadlock fix). Inline comment: `// Phase 12 / DS-15 — pin 1.18.3+ per RESEARCH Risk #6 (main-thread deadlock fix in 1.18.0).`.
    4. `targets:` массив с тремя элементами:
       - `.target(name: "DesignSystem")` (без изменений — DSColor/ButtonStyles/Typography pickup автоматический через source-glob).
       - `.testTarget(name: "DesignSystemTests", dependencies: ["DesignSystem"])` — unit tests для DSColor + DSTokens (Task 1 + Task 2).
       - `.testTarget(name: "DesignSystemSnapshotTests", dependencies: ["DesignSystem", .product(name: "SnapshotTesting", package: "swift-snapshot-testing")])` — snapshot infra (DS-15).
    5. Inline-комментарий per target: `// Phase 12 / DS-15 — unit token assertions.` и `// Phase 12 / DS-15 — pixel-perfect image snapshots (baselines под __Snapshots__/).`
    6. Создать пустой `BBTB/Packages/DesignSystem/Tests/DesignSystemSnapshotTests/__Snapshots__/.gitkeep` чтобы директория для baseline PNG-файлов попала в Git заранее (per RESEARCH §6.5 baseline storage policy).
  </action>
  <verify>
    <automated>swift package --package-path BBTB/Packages/DesignSystem resolve 2>&amp;1 | tail -20 &amp;&amp; swift build --package-path BBTB/Packages/DesignSystem 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    `swift package resolve` успешно загружает `swift-snapshot-testing` (`Package.resolved` обновлён с pin ≥1.18.3). `swift build` SUCCEEDED. `grep -c "swift-snapshot-testing" BBTB/Packages/DesignSystem/Package.swift` ≥ 1. `grep -c "testTarget" BBTB/Packages/DesignSystem/Package.swift` = 2. `test -d BBTB/Packages/DesignSystem/Tests/DesignSystemSnapshotTests/__Snapshots__ &amp;&amp; echo OK`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: ButtonStyle baseline snapshot tests — proof-of-infrastructure для DS-15 + record initial baselines</name>
  <files>BBTB/Packages/DesignSystem/Tests/DesignSystemSnapshotTests/ButtonStyleSnapshotTests.swift</files>
  <read_first>
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-RESEARCH.md §2.4 (sample test, lines 446-472) + §6.4 (snapshot patterns, lines 738-774) + §6.6 (precision table, lines 786-794)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-UI-SPEC.md §5 (Visual Acceptance Criteria, lines 175-198)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-PATTERNS.md §«NEW · DesignSystemSnapshotTests» (lines 562-637 + test file header convention §5 lines 765-787)
    - BBTB/Packages/DesignSystem/Sources/DesignSystem/ButtonStyles.swift (Task 3 — то что тестируем)
  </read_first>
  <behavior>
    - Test 1: `testPrimaryButton_default_dark` — Button("Подключить") + PrimaryButtonStyle, frame 300×56, preferredColorScheme(.dark) — baseline записан + соответствует на повторных запусках (precision 1.0, perceptualPrecision 0.98 per UI-SPEC §5).
    - Test 2: `testSecondaryButton_default_dark` — Button("Отмена") + SecondaryButtonStyle, frame 300×56, .dark — baseline записан.
    - Test 3: `testPrimaryButton_default_light` — то же что Test 1 но .light (DS-10 + D-05 wire-only Light verification).
    - **Note:** Baselines записываются один раз при первой пробежке (`isRecording = true`), коммитятся, потом проверяются (`isRecording = false`). Plan 12-02 расширит этот test target тестами на ConnectionButton / BBTBSpinner / ServerRow / AutoCell.
  </behavior>
  <action>
    Создать `BBTB/Packages/DesignSystem/Tests/DesignSystemSnapshotTests/ButtonStyleSnapshotTests.swift` per PATTERNS §«NEW · DesignSystemSnapshotTests» (snapshot infra sketch verbatim) + RESEARCH §2.4 sample test:

    1. File header per PATTERNS §«Shared Patterns 5» canonical example:
       ```
       // ButtonStyleSnapshotTests.swift — Phase 12 / Plan 12-01 / Task 5 / DS-10 / DS-15.
       //
       // Snapshot baseline для PrimaryButtonStyle + SecondaryButtonStyle (M7 prereq).
       // Это proof-of-infrastructure для DS-15: убеждаемся что swift-snapshot-testing 1.18.3+
       // корректно работает в DesignSystem package перед расширением test corpus в Plan 12-02.
       //
       // Подход: `.image(layout: .fixed(width:height:))` + perceptualPrecision per UI-SPEC §5
       // (0.98 для text+AA; 1.0 для solid fills — gradient strokes тут не задействованы).
       //
       // Что НЕ тестируется здесь:
       // - Reduce-Motion fallback (UI-SPEC §3.8) — static snapshot не различает default vs reduce-motion
       //   когда `isPressed = false`; динамическое поведение тестируется UAT'ом.
       // - Pressed-state — отдельный тест в Plan 12-02 если потребуется (Phase 12 D-04 tight scope).
       ```
    2. `import XCTest`, `import SwiftUI`, `import SnapshotTesting`, `@testable import DesignSystem`.
    3. `@MainActor` `final class ButtonStyleSnapshotTests: XCTestCase` — `@MainActor` обязателен per RESEARCH §6.1 (ImageRenderer is MainActor).
    4. `override func setUp() { super.setUp(); /* isRecording = true */ }` — раскомментировать **только** при первой пробежке для записи baseline, после — закомментировать (PATTERNS §«Snapshot infrastructure»).
    5. Три test method'а: `testPrimaryButton_default_dark`, `testSecondaryButton_default_dark`, `testPrimaryButton_default_light`. Каждый: `let view = Button("Подключить", action: {}).buttonStyle(PrimaryButtonStyle()).frame(width: 300, height: 56).background(DS.Color.canvas).preferredColorScheme(.dark)` (или .light) + `assertSnapshot(of: view, as: .image(precision: 1.0, perceptualPrecision: 0.98, layout: .fixed(width: 300, height: 56)))`.
    6. Russian inline error messages в `XCTContext`-style — `assertSnapshot` сам печатает diff path при fail, но в `record:`/`testName:` параметрах можно использовать стрингу `"DS-10 / M7 — PrimaryButton Dark default state"` (опционально). Минимум — comment над каждым test method'ом на русском с reference на DS-* + M-#.
    7. **First-run protocol:** В git commit сразу включить baseline PNG'и (под `__Snapshots__/ButtonStyleSnapshotTests/`). Executor должен:
       - Раскомментировать `isRecording = true`, запустить тесты — все три "fail" (baseline записывается).
       - Закомментировать `isRecording = true` обратно, запустить — все три PASS (baseline confirmed).
       - Commit все три PNG (~10-100KB каждый per RESEARCH §6.5).
  </action>
  <verify>
    <automated>xcodebuild test -scheme DesignSystem -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing DesignSystemSnapshotTests/ButtonStyleSnapshotTests 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    Все 3 функции ButtonStyleSnapshotTests PASS (после initial baseline recording + commit). Файлы `BBTB/Packages/DesignSystem/Tests/DesignSystemSnapshotTests/__Snapshots__/ButtonStyleSnapshotTests/testPrimaryButton_default_dark.1.png` (и аналоги для Test 2 + Test 3) существуют в Git (size 10-100KB каждый). `git status BBTB/Packages/DesignSystem/Tests/DesignSystemSnapshotTests/` пусто (всё закоммичено). Full regression gate: `swift test --package-path BBTB/Packages/DesignSystem` зелёный (DSTokensTests + DSColorTests + ButtonStyleSnapshotTests — все targets). AppFeatures swift test полный (`swift test --package-path BBTB/Packages/AppFeatures 2>&amp;1 | tail -20`) — green, ~207 тестов preserve. iOS + macOS xcodebuild SUCCEEDED через Tuist (`tuist generate &amp;&amp; xcodebuild build -workspace BBTB.xcworkspace -scheme BBTB -destination 'platform=iOS Simulator,name=iPhone 16'` + same для macOS destination).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Plan 12-01 — pure-visual foundation. Никаких новых trust boundaries:
- Никаких новых API endpoints (DesignSystem package — статические токены).
- Никаких новых external inputs (только Swift literal hex constants).
- Никаких изменений authn/authz/network layers.
- Никаких новых third-party runtime dependencies (swift-snapshot-testing — **test-only**, не shipping; не попадает в App bundle).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | swift-snapshot-testing dep | accept | Test-only dep, не входит в shipping bundle (verified через Package.swift `testTarget` only). Pinned `from: "1.18.3"` minimum. SPM signature verification handled by Apple toolchain. |
| T-12-01-02 | Information Disclosure | DSColor hex constants | accept | Brand colors не secret (опубликованы в Figma file + figma-tokens.json + CODE-CONNECT.md уже). Никаких токенов авторизации / ключей. |
| T-12-01-03 | Denial of Service | Snapshot baseline PNG'и в Git | accept | ~10-100KB per snapshot per RESEARCH §6.5; 3 baseline в Plan 12-01 = ≤300KB. Phase 12 total ≤30 snapshots = ≤3MB. Manageable repo size. |
| T-12-01-04 | Repudiation | accessibility a11y regression (DS.Typography меняет fontWidth для 95 call-sites) | mitigate | Task 1 сохраняет имена deprecated aliases + Plan 12-02 manual UAT по 7 экранам включает VoiceOver round-trip; UI-SPEC §3.4 Dynamic Type explicit non-support fixated. Reduce-Motion fallback gate в Task 3 (ButtonStyles) + Plan 12-02 BBTBSpinner. |

**ASVS L1 compliance:** Phase 12 не вводит новый user-input, auth, sessions, crypto. Все требования L1 — N/A для этой фазы (token-extension + test infrastructure). Существующие R1-R21 invariants Phase 1+ preserved (нет изменений в PacketTunnelKit / sing-box / network code).
</threat_model>

<verification>
**Plan 12-01 verification gates:**

1. **Unit tests green:** `swift test --package-path BBTB/Packages/DesignSystem` зелёный (DSTokensTests + DSColorTests + ButtonStyleSnapshotTests).
2. **Existing regression green:** `swift test --package-path BBTB/Packages/AppFeatures 2>&1 | tail -20` — все ~207 тестов AppFeatures (ConnectionButtonTests + ServerListSheetHeightTests + других) PASS. Numeric token update (140→280) не ломает helper-only тесты потому что они тестируют `isConnecting` boolean, не diameter.
3. **iOS xcodebuild green:** `tuist generate && xcodebuild build -workspace BBTB.xcworkspace -scheme BBTB -destination 'platform=iOS Simulator,name=iPhone 16'` SUCCEEDED.
4. **macOS xcodebuild green:** `xcodebuild build -workspace BBTB.xcworkspace -scheme BBTB-macOS -destination 'platform=macOS,arch=arm64'` SUCCEEDED (ad-hoc signing).
5. **R1/R6 invariants preserved:** `bash scripts/validate-r1-r6.sh` PASS (Phase 12 не трогает sing-box код).
6. **Deprecated alias warning:** в build log от AppFeatures должно быть несколько `'accent' is deprecated: renamed to 'DS.Color.accent'` warnings (proof что migration aliases работают). Эти warnings очищаются в Plan 12-02 при wiring call-sites — здесь они допустимы.
7. **Token hex bound check:** `swift test --filter DSColorTests` доказывает что accent Dark = sRGB(0x14,0x66,0x4B); все остальные 14 tokens compile-only + non-empty.

**No manual UAT в Plan 12-01** — это pure foundation. Manual UAT по 7 экранам — finale Plan 12-02.
</verification>

<success_criteria>
- [ ] **DS-01** — `DS.Color` enum с 15 семантическими токенами существует и тестируется (`DSColorTests` PASS).
- [ ] **DS-02** — `DS.Typography.Size` enum с 7 CGFloat constants существует (`DSTokensTests.test_typographySize_sevenConstants` PASS).
- [ ] **DS-03** — `DS.Radius.section = 24` + `DS.Radius.sheet = 32` (`DSTokensTests.test_radius_sectionAndSheet` PASS).
- [ ] **DS-04** — `DS.Blur.pill = 4` (`DSTokensTests.test_blur_pill` PASS).
- [ ] **DS-05** — `DS.ConnectionButtonSize.compactDiameter = 280; regularDiameter = 320; compactIcon = 112; regularIcon = 128` (`DSTokensTests.test_connectionButtonSize_phase12Values` PASS).
- [ ] **DS-06** — `DS.Typography.expanded(_:weight:)` helper + 9 sized presets + 6 deprecated aliases (display/title/body/callout/subheadline/caption) внутри возвращают expanded() Font (`DSTokensTests.test_typographyPresets_compileCheck` PASS).
- [ ] **DS-07** — `DS.Color.accent = #14664B` Dark + Light (`DSColorTests.test_accent_darkHexMatchesFigma` + `test_accent_lightHexMatchesFigma` PASS); `DS.accent` остаётся deprecated alias.
- [ ] **DS-10** — `PrimaryButtonStyle` + `SecondaryButtonStyle` compile + render baseline (`ButtonStyleSnapshotTests` 3 функции PASS) + Reduce-Motion gate присутствует (`grep accessibilityReduceMotion ButtonStyles.swift` ≥ 2).
- [ ] **DS-15 (infrastructure)** — `swift-snapshot-testing` ≥ 1.18.3 в Package.swift + `DesignSystemTests` + `DesignSystemSnapshotTests` targets компилируются + baseline записаны и закоммичены.
- [ ] **Regression** — AppFeatures `swift test` все ~207 тестов зелёные (deprecated aliases preserve backward compat).
- [ ] **Build** — iOS + macOS xcodebuild SUCCEEDED.
- [ ] **R1/R6 invariants** — `validate-r1-r6.sh` PASS.
</success_criteria>

<output>
After completion, create `.planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-01-SUMMARY.md` documenting:
- Files created/modified (8 files: 4 source + 1 manifest + 3 test).
- Tests added (DSColorTests, DSTokensTests, ButtonStyleSnapshotTests) — total ≥ 11 functions.
- Baseline PNGs committed (3 baselines под `__Snapshots__/ButtonStyleSnapshotTests/`).
- Deprecated aliases preserved (`DS.accent` → `DS.Color.accent`, 6 Typography aliases through expanded()).
- ConnectionButtonSize numerics updated (140→280, 160→320, 56→112, 64→128).
- swift-snapshot-testing 1.18.3 pinned in Package.resolved.
- Foundation ready for Plan 12-02 application layer.
</output>
