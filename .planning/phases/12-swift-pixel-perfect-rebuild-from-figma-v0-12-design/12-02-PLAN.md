---
phase: 12-swift-pixel-perfect-rebuild-from-figma-v0-12-design
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - BBTB/Packages/DesignSystem/Sources/DesignSystem/Spinner.swift
  - BBTB/Packages/AppFeatures/Package.swift
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift
  - BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerRow.swift
  - BBTB/Packages/AppFeatures/Sources/ServerListFeature/AutoCell.swift
  - BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift
  - BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/Snapshots/ConnectionButtonSnapshotTests.swift
  - BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/Snapshots/OnboardingViewSnapshotTests.swift
  - BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/Snapshots/__Snapshots__/.gitkeep
  - BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/Snapshots/ServerListSnapshotTests.swift
  - BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/Snapshots/ServerRowFixtures.swift
  - BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/Snapshots/__Snapshots__/.gitkeep
  - BBTB/Packages/DesignSystem/Tests/DesignSystemSnapshotTests/SpinnerSnapshotTests.swift
autonomous: false
requirements: [DS-08, DS-09, DS-11, DS-12, DS-13, DS-14, DS-15, UX-09]
tags: [design-system, ios, swiftui, snapshot-testing, application, ui-checkpoint]

user_setup: []

must_haves:
  truths:
    - "ConnectionButton при `state == .connecting` рендерит видимую power-icon (opacity 1) и кольцо BBTBSpinner снаружи Circle (diameter+24) — power-icon больше не скрыт через .opacity(isConnecting ? 0 : 1)."
    - "BBTBSpinner монтируется в ConnectionButton через `.overlay { if isConnecting { ... } }` на самом `Circle()` (W3 fix — НЕ sibling-in-ZStack), чтобы родительский HomeView VStack/HStack frame не пересчитывался при isConnecting toggle. Acceptance criterion: визуально нет «прыжков» соседних view'ов (timer label, server name) при переключении .idle ↔ .connecting ↔ .connected на iPhone 16 simulator."
    - "ConnectionButton.fillColor возвращает `DS.Color.controlIdle` для .empty/.idle/.connecting, `DS.Color.accent` для .connected, `DS.Color.error` для .error — никаких inline `.gray/.orange/.accentColor/Color.red`."
    - "`ConnectionButton.fillColor` имеет `internal` access level (НЕ private) для regression-тестирования из `ConnectionButtonTests` (Phase 11 D-05 / Plan 11-07 Task 7.1 Alternative A pattern), с doc-comment `/// internal for ConnectionButtonTests fillColor assertions, see Phase 11 D-05` — W2 fix."
    - "BBTBSpinner — public SwiftUI struct в DesignSystem с Circle.trim(from:0,to:0.85) + AngularGradient stroke + .rotationEffect анимированный 1.2с linear repeatForever (default motion path). Reduce-Motion fallback — **статичное кольцо с pulsating opacity 0.6↔1.0, цикл 1.0с** (W4 lock — discrete-snap альтернатива из UI-SPEC §3.8 удалена в revision iteration 1; executor НЕ выбирает между вариантами)."
    - "OnboardingView рендерит hero text split: `Text(\"Интернет, каким он \").foregroundStyle(DS.Color.textPrimary) + Text(\"должен быть\").foregroundStyle(DS.Color.accent)` с `DS.Typography.expanded(DS.Typography.Size.display, weight: .semibold)` font (**48pt Semibold per RESEARCH §12 Q1 RESOLVED — B2 lock**, НЕ `titleScreen=16`); CTA Paste = `PrimaryButtonStyle`, CTA QR = `SecondaryButtonStyle`."
    - "OnboardingView оба CTA вызывают `.sensoryFeedback(.impact(weight: .light), trigger: tapCounter)` где `tapCounter` — локальный `@State Int` (НЕ привязан к ConnectionState, per UI-SPEC §2.1 Pitfall 6)."
    - "ServerRow использует `DS.Color.textPrimary/.textSecondary/.iconMuted` (а не SwiftUI semantic `.primary/.secondary/.tertiary`); checkmark fill = `DS.Color.accent` (а не `Color.accentColor`); подсвеченная строка имеет background `DS.Color.accent` при `isSelected = true`."
    - "AutoCell использует `RoundedRectangle(cornerRadius: DS.Radius.section)` (24pt вместо 16pt cardLarge); selected fill = `DS.Color.accent` / unselected fill = `DS.Color.surfaceSunken` (не secondary.opacity(0.10))."
    - "ServerListSheet `sheetContent` обёрнут `.background(DS.Color.surface).clipShape(UnevenRoundedRectangle(topLeadingRadius: DS.Radius.sheet, bottomLeadingRadius: 0, bottomTrailingRadius: 0, topTrailingRadius: DS.Radius.sheet, style: .continuous))` — 32pt только верхние углы."
    - "Все существующие `accessibilityIdentifier` строки preserved: `BBTB.ConnectionButton`, `BBTB.Onboarding.PasteButton`, `BBTB.Onboarding.QRButton`, `BBTB.ServerListSheet`, `BBTB.ServerListSheet.AutoCell`, `BBTB.ServerListSheet.ServerRow.<UUID>`, `BBTB.ServerListSheet.ServerRow.Detail.<UUID>` — UAT регистрирует их через xcuitest."
    - "Snapshot тесты ≥ 11 функций PASS в Plan 12-02 (revision iteration 1): ConnectionButton 5 состояния (.idle compact, .connecting, .connected(since:), .error(message:), **.idle regular W1**) + BBTBSpinner 1 frozen frame (от Task 6) + ServerRow 2 (default+selected) + AutoCell 2 (default+selected) + **test_serverListSheet_corners_dark W5** + **test_onboardingView_hero_dark W5+B2**. perceptualPrecision per UI-SPEC §5 (0.97 gradient, 0.98 text, 1.0 solid). Phase 12 total = 15 baselines (3 от 12-01 ButtonStyle + 12 от 12-02)."
    - "Existing ~207 AppFeatures тестов PASS (regression preserve): `ConnectionButtonTests.isConnecting` 5 функций + `ServerListSheetHeightTests` 4 функции + 198 других — никаких регрессий."
    - "Manual UAT 7 экранов отмечен PASS пользователем в `12-UAT.md` (D-08 hybrid acceptance — финальный checkpoint фазы 12)."
  artifacts:
    - path: "BBTB/Packages/DesignSystem/Sources/DesignSystem/Spinner.swift"
      provides: "BBTBSpinner — public rotating ring view с AngularGradient stroke + Reduce-Motion fallback"
      exports: ["BBTBSpinner"]
      contains: "Circle().trim(from: 0, to: 0.85)"
    - path: "BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift"
      provides: "ConnectionButton с DS.Color tokens (M3) + BBTBSpinner ring (M6) + power-icon visible (no opacity hide)"
      contains: "BBTBSpinner(diameter: diameter + 24"
    - path: "BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift"
      provides: "OnboardingView rebuilt: hero text split + PrimaryButtonStyle/SecondaryButtonStyle CTA + .sensoryFeedback haptic"
      contains: "PrimaryButtonStyle"
    - path: "BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerRow.swift"
      provides: "ServerRow с DS.Color tokens + selected background accent + Reduce-Motion gate на selected animation"
      contains: "DS.Color.accent"
    - path: "BBTB/Packages/AppFeatures/Sources/ServerListFeature/AutoCell.swift"
      provides: "AutoCell с DS.Radius.section (24pt) + DS.Color.accent/.surfaceSunken fill"
      contains: "DS.Radius.section"
    - path: "BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift"
      provides: "ServerListSheet с UnevenRoundedRectangle 32pt top corners"
      contains: "UnevenRoundedRectangle"
    - path: "BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/Snapshots/ConnectionButtonSnapshotTests.swift"
      provides: "Snapshot tests для ConnectionButton 4 states + BBTBSpinner integration (DS-08, DS-09)"
    - path: "BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/Snapshots/ServerListSnapshotTests.swift"
      provides: "Snapshot tests для ServerRow + AutoCell + test_serverListSheet_corners_dark (W5 fix, DS-12, DS-13, DS-14)"
    - path: "BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/Snapshots/ServerRowFixtures.swift"
      provides: "Deterministic ServerConfig fixture для ServerListSnapshotTests (N2 fix — `.preview` static не существует в VPNCore.ServerConfig)"
      exports: ["ServerRowFixtures.sample"]
    - path: "BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/Snapshots/OnboardingViewSnapshotTests.swift"
      provides: "test_onboardingView_hero_dark — full-screen baseline для M7 visual lock + 48pt hero text size assertion (W5+B2 fix, DS-11)"
    - path: "BBTB/Packages/DesignSystem/Tests/DesignSystemSnapshotTests/SpinnerSnapshotTests.swift"
      provides: "Frozen-frame snapshot BBTBSpinner для DS-08 baseline"
  key_links:
    - from: "ConnectionButton.body ZStack"
      to: "BBTBSpinner"
      via: "if isConnecting { BBTBSpinner(diameter: diameter + 24, ...) } снаружи Circle"
      pattern: "BBTBSpinner\\(diameter:.*\\+ 24"
    - from: "ConnectionButton.fillColor switch"
      to: "DS.Color tokens"
      via: "switch state → controlIdle/accent/error"
      pattern: "DS\\.Color\\.(controlIdle|accent|error)"
    - from: "OnboardingView CTA"
      to: "PrimaryButtonStyle / SecondaryButtonStyle"
      via: ".buttonStyle(PrimaryButtonStyle()) / SecondaryButtonStyle()"
      pattern: "buttonStyle\\((Primary|Secondary)ButtonStyle\\(\\)\\)"
    - from: "OnboardingView CTA tap"
      to: ".sensoryFeedback haptic"
      via: "@State private var tapCounter + .sensoryFeedback(.impact(weight: .light), trigger: tapCounter)"
      pattern: "sensoryFeedback\\(\\.impact"
    - from: "ServerListSheet sheetContent"
      to: "UnevenRoundedRectangle clipShape"
      via: ".background(DS.Color.surface).clipShape(UnevenRoundedRectangle(topLeadingRadius:...))"
      pattern: "UnevenRoundedRectangle\\(topLeadingRadius:"
    - from: "AutoCell pill background"
      to: "DS.Radius.section + DS.Color.accent/.surfaceSunken"
      via: "RoundedRectangle(cornerRadius: DS.Radius.section).fill(isSelected ? DS.Color.accent : DS.Color.surfaceSunken)"
      pattern: "RoundedRectangle\\(cornerRadius: DS\\.Radius\\.section\\)"
---

<objective>
Plan 12-02 (Application) применяет foundation Plan 12-01 ко всем 6 целевым SwiftUI view'ам и закрывает 10 mismatches M1-M10 (M1+M2 уже applied через numeric token update в 12-01 — иконка/диаметр читают новые значения автоматически; здесь добавляем колор-токены, BBTBSpinner, OnboardingView rebuild, ServerRow/AutoCell/ServerListSheet token alignment).

**Quick wins → heavy lifts ordering (per D-03):**
1. ConnectionButton fillColor switch на DS.Color (M3 — мелкий surgical fix, low risk) — Task 1
2. ServerListSheet UnevenRoundedRectangle (M9 — small targeted change, требует Wave 1 visual verify по Risk #2) — Task 2
3. AutoCell pill design + Radius.section + accent fill (M10 + M8-pill — small surgical) — Task 3
4. ServerRow tokens align + selected background (M8 — small surgical) — Task 4
5. **Wave-1 manual visual checkpoint** (executor сверяет M3+M9+M10+M8 против Figma final-*.png — gating decision до запуска heavy lifts) — Task 5
6. BBTBSpinner создать + integrate в ConnectionButton (M6 — heavy, requires animation tuning) — Task 6
7. OnboardingView rebuild (M7 — heaviest: hero text split + ButtonStyle + haptic) — Task 7
8. Snapshot test corpus для всех 4 компонентов (DS-15 component portion — ≥ 8 snapshot функций) — Task 8
9. **Phase closure manual UAT** — full 7-screen comparison против Figma — Task 9

Purpose: Pixel-perfect rebuild от existing placeholder UI к Figma BBTB v3 visual fidelity, **с сохранением ~207 регрессий + всех accessibilityIdentifier**. Snapshot infrastructure ловит regressions автоматически.

Output: 6 modified views + 1 new BBTBSpinner + ≥ 10 baseline snapshot PNG'ов + UX-09 ✓ Validated после manual UAT signoff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-CONTEXT.md
@.planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-RESEARCH.md
@.planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-PATTERNS.md
@.planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-UI-SPEC.md
@.planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-01-PLAN.md
@BBTB/Packages/DesignSystem/Tokens/CODE-CONNECT.md
@BBTB/Packages/DesignSystem/Sources/DesignSystem/DesignSystem.swift
@BBTB/Packages/DesignSystem/Sources/DesignSystem/DSColor.swift
@BBTB/Packages/DesignSystem/Sources/DesignSystem/ButtonStyles.swift
@BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift
@BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift
@BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerRow.swift
@BBTB/Packages/AppFeatures/Sources/ServerListFeature/AutoCell.swift
@BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift

<figma_references>
**Reference screenshots для visual comparison (executor открывает рядом с симулятором):**
- `.planning/phases/11-onboarding-ux-polish/figma-inspect/final-01-onboarding.png` — Onboarding (M7 / DS-11)
- `.planning/phases/11-onboarding-ux-polish/figma-inspect/02-home-disconnected.png` — Home idle (DS-09 .idle)
- `.planning/phases/11-onboarding-ux-polish/figma-inspect/03-home-connecting.png` — Home connecting (DS-08 + DS-09 .connecting + spinner placement)
- `.planning/phases/11-onboarding-ux-polish/figma-inspect/04-home-error.png` — Home error (DS-09 .error)
- `.planning/phases/11-onboarding-ux-polish/figma-inspect/05-home-connected.png` — Home connected (DS-09 .connected)
- `.planning/phases/11-onboarding-ux-polish/figma-inspect/06-servers-selected.png` — Servers selected (DS-12 + DS-13 + DS-14)
- `.planning/phases/11-onboarding-ux-polish/figma-inspect/07-servers-auto.png` — Servers auto (DS-13 auto-cell selected)

**Figma file (executor открывает в браузере для inspect):**
- `https://www.figma.com/design/tI6DFQDU6PdOSmd19BGXqg/BBTB-v3` — узлы из CODE-CONNECT.md §1.x (3054:712, 3057:167, 3071:219, 3071:227, 3064:1316, 3062:304, и т.д.)
</figma_references>

<interfaces>
<!-- Foundation Plan 12-01 даёт. Эти токены/стили читаются здесь. -->

From DesignSystem (после Plan 12-01):
- DS.Color.canvas / surface / surfaceSunken / surfaceHeader / divider / controlIdle / accent / error / textPrimary / textSecondary / textTertiary / textInverse / iconPrimary / iconSecondary / iconMuted
- DS.Typography.expanded(_:weight:) helper + 9 presets (displayTimer / titleScreen / titleSection / titleUppercase / labelButton / bodyDefault / bodyCaption / bodyMicro / tipsLight) + 6 deprecated aliases (display/title/body/callout/subheadline/caption) — все возвращают expanded Font.
- DS.Typography.Size (display=48, title=16, labelButton=14, body=12, tips=10, caption=9, micro=8)
- DS.Radius.section (24) / DS.Radius.sheet (32) + existing small/card/cardLarge/button
- DS.Blur.pill (4)
- DS.ConnectionButtonSize.compactDiameter (280) / regularDiameter (320) / compactIcon (112) / regularIcon (128) — `ConnectionButton.swift` уже читает эти константы без правки sourcecode
- DS.accent — deprecated alias на DS.Color.accent (call-sites постепенно мигрируют здесь)
- PrimaryButtonStyle / SecondaryButtonStyle — public structs ButtonStyle с Capsule pill + pressed scaleEffect 0.97 / opacity 0.92 (default motion path) + Reduce-Motion fallback
- swift-snapshot-testing 1.18.3+ через AppFeatures.Package.swift dep (Task будет добавлен ниже)

From existing AppFeatures (preserve):
- `public struct ConnectionButton: View { public let state: ConnectionState; public let action: () -> Void }`
- `internal var isConnecting: Bool` (5 existing tests reference)
- `public struct OnboardingView: View` с `@ObservedObject viewModel: MainScreenViewModel + onPaste + onScanQR + onDismiss`
- `public struct ServerRow: View` с `server: ServerConfig, isSelected: Bool, pingState: PingState, onTap/onDelete/onDetailTap`
- `public struct AutoCell: View` с `isSelected: Bool, onTap: () -> Void`
- `private var sheetContent: some View` в ServerListSheet (lines ~136-208 — NavigationStack { VStack(spacing: 0) {...} })
- 7 height-tuning static helpers (estimatedHeight, computeDetents) — preserve untouched (ServerListSheetHeightTests reference)
- All `BBTB.*` accessibilityIdentifier strings — preserve verbatim

From RESEARCH §4.5 ConnectionButton target body (lines 615-650):
```
ZStack {
  Circle().fill(fillColor).frame(width: diameter, height: diameter)
  Image(systemName: "power").font(.system(size: iconSize, weight: .medium))
    .foregroundStyle(DS.Color.textPrimary)
    .symbolEffect(.bounce, value: state)
    .opacity(1)  // Phase 12: видим даже в .connecting
  if isConnecting {
    BBTBSpinner(diameter: diameter + 24, lineWidth: 6, speed: 1.2)
      .accessibilityHidden(true)
  }
}
```

From RESEARCH §4.7 Onboarding hero text split:
```
(Text("Интернет, каким он ").foregroundStyle(DS.Color.textPrimary)
 + Text("должен быть").foregroundStyle(DS.Color.accent))
  .font(DS.Typography.titleScreen)
  .multilineTextAlignment(.center)
```

From RESEARCH §2.6 sheet rounded top corners (lines 547-563):
```
.background(DS.Color.surface)
.clipShape(UnevenRoundedRectangle(
  topLeadingRadius: DS.Radius.sheet,
  bottomLeadingRadius: 0,
  bottomTrailingRadius: 0,
  topTrailingRadius: DS.Radius.sheet,
  style: .continuous))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConnectionButton.fillColor switch на DS.Color tokens (M3 / DS-09) — quick win, mechanical swap</name>
  <files>BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift</files>
  <read_first>
    - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift (целиком, 92 lines — для понимания структуры fillColor switch lines 76-84)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-RESEARCH.md §4.5 (lines 615-650 — target body) + §3 M3 allocation
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-PATTERNS.md §MOD-3 (lines 253-321)
    - BBTB/Packages/DesignSystem/Tokens/CODE-CONNECT.md §1.1 (lines 21-56 — visual fill values)
    - BBTB/Packages/DesignSystem/Sources/DesignSystem/DSColor.swift (Plan 12-01 Task 2 output — verify DS.Color.controlIdle/.accent/.error available)
  </read_first>
  <behavior>
    - Test 1: При `state = .idle` `fillColor` resolved Dark = sRGB(0x22/255, 0x22/255, 0x22/255) — DS.Color.controlIdle Dark.
    - Test 2: При `state = .connecting` `fillColor` resolved Dark = sRGB(0x22/255, 0x22/255, 0x22/255) — controlIdle (Figma: connecting variant = idle fill + spinner ring AROUND, не оранжевый).
    - Test 3: При `state = .connected(since:)` `fillColor` resolved Dark = sRGB(0x14/255, 0x66/255, 0x4B/255) — DS.Color.accent.
    - Test 4: При `state = .error(message:)` `fillColor` resolved Dark = sRGB(0x66/255, 0x14/255, 0x14/255) — DS.Color.error.
    - Test 5: `isConnecting` boolean preserved (5 existing ConnectionButtonTests функций PASS) — `isConnecting = true` только при `.connecting`.
    - **NB:** Этот task НЕ trogает spinner / opacity / power-icon (это Task 6). Здесь только fillColor swap.
  </behavior>
  <action>
    Замена `fillColor` switch в `ConnectionButton.swift` lines 76-84 на DS.Color-based per RESEARCH §4.5:

    1. Сохранить весь body / isConnecting / diameter / iconSize / disabled — НЕ трогать.
    2. Заменить `private var fillColor: Color` на:
       ```
       private var fillColor: SwiftUI.Color {
           switch state {
           case .empty, .idle:  return DS.Color.controlIdle
           case .connecting:    return DS.Color.controlIdle  // Figma .connecting = idle fill + spinner ring AROUND (Task 6)
           case .connected:     return DS.Color.accent
           case .error:         return DS.Color.error
           }
       }
       ```
    3. Imports — добавить ничего (DesignSystem уже import'нут lines 2).
    4. Доc-comment над `fillColor`: `/// Phase 12 / DS-09 / M3 — fill switch на DS.Color семантические токены. См. CODE-CONNECT.md §1.1.`
    5. **CRITICAL preserve verbatim** (не трогать):
       - `accessibilityIdentifier("BBTB.ConnectionButton")` line 46 — ~207 тестов референсят.
       - `internal var isConnecting: Bool` lines 52-55 — `ConnectionButtonTests` PASS gate (5 функций).
       - `Image(systemName: "power").foregroundStyle(.white).symbolEffect(.bounce, value: state)` lines 23-26 — НЕ менять foregroundStyle сейчас (Task 6 решит). НЕ трогать `.opacity(isConnecting ? 0 : 1)` сейчас (Task 6 решит — RESEARCH §2.1 spinner-placement).
       - `ProgressView()` placeholder lines 32-41 — НЕ трогать (Task 6 заменит на BBTBSpinner).

    Добавить новый тестовый метод в `ConnectionButtonTests.swift` (existing test file, 5 функций) — **W2 fix:**
    - **Access-level migration:** `fillColor` менять с `private var` на `internal var fillColor: Color` с doc-comment `/// Phase 11 D-05 pattern (Plan 11-07 Task 7.1 Alternative A) — internal для @testable access из ConnectionButtonTests fillColor assertions. Совпадает с существующим internal `isConnecting`.` (precedent: Phase 11 уже сделал так же для `isConnecting`).
    - `test_fillColor_idleReturnsControlIdle` / `test_fillColor_connectedReturnsAccent` / `test_fillColor_errorReturnsError` — 3 функции через `@testable import MainScreenFeature` (как в существующих `ConnectionButtonTests`).
    - **Explicit MainActor setUp + UITraitCollection** (W2 fix — было неявное traits-resolution): класс `ConnectionButtonTests` уже `@MainActor` annotated (existing pattern, ConnectionButtonTests.swift line 22). Каждый new test method объявляет explicit traits через `let darkTraits = UITraitCollection(userInterfaceStyle: .dark)` (НЕ `UITraitCollection.current` или другой ambient guess) и сравнивает через `let resolved = UIColor(button.fillColor).resolvedColor(with: darkTraits); XCTAssertEqual(resolved.cgColor.components?[0], 0x14/255.0, accuracy: 0.003, "DS-09 / M3 — .connected должен резолвить DS.Color.accent.red = 0x14/255")`.
    - **Альтернативный path** (если `UIColor(button.fillColor).resolvedColor(with:)` нестабилен на SwiftUI Color в Swift 6 strict-concurrency): сравнение через direct DS.Color compare — `XCTAssertEqual(String(describing: button.fillColor), String(describing: DS.Color.accent), "...")` (semantic equality через debug description). Executor выбирает по `swift test` зелёному результату.
    - Russian assertion messages.
  </action>
  <verify>
    <automated>swift test --package-path BBTB/Packages/AppFeatures --filter ConnectionButtonTests 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    8 функций ConnectionButtonTests PASS (5 existing + 3 new fillColor). `grep -c "DS\\.Color\\." BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift` ≥ 3 (controlIdle + accent + error). `grep -E "\\.gray|\\.orange|\\.accentColor|Color\\.red" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift | grep -v '^//'` пусто (никаких inline colors в продакшен-коде). `accessibilityIdentifier("BBTB.ConnectionButton")` сохранён (grep = 1). iOS xcodebuild SUCCEEDED.
  </done>
</task>

<task type="auto">
  <name>Task 2: ServerListSheet — UnevenRoundedRectangle 32pt top corners (M9 / DS-14) — Wave 1 visual verify per Risk #2</name>
  <files>BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift</files>
  <read_first>
    - BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift (lines 136-210 — sheetContent body; lines 52-58 — height constants preserve list; lines 92-99 — presentationDetents/dragIndicator stack)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-RESEARCH.md §2.6 (lines 539-568 — UnevenRoundedRectangle decision + Risk #2 warning) + §9 Pitfall 7 (lines 860-862 — clipShape vs background ordering)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-PATTERNS.md §MOD-7 (lines 521-559)
    - BBTB/Packages/DesignSystem/Tokens/CODE-CONNECT.md §1.7 + §2.2 (DS.Radius.sheet = 32pt)
  </read_first>
  <action>
    Применить UnevenRoundedRectangle к sheetContent в ServerListSheet.swift:

    1. Найти outermost `VStack(spacing: 0)` внутри `NavigationStack { ... }` (около line 142). Это контейнер всего sheet content (Header + Divider + ScrollView).
    2. После закрывающей `}` этого `VStack` (около line 207) добавить chain:
       ```
       .background(DS.Color.surface)
       .clipShape(
           UnevenRoundedRectangle(
               topLeadingRadius: DS.Radius.sheet,
               bottomLeadingRadius: 0,
               bottomTrailingRadius: 0,
               topTrailingRadius: DS.Radius.sheet,
               style: .continuous
           )
       )
       ```
       **Pitfall 7 (RESEARCH §9):** `.background` ДО `.clipShape` — иначе background рисуется как fill, не clipped.
    3. **CRITICAL preserve** (не трогать):
       - `.presentationDetents(detents)` line 92
       - `.onAppear` / `.onChange` detents drivers lines 93-98 (Phase 6e Wave 2 L7 fix — referenced by ServerListSheetHeightTests)
       - `.presentationDragIndicator(.visible)` line 99
       - `.refreshable { ... }` line 198-200
       - `.navigationDestination(item: $viewModel.openServerDetail)` line 204
       - `.accessibilityIdentifier("BBTB.ServerListSheet")` line 201
       - 7 height-tuning constants (`headerH`, `autoCellH`, ...) lines 52-58 + 4 static helpers (`estimatedHeight`, `computeDetents`) — `ServerListSheetHeightTests` 4 функции PASS gate.
    4. Doc-comment в начало sheetContent блока: `/// Phase 12 / DS-14 / M9 — top corners 32pt (UnevenRoundedRectangle). Risk #2: Wave 1 visual verify на iOS 18 simulator — clipShape поверх .presentationDetents. См. RESEARCH §2.6.`

    **Wave 1 visual verify (executor responsibility):** Запустить iOS Simulator на iPhone 16, открыть ServerListSheet (tap по "Список серверов"), убедиться что верхние углы скруглены 32pt **поверх** drag indicator, не под ним; нижние углы 0. Сравнить с `final-04-servers-selected.png`. Если возник rendering artifact (Risk #2 fallback) — переместить `.background + .clipShape` ВНУТРЬ NavigationStack VStack, **не на уровне** `.presentationDetents` modifier chain.
  </action>
  <verify>
    <automated>swift test --package-path BBTB/Packages/AppFeatures --filter ServerListSheetHeightTests 2>&amp;1 | tail -15 &amp;&amp; grep -c "UnevenRoundedRectangle" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift</automated>
  </verify>
  <done>
    `ServerListSheetHeightTests` 4 функции PASS (height-tuning helpers preserve). `grep -c "UnevenRoundedRectangle(topLeadingRadius: DS.Radius.sheet" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift` = 1. `grep -c "DS\\.Color\\.surface" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift` ≥ 1. `.presentationDetents` + `.presentationDragIndicator` строки preserved (`grep -c "presentationDetents" ... = 1`). iOS xcodebuild SUCCEEDED. Executor подтверждает в commit message visual review verdict: PASS / fallback applied.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AutoCell — DS.Radius.section + DS.Color.accent/surfaceSunken fill + iconPrimary/iconSecondary icon (M10 + M8-pill / DS-13)</name>
  <files>BBTB/Packages/AppFeatures/Sources/ServerListFeature/AutoCell.swift</files>
  <read_first>
    - BBTB/Packages/AppFeatures/Sources/ServerListFeature/AutoCell.swift (целиком, 77 lines)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-RESEARCH.md §3 DS-13 + RESEARCH §2 (Architectural Responsibility Map row для AutoCell)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-PATTERNS.md §MOD-6 (lines 467-518)
    - BBTB/Packages/DesignSystem/Tokens/CODE-CONNECT.md §1.6 (lines 139-160 — AutoCell visual mapping)
    - `.planning/phases/11-onboarding-ux-polish/figma-inspect/07-servers-auto.png` (reference)
  </read_first>
  <behavior>
    - Behavior 1: При `isSelected = true` фон pill = `RoundedRectangle(cornerRadius: 24).fill(DS.Color.accent)`; bolt-icon foregroundStyle = `DS.Color.iconPrimary` (white в Dark mode); inner circle background = `DS.Color.accent.opacity(0.15)` (subtle highlight).
    - Behavior 2: При `isSelected = false` фон pill = `RoundedRectangle(cornerRadius: 24).fill(DS.Color.surfaceSunken)`; bolt-icon foregroundStyle = `DS.Color.iconSecondary`; inner circle background = `DS.Color.surface.opacity(0.5)` (subtle hint).
    - Behavior 3: `.symbolEffect(.bounce, value: isSelected)` сохраняется на bouncyCheckmark + добавляется `.disabled(accessibilityReduceMotion)` per UI-SPEC §2.7 / §3.8.
    - Behavior 4: `accessibilityIdentifier("BBTB.ServerListSheet.AutoCell")` preserved.
  </behavior>
  <action>
    Modifications в `AutoCell.swift` per PATTERNS §MOD-6:

    1. Импорт: добавить `@Environment(\.accessibilityReduceMotion) private var reduceMotion: Bool` к struct properties (после `public let onTap`).
    2. Line 28 (`Image bolt.fill .foregroundStyle(isSelected ? Color.accentColor : .secondary)`) → `.foregroundStyle(isSelected ? DS.Color.iconPrimary : DS.Color.iconSecondary)`.
    3. Lines 30-36 (inner Circle().fill background) → `.background(Circle().fill(isSelected ? DS.Color.accent.opacity(0.25) : DS.Color.surface.opacity(0.5)))`. Точное opacity value (0.25 vs 0.15) — executor подтверждает Wave 1 visual review против Figma (final-07-servers-auto.png).
    4. Lines 37-43 (Text title/subtitle) → `.foregroundStyle(isSelected ? DS.Color.textPrimary : DS.Color.textPrimary)` для title; `.foregroundStyle(isSelected ? DS.Color.textPrimary.opacity(0.8) : DS.Color.textSecondary)` для subtitle. (Title всегда white на тёмном фоне; subtitle меняется per selected.)
    5. Lines 53-55 (outer RoundedRectangle background) → `RoundedRectangle(cornerRadius: DS.Radius.section).fill(isSelected ? DS.Color.accent : DS.Color.surfaceSunken)` — М10 swap cardLarge=16 на section=24 + M3 colors.
    6. Lines 65-75 (bouncyCheckmark @ViewBuilder): добавить `.disabled(reduceMotion)` после `.symbolEffect(.bounce, value: isSelected)` per UI-SPEC §3.8 Reduce-Motion fallback table.
    7. Доc-comment над body: `/// Phase 12 / DS-13 / M8+M10 — pill design: accent fill (selected) / surfaceSunken (unselected), 24pt section radius, iconPrimary/iconSecondary icon color. См. CODE-CONNECT.md §1.6 + RESEARCH §3.`
    8. **CRITICAL preserve**:
       - `accessibilityIdentifier("BBTB.ServerListSheet.AutoCell")` line 58 (renaming = test break).
       - `.accessibilityElement(children: .combine)` line 59 + label/value/hint lines 60-62.
       - `frame(minHeight: 72)` line 51 — D-04 tight scope: НЕ менять (4 height tests references).
       - `buttonStyle(.plain)` line 57.
       - `bouncyCheckmark` ViewBuilder structure (только добавить `.disabled(reduceMotion)`).

    Добавить test метод в `ServerListSnapshotTests.swift` (Task 8) — здесь только source-code change.
  </action>
  <verify>
    <automated>swift test --package-path BBTB/Packages/AppFeatures --filter ServerListFeatureTests 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    `swift test` ServerListFeatureTests все existing функции PASS (regression preserve). `grep -c "DS\\.Radius\\.section" BBTB/Packages/AppFeatures/Sources/ServerListFeature/AutoCell.swift` ≥ 1. `grep -c "DS\\.Color\\.accent\\|DS\\.Color\\.surfaceSunken\\|DS\\.Color\\.iconPrimary\\|DS\\.Color\\.iconSecondary" BBTB/Packages/AppFeatures/Sources/ServerListFeature/AutoCell.swift` ≥ 4. `grep -c "Color\\.accentColor\\|\\.secondary\\.opacity" BBTB/Packages/AppFeatures/Sources/ServerListFeature/AutoCell.swift` = 0 (никаких остатков system colors в продакшен). `accessibilityReduceMotion` присутствует ≥ 1. iOS xcodebuild SUCCEEDED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: ServerRow — DS.Color tokens align + selected background accent + iconSecondary/iconMuted swap (M8 / DS-12)</name>
  <files>BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerRow.swift</files>
  <read_first>
    - BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerRow.swift (целиком, 131 lines)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-RESEARCH.md §3 DS-12 + §«Architectural Responsibility Map» row для ServerRow
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-PATTERNS.md §MOD-5 (lines 409-465)
    - BBTB/Packages/DesignSystem/Tokens/CODE-CONNECT.md §1.4 + §1.5 (lines 93-136 — ServerRow + ServerRow Selected mapping)
    - `.planning/phases/11-onboarding-ux-polish/figma-inspect/06-servers-selected.png` (reference)
  </read_first>
  <behavior>
    - Behavior 1: При `isSelected = false` ServerRow renders: text name = `DS.Color.textPrimary`; checkmark — отсутствует; chevron foregroundStyle = `DS.Color.iconSecondary`. Background — `Color.clear`.
    - Behavior 2: При `isSelected = true` ServerRow renders: background = `DS.Color.accent`; text name = `DS.Color.textPrimary` (preserve white); checkmark fill = `DS.Color.iconMuted` (#CCCCCC); chevron = `DS.Color.iconMuted`.
    - Behavior 3: При `!server.isSupported` или `server.isUnreachable` — opacity 0.4 preserved (existing); text name = `DS.Color.textSecondary`.
    - Behavior 4: Все existing accessibilityIdentifier preserved + `.accessibilityAddTraits(.isSelected)` добавляется когда `isSelected = true` per UI-SPEC §3.3.
    - **NB:** legacy `UIImpactFeedbackGenerator` в `handleTap()` lines 124-128 — НЕ migrate to `.sensoryFeedback` (D-04 tight scope explicit, UI-SPEC §2.1 Pitfall).
  </behavior>
  <action>
    Token swap в `ServerRow.swift` per PATTERNS §MOD-5:

    1. Импорт: добавить `@Environment(\.accessibilityReduceMotion) private var reduceMotion: Bool`.
    2. Line 52 (`Text(server.name).foregroundStyle(server.isSupported ? .primary : .secondary)`) → `.foregroundStyle(server.isSupported ? (isSelected ? DS.Color.textPrimary : DS.Color.textPrimary) : DS.Color.textSecondary)`. Title-text white в обоих selected/default per CODE-CONNECT.md §1.5 "Server name text stays white in both states".
    3. Line 56 (`Text(L10n.serverListUnsupportedBadge).foregroundStyle(.tertiary)`) → `.foregroundStyle(DS.Color.textTertiary)`.
    4. Line 68 (`Image checkmark.circle.fill .foregroundStyle(Color.accentColor)`) → `.foregroundStyle(DS.Color.iconMuted)` (CODE-CONNECT.md §1.5: selected → iconMuted).
    5. Line 75 (`Image chevron.right .foregroundStyle(.tertiary)`) → `.foregroundStyle(isSelected ? DS.Color.iconMuted : DS.Color.iconSecondary)`.
    6. После line 86 (`.opacity(rowOpacity)`) добавить background chain: `.background(isSelected ? DS.Color.accent : Color.clear).animation(reduceMotion ? nil : .easeInOut(duration: 0.2), value: isSelected)` per UI-SPEC §2.4 + §3.8.
    7. После line 91 (`.accessibilityIdentifier(...)`) добавить `.accessibilityAddTraits(isSelected ? .isSelected : [])` per UI-SPEC §3.3.
    8. Doc-comment над body: `/// Phase 12 / DS-12 / M8 — token alignment + selected background accent + Reduce-Motion gate. См. CODE-CONNECT.md §1.4/§1.5 + UI-SPEC §2.4/§3.3.`
    9. **CRITICAL preserve**:
       - All `accessibilityIdentifier`s (Detail.\(server.id.uuidString) line 79, main one line 90).
       - `.accessibilityElement(children: .combine)` + label/value/hint lines 91-94.
       - `.contextMenu` swipe-actions lines 95-99.
       - `private func handleTap()` lines 124-129 — UIImpactFeedbackGenerator preserve (D-04).
       - `.frame(minHeight: 56)` line 84 — D-04 tight scope: НЕ менять до Wave 1 visual verify (Figma говорит 52pt, но 1px = baseline regression risk).
  </action>
  <verify>
    <automated>swift test --package-path BBTB/Packages/AppFeatures --filter ServerListFeatureTests 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    ServerListFeatureTests все existing функции PASS. `grep -c "DS\\.Color\\." BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerRow.swift` ≥ 6 (textPrimary + textSecondary + textTertiary + iconSecondary + iconMuted + accent). `grep -E "Color\\.accentColor|\\.foregroundStyle\\(\\.(primary|secondary|tertiary)\\)" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerRow.swift | grep -v "^//"` = 0 в продакшене (никаких system semantic colors в нашем pixel-perfect ServerRow). `accessibilityAddTraits` с `.isSelected` ≥ 1 (UI-SPEC §3.3 contract). iOS xcodebuild SUCCEEDED.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Wave-1 Visual Checkpoint — M3 + M8 + M9 + M10 quick wins side-by-side с Figma final-*.png</name>
  <files>None — checkpoint task. Visual review против Figma final-*.png reference screenshots (M3 ConnectionButton fills, M9 sheet corners, M10 section radius, M8 row tokens).</files>
  <action>Checkpoint task — paused execution for human visual verification. Executor запускает app в iPhone 16 simulator iOS 18 и сверяет 4 экрана side-by-side с Figma reference screenshots (см. полный protocol в `<how-to-verify>` ниже). НЕ writing code. Если visual diff обнаружен — фиксирует в issue note и возвращает к Task 1-4 для surgical fix перед Task 6.</action>
  <verify>Manual: human visually compares 4 screens × Figma reference; checkpoint passes когда user types `approved` либо описывает `issue:<...>` для retry loop.</verify>
  <done>User signals `approved` в orchestrator chat (или `issue:<...>` triggers retry на затронутую Task 1-4). После approval — Task 6 unlocked.</done>
  <what-built>
    Task 1 (ConnectionButton fillColor → DS.Color tokens), Task 2 (ServerListSheet UnevenRoundedRectangle 32pt top corners), Task 3 (AutoCell pill — DS.Radius.section + DS.Color.accent/surfaceSunken), Task 4 (ServerRow — DS.Color tokens + selected background accent).

    Эти 4 quick wins создают visual fidelity для большинства экранов БЕЗ heavy lifts (BBTBSpinner, OnboardingView). Перед запуском heavy lifts — verify что foundation сидит правильно.
  </what-built>
  <how-to-verify>
    1. Сгенерировать Xcode workspace: `tuist generate`
    2. Запустить BBTB на iPhone 16 simulator iOS 18: `xcodebuild build -workspace BBTB.xcworkspace -scheme BBTB -destination 'platform=iOS Simulator,name=iPhone 16'` + open Simulator + run app.
    3. **Screen 1 — Home idle (DS-09 .idle, M3):** ConnectionButton должна быть тёмно-серая (`#222222` = controlIdle), НЕ светло-серая. Сравнить с `.planning/phases/11-onboarding-ux-polish/figma-inspect/02-home-disconnected.png`.
    4. **Screen 2 — Home connected (M3):** Подключиться к серверу (вставить config); ConnectionButton должна стать accent-green (`#14664B`). Сравнить с `05-home-connected.png`.
    5. **Screen 3 — Home error (M3):** Имитировать error state (положить broken config); кнопка должна стать тёмно-красная (`#661414`). Сравнить с `04-home-error.png`.
    6. **Screen 4 — Servers sheet (M9 + M8 + M10):** Тапнуть TopBar → "Серверы" → открыть ServerListSheet. Verify:
       - 32pt rounded top corners (UnevenRoundedRectangle) — углы видны над drag indicator, БЕЗ artifacts (Risk #2).
       - Surface цвет внутри sheet = `#222222` (DS.Color.surface).
       - AutoCell — 24pt rounded corners (DS.Radius.section), selected = accent fill, unselected = surfaceSunken fill.
       - ServerRow selected → background accent green, checkmark+chevron светло-серые (`#CCCCCC` = iconMuted). Default ServerRow → transparent background, chevron `#808080` (iconSecondary).
       - Сравнить с `06-servers-selected.png` + `07-servers-auto.png`.
    7. **Pixel-diff tolerance:** ≤2px на ключевых элементах (button Ø, padding, corner radii, font sizes). Anti-aliasing на тексте и gradient — игнорируем (D-10).
    8. **Reject criteria:** если M9 (sheet corners) сломал presentationDetents rendering — apply Risk #2 fallback (переместить `.background + .clipShape` внутрь NavigationStack VStack), повторить screen 4.
    9. **Approval signal:** Тип `approved` если все 4 screens соответствуют Figma final-*.png в пределах 2px tolerance. Тип `issue:<описание>` если есть проблема — Task 6+ wait до фикса.
  </how-to-verify>
  <resume-signal>Тип `approved` либо `issue: <описание проблемы и какой Task fix>`</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Create BBTBSpinner + integrate в ConnectionButton (M6 / DS-08 + DS-09 spinner placement) — heavy lift с Reduce-Motion fallback</name>
  <files>BBTB/Packages/DesignSystem/Sources/DesignSystem/Spinner.swift, BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift, BBTB/Packages/DesignSystem/Tests/DesignSystemSnapshotTests/SpinnerSnapshotTests.swift</files>
  <read_first>
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-RESEARCH.md §2.1 (lines 147-229 — BBTBSpinner Option B + placement decision)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-PATTERNS.md §«NEW · Spinner.swift» (lines 187-249)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-UI-SPEC.md §2.2 (lines 55-59 — motion contract + Reduce-Motion fallback + battery guard) + §3.2 (lines 99-101 — VoiceOver decorative)
    - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift (после Task 1)
    - `.planning/phases/11-onboarding-ux-polish/figma-inspect/03-home-connecting.png` (reference — spinner ring around button)
    - BBTB/Packages/DesignSystem/Sources/DesignSystem/DSColor.swift (DS.Color.iconPrimary/.iconMuted/.iconSecondary — gradient stops)
  </read_first>
  <behavior>
    - Behavior 1: BBTBSpinner default mode рендерит `Circle().trim(from: 0, to: 0.85)` со `.stroke(AngularGradient(colors: [iconPrimary, iconMuted, iconSecondary, .clear], center: .center, startAngle: 0, endAngle: 360), style: StrokeStyle(lineWidth: 6, lineCap: .round))`, `.frame(width: diameter, height: diameter)`, `.rotationEffect(.degrees(angle))` где angle анимируется `withAnimation(.linear(duration: 1.2).repeatForever(autoreverses: false))` от 0 до 360.
    - Behavior 2 (**W4 lock**): При `accessibilityReduceMotion = true` — анимация заменяется на **static `.rotationEffect(.degrees(0))` БЕЗ `withAnimation` + pulsating `.opacity` 0.6↔1.0 (cycle 1.0с, `withAnimation(.easeInOut(duration: 1.0).repeatForever(autoreverses: true))`)**. NO discrete-snap альтернативы, NO executor-choice hedge — UI-SPEC §3.8 «discrete 4-frame snap OR static ring with opacity pulse» альтернатива из Phase 12 revision iteration 1 разрешена в пользу pulsating opacity (W4 — final decision; rationale: проще audit, 100% predictable, нет flickering от snap frames).
    - Behavior 3: `BBTBSpinner.accessibilityHidden(true)` — ring decorative; статус озвучивается parent кнопкой (UI-SPEC §3.2).
    - Behavior 4: ConnectionButton при `isConnecting` рендерит BBTBSpinner С diameter `+ 24` (ring AROUND circle, не over), power-icon `.opacity(1)` (видна), `Image.symbolEffect(.bounce, value: state).disabled(accessibilityReduceMotion)` (UI-SPEC §2.7).
    - Behavior 5: Snapshot frozen-frame BBTBSpinner (rotation=0°) recorded baseline PASS на повторных запусках (`perceptualPrecision: 0.97` per UI-SPEC §5 для gradient stroke).
  </behavior>
  <action>
    **Step 1:** Создать `BBTB/Packages/DesignSystem/Sources/DesignSystem/Spinner.swift` per RESEARCH §2.1 sketch (lines 174-219 verbatim) + Reduce-Motion fallback:

    1. Header: `/// Phase 12 / DS-08 / M6 — BBTBSpinner: rotating ring placeholder для ConnectionButton .connecting state. Заменяет ProgressView() из Phase 11 UX-08 D-05. Figma component Spinner (3057:167) — 4-frame ring with grayscale AngularGradient stroke. См. RESEARCH §2.1 + UI-SPEC §2.2/§3.8.`
    2. `import SwiftUI`.
    3. `public struct BBTBSpinner: View`:
       - Public params: `diameter: CGFloat = 280, lineWidth: CGFloat = 6, speed: Double = 1.2`.
       - `@State private var angle: Double = 0`.
       - `@State private var pulseOpacity: Double = 1.0` (для Reduce-Motion fallback).
       - `@Environment(\.accessibilityReduceMotion) private var reduceMotion: Bool`.
       - `public init(diameter: CGFloat = 280, lineWidth: CGFloat = 6, speed: Double = 1.2) { ... }`.
       - `public var body: some View` который возвращает `Circle().trim(from: 0, to: 0.85).stroke(AngularGradient(colors: [DS.Color.iconPrimary, DS.Color.iconMuted, DS.Color.iconSecondary, Color.clear], center: .center, startAngle: .degrees(0), endAngle: .degrees(360)), style: StrokeStyle(lineWidth: lineWidth, lineCap: .round)).frame(width: diameter, height: diameter).rotationEffect(.degrees(reduceMotion ? 0 : angle)).opacity(reduceMotion ? pulseOpacity : 1.0).onAppear { if reduceMotion { withAnimation(.easeInOut(duration: 1.0).repeatForever(autoreverses: true)) { pulseOpacity = 0.6 } } else { withAnimation(.linear(duration: speed).repeatForever(autoreverses: false)) { angle = 360 } } }.accessibilityHidden(true)`.
    4. Doc-comment над `body`: `/// Default motion: linear 1.2s repeatForever rotation. Reduce-Motion fallback: pulsating opacity 0.6↔1.0 cycle 1s (UI-SPEC §3.8). Battery guard via conditional mount в parent (Pitfall 3).`

    **Step 2:** Update `ConnectionButton.swift` body (RESEARCH §4.5 target) — **W3 fix: spinner как `.overlay` на Circle, НЕ sibling в ZStack** (чтобы parent VStack/HStack frame не пересчитывался при isConnecting toggle):

    1. Line 25 `.foregroundStyle(.white)` → `.foregroundStyle(DS.Color.textPrimary)`.
    2. Line 31 `.opacity(isConnecting ? 0 : 1)` → `.opacity(1)` (power-icon видна во всех states — RESEARCH §2.1 spinner-placement).
    3. **W3 fix — заменить sibling-in-ZStack pattern на `.overlay` на Circle:** Lines 32-41 (`if isConnecting { ProgressView() ... }`) удалить полностью **из тела ZStack** и заменить на `.overlay` modifier **на самом `Circle()`** в строке 20-22:
       ```
       Circle()
           .fill(fillColor)
           .frame(width: diameter, height: diameter)
           .overlay {
               if isConnecting {
                   BBTBSpinner(diameter: diameter + 24, lineWidth: 6, speed: 1.2)
                       .accessibilityHidden(true)
               }
           }
       ```
       **Why W3 fix:** overlay не участвует в layout calculation родителя — frame ConnectionButton остаётся = `diameter × diameter` независимо от isConnecting. Sibling-в-ZStack паттерн (старый план) делал ZStack frame = max(child frames) = `(diameter+24) × (diameter+24)` при `.connecting` и `diameter × diameter` в остальных state'ах → parent HomeView VStack/HStack пересчитывал layout каждый toggle, что вызывало visual reflow при connecting↔connected переходе. **Acceptance criterion (W3):** Parent VStack/HStack frame НЕ recalculated when isConnecting toggles — verify через `_printChanges()` либо UAT visual check (Task 9): на iPhone 16 simulator переключение между .idle/.connecting/.connected не вызывает «прыжков» соседних view'ов (timer label под кнопкой, server name над кнопкой).
    4. После line 26 (`.symbolEffect(.bounce, value: state)`) добавить `@Environment(\.accessibilityReduceMotion) private var reduceMotion: Bool` к struct + `.disabled(reduceMotion)` после symbolEffect (UI-SPEC §2.7 fallback).
    5. Импорт: `import DesignSystem` уже есть.
    6. Update Phase 11 comments (lines 27-30, 33-35) — заменить на `// Phase 12 / DS-08 / M6 — Spinner placement: ring AROUND Circle через .overlay (W3 fix — НЕ sibling-in-ZStack чтобы parent frame не пересчитывался при isConnecting toggle). См. RESEARCH §2.1 + Plan 12-02 W3 revision note.`.
    7. **CRITICAL preserve:**
       - `accessibilityIdentifier("BBTB.ConnectionButton")` line 46 — ~207 тестов референсят.
       - `isConnecting: Bool` boolean computed prop — `ConnectionButtonTests` 5 функций PASS gate.
       - `fillColor: Color` (после Task 1 W2 fix — `internal`, не private; W2 doc-comment present).
       - diameter / iconSize switch logic — НЕ менять (читает уже-обновлённые DS.ConnectionButtonSize константы from Plan 12-01).

    **Step 3:** Создать `BBTB/Packages/DesignSystem/Tests/DesignSystemSnapshotTests/SpinnerSnapshotTests.swift` per RESEARCH §2.4 sample (lines 446-472):

    1. Header per PATTERNS §«Shared Patterns 5» convention. Reference DS-08 + M6.
    2. `@MainActor final class SpinnerSnapshotTests: XCTestCase`.
    3. Test 1: `testSpinner280pt_darkMode_frozen` — `BBTBSpinner(diameter: 280, lineWidth: 6, speed: 1.2).frame(width: 320, height: 320).background(SwiftUI.Color.black).preferredColorScheme(.dark)`. `assertSnapshot(of: view, as: .image(precision: 1.0, perceptualPrecision: 0.97, layout: .fixed(width: 320, height: 320)))` (perceptualPrecision 0.97 per UI-SPEC §5 для gradient stroke).
    4. **Note про anti-flake:** SwiftUI `.onAppear` запускает withAnimation, что делает rotation timing зависимым от render schedule. Для стабильности теста — рассмотреть mock'аnie angle через `@available` parameter exposure ИЛИ принять что angle = 0 на initial snapshot frame (SwiftUI рендерит `rotationEffect(.degrees(0))` на первом фрейме до `onAppear` асинхронного эффекта). Executor валидирует через 3 повторных пробега — если flake появляется, обернуть BBTBSpinner в `.transaction { $0.disablesAnimations = true }` для snapshot.

    Initial baseline record protocol (как в Plan 12-01 Task 5):
    - Раскомментировать `isRecording = true` → запустить → "fail" (baseline записан) → commit PNG.
    - Закомментировать → запустить → PASS → final commit.
  </action>
  <verify>
    <automated>xcodebuild test -scheme DesignSystem -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing DesignSystemSnapshotTests/SpinnerSnapshotTests 2>&amp;1 | tail -20 &amp;&amp; swift test --package-path BBTB/Packages/AppFeatures --filter ConnectionButtonTests 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    `SpinnerSnapshotTests.testSpinner280pt_darkMode_frozen` PASS на 3 повторных пробегах (no flake). Baseline PNG закоммичен (~50-100KB) в `BBTB/Packages/DesignSystem/Tests/DesignSystemSnapshotTests/__Snapshots__/SpinnerSnapshotTests/`. `grep -c "BBTBSpinner(diameter: diameter + 24" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift` = 1. `grep -c "ProgressView" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift` = 0 (placeholder ушёл). `grep -c "opacity(isConnecting ? 0 : 1)" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift` = 0 (icon видна всегда). `ConnectionButtonTests` 8 функций (5 existing + 3 fillColor от Task 1) PASS. iOS xcodebuild SUCCEEDED. Manual visual: на Simulator iPhone 16, кликнуть Start → power-icon видна + ring around → сравнение с `final-03-home-connecting.png` PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: OnboardingView rebuild — hero text split + PrimaryButton/SecondaryButton + sensoryFeedback haptic (M7 / DS-11) — heaviest</name>
  <files>BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift</files>
  <read_first>
    - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift (целиком, 134 lines — preserve onChange / identifiers / 2 CTA structure)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-RESEARCH.md §4.7 (lines 656-676 — hero text split sketch) + §2.5 (lines 524-538 — ButtonStyle application + haptic note)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-PATTERNS.md §MOD-4 (lines 323-406)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-UI-SPEC.md §2.1 (haptic Pitfall 6 — tapCounter local, не ConnectionState) + §2.6 (Onboarding transitions)
    - `.planning/phases/11-onboarding-ux-polish/figma-inspect/final-01-onboarding.png` (Figma reference)
    - BBTB/Packages/DesignSystem/Sources/DesignSystem/ButtonStyles.swift (Plan 12-01 Task 3 output)
  </read_first>
  <behavior>
    - Behavior 1 (**B2 lock**): Hero text рендерится одной `Text` concatenation: `(Text("Интернет, каким он ").foregroundStyle(DS.Color.textPrimary) + Text("должен быть").foregroundStyle(DS.Color.accent)).font(DS.Typography.expanded(DS.Typography.Size.display, weight: .semibold)).multilineTextAlignment(.center)` — **48pt SF Pro Expanded Semibold per RESEARCH §12 Q1 RESOLVED**. NO `titleScreen=16` (16pt — для секций, не hero). Точные русские строки per CONTEXT.md `<specifics>`.
    - Behavior 2: Primary CTA (Paste) использует `.buttonStyle(PrimaryButtonStyle())` + `.sensoryFeedback(.impact(weight: .light), trigger: pasteTapCounter)` где `pasteTapCounter` — `@State private var Int = 0` инкрементируется в action.
    - Behavior 3: Secondary CTA (QR) использует `.buttonStyle(SecondaryButtonStyle())` + `.sensoryFeedback(.impact(weight: .light), trigger: qrTapCounter)` (отдельный counter).
    - Behavior 4: `accessibilityIdentifier("BBTB.Onboarding.PasteButton")` + `BBTB.Onboarding.QRButton` preserved (test references из Phase 11).
    - Behavior 5: `.onChange(of: viewModel.state)` + `dismissIfImported(_:)` lines 116-133 preserved (D-03 Phase 11 — auto-dismiss after import). Только 2 CTA, без file picker (D-04 Phase 11).
    - Behavior 6: Иконка top — заменена на token-driven размер (`.font(.system(size: DS.Typography.Size.display * 1.5))` ≈ 72pt — preserve existing) ИЛИ убрана если Figma `final-01-onboarding.png` не показывает иконку. Executor смотрит Figma и решает.
  </behavior>
  <action>
    Rebuild `OnboardingView.body` per RESEARCH §4.7 + PATTERNS §MOD-4. Сохранить header comment + D-01..D-04 CRITICAL preserve list (lines 64-67):

    1. К struct добавить `@State private var pasteTapCounter: Int = 0` + `@State private var qrTapCounter: Int = 0` per UI-SPEC §2.1 (Pitfall 6 — local counters, НЕ ConnectionState).
    2. Body rebuild:
       ```
       VStack(spacing: DS.Spacing.xxl) {
           Spacer()
           // Иконка — executor смотрит Figma final-01-onboarding.png и решает:
           // (a) если в Figma top-icon есть → preserve Image(systemName: "shield.lefthalf.filled") но `.foregroundStyle(DS.Color.accent)` вместо `.tint`.
           // (b) если нет → удалить полностью + сократить top Spacer до Spacer().frame(height: DS.Spacing.xxxl).
           Image(systemName: "shield.lefthalf.filled")
               .font(.system(size: 72))
               .foregroundStyle(DS.Color.accent)
               .accessibilityHidden(true)

           // Hero text split per RESEARCH §4.7 + CONTEXT.md <specifics>.
           VStack(spacing: DS.Spacing.md) {
               (Text("Интернет, каким он ")
                   .foregroundStyle(DS.Color.textPrimary)
                + Text("должен быть")
                   .foregroundStyle(DS.Color.accent))
                   .font(DS.Typography.expanded(DS.Typography.Size.display, weight: .semibold))  // B2 lock: 48pt SF Pro Expanded Semibold per RESEARCH §12 Q1 RESOLVED
                   .multilineTextAlignment(.center)

               Text(L10n.onboardingSubtitle)
                   .font(DS.Typography.bodyDefault)
                   .foregroundStyle(DS.Color.textSecondary)
                   .multilineTextAlignment(.center)
           }
           .padding(.horizontal, DS.Spacing.xl)

           Spacer()

           // 2 CTA — D-02 Phase 11 strict: ровно 2 кнопки, без file picker.
           VStack(spacing: DS.Spacing.md) {
               Button(L10n.onboardingPaste) {
                   pasteTapCounter += 1
                   onPaste()
               }
               .buttonStyle(PrimaryButtonStyle())
               .sensoryFeedback(.impact(weight: .light), trigger: pasteTapCounter)
               .accessibilityIdentifier("BBTB.Onboarding.PasteButton")
               .accessibilityLabel(Text(L10n.onboardingPaste))

               Button(L10n.onboardingScanQR) {
                   qrTapCounter += 1
                   onScanQR()
               }
               .buttonStyle(SecondaryButtonStyle())
               .sensoryFeedback(.impact(weight: .light), trigger: qrTapCounter)
               .accessibilityIdentifier("BBTB.Onboarding.QRButton")
               .accessibilityLabel(Text(L10n.onboardingScanQR))
           }
           .padding(.horizontal, DS.Spacing.xl)
           .padding(.bottom, DS.Spacing.xl)
       }
       .frame(maxWidth: .infinity, maxHeight: .infinity)
       .background(DS.Color.canvas)
       #if os(macOS)
       .frame(minWidth: 480, minHeight: 640)
       #endif
       .onChange(of: viewModel.state) { _, newState in
           dismissIfImported(newState)
       }
       ```
    3. Doc-comment над body: `/// Phase 12 / DS-11 / M7 — Figma pixel-perfect rebuild: hero text split (white + accent), 2 CTA Primary/Secondary pill style, sensoryFeedback haptic (iOS 17+). Phase 11 D-01..D-04 CRITICAL preserve: identifiers + onChange dismiss logic + 2-CTA contract.`
    4. **CRITICAL preserve untouched:**
       - struct properties (`viewModel`, `onPaste`, `onScanQR`, `onDismiss`) + their docs lines 27-42.
       - `public init(...)` signature lines 43-53.
       - `accessibilityIdentifier`s lines 98, 104 (verbatim).
       - `dismissIfImported(_:)` private func lines 123-133.
       - `.onChange(of: viewModel.state)` line 118.
    5. **Update existing Phase 11 placeholder comments** lines 56-68 — replace with: `// Phase 12 / DS-11 / M7 — Figma rebuild applied. Phase 11 placeholder заменён на: hero text split + PrimaryButton/SecondaryButton + sensoryFeedback. CRITICAL preserve (D-01/D-02/D-03 Phase 11): identifiers + onChange dismiss + 2-CTA contract.`
  </action>
  <verify>
    <automated>swift test --package-path BBTB/Packages/AppFeatures --filter MainScreenFeatureTests 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    `MainScreenFeatureTests` все existing функции PASS (D-01..D-04 invariants preserved — identifiers, dismiss logic, 2 CTA). `grep -c "PrimaryButtonStyle()\\|SecondaryButtonStyle()" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift` = 2. `grep -c "sensoryFeedback(.impact(weight: .light)" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift` = 2. `grep -E "buttonStyle\\(\\.borderedProminent|buttonStyle\\(\\.bordered\\)" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift | grep -v '^//'` = 0 (system styles ушли). `grep -c "Интернет, каким он\\|должен быть" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift` ≥ 2 (Text split). `accessibilityIdentifier("BBTB.Onboarding.PasteButton")` + `BBTB.Onboarding.QRButton` preserved (grep = 1 каждый). iOS xcodebuild SUCCEEDED.
  </done>
</task>

<task type="auto">
  <name>Task 8: Snapshot test corpus — ConnectionButton 4 states + ServerRow 2 states + AutoCell 2 states + AppFeatures Package.swift dep wiring (DS-15 component portion)</name>
  <files>BBTB/Packages/AppFeatures/Package.swift, BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/Snapshots/ConnectionButtonSnapshotTests.swift, BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/Snapshots/OnboardingViewSnapshotTests.swift, BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/Snapshots/__Snapshots__/.gitkeep, BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/Snapshots/ServerListSnapshotTests.swift, BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/Snapshots/ServerRowFixtures.swift, BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/Snapshots/__Snapshots__/.gitkeep</files>
  <read_first>
    - BBTB/Packages/AppFeatures/Package.swift (lines 13-100 — dependencies + testTarget structure)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-RESEARCH.md §6 (validation architecture — sampling rate + 16 req → test map + threshold table)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-UI-SPEC.md §4 (State Coverage Matrix) + §5 (Visual Acceptance Criteria — precision/perceptualPrecision per element type)
    - .planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-PATTERNS.md §«NEW · DesignSystemSnapshotTests» + §«Shared Patterns 5» (test file header)
    - 6 modified source files Tasks 1-7
  </read_first>
  <action>
    **Step 1: AppFeatures/Package.swift** — добавить swift-snapshot-testing dep + расширить два testTarget с SnapshotTesting product.

    1. В `dependencies:` массив (lines 13-41) добавить: `.package(url: "https://github.com/pointfreeco/swift-snapshot-testing", from: "1.18.3")` с inline comment `// Phase 12 / DS-15 — pixel-perfect snapshot baselines для ConnectionButton/ServerRow/AutoCell.`
    2. В `.testTarget(name: "MainScreenFeatureTests", ...)` добавить к dependencies: `.product(name: "SnapshotTesting", package: "swift-snapshot-testing")`.
    3. В `.testTarget(name: "ServerListFeatureTests", ...)` добавить аналогичный dep.

    **Step 2: ConnectionButtonSnapshotTests.swift** — 4 snapshot функции per UI-SPEC §4 State Coverage Matrix (5 ConnectionState кейсов, но `.empty == .idle` visually).

    1. Path: `BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/Snapshots/ConnectionButtonSnapshotTests.swift`.
    2. Header per PATTERNS §«Shared Patterns 5» convention. Reference DS-08 + DS-09 + M3 + M6 + UX-09.
    3. `@MainActor final class ConnectionButtonSnapshotTests: XCTestCase`.
    4. 5 test method'а (4 compact + 1 regular size class — W1 fix для DS-05 regular coverage):
       - `test_connectionButton_idle_dark` — `ConnectionButton(state: .idle, action: {}).frame(width: 320, height: 320).background(DS.Color.canvas).preferredColorScheme(.dark)` (compact size class default). perceptualPrecision: 1.0 (solid fill).
       - `test_connectionButton_connecting_dark` — `state: .connecting`. perceptualPrecision: 0.97 (BBTBSpinner gradient). **NB:** для anti-flake — `.transaction { $0.disablesAnimations = true }` перед `assertSnapshot`, иначе rotationEffect создаёт flaky baseline.
       - `test_connectionButton_connected_dark` — `state: .connected(since: Date(timeIntervalSince1970: 0))`. perceptualPrecision: 1.0.
       - `test_connectionButton_error_dark` — `state: .error(message: "Тестовая ошибка")`. perceptualPrecision: 1.0.
       - **`test_connectionButton_idle_regular` (W1 fix)** — `ConnectionButton(state: .idle, action: {}).environment(\.horizontalSizeClass, .regular).frame(width: 360, height: 360).background(DS.Color.canvas).preferredColorScheme(.dark)`. Это locks `regularDiameter=320pt` / `regularIcon=128pt` baseline (DS-05 regular size class) — без этого теста только compact path validates через snapshots. perceptualPrecision: 1.0.
    5. Initial baseline record protocol (как в Plan 12-01 Task 5 N3) — `record: .missing` default + env var `SNAPSHOT_TESTING_RECORD=1` для full re-record.

    **Step 3: ServerListSnapshotTests.swift + Onboarding/Sheet snapshot coverage (W5 fix — Sampling Continuity gap closure)** — 4 row/cell функции + 2 NEW функции (`test_serverListSheet_corners_dark` + `test_onboardingView_hero_dark`).

    1. Path: `BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/Snapshots/ServerListSnapshotTests.swift` для ServerList; `BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/Snapshots/OnboardingViewSnapshotTests.swift` для Onboarding hero (новый file).
    2. Headers per convention. Reference DS-12 + DS-13 + DS-14 + DS-11 (Onboarding) + W5 revision note.

    3. **N2 fix — ServerConfig fixture verification + creation sub-step (cross-check):**
       - Executor проверяет `BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift` через `grep -n "static let preview\|static var preview" BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift`. **На момент revision iteration 1: `.preview` static фикстура НЕ существует** (verified; ServerConfig имеет только extension с countryFlag/isUnreachable/identity helpers, без preview instance).
       - **Sub-step 3a (создание fixture file):** Создать `BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/Snapshots/ServerRowFixtures.swift` с deterministic ServerConfig instance:
         ```swift
         // ServerRowFixtures.swift — Phase 12 / Plan 12-02 / Task 8 / N2.
         //
         // Deterministic ServerConfig для snapshot тестов (UUID/host/port фиксированы,
         // чтобы baseline PNG не дрейфовал между прогонами). `.preview` static не существует
         // в VPNCore.ServerConfig (verified 2026-05-16 revision iteration 1), потому создаём
         // local-to-test fixture. Если в Phase 13+ потребуется shared fixture — promote в
         // VPNCore as `public static let preview: ServerConfig`.
         import VPNCore

         enum ServerRowFixtures {
             static let sample = ServerConfig(
                 id: UUID(uuidString: "00000000-0000-0000-0000-000000000001")!,
                 name: "Amsterdam #1",
                 host: "ams1.example.test",
                 port: 443,
                 protocolID: "vless-reality",
                 protocolDisplayName: "VLESS Reality",
                 sni: "www.cloudflare.com",
                 rawURI: "vless://...",  // value не важен для visual snapshot
                 subscriptionID: nil,
                 countryCode: "NL",
                 lastPingedAt: nil,
                 failedProbeCount: 0,
                 missingFromLastFetch: false,
                 transportOverride: nil
             )
         }
         ```
       - Executor сверяет `ServerConfig.init(...)` signature через `Read BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift` (lines 70-99 имеют actual public init с 14 params) — fixture создаётся ровно под текущую signature.

    4. Test method'ы (ServerListSnapshotTests):
       - `test_serverRow_default_dark` — `ServerRow(server: ServerRowFixtures.sample, isSelected: false, pingState: .completed(.success(latency: 28)), onTap: {}, onDelete: {}, onDetailTap: {}).frame(width: 375, height: 56).background(DS.Color.surface).preferredColorScheme(.dark)`. perceptualPrecision: 0.98 (text + AA).
       - `test_serverRow_selected_dark` — same но `isSelected: true`. Verify accent background.
       - `test_autoCell_default_dark` — `AutoCell(isSelected: false, onTap: {}).frame(width: 375, height: 80).background(DS.Color.surface).preferredColorScheme(.dark)`. perceptualPrecision: 0.98.
       - `test_autoCell_selected_dark` — same но `isSelected: true`. Verify accent fill + 24pt radius.
       - **`test_serverListSheet_corners_dark` (W5 fix #1)** — render `ServerListSheet.sheetContent` (либо minimal mock NavigationStack + VStack + UnevenRoundedRectangle clipShape — executor выбирает minimal path если sheetContent privacy блокирует direct access; если `private var sheetContent` → use `@testable` чтобы получить доступ ИЛИ render minimal wrapper view с тем же clipShape chain что в production). Frame `width: 375, height: 700`, `.preferredColorScheme(.dark)`, snapshot конкретно верхних 100pt — `layout: .fixed(width: 375, height: 100)` либо crop через `.frame(width: 375, height: 100, alignment: .top).clipped()` — locks UnevenRoundedRectangle 32pt top corners (DS-14). perceptualPrecision: 0.98 (corner anti-aliasing).

    5. Test method (Onboarding — new file `OnboardingViewSnapshotTests.swift` в `MainScreenFeatureTests/Snapshots/`):
       - **`test_onboardingView_hero_dark` (W5 fix #2 + B2 visual lock)** — render full `OnboardingView(viewModel: <mock>, onPaste: {}, onScanQR: {}, onDismiss: {})` через @MainActor; frame `375×812` (iPhone 16 portrait), `.preferredColorScheme(.dark)`, full snapshot. Это locks (a) hero text 48pt SF Pro Expanded Semibold (B2 — будет fail если кто-то откатит на titleScreen=16); (b) 2 CTA pill стили (PrimaryButtonStyle accent + SecondaryButtonStyle inverted); (c) M7 visual целиком. perceptualPrecision: 0.98 (large text + AA + gradient на icons).
       - Mock viewModel: `MainScreenViewModel.previewInstance` если есть, либо minimal `MainScreenViewModel()` с initial `.empty` state (тестируем UI верстку, не state transitions — `.onChange(of: viewModel.state)` ничего не сделает в snapshot frame).

    6. **Step 4:** создать `.gitkeep` файлы в `__Snapshots__/` директориях:
       - `BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/Snapshots/__Snapshots__/.gitkeep`
       - `BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/Snapshots/__Snapshots__/.gitkeep`
       (оба директорий уже в files_modified frontmatter).

    7. **Total snapshot baselines в Plan 12-02 (после revision iteration 1):** 5 ConnectionButton (W1 +1 regular) + 4 ServerList rows/cells + 1 ServerListSheet corners (W5) + 1 Onboarding hero (W5+B2) + 1 Spinner (Task 6) = **12 baselines** в Plan 12-02 + 3 ButtonStyle baselines от Plan 12-01 = **15 total** в Phase 12. Размер ~10-100KB каждый = ≤1.5MB total (manageable, без LFS).
  </action>
  <verify>
    <automated>xcodebuild test -workspace BBTB.xcworkspace -scheme BBTB -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing MainScreenFeatureTests/ConnectionButtonSnapshotTests -only-testing ServerListFeatureTests/ServerListSnapshotTests 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    **11 snapshot функций PASS (revision iteration 1):** 5 ConnectionButton (W1 — incl. test_connectionButton_idle_regular) + 4 ServerRow/AutoCell + 1 test_serverListSheet_corners_dark (W5) + 1 test_onboardingView_hero_dark (W5+B2) = 11 в Plan 12-02. Все baseline PNGs закоммичены под `__Snapshots__/`. `git status BBTB/Packages/AppFeatures/Tests/` пусто (всё committed). `grep -c "swift-snapshot-testing" BBTB/Packages/AppFeatures/Package.swift` ≥ 1. `grep -c "SnapshotTesting" BBTB/Packages/AppFeatures/Package.swift` ≥ 2 (oба testTarget'а). **N2 verify:** `test -f BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/Snapshots/ServerRowFixtures.swift && echo OK` (fixture file present). **W1 verify:** `grep -c "test_connectionButton_idle_regular" BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/Snapshots/ConnectionButtonSnapshotTests.swift` = 1. **W5 verify:** `grep -c "test_serverListSheet_corners_dark" BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/Snapshots/ServerListSnapshotTests.swift` = 1; `grep -c "test_onboardingView_hero_dark" BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/Snapshots/OnboardingViewSnapshotTests.swift` = 1. Total Phase 12 snapshot functions = **15** (3 Plan 12-01 ButtonStyles + 1 Plan 12-02 Spinner + 5 ConnectionButton + 4 ServerList + 1 Sheet corners + 1 Onboarding hero). Полный AppFeatures regression — 207+ existing тестов + 11 new snapshot PASS. iOS + macOS xcodebuild SUCCEEDED.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Phase 12 closure — full 7-screen manual UAT pixel-perfect comparison (UX-09 ✓ Validated)</name>
  <files>.planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-UAT.md (created by executor), .planning/REQUIREMENTS.md (UX-09 line update on approval), .planning/STATE.md + .planning/ROADMAP.md (phase closure updates)</files>
  <action>Phase 12 closure checkpoint — Executor запускает app + side-by-side Figma comparison по 7 экранам, создаёт `12-UAT.md` с таблицей verdicts, прогоняет VoiceOver/Reduce-Motion/contrast/tap-target a11y gate, прогоняет full regression + R1/R6 invariants. User просматривает UAT.md и approves либо запрашивает retry (см. полный protocol в `<how-to-verify>` ниже). На approval — UX-09 переводится в `[x]` в REQUIREMENTS.md строка 92 + REQUIREMENTS/ROADMAP/STATE phase closure updates.</action>
  <verify>Manual: full 7-screen UAT таблица + a11y section + regression section в `12-UAT.md`, user types `approved` (или `retry:<N>` для loop, `borderline-accept` для anti-aliasing tolerance per D-10).</verify>
  <done>`12-UAT.md` создан и user approved; REQUIREMENTS.md UX-09 строка 92 переведена в `[x]` с Phase 12 reference; STATE.md + ROADMAP.md закрытие Phase 12 (status → ✅ Closed, completed_phases++, completed_plans+=2); wiki long-term memory page `wiki/figma-pixel-perfect-rebuild-2026.md` создана. Next phase: `/gsd-discuss-phase 13` (TestFlight & Distribution).</done>
  <what-built>
    Полный Phase 12 implementation: 12 snapshot baselines + 6 modified views + BBTBSpinner + ButtonStyles + DS.Color/Typography/Radius/Blur extension + ~207 existing regression preserved. Все 10 mismatches M1-M10 resolved через Plan 12-01 Foundation + Plan 12-02 Application.
  </what-built>
  <how-to-verify>
    Финальная manual UAT по 7 экранам Figma (UI-SPEC §4 State Coverage Matrix + CONTEXT.md D-08 hybrid acceptance). Executor выполняет, user signing off в `12-UAT.md`.

    **Setup:**
    1. `tuist generate`
    2. Open BBTB.xcworkspace; select iPhone 16 simulator iOS 18.0+; build BBTB scheme.
    3. Launch app, добавить тестовую subscription (любой рабочий config) чтобы получить серверы в пуле.
    4. Open Figma side-by-side: `https://www.figma.com/design/tI6DFQDU6PdOSmd19BGXqg/BBTB-v3` (узлы 3062:304 / 3043:341 / 3047:538 / 3047:568 / 3047:598 / 3064:350 / 3064:1579).

    **Per-screen verify checklist (pixel-diff ≤ 2px tolerance per D-10):**

    | # | Screen | Figma node | Verify points | PASS criteria |
    |---|--------|------------|--------------|---------------|
    | 1 | Onboarding | 3062:304 | Hero text split «Интернет, каким он» белый + «должен быть» accent; 2 pill CTA Primary (accent) + Secondary (white); SF Pro Expanded font; haptic на каждый tap (физическое устройство preferred) | Все 4 элемента совпадают; identifiers `BBTB.Onboarding.PasteButton` + `BBTB.Onboarding.QRButton` доступны через Accessibility Inspector |
    | 2 | Home Disconnected (.idle) | 3043:341 | ConnectionButton 280pt dark grey (#222222 controlIdle); power-icon visible 112pt; SF Pro Expanded на всех text | Кнопка большая dark; никакого .gray system color |
    | 3 | Home Connecting | 3047:538 | ConnectionButton 280pt controlIdle (НЕ orange); BBTBSpinner ring AROUND кнопки (diameter 304); power-icon ВИДНА; ring grayscale gradient stroke 6pt | Spinner вращается 1.2с; ring снаружи Circle, не overlay |
    | 4 | Home Error | 3047:568 | ConnectionButton 280pt dark red (#661414); error message displayed | dark red fill; никакого Color.red.opacity |
    | 5 | Home Connected | 3047:598 | ConnectionButton 280pt accent green (#14664B); timer 00:01:07 в SF Pro Expanded Medium 48pt; power-icon visible | accent green точно #14664B; timer Expanded не rounded |
    | 6 | Servers Selected | 3064:350 | Sheet 32pt top rounded corners (UnevenRoundedRectangle); ServerListSheet height + detents preserved; ServerRow selected = accent green background; AutoCell 24pt section radius; checkmark + chevron iconMuted (#CCCCCC) | Углы 32pt over drag indicator; selected row distinct |
    | 7 | Servers Auto | 3064:1579 | AutoCell selected = accent fill, default = surfaceSunken fill; bouncyCheckmark animates on toggle | AutoCell pill 24pt; smooth state transition |

    **Accessibility validation (UI-SPEC §3):**
    - VoiceOver round-trip на iOS 18: открыть Settings → Accessibility → VoiceOver, активировать; тапнуть по ConnectionButton — должен прочитать "Подключение VPN, отключено, двойное касание чтобы подключиться" (UI-SPEC §3.1 .idle row).
    - Reduce Motion: Settings → Accessibility → Motion → Reduce Motion ON. Restart app, перейти в connecting state — BBTBSpinner НЕ должен вращаться (либо pulsating opacity, либо static — executor выбирает per UI-SPEC §3.8). Tap PrimaryButton — НЕ должен scale-effect (per UI-SPEC §3.8).
    - Color contrast (UI-SPEC §3.5): Xcode → Open Developer Tool → Accessibility Inspector → Color Contrast Calculator. Проверить 5 пар из §3.5 таблицы. Все PASS / borderline / FAIL фиксировать в `12-UAT.md`.
    - Tap targets (UI-SPEC §3.6): через Accessibility Inspector → Inspection Mode → tap каждый интерактивный элемент. Все ≥ 44×44pt.

    **Wire-only Light-mode artifact note (W6 fix):**
    - **SecondaryButtonStyle в Light mode выглядит инвертированно** (чёрная pill + белый текст). Это **known wire-only artifact от D-05** — Light получает hex values из figma-tokens.json, но designer ещё не нарисовал Light версии экранов. Не FAIL — `borderline-accept`-able per D-10. Final visual tuning делается когда designer дорисует Light в Figma (Light-mode pass — backlog post-v1.0).
    - В UAT таблице screen 1 (Onboarding): если переключить iOS Settings → Display → Light mode и Onboarding отобразится «invert», помечать **`PASS (wire-only artifact)`** в `12-UAT.md`, не FAIL.
    - Plan 12-01 Task 3 содержит doc-comment про этот artifact в SecondaryButtonStyle source.

    **Regression validation:**
    - `swift test --package-path BBTB/Packages/AppFeatures 2>&amp;1 | tail -20` — все 207 existing + 11 new snapshot функций в Plan 12-02 PASS (+ 4 в DesignSystem package — 3 ButtonStyle от 12-01 + 1 Spinner от 12-02 Task 6 = 15 baselines Phase 12 total).
    - `bash scripts/validate-r1-r6.sh` — все invariants PASS (Phase 12 не трогает sing-box).
    - `xcodebuild build -workspace BBTB.xcworkspace -scheme BBTB -destination 'platform=iOS Simulator,name=iPhone 16'` + macOS — SUCCEEDED.

    **Approval gate:**
    Executor создаёт `.planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-UAT.md` со таблицей 7 screens × 4-5 verify points × verdict (PASS/borderline/FAIL) + a11y section + regression section. User читает + типит `approved` если PASS на всех 7 screens + 0 a11y FAIL + 0 regression. Если есть borderline — user конкретно говорит accept либо retry.

    **Resume signal options:**
    - `approved` — Phase 12 closure: UX-09 ✓ Validated, all 10 mismatches resolved, REQUIREMENTS / ROADMAP / STATE updates → Phase 13 (TestFlight & Distribution).
    - `retry:<screen N> <issue>` — конкретный fix → another iteration → re-UAT той же screen.
    - `borderline-accept` — user принимает <2px diff на anti-aliasing как acceptable per D-10.
  </how-to-verify>
  <resume-signal>Type `approved` либо `retry:<N> <issue>` либо `borderline-accept`</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Plan 12-02 — pure UI/visual changes. Никаких новых trust boundaries.
- Никаких new endpoints, никаких новых user-input parsers, никаких auth/network/crypto изменений.
- BBTBSpinner / ButtonStyle / accessibilityIdentifier — все decorative + a11y, без security implications.
- ServerRow / AutoCell / ConnectionButton — те же data flows что Phase 3+ (ServerConfig + PingState + ConnectionState reads).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | snapshot baseline PNG'и | accept | Read-only artifacts в Git; не используются runtime. Если злоумышленник заменит baseline → test will FAIL на CI, не PASS — fail-safe. |
| T-12-02-02 | Information Disclosure | accessibilityIdentifier strings exposed в .ipa | accept | `BBTB.*` identifiers уже public Phase 1+; нет sensitive info. Сохранение идентификаторов — UAT contract. |
| T-12-02-03 | Repudiation | a11y regression (VoiceOver labels, Reduce-Motion fallback) | mitigate | Task 9 manual UAT включает VoiceOver round-trip + Reduce Motion test + contrast calculator. UI-SPEC §3 explicit gates. Reduce-Motion fallback в BBTBSpinner (Task 6) + ButtonStyles (Plan 12-01 Task 3) + AutoCell (Task 3) + ServerRow (Task 4) — gate-checked в acceptance criteria. |
| T-12-02-04 | DoS | Spinner animation drain battery при hidden view | mitigate | UI-SPEC §2.2 battery guard: BBTBSpinner монтируется условно `if isConnecting { BBTBSpinner(...) }` — при `.connected/.error/.idle` view удаляется, animation auto-stops (RESEARCH §9 Pitfall 3). Task 6 acceptance criteria включает this contract. |
| T-12-02-05 | Elevation of Privilege | (NONE) | N/A | Phase 12 не трогает entitlements, App Group, keychain, VPN profile management. |

**ASVS L1 compliance:** Phase 12 не вводит новый user-input или auth surface. Только visual/a11y/test changes. R1-R21 invariants Phase 1+ preserved (никаких изменений в PacketTunnelKit / sing-box / network code). `validate-r1-r6.sh` PASS gate в Task 9.
</threat_model>

<verification>
**Plan 12-02 verification gates:**

1. **Test suite green:** `swift test --package-path BBTB/Packages/AppFeatures 2>&1 | tail -20` — 207 existing + 12 snapshot функций PASS. Никаких регрессий.
2. **iOS + macOS xcodebuild green:** `xcodebuild build -workspace BBTB.xcworkspace -scheme BBTB -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5` SUCCEEDED + macOS destination SUCCEEDED.
3. **R1/R6 invariants preserved:** `bash scripts/validate-r1-r6.sh 2>&1 | tail -5` PASS (Phase 12 не трогает sing-box).
4. **Snapshot anti-flake:** Каждый snapshot test pass на ≥3 повторных пробегах подряд (executor validate).
5. **accessibilityIdentifier preserve:** `grep -r "BBTB\\." BBTB/Packages/AppFeatures/Sources/ 2>&1 | wc -l` ≥ 19 (количество matches не упало vs Phase 11).
6. **No system semantic colors:** `grep -rE "\\.foregroundStyle\\(\\.(primary|secondary|tertiary)\\)" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ BBTB/Packages/AppFeatures/Sources/ServerListFeature/ 2>&1 | grep -v '^//' | wc -l` минимум — все ключевые места используют DS.Color.* (несколько acceptable если non-tokenized).
7. **No remaining ProgressView placeholder:** `grep -rn "ProgressView" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift` = 0.
8. **Manual UAT signoff:** `12-UAT.md` создан + user approved Task 9.

**UX-09 Validation path (REQUIREMENTS.md update):**
- Перед Task 9: UX-09 в REQUIREMENTS.md остаётся `figma-pending`.
- После Task 9 user approval: planner/closure обновляет REQUIREMENTS.md строку 92 — заменить `[~]` на `[x]` с timestamp + Phase 12 reference.
</verification>

<success_criteria>
- [ ] **DS-08** — BBTBSpinner существует в DesignSystem, snapshot baseline recorded + PASS, Reduce-Motion fallback присутствует.
- [ ] **DS-09** — ConnectionButton.fillColor → DS.Color.controlIdle/.accent/.error switch; spinner = BBTBSpinner ring (diameter+24, не ProgressView); power-icon visible (opacity 1) во всех states; 4 snapshot baseline recorded + PASS.
- [ ] **DS-11** — OnboardingView rebuilt: hero text split + PrimaryButton/SecondaryButton + sensoryFeedback haptic; 2 CTA identifiers preserved.
- [ ] **DS-12** — ServerRow tokens align: textPrimary/textSecondary/iconMuted/iconSecondary; selected background accent; reduce-motion gate на selected animation; 2 snapshot baseline recorded + PASS.
- [ ] **DS-13** — AutoCell pill: 24pt section radius; accent/surfaceSunken fill; iconPrimary/iconSecondary icon; 2 snapshot baseline recorded + PASS.
- [ ] **DS-14** — ServerListSheet UnevenRoundedRectangle 32pt top corners; presentationDetents/dragIndicator preserved.
- [ ] **DS-15 (component portion, revision iteration 1)** — **11 snapshot функций в Plan 12-02** (5 ConnectionButton incl. W1 regular + 4 ServerList rows/cells + 1 W5 serverListSheet_corners + 1 W5+B2 onboardingView_hero) + 1 SpinnerSnapshotTests (Task 6) + 3 от Plan 12-01 = **15 total** baseline PNG'ов закоммичены и зелёные на ≥3 повторных пробегах.
- [ ] **UX-09** — Phase 12 closure manual UAT 7 screens user-approved в `12-UAT.md`; REQUIREMENTS.md UX-09 → `[x]` Validated.
- [ ] **Regression** — 207 existing AppFeatures тестов PASS + 11 new snapshot функций Plan 12-02 PASS = **218 в AppFeatures** + 4 в DesignSystem (DSColor + DSTokens + ButtonStyle + Spinner). Total Phase 12 test suite green.
- [ ] **Build** — iOS + macOS xcodebuild SUCCEEDED.
- [ ] **R1/R6 invariants** — `validate-r1-r6.sh` PASS.
- [ ] **Anti-flake** — snapshot tests PASS на 3 повторных пробегах подряд.
- [ ] **B2 fix verified** — `grep -c "expanded(DS.Typography.Size.display, weight: .semibold)" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift` ≥ 1; `grep -c "DS.Typography.titleScreen" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift` = 0 в hero text block; hedge comment ("может быть display=48 для hero") удалён.
- [ ] **W1 fix verified** — `test_connectionButton_idle_regular` существует и PASS.
- [ ] **W2 fix verified** — ConnectionButton.fillColor имеет `internal` access level (НЕ private) + doc-comment про Phase 11 D-05 pattern present; tests используют `@MainActor` + explicit `UITraitCollection(userInterfaceStyle: .dark)`.
- [ ] **W3 fix verified** — `grep -c "\\.overlay {" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift` ≥ 1 (overlay pattern); spinner НЕ siblings в ZStack; parent VStack/HStack frame stable при isConnecting toggle (UAT visual check Task 9).
- [ ] **W4 fix verified** — BBTBSpinner Reduce-Motion fallback locked to pulsating opacity 0.6↔1.0 cycle 1.0s; alternative discrete-snap path удалён из Behavior 2 и Spinner.swift action.
- [ ] **W5 fix verified** — 2 new snapshot tests (`test_serverListSheet_corners_dark` + `test_onboardingView_hero_dark`) существуют и PASS.
- [ ] **W6 fix verified** — Task 9 UAT how-to-verify содержит wire-only Light-mode artifact note; Plan 12-01 SecondaryButtonStyle doc-comment grep `wire-only` ≥ 1.
- [ ] **N2 fix verified** — `BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/Snapshots/ServerRowFixtures.swift` существует и используется в ServerListSnapshotTests.
- [ ] **Accessibility** — VoiceOver labels per UI-SPEC §3.1 contract, Reduce Motion fallback verified, color contrast pairs PASS/borderline-accepted в UAT, tap targets ≥44pt.
- [ ] **All 10 mismatches resolved:** M1+M2 (numerics через 12-01) + M3 (Task 1) + M4 (через 12-01 expanded helper) + M5 (через 12-01 DS.Color.accent) + M6 (Task 6) + M7 (Task 7) + M8 (Task 4 ServerRow + Task 3 AutoCell padding) + M9 (Task 2) + M10 (Task 3 AutoCell section radius).
</success_criteria>

<output>
After completion, create `.planning/phases/12-swift-pixel-perfect-rebuild-from-figma-v0-12-design/12-02-SUMMARY.md` documenting:
- 6 modified views + 1 new BBTBSpinner + 1 Package.swift update + 2 snapshot test files.
- 9 new snapshot baseline PNGs committed (1 Spinner + 4 ConnectionButton + 4 ServerList).
- All 10 mismatches M1-M10 status table (resolved / fallback applied / borderline accepted).
- UX-09 Validated path: `12-UAT.md` + REQUIREMENTS.md update.
- Reduce-Motion fallback applied: BBTBSpinner / ButtonStyle / AutoCell / ServerRow.
- Total Phase 12 snapshot corpus: 12 baselines (3 ButtonStyle + 1 Spinner + 4 ConnectionButton + 4 ServerList).
- Regression preserved: 207 existing AppFeatures tests + 12 snapshot = 219 PASS.
- Architectural decisions captured to `wiki/figma-pixel-perfect-rebuild-2026.md` (new wiki page per CLAUDE.md rule «каждое архитектурное решение фиксируется в wiki»):
  - DS.Color literal pattern (вместо Asset Catalog — Risk #4 Pitfall 1).
  - SF Pro Expanded через `.fontWidth(.expanded)` (вместо .otf bundle — Apple Font SLA §2B).
  - BBTBSpinner Circle.trim+AngularGradient Option B (вместо Canvas / SF Symbol .symbolEffect).
  - UnevenRoundedRectangle pure SwiftUI (вместо UIBezierPath).
  - Snapshot library `swift-snapshot-testing` 1.18.3+ (вместо hand-rolled XCTAttachment).
  - Reduce-Motion fallback patterns для всех animated UI.
- Phase 13 prerequisites unchanged (Apple Distribution credentials, SPKI pins, macOS UAT replay, Numerical Instruments baseline).
</output>
