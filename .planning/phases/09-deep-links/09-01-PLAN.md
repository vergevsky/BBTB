---
phase: 09-deep-links
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift
  - BBTB/Packages/DeepLinks/Package.swift
  - BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkHandler.swift
  - BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkError.swift
  - BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinksLogger.swift
  - BBTB/Packages/DeepLinks/Sources/DeepLinks/TokenFetcher.swift
  - BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkRouter.swift
  - BBTB/Packages/DeepLinks/Tests/DeepLinksTests/DeepLinkRouterTests.swift
autonomous: true
requirements:
  - DEEP-01
  - DEEP-02
  - DEEP-03  # deferred to v1+ — architectural stub only (TokenFetcher protocol)
  - DEEP-04  # deferred to v1+ — no implementation in v0.9
  - DEEP-05

tags:
  - deep-links
  - swiftpm-package
  - actor
  - scope-amendment

must_haves:
  truths:
    - "REQUIREMENTS.md и ROADMAP.md фиксируют scope amendment Phase 9 (DEEP-03/04 deferred to v1+)"
    - "`ImportSource.deepLink` enum case существует в VPNCore — все switch sites compile clean"
    - "Новый SwiftPM пакет `BBTB/Packages/DeepLinks` собирается изолированно (`swift test --package-path Packages/DeepLinks` → 0 exit code)"
    - "`DeepLinkRouter` actor + `protocol DeepLinkHandler` + `DeepLinkError` + `DeepLinksLogger` + `TokenFetcher` placeholder protocol существуют и публично доступны"
    - "Тесты DeepLinkRouterTests покрывают register + handle-iteration + unhandled-throw сценарии"
  artifacts:
    - path: "BBTB/Packages/DeepLinks/Package.swift"
      provides: "SwiftPM пакет с iOS 18 / macOS 15, swift-tools 6.0, deps: VPNCore + ConfigParser"
      contains: "let package = Package(\n    name: \"DeepLinks\""
    - path: "BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkRouter.swift"
      provides: "public actor DeepLinkRouter + register + handle"
      contains: "public actor DeepLinkRouter"
    - path: "BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkHandler.swift"
      provides: "public protocol DeepLinkHandler: Sendable { canHandle / handle }"
      contains: "public protocol DeepLinkHandler"
    - path: "BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkError.swift"
      provides: "public enum DeepLinkError: Error, LocalizedError, Equatable, Sendable"
      contains: "public enum DeepLinkError"
    - path: "BBTB/Packages/DeepLinks/Sources/DeepLinks/TokenFetcher.swift"
      provides: "public protocol TokenFetcher: Sendable (v1+ placeholder per D-03)"
      contains: "public protocol TokenFetcher"
    - path: "BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinksLogger.swift"
      provides: "OSLog wrapper с тремя категориями (router/importer/token)"
      contains: "subsystem: \"app.bbtb.client\""
    - path: "BBTB/Packages/DeepLinks/Tests/DeepLinksTests/DeepLinkRouterTests.swift"
      provides: "register + iterate + unhandled throw tests"
      contains: "func test_handle_"
    - path: "BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift"
      provides: "ImportSource enum с новым `.deepLink` case"
      contains: "case deepLink"
  key_links:
    - from: ".planning/ROADMAP.md Phase 9 entry"
      to: "REQUIREMENTS.md DEEP-01..05"
      via: "Requirements list + scope amendment block"
      pattern: "DEEP-03.*Out of Scope.*v1\\+"
    - from: "BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkRouter.swift"
      to: "BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkHandler.swift"
      via: "private var handlers: [any DeepLinkHandler]"
      pattern: "handlers: \\[any DeepLinkHandler\\]"
---

<objective>
**Phase Goal (user story per MVP_MODE):**

**As a** разработчик BBTB на пути к v1.0, **I want to** иметь готовый клиентский routing-каркас для deep links (custom scheme + Universal Links) с расширяемым `DeepLinkHandler` registry и архитектурной заглушкой для v1+ token-flow, **so that** в следующих волнах Phase 9 я добавляю один концретный `ImportHandler` + entitlements + Info.plist + App-wiring без переписывания базы, а в v1+ DEEP-03 token-fetch добавляется простой регистрацией нового handler'а без изменения routing core.

Purpose: Wave 1 — **скелет SwiftPM пакета `DeepLinks` + scope amendment в REQUIREMENTS/ROADMAP + новый `.deepLink` case в `ImportSource`.** Это контрактная база для Wave 2 (concrete ImportHandler), Wave 3 (App-wiring + entitlements + Info.plist) и Wave 4 (AASA hosting + Apple Developer Portal + UAT).

Output:
- `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md` обновлены — DEEP-03 и DEEP-04 явно помечены «Out of Scope v0.9 → v1+ backlog» с rationale ссылающимся на CONTEXT.md D-01..D-03.
- `BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift` — `ImportSource` enum получает новый `case deepLink`. Все существующие switch sites — audited grep gate'ом (Pitfall #3 защита).
- `BBTB/Packages/DeepLinks/` — новый SwiftPM пакет: Package.swift, 5 source файлов (`DeepLinkRouter`, `DeepLinkHandler`, `DeepLinkError`, `DeepLinksLogger`, `TokenFetcher`), 1 test файл (`DeepLinkRouterTests`).
- `swift test --package-path BBTB/Packages/DeepLinks` exits 0 — все unit tests зелёные.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-deep-links/09-CONTEXT.md
@.planning/phases/09-deep-links/09-RESEARCH.md
@.planning/phases/09-deep-links/09-PATTERNS.md
@.planning/phases/09-deep-links/09-VALIDATION.md
@BBTB/Packages/RulesEngine/Package.swift
@BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesEngineCoordinator.swift
@BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesEngineLogger.swift
@BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift
@BBTB/Packages/ProtocolRegistry/Sources/ProtocolRegistry/ProtocolRegistry.swift
@CLAUDE.md

<interfaces>
<!-- Key types и contracts которые executor должен использовать. -->
<!-- Извлечено из codebase прямым grep — executor не должен искать. -->

From BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift (lines 289-295, CURRENT state — добавить `.deepLink`):
```swift
public enum ImportSource: Sendable, Equatable {
    case pasteboard
    case subscriptionURL(URL)
    case jsonEndpoint(URL)
    case qrCode
    case multilineText
}
```

After this plan ImportSource MUST be:
```swift
public enum ImportSource: Sendable, Equatable {
    case pasteboard
    case subscriptionURL(URL)
    case jsonEndpoint(URL)
    case qrCode
    case multilineText
    case deepLink  // Phase 9 / DEEP-01
}
```

From BBTB/Packages/ConfigParser/Sources/ConfigParser/ConfigImporting.swift (line 26 — already public, no change needed):
```swift
public protocol ConfigImporting: Sendable {
    func importFromRawInput(_ raw: String, source: ImportSource) async throws -> ImportResult
}
```

From BBTB/Packages/RulesEngine/Package.swift (analog — структурный template для DeepLinks/Package.swift):
- swift-tools-version: 6.0
- platforms: `[.iOS(.v18), .macOS(.v15)]`
- dependencies: `[.package(path: "../VPNCore"), .package(path: "../ConfigParser")]`
- target dependencies: `["VPNCore", "ConfigParser"]`
- **NO `resources:` section** (DeepLinks не имеет bundled assets в v0.9)
- **NO external SwiftPM deps** (Phase 9 — pure Foundation + OSLog)
- **NO `linkerSettings:` block в testTarget** (DeepLinks не тянет libbox через зависимости)

From BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesEngineLogger.swift (full file — analog для DeepLinksLogger.swift):
```swift
import Foundation
import OSLog

enum RulesEngineLogger {
    static let coordinator = Logger(subsystem: "app.bbtb.client", category: "rules.coordinator")
    static let fetcher = Logger(subsystem: "app.bbtb.client", category: "rules.fetcher")
    static let signer = Logger(subsystem: "app.bbtb.client", category: "rules.signer")
}
```

From BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesEngineCoordinator.swift (line 109 — analog для DeepLinkRouter actor declaration; line 53-67 — PerfSignposter pattern):
```swift
public actor RulesEngineCoordinator {
    // ... actor-isolated state
}

enum PerfSignposter {
    static let client = OSSignposter(
        subsystem: "app.bbtb.client",
        category: "performance"
    )
}
```

From BBTB/Packages/ProtocolRegistry/Sources/ProtocolRegistry/ProtocolRegistry.swift (lines 6-26 — extensible registry pattern; iteration through registered handlers).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Update REQUIREMENTS.md + ROADMAP.md with Phase 9 scope amendment (DEEP-03/04 → v1+)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/09-deep-links/09-CONTEXT.md (scope amendment block at top + D-01..D-03)
    - .planning/REQUIREMENTS.md lines 142-148 (current DEEP-01..05 entries)
    - .planning/ROADMAP.md lines 364-375 (Phase 9 section as it stands)
  </read_first>
  <behavior>
    - REQUIREMENTS.md DEEP-03 entry helle now starts: `~~**DEEP-03**: Endpoint ...~~ → **Out of Scope v0.9** _(Phase 9 scope amendment 2026-05-15 per D-01..D-03. Архитектурная заглушка `TokenFetcher` protocol реализована в `BBTB/Packages/DeepLinks` для v1+ regеnerации. См. `wiki/deep-links.md` после полного обновления в W4.)_`
    - REQUIREMENTS.md DEEP-04 entry strikethrough'd similarly with v1+ rationale.
    - DEEP-01 / DEEP-02 / DEEP-05 stay as `- [ ]` (work-in-progress); их text не меняется в эту задачу.
    - REQUIREMENTS.md footer "Last updated" line gets new entry: `*Last updated: 2026-05-15 — Phase 9 W1 scope amendment: DEEP-03 + DEEP-04 carved out to v1+ backlog per D-01..D-03 in 09-CONTEXT.md (token endpoint + landing page deferred; only AASA + clientside routing в v0.9).*`
    - ROADMAP.md Phase 9 section:
        - Header line `**Goal:**` НЕ менять (user story emit делается в plan-level `<objective>` block above; не нужно дублировать в ROADMAP).
        - Add scope-amendment note immediately AFTER "**Requirements:** DEEP-01, DEEP-02, DEEP-03, DEEP-04, DEEP-05" line: `**Scope amendment (2026-05-15 in /gsd-discuss-phase 9):** DEEP-03 (token endpoint `/c/{token}`) и DEEP-04 (landing page) carved out → v1+ backlog. Phase 9 реализует только клиентскую часть + минимальный AASA-сервер. Архитектурная заглушка `TokenFetcher` protocol сохраняется в пакете для v1+ resume.`
        - Append after Success Criteria блок: `**Plans:** 4 plans (waves 1-4)` + plan list:
            ```
            Plans:
            **Wave 1**
            - [ ] 09-01-PLAN.md — Wave 1: SwiftPM пакет DeepLinks (Router actor + Handler protocol + Error enum + Logger + TokenFetcher placeholder) + ImportSource.deepLink case + scope amendment в REQUIREMENTS/ROADMAP + DeepLinkRouterTests

            **Wave 2** *(blocked on Wave 1 completion)*
            - [ ] 09-02-PLAN.md — Wave 2: ImportHandler concrete (DEEP-01 + DEEP-02 URL parsing) + RemoteTokenFetchHandler stub + ImportHandler/URLParsing tests + L10n keys + ImportSource switch-exhaustiveness grep gate

            **Wave 3** *(blocked on Wave 2 completion)*
            - [ ] 09-03-PLAN.md — Wave 3: Tuist Project.swift wire-up + entitlements (Associated Domains) + Info.plist (CFBundleURLTypes) + BBTB_iOSApp/BBTB_macOSApp init+modifiers + MainScreenViewModel.handleDeepLink + pendingDeepLink cold-start buffer + iOS/macOS xcodebuild green

            **Wave 4** *(blocked on Wave 3 completion, contains 2 checkpoints + AASA deploy)*
            - [ ] 09-04-PLAN.md — Wave 4: AASA file content + nginx OR Cloudflare Pages deploy + Apple Developer Portal Associated Domains capability + iPhone+macOS device UAT (F1 cold-start, F2 warm-start, F3 macOS Universal Link, F4 error alert) + wiki/deep-links.md full rewrite + closure
            ```
  </behavior>
  <action>
    Edit `.planning/REQUIREMENTS.md`:
    1. Replace line 146 (current `- [ ] **DEEP-03**: Endpoint ...`) with the strikethrough Out-of-Scope block described in behavior. Use Edit tool with exact line 146 match.
    2. Replace line 147 (current `- [ ] **DEEP-04**: Landing page ...`) with the analogous strikethrough Out-of-Scope block.
    3. Replace the existing `*Last updated: 2026-05-15 — Phase 8 W0 amendment...*` footer with the new Phase 9 footer line. Keep the prior "Previous:" line shifted down (don't lose history).

    Edit `.planning/ROADMAP.md` Phase 9 section (lines 364..375):
    1. Keep current `**Goal:**` line untouched.
    2. After `**Requirements:** DEEP-01, DEEP-02, DEEP-03, DEEP-04, DEEP-05` insert TWO blank lines + the **Scope amendment** paragraph described in behavior.
    3. After the last `Success Criteria` bullet (current item 4: `**DeepLinkRouter** корректно парсит...`) append a blank line + `**Plans:** 4 plans (waves 1-4)` + the Plans list described in behavior. Pattern: mirror Phase 8 ROADMAP entry structure (see lines 335-360 of ROADMAP.md for exact format).

    Do NOT touch other phases. Do NOT touch DEEP-01 / DEEP-02 / DEEP-05 wording (they remain in scope).
  </action>
  <verify>
    <automated>grep -c "DEEP-03.*Out of Scope.*v0.9" .planning/REQUIREMENTS.md | grep -v '^0$'</automated>
  </verify>
  <done>
    - `grep -E "DEEP-03.*Out of Scope" .planning/REQUIREMENTS.md` returns at least 1 match (DEEP-03 carved).
    - `grep -E "DEEP-04.*Out of Scope" .planning/REQUIREMENTS.md` returns at least 1 match.
    - `grep -E "Scope amendment.*2026-05-15.*/gsd-discuss-phase 9" .planning/ROADMAP.md` returns at least 1 match.
    - `grep -c "09-01-PLAN.md\|09-02-PLAN.md\|09-03-PLAN.md\|09-04-PLAN.md" .planning/ROADMAP.md` >= 4 (all four plan refs added).
    - `grep -c "Last updated: 2026-05-15 — Phase 9 W1 scope" .planning/REQUIREMENTS.md` >= 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: Add ImportSource.deepLink case + audit existing switch sites (Pitfall #3)</name>
  <files>BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift</files>
  <read_first>
    - BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift (lines 285-300, current ImportSource enum)
    - .planning/phases/09-deep-links/09-RESEARCH.md § "Pitfall 3: ImportSource switch exhaustiveness"
    - .planning/phases/09-deep-links/09-PATTERNS.md § "17) BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift (MODIFY line 289-295)"
    - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift line 678-682 (existing switch source { case .qrCode where raw != nil: ... default: ... } — verified compatible с new case через default)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift line 64 (`case .subscriptionURL(let url):` site) — needs audit
  </read_first>
  <behavior>
    - `BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift` ImportSource enum gets `case deepLink` after `case multilineText`.
    - Comment line above the case: `/// Phase 9 / DEEP-01 — URL открыт через bbtb:// scheme или Universal Link.`
    - ALL existing `switch` sites над `ImportSource` either (a) already use `default:` and stay clean, OR (b) explicitly handle `.deepLink` with appropriate behavior.
    - `swift build` для VPNCore + dependent packages succeeds — никаких compile-time `Switch must be exhaustive` warnings/errors.
    - Audit shell trace report committed inline в task summary: list of all switch-sites grep'd и решение для каждого (`.default` covers OR explicit case added).
  </behavior>
  <action>
    1. Edit `BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift` lines 289-295 (ImportSource enum). Insert before the closing brace `}`:
       ```swift
           /// Phase 9 / DEEP-01 — URL открыт через bbtb:// scheme или Universal Link.
           case deepLink
       ```

    2. Run audit (per Pitfall #3 — preserve exhaustiveness):
       ```bash
       grep -rn "switch.*[Ii]mport[Ss]ource\|case \.pasteboard\|case \.subscriptionURL\|case \.jsonEndpoint\|case \.qrCode\|case \.multilineText" BBTB/Packages/ | grep -v ".build/" | grep -v "/Fixtures/"
       ```

    3. For each match — determine site type:
       - **If `switch source` без `default:`** (rare, would be exhaustive): add `case .deepLink:` branch with appropriate behavior (mirror `.qrCode` or `.pasteboard` case based on context — see MainScreenViewModel.performImport pattern as canonical fallback example which uses `default:`).
       - **If `switch source` WITH `default:`** (common — MainScreenViewModel line 678): no change required; new case absorbs into existing `default:` branch.
       - **If `if case .pasteboard = ...` pattern**: no change required (single-case match).

    4. **Confirmed safe site map** (per behavior pre-flight grep):
       - `MainScreenViewModel.swift:679` — `switch source { case .qrCode where raw != nil: ... default: ... }` — `default:` absorbs `.deepLink`. **NO CHANGE.**
       - `UniversalImportParser.swift:64` — `case .subscriptionURL(let url):` — single-case extraction inside larger switch chain (`if case .subscriptionURL`); investigate the actual switch parent. If full switch over ImportSource — add `.deepLink` branch routing to "treat as multiline text fallback" path (mirror `.multilineText` behavior).
       - `UniversalImportParserTests.swift:166` — `if case .subscriptionURL = result.source` — single-case match. **NO CHANGE.**

    5. Run `cd BBTB && swift build --package-path Packages/VPNCore` → MUST succeed (no errors).

    6. Run full audit gate (this is the grep gate required by Pitfall #3 and `<verify>`):
       ```bash
       grep -rn "switch.*[Ii]mport[Ss]ource" BBTB/Packages/ | grep -v ".build/" | while read -r line; do
         echo "$line"
       done
       ```

    Document the audit decision inline в commit message body.
  </action>
  <verify>
    <automated>cd BBTB && swift build --package-path Packages/VPNCore 2>&1 | grep -E "Build complete|error:" | head -5</automated>
  </verify>
  <done>
    - `grep -c "case deepLink" BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift` == 1.
    - `cd BBTB && swift build --package-path Packages/VPNCore` exit code 0.
    - `cd BBTB && swift build --package-path Packages/ConfigParser` exit code 0.
    - `cd BBTB && swift build --package-path Packages/AppFeatures` exit code 0.
    - `grep -rn "switch.*ImportSource" BBTB/Packages/ | grep -v ".build/" | grep -v "default:" | grep -v "case .deepLink"` returns 0 lines (every full switch over ImportSource either has `default:` or explicit `.deepLink` case).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.3: Create DeepLinks SwiftPM package skeleton — Package.swift + 5 source files + DeepLinkRouterTests</name>
  <files>
    BBTB/Packages/DeepLinks/Package.swift,
    BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkHandler.swift,
    BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkError.swift,
    BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinksLogger.swift,
    BBTB/Packages/DeepLinks/Sources/DeepLinks/TokenFetcher.swift,
    BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkRouter.swift,
    BBTB/Packages/DeepLinks/Tests/DeepLinksTests/DeepLinkRouterTests.swift
  </files>
  <read_first>
    - BBTB/Packages/RulesEngine/Package.swift (analog template — verify swift-tools-version 6.0, platforms, structure)
    - BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesEngineCoordinator.swift lines 1-30, 53-67, 109-200 (actor + PerfSignposter + register/iterate skeleton)
    - BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesEngineLogger.swift (full file — logger wrapper analog)
    - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift lines 22-50 (ImporterError pattern для DeepLinkError LocalizedError)
    - BBTB/Packages/ProtocolRegistry/Sources/ProtocolRegistry/ProtocolRegistry.swift (lines 6-26 — extensible registry iteration pattern)
    - .planning/phases/09-deep-links/09-PATTERNS.md sections 1-3, 6, 7, 8, 9 (exact assemble instructions для каждого файла)
    - .planning/phases/09-deep-links/09-RESEARCH.md § "Pattern 3" code blocks + § "Code Examples" Example 3
    - .planning/phases/09-deep-links/09-UI-SPEC.md (DeepLinkError cases must align с L10n keys to be added в W2 — но Phase 9 W1 inline-russian fallback acceptable; W2 task swaps to L10n)
  </read_first>
  <behavior>
    - `BBTB/Packages/DeepLinks/Package.swift` существует, swift-tools 6.0, iOS 18 + macOS 15, library product "DeepLinks", deps `VPNCore` + `ConfigParser` (local paths), single target "DeepLinks" + testTarget "DeepLinksTests". NO `resources:`, NO external SwiftPM deps, NO `linkerSettings:` (DeepLinks is pure Foundation/OSLog).
    - `DeepLinkHandler.swift` defines `public protocol DeepLinkHandler: Sendable` со static identifier + `canHandle(_:) -> Bool` + `handle(_:) async throws`.
    - `DeepLinkError.swift` defines `public enum DeepLinkError: Error, LocalizedError, Equatable, Sendable` с 5 cases: `.unhandled(url:)`, `.missingQueryParameter(name:)`, `.invalidParameterValue(name:reason:)`, `.importFailed(underlying:)`, `.notImplemented`. Inline Russian errorDescription fallback strings (W2 task will swap to L10n keys).
    - `DeepLinksLogger.swift` — enum DeepLinksLogger { static let router/importer/token = Logger(subsystem: "app.bbtb.client", category: "deep-links.<name>") }.
    - `TokenFetcher.swift` — `public protocol TokenFetcher: Sendable { func fetchConfig(forToken: String) async throws -> String }`. NO implementations. Doc-comment explains this is v1+ placeholder per D-03.
    - `DeepLinkRouter.swift` — `public actor DeepLinkRouter` с:
        - `private var handlers: [any DeepLinkHandler] = []`
        - `public init() {}` (no DI required в v0.9; handlers register'ятся через `register`)
        - `public func register(_ handler: any DeepLinkHandler)` — appends to `handlers` array, logs via `DeepLinksLogger.router.notice(...)` с identifier.
        - `public func handle(_ url: URL) async throws` — iterates `handlers` in order; first matching `canHandle == true` gets `try await handler.handle(url)`; if no match — throws `DeepLinkError.unhandled(url: url)`. Logs `notice` для каждого `handle` call (URL `privacy: .public`) и `error` для unhandled.
        - PerfSignposter span around the entire `handle` body (mirror RulesEngineCoordinator pattern lines 53-67 + signpost in `handle` method).
    - `DeepLinkRouterTests.swift` — XCTestCase с 3 fake-handler-driven tests:
        - `test_handle_dispatchesToFirstMatchingHandler` — register 2 handlers, only second matches, verify second.handle invoked exactly once.
        - `test_handle_throwsUnhandledWhenNoHandlerMatches` — register handler that always returns false from canHandle, expect DeepLinkError.unhandled thrown.
        - `test_handle_registrationOrderMatters` — register 2 handlers both matching, verify FIRST registered wins (order semantics).
    - All 5 source files + 1 test file compile cleanly: `swift test --package-path BBTB/Packages/DeepLinks` exit 0.
  </behavior>
  <action>
    1. Create directory structure:
       ```bash
       mkdir -p BBTB/Packages/DeepLinks/Sources/DeepLinks
       mkdir -p BBTB/Packages/DeepLinks/Tests/DeepLinksTests
       ```

    2. **`BBTB/Packages/DeepLinks/Package.swift`** — copy structural template from `BBTB/Packages/RulesEngine/Package.swift` with these surgical edits:
       - Header doc block: replace RulesEngine description with: `// Phase 9 / DEEP-05 — Deep-link routing SwiftPM package.\n//\n// Domain: парсит входящие URL (bbtb:// custom scheme + https://import.bbtb.app Universal Links)\n// и dispatches их на зарегистрированные DeepLinkHandler'ы.\n//\n// Architecture: actor coordinator (DeepLinkRouter) + extensible handler registry\n// (DeepLinkHandler protocol). Phase 9 регистрирует ONE concrete (ImportHandler в Wave 2).\n// В v1+ добавляется RemoteTokenFetchHandler (stub уже в пакете per D-03).\n//\n// External dep: NONE — pure Foundation/OSLog. SSRF/HTTPS validation уже выполняется\n// внутри ConfigImporter.importFromRawInput → SubscriptionURLFetcher (Phase 2 hardened).`
       - `name: "DeepLinks"` + `platforms: [.iOS(.v18), .macOS(.v15)]`
       - `products: [.library(name: "DeepLinks", targets: ["DeepLinks"])]`
       - `dependencies: [.package(path: "../VPNCore"), .package(path: "../ConfigParser")]`
       - `targets:`
           - `.target(name: "DeepLinks", dependencies: ["VPNCore", "ConfigParser"])` — NO resources block.
           - `.testTarget(name: "DeepLinksTests", dependencies: ["DeepLinks"])` — NO linkerSettings block.

    3. **`Sources/DeepLinks/DeepLinkHandler.swift`** — copy from 09-PATTERNS.md § 3 verbatim. Must contain:
       - `import Foundation`
       - `public protocol DeepLinkHandler: Sendable {`
       - `static var identifier: String { get }` (mirror RESEARCH.md Pattern 3 code block)
       - `func canHandle(_ url: URL) -> Bool`
       - `func handle(_ url: URL) async throws`
       - `}`
       - Doc-comments per PATTERNS.md spec.

    4. **`Sources/DeepLinks/DeepLinkError.swift`** — full enum per RESEARCH.md Pattern 3:
       ```swift
       import Foundation

       public enum DeepLinkError: Error, LocalizedError, Equatable, Sendable {
           case unhandled(url: URL)
           case missingQueryParameter(name: String)
           case invalidParameterValue(name: String, reason: String)
           case importFailed(underlying: String)
           case notImplemented

           public var errorDescription: String? {
               switch self {
               case .unhandled(let url):
                   return "Не удалось обработать ссылку: \(url.absoluteString)"
               case .missingQueryParameter(let name):
                   return "В ссылке отсутствует параметр «\(name)»"
               case .invalidParameterValue(let name, let reason):
                   return "Параметр «\(name)» некорректен: \(reason)"
               case .importFailed(let underlying):
                   return "Импорт не удался: \(underlying)"
               case .notImplemented:
                   return "Эта функция станет доступна в следующей версии."
               }
           }
       }
       ```
       (W2 task may swap inline strings to L10n keys; for W1 inline RU strings sufficient.)

    5. **`Sources/DeepLinks/DeepLinksLogger.swift`** — exact mirror of RulesEngineLogger.swift with category strings `deep-links.router` / `deep-links.importer` / `deep-links.token`. Three `static let` properties on enum.

    6. **`Sources/DeepLinks/TokenFetcher.swift`** — protocol placeholder per PATTERNS.md § 6:
       ```swift
       import Foundation

       /// Phase 9 / D-03 — PROTOCOL PLACEHOLDER for v1+ DEEP-03 token endpoint.
       /// **No conformers in Phase 9.** In v1+ planner will implement MarzbanDirectTokenFetcher
       /// or ShlinkTokenFetcher. Sendable required for cross-actor use from DeepLinkRouter.
       public protocol TokenFetcher: Sendable {
           func fetchConfig(forToken token: String) async throws -> String
       }
       ```

    7. **`Sources/DeepLinks/DeepLinkRouter.swift`** — actor implementation per RESEARCH.md Pattern 3 + PATTERNS.md § 2:
       - `import Foundation` + `import os.signpost`
       - `enum PerfSignposter { static let client = OSSignposter(subsystem: "app.bbtb.client", category: "performance") }` (file-local — mirror RulesEngineCoordinator.swift lines 53-67)
       - `public actor DeepLinkRouter { ... }` with:
           - `private var handlers: [any DeepLinkHandler] = []`
           - `public init() {}`
           - `public func register(_ handler: any DeepLinkHandler)` — appends + logs `DeepLinksLogger.router.notice("registered handler=\(type(of: handler).identifier, privacy: .public)")`.
           - `public func handle(_ url: URL) async throws` — wrap in signpost span (`beginInterval("DeepLinkHandle", id: signpostID)` + defer `endInterval`); log notice `"handle url=\(url.absoluteString, privacy: .public)"`; iterate `handlers`; first canHandle wins → `try await h.handle(url)` + return; if loop exits without match → log error + `throw DeepLinkError.unhandled(url: url)`.

    8. **`Tests/DeepLinksTests/DeepLinkRouterTests.swift`** — XCTestCase per PATTERNS.md § 9:
       ```swift
       import XCTest
       @testable import DeepLinks

       final class DeepLinkRouterTests: XCTestCase {
           // Fake handler — capture invocations for assertion.
           struct FakeHandler: DeepLinkHandler {
               static let identifier = "fake"
               let predicate: @Sendable (URL) -> Bool
               let recorder: HandleRecorder

               func canHandle(_ url: URL) -> Bool { predicate(url) }
               func handle(_ url: URL) async throws { await recorder.record(url) }
           }

           actor HandleRecorder {
               private(set) var urls: [URL] = []
               func record(_ url: URL) { urls.append(url) }
           }

           func test_handle_dispatchesToFirstMatchingHandler() async throws {
               let recorder1 = HandleRecorder()
               let recorder2 = HandleRecorder()
               let router = DeepLinkRouter()
               await router.register(FakeHandler(predicate: { _ in false }, recorder: recorder1))
               await router.register(FakeHandler(predicate: { _ in true }, recorder: recorder2))
               let url = URL(string: "bbtb://import?url=https%3A%2F%2Fexample.com")!
               try await router.handle(url)
               let r1 = await recorder1.urls
               let r2 = await recorder2.urls
               XCTAssertEqual(r1.count, 0, "first handler should not match")
               XCTAssertEqual(r2.count, 1, "second handler should receive URL")
               XCTAssertEqual(r2.first, url)
           }

           func test_handle_throwsUnhandledWhenNoHandlerMatches() async throws {
               let router = DeepLinkRouter()
               let recorder = HandleRecorder()
               await router.register(FakeHandler(predicate: { _ in false }, recorder: recorder))
               let url = URL(string: "bbtb://unknown")!
               do {
                   try await router.handle(url)
                   XCTFail("expected DeepLinkError.unhandled")
               } catch let err as DeepLinkError {
                   guard case .unhandled(let u) = err else {
                       XCTFail("wrong case: \(err)")
                       return
                   }
                   XCTAssertEqual(u, url)
               }
           }

           func test_handle_registrationOrderMatters_firstMatchWins() async throws {
               let recorder1 = HandleRecorder()
               let recorder2 = HandleRecorder()
               let router = DeepLinkRouter()
               // BOTH match — verify first registered wins.
               await router.register(FakeHandler(predicate: { _ in true }, recorder: recorder1))
               await router.register(FakeHandler(predicate: { _ in true }, recorder: recorder2))
               let url = URL(string: "bbtb://import?url=foo")!
               try await router.handle(url)
               let r1 = await recorder1.urls
               let r2 = await recorder2.urls
               XCTAssertEqual(r1.count, 1, "first-registered handler should win")
               XCTAssertEqual(r2.count, 0)
           }
       }
       ```

    9. Run: `cd BBTB && swift test --package-path Packages/DeepLinks` → MUST exit 0, all 3 tests PASS.

    10. **Codex consultation note (per CLAUDE.md):** Codex MCP consultation for `protocol DeepLinkHandler` Swift 6 strict-concurrency signature was NOT possible in this planning session (mcp__codex__codex prompt file `~/.claude/prompts/architect.md` not found on this machine). The signature used here (`Sendable` constraint + `static var identifier: String { get }` + async-throws `handle`) follows codebase canonical pattern (RulesEngineCoordinator + ProtocolRegistry merged). If Codex consultation reveals different recommendation post-merge — open a Phase 9 follow-up issue. **Existing canonical conformance verified:** RulesEngineCoordinator (Phase 8 closed, working with Swift 6 strict concurrency) uses same `public actor` + `Sendable` registered handlers pattern → high confidence the chosen signature is safe.
  </action>
  <verify>
    <automated>cd BBTB && swift test --package-path Packages/DeepLinks 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `ls BBTB/Packages/DeepLinks/Sources/DeepLinks/*.swift` lists exactly: DeepLinkError.swift, DeepLinkHandler.swift, DeepLinkRouter.swift, DeepLinksLogger.swift, TokenFetcher.swift (5 files).
    - `ls BBTB/Packages/DeepLinks/Tests/DeepLinksTests/*.swift` lists exactly: DeepLinkRouterTests.swift (1 file).
    - `grep -c "public actor DeepLinkRouter" BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkRouter.swift` == 1.
    - `grep -c "public protocol DeepLinkHandler: Sendable" BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkHandler.swift` == 1.
    - `grep -c "public protocol TokenFetcher: Sendable" BBTB/Packages/DeepLinks/Sources/DeepLinks/TokenFetcher.swift` == 1.
    - `grep -c "case .notImplemented\|case notImplemented" BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkError.swift` >= 1.
    - `cd BBTB && swift test --package-path Packages/DeepLinks` exit 0; output contains "Test Suite 'DeepLinkRouterTests' passed" + executed 3 tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External URL source → SwiftPM package boundary | Phase 9 W1 пакет НЕ обрабатывает внешние URLs (это W2 ImportHandler scope). Здесь boundary только тестовый: unit test handlers получают fake URLs. Реальный inbound URL boundary активируется в Wave 3 wiring. |
| Test-only boundary | DeepLinkRouterTests запускают actor через XCTestCase. Все fake handlers — in-memory Sendable structs. Нет network IO, нет filesystem IO. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | DeepLinkRouter registration | accept | Wave 1 — register API is internal-process; нет cross-process spoofing surface. Risk activates Wave 3 (when App.init registers handlers). Documented for W3 threat_model. |
| T-09-02 | Tampering | DeepLinkRouter.handle URL parsing | mitigate | URL parameter type is `URL` (Foundation framework constructed). Actor isolation prevents concurrent mutation of `handlers` array. No serialization/deserialization of URL inputs in this wave. |
| T-09-03 | Repudiation | — | accept | Logger calls emit notice/error levels; OS-level auditing if needed via Console.app. Not security-critical pre-W4. |
| T-09-04 | Information Disclosure | DeepLinksLogger | mitigate | All URL log statements use `privacy: .public` ONLY for scheme/identifier metadata. Subscription token bodies — handled в W2 ImportHandler, will use `privacy: .private`. **Wave 1 surface:** zero user-input URLs logged (only fake test URLs reach logger in Wave 1). |
| T-09-05 | DoS | DeepLinkRouter.handlers array | accept | Array size bounded by static registration count (Phase 9 = 1 handler; v1+ = 2). No user-controlled growth. |
| T-09-06 | EoP | DeepLinkError public enum | mitigate | Equatable + Sendable + LocalizedError только — нет hidden capability tokens, нет file paths or process identifiers leaked via errorDescription. Inline RU strings are static literals (no format-string injection surface). |
| T-09-07 | Tampering | ImportSource.deepLink case | mitigate | Enum case is private to ParsedConfigs.swift codepath; new case requires recompile of ALL packages that depend on VPNCore (compile-time exhaustiveness). Pitfall #3 grep gate enforces no silent-fallthrough on missing case. |
| T-09-08 | Information Disclosure | TokenFetcher protocol placeholder | mitigate | Protocol has NO conformers in v0.9 — zero data flow possible. Doc-comment explicitly notes "no implementations in Phase 9". v1+ implementations will need fresh threat_model. |

**Block-on:** none — all threats either mitigated or accepted with rationale.
</threat_model>

<verification>
**Per-task verification** — see each task's `<verify>` block.

**Plan-level verification (run before commit):**

1. `cd BBTB && swift build --package-path Packages/VPNCore` → exit 0.
2. `cd BBTB && swift build --package-path Packages/ConfigParser` → exit 0.
3. `cd BBTB && swift build --package-path Packages/AppFeatures` → exit 0.
4. `cd BBTB && swift test --package-path Packages/DeepLinks` → exit 0, 3 tests passed.
5. Grep gate: `grep -rn "switch.*ImportSource" BBTB/Packages/ | grep -v ".build/" | grep -v "default:" | grep -v "case .deepLink" | wc -l` → must be `0`.
6. Document presence: `grep -E "DEEP-03.*Out of Scope.*v0.9" .planning/REQUIREMENTS.md | wc -l` → ≥ 1.
7. Document presence: `grep -E "Plans:.*4 plans" .planning/ROADMAP.md` → ≥ 1 match.
</verification>

<success_criteria>
1. ✅ `BBTB/Packages/DeepLinks/` SwiftPM пакет существует, собирается, тесты зелёные.
2. ✅ `ImportSource.deepLink` case добавлен, все switch sites compile-clean (Pitfall #3 audit clean).
3. ✅ Scope amendment явно зафиксирован в REQUIREMENTS.md (DEEP-03/04 strikethrough) и ROADMAP.md (Phase 9 entry with Plans list).
4. ✅ `DeepLinkRouter` actor + `DeepLinkHandler` protocol + `DeepLinkError` enum + `DeepLinksLogger` + `TokenFetcher` placeholder — все 5 source файлов public + Sendable-safe для Swift 6 strict concurrency.
5. ✅ Tests cover register-and-iterate semantics + registration-order semantics + unhandled throw semantics (3 tests, all PASS).
6. ✅ Threat model T-09-01..T-09-08 documented; W1 disposition per threat.
7. ✅ Codex consultation gap acknowledged inline (Codex MCP prompt file not found on machine); follow-up reminder per CLAUDE.md в Task 1.3 action notes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-deep-links/09-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md` template. Capture:
- Files created (paths).
- Tests added (count, names).
- Scope amendment text added to REQUIREMENTS.md / ROADMAP.md (diff snippet).
- Switch-exhaustiveness audit decisions (per-site).
- Codex consultation gap note (carry-forward for Wave 2 or post-phase).
- Any deviations from PATTERNS.md / RESEARCH.md (none expected; document if any).
</output>
