---
phase: 09-deep-links
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - BBTB/Packages/DeepLinks/Sources/DeepLinks/Handlers/ImportHandler.swift
  - BBTB/Packages/DeepLinks/Sources/DeepLinks/Handlers/RemoteTokenFetchHandler.swift
  - BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkError.swift
  - BBTB/Packages/DeepLinks/Tests/DeepLinksTests/ImportHandlerTests.swift
  - BBTB/Packages/DeepLinks/Tests/DeepLinksTests/URLParsingTests.swift
  - BBTB/Packages/Localization/Sources/Localization/Resources/Localizable.xcstrings
  - BBTB/Packages/Localization/Sources/Localization/L10n.swift
autonomous: true
requirements:
  - DEEP-01
  - DEEP-02
  - DEEP-03  # stub-only (RemoteTokenFetchHandler not registered)
  - DEEP-05

tags:
  - deep-links
  - url-parsing
  - l10n
  - import-handler

must_haves:
  truths:
    - "`ImportHandler` концретно accept'ит `bbtb://import?url=…` И `https://import.bbtb.app/import?…` (двусторонний path convergence per RESEARCH.md Pattern 5)"
    - "`ImportHandler.handle` extracts `url` query item, validates non-empty + URL-parseable, delegates to `ConfigImporting.importFromRawInput(_, source: .deepLink)`"
    - "`RemoteTokenFetchHandler` существует как public struct stub — `canHandle` всегда false, `handle` бросает `.notImplemented` (per D-03)"
    - "5 новых L10n keys (`alert.deep_link_error.title`, `deep_link.error.unhandled`, `deep_link.error.missing_parameter`, `deep_link.error.invalid_parameter`, `deep_link.error.import_failed`) добавлены ru+en"
    - "`DeepLinkError.errorDescription` использует L10n keys (не inline RU strings, swapped из Plan 01 fallback)"
    - "Unit tests для ImportHandler покрывают canHandle (4 scenarios: bbtb-import/UL/wrong-host/wrong-path) + handle (5 scenarios: success/missing-url/empty-url/invalid-url/importer-throws)"
    - "URLParsingTests покрывают percent-encoding edge cases (Pitfall #5 — double-encoded, plus signs, multibyte)"
  artifacts:
    - path: "BBTB/Packages/DeepLinks/Sources/DeepLinks/Handlers/ImportHandler.swift"
      provides: "public struct ImportHandler: DeepLinkHandler с DI'ed ConfigImporting + canHandle/handle"
      contains: "public struct ImportHandler: DeepLinkHandler"
    - path: "BBTB/Packages/DeepLinks/Sources/DeepLinks/Handlers/RemoteTokenFetchHandler.swift"
      provides: "public struct RemoteTokenFetchHandler — stub for v1+ DEEP-03"
      contains: "case .notImplemented\\|throw DeepLinkError.notImplemented"
    - path: "BBTB/Packages/DeepLinks/Tests/DeepLinksTests/ImportHandlerTests.swift"
      provides: "9+ tests — canHandle + handle full coverage"
      contains: "test_canHandle_\\|test_handle_"
    - path: "BBTB/Packages/DeepLinks/Tests/DeepLinksTests/URLParsingTests.swift"
      provides: "URLComponents edge case tests (Pitfall #5)"
      contains: "test_doubleEncoded\\|test_percentDecode"
    - path: "BBTB/Packages/Localization/Sources/Localization/Resources/Localizable.xcstrings"
      provides: "5 new deep-link L10n keys ru+en"
      contains: "alert.deep_link_error.title\\|deep_link.error.unhandled"
    - path: "BBTB/Packages/Localization/Sources/Localization/L10n.swift"
      provides: "L10n accessors для 5 new keys (split per Phase 6e L3 — launch-critical title как static let, error bodies как static var)"
      contains: "alertDeepLinkErrorTitle\\|deepLinkErrorUnhandled"
  key_links:
    - from: "ImportHandler.handle"
      to: "ConfigImporting.importFromRawInput"
      via: "DI'ed `importer` property"
      pattern: "importer\\.importFromRawInput\\(.*source: \\.deepLink"
    - from: "DeepLinkError.errorDescription"
      to: "L10n.deepLinkErrorUnhandled (+ 4 others)"
      via: "L10n accessor call в errorDescription switch"
      pattern: "L10n\\.deepLinkError"
---

<objective>
**Phase Goal (user story per MVP_MODE):**

**As a** разработчик BBTB, **I want to** иметь готовый `ImportHandler` который accept'ит ОБЕ deep-link дорожки (`bbtb://import?url=…` + Universal Link `https://import.bbtb.app/import?…`) и delegates импорт в существующий `ConfigImporter`, плюс v1+ stub для будущего token-flow, **so that** Wave 3 wiring just registers handlers в App.init без бизнес-логики, а в Wave 4 — manual UAT проверяет реальный e2e flow.

Purpose: Wave 2 — **concrete handlers + L10n + URL parsing tests.** Без actual App-wiring (это Wave 3) — handler работает изолированно через unit tests с fake `ConfigImporting`. Phase 9 vertical slice: после Wave 2 можно вызвать `try await router.handle(URL(string: "bbtb://import?url=https://panel.example.com/sub/abc")!)` из теста и получить корректную дисptch к stub-importer.

Output:
- `BBTB/Packages/DeepLinks/Sources/DeepLinks/Handlers/ImportHandler.swift` — concrete handler с DI'ed `ConfigImporting` dependency.
- `BBTB/Packages/DeepLinks/Sources/DeepLinks/Handlers/RemoteTokenFetchHandler.swift` — stub (per D-03), canHandle всегда false, handle throws `.notImplemented`.
- 5 новых L10n ключей добавлены в `Localizable.xcstrings` + accessors в `L10n.swift` (per UI-SPEC § Code Hooks table).
- `DeepLinkError.errorDescription` обновлён — inline RU strings из Plan 01 заменены на L10n accessor calls.
- `ImportHandlerTests.swift` + `URLParsingTests.swift` — ≥12 тестов покрывают все scenarios из VALIDATION.md Per-Task Verification Map (09-01-03..09-02-02).
- `swift test --package-path BBTB/Packages/DeepLinks` зелёный (3+9+тесты URLParsing = ~15 тестов).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-deep-links/09-CONTEXT.md
@.planning/phases/09-deep-links/09-RESEARCH.md
@.planning/phases/09-deep-links/09-PATTERNS.md
@.planning/phases/09-deep-links/09-UI-SPEC.md
@.planning/phases/09-deep-links/09-VALIDATION.md
@.planning/phases/09-deep-links/09-01-PLAN.md
@BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/ConfigImporting.swift
@BBTB/Packages/Localization/Sources/Localization/L10n.swift
@BBTB/Packages/Localization/Sources/Localization/Resources/Localizable.xcstrings

<interfaces>
<!-- Contracts извлечены из codebase; executor использует напрямую. -->

From BBTB/Packages/ConfigParser/Sources/ConfigParser/ConfigImporting.swift (line 26 — DI target for ImportHandler):
```swift
public protocol ConfigImporting: Sendable {
    func importFromRawInput(_ raw: String, source: ImportSource) async throws -> ImportResult
    // (other methods exist but ImportHandler uses only importFromRawInput)
}
```

From BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift (lines 27-49 — error pattern Reference; reuse `ImporterError: LocalizedError` localized strings via `.localizedDescription` rethrow).

From BBTB/Packages/Localization/Sources/Localization/L10n.swift (existing accessor pattern; verify file structure before editing):
- L10n enum or static struct with `static let` (launch-critical) и `static var` (lazy) accessors.
- Phase 6e Theme A established split: launch-critical strings as `static let`, otherwise `static var` (lazy computed property reading from xcstrings).

From BBTB/Packages/DeepLinks (Plan 01 result — already exists):
```swift
public protocol DeepLinkHandler: Sendable {
    static var identifier: String { get }
    func canHandle(_ url: URL) -> Bool
    func handle(_ url: URL) async throws
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.1: Add 5 L10n keys (xcstrings + L10n.swift) + swap DeepLinkError.errorDescription to use them</name>
  <files>
    BBTB/Packages/Localization/Sources/Localization/Resources/Localizable.xcstrings,
    BBTB/Packages/Localization/Sources/Localization/L10n.swift,
    BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkError.swift
  </files>
  <read_first>
    - BBTB/Packages/Localization/Sources/Localization/L10n.swift (full file — verify accessor split pattern: launch-critical `static let` vs lazy `static var`)
    - BBTB/Packages/Localization/Sources/Localization/Resources/Localizable.xcstrings (skim head — see existing key structure; xcstrings is JSON-formatted)
    - .planning/phases/09-deep-links/09-UI-SPEC.md § "Copywriting Contract" → "Element-by-element contract" table (exact RU + EN strings + 5 keys)
    - BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkError.swift (Plan 01 output — current errorDescription has inline RU strings; Wave 2 swaps to L10n)
  </read_first>
  <behavior>
    - `Localizable.xcstrings` contains 5 new keys with both ru + en values per UI-SPEC table:
        - `alert.deep_link_error.title` — "Не удалось открыть ссылку" / "Couldn't open link"
        - `deep_link.error.unhandled` — "Ссылка не поддерживается. Скопируйте подписку и импортируйте через меню «+»." / "This link isn't supported. Copy the subscription URL and import it via the + menu."
        - `deep_link.error.missing_parameter` (format `%@`) — "В ссылке не хватает параметра «%@». Попросите отправителя прислать ссылку заново." / "The link is missing the «%@» parameter. Ask the sender to share it again."
        - `deep_link.error.invalid_parameter` (format `%1$@`, `%2$@`) — "Параметр «%1$@» некорректен: %2$@. Попросите отправителя прислать ссылку заново." / "The «%1$@» parameter is invalid: %2$@. Ask the sender to share it again."
        - `deep_link.error.import_failed` (format `%@`) — "Импорт не удался: %@. Проверьте интернет и попробуйте ещё раз через меню «+»." / "Import failed: %@. Check your internet and try again via the + menu."
    - `L10n.swift` exposes 5 accessors:
        - `static let alertDeepLinkErrorTitle: String` (launch-critical — title может появиться сразу после cold-start; eager init per Phase 6e Theme A pattern для launch-critical).
        - `static var deepLinkErrorUnhandled: String { get }` (lazy).
        - `static func deepLinkErrorMissingParameter(_ name: String) -> String` (function для format substitution).
        - `static func deepLinkErrorInvalidParameter(name: String, reason: String) -> String`.
        - `static func deepLinkErrorImportFailed(_ underlying: String) -> String`.
    - `DeepLinkError.errorDescription` теперь использует L10n accessors вместо inline RU strings:
        ```swift
        public var errorDescription: String? {
            switch self {
            case .unhandled: return L10n.deepLinkErrorUnhandled
            case .missingQueryParameter(let name): return L10n.deepLinkErrorMissingParameter(name)
            case .invalidParameterValue(let name, let reason): return L10n.deepLinkErrorInvalidParameter(name: name, reason: reason)
            case .importFailed(let underlying): return L10n.deepLinkErrorImportFailed(underlying)
            case .notImplemented: return L10n.deepLinkErrorUnhandled  // fallback — v1+ stub
            }
        }
        ```
    - DeepLinks Package.swift gains `Localization` dependency (`.package(path: "../Localization")` + `dependencies: ["Localization"]` on target).
    - `swift test --package-path BBTB/Packages/DeepLinks` зелёный после правки imports в DeepLinkError.swift.
  </behavior>
  <action>
    1. Read `BBTB/Packages/Localization/Sources/Localization/L10n.swift` fully — observe existing pattern (find at least one `static let` and one `static var` example to mirror).

    2. Read `BBTB/Packages/Localization/Sources/Localization/Resources/Localizable.xcstrings` (Resources). Format is JSON with `"strings"` dict; each key has `"localizations"` with `"ru"` / `"en"` `"stringUnit": { "state": "translated", "value": "…" }`. Mirror an existing entry structure exactly.

    3. Edit `Localizable.xcstrings` — add 5 entries inside the top-level `"strings"` object. Preserve sort order if the file is sorted; otherwise append before the closing `}`. For format strings (containing `%@` or `%1$@`/`%2$@`), include `"substitutions"` block ONLY if the file's existing format-string entries use it; otherwise plain `"value"` is sufficient for `String(format: …, arg)` runtime substitution.

    4. Edit `L10n.swift` — append 5 accessor declarations near other "alert.*" / domain-grouped accessors. Pattern:
       ```swift
       /// Phase 9 / DEEP-08 — alert title (launch-critical: can show immediately after cold-start
       /// if user opens app via deep link with malformed URL).
       public static let alertDeepLinkErrorTitle = NSLocalizedString(
           "alert.deep_link_error.title", bundle: .module, comment: ""
       )

       /// Phase 9 — generic "unsupported link" body (lazy per Phase 6e Theme A).
       public static var deepLinkErrorUnhandled: String {
           NSLocalizedString("deep_link.error.unhandled", bundle: .module, comment: "")
       }

       public static func deepLinkErrorMissingParameter(_ name: String) -> String {
           let format = NSLocalizedString("deep_link.error.missing_parameter", bundle: .module, comment: "")
           return String(format: format, name)
       }

       public static func deepLinkErrorInvalidParameter(name: String, reason: String) -> String {
           let format = NSLocalizedString("deep_link.error.invalid_parameter", bundle: .module, comment: "")
           return String(format: format, name, reason)
       }

       public static func deepLinkErrorImportFailed(_ underlying: String) -> String {
           let format = NSLocalizedString("deep_link.error.import_failed", bundle: .module, comment: "")
           return String(format: format, underlying)
       }
       ```
       Match exact NSLocalizedString call signature from existing accessors in the file (the `.module` bundle reference IS the standard SwiftPM resource-bundle pattern; verify it matches existing accessors before assuming).

    5. Edit `BBTB/Packages/DeepLinks/Package.swift`:
       - Add `.package(path: "../Localization")` to `dependencies` array.
       - Add `"Localization"` to `.target` dependencies array.

    6. Edit `BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkError.swift`:
       - Add `import Localization` after `import Foundation`.
       - Replace `errorDescription` body with the L10n-backed switch (per behavior code block above).

    7. Run `cd BBTB && swift test --package-path Packages/DeepLinks` → MUST exit 0; 3 Plan 01 tests still PASS (DeepLinkRouterTests don't assert on RU strings, so L10n change is transparent).
  </action>
  <verify>
    <automated>cd BBTB && swift test --package-path Packages/DeepLinks 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `grep -c "alert.deep_link_error.title\|deep_link.error.unhandled\|deep_link.error.missing_parameter\|deep_link.error.invalid_parameter\|deep_link.error.import_failed" BBTB/Packages/Localization/Sources/Localization/Resources/Localizable.xcstrings` >= 5.
    - `grep -c "alertDeepLinkErrorTitle\|deepLinkErrorUnhandled\|deepLinkErrorMissingParameter\|deepLinkErrorInvalidParameter\|deepLinkErrorImportFailed" BBTB/Packages/Localization/Sources/Localization/L10n.swift` >= 5.
    - `grep -c "import Localization" BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkError.swift` == 1.
    - `grep -c "L10n.deepLinkError" BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkError.swift` >= 4.
    - `grep -c "package(path: \"../Localization\")" BBTB/Packages/DeepLinks/Package.swift` == 1.
    - `cd BBTB && swift test --package-path Packages/DeepLinks` exit 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.2: Implement ImportHandler + RemoteTokenFetchHandler stub + 9-test ImportHandlerTests</name>
  <files>
    BBTB/Packages/DeepLinks/Sources/DeepLinks/Handlers/ImportHandler.swift,
    BBTB/Packages/DeepLinks/Sources/DeepLinks/Handlers/RemoteTokenFetchHandler.swift,
    BBTB/Packages/DeepLinks/Tests/DeepLinksTests/ImportHandlerTests.swift
  </files>
  <read_first>
    - .planning/phases/09-deep-links/09-PATTERNS.md § 4 (ImportHandler full assembly) + § 5 (RemoteTokenFetchHandler stub) + § 9 (test patterns)
    - .planning/phases/09-deep-links/09-RESEARCH.md § "Pattern 5: ImportHandler" code block (Pitfall #5 double-encode guard в pattern)
    - .planning/phases/09-deep-links/09-VALIDATION.md Per-Task Verification Map rows 09-01-03..09-01-07 + 09-02-01..09-02-02 (test scenarios mandatory)
    - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift lines 27-49 (ImporterError shape — DeepLinkError.importFailed wraps `error.localizedDescription` per RESEARCH Pattern 5)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/ConfigImporting.swift (verify `importFromRawInput` signature)
    - BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkRouter.swift (Plan 01 output — verify `register(_:)` API signature)
  </read_first>
  <behavior>
    - `ImportHandler` is a public struct conforming to `DeepLinkHandler`:
        - `static let identifier = "import"`.
        - DI'ed `ConfigImporting` via init.
        - `canHandle(_:)` returns true for `bbtb://import` AND `https://import.bbtb.app/import*`. Case-insensitive scheme/host comparison.
        - `handle(_:)` parses `URLComponents`, extracts `url` query item, validates non-empty + `URL(string:)` parseable, delegates to `importer.importFromRawInput(rawValue, source: .deepLink)`. Wraps importer errors in `DeepLinkError.importFailed(underlying:)`.
    - `RemoteTokenFetchHandler` exists as public struct with DI'ed `TokenFetcher` + `ConfigImporting`; both `canHandle` returns false and `handle` throws `.notImplemented` (per D-03).
    - 9 ImportHandler tests + 3 URLParsingTests cover full scenario matrix from VALIDATION.md.
    - Fake `ConfigImporting` stub captures `importFromRawInput` calls для assertions (mirror FakeHandler approach in DeepLinkRouterTests).
  </behavior>
  <action>
    1. Create `BBTB/Packages/DeepLinks/Sources/DeepLinks/Handlers/` directory.

    2. **`Handlers/ImportHandler.swift`** — per PATTERNS.md § 4 + RESEARCH.md Pattern 5:
       ```swift
       import Foundation
       import ConfigParser
       import VPNCore

       /// Phase 9 / DEEP-01 + DEEP-02 — concrete handler для `bbtb://import?url=…`
       /// и `https://import.bbtb.app/import?…` deep links. Forwards extracted URL
       /// to existing `ConfigImporting.importFromRawInput` pipeline (Phase 2+ universal parser).
       public struct ImportHandler: DeepLinkHandler {
           public static let identifier = "import"

           private let importer: ConfigImporting

           public init(importer: ConfigImporting) {
               self.importer = importer
           }

           public func canHandle(_ url: URL) -> Bool {
               let scheme = url.scheme?.lowercased()
               // bbtb://import?url=…  → custom scheme
               if scheme == "bbtb", url.host?.lowercased() == "import" {
                   return true
               }
               // https://import.bbtb.app/import…  → Universal Link
               if scheme == "https",
                  url.host?.lowercased() == "import.bbtb.app",
                  url.path.hasPrefix("/import") {
                   return true
               }
               return false
           }

           public func handle(_ url: URL) async throws {
               DeepLinksLogger.importer.notice("ImportHandler.handle url=\(url.absoluteString, privacy: .public)")
               guard let components = URLComponents(url: url, resolvingAgainstBaseURL: false) else {
                   throw DeepLinkError.invalidParameterValue(name: "url", reason: "не URL")
               }
               guard let urlItem = components.queryItems?.first(where: { $0.name == "url" }),
                     let rawValue = urlItem.value, !rawValue.isEmpty
               else {
                   throw DeepLinkError.missingQueryParameter(name: "url")
               }
               // Defense against double-encoding (Pitfall #5).
               guard URL(string: rawValue) != nil else {
                   throw DeepLinkError.invalidParameterValue(name: "url", reason: "не похоже на URL")
               }
               do {
                   _ = try await importer.importFromRawInput(rawValue, source: .deepLink)
               } catch {
                   throw DeepLinkError.importFailed(underlying: error.localizedDescription)
               }
           }
       }
       ```

    3. **`Handlers/RemoteTokenFetchHandler.swift`** — per PATTERNS.md § 5:
       ```swift
       import Foundation
       import ConfigParser

       /// Phase 9 / D-03 — STUB for v1+ DEEP-03 token endpoint.
       /// NOT registered with DeepLinkRouter в v0.9. Когда в v1+ появится
       /// `GET /c/{token}`, planner реализует body через `TokenFetcher` protocol.
       public struct RemoteTokenFetchHandler: DeepLinkHandler {
           public static let identifier = "remote-token-fetch"

           private let tokenFetcher: TokenFetcher
           private let importer: ConfigImporting

           public init(tokenFetcher: TokenFetcher, importer: ConfigImporting) {
               self.tokenFetcher = tokenFetcher
               self.importer = importer
           }

           public func canHandle(_ url: URL) -> Bool {
               // v0.9 stub — НИКОГДА не matching (DEEP-03 deferred к v1+).
               return false
           }

           public func handle(_ url: URL) async throws {
               // TODO(v1+ DEEP-03): extract token from path, call tokenFetcher.fetchConfig,
               // forward raw config to importer.importFromRawInput(_, source: .deepLink).
               throw DeepLinkError.notImplemented
           }
       }
       ```

    4. **`Tests/DeepLinksTests/ImportHandlerTests.swift`** — 9 tests (VALIDATION.md rows 09-01-03..09-01-07 + 09-02-01..09-02-02 + register-into-router happy path):
       ```swift
       import XCTest
       @testable import DeepLinks
       import ConfigParser
       import VPNCore

       final class ImportHandlerTests: XCTestCase {
           // Fake ConfigImporting capturing calls.
           final class FakeImporter: ConfigImporting, @unchecked Sendable {
               var capturedInput: String?
               var capturedSource: ImportSource?
               var stubResult: ImportResult?
               var stubError: Error?

               func importFromRawInput(_ raw: String, source: ImportSource) async throws -> ImportResult {
                   capturedInput = raw
                   capturedSource = source
                   if let err = stubError { throw err }
                   guard let result = stubResult else {
                       throw NSError(domain: "FakeImporter", code: -1)
                   }
                   return result
               }
               // ... stub other ConfigImporting methods as needed (return defaults / throw "not implemented")
               // VERIFY actual ConfigImporting protocol members in source-of-truth before final stub.
           }

           // 1. canHandle — bbtb://import → true
           func test_canHandle_bbtbImport_returnsTrue() throws {
               let h = ImportHandler(importer: FakeImporter())
               XCTAssertTrue(h.canHandle(URL(string: "bbtb://import?url=https://example.com")!))
           }

           // 2. canHandle — bbtb://other → false
           func test_canHandle_unknownScheme_returnsFalse() throws {
               let h = ImportHandler(importer: FakeImporter())
               XCTAssertFalse(h.canHandle(URL(string: "bbtb://connect")!))
               XCTAssertFalse(h.canHandle(URL(string: "vless://abc@host:443")!))
           }

           // 3. canHandle — Universal Link → true
           func test_canHandle_universalLink_returnsTrue() throws {
               let h = ImportHandler(importer: FakeImporter())
               XCTAssertTrue(h.canHandle(URL(string: "https://import.bbtb.app/import?url=https://example.com")!))
               XCTAssertTrue(h.canHandle(URL(string: "https://import.bbtb.app/import/anything?x=1")!))
           }

           // 4. canHandle — Universal Link wrong host/path → false
           func test_canHandle_otherPath_returnsFalse() throws {
               let h = ImportHandler(importer: FakeImporter())
               XCTAssertFalse(h.canHandle(URL(string: "https://import.bbtb.app/landing")!))
               XCTAssertFalse(h.canHandle(URL(string: "https://other.bbtb.app/import")!))
               XCTAssertFalse(h.canHandle(URL(string: "http://import.bbtb.app/import?x=y")!))  // http, not https
           }

           // 5. handle — success → calls importer с decoded URL + source=.deepLink
           func test_handle_callsImporter_withDecodedURL_andDeepLinkSource() async throws {
               let importer = FakeImporter()
               // Use a "successful" ImportResult shape — construct minimal valid value or
               // make FakeImporter return Result<Void> if ImportResult is not constructible.
               importer.stubResult = makeEmptyImportResult()  // helper — see § 6 below.
               let h = ImportHandler(importer: importer)
               let encoded = "https%3A%2F%2Fpanel.example.com%2Fsub%2Fabc"
               let url = URL(string: "bbtb://import?url=\(encoded)")!
               try await h.handle(url)
               XCTAssertEqual(importer.capturedInput, "https://panel.example.com/sub/abc")
               XCTAssertEqual(importer.capturedSource, .deepLink)
           }

           // 6. handle — missing `url` → throws .missingQueryParameter
           func test_handle_missingURL_throws() async throws {
               let h = ImportHandler(importer: FakeImporter())
               do {
                   try await h.handle(URL(string: "bbtb://import")!)
                   XCTFail("expected throw")
               } catch let err as DeepLinkError {
                   guard case .missingQueryParameter(let name) = err else { XCTFail("wrong case: \(err)"); return }
                   XCTAssertEqual(name, "url")
               }
           }

           // 7. handle — empty `url=` → throws .missingQueryParameter
           func test_handle_emptyURL_throws() async throws {
               let h = ImportHandler(importer: FakeImporter())
               do {
                   try await h.handle(URL(string: "bbtb://import?url=")!)
                   XCTFail("expected throw")
               } catch let err as DeepLinkError {
                   guard case .missingQueryParameter = err else { XCTFail("wrong case: \(err)"); return }
               }
           }

           // 8. handle — value не URL → throws .invalidParameterValue
           func test_handle_invalidURL_throws() async throws {
               let h = ImportHandler(importer: FakeImporter())
               do {
                   // URL with embedded space — `URL(string: ...)` returns nil.
                   try await h.handle(URL(string: "bbtb://import?url=not%20a%20valid url")!)
                   XCTFail("expected throw")
               } catch let err as DeepLinkError {
                   guard case .invalidParameterValue(let name, _) = err else { XCTFail("wrong case: \(err)"); return }
                   XCTAssertEqual(name, "url")
               }
           }

           // 9. handle — importer throws → wraps в .importFailed
           func test_handle_importerThrows_wrapsAsImportFailed() async throws {
               let importer = FakeImporter()
               importer.stubError = NSError(domain: "test", code: 42, userInfo: [NSLocalizedDescriptionKey: "test-error"])
               let h = ImportHandler(importer: importer)
               do {
                   try await h.handle(URL(string: "bbtb://import?url=https%3A%2F%2Fexample.com")!)
                   XCTFail("expected throw")
               } catch let err as DeepLinkError {
                   guard case .importFailed(let underlying) = err else { XCTFail("wrong case: \(err)"); return }
                   XCTAssertEqual(underlying, "test-error")
               }
           }

           // Helper.
           private func makeEmptyImportResult() -> ImportResult {
               // Per VPNCore ImportResult init signature — construct minimal valid empty result.
               // Inspect ImportResult in BBTB/Packages/VPNCore (or wherever defined) and fill in.
               // If unconstructible, refactor FakeImporter to not return ImportResult (change signature
               // в tests-only protocol extension OR use Sendable empty value through async-throws fallthrough).
               fatalError("Replace with actual ImportResult init when executor reads source")
           }
       }
       ```

       **Executor note:** the `makeEmptyImportResult()` helper above is a placeholder. The executor MUST:
       (a) Read `ImportResult` definition in `BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift` (or wherever defined; grep `public struct ImportResult` or `public typealias ImportResult`).
       (b) Construct a minimal valid `ImportResult` value (empty servers / empty pool / placeholder metadata).
       (c) Replace the `fatalError("...")` body with the actual construction.
       (d) Verify FakeImporter conforms to FULL `ConfigImporting` protocol — stub additional protocol methods with `fatalError("not used in test")` for safety + Sendable conformance handled via `@unchecked Sendable`.

    5. Run `cd BBTB && swift test --package-path Packages/DeepLinks` → MUST exit 0; 3 + 9 = 12 tests PASS (Plan 01 + Plan 02 Task 2.2 combined).
  </action>
  <verify>
    <automated>cd BBTB && swift test --package-path Packages/DeepLinks --filter ImportHandlerTests 2>&1 | tail -15</automated>
  </verify>
  <done>
    - `grep -c "public struct ImportHandler: DeepLinkHandler" BBTB/Packages/DeepLinks/Sources/DeepLinks/Handlers/ImportHandler.swift` == 1.
    - `grep -c "public struct RemoteTokenFetchHandler: DeepLinkHandler" BBTB/Packages/DeepLinks/Sources/DeepLinks/Handlers/RemoteTokenFetchHandler.swift` == 1.
    - `grep -c "case .notImplemented\|case notImplemented" BBTB/Packages/DeepLinks/Sources/DeepLinks/Handlers/RemoteTokenFetchHandler.swift` >= 1.
    - `grep -c "source: \\.deepLink" BBTB/Packages/DeepLinks/Sources/DeepLinks/Handlers/ImportHandler.swift` >= 1.
    - `cd BBTB && swift test --package-path Packages/DeepLinks --filter ImportHandlerTests` exit 0, all 9 tests PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.3: Add URLParsingTests covering Pitfall #5 edge cases (percent-encoding, double-encode, multibyte, plus signs)</name>
  <files>BBTB/Packages/DeepLinks/Tests/DeepLinksTests/URLParsingTests.swift</files>
  <read_first>
    - .planning/phases/09-deep-links/09-RESEARCH.md § "Pitfall 5: bbtb://import?url=… с уже-encoded URL — двойное декодирование"
    - .planning/phases/09-deep-links/09-VALIDATION.md "Wave 0 Requirements" mention URLParsingTests требование
    - BBTB/Packages/DeepLinks/Sources/DeepLinks/Handlers/ImportHandler.swift (Task 2.2 output — verify canHandle/handle semantics for edge-case input)
  </read_first>
  <behavior>
    - 5 unit tests covering URL parsing edge cases:
        1. Standard percent-encoded URL → auto-decoded correctly by URLComponents.
        2. Double-encoded URL (e.g. `https%253A%252F%252F...`) → first decode gives `https%3A%2F%2F...` which IS a valid URL string ("URL(string:)" non-nil) so passes ImportHandler validation — verify it forwards to importer as-is (importer pipeline handles or rejects per IMPORT-* requirements; deep link layer doesn't double-decode).
        3. URL containing `+` (plus sign — query encoding for space in form-data, but URLComponents handles it depending on context — test for behavioral consistency).
        4. URL with multibyte (Cyrillic, emoji) → URLComponents respects RFC 3986 percent encoding for non-ASCII.
        5. Empty query string vs missing query string → both throw `.missingQueryParameter`.
    - All 5 tests PASS.
  </behavior>
  <action>
    Create `BBTB/Packages/DeepLinks/Tests/DeepLinksTests/URLParsingTests.swift`:

    ```swift
    import XCTest
    @testable import DeepLinks
    import ConfigParser
    import VPNCore

    final class URLParsingTests: XCTestCase {
        // Reuse FakeImporter from ImportHandlerTests — duplicate inline for test isolation
        // (XCTest may not share private types across files; use minimal nested fake).
        final class CaptureImporter: ConfigImporting, @unchecked Sendable {
            var captured: String?
            func importFromRawInput(_ raw: String, source: ImportSource) async throws -> ImportResult {
                captured = raw
                // Same placeholder note as ImportHandlerTests: executor constructs ImportResult per VPNCore.
                fatalError("Replace with actual ImportResult init in executor pass")
            }
            // Stub remaining ConfigImporting methods with fatalError("not used").
        }

        // 1. Standard percent-encoding — URLComponents auto-decodes.
        func test_standardPercentEncoded_decodesToOriginalURL() async throws {
            let importer = CaptureImporter()
            let h = ImportHandler(importer: importer)
            let original = "https://panel.example.com/sub/abc?token=xyz"
            let encoded = original.addingPercentEncoding(withAllowedCharacters: .urlQueryAllowed)!
            let url = URL(string: "bbtb://import?url=\(encoded)")!
            // Note: importer would throw fatalError post-capture — wrap in try? to capture-then-stop.
            _ = try? await h.handle(url)
            XCTAssertEqual(importer.captured, original)
        }

        // 2. Double-encoded — first decode preserves intermediate form (intentional per Pitfall #5).
        func test_doubleEncodedURL_singleDecodeOnly() async throws {
            let importer = CaptureImporter()
            let h = ImportHandler(importer: importer)
            // `https%3A%2F%2Fexample.com` already once-encoded.
            // Wrapping в дополнительный encoder делает `https%253A%252F%252Fexample.com`.
            let outerEncoded = "https%253A%252F%252Fexample.com"
            let url = URL(string: "bbtb://import?url=\(outerEncoded)")!
            _ = try? await h.handle(url)
            // After URLComponents.queryItems decode: `https%3A%2F%2Fexample.com`.
            // ImportHandler does NOT decode again (per RESEARCH.md "НЕ делать double-decode").
            // URL(string: "https%3A%2F%2Fexample.com") returns nil (percent triplet не valid в host),
            // so handle() throws .invalidParameterValue.
            // Verify captured is nil (handler did not reach importer):
            XCTAssertNil(importer.captured)
        }

        // 3. Plus sign in query — URLComponents preserves as `+`, not decoded as space.
        func test_plusSignInQueryValue_preservedAsPlus() async throws {
            let importer = CaptureImporter()
            let h = ImportHandler(importer: importer)
            let url = URL(string: "bbtb://import?url=https%3A%2F%2Fexample.com%2Fpath%3Fa%3D1%2B2")!
            _ = try? await h.handle(url)
            XCTAssertEqual(importer.captured, "https://example.com/path?a=1+2")
        }

        // 4. Multibyte (Cyrillic) percent-encoded host segment — auto-decode preserves UTF-8.
        func test_multibytePercentEncoded_decodesUTF8() async throws {
            let importer = CaptureImporter()
            let h = ImportHandler(importer: importer)
            // Cyrillic "тест" UTF-8 encoded.
            let original = "https://example.com/тест"
            let encoded = original.addingPercentEncoding(withAllowedCharacters: .urlQueryAllowed)!
            let url = URL(string: "bbtb://import?url=\(encoded)")!
            _ = try? await h.handle(url)
            XCTAssertEqual(importer.captured, original)
        }

        // 5. Empty `?` (no query items) vs missing `?` — both throw .missingQueryParameter.
        func test_emptyQueryString_throwsMissingParam() async throws {
            let h = ImportHandler(importer: CaptureImporter())
            do {
                try await h.handle(URL(string: "bbtb://import?")!)
                XCTFail("expected throw")
            } catch let err as DeepLinkError {
                guard case .missingQueryParameter = err else { XCTFail("wrong case: \(err)"); return }
            }
        }
    }
    ```

    Same executor note as Task 2.2 applies — replace `fatalError("Replace with actual ImportResult init")` with real ImportResult initialization once that type is read.

    Run `cd BBTB && swift test --package-path Packages/DeepLinks` — MUST be green, total tests = 3 (router) + 9 (import handler) + 5 (URL parsing) = ~17 tests passing.
  </action>
  <verify>
    <automated>cd BBTB && swift test --package-path Packages/DeepLinks --filter URLParsingTests 2>&1 | tail -15</automated>
  </verify>
  <done>
    - `grep -c "test_standardPercentEncoded_decodesToOriginalURL\|test_doubleEncodedURL_singleDecodeOnly\|test_plusSignInQueryValue\|test_multibytePercentEncoded\|test_emptyQueryString" BBTB/Packages/DeepLinks/Tests/DeepLinksTests/URLParsingTests.swift` >= 5.
    - `cd BBTB && swift test --package-path Packages/DeepLinks --filter URLParsingTests` exit 0; 5 tests PASS.
    - Full suite still green: `cd BBTB && swift test --package-path Packages/DeepLinks` exit 0, 17 total tests PASS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External URL → ImportHandler.canHandle | URL может быть произвольным input (string-derived). Validation: pattern-match scheme/host/path constants. No-IO predicate. |
| External URL → ImportHandler.handle → ConfigImporting.importFromRawInput | URL extracted query value forwarded to Phase 2-hardened importer pipeline. Importer has its own SSRF + size + redirect guards (SubscriptionURLFetcher, ConfigImporter validation). |
| L10n strings → SwiftUI alert presentation | Format-substituted strings used in user-facing alerts. Format args (URL substrings) reach view layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | ImportHandler.canHandle scheme/host match | mitigate | Case-insensitive but exact match on `"bbtb"` scheme + `"import"` host (custom path) OR `"https"` scheme + `"import.bbtb.app"` host + path prefix `/import`. Universal Link path covered by AASA enforcement (Apple CDN — Wave 4). Custom scheme path has known limitation (T-09-01 Pitfall #6 — другое приложение может зарегистрировать `bbtb://` schedule) — documented в FAQ, mitigated through Universal Link primary path. |
| T-09-02 | Tampering | URL query `url` parameter parsing | mitigate | URLComponents auto-decodes per RFC 3986. Post-decode `URL(string:)` re-validation rejects malformed input (Pitfall #5 double-encode case). Empty rejection throws `.missingQueryParameter`. ImporterError downstream rethrown via `.importFailed` — no info leak в error description (используется `error.localizedDescription` который уже sanitized). |
| T-09-03 | Repudiation | DeepLinksLogger.importer notice traces | accept | `notice` level = always captured. URL string at `privacy: .public` shows only scheme+host (Foundation URL pretty-print masks query value за privacy boundary; subscription token bodies НЕ должны быть в Console — verify в W3 UAT). Если в production случается info-leak — fix как hotfix через `privacy: .private` swap. |
| T-09-04 | Information Disclosure | DeepLinkError.errorDescription via L10n format substitution | mitigate | `%@` arg substitution использует pre-sanitized values: `name` (always literal `"url"`); `reason` (literal predicate strings — никогда не contains user input); `underlying` (already-localized importer error description). No URL bodies pass into format args. Verify в Task 2.2 review. |
| T-09-05 | DoS | URLComponents parsing | accept | Foundation framework — Apple-hardened. No path для unbounded recursion or pathological input. |
| T-09-06 | EoP | RemoteTokenFetchHandler stub | mitigate | `canHandle` всегда false → `handle` НИКОГДА не вызывается from router. Even if called directly (e.g., test forgets canHandle gate), `handle` throws `.notImplemented` immediately — no side effect. |
| T-09-07 | Tampering | ImportHandler delegates to importer | mitigate | Importer pipeline is Phase 2+ hardened (SSRF guard, max body size, redirect cap, HTTPS-only). Phase 9 deep link layer adds no new trust — just routes pre-validated URL into pipeline. |
| T-09-08 | Information Disclosure | Cyrillic / multibyte test fixtures | mitigate | URLParsingTests uses static fixture URLs — no real subscription tokens. Tests don't import from actual SwiftData/Keychain (FakeImporter captures without persistence). |

**Block-on:** none — all mitigated or accepted with documented rationale.
</threat_model>

<verification>
**Plan-level verification:**

1. `cd BBTB && swift test --package-path Packages/DeepLinks` → exit 0, ≥17 tests PASS (3 router + 9 import + 5 URL parsing).
2. `cd BBTB && swift build --package-path Packages/Localization` → exit 0 (xcstrings still valid after edits).
3. `cd BBTB && swift build --package-path Packages/AppFeatures` → exit 0 (Localization change is additive — no consumer break).
4. Grep gate: `grep -c "L10n.deepLinkError" BBTB/Packages/DeepLinks/Sources/DeepLinks/DeepLinkError.swift` >= 4.
5. Grep gate: `grep -c "case deepLink\|.deepLink" BBTB/Packages/DeepLinks/Sources/DeepLinks/Handlers/ImportHandler.swift` >= 1.
</verification>

<success_criteria>
1. ✅ `ImportHandler` accepts both `bbtb://import?url=…` и `https://import.bbtb.app/import…` paths.
2. ✅ `ImportHandler.handle` delegates to `ConfigImporting.importFromRawInput` с `source: .deepLink`.
3. ✅ `RemoteTokenFetchHandler` exists as v1+ stub; throws `.notImplemented` если используется.
4. ✅ 5 L10n keys (xcstrings ru+en + L10n.swift accessors) добавлены per UI-SPEC Copywriting Contract.
5. ✅ `DeepLinkError.errorDescription` использует L10n (no inline RU strings).
6. ✅ ≥17 tests зелёные (3 Plan-01 + 9 ImportHandler + 5 URL parsing).
7. ✅ Threat model T-09-01..T-09-08 documented; W2 disposition per threat.
</success_criteria>

<output>
After completion, create `.planning/phases/09-deep-links/09-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md` template:
- New files (paths + line counts).
- Tests added (count, names, scenarios from VALIDATION.md).
- L10n keys added (5 keys ru+en).
- DeepLinkError swap to L10n (diff fragment).
- Any RESEARCH/PATTERNS deviations.
</output>
