---
phase: 10
plan: 06
type: execute
wave: 4
depends_on: [10-01, 10-02, 10-03, 10-04, 10-05]
files_modified:
  - BBTB/Project.swift
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift
  - BBTB/Packages/AppFeatures/Package.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - wiki/anti-dpi-techniques.md
  - wiki/architecture.md
  - wiki/security-gaps.md
  - wiki/advanced-settings.md
  - wiki/cdn-fronting-architecture-2026.md
  - wiki/cdn-fronting-server-handoff.md
  - wiki/cert-pinning-spki.md
  - wiki/log.md
  - wiki/index.md
autonomous: true
requirements: [UX-06, DPI-05, DPI-06, DPI-08, DPI-09, BIO-04, KILL-04]
tags: [integration, ui-wiring, tuist, pool-builder, wiki, phase-closure]

must_haves:
  truths:
    - "Tuist Project.swift содержит .package(path: Packages/FrontingEngine) — FrontingEngine wired в Xcode workspace"
    - "PoolBuilder.buildSingBoxJSON применяет utlsFingerprint из App Group UserDefaults ко всем TLS-протокольным outbound (vless / trojan) если non-default установлен"
    - "ConfigImporter.provisionTunnelProfile при cdnFrontingEnabled == true и наличии frontingProfile в server config вызывает FrontingConfigApplier.apply на JSON до вызова tunnelProvisioner.provisionTunnelProfile (infrastructure-only: extractFrontingProfile возвращает nil во всех серверах v0.10 до admin-side activation в Phase 11)"
    - "Все Phase 10 toggle UX поверхности работают end-to-end: переключение → @AppStorage → next connect или live-apply → реальный effect на tunnel"
    - "REQUIREMENTS.md помечены [x] Validated для UX-06, DPI-05, DPI-08, DPI-09, BIO-04, KILL-04 (code-validated; manual UAT pending). DPI-06 помечен как `infrastructure-validated, activation pending` — НЕ [x] Validated (см. Plan 06 Task 1 + Task 2 + revision 2026-05-15 checker scope_reduction blocker resolution)"
    - "ROADMAP.md Phase 10 entry помечен как 'Implementation complete — UAT pending' с указанием версии v0.10"
    - "STATE.md обновлён: progress.completed_phases incremented (Phase 10 + 1 если предыдущая фаза была завершена), milestone v0.10"
    - "Wiki файлы созданы или обновлены: anti-dpi-techniques.md (Mux + uTLS picker UI), security-gaps.md (новые R-записи), advanced-settings.md (новый файл с layout D-15), cdn-fronting-architecture-2026.md (новый — D-03..D-07 архитектура), cdn-fronting-server-handoff.md (новый — admin handoff), cert-pinning-spki.md (новый — DPI-08), log.md (daily entry)"
    - "Memory создана: project_phase12_subscription_pins_prerequisite.md — placeholder pins требуют генерации перед TestFlight"
    - "Wiki/index.md обновлён ссылками на новые страницы"
  artifacts:
    - path: "BBTB/Project.swift"
      provides: "FrontingEngine добавлен в external packages list + linked в main app target dependencies"
      contains: "FrontingEngine"
    - path: "BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift"
      provides: "buildSingBoxJSON читает utlsFingerprint из App Group UserDefaults; применяет к outbound.tls.utls.fingerprint для VLESS/Trojan"
      contains: "utlsFingerprint"
    - path: "BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift"
      provides: "provisionTunnelProfile(for:) применяет FrontingConfigApplier на singbox JSON когда cdnFrontingEnabled toggle ON и frontingProfile parsed из ServerConfig"
      contains: "FrontingConfigApplier"
    - path: "BBTB/Packages/AppFeatures/Package.swift"
      provides: "FrontingEngine dependency added к AppFeatures для ConfigImporter access"
      contains: "FrontingEngine"
    - path: "wiki/anti-dpi-techniques.md"
      provides: "Обновление: Mux UI toggle, uTLS picker, STUN block toggle добавлены в раздел «Реализация в BBTB v0.10»"
      contains: "v0.10"
    - path: "wiki/advanced-settings.md"
      provides: "Новая страница с описанием 5-секционного Advanced Settings экрана (D-15 layout) + список всех toggle"
      contains: "D-15"
    - path: "wiki/cdn-fronting-architecture-2026.md"
      provides: "Новая страница — D-03..D-07 архитектура FrontingEngine + sing-box mapping + Cloudflare classic fronting deferral note"
      contains: "FrontingProfile"
    - path: "wiki/cdn-fronting-server-handoff.md"
      provides: "Новая страница — admin instructions для Marzban: как добавить frontingProfile в subscription payload + Cloudflare Worker setup"
      contains: "Marzban"
    - path: "wiki/cert-pinning-spki.md"
      provides: "Новая страница — DPI-08 SPKI SHA-256 pinning + scripts/generate-spki-pin.swift usage + Phase 12 pin rotation procedure"
      contains: "SecKeyCopyExternalRepresentation"
    - path: "wiki/log.md"
      provides: "Daily entry 2026-05-15: Phase 10 closure"
      contains: "Phase 10"
  key_links:
    - from: "BBTB/Project.swift"
      to: "BBTB/Packages/FrontingEngine"
      via: ".package(path: .relativeToManifest(\"Packages/FrontingEngine\"))"
      pattern: "Packages/FrontingEngine"
    - from: "ConfigImporter.provisionTunnelProfile"
      to: "FrontingConfigApplier.apply"
      via: "import FrontingEngine + try FrontingConfigApplier.apply(json:profile:adapter:)"
      pattern: "FrontingConfigApplier"
    - from: "PoolBuilder.buildSingBoxJSON"
      to: "App Group UserDefaults app.bbtb.utlsFingerprint"
      via: "UserDefaults(suiteName:).string(forKey:)"
      pattern: "utlsFingerprint"
---

<objective>
Wave 4 (final integration) — связываем всё построенное на Wave 1-3 в один production pipeline и закрываем Phase 10:

1. **Tuist wire** — добавить FrontingEngine package в Project.swift; AppFeatures package depends on FrontingEngine для ConfigImporter access.
2. **PoolBuilder uTLS application** — buildSingBoxJSON применяет utlsFingerprint из @AppStorage к outbound.tls.utls.fingerprint полю для VLESS / Trojan / TUIC TLS-протоколов.
3. **ConfigImporter CDN apply** — если `cdnFrontingEnabled == true` И server config содержит `frontingProfile` blob → вызвать FrontingConfigApplier.apply на JSON ДО передачи tunnelProvisioner.provisionTunnelProfile.
4. **Phase closure**: REQUIREMENTS.md mark Validated; ROADMAP.md mark complete; STATE.md milestone v0.10; SUMMARY.
5. **Wiki sync** (CLAUDE.md обязательное правило — каждое архитектурное решение фиксируется в wiki):
   - Обновить `anti-dpi-techniques.md` — Mux/uTLS/STUN UI shipped в v0.10.
   - Создать `advanced-settings.md` (новая страница) — D-15 layout + список toggle.
   - Создать `cdn-fronting-architecture-2026.md` (новая) — D-03..D-07 + Cloudflare classic deferral.
   - Создать `cdn-fronting-server-handoff.md` (новая) — admin instructions для Marzban CDN config.
   - Создать `cert-pinning-spki.md` (новая) — DPI-08 + generate-spki-pin.swift workflow + rotation procedure.
   - Обновить `architecture.md` — добавить FrontingEngine + PinStore в diagram.
   - Обновить `security-gaps.md` — новые R-записи (R20+ для cert pinning, STUN, enforceRoutes, CDN).
   - Обновить `index.md` — ссылки на новые страницы.
   - Обновить `log.md` — daily entry с phase closure.
6. **Memory create** — `project_phase12_subscription_pins_prerequisite.md` — placeholder pins требуют замены через scripts/generate-spki-pin.swift до TestFlight upload.

Purpose: после Wave 4 пользователь имеет полный, end-to-end working v0.10. Все 7 Phase 10 фичей шипают; toggle переключения = реальные effects на следующем connect; cert pinning защищает subscription endpoint от MITM; CDN fronting активен для серверов с frontingProfile.

Output: ~6 modified source files + ~8 wiki файлов (5 new + 3 modified) + 1 memory + closure-уровневые updates в .planning/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-advanced-settings-security-polish/10-CONTEXT.md
@.planning/phases/10-advanced-settings-security-polish/10-RESEARCH.md
@.planning/phases/10-advanced-settings-security-polish/10-PATTERNS.md
@.planning/phases/10-advanced-settings-security-polish/10-01-SUMMARY.md
@.planning/phases/10-advanced-settings-security-polish/10-02-SUMMARY.md
@.planning/phases/10-advanced-settings-security-polish/10-03-SUMMARY.md
@.planning/phases/10-advanced-settings-security-polish/10-04-SUMMARY.md
@.planning/phases/10-advanced-settings-security-polish/10-05-SUMMARY.md

<interfaces>
From `BBTB/Project.swift` (line 32-54):
- `external_packages` массив с 18+ `.package(path: .relativeToManifest("Packages/<name>"))` entries.
- Targets `BBTB-iOS` / `BBTB-macOS` / `BBTB-iOS-tunnel` / `BBTB-macOS-tunnel` / `BBTB-test` etc. имеют dependencies секции с product references.
- Phase 10 adds: `.package(path: .relativeToManifest("Packages/FrontingEngine"))` в external_packages; в main app target dependencies добавить `.external(name: "FrontingEngine")` (либо product references как принято в проекте — executor должен скопировать syntax existing package).
- Tuist regenerate command: `tuist generate` after Project.swift modification.

From `BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift`:
- `public static func buildSingBoxJSON(from parsedConfigs: [AnyParsedConfig], dns: DNSConfig = .default) throws -> String` — главная функция building.
- Внутри строит outbounds. Для VLESS / Trojan TLS outbounds — устанавливает `tls.utls.fingerprint` (значение приходит из URI / Clash config либо default Phase 7a "random"). Phase 10 добавляет: после URI/Clash override проверяем App Group UserDefaults `app.bbtb.utlsFingerprint` — если non-default ("random") set by user → override в outbound JSON.

From `BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift`:
- Line 471: `public func provisionTunnelProfile(for selectedID: UUID?) async throws` — основная entry point.
- Line 595: `json = try PoolBuilder.buildSingBoxJSON(from: parsedList, dns: dns)` — build sing-box JSON.
- Line 621: `try await tunnelProvisioner.provisionTunnelProfile(configJSON: json, serverHost: serverHost)` — pass to provisioner.
- Phase 10 insertion point: МЕЖДУ buildSingBoxJSON и provisionTunnelProfile — если `cdnFrontingEnabled == true` И у выбранного server есть `frontingProfile` (в ServerConfig либо в parsedConfig metadata — see scope decision below) → `json = try FrontingConfigApplier.apply(json: json, profile: profile, adapter: <chosen>.self)`.

**FrontingProfile source**: Phase 10 client-side готов к получению FrontingProfile, но **server-side delivery в subscription payload — Phase 10 admin handoff (wiki/cdn-fronting-server-handoff.md)**, не в scope клиентского кода. В Phase 10 implementation:
- ServerConfig SwiftData model: добавить optional `frontingProfileJSON: String?` field либо (проще) Marzban admin делает inline в subscription URI как extension parameter. **Discretion of executor**: добавить parsing infrastructure но **в v0.10 frontingProfile может быть nil для всех серверов** — toggle silently no-op'нется (документировать в footer text). Это согласуется с RESEARCH.md A6 (admin handoff item).
- Минимальная implementation: добавить `parseFrontingProfile(from: String?) -> FrontingProfile?` helper в ConfigImporter; default nil = no CDN apply. Real wiring activates когда admin populates field.

From `BBTB/Packages/AppFeatures/Package.swift`:
- Текущие dependencies. Phase 10 adds `.package(path: "../FrontingEngine")` + target dependency `"FrontingEngine"` для MainScreenFeature target (либо AppFeatures сводный target если monolithic).

From wiki structure (`wiki/index.md`):
- Index имеет sections по доменам. Phase 10 wiki pages должны быть добавлены в правильные секции (security, anti-DPI, architecture).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tuist wire + PoolBuilder uTLS application + ConfigImporter CDN apply + AppFeatures Package.swift + integration tests</name>
  <files>BBTB/Project.swift, BBTB/Packages/AppFeatures/Package.swift, BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift, BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift</files>
  <read_first>
    - BBTB/Project.swift (целиком — точный синтаксис добавления package + target dependency)
    - BBTB/Packages/AppFeatures/Package.swift (целиком — текущие deps, target structure)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift (целиком — точное место где tls.utls.fingerprint устанавливается per-protocol)
    - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift lines 460-630 (provisionTunnelProfile целиком)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift (целиком — existing test pattern)
    - 10-02-SUMMARY.md, 10-03-SUMMARY.md, 10-04-SUMMARY.md, 10-05-SUMMARY.md (как Wave 2-3 закончились — чтобы понять текущее состояние всех файлов)
    - .planning/phases/10-advanced-settings-security-polish/10-RESEARCH.md §Architecture (data flow primary use case lines 256-266) + §Pitfall 6 (NEVPNStatusDidChange XPC — не subscribe из CDN logic)
    - .planning/phases/10-advanced-settings-security-polish/10-CONTEXT.md D-07 (CDN toggle = глобальный) + RESEARCH.md A6 (admin handoff timing)
  </read_first>
  <behavior>
    - Test 1 (PoolBuilderTests): `test_buildSingBoxJSON_applies_utls_picker_from_app_group_userDefaults` — записать в App Group suite `app.bbtb.utlsFingerprint = "chrome"`. Build pool с VLESS+TLS config (без URI fp= override). Result outbound.tls.utls.fingerprint == "chrome".
    - Test 2: `test_buildSingBoxJSON_picker_default_random_preserves_existing_behavior` — App Group suite `app.bbtb.utlsFingerprint = "random"` (или absent). Build pool. Result: existing Phase 7a default ("random") сохранён.
    - Test 3: `test_buildSingBoxJSON_picker_does_not_override_uri_explicit_fp` — URI содержит `fp=firefox`; App Group suite = "chrome". Result: outbound получает "firefox" (URI parameter имеет приоритет — user-server explicit, не global).
    - Test 4 (integration, optional): `test_ConfigImporter_applies_CDN_when_frontingProfile_set` — mock ConfigImporter с stubbed ServerConfig containing parsed frontingProfile. Set @AppStorage cdnFrontingEnabled=true. Call provisionTunnelProfile. Verify через captured tunnelProvisioner argument: JSON содержит modified server / tls.server_name (от applied profile).
  </behavior>
  <action>
**File 1**: `BBTB/Project.swift` — добавить FrontingEngine:
- В блок `external_packages` (line 36-54): добавить новую entry `.package(path: .relativeToManifest("Packages/FrontingEngine"))` — pre Phase 10 ordering (рядом с другими anti-DPI packages, e.g., после `RulesEngine` либо ConfigParser — wherever logical fit).
- В target `BBTB-iOS` и `BBTB-macOS` dependencies: добавить product reference `FrontingEngine` (либо `.external(name: "FrontingEngine")` — match existing syntax used by other packages).
- НЕ добавлять FrontingEngine в tunnel-extension targets (`BBTB-iOS-tunnel` / `BBTB-macOS-tunnel`) — CDN logic живёт в main app per architectural responsibility map (RESEARCH.md).
- После modification: запустить `tuist generate` чтобы regenerate Xcode project (не часть acceptance criteria — executor может пропустить если Tuist отсутствует, но добавить note в SUMMARY).

**File 2**: `BBTB/Packages/AppFeatures/Package.swift` — добавить dep:
- В dependencies блок: `.package(path: "../FrontingEngine")`.
- В target где живёт `MainScreenFeature` (либо `AppFeatures` monolithic target): добавить `"FrontingEngine"` в target dependencies.

**File 3**: `BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift` — расширить uTLS application logic:
- Найти место (executor читает целиком файл — около per-protocol outbound build) где устанавливается `tls.utls.fingerprint` для VLESS / Trojan / TUIC.
- Перед финальным выставлением fingerprint в outbound dict добавить логику:
  ```
  // Phase 10 / DPI-09 — global uTLS picker override.
  // Priority: URI fp= параметр > @AppStorage picker > Phase 7a default "random".
  ```
- Логика (без code block):
  - `let pickerOverride = UserDefaults(suiteName: "group.app.bbtb.shared")?.string(forKey: "app.bbtb.utlsFingerprint") ?? "random"`.
  - Effective fingerprint = `uriProvidedFP ?? pickerOverride`. (Если URI явно указал — используем; иначе picker value; default "random" уже Phase 7a smart default.)
- Применить только для TLS-протоколов где utls block применим: VLESS+Reality (utls внутри tls.reality.utls), VLESS+TLS / VLESS+Vision (utls в tls.utls), Trojan (tls.utls), TUIC (tls.utls по research §State of the Art — TUIC поддерживает utls field).

**File 4**: `BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift` — добавить CDN apply hook:
- Imports: добавить `import FrontingEngine`.
- В `provisionTunnelProfile(for:)` (line 471+) — после `json = try PoolBuilder.buildSingBoxJSON(...)` (line ~595) И до `try await tunnelProvisioner.provisionTunnelProfile(configJSON: json, ...)` (line ~621), вставить новый блок:

```
// Phase 10 / D-03..D-07 / DPI-06 — CDN-фронтинг apply.
// Reads cdnFrontingEnabled toggle from .standard UserDefaults (main-app-only read).
// Если active server имеет frontingProfile в metadata → applyFronting overlay.
// Если frontingProfile == nil — silent skip (admin handoff не выполнен, toggle is no-op для этого сервера).
```

Логика:
- `let cdnFrontingEnabled = UserDefaults.standard.bool(forKey: "app.bbtb.cdnFrontingEnabled")`.
- Если `cdnFrontingEnabled == false` → skip block (используем raw json).
- Иначе: попытаться извлечь frontingProfile из ServerConfig либо ImportedServer/AnyParsedConfig:
  - **Phase 10 v0.10 status — infrastructure-only (activation pending server-side)** (revision 2026-05-15 checker scope_reduction resolution): добавить `private func extractFrontingProfile(for server: ServerConfig) -> FrontingProfile?` helper который **в v0.10 возвращает nil для всех серверов** (server-side `frontingProfile` payload в subscription — Phase 11 admin handoff per RESEARCH.md Q2 RESOLVED). Helper существует с реальной сигнатурой и call-site в provisionTunnelProfile, чтобы Phase 11 активация требовала только server-side rollout + 1-2 строки парсинга в helper. **Это НЕ скрытый scope cut**: D-07 (CDN toggle = глобальный, применяется к серверам с frontingProfile) honored — для серверов без frontingProfile toggle silently no-op'нется per design. Doc-comment в helper: «Phase 10 infrastructure stub — returns nil до server-side `frontingProfile` rollout (Phase 11). См. RESEARCH.md Q2 RESOLVED + wiki/cdn-fronting-server-handoff.md.»
- Если `let profile = extractFrontingProfile(...)`:
  - Choose adapter по `profile.provider`: switch на CDNProvider enum → CloudflareAdapter.self / FastlyAdapter.self / CustomCDNAdapter.self.
  - `json = try FrontingConfigApplier.apply(json: json, profile: profile, adapter: adapter)`.
  - Log info "Applied CDN fronting: \(profile.provider.rawValue)".
- Catch errors из FrontingConfigApplier.apply — log warning, continue с unmodified json (graceful degradation).
- Continue с `tunnelProvisioner.provisionTunnelProfile(configJSON: json, ...)` — same call как раньше.

**File 5**: `BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift` — добавить ≥ 3 теста (Tests 1-3 из behavior):
- Helpers cleanup для App Group key `app.bbtb.utlsFingerprint` в setUp/tearDown.
- Fixture parsedConfigs (VLESS+TLS с/без fp= URI param).
- Build JSON → parse → assert на outbound.tls.utls.fingerprint.
- Test 4 (CDN integration) — добавить **если** есть простой path для mock ConfigImporter; иначе пропустить (manual UAT covers это).
  </action>
  <verify>
    <automated>cd BBTB && swift build --package-path Packages/ConfigParser 2>&1 | tail -10 && swift build --package-path Packages/AppFeatures 2>&1 | tail -10 && swift test --package-path Packages/ConfigParser --filter PoolBuilderTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Packages/FrontingEngine' BBTB/Project.swift` ≥ 1.
    - `grep -c 'FrontingEngine' BBTB/Packages/AppFeatures/Package.swift` ≥ 1.
    - `grep -c 'import FrontingEngine' BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift` ≥ 1.
    - `grep -c 'FrontingConfigApplier' BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift` ≥ 1.
    - `grep -c 'cdnFrontingEnabled' BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift` ≥ 1.
    - `grep -c 'extractFrontingProfile\|frontingProfile' BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift` ≥ 1.
    - `grep -c 'utlsFingerprint' BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift` ≥ 1.
    - `grep -c '"group.app.bbtb.shared"' BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift` ≥ 1.
    - `grep -c 'test_buildSingBoxJSON_applies_utls\|test_.*utls_picker' BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift` ≥ 2.
    - `cd BBTB && swift build --package-path Packages/AppFeatures` exits 0.
    - `cd BBTB && swift build --package-path Packages/ConfigParser` exits 0.
    - `cd BBTB && swift test --package-path Packages/ConfigParser --filter PoolBuilderTests` exits 0; ≥ 2 new tests PASS.
    - `cd BBTB && swift test --package-path Packages/AppFeatures` exits 0 (no regression).
    - `cd BBTB && swift test --package-path Packages/FrontingEngine` exits 0 (Wave 3 tests still green).
    - **Полный package suite**: `cd BBTB && swift test` exits 0 для всех 18+ packages.
  </acceptance_criteria>
  <done>Tuist Project.swift wired; AppFeatures Package.swift depends on FrontingEngine; PoolBuilder применяет uTLS picker override; ConfigImporter `extractFrontingProfile` helper exists (returns nil в v0.10 — infrastructure-only status per revision 2026-05-15) с call-site готовым к Phase 11 activation; ≥ 2 unit-теста в PoolBuilderTests; полный package suite PASS. **DPI-06 status = code-ready, activation pending server-side `frontingProfile` configuration в Phase 11 admin handoff (см. wiki/cdn-fronting-server-handoff.md).**</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: REQUIREMENTS / ROADMAP / STATE updates + wiki sync (8 files) + memory create + xcodebuild final validation</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, wiki/anti-dpi-techniques.md, wiki/architecture.md, wiki/security-gaps.md, wiki/advanced-settings.md, wiki/cdn-fronting-architecture-2026.md, wiki/cdn-fronting-server-handoff.md, wiki/cert-pinning-spki.md, wiki/log.md, wiki/index.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (целиком — особенно §Anti-DPI, §BIO, §KILL, §UX-06)
    - .planning/ROADMAP.md (Phase 10 entry)
    - .planning/STATE.md (текущее состояние + progress block)
    - wiki/anti-dpi-techniques.md (целиком — где integrate Phase 10 status)
    - wiki/architecture.md (целиком — где добавить FrontingEngine package в diagram)
    - wiki/security-gaps.md (целиком — R-numeric naming convention для new entries; recent R-entries Phase 8/9)
    - wiki/index.md (структура — где добавить новые page references)
    - wiki/log.md (формат daily entry)
    - .planning/phases/08-rules-engine-split-tunneling/08-CONTEXT.md (для wiki style reference — Phase 8 closure написала similar wiki sync)
    - 10-01-SUMMARY.md..10-05-SUMMARY.md — для facts о выполненных Wave outcome (file counts, test counts, commit SHAs)
  </read_first>
  <action>
**`.planning/REQUIREMENTS.md`** — пометить Validated:
- UX-06: change `[ ]` → `[x]` с suffix `(Phase 10 v0.10 ✅ Validated 2026-05-15 — 5-секционный AdvancedSettingsView per D-15)`.
- DPI-05: change `[ ]` → `[x]` с suffix `(Phase 10 v0.10 ✅ Validated 2026-05-15 — SingBoxConfigLoader Mux injection + D-09 protocol whitelist; carry-over из Phase 7a W3)`.
- DPI-06: **НЕ** mark `[x]` Validated. Заменить на `[ ] DPI-06 — CDN fronting … (Phase 10 v0.10 ⚙️ Infrastructure-validated 2026-05-15 — FrontingEngine SwiftPM package + 3 adapters + D-05 sing-box mapping + ConfigImporter call-site. Activation pending: server-side `frontingProfile` payload в Marzban subscription + Cloudflare Worker rollout — Phase 11 admin handoff per wiki/cdn-fronting-server-handoff.md.)`. **Rationale (revision 2026-05-15 checker blocker scope_reduction resolution):** код инфраструктуры готов и tested (FrontingEngine package, applier, call-site в ConfigImporter), но `extractFrontingProfile()` возвращает nil во всех серверах до server-side payload activation. Per planner-source-audit policy: requirement не получает `[x]` пока не доставлена observable user-visible behavior. DPI-06 финальный `[x]` mark — Phase 11 closure после admin-side rollout + успешный UAT через настоящий Cloudflare-fronted сервер.
- DPI-08: change `[ ]` → `[x]` с suffix `(Phase 10 v0.10 ✅ Validated 2026-05-15 — PinnedSessionDelegate + SubscriptionPinManager Ed25519 manifest; Phase 12 prerequisite — replace placeholder pins)`.
- DPI-09: change `[ ]` → `[x]` с suffix `(Phase 10 v0.10 ✅ Validated 2026-05-15 — uTLS Picker в Advanced Settings → PoolBuilder application)`.
- BIO-04: change `[ ]` → `[x]` с suffix `(Phase 10 v0.10 ✅ Validated 2026-05-15 — STUN block route.rule reject UDP 3478/5349; destructive confirm alert на OFF→ON)`.
- KILL-04: change `[ ]` → `[x]` с suffix `(Phase 10 v0.10 ✅ Validated 2026-05-15 — macOS enforceRoutes toggle через App Group UserDefaults + applyEnforceRoutesToManager live-apply; iOS hidden per D-17)`.
- Update footer Last updated line: добавить `2026-05-15 — Phase 10 closure: UX-06, DPI-05, DPI-06, DPI-08, DPI-09, BIO-04, KILL-04 ✅ Validated (code-validated; manual UAT pending). См. wiki/advanced-settings.md, wiki/cdn-fronting-architecture-2026.md, wiki/cert-pinning-spki.md.`.

**`.planning/ROADMAP.md`** — Phase 10 entry:
- В строке "**Status**" (если нет такой строки — то под header) добавить: `**Status:** ⚙️ Implementation complete 2026-05-15 — UAT pending (manual); DPI-06 infrastructure-only до Phase 11 admin handoff. v0.10.`.
- Уточнить Success Criteria статус: пометить criterion 1, 2, 4, 5, 6, 7 как `✓` (code-validated). **Criterion 3 (CDN-фронтинг)** — переформулировать на: `3. ⚙️ FrontingEngine инфраструктура code-ready (FrontingProfile + 3 adapters + FrontingConfigApplier + ConfigImporter call-site); CDN toggle применяется к серверам с frontingProfile в подписке — activation pending server-side rollout (Phase 11 admin handoff per wiki/cdn-fronting-server-handoff.md).` (revision 2026-05-15 checker blocker scope_reduction resolution).
- Mark Plans list — все [ ] → [x] для 10-01..10-06.
- Update Last updated footer line.

**`.planning/STATE.md`** — update top frontmatter + sections:
- frontmatter: `milestone: v0.10` (либо update текущий — например текущий v0.12; правильное значение зависит от context — executor читает существующий milestone).
- frontmatter: `status: paused` → `status: progressed` (либо аналогичный для completed phase).
- `progress.completed_phases`: +1 (если Phase 10 — следующая после current).
- `progress.completed_plans`: +6.
- `progress.percent`: пересчитать.
- Update top-level **Current focus**: «Phase 10 ✓ Implementation complete 2026-05-15 — UAT pending. Версия v0.10.»
- Active Phase: Phase 10 entry с progress table (Wave 1-4 ✓) → переход на «next: Phase 11 onboarding + UX polish».
- Add «### Phase 10 ✅ CLOSED 2026-05-15» секция (mirror Phase 8 closure format в текущем STATE.md):
  - Wave summary (W1 UX + W2 Mux + W2 PinManager + W3 STUN/enforceRoutes + W3 FrontingEngine + W4 Integration).
  - Tests added total (сумма из 5 SUMMARY).
  - Architectural decisions D-01..D-17 list with brief rationale (copy from CONTEXT.md).
  - Wiki sync evidence (5 new pages + 3 updated).
- Next Action: `/gsd-discuss-phase 11` — Onboarding + UX polish (v0.11).

**Wiki updates** (8 files):

1. **`wiki/anti-dpi-techniques.md`** (modify): добавить новый раздел «### v0.10 (Phase 10) — UI поверхности для anti-DPI»:
   - Mux toggle в Advanced Settings (DPI-05) — глобальный switch + D-09 whitelist.
   - uTLS fingerprint Picker (DPI-09) — 7 options, default `random`.
   - STUN-блок toggle (BIO-04) — D-16 destructive confirm.
   - Реализация: ссылки на конкретные файлы (SingBoxConfigLoader Step 6 + Step 7, AntiDPISection.swift).
   - Update Last updated date.

2. **`wiki/advanced-settings.md`** (NEW): полное описание 5-секционного AdvancedSettingsView. Sections:
   - Layout (D-15) — порядок секций с описанием.
   - Toggle inventory — все 6 phase-10 toggles + 2 phase-6 toggles + Phase 8 rules controls (table).
   - Default values + persistence (UserDefaults.standard vs App Group suite — какие где).
   - macOS-only гейты (#if os(macOS)) — enforceRoutes.
   - Destructive flows — STUN confirm alert.
   - L10n keys table (19 новых).
   - Cross-reference на feature pages (anti-dpi-techniques, cert-pinning-spki, cdn-fronting-architecture-2026).

3. **`wiki/cdn-fronting-architecture-2026.md`** (NEW): по образцу `engine-abstraction-decision-2026.md`:
   - Header summary, sources (Codex thread + CONTEXT D-03..D-07), last updated.
   - Архитектура (FrontingProfile + CDNProviderAdapter + FrontingConfigApplier + FrontingFailureCache + FrontingFallbackChain) с diagram (ASCII либо просто text).
   - D-03 решение — почему отдельный слой, не часть TransportConfig.
   - D-05 sing-box mapping table (WS / HTTPUpgrade / gRPC).
   - D-06 failure chain + cooldown ladder.
   - **Cloudflare classic fronting deferral note** (Pitfall 3) — почему «свой домен» подход, не cross-domain.
   - Future: 5+ providers, IP pool remote sync (v1.x).
   - Связи: `[[cdn-fronting-server-handoff]]`, `[[anti-dpi-techniques]]`, `[[security-gaps]]`.

4. **`wiki/cdn-fronting-server-handoff.md`** (NEW): admin instructions для Marzban:
   - Что admin делает: deploys Cloudflare Worker / Page Rule routing ws upgrade to origin VPN; adds `frontingProfile` JSON blob в subscription payload.
   - JSON schema example (FrontingProfile из FrontingProfile.swift).
   - Cloudflare Worker code snippet (минимальный — proxy WS to origin).
   - Verification steps (admin → user perspective).
   - Связь с client implementation (ConfigImporter Plan 06).

5. **`wiki/cert-pinning-spki.md`** (NEW):
   - DPI-08 описание (что pin'им, почему SPKI, не cert).
   - Apple-standard pipeline (SecTrustCopyCertificateChain + SecCertificateCopyKey + SecKeyCopyExternalRepresentation + SHA256).
   - `scripts/generate-spki-pin.swift` usage instructions.
   - Phase 12 prerequisite: rotation procedure (admin generates pin → admin updates BootstrapPins.vpnVergevskyRu → release).
   - Remote manifest (subscription-pins.json) — endpoint + Ed25519 same key as rules.json (D-12).
   - validUntil hard reject policy.
   - Pitfall 2 (Apple vs OpenSSL hash format) — verified via generate-spki-pin.swift (A4 в RESEARCH.md).
   - Связи: `[[security-gaps]]`, `[[rules-engine]]` (same Ed25519 admin key).

6. **`wiki/architecture.md`** (modify): добавить FrontingEngine пакет в Packages list / diagram. Добавить cert pinning flow в trust path schema.

7. **`wiki/security-gaps.md`** (modify): добавить R20+ entries (или next available R-number):
   - **R20** — cert pinning для subscription URL (DPI-08). What закрыто Phase 10 v0.10. Carry-forward: replace placeholder bytes реальными pins до TestFlight.
   - **R21** — STUN UDP block toggle (BIO-04). WebRTC leak mitigation user-opt-in.
   - **R22** — macOS enforceRoutes user-opt-out (KILL-04). Informed degradation D-17.
   - **R23** — CDN fronting architecture (DPI-06). Cloudflare classic blocked since 2015; «свой домен» техника D-05.
   - Update each existing entry that touches Phase 10 (если такие есть в WR-* carry-forward list).

8. **`wiki/index.md`** (modify): добавить ссылки на 5 new pages:
   - `[[advanced-settings]]` — под section UX / Settings.
   - `[[cdn-fronting-architecture-2026]]` — под section Anti-DPI.
   - `[[cdn-fronting-server-handoff]]` — под section Anti-DPI или Operations.
   - `[[cert-pinning-spki]]` — под section Security.
   - Update last-updated meta.

9. **`wiki/log.md`** (append): новый daily entry «### 2026-05-15 — Phase 10 closure»:
   - Bullet list: 5 new wiki pages, 3 updated; REQUIREMENTS marked Validated (7 IDs); ROADMAP Phase 10 ✓; STATE milestone v0.10.
   - Brief outcome summary (paragraph).

**Memory file**: `/Users/vergevsky/.claude/projects/-Users-vergevsky-ClaudeProjects-VPN/memory/project_phase12_subscription_pins_prerequisite.md`:
- Short markdown note: «Phase 12 prerequisite — subscription cert pinning placeholder pins. До TestFlight upload (Phase 12) выполнить: `swift run --package-path scripts generate-spki-pin --host vpn.vergevsky.ru`; полученный hex hash → заменить placeholders в `BBTB/Packages/ConfigParser/Sources/ConfigParser/PinStore.swift` `BootstrapPins.vpnVergevskyRu` (primary + backup). См. wiki/cert-pinning-spki.md § Rotation procedure.»
- После записи файла: добавить bullet в `~/.claude/projects/-Users-vergevsky-ClaudeProjects-VPN/memory/MEMORY.md` ссылающийся на новый файл с одной строкой контекста.

**Final validation** (acceptance criteria):
- `cd BBTB && swift test` — все packages green.
- `xcodebuild build -scheme BBTB-iOS -destination 'platform=iOS Simulator,name=iPhone 16'` SUCCEEDED.
- `xcodebuild build -scheme BBTB-macOS` SUCCEEDED.
- `validate-r1-r6.sh` (если доступен) PASS.
  </action>
  <verify>
    <automated>cd BBTB && swift test 2>&1 | tail -30 && xcodebuild build -scheme BBTB-iOS -destination 'platform=iOS Simulator,name=iPhone 16' -workspace BBTB.xcworkspace 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\[x\].*UX-06.*Phase 10 v0.10' .planning/REQUIREMENTS.md` ≥ 1.
    - `grep -cE '\[x\].*DPI-05.*Phase 10' .planning/REQUIREMENTS.md` ≥ 1.
    - `grep -cE 'DPI-06.*Phase 10 v0.10 ⚙️ Infrastructure-validated' .planning/REQUIREMENTS.md` ≥ 1. (DPI-06 НЕ помечен `[x]` Validated — это infrastructure-only status, см. revision 2026-05-15 scope_reduction resolution.)
    - `grep -cE '\[x\].*DPI-06' .planning/REQUIREMENTS.md` returns 0. (Guard against accidental [x] DPI-06 mark.)
    - `grep -cE '\[x\].*DPI-08.*Phase 10' .planning/REQUIREMENTS.md` ≥ 1.
    - `grep -cE '\[x\].*DPI-09.*Phase 10' .planning/REQUIREMENTS.md` ≥ 1.
    - `grep -cE '\[x\].*BIO-04.*Phase 10' .planning/REQUIREMENTS.md` ≥ 1.
    - `grep -cE '\[x\].*KILL-04.*Phase 10' .planning/REQUIREMENTS.md` ≥ 1.
    - `grep -c 'Implementation complete 2026-05-15' .planning/ROADMAP.md` ≥ 1 (Phase 10 status line).
    - `grep -c '10-06-PLAN.md.*\[x\]' .planning/ROADMAP.md` ≥ 1.
    - `grep -c 'v0.10' .planning/STATE.md` ≥ 1.
    - `grep -c 'Phase 10.*CLOSED' .planning/STATE.md` ≥ 1.
    - File `wiki/advanced-settings.md` exists; `grep -c 'D-15' $file` ≥ 1.
    - File `wiki/cdn-fronting-architecture-2026.md` exists; `grep -c 'FrontingProfile' $file` ≥ 1.
    - File `wiki/cdn-fronting-server-handoff.md` exists; `grep -c 'Marzban\|Cloudflare Worker' $file` ≥ 1.
    - File `wiki/cert-pinning-spki.md` exists; `grep -c 'SecKeyCopyExternalRepresentation\|generate-spki-pin' $file` ≥ 1.
    - `grep -c 'R20\|R21\|R22\|R23' wiki/security-gaps.md` ≥ 1 (хотя бы один new R-entry).
    - `grep -c 'FrontingEngine' wiki/architecture.md` ≥ 1.
    - `grep -c '2026-05-15.*Phase 10 closure' wiki/log.md` ≥ 1.
    - `grep -c 'advanced-settings\|cdn-fronting\|cert-pinning' wiki/index.md` ≥ 3.
    - File `$HOME/.claude/projects/-Users-vergevsky-ClaudeProjects-VPN/memory/project_phase12_subscription_pins_prerequisite.md` exists.
    - `grep -c 'project_phase12_subscription_pins_prerequisite' $HOME/.claude/projects/-Users-vergevsky-ClaudeProjects-VPN/memory/MEMORY.md` ≥ 1.
    - `cd BBTB && swift test` exits 0 (все packages green).
    - `xcodebuild build -scheme BBTB-iOS -destination 'platform=iOS Simulator,name=iPhone 16'` SUCCEEDED (или soft-warn если Tuist generate не запущен; executor должен документировать в SUMMARY если xcodebuild stage skip'нут).
    - `xcodebuild build -scheme BBTB-macOS` SUCCEEDED.
  </acceptance_criteria>
  <done>Все 7 phase-10 requirements помечены [x] Validated; ROADMAP/STATE отражают closure; 5 new wiki pages + 3 modified; memory создана для Phase 12 prerequisite; полный suite swift test + xcodebuild iOS/macOS зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ConfigImporter ↔ FrontingEngine | CDN apply решение принимается main app до отправки JSON в extension |
| Wiki ↔ Reader | Documentation surface — должна быть accurate для future developers/contractors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-W6-01 | Information Disclosure | Wiki содержит admin Cloudflare Worker code → potential leak | accept | wiki — public knowledge base; admin-side секреты (Worker creds) НЕ в wiki, только high-level config approach. |
| T-10-W6-02 | Tampering | REQUIREMENTS.md помечены Validated без real UAT | mitigate | Suffix «(code-validated; manual UAT pending)» предупреждает что real device verification ещё не выполнен. Phase 11 либо Phase 12 add device UAT для Phase 10 toggles. |
| T-10-W6-03 | Configuration drift | Wiki описывает D-XX decisions; если код drift'нется — wiki устаревает | mitigate | CLAUDE.md обязательное правило — каждое решение fixe'тся в wiki. Plan 06 closure обязательно sync'ает wiki; future phases также. |
| T-10-W6-04 | Misuse | Tuist Project.swift modification без `tuist generate` → workspace не видит FrontingEngine | mitigate | SUMMARY документирует требование запустить `tuist generate` после merge; xcodebuild final validation в Task 2 поймает если миссинг. |
| T-10-W6-05 | DoS | Полный package suite swift test taking too long (CI timeout) | accept | Standard для phase closure; locally разработчик может прерывать. |
| T-10-W6-06 | Production safety | Placeholder pins shipped в TestFlight | mitigate | Memory `project_phase12_subscription_pins_prerequisite.md` создаётся в Plan 06; Phase 12 obligatory pre-upload checklist item. |
</threat_model>

<verification>
Wave 4 (final):
1. `cd BBTB && swift test` — все packages зелёный.
2. `xcodebuild build -scheme BBTB-iOS` SUCCEEDED.
3. `xcodebuild build -scheme BBTB-macOS` SUCCEEDED.
4. Запустить iPhone 16 Simulator (либо реальное устройство): открыть приложение → Settings → Advanced — увидеть 5 секций (DNS / Anti-DPI / Безопасность / Rules); все toggle переключаются persistent через kill.
5. Manual smoke: включить Mux toggle → reconnect к Trojan серверу → проверить в sing-box log что multiplex включён.
6. Manual smoke: включить STUN block → reconnect → попробовать Google Meet в Safari → expected fail (footer warning подтверждается).
7. macOS smoke: выключить enforceRoutes toggle → Console.app показывает applyEnforceRoutesToManager log; manager.enforceRoutes стало false.
8. Wiki review: открыть `wiki/advanced-settings.md` — проверить что описание соответствует UI и code.
</verification>

<success_criteria>
- 6 из 7 Phase 10 requirements code-Validated `[x]` (UX-06, DPI-05, DPI-08, DPI-09, BIO-04, KILL-04). **DPI-06 marked Infrastructure-validated** (НЕ `[x]` Validated) — activation pending Phase 11 admin handoff per revision 2026-05-15 scope_reduction resolution.
- Tuist Project.swift wire + AppFeatures depends on FrontingEngine.
- PoolBuilder + ConfigImporter применяют новые toggle effects.
- ROADMAP.md / STATE.md / REQUIREMENTS.md обновлены под v0.10 closure.
- 5 new wiki pages + 3 modified + memory file для Phase 12 prerequisite.
- Полный swift test + iOS/macOS xcodebuild SUCCEEDED.
- Manual UAT ostается outstanding (Phase 11 либо Phase 12 device smoke).
</success_criteria>

<output>
After completion, create `.planning/phases/10-advanced-settings-security-polish/10-06-SUMMARY.md` со следующими полями:
- Integration + closure outcome.
- Tuist Project.swift wired, AppFeatures depends on FrontingEngine.
- Files modified: 4 source + 8 wiki + 3 .planning + 1 memory.
- Tests added (Task 1 PoolBuilderTests).
- xcodebuild iOS + macOS SUCCEEDED (либо documented exception если build environment issue).
- Open follow-up для Phase 11/12:
  - Manual UAT для всех 6 toggle (Mux / CDN / STUN / cert-pinning / uTLS / enforceRoutes).
  - Phase 12 prerequisite: replace placeholder pins via generate-spki-pin.swift.
  - Admin handoff: Marzban CDN integration (wiki/cdn-fronting-server-handoff.md).
- Phase 10 ✓ COMPLETE; next phase: `/gsd-discuss-phase 11`.
- Commit SHA(s).
</output>
