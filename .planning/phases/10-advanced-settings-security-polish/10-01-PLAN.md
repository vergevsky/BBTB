---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift
  - BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdvancedSettingsView.swift
  - BBTB/Packages/AppFeatures/Sources/SettingsFeature/AntiDPISection.swift
  - BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift
  - BBTB/Packages/AppFeatures/Sources/SettingsFeature/UTLSPickerView.swift
  - BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings
  - BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/SettingsViewModelTests.swift
autonomous: true
requirements: [UX-06, DPI-09]
tags: [advanced-settings, settings-viewmodel, ui-skeleton]

must_haves:
  truths:
    - "REQUIREMENTS.md и ROADMAP.md формально отражают scope amendment: BIO-01..04 и ONDEMAND-01 перенесены в deferred с указанием D-01/D-02"
    - "AdvancedSettingsView рендерит 5 секций в порядке: MinAppVersionBanner (conditional) → DNS → Anti-DPI → Безопасность → Rules"
    - "Пользователь видит в Anti-DPI секции: CDN-фронтинг toggle, Mux toggle, uTLS picker (.menu), STUN-блок toggle"
    - "Пользователь видит в Безопасность секции: Cert pinning toggle + (только macOS) macOS enforceRoutes toggle"
    - "Все 6 новых значений (cdnFrontingEnabled, muxEnabled, stunBlockEnabled, certPinningEnabled, utlsFingerprint, macOSDisableEnforceRoutes) персистятся между запусками через @AppStorage"
    - "macOSDisableEnforceRoutes использует App Group UserDefaults suite (group.app.bbtb.shared) так что extension сможет прочитать ту же запись"
    - "STUN-блок toggle при OFF→ON показывает .alert с кнопками Включить (destructive) / Отмена; Cancel возвращает toggle в OFF"
    - "macOS enforceRoutes toggle при OFF меняет цвет footer на .red согласно D-17 (UI-SPEC §State Transitions)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "BIO-01..04 + ONDEMAND-01 помечены deferred (D-01/D-02); DPI-05/06/08/09 + BIO-04 + KILL-04 + UX-06 описание мигрировано к Phase 10 v0.10 scope"
      contains: "BIO-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 10 entry: Requirements line обновлён, Plans count = 6, plan list 10-01..10-06"
      contains: "10-01-PLAN.md"
    - path: "BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift"
      provides: "6 новых @AppStorage props + utlsOptions static let + stunBlockShowConfirm @Published state"
      contains: "cdnFrontingEnabled"
    - path: "BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdvancedSettingsView.swift"
      provides: "5-секционный Form per D-15"
      contains: "AntiDPISection"
    - path: "BBTB/Packages/AppFeatures/Sources/SettingsFeature/AntiDPISection.swift"
      provides: "Подвью Anti-DPI секции: 3 toggle + 1 picker, header + footer"
      contains: "AntiDPISection"
    - path: "BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift"
      provides: "Подвью Безопасность: cert pinning toggle + macOS-only enforceRoutes toggle"
      contains: "SecuritySection"
    - path: "BBTB/Packages/AppFeatures/Sources/SettingsFeature/UTLSPickerView.swift"
      provides: "Picker .menu со списком 7 fingerprint values + локализованными labels"
      contains: "UTLSPickerView"
    - path: "BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings"
      provides: "Новые L10n ключи для Phase 10 (settings.antiDpi.*, settings.security.*) на ru + en"
      contains: "settings.antiDpi.section"
  key_links:
    - from: "BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdvancedSettingsView.swift"
      to: "AntiDPISection + SecuritySection"
      via: "Section view composition + viewModel @ObservedObject"
      pattern: "AntiDPISection\\(viewModel:|SecuritySection\\(viewModel:"
    - from: "BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift"
      to: "UserDefaults (group.app.bbtb.shared)"
      via: "@AppStorage(_, store:) для macOSDisableEnforceRoutes"
      pattern: "store:.*UserDefaults\\(suiteName:"
---

<objective>
Wave 1 — закладываем фундамент Phase 10: формальное обновление scope в REQUIREMENTS.md / ROADMAP.md и полная UI-skeleton для Advanced Settings с 5 секциями и 6 новых @AppStorage toggles. Никакой business-логики (Mux injection, CDN apply, pin checks, enforceRoutes routing) — только persistent state + UI. Эта база разблокирует параллельную работу всех следующих wave'ов.

Purpose: дать пользователю сразу видимый прогресс (новые toggle'ы появляются в Расширенных) и закрепить scope amendment в письменных артефактах до того, как кто-то начнёт писать код, ориентируясь на устаревший BIO-01..04 список из ROADMAP.

Output: обновлённый REQUIREMENTS.md + ROADMAP.md + расширенный SettingsViewModel + 3 новых SwiftUI subview (AntiDPISection, SecuritySection, UTLSPickerView) + 19 новых L10n ключей + ≥ 6 новых unit-тестов на персистентность toggle'ов и default values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-advanced-settings-security-polish/10-CONTEXT.md
@.planning/phases/10-advanced-settings-security-polish/10-RESEARCH.md
@.planning/phases/10-advanced-settings-security-polish/10-PATTERNS.md
@.planning/phases/10-advanced-settings-security-polish/10-UI-SPEC.md

<interfaces>
Извлечения из существующего кода — executor должен использовать как есть, не исследовать заново.

From `BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift`:
- `public final class SettingsViewModel: ObservableObject`
- Существующие @AppStorage props (lines 27-49):
  - `@AppStorage("app.bbtb.killSwitchEnabled") public var killSwitchEnabled: Bool = false`
  - `@AppStorage("app.bbtb.customDNS") public var customDNS: String = ""`
  - `@AppStorage("app.bbtb.adBlockEnabled") public var adBlockEnabled: Bool = false`
  - `@AppStorage("app.bbtb.autoReconnectEnabled") public var autoReconnectEnabled: Bool = true`
- Pattern для App-Group-backed AppStorage из RESEARCH.md §Pitfall 5:
  - `@AppStorage("key", store: UserDefaults(suiteName: "group.app.bbtb.shared")) public var foo: Bool = false`
- Существующие методы: `applyAutoReconnectToManager()` async — паттерн live-apply через NETunnelProviderManager.loadAllFromPreferences

From `BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdvancedSettingsView.swift`:
- `public struct AdvancedSettingsView: View` принимает `@ObservedObject var viewModel: SettingsViewModel`
- Текущий Form имеет 4 неименованных секции (MinAppVersionBanner / DNS / RulesViewerSection / ForceUpdateRulesButton); D-15 требует 5 именованных секций с явными header/footer.
- Существующие подвью: `AdBlockToggleSection(isOn:footerText:)`, `CustomDNSField(text:)`, `MinAppVersionBanner(currentVersion:onTap:)`, `RulesViewerSection(snapshot:)`, `ForceUpdateRulesButton(...)` — переиспользуются as-is.

From `BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings`:
- Существующий формат — Apple xcstrings (JSON). Ключи: `settings.dns.section`, `settings.advanced.title`, `rules.forceUpdate.*`, и т.д.
- Naming convention: `settings.<area>.<element>.<role>` camelCase / kebab-нет — точечная иерархия как в Phase 8.

From `BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/SettingsViewModelTests.swift`:
- Уже существует — extend, не заменять.
- Использует Swift Testing (`@Test`, `#expect`) либо XCTest (проверить current file pattern до начала редактирования).

Project config (`.planning/config.json`):
- `app_group: "group.app.bbtb.shared"` — используем эту строку буквально для UserDefaults(suiteName:).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: REQUIREMENTS / ROADMAP scope amendment + новые @AppStorage props в SettingsViewModel</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift, BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/SettingsViewModelTests.swift</files>
  <read_first>
    - .planning/REQUIREMENTS.md (целиком — особенно §BIO + §ONDEMAND + §Anti-DPI секции, чтобы видеть текущие формулировки)
    - .planning/ROADMAP.md (целиком — особенно Phase 10 entry, чтобы переписать список requirements и plans)
    - .planning/phases/10-advanced-settings-security-polish/10-CONTEXT.md §Area A (D-01..D-02), §Area E (D-15..D-17) — формулировки scope amendment
    - BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift — текущая структура класса, существующие @AppStorage props, расположение public methods
    - BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/SettingsViewModelTests.swift — текущий test framework (Swift Testing или XCTest) и стиль assertion
    - .planning/phases/10-advanced-settings-security-polish/10-PATTERNS.md строки 112-159 (SettingsViewModel pattern с новыми props)
    - .planning/phases/10-advanced-settings-security-polish/10-RESEARCH.md §Pattern 2 (@AppStorage) + §Pitfall 5 (App Group UserDefaults для cross-process visibility)
  </read_first>
  <behavior>
    - Test 1: `SettingsViewModelTests.test_phase10_defaults` — после instantiate SettingsViewModel(): cdnFrontingEnabled=false, muxEnabled=false, stunBlockEnabled=false, certPinningEnabled=true, utlsFingerprint="random", и (macOS) macOSDisableEnforceRoutes=false. Тест чистит UserDefaults.standard и group suite ДО каждого запуска чтобы избежать leak'а между тестами.
    - Test 2: `SettingsViewModelTests.test_phase10_persistence_roundtrip` — записать вручную в UserDefaults.standard ключи cdnFrontingEnabled=true / muxEnabled=true / stunBlockEnabled=true / certPinningEnabled=false / utlsFingerprint="chrome"; создать VM; убедиться что @AppStorage props читают эти значения.
    - Test 3 (только #if os(macOS)): `test_macOSDisableEnforceRoutes_uses_app_group_suite` — записать в `UserDefaults(suiteName: "group.app.bbtb.shared")` ключ `app.bbtb.macOSDisableEnforceRoutes`=true; создать VM; macOSDisableEnforceRoutes должен прочитать `true` из суite, не из .standard.
    - Test 4: `test_utlsOptions_contains_all_seven` — `SettingsViewModel.utlsOptions` равен `["random","chrome","firefox","safari","ios","android","edge"]` (порядок важен — UI отображает в этом порядке).
    - Test 5: `test_stunBlockShowConfirm_initial_false` — `viewModel.stunBlockShowConfirm` инициализирован false (используется UI alert binding'ом).
    - Test 6: `test_phase10_keys_dont_collide` — ни один из 6 новых @AppStorage ключей не равен ни одному из существующих (`killSwitchEnabled`, `customDNS`, `adBlockEnabled`, `autoReconnectEnabled`, `minAppVersion.dismissed`).
  </behavior>
  <action>
В REQUIREMENTS.md: переписать §BIO раздел — BIO-01, BIO-02, BIO-03 и BIO-04 пометить как deferred с явной формулировкой «→ **Out of Scope v0.10** _(Phase 10 scope amendment 2026-05-15 per D-01 in 10-CONTEXT.md. BIO-04 STUN block перенесён в DPI-секцию как Phase 10 anti-DPI toggle.)_» — нет, проверь: BIO-04 ОСТАЁТСЯ в scope (он в phase_req_ids). Поэтому BIO-01, BIO-02, BIO-03 → deferred, **BIO-04 остаётся [ ] in-scope Phase 10**. ONDEMAND-01: пометить deferred с формулировкой «→ **Out of Scope v0.10** _(Phase 10 scope amendment 2026-05-15 per D-02; manual SSID whitelist рассматривается в v1.x.)_».

В REQUIREMENTS.md дополнительно: добавить footer line `*Last updated: 2026-05-15 — Phase 10 scope amendment: BIO-01/02/03 + ONDEMAND-01 → deferred per D-01/D-02 in 10-CONTEXT.md. BIO-04 (STUN block) и KILL-04 (macOS enforceRoutes) остаются in-scope Phase 10.*` сразу под существующим Last updated, не удаляя предыдущие.

В ROADMAP.md, секция `### Phase 10: Advanced settings + Security polish`:
- Перепишем строку **Requirements:**: было `UX-06, BIO-01, BIO-02, BIO-03, BIO-04, DPI-06, DPI-08, DPI-09, ONDEMAND-01, KILL-04` — становится `UX-06, DPI-05, DPI-06, DPI-08, DPI-09, BIO-04, KILL-04`. Сразу под этой строкой добавить строку `**Scope amendment (2026-05-15 in /gsd-discuss-phase 10):** BIO-01/02/03 (UI app lock) + ONDEMAND-01 (Wi-Fi SSID rules) → deferred per D-01/D-02 in 10-CONTEXT.md. DPI-05 (Mux infrastructure) — carry-over из Phase 7a W3.`.
- Перепишем строку 1 Success Criteria: «Все опции в Расширенных функциональны и сохраняются между запусками» → расшифровать как: «CDN-фронтинг toggle, Mux toggle, uTLS picker, STUN-блок toggle, Cert pinning toggle, и (только macOS) enforceRoutes toggle — все 6 настроек функциональны и persists через @AppStorage». Криtерий 2 (биометрия) убрать. Перенумеровать.
- Добавить блок **Plans:** 6 plans (waves 1-6) и список:
  - [ ] 10-01-PLAN.md — UX skeleton + scope amendment + SettingsViewModel toggle props (UX-06, DPI-09)
  - [ ] 10-02-PLAN.md — Mux injection в SingBoxConfigLoader + protocol whitelist + 6+ tests (DPI-05)
  - [ ] 10-03-PLAN.md — STUN-блок route.rule + macOS enforceRoutes runtime + live-apply (BIO-04, KILL-04)
  - [ ] 10-04-PLAN.md — Cert pinning: PinnedSessionDelegate + PinStore + SubscriptionPinManager + bootstrap manifest (DPI-08)
  - [ ] 10-05-PLAN.md — CDN-фронтинг: FrontingEngine package + FrontingProfile + CDNProviderAdapter + FrontingConfigApplier + fallback cache (DPI-06)
  - [ ] 10-06-PLAN.md — Integration: PoolBuilder uTLS wiring + ConfigImporter CDN apply + wiki sync + v0.10 closure (UX-06, DPI-09)
- Обновить `*Last updated:*` footer строку: добавить `2026-05-15 — Phase 10 plan list (6 plans) + scope amendment (BIO-01/02/03 + ONDEMAND-01 → deferred) added by /gsd-plan-phase 10.`

В `BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift`:
- Добавить новый MARK блок `// MARK: - Phase 10 — Anti-DPI + Security toggles` сразу после строки `autoReconnectEnabled` (line 49).
- Добавить **ровно** следующие 6 @AppStorage properties в указанном порядке (имена ключей не менять):
  1. `cdnFrontingEnabled: Bool = false` под ключом `"app.bbtb.cdnFrontingEnabled"` без store: параметра (read by main app only).
  2. `muxEnabled: Bool = false` под ключом `"app.bbtb.muxEnabled"` — без store: (extension читает через `UserDefaults(suiteName:)` напрямую в SingBoxConfigLoader; см. Plan 02). **ВАЖНО:** дополнительно зеркалить в App Group suite через willSet/property observer не нужно — Plan 02 покажет что extension читает суite. Здесь mux key пишем **только в App Group suite** — добавить `store: UserDefaults(suiteName: "group.app.bbtb.shared")` к @AppStorage, чтобы один и тот же storage location для main app и extension. Аналогично для `stunBlockEnabled` (читается в expandConfigForTunnel) и `utlsFingerprint` (читается в PoolBuilder в main app — но для consistency держим в App Group suite).
  3. `stunBlockEnabled: Bool = false` под ключом `"app.bbtb.stunBlockEnabled"` со `store: UserDefaults(suiteName: "group.app.bbtb.shared")`.
  4. `certPinningEnabled: Bool = true` под ключом `"app.bbtb.certPinningEnabled"` — без store: (main app only).
  5. `utlsFingerprint: String = "random"` под ключом `"app.bbtb.utlsFingerprint"` — `store: UserDefaults(suiteName: "group.app.bbtb.shared")` (PoolBuilder в main app строит JSON, но extension читает финальный JSON — fingerprint оказывается в configJSON, поэтому хранилище не критично; для consistency держим в App Group).
  6. `#if os(macOS) macOSDisableEnforceRoutes: Bool = false #endif` под ключом `"app.bbtb.macOSDisableEnforceRoutes"` со `store: UserDefaults(suiteName: "group.app.bbtb.shared")` — критично! Иначе extension не увидит (RESEARCH.md §Pitfall 5).
- Под этими props добавить `public static let utlsOptions: [String] = ["random", "chrome", "firefox", "safari", "ios", "android", "edge"]` (порядок locked в UI-SPEC §Copywriting).
- Добавить `@Published public var stunBlockShowConfirm: Bool = false` для биндинга .alert (UI-SPEC §Interaction §STUN-block).
- Никаких изменений в существующих props / methods. Не трогать строки до line 49 и после `applyAutoReconnectToManager`.
- Документ-комментарий блока: «// Phase 10 / D-15 / RESEARCH.md §Pattern 2 + §Pitfall 5. App Group suite для всех ключей, читаемых из NE extension (mux/stun/utls/enforceRoutes); .standard для main-app-only ключей (cdn/certPinning).»

В `BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/SettingsViewModelTests.swift`:
- Добавить 6 новых тестов согласно списку behavior выше. Использовать тот же test framework, который уже в файле (Swift Testing `@Test` либо XCTest `func testXxx`). Каждый тест ДО создания `SettingsViewModel()` должен удалить тестовые ключи из обоих storage locations (UserDefaults.standard и `UserDefaults(suiteName: "group.app.bbtb.shared")?`), используя `removeObject(forKey:)`. После теста (или внутри defer) — тоже cleanup.
- Hardcoded suite name = `"group.app.bbtb.shared"` (matches config.json).
  </action>
  <verify>
    <automated>cd BBTB && swift test --package-path Packages/AppFeatures --filter SettingsViewModelTests/test_phase10 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'BIO-04.*\[ \].*Out of Scope' .planning/REQUIREMENTS.md` returns 0 (BIO-04 stays in-scope, not crossed out).
    - `grep -c 'BIO-01.*Out of Scope v0.10' .planning/REQUIREMENTS.md` returns ≥ 1.
    - `grep -c 'BIO-02.*Out of Scope v0.10' .planning/REQUIREMENTS.md` returns ≥ 1.
    - `grep -c 'BIO-03.*Out of Scope v0.10' .planning/REQUIREMENTS.md` returns ≥ 1.
    - `grep -c 'ONDEMAND-01.*Out of Scope v0.10' .planning/REQUIREMENTS.md` returns ≥ 1.
    - `grep -n '^**Requirements:** UX-06, DPI-05, DPI-06, DPI-08, DPI-09, BIO-04, KILL-04' .planning/ROADMAP.md` returns exact match line in Phase 10 entry.
    - `grep -c '10-01-PLAN.md.*UX skeleton' .planning/ROADMAP.md` ≥ 1.
    - `grep -c '10-06-PLAN.md' .planning/ROADMAP.md` ≥ 1.
    - `grep -c 'cdnFrontingEnabled' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift` ≥ 1.
    - `grep -c 'muxEnabled' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift` ≥ 1.
    - `grep -c 'stunBlockEnabled' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift` ≥ 1.
    - `grep -c 'certPinningEnabled' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift` ≥ 1.
    - `grep -c 'utlsFingerprint' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift` ≥ 1.
    - `grep -c 'macOSDisableEnforceRoutes' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift` ≥ 1.
    - `grep -c 'utlsOptions: \[String\]' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift` ≥ 1.
    - `grep -cE 'store:.*UserDefaults\(suiteName: "group.app.bbtb.shared"\)' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift` ≥ 4 (mux + stun + utls + enforceRoutes).
    - `cd BBTB && swift test --package-path Packages/AppFeatures --filter SettingsViewModelTests` exits 0; output shows ≥ 6 new test names containing `test_phase10` or `test_macOSDisableEnforceRoutes` or `test_utlsOptions` or `test_stunBlockShowConfirm`.
    - No regression in existing SettingsViewModelTests (all previously-passing tests still pass).
  </acceptance_criteria>
  <done>Скоуп-сдвиг зафиксирован в обоих документах; 6 новых toggle properties + utlsOptions static + stunBlockShowConfirm published — все на месте; 4 ключа, читаемые extension'ом, привязаны к App Group suite; добавлено ≥ 6 unit-тестов, все зелёные.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AdvancedSettingsView 5-секционная реструктуризация + AntiDPISection + SecuritySection + UTLSPickerView + L10n keys</name>
  <files>BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdvancedSettingsView.swift, BBTB/Packages/AppFeatures/Sources/SettingsFeature/AntiDPISection.swift, BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift, BBTB/Packages/AppFeatures/Sources/SettingsFeature/UTLSPickerView.swift, BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings</files>
  <read_first>
    - BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdvancedSettingsView.swift (целиком — Form structure)
    - BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdBlockToggleSection.swift (Section row pattern)
    - BBTB/Packages/AppFeatures/Sources/SettingsFeature/KillSwitchToggleSection.swift (toggle + footer pattern)
    - BBTB/Packages/AppFeatures/Sources/SettingsFeature/AutoReconnectToggleSection.swift (newer pattern с @ObservedObject vs @Binding)
    - BBTB/Packages/AppFeatures/Sources/SettingsFeature/MinAppVersionBanner.swift (header banner shape)
    - BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings (формат JSON xcstrings + существующие ключи settings.*)
    - .planning/phases/10-advanced-settings-security-polish/10-UI-SPEC.md §Layout Contract (lines 199-269), §Copywriting (lines 119-175), §Accessibility (lines 327-355), §State Transitions (lines 384-403)
    - .planning/phases/10-advanced-settings-security-polish/10-RESEARCH.md §Pattern 1 (Form Section) + §Pitfall 7 (Picker raw value String)
  </read_first>
  <action>
Создать новый файл `BBTB/Packages/AppFeatures/Sources/SettingsFeature/UTLSPickerView.swift` — `public struct UTLSPickerView: View` принимает `@Binding var selection: String`. Использует `Picker(L10n.settingsAntiDpiUtlsLabel, selection: $selection)` с `.pickerStyle(.menu)`; iterator по `SettingsViewModel.utlsOptions` (lookup статической константы из VM). Каждый ForEach элемент — `Text(uTLSLocalizedLabel(for: $0)).tag($0)`, где `uTLSLocalizedLabel(for:)` — приватная вспомогательная функция мапящая `"random"` → `L10n.settingsAntiDpiUtlsOptionRandom`, `"chrome"` → `L10n.settingsAntiDpiUtlsOptionChrome` и т.д. (7 веток). Установить `.accessibilityIdentifier("bbtb.settings.antiDpi.utls.picker")` и `.accessibilityHint(Text(L10n.settingsAntiDpiUtlsFooter))`. **Дефенсивный fallback** (UI-SPEC §Edge States): если `selection` имеет значение не из утls Options — отобразить `random` (но в storage не переписывать; just display). Реализовать через computed `displayedSelection: String { utlsOptions.contains(selection) ? selection : "random" }`.

Создать новый файл `BBTB/Packages/AppFeatures/Sources/SettingsFeature/AntiDPISection.swift` — `public struct AntiDPISection: View` принимает `@ObservedObject var viewModel: SettingsViewModel`. Тело возвращает `Section { ... } header: { Text(L10n.settingsAntiDpiSection) } footer: { Text(L10n.settingsAntiDpiSectionFooter) }`. Внутри 4 rows строго в порядке D-15 / UI-SPEC §Per-section content ordering:
  1. `Toggle(L10n.settingsAntiDpiCdnLabel, isOn: $viewModel.cdnFrontingEnabled).accessibilityIdentifier("bbtb.settings.antiDpi.cdn.toggle").accessibilityHint(Text(L10n.settingsAntiDpiCdnFooter))`.
  2. `Toggle(L10n.settingsAntiDpiMuxLabel, isOn: $viewModel.muxEnabled).accessibilityIdentifier("bbtb.settings.antiDpi.mux.toggle").accessibilityHint(Text(L10n.settingsAntiDpiMuxFooter))`.
  3. `UTLSPickerView(selection: $viewModel.utlsFingerprint)` (которая внутри уже ставит accessibilityIdentifier).
  4. STUN-блок toggle с custom binding, который перехватывает OFF→ON и показывает alert. Реализация:
     - Computed binding `stunBindingProxy: Binding<Bool>` — get returns `viewModel.stunBlockEnabled`; set:
       - Если new == true && current == false → не записывать в storage; вместо этого выставить `viewModel.stunBlockShowConfirm = true` (alert триггер).
       - Если new == false → записать в storage напрямую (`viewModel.stunBlockEnabled = false`).
     - `Toggle(L10n.settingsAntiDpiStunLabel, isOn: stunBindingProxy).accessibilityIdentifier("bbtb.settings.antiDpi.stun.toggle")`.
     - Sibling `.alert(L10n.settingsAntiDpiStunConfirmTitle, isPresented: $viewModel.stunBlockShowConfirm)` с двумя кнопками: destructive «Включить» → set `viewModel.stunBlockEnabled = true` + `UIImpactFeedbackGenerator(style: .light).impactOccurred()` (под `#if canImport(UIKit) && !os(watchOS)`); cancel «Отмена» → no-op (stunBlockEnabled остаётся false; toggle вернётся в OFF т.к. binding read возвращает storage value). `.message`: `Text(L10n.settingsAntiDpiStunConfirmMessage)`.

Создать новый файл `BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift` — `public struct SecuritySection: View` принимает `@ObservedObject var viewModel: SettingsViewModel`. Тело: `Section { ... } header: { Text(L10n.settingsSecuritySection) } footer: { footerText }`. Внутри:
  1. `Toggle(L10n.settingsSecurityCertPinningLabel, isOn: $viewModel.certPinningEnabled).accessibilityIdentifier("bbtb.settings.security.certPinning.toggle").accessibilityHint(Text(L10n.settingsSecurityCertPinningFooter))`.
  2. `#if os(macOS)` блок: `Toggle(L10n.settingsSecurityEnforceRoutesLabel, isOn: enforceRoutesUIBinding).accessibilityIdentifier("bbtb.settings.security.enforceRoutes.toggle").accessibilityHint(Text(L10n.settingsSecurityEnforceRoutesFooterOn))` где `enforceRoutesUIBinding: Binding<Bool>` — get returns `!viewModel.macOSDisableEnforceRoutes` (инверсия per UI-SPEC §Interaction — UI «enforceRoutes ON» хранится как `macOSDisableEnforceRoutes = false`); set inverts and writes `viewModel.macOSDisableEnforceRoutes = !$0`. `#endif`.
  3. Computed `footerText: Text` — на macOS: если enforceRoutes UI binding == false (т.е. storage `macOSDisableEnforceRoutes == true`), вернуть `Text(L10n.settingsSecurityEnforceRoutesFooterOff).foregroundStyle(.red)`; иначе `Text(L10n.settingsSecurityCertPinningFooter)`. На iOS: только `Text(L10n.settingsSecurityCertPinningFooter)`.

Модифицировать `BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdvancedSettingsView.swift` (полный body Form replacement; не трогать struct decl + viewModel property + navigationTitle modifiers за пределами Form):
  - Перепишем body Form так чтобы порядок секций строго совпал с UI-SPEC §Layout Contract (D-15 LOCKED):
    1. (existing, conditional) `if viewModel.showMinAppVersionBanner { Section { MinAppVersionBanner(...) }.listRowBackground(...) }` — сохранить как есть.
    2. (existing) DNS Section — added `header: { Text(L10n.settingsDnsSection) }` если её не было; footer уже есть.
    3. (NEW) `AntiDPISection(viewModel: viewModel)`.
    4. (NEW) `SecuritySection(viewModel: viewModel)`.
    5. (existing) Rules viewer Section: `Section { RulesViewerSection(snapshot: viewModel.rulesSnapshot) } header: { Text(L10n.settingsRulesViewerSection) }` (header добавить если отсутствует).
    6. (existing) ForceUpdate Section — оставить как есть.
  - Не менять modifier'ы `.navigationTitle` / `.navigationBarTitleDisplayMode`.
  - Не добавлять `.padding(...)` (UI-SPEC §Spacing forbids row padding overrides).

Локализация — добавить в `BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings`. Полный список 19 новых ключей с локализацией ru/en строго по UI-SPEC §Copywriting (lines 130-156). Формат — JSON Apple xcstrings; сохранить existing структуру (sourceLanguage, strings dict). При добавлении ключа использовать тот же `extractionState: "manual"` либо `"migrated"` который уже в файле для других settings.* ключей. Не трогать существующие ключи. Список добавляемых (значения ru / en точно как в UI-SPEC):

  | Key | RU | EN |
  |-----|----|----|
  | settings.antiDpi.section | Anti-DPI | Anti-DPI |
  | settings.antiDpi.sectionFooter | Настройки для обхода ТСПУ. Применяются при следующем подключении. | Anti-DPI settings. Applied on next connection. |
  | settings.antiDpi.cdn.label | CDN-фронтинг | CDN fronting |
  | settings.antiDpi.cdn.footer | Маскирует трафик под обычные сайты через Cloudflare/Fastly. Применяется только к серверам, у которых в подписке указаны CDN-данные. | Masks traffic as ordinary websites via Cloudflare/Fastly. Applies only to servers that include CDN data in their subscription. |
  | settings.antiDpi.mux.label | Мультиплексирование (Mux) | Multiplexing (Mux) |
  | settings.antiDpi.mux.footer | Объединяет несколько соединений в одно — снижает количество TLS-рукопожатий. Работает для VLESS+TLS, Trojan и Shadowsocks-2022. Несовместимо с Reality/Vision/TUIC/Hysteria2 — для них настройка игнорируется. | Combines multiple connections into one — reduces TLS handshakes. Works for VLESS+TLS, Trojan, Shadowsocks-2022. Not compatible with Reality/Vision/TUIC/Hysteria2 — ignored for those. |
  | settings.antiDpi.utls.label | TLS-отпечаток (uTLS) | TLS fingerprint (uTLS) |
  | settings.antiDpi.utls.footer | Имитирует TLS-рукопожатие популярного браузера. «Случайный» — рекомендация по умолчанию: отпечаток меняется при каждом подключении. | Mimics a popular browser's TLS handshake. "Random" is the recommended default — fingerprint changes on each connection. |
  | settings.antiDpi.utls.option.random | Случайный | Random |
  | settings.antiDpi.utls.option.chrome | Chrome | Chrome |
  | settings.antiDpi.utls.option.firefox | Firefox | Firefox |
  | settings.antiDpi.utls.option.safari | Safari | Safari |
  | settings.antiDpi.utls.option.ios | iOS Safari | iOS Safari |
  | settings.antiDpi.utls.option.android | Android | Android |
  | settings.antiDpi.utls.option.edge | Edge | Edge |
  | settings.antiDpi.stun.label | Блокировать STUN-трафик | Block STUN traffic |
  | settings.antiDpi.stun.footer | Защищает от утечки IP через WebRTC. Сломает видеозвонки в браузере (Google Meet, Zoom, Discord-web). Не влияет на нативные приложения. | Protects against WebRTC IP leaks. Will break browser-based video calls (Google Meet, Zoom, Discord web). Does not affect native apps. |
  | settings.antiDpi.stun.confirm.title | Включить блокировку STUN? | Block STUN traffic? |
  | settings.antiDpi.stun.confirm.message | Видеозвонки в браузере перестанут работать, пока опция включена. Нативные приложения (FaceTime, Telegram) не пострадают. | Browser video calls will stop working while this is enabled. Native apps (FaceTime, Telegram) are unaffected. |
  | settings.antiDpi.stun.confirm.action | Включить | Enable |
  | settings.antiDpi.stun.confirm.cancel | Отмена | Cancel |
  | settings.security.section | Безопасность | Security |
  | settings.security.certPinning.label | Pinning сертификата подписки | Subscription certificate pinning |
  | settings.security.certPinning.footer | Защищает соединение с панелью подписок от подмены сертификата. Если pinning сломался после смены сертификата на стороне сервера — временно отключите. | Protects the subscription endpoint connection from certificate substitution. If pinning breaks after a server certificate change — disable temporarily. |
  | settings.security.enforceRoutes.label | Принудительная маршрутизация (macOS) | Force route enforcement (macOS) |
  | settings.security.enforceRoutes.footer.on | Весь трафик идёт через VPN. Если туннель упал — ОС блокирует сетевой трафик до восстановления. Безопаснее держать включённым. | All traffic goes through VPN. If the tunnel fails — the OS blocks traffic until it recovers. Safer to keep enabled. |
  | settings.security.enforceRoutes.footer.off | Выкл = трафик идёт напрямую если VPN упал. Возможны утечки IP. | Off = traffic goes direct if VPN fails. IP leaks are possible. |
  | settings.rules.viewer.section | Правила | Rules |

Локализационные имена в Swift коде — следовать existing `L10n` enum pattern (если в проекте используется генератор SwiftGen/xcstrings literal — посмотреть как существующие ключи `L10n.settingsDnsSection` появляются). Если xcstrings обрабатываются через native String Catalog (Swift compiler-generated), то использовать `String(localized: "settings.antiDpi.section")` напрямую. Executor должен выбрать правильный paradigm на основе того что уже в коде (см. AdvancedSettingsView.swift существующее обращение к L10n).
  </action>
  <verify>
    <automated>cd BBTB && swift build --package-path Packages/AppFeatures 2>&1 | tail -30 && swift test --package-path Packages/AppFeatures 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `swift build --package-path BBTB/Packages/AppFeatures` exits 0.
    - File `BBTB/Packages/AppFeatures/Sources/SettingsFeature/AntiDPISection.swift` exists; `grep -c 'public struct AntiDPISection' $file` ≥ 1.
    - File `BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift` exists; `grep -c 'public struct SecuritySection' $file` ≥ 1.
    - File `BBTB/Packages/AppFeatures/Sources/SettingsFeature/UTLSPickerView.swift` exists; `grep -c 'pickerStyle(.menu)' $file` ≥ 1.
    - `grep -c 'AntiDPISection(viewModel:' BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdvancedSettingsView.swift` ≥ 1.
    - `grep -c 'SecuritySection(viewModel:' BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdvancedSettingsView.swift` ≥ 1.
    - `grep -c '#if os(macOS)' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift` ≥ 1 (enforceRoutes toggle macOS-only).
    - `grep -cE '#if os\(macOS\)' BBTB/Packages/AppFeatures/Sources/SettingsFeature/AntiDPISection.swift` = 0 (Anti-DPI cross-platform).
    - `grep -c 'stunBlockShowConfirm' BBTB/Packages/AppFeatures/Sources/SettingsFeature/AntiDPISection.swift` ≥ 1 (alert binding wired).
    - `grep -c 'accessibilityIdentifier("bbtb.settings.antiDpi' BBTB/Packages/AppFeatures/Sources/SettingsFeature/AntiDPISection.swift` ≥ 3.
    - `grep -c 'accessibilityIdentifier("bbtb.settings.security' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift` ≥ 1.
    - `grep -c 'settings.antiDpi.section' BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings` ≥ 1.
    - `grep -c 'settings.antiDpi.stun.confirm.title' BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings` ≥ 1.
    - `grep -c 'settings.security.enforceRoutes.footer.off' BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings` ≥ 1.
    - `grep -cE '\.padding\(' BBTB/Packages/AppFeatures/Sources/SettingsFeature/AntiDPISection.swift` returns 0 (UI-SPEC forbids padding on Form rows).
    - `grep -cE '\.padding\(' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift` returns 0.
    - `grep -cE 'Color\(hex|Color\(red:' BBTB/Packages/AppFeatures/Sources/SettingsFeature/AntiDPISection.swift BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift BBTB/Packages/AppFeatures/Sources/SettingsFeature/UTLSPickerView.swift` returns 0 (semantic colors only).
    - `grep -cE '\.font\(\.system\(size:' BBTB/Packages/AppFeatures/Sources/SettingsFeature/AntiDPISection.swift BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift BBTB/Packages/AppFeatures/Sources/SettingsFeature/UTLSPickerView.swift` returns 0 (no fixed point sizes).
    - `cd BBTB && swift test --package-path Packages/AppFeatures` returns 0 (all existing tests still pass, including new ones from Task 1).
  </acceptance_criteria>
  <done>5-секционная Form собрана; 3 новых SwiftUI subview скомпилированы и подключены; STUN destructive alert проводка реализована через stunBlockShowConfirm; macOS enforceRoutes — корректная инверсия + красный footer при OFF; 19 L10n ключей добавлены ru+en; build чистый, тесты зелёные. **Snapshot тесты `AdvancedSettingsViewSnapshotTests.swift` (RESEARCH.md Wave 0 Gaps lines 979-980) — deferred-to-manual-UAT** (revision 2026-05-15 checker task_completeness warning resolution): SwiftUI snapshots нестабильны без точного pinned Simulator model/OS fixture (флаки больше пользы на этом этапе); 5-секционный layout валидируется через manual UAT шаг в Plan 06 closure (см. `<verification>` ниже step 4 — Simulator iPhone 16 + macOS smoke).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → @AppStorage | Пользователь меняет toggle; значение сразу пишется в UserDefaults (либо .standard либо App Group suite). |
| Main app → NE extension (через App Group UserDefaults) | Ключи muxEnabled / stunBlockEnabled / utlsFingerprint / macOSDisableEnforceRoutes должны быть видимы extension'у при следующем connect. |
| Documentation drift | REQUIREMENTS.md / ROADMAP.md / wiki — расхождение между задеференным BIO scope и in-scope BIO-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-W1-01 | Tampering | App Group UserDefaults записи извне sandbox | accept | App Group sandbox защищает обе стороны; запись возможна только из main app или extension с тем же App Group entitlement (R6 invariant Phase 1). |
| T-10-W1-02 | Information Disclosure | utlsFingerprint в App Group suite (не секрет, но видно extension'у) | accept | Не PII, не credential; default `random` — не идентифицирует пользователя. |
| T-10-W1-03 | Repudiation | scope amendment без подписи | accept | Git commit history + STATE.md daily log = audit trail. |
| T-10-W1-04 | Misuse | Пользователь видит «macOS enforceRoutes» на iOS и путается | mitigate | `#if os(macOS)` гейт в SecuritySection — toggle physically отсутствует на iOS. UI-SPEC §Platform Conditional Contract. |
| T-10-W1-05 | Misuse | Пользователь случайно включает STUN-блок и звонки в браузере перестают работать | mitigate | Destructive confirmation .alert при OFF→ON per D-16 + footer text «сломает звонки в браузере». UI-SPEC §Interaction. |
| T-10-W1-06 | Tampering | Сломанный xcstrings (битый JSON) → runtime crash при поиске ключа | mitigate | Swift build проверяет xcstrings на синтаксис; missing-key warnings гасятся переводом ru+en для всех 19 ключей. Acceptance criteria grep'ает наличие ключей. |
| T-10-W1-07 | Spoofing | utlsFingerprint в storage — произвольная строка (если кто-то ручками поправит UserDefaults) | mitigate | UTLSPickerView fallback на `"random"` если current value не в utlsOptions (UI-SPEC §Edge States). Хранилище не переписываем — только дисплей. |
</threat_model>

<verification>
Полная проверка Wave 1:

1. `swift build --package-path BBTB/Packages/AppFeatures` чистая на iOS и macOS.
2. `swift test --package-path BBTB/Packages/AppFeatures` — все существующие тесты + ≥ 6 новых для Phase 10 (test_phase10_defaults / persistence / app_group_suite / utlsOptions / stunBlockShowConfirm / keys_no_collision) PASS.
3. `xcodebuild build -scheme BBTB-iOS -destination 'platform=iOS Simulator,name=iPhone 16' -workspace BBTB.xcworkspace` SUCCEEDED (если в проекте xcworkspace; иначе через `tuist generate` + xcodebuild scheme).
4. `xcodebuild build -scheme BBTB-macOS` SUCCEEDED.
5. REQUIREMENTS.md и ROADMAP.md явно отражают: BIO-01/02/03 + ONDEMAND-01 deferred; BIO-04 + KILL-04 in-scope; DPI-05 carry-over из Phase 7a; Phase 10 имеет 6 plans.
6. **Manual UAT — 5-секционный layout** (заменяет deferred snapshot тесты per revision 2026-05-15): запустить приложение в Simulator (iPhone 16 + Mac), открыть Settings → Расширенные. Проверить визуально:
   - Видны 5 секций строго в порядке D-15: DNS / Anti-DPI / Безопасность / Rules (плюс MinAppVersionBanner conditional sektsiya сверху если применимо).
   - В Anti-DPI секции: 3 toggle + 1 picker (CDN-фронтинг, Mux, uTLS Picker, STUN-блок) в порядке D-15.
   - В Безопасность секции: Cert pinning toggle; на macOS — дополнительный enforceRoutes toggle (на iOS отсутствует).
   - Tap'нуть STUN-блок toggle (OFF→ON) — должен появиться destructive .alert; Cancel возвращает toggle в OFF; Включить — пишет в storage.
   - Footer enforceRoutes на macOS — красный при toggle OFF (macOSDisableEnforceRoutes=true).
   - Скриншоты iOS + macOS экранов прикрепить к 10-01-SUMMARY.md как evidence.
</verification>

<success_criteria>
- REQUIREMENTS.md и ROADMAP.md содержат текущий Phase 10 scope (UX-06, DPI-05, DPI-06, DPI-08, DPI-09, BIO-04, KILL-04) с явной формулировкой deferral для BIO-01/02/03 + ONDEMAND-01.
- SettingsViewModel содержит 6 новых @AppStorage props с правильным suite mapping (App Group для extension-reads).
- AdvancedSettingsView рендерит 5 именованных секций; AntiDPISection + SecuritySection + UTLSPickerView — отдельные SwiftUI struct'ы.
- 19 L10n ключей добавлены ru+en в Localizable.xcstrings.
- ≥ 6 unit-тестов в SettingsViewModelTests зелёные; iOS + macOS build SUCCEEDED.
- Никаких visual regression в DNS / Rules секциях (они переиспользуют existing subview).
</success_criteria>

<output>
After completion, create `.planning/phases/10-advanced-settings-security-polish/10-01-SUMMARY.md` со следующими полями:
- Wave 1 outcome: scope amendment + UI skeleton + 6 toggle props.
- Files modified count.
- Tests added count + всё passing.
- Open follow-ups для Plan 02..06 (e.g., «extension читает muxEnabled из App Group suite — verified в Plan 02 expandConfigForTunnel»).
- Commit SHA(s).
</output>
