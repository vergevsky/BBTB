---
phase: 10
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift
  - BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/PlatformSpecific/macOS.swift
  - BBTB/Packages/KillSwitch/Sources/KillSwitch/KillSwitch.swift
  - BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift
  - BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift
  - BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift
  - BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/PlatformHooksTests.swift
  - BBTB/Packages/KillSwitch/Tests/KillSwitchTests/KillSwitchTests.swift
  - BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/SettingsViewModelTests.swift
autonomous: true
requirements: [BIO-04, KILL-04]
tags: [stun-block, kill-switch, enforce-routes, macos, anti-dpi]

must_haves:
  truths:
    - "expandConfigForTunnel инжектирует route.rule для STUN block только когда app.bbtb.stunBlockEnabled = true в App Group UserDefaults"
    - "STUN block правило: port=[3478, 5349], network=\"udp\", action=\"reject\", method=\"drop\" (silent drop, без ICMP unreachable)"
    - "STUN block правило вставлено в route.rules ПОСЛЕ существующего hijack-dns правила (DNS должен работать) и ДО priority rules от Phase 8 rule_set"
    - "Повторный вызов expandConfigForTunnel не дублирует STUN block правило (idempotent — обнаружение по tag=\"bbtb-stun-block\")"
    - "На macOS PlatformHooks.shouldDisableEnforceRoutes() читает app.bbtb.macOSDisableEnforceRoutes из App Group UserDefaults и возвращает его значение"
    - "На iOS PlatformHooks недоступен (#if os(macOS)) — KillSwitch.platformShouldDisableEnforceRoutes() возвращает false без чтения UserDefaults"
    - "KillSwitch.apply(to:enabled:) на macOS при macOSDisableEnforceRoutes=true устанавливает proto.enforceRoutes=false (если includeAllNetworks=true)"
    - "applyEnforceRoutesToManager() async метод существует в SettingsViewModel (#if os(macOS)) и при изменении toggle обновляет существующий NETunnelProviderManager через saveToPreferences"
    - "STUN block injection и Mux injection (Plan 02) сосуществуют — оба шага идут после Phase 8 rule_set и до финальной сериализации; ни один не ломает другой"
  artifacts:
    - path: "BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift"
      provides: "expandConfigForTunnel шаг 6 — STUN block route.rule injection (D-16, BIO-04). Plan 02 уже добавил шаг 7 Mux; этот план встраивает шаг 6 ПЕРЕД шагом 7."
      contains: "bbtb-stun-block"
    - path: "BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/PlatformSpecific/macOS.swift"
      provides: "PlatformHooks.shouldDisableEnforceRoutes() читает реальный @AppStorage через App Group suite"
      contains: "macOSDisableEnforceRoutes"
    - path: "BBTB/Packages/KillSwitch/Sources/KillSwitch/KillSwitch.swift"
      provides: "platformShouldDisableEnforceRoutes() macOS-impl читает App Group suite"
      contains: "macOSDisableEnforceRoutes"
    - path: "BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift"
      provides: "applyEnforceRoutesToManager() async (macOS only) — live-apply existing manager"
      contains: "applyEnforceRoutesToManager"
    - path: "BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift"
      provides: ".onChange(of: macOSDisableEnforceRoutes) wire-up для live-apply вызова"
      contains: "applyEnforceRoutesToManager"
    - path: "BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/PlatformHooksTests.swift"
      provides: "Новый тест файл (macOS only) — App Group UserDefaults read"
      contains: "PlatformHooksTests"
  key_links:
    - from: "SingBoxConfigLoader.expandConfigForTunnel step 6"
      to: "app.bbtb.stunBlockEnabled UserDefaults (App Group suite)"
      via: "UserDefaults(suiteName: AppGroupContainer.identifier).bool"
      pattern: "stunBlockEnabled"
    - from: "KillSwitch.platformShouldDisableEnforceRoutes (macOS)"
      to: "app.bbtb.macOSDisableEnforceRoutes UserDefaults (App Group suite)"
      via: "UserDefaults(suiteName: \"group.app.bbtb.shared\").bool"
      pattern: "macOSDisableEnforceRoutes"
    - from: "SettingsViewModel.applyEnforceRoutesToManager"
      to: "NETunnelProviderManager.saveToPreferences"
      via: "loadAll + filter ours + KillSwitch.apply + save"
      pattern: "applyEnforceRoutesToManager"
---

<objective>
Wave 3 — реализуем оставшиеся два phase-10 «runtime» сценария, которые не требуют новых SwiftPM пакетов. **Зависит от Plan 02** (Mux injection уже встроен как шаг 7), поэтому план встраивает STUN block как шаг 6 — ровно перед существующим шагом 7. Это устраняет file conflict в SingBoxConfigLoader.swift.

1. **BIO-04 STUN-блок** — добавить route.rule reject для UDP 3478/5349 в expandConfigForTunnel шаг 6 (между Phase 8 rule_set и Plan 02's шагом 7 Mux). Идемпотентно через `tag="bbtb-stun-block"`.
2. **KILL-04 macOS enforceRoutes toggle** — закрыть Phase 1 заглушки: `PlatformHooks.shouldDisableEnforceRoutes()` (macOS-only файл) + `KillSwitch.platformShouldDisableEnforceRoutes()` (cross-package, macOS impl) — оба читают `app.bbtb.macOSDisableEnforceRoutes` из App Group UserDefaults. Добавить SettingsViewModel.applyEnforceRoutesToManager() async для live-apply на изменение toggle.

Purpose: после Wave 3 пользователь может: (a) включить STUN-блок → reconnect → видеозвонки в браузере перестают работать (D-16); (b) на macOS выключить enforceRoutes → applyEnforceRoutesToManager обновляет manager → следующий connect использует enforceRoutes=false.

Output: ~50 строк в SingBoxConfigLoader (новый шаг 6) + ~10 строк в каждом из двух macOS hooks + ~30 строк applyEnforceRoutesToManager + .onChange wire-up в SecuritySection + ≥ 7 unit-тестов в трёх тестовых файлах.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-advanced-settings-security-polish/10-CONTEXT.md
@.planning/phases/10-advanced-settings-security-polish/10-RESEARCH.md
@.planning/phases/10-advanced-settings-security-polish/10-PATTERNS.md
@.planning/phases/10-advanced-settings-security-polish/10-01-SUMMARY.md
@.planning/phases/10-advanced-settings-security-polish/10-02-SUMMARY.md

<interfaces>
From `BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift` (after Plan 02 merged):
- `expandConfigForTunnel` теперь имеет шаги 0..5b (existing) + шаг 7 (Mux, Plan 02). Phase 10 Plan 03 встраивает шаг 6 (STUN) **между** шагом 5b и шагом 7.
- Insertion point — после `root["route"] = route` (Phase 8 rule_set финал) и **до** `let muxEnabled = UserDefaults(...).bool(forKey: "app.bbtb.muxEnabled")` (Plan 02 шаг 7 marker).

From `BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/PlatformSpecific/macOS.swift`:
- `#if os(macOS)` enum `PlatformHooks` с `public static func shouldDisableEnforceRoutes() -> Bool { return false }` — Phase 1 заглушка, Phase 10 заменяет return на UserDefaults read.

From `BBTB/Packages/KillSwitch/Sources/KillSwitch/KillSwitch.swift`:
- `public static func platformShouldDisableEnforceRoutes() -> Bool` (line 50) — duplicate hook, тоже Phase 1 заглушка (`return false`). KillSwitch package не зависит от PacketTunnelKit (Phase 1 design), поэтому **не может** напрямую использовать `AppGroupContainer.identifier`. Solution: hardcode suite name string `"group.app.bbtb.shared"` (matches config.json + matches AppGroupContainer constant).
- `public static func apply(to proto: NETunnelProviderProtocol, enabled: Bool)` (line 26) — уже использует platformShouldDisableEnforceRoutes internally; не трогаем сигнатуру.

From `BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift`:
- Pattern для live-apply есть: `applyAutoReconnectToManager() async` (line 271; nonisolated public func). Phase 10 добавляет аналог `applyEnforceRoutesToManager` под `#if os(macOS)`.
- Helper `ManagerSelector.ourManagers(from:)` — фильтрует bbtb managers (Phase 6c W-02).
- Notification `.bbtbProvisionerDidSave` — post после успешного save (используется в applyAutoReconnectToManager).

From `BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift` (создан Wave 1 Plan 01):
- Toggle сейчас изменяет `viewModel.macOSDisableEnforceRoutes` через computed binding (UI inverted). Wave 3 добавляет `.onChange(of: viewModel.macOSDisableEnforceRoutes) { ... Task { await viewModel.applyEnforceRoutesToManager() } }` для live-apply.

Sing-box 1.13 route.rule schema for reject (D-16):
- `tag`: `"bbtb-stun-block"`.
- `port`: `[3478, 5349]` (Array Int).
- `network`: `"udp"`.
- `action`: `"reject"`.
- `method`: `"drop"` — silent drop, без ICMP unreachable, чтобы DPI не получал сигнал блокировки.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: STUN block route.rule injection в expandConfigForTunnel (шаг 6) + 4 unit tests</name>
  <files>BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift, BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift</files>
  <read_first>
    - BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift (целиком — точная sequence шагов ПОСЛЕ Plan 02 merge; должен видеть шаг 7 Mux уже на месте)
    - BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/AppGroupContainer.swift (identifier константа)
    - BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift (целиком — переиспользовать fixture helpers от Plan 02)
    - .planning/phases/10-advanced-settings-security-polish/10-RESEARCH.md §Pattern 3 + §Pitfall 4 (STUN block ломает звонки в браузере) + §Code Examples Step 6 (готовый snippet)
    - .planning/phases/10-advanced-settings-security-polish/10-CONTEXT.md D-16
    - .planning/phases/10-advanced-settings-security-polish/10-PATTERNS.md lines 226-259 (STUN injection snippet)
    - 10-02-SUMMARY.md — точное знание где именно встроен шаг 7 Mux после Plan 02
  </read_first>
  <behavior>
    - Test 1: `test_stun_block_rule_inserted_when_enabled` — App Group UserDefaults `app.bbtb.stunBlockEnabled=true`. expandConfigForTunnel на minimal fixture (route.rules содержит только {action: "hijack-dns"}). Результат: route.rules содержит entry с `tag="bbtb-stun-block"`, `port=[3478,5349]`, `network="udp"`, `action="reject"`, `method="drop"`. Entry находится ПОСЛЕ hijack-dns в порядке rules (index > index hijack-dns).
    - Test 2: `test_stun_block_rule_absent_when_disabled` — stunBlockEnabled=false (или ключ отсутствует). expand на той же fixture. Результат: НИ ОДНО правило не содержит `tag="bbtb-stun-block"`.
    - Test 3: `test_stun_block_idempotent` — stunBlockEnabled=true. Двойной expand. Результат: ровно ОДНО правило с `tag="bbtb-stun-block"` (не дублируется).
    - Test 4: `test_stun_block_shape` — stunBlockEnabled=true. expand. Inspect полное содержимое STUN block правила: keys `tag`, `port`, `network`, `action`, `method`. `port` равен Array `[3478, 5349]` именно в этом порядке; `network` — String `"udp"`; `action` — `"reject"`; `method` — `"drop"`.
    - Test 5: `test_stun_block_coexists_with_mux` — stunBlockEnabled=true И muxEnabled=true. Fixture с trojan outbound + route.rules: [hijack-dns]. Результат: route.rules содержит bbtb-stun-block rule; outbounds[0].multiplex присутствует. Оба phase-10 inject'а совместимы.
    - Test 6 (bonus): `test_stun_block_preserves_phase8_rule_set_priority` — fixture с уже-инжектированными Phase 8 rule_set rules. stunBlockEnabled=true. Результат: STUN block присутствует, Phase 8 rules сохранены без изменений по order'у.
  </behavior>
  <action>
В `SingBoxConfigLoader.swift` ПОСЛЕ Phase 8 шага 5b (rule_set injection, заканчивается на `root["route"] = route`) и **ДО** шага 7 (Mux injection от Plan 02, начинается с `let muxEnabled = UserDefaults(suiteName: AppGroupContainer.identifier)?.bool(forKey: "app.bbtb.muxEnabled")`) — добавить новый блок:

```
// 6. Phase 10 / D-16 / BIO-04 — STUN block route.rule injection.
// Reads app.bbtb.stunBlockEnabled из App Group UserDefaults (записан Wave 1).
// Idempotent: ищем правило с tag="bbtb-stun-block"; если есть — skip.
// Sing-box 1.13.11 НЕ имеет protocol="stun" matcher → используем port+network+action.
// method="drop" — silent drop, без ICMP unreachable (чтобы DPI не получал сигнал).
```

Логика:
- `let stunBlockEnabled = UserDefaults(suiteName: AppGroupContainer.identifier)?.bool(forKey: "app.bbtb.stunBlockEnabled") ?? false`.
- Если `stunBlockEnabled == false` → skip block (продолжить к шагу 7).
- `if var route = root["route"] as? [String: Any]`:
  - `var rules = (route["rules"] as? [[String: Any]]) ?? []`.
  - Idempotency: `let alreadyHasStun = rules.contains { ($0["tag"] as? String) == "bbtb-stun-block" }`. Если true → skip block.
  - Insertion index — после hijack-dns: `let insertIdx = rules.firstIndex { ($0["action"] as? String) == "hijack-dns" }.map { $0 + 1 } ?? rules.count`.
  - Сконструировать stun rule с 5 ключами:
    - `"tag": "bbtb-stun-block"`.
    - `"port": [3478, 5349]`.
    - `"network": "udp"`.
    - `"action": "reject"`.
    - `"method": "drop"`.
  - `rules.insert(stunRule, at: insertIdx)`.
  - `route["rules"] = rules; root["route"] = route`.

В `SingBoxConfigLoaderTests.swift` — добавить 6 тестов (4 базовых + 2 bonus) с описанными behavior. Использовать те же fixture helpers, которые Plan 02 уже добавил (если есть `makeMinimalSingBoxJSON` / `makeOutbound` / `setMuxToggle` helpers — переиспользуй; добавь аналогичный `setStunBlockToggle(_ value: Bool)`). Cleanup `app.bbtb.stunBlockEnabled` в setUp/tearDown.
  </action>
  <verify>
    <automated>cd BBTB && swift test --package-path Packages/PacketTunnelKit --filter SingBoxConfigLoaderTests 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"bbtb-stun-block"' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift` ≥ 1.
    - `grep -c '"app.bbtb.stunBlockEnabled"' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift` ≥ 1.
    - `grep -cE '\[3478, 5349\]' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift` ≥ 1.
    - `grep -c '"method": "drop"' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift` ≥ 1.
    - `grep -c 'test_stun_block' BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift` ≥ 4.
    - `grep -c 'test_stun_block_coexists_with_mux' BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift` ≥ 1 (STUN + Mux integration).
    - `cd BBTB && swift test --package-path Packages/PacketTunnelKit --filter SingBoxConfigLoaderTests` exits 0; output показывает ≥ 4 новых STUN tests PASS; Plan 02 Mux tests тоже остаются PASS.
    - `cd BBTB && swift test --package-path Packages/PacketTunnelKit` exits 0 (полный package suite).
  </acceptance_criteria>
  <done>STUN block injection реализован как шаг 6; сосуществует с шагом 7 Mux; idempotent; 6 unit-тестов зелёные.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: macOS enforceRoutes — PlatformHooks + KillSwitch implementation + applyEnforceRoutesToManager + onChange wire + 3 unit tests</name>
  <files>BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/PlatformSpecific/macOS.swift, BBTB/Packages/KillSwitch/Sources/KillSwitch/KillSwitch.swift, BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift, BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift, BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/PlatformHooksTests.swift, BBTB/Packages/KillSwitch/Tests/KillSwitchTests/KillSwitchTests.swift, BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/SettingsViewModelTests.swift</files>
  <read_first>
    - BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/PlatformSpecific/macOS.swift (целиком — текущая Phase 1 заглушка)
    - BBTB/Packages/KillSwitch/Sources/KillSwitch/KillSwitch.swift (целиком — apply(to:enabled:) логика + platformShouldDisableEnforceRoutes заглушка line 50-55)
    - BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift (целиком — особенно applyAutoReconnectToManager как pattern reference; см. RESEARCH.md §Code Examples → macOS enforceRoutes live-apply listing)
    - BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift (создан Wave 1 — куда добавить .onChange)
    - BBTB/Packages/AppFeatures/Sources/SettingsFeature/ManagerSelector.swift (если существует; Phase 6c artifact для ourManagers helper)
    - BBTB/Packages/KillSwitch/Tests/KillSwitchTests/KillSwitchTests.swift (текущий test файл)
    - .planning/phases/10-advanced-settings-security-polish/10-RESEARCH.md §Pattern 5 (PlatformHooks indirection) + §Pitfall 5 (@AppStorage extension visibility) + §Code Examples → macOS enforceRoutes live-apply (целиком)
    - .planning/phases/10-advanced-settings-security-polish/10-CONTEXT.md D-17
  </read_first>
  <behavior>
    - Test 1 (PlatformHooksTests, macOS-only): `test_shouldDisableEnforceRoutes_reads_app_group_userDefaults` — записать в `UserDefaults(suiteName: "group.app.bbtb.shared")?` ключ `app.bbtb.macOSDisableEnforceRoutes=true`. `PlatformHooks.shouldDisableEnforceRoutes()` возвращает true. Затем записать false → returns false. Затем remove key → returns false (default).
    - Test 2 (KillSwitchTests, macOS-only): `test_apply_respects_macOS_disable_enforceRoutes_toggle` — создать NETunnelProviderProtocol; записать App Group key=true; `KillSwitch.apply(to: proto, enabled: true)`; assert `proto.enforceRoutes == false`. Затем key=false → apply → `proto.enforceRoutes == true` (с includeAllNetworks=true).
    - Test 3 (KillSwitchTests, iOS-only): `test_apply_ignores_enforceRoutes_toggle_on_iOS` — проверка через compilation: на iOS scheme билд проходит, `platformShouldDisableEnforceRoutes()` возвращает false независимо от UserDefaults записи. (Если можно реально вызвать на iOS — записать suite key=true → call → return false.)
    - Test 4 (SettingsViewModelTests, macOS-only): `test_applyEnforceRoutesToManager_method_exists_and_callable` — smoke test: создать VM, await `vm.applyEnforceRoutesToManager()`, не должно бросать (graceful handle отсутствия managers через log.warning). Не assert state — integration test через UAT.
  </behavior>
  <action>
**File 1**: `BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/PlatformSpecific/macOS.swift` — заменить тело `shouldDisableEnforceRoutes`:
- Внутри `#if os(macOS)` блока: `public static func shouldDisableEnforceRoutes() -> Bool`:
  - `let defaults = UserDefaults(suiteName: AppGroupContainer.identifier)`.
  - `return defaults?.bool(forKey: "app.bbtb.macOSDisableEnforceRoutes") ?? false`.
- Обновить doc-comment: убрать «Phase 1 — hardcoded false»; написать «Phase 10 / D-17 / KILL-04 — читает app.bbtb.macOSDisableEnforceRoutes из App Group UserDefaults suite (записан SettingsViewModel @AppStorage). Default false = enforceRoutes остаётся true (текущее поведение).»

**File 2**: `BBTB/Packages/KillSwitch/Sources/KillSwitch/KillSwitch.swift` — заменить тело `platformShouldDisableEnforceRoutes`:
- `public static func platformShouldDisableEnforceRoutes() -> Bool`:
  - `#if os(macOS)`:
    - `let defaults = UserDefaults(suiteName: "group.app.bbtb.shared")` (hardcoded — KillSwitch package не depends on PacketTunnelKit; suite name синхронизирован с `.planning/config.json` `app_group` field).
    - `return defaults?.bool(forKey: "app.bbtb.macOSDisableEnforceRoutes") ?? false`.
  - `#else`: `return false`.
- Обновить doc-comment аналогично File 1.
- Добавить в комментарий note: «Suite name строка hardcoded — KillSwitch package не depends on PacketTunnelKit. При изменении app_group в config.json обновить здесь.»

**File 3**: `BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift` — добавить **в конец файла** (после существующих методов; не модифицировать существующие):
- `#if os(macOS) extension SettingsViewModel { ... } #endif` блок:
  - `nonisolated public func applyEnforceRoutesToManager() async`:
    - Логика согласно RESEARCH.md §Code Examples → macOS enforceRoutes live-apply (полный listing предоставлен):
      1. `let log = Logger(subsystem: "app.bbtb.client", category: "settings-enforce-routes")`.
      2. Try `let managers = try await NETunnelProviderManager.loadAllFromPreferences()`. Catch → log warning, return.
      3. `let ours = ManagerSelector.ourManagers(from: managers)` (используем существующий Phase 6c helper).
      4. `var anyManagerSaved = false`.
      5. For each manager в ours:
         - Guard `let proto = manager.protocolConfiguration as? NETunnelProviderProtocol`.
         - `let killSwitchEnabled = UserDefaults.standard.object(forKey: "app.bbtb.killSwitchEnabled") as? Bool ?? false`.
         - `KillSwitch.apply(to: proto, enabled: killSwitchEnabled)`.
         - Try `try await manager.saveToPreferences()` + `try await manager.loadFromPreferences()`. Catch → log.error, continue.
         - При успехе: `anyManagerSaved = true`.
      6. Если `anyManagerSaved` — `NotificationCenter.default.post(name: .bbtbProvisionerDidSave, object: nil)`.

**File 4**: `BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift` (созданный Wave 1) — добавить modifier к macOS-only enforceRoutes Toggle (под существующим `#if os(macOS)`):
- `.onChange(of: viewModel.macOSDisableEnforceRoutes) { _, _ in Task { await viewModel.applyEnforceRoutesToManager() } }`.
- iOS 17+ / macOS 14+ two-parameter signature (project min target iOS 18 / macOS 15).

**Тесты:**

`BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/PlatformHooksTests.swift` (NEW file, целиком создаётся):
- `#if os(macOS) import XCTest @testable import PacketTunnelKit final class PlatformHooksTests: XCTestCase { ... } #endif`.
- setUp/tearDown: cleanup `UserDefaults(suiteName: "group.app.bbtb.shared")?.removeObject(forKey: "app.bbtb.macOSDisableEnforceRoutes")`.
- `func test_shouldDisableEnforceRoutes_default_false`: ничего не записываем → `XCTAssertFalse(PlatformHooks.shouldDisableEnforceRoutes())`.
- `func test_shouldDisableEnforceRoutes_reads_true_from_app_group`: записать suite key=true → `XCTAssertTrue(PlatformHooks.shouldDisableEnforceRoutes())`.
- `func test_shouldDisableEnforceRoutes_reads_false_from_app_group`: записать suite key=false → `XCTAssertFalse`.

`BBTB/Packages/KillSwitch/Tests/KillSwitchTests/KillSwitchTests.swift` — добавить (не заменять):
- `func test_apply_respects_macOS_disable_enforceRoutes_toggle` (macOS only): создать `NETunnelProviderProtocol()`; cleanup suite key; записать `UserDefaults(suiteName: "group.app.bbtb.shared")?.set(true, forKey: "app.bbtb.macOSDisableEnforceRoutes")`; `KillSwitch.apply(to: proto, enabled: true)`; `XCTAssertFalse(proto.enforceRoutes)`. Затем cleanup; записать false; apply; `XCTAssertTrue(proto.enforceRoutes)`.
- `func test_apply_iOS_ignores_disable_toggle` (iOS only): аналогично но `XCTAssertTrue(proto.enforceRoutes)` независимо от suite key value.

`BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/SettingsViewModelTests.swift` — добавить (macOS only):
- `func test_applyEnforceRoutesToManager_method_exists_and_callable` (macOS only): smoke — создать VM, await `vm.applyEnforceRoutesToManager()`, не должно бросать. Не assert state.
  </action>
  <verify>
    <automated>cd BBTB && swift test --package-path Packages/PacketTunnelKit --filter PlatformHooksTests 2>&1 | tail -10 && swift test --package-path Packages/KillSwitch 2>&1 | tail -10 && swift test --package-path Packages/AppFeatures --filter SettingsViewModelTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'macOSDisableEnforceRoutes' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/PlatformSpecific/macOS.swift` ≥ 1.
    - `grep -c 'AppGroupContainer.identifier' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/PlatformSpecific/macOS.swift` ≥ 1.
    - PlatformSpecific/macOS.swift: `grep -vE '^\s*//|^\s*\*' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/PlatformSpecific/macOS.swift | grep -c 'return false$'` = 0 (хардкод false больше не присутствует в коде; разрешено только в комментариях).
    - `grep -c 'macOSDisableEnforceRoutes' BBTB/Packages/KillSwitch/Sources/KillSwitch/KillSwitch.swift` ≥ 1.
    - `grep -c '"group.app.bbtb.shared"' BBTB/Packages/KillSwitch/Sources/KillSwitch/KillSwitch.swift` ≥ 1 (hardcoded suite — KillSwitch не depends on PacketTunnelKit).
    - `grep -cE '#if os\(macOS\)' BBTB/Packages/KillSwitch/Sources/KillSwitch/KillSwitch.swift` ≥ 1 (platform-conditional).
    - `grep -c 'applyEnforceRoutesToManager' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift` ≥ 1.
    - `grep -c 'nonisolated public func applyEnforceRoutesToManager' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift` ≥ 1.
    - `grep -c '.bbtbProvisionerDidSave' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift` ≥ 2 (existing autoReconnect + new enforceRoutes).
    - `grep -cE '\.onChange\(of: viewModel\.macOSDisableEnforceRoutes' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SecuritySection.swift` ≥ 1.
    - File `BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/PlatformHooksTests.swift` exists.
    - `cd BBTB && swift test --package-path Packages/PacketTunnelKit --filter PlatformHooksTests` exits 0 (на macOS scheme — ≥ 3 tests PASS).
    - `cd BBTB && swift test --package-path Packages/KillSwitch` exits 0; ≥ 1 новый test про enforce routes toggle PASS.
    - `cd BBTB && swift test --package-path Packages/AppFeatures` exits 0; новый test про applyEnforceRoutesToManager existence PASS.
    - На iOS scheme — все package builds SUCCEEDED.
  </acceptance_criteria>
  <done>PlatformHooks + KillSwitch macOS implementations читают App Group UserDefaults; applyEnforceRoutesToManager wired через .onChange в SecuritySection; новые тесты в трёх test файлах зелёные; iOS scheme не сломан.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → @AppStorage (App Group) | macOS toggle меняет ключ — обе стороны (main app для UI + extension для apply) читают то же значение. |
| KillSwitch package → UserDefaults | KillSwitch package не depends on PacketTunnelKit (Phase 1 design); hardcodes suite name. Drift risk при изменении app_group. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-W3-01 | Tampering | App Group UserDefaults перехватывает не-наш bundle | accept | App Group sandbox + same team-id. R6 invariant. |
| T-10-W3-02 | Misuse | Пользователь выключает enforceRoutes на macOS и теряет IP-защиту при падении туннеля | accept | UI footer .red + предупреждение в Russian (D-17). Информированный opt-out. |
| T-10-W3-03 | Information Disclosure | STUN-блок выключен по умолчанию → WebRTC leak возможен | accept | D-16 UX trade-off — STUN OFF default. FAQ в Phase 11. |
| T-10-W3-04 | DoS | sing-box 1.13.11 не поддерживает `method=drop` → expand throws | mitigate | RESEARCH.md A7 — verified в sing-box docs. Defensive: rule всё равно работает (reject) если method unknown. |
| T-10-W3-05 | Configuration drift | `"group.app.bbtb.shared"` hardcoded в KillSwitch | mitigate | Doc-comment предупреждает разработчика при изменении app_group. Acceptance criteria grep подтверждает строку. |
| T-10-W3-06 | Invariant break | Шаги 6 (STUN) + 7 (Mux) coexistence | mitigate | Test `test_stun_block_coexists_with_mux` явно покрывает оба inject'а одновременно. |
| T-10-W3-07 | Race | applyEnforceRoutesToManager вызывается одновременно с другим saveToPreferences | accept | Apple managed: saveToPreferences serialized. Phase 6c аналогичный pattern без race issues. |
</threat_model>

<verification>
Wave 3 verification:
1. `cd BBTB && swift test` всех пакетов зелёный.
2. `xcodebuild build -scheme BBTB-iOS -destination 'platform=iOS Simulator,name=iPhone 16'` SUCCEEDED.
3. `xcodebuild build -scheme BBTB-macOS` SUCCEEDED.
4. На macOS Simulator: toggle macOS enforceRoutes в Расширенных → Console.app показывает `applyEnforceRoutesToManager` log entry; bbtbProvisionerDidSave notification posted.
5. Fixture VLESS+Trojan с muxEnabled+stunBlockEnabled оба true → expand → multiplex для Trojan, нет multiplex для VLESS+Reality, есть bbtb-stun-block правило.
6. `validate-r1-r6.sh` (если доступен) PASS.
</verification>

<success_criteria>
- BIO-04 STUN-блок: route.rule инжектится строго с port=[3478,5349] / network=udp / action=reject / method=drop при toggle ON; idempotent через tag; coexists с Mux injection (Plan 02).
- KILL-04 macOS enforceRoutes: оба hook'а (PlatformHooks + KillSwitch) читают App Group UserDefaults; applyEnforceRoutesToManager() реализует live-apply при изменении toggle.
- ≥ 7 unit-тестов across 3 test файлов зелёные.
- Никаких регрессий в Phase 1 / 6 / 8 invariants.
</success_criteria>

<output>
After completion, create `.planning/phases/10-advanced-settings-security-polish/10-03-SUMMARY.md` со следующими полями:
- STUN block + macOS enforceRoutes реализованы.
- Files changed: 7.
- Tests added count.
- Commit SHA(s).
</output>
