---
phase: 10
plan: 05
type: execute
wave: 3
depends_on: [10-01, 10-04]
files_modified:
  - BBTB/Packages/FrontingEngine/Package.swift
  - BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingProfile.swift
  - BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CDNProviderAdapter.swift
  - BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CloudflareAdapter.swift
  - BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FastlyAdapter.swift
  - BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CustomCDNAdapter.swift
  - BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingConfigApplier.swift
  - BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingFailureCache.swift
  - BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingFallbackChain.swift
  - BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingError.swift
  - BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/AppGroupContainer.swift
  - BBTB/Packages/FrontingEngine/Tests/FrontingEngineTests/FrontingConfigApplierTests.swift
  - BBTB/Packages/FrontingEngine/Tests/FrontingEngineTests/FrontingFallbackChainTests.swift
  - BBTB/Packages/FrontingEngine/Tests/FrontingEngineTests/FrontingProfileTests.swift
autonomous: true
requirements: [DPI-06]
tags: [cdn, fronting, anti-dpi, swiftpm-package]

must_haves:
  truths:
    - "Новый SwiftPM пакет FrontingEngine компилируется на iOS 18+ и macOS 15+"
    - "FrontingProfile — Codable Sendable struct с полями provider, connectHost, connectPort, sniHost, httpHost, mode (соответствует D-03 schema)"
    - "CDNProviderAdapter protocol определяет applyFronting(to:profile:) static func, реализуется CloudflareAdapter / FastlyAdapter / CustomCDNAdapter"
    - "FrontingConfigApplier.apply(outbound:profile:) меняет outbound.server, outbound.server_port, outbound.tls.server_name; для transport.type=ws — transport.headers.Host; для httpupgrade — transport.host; для grpc — только server_name (D-05)"
    - "FrontingConfigApplier пропускает outbound (return unchanged) когда outbound.type ∈ {tuic, hysteria2} (D-05 blacklist)"
    - "FrontingConfigApplier пропускает outbound (return unchanged) когда outbound.tls.reality.enabled == true (Reality защита XTLS)"
    - "FrontingFailureCache — actor с in-memory + App Group JSON persistence; cooldown 6-24ч проверяется через computed deadline"
    - "FrontingFallbackChain — actor с providers + .domain → .ipPool → fallback на direct/next-node; sequential, concurrency=1 (DEC-06d-04)"
    - "AppGroupContainer.cdnFailureCacheURL — точка persistence для FailureCache (Library/Caches/cdn/cdn-failure-cache.json)"
  artifacts:
    - path: "BBTB/Packages/FrontingEngine/Package.swift"
      provides: "Новый SwiftPM пакет: swift-tools-version 6.0, iOS 18 / macOS 15, library FrontingEngine, deps VPNCore + PacketTunnelKit (для AppGroupContainer), resources block"
      contains: "FrontingEngine"
    - path: "BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingProfile.swift"
      provides: "Codable Sendable struct + nested CDNProvider enum + FrontingMode enum"
      contains: "FrontingProfile"
    - path: "BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CDNProviderAdapter.swift"
      provides: "Protocol + provider/displayName/applyFronting static API"
      contains: "CDNProviderAdapter"
    - path: "BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CloudflareAdapter.swift"
      provides: "CloudflareAdapter: CDNProviderAdapter — переопределяет SNI + Host header (D-05 mapping)"
      contains: "CloudflareAdapter"
    - path: "BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FastlyAdapter.swift"
      provides: "FastlyAdapter — аналог Cloudflare с Fastly-specific header naming"
      contains: "FastlyAdapter"
    - path: "BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CustomCDNAdapter.swift"
      provides: "CustomCDNAdapter — generic SNI + Host swap, без CDN-specific assumptions"
      contains: "CustomCDNAdapter"
    - path: "BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingConfigApplier.swift"
      provides: "Static apply(json:profile:adapter:) + apply(outbound:profile:adapter:) (json variant для batch, outbound variant — single)"
      contains: "FrontingConfigApplier"
    - path: "BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingFailureCache.swift"
      provides: "Actor с failureScores[(provider, ip, networkType)] → score + cooldownUntil"
      contains: "FrontingFailureCache"
    - path: "BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingFallbackChain.swift"
      provides: "Actor — async nextEndpoint(for serverID:cancellation:) returns (FrontingProfile?, exhausted: Bool)"
      contains: "FrontingFallbackChain"
    - path: "BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingError.swift"
      provides: "Error enum: malformedJSON, unsupportedTransport, providerBlacklisted, fallbackExhausted"
      contains: "FrontingError"
    - path: "BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/AppGroupContainer.swift"
      provides: "cdnFailureCacheURL → Library/Caches/cdn/cdn-failure-cache.json"
      contains: "cdnFailureCacheURL"
  key_links:
    - from: "FrontingConfigApplier.apply"
      to: "CDNProviderAdapter.applyFronting"
      via: "static dispatch через adapter: any CDNProviderAdapter.Type parameter"
      pattern: "adapter.applyFronting"
    - from: "FrontingFailureCache"
      to: "AppGroupContainer.cdnFailureCacheURL"
      via: "JSON read/write на disk"
      pattern: "cdnFailureCacheURL"
---

<objective>
Wave 2 — реализуем **DPI-06 CDN-фронтинг** как отдельный SwiftPM пакет `FrontingEngine`, по архитектуре D-03..D-07 (Codex thread `019e2b02-09fc-77b1-8acc-cc4f794c5235` recommended).

Архитектура:
1. **FrontingProfile** — Codable struct в FrontingEngine. Не часть TransportConfig (D-03 critical decision — иначе 50+ транспортов дублируют CDN логику).
2. **CDNProviderAdapter** protocol — open для добавления Cloudflare/Fastly/Bunny/Custom без правки TransportRegistry или PacketTunnelKit.
3. **FrontingConfigApplier** — JSON overlay над `expandConfigForTunnel` output. Меняет `server` / `tls.server_name` / `transport.headers.Host` (WS) / `transport.host` (HTTPUpgrade). Skip Reality/TUIC/Hysteria2 (D-05).
4. **FrontingFailureCache** — actor + App Group JSON persistence: score + cooldown 6-24ч на `(provider, ip, networkType)` (D-06).
5. **FrontingFallbackChain** — actor sequential provider chain (DEC-06d-04 concurrency=1).

Purpose: **пакет код-готов, но НЕ wired в production yet** — wiring через ConfigImporter происходит в Plan 06 (Wave 3). Wave 2 даёт unit-tested пакет; Wave 3 интегрирует в pipeline. Это правильная separation для context window — Plan 05 huge но контейнирован.

Output: ~10 новых Swift файлов в новом FrontingEngine package + 1 modified AppGroupContainer.swift + ≥ 8 unit-тестов across 3 test файлов + Tuist integration (Plan 06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-advanced-settings-security-polish/10-CONTEXT.md
@.planning/phases/10-advanced-settings-security-polish/10-RESEARCH.md
@.planning/phases/10-advanced-settings-security-polish/10-PATTERNS.md
@.planning/phases/10-advanced-settings-security-polish/10-01-SUMMARY.md

<interfaces>
From `BBTB/Packages/RulesEngine/Package.swift`:
- Pattern для нового SwiftPM пакета:
  - `swift-tools-version: 6.0`
  - `platforms: [.iOS(.v18), .macOS(.v15)]`
  - `products: [.library(name: "FrontingEngine", targets: ["FrontingEngine"])]`
  - `dependencies: [.package(path: "../VPNCore"), .package(path: "../PacketTunnelKit")]` (для AppGroupContainer)
  - `targets: [.target(name: "FrontingEngine", dependencies: [...], resources: [.process("Resources")]), .testTarget(...)]`
  - testTarget linkerSettings: mirror RulesEngine pattern (libbox cascade)

From `BBTB/Packages/TransportRegistry/Sources/TransportRegistry/TransportHandler.swift`:
- Pattern для CDNProviderAdapter protocol:
  - `public protocol TransportHandler: Sendable { static var identifier: String { get }; static var displayName: String { get }; static var supportedProtocols: [String] { get }; static func buildTransportBlock(for config: TransportConfig) -> [String: Any]? }`.

From `BBTB/Packages/VPNCore/Sources/VPNCore/TransportConfig.swift`:
- Pattern для FrontingProfile Codable + Sendable + Equatable + Hashable enum/struct.

From `BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/AppGroupContainer.swift`:
- Add new path `cdnFailureCacheURL`: `Library/Caches/cdn/cdn-failure-cache.json` (mirror rulesCacheDirectory pattern).
- Plan 04 уже добавляет `certPinManifestDirectory` — этот Plan 05 добавляет cdnFailureCacheURL рядом. Файл будет touched обоими планами (Plan 04 + Plan 05). **Wave 2 параллелизация**: оба плана модифицируют AppGroupContainer.swift → возможный конфликт. Executor должен либо последовательно применять (Plan 04 → Plan 05 в одной wave merge), либо carefully objet-merge на git level. Содержательно — два независимых property addition; merge тривиален.

From `BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift`:
- Pattern для JSON dict mutation в `expandConfigForTunnel` lines 270-323 — paragon для FrontingConfigApplier.apply.

Sing-box 1.13.11 transport schema (verified):
- WebSocket outbound: `outbound.transport = {"type": "ws", "path": "/path", "headers": {"Host": "..."}}`. CDN: подменить `headers.Host` на frontingProfile.httpHost.
- HTTPUpgrade outbound: `outbound.transport = {"type": "httpupgrade", "host": "...", "path": "/"}`. CDN: подменить transport.host.
- gRPC outbound: `outbound.transport = {"type": "grpc", "service_name": "..."}`. CDN: только server_name (TLS SNI) меняется; service_name остаётся.
- TCP outbound: нет transport блока. CDN inapplicable.

CDN-mapping table из D-05 / RESEARCH.md Pattern 5:

| Outbound transport type | What CDN adapter changes |
|--------------------------|--------------------------|
| ws | `transport.headers.Host` |
| httpupgrade | `transport.host` |
| grpc | (nothing in transport — server_name via tls already covers SNI) |
| tcp / nil | (no transport block; if CDN required, profile defines TLS server_name only) |

D-05 blacklist: `type ∈ {tuic, hysteria2}` ИЛИ `tls.reality.enabled == true` ИЛИ `flow == "xtls-rprx-vision"` → skip apply.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: FrontingEngine SwiftPM package scaffolding + FrontingProfile + CDNProviderAdapter + 3 adapters + FrontingError + AppGroupContainer.cdnFailureCacheURL</name>
  <files>BBTB/Packages/FrontingEngine/Package.swift, BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingProfile.swift, BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CDNProviderAdapter.swift, BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CloudflareAdapter.swift, BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FastlyAdapter.swift, BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CustomCDNAdapter.swift, BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingError.swift, BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/AppGroupContainer.swift, BBTB/Packages/FrontingEngine/Tests/FrontingEngineTests/FrontingProfileTests.swift</files>
  <read_first>
    - BBTB/Packages/RulesEngine/Package.swift (целиком — точный template для FrontingEngine)
    - BBTB/Packages/TransportRegistry/Sources/TransportRegistry/TransportHandler.swift (целиком — protocol pattern)
    - BBTB/Packages/VPNCore/Sources/VPNCore/TransportConfig.swift (целиком — Codable Sendable enum pattern)
    - BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/AppGroupContainer.swift (lines 55-58 — rulesCacheDirectory pattern; точно где добавить cdnFailureCacheURL)
    - .planning/phases/10-advanced-settings-security-polish/10-RESEARCH.md §Standard Stack (Supporting) + §Architecture (system diagram CDN flow) + §Pitfall 3 (Cloudflare classic fronting blocked 2015)
    - .planning/phases/10-advanced-settings-security-polish/10-CONTEXT.md D-03..D-07
    - .planning/phases/10-advanced-settings-security-polish/10-PATTERNS.md lines 311-371 (FrontingProfile + CDNProviderAdapter analog mapping)
  </read_first>
  <action>
**File 1**: `BBTB/Packages/FrontingEngine/Package.swift` (NEW) — exact mirror of RulesEngine/Package.swift adapted для FrontingEngine:
- `swift-tools-version: 6.0`.
- Package name `"FrontingEngine"`, platforms `[.iOS(.v18), .macOS(.v15)]`.
- products: `[.library(name: "FrontingEngine", targets: ["FrontingEngine"])]`.
- dependencies: `[.package(path: "../VPNCore"), .package(path: "../PacketTunnelKit")]`. **NB:** PacketTunnelKit dependency для AppGroupContainer — minimal coupling, не для ConfigParser/sing-box.
- target `FrontingEngine` dependencies: `["VPNCore", "PacketTunnelKit"]`; resources: `[.process("Resources")]`.
- testTarget `FrontingEngineTests` dependencies: `["FrontingEngine"]`; linkerSettings: mirror RulesEngine lines 64-75 (libbox cascade через PacketTunnelKit transitive).

**File 2**: `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingProfile.swift`:
- Doc-comment: «DPI-06 / D-03 — CDN dial target overlay. Не часть TransportConfig (D-03 critical: иначе 50+ транспортов дублируют CDN логику).»
- `public enum CDNProvider: String, Codable, Sendable, CaseIterable { case cloudflare, fastly, custom }`.
- `public enum FrontingMode: String, Codable, Sendable, CaseIterable { case domain, ipPool, remoteSigned }`.
- `public struct FrontingProfile: Codable, Sendable, Equatable, Hashable`:
  - `public let provider: CDNProvider`.
  - `public let connectHost: String` — IP CDN либо domain pointing to CDN.
  - `public let connectPort: Int` — обычно 443.
  - `public let sniHost: String` — fronted hostname (TLS server_name).
  - `public let httpHost: String` — Host / :authority header value.
  - `public let mode: FrontingMode`.
  - `public init(...)` — все 6 fields параметрами.

**File 3**: `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CDNProviderAdapter.swift`:
- Doc-comment: «DPI-06 / D-04 — protocol для добавления Cloudflare/Fastly/Bunny/Custom. Mirror TransportHandler structure.»
- `public protocol CDNProviderAdapter: Sendable`:
  - `static var provider: CDNProvider { get }`.
  - `static var displayName: String { get }`.
  - `static func applyFronting(to outbound: inout [String: Any], profile: FrontingProfile) -> Bool`. Returns true если applied, false если outbound incompatible (Reality/TUIC/Hy2) или transport type unsupported.

**File 4**: `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CloudflareAdapter.swift`:
- `public enum CloudflareAdapter: CDNProviderAdapter`:
  - `public static let provider: CDNProvider = .cloudflare`.
  - `public static let displayName: String = "Cloudflare"`.
  - `public static func applyFronting(to outbound: inout [String: Any], profile: FrontingProfile) -> Bool`:
    1. **D-05 blacklist**:
       - Если `outbound["type"] as? String` ∈ {"tuic", "hysteria2"} → return false.
       - Если `outbound["tls"] as? [String: Any]?["reality"] as? [String: Any]?["enabled"] as? Bool == true` → return false.
       - Если `outbound["flow"] as? String == "xtls-rprx-vision"` → return false.
    2. Override dial target: `outbound["server"] = profile.connectHost`; `outbound["server_port"] = profile.connectPort`.
    3. Override SNI: если `var tls = outbound["tls"] as? [String: Any]` → `tls["server_name"] = profile.sniHost`; `outbound["tls"] = tls`. (Если tls отсутствует — создать новый dict.)
    4. Override transport-specific header:
       - Достать `outbound["transport"] as? [String: Any]`. Если nil → skip step 4 (vanilla TCP transport).
       - `let transportType = transport["type"] as? String ?? ""`.
       - Switch transportType:
         - `"ws"`: `var headers = (transport["headers"] as? [String: Any]) ?? [:]; headers["Host"] = profile.httpHost; transport["headers"] = headers`.
         - `"httpupgrade"`: `transport["host"] = profile.httpHost`.
         - `"grpc"`: nothing — server_name (set above) уже covers gRPC :authority.
         - default: skip (TCP / unknown).
       - `outbound["transport"] = transport`.
    5. Return true.

**File 5**: `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FastlyAdapter.swift`:
- `public enum FastlyAdapter: CDNProviderAdapter`:
  - `public static let provider: CDNProvider = .fastly`.
  - `public static let displayName: String = "Fastly"`.
  - `public static func applyFronting(to outbound: inout [String: Any], profile: FrontingProfile) -> Bool`:
    - Идентичная логика CloudflareAdapter (одинаковые sing-box transport mappings). Если Fastly требует distinct header — extend позже. В v0.10 — equivalent поведение.

**File 6**: `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CustomCDNAdapter.swift`:
- `public enum CustomCDNAdapter: CDNProviderAdapter`:
  - `public static let provider: CDNProvider = .custom`.
  - `public static let displayName: String = "Custom CDN"`.
  - Identical applyFronting body. Generic semantics для admin's own CDN deployments.

**File 7**: `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingError.swift`:
- `public enum FrontingError: Error, Sendable, Equatable`:
  - `case malformedJSON`
  - `case unsupportedTransport(String)`
  - `case providerBlacklisted(String)`
  - `case fallbackExhausted`
  - `case ioError(String)`

**File 8**: `BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/AppGroupContainer.swift` — добавить новое property:
- `public static var cdnFailureCacheURL: URL`:
  - Doc-comment: «Phase 10 / D-06 — CDN failure score cache. Stores failure scores и cooldown deadlines по (provider, ip, networkType). Main app writer (FrontingEngine FrontingFailureCache actor). Extension не читает (CDN решение принимается main app в ConfigImporter).»
  - Logic: `let dir = url.appendingPathComponent("Library/Caches/cdn", isDirectory: true); try? FileManager.default.createDirectory(at: dir, withIntermediateDirectories: true); return dir.appendingPathComponent("cdn-failure-cache.json")`.
- **Координация с Plan 04** (если выполняется параллельно): Plan 04 добавляет `certPinManifestDirectory` в тот же файл. Оба addition — независимые `public static var` properties. Расположить cdnFailureCacheURL рядом с certPinManifestDirectory (либо в одной merge — executor разруливает linear).

**File 9**: `BBTB/Packages/FrontingEngine/Tests/FrontingEngineTests/FrontingProfileTests.swift`:
- Базовые tests:
  - `test_FrontingProfile_codable_roundtrip` — encode + decode сохраняет все 6 fields.
  - `test_CDNProvider_caseIterable` — `.allCases.count == 3` (cloudflare/fastly/custom).
  - `test_FrontingMode_caseIterable` — `.allCases.count == 3` (domain/ipPool/remoteSigned).
  - `test_CloudflareAdapter_provider_identifier` — `CloudflareAdapter.provider == .cloudflare`.

**Tuist note**: Plan 06 (Wave 3) обновит `BBTB/Project.swift` чтобы добавить `.package(path: "Packages/FrontingEngine")` к manifest. В Plan 05 — только SwiftPM-уровень build, без Xcode wiring.
  </action>
  <verify>
    <automated>cd BBTB && swift build --package-path Packages/FrontingEngine 2>&1 | tail -20 && swift test --package-path Packages/FrontingEngine --filter FrontingProfileTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `BBTB/Packages/FrontingEngine/Package.swift` exists; `grep -c 'swift-tools-version: 6.0' $file` ≥ 1; `grep -c '.iOS(.v18)' $file` ≥ 1; `grep -c 'PacketTunnelKit' $file` ≥ 1.
    - File `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingProfile.swift` exists; `grep -c 'public struct FrontingProfile: Codable' $file` ≥ 1; `grep -c 'connectHost\|sniHost\|httpHost' $file` ≥ 1.
    - File `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CDNProviderAdapter.swift` exists; `grep -c 'protocol CDNProviderAdapter' $file` ≥ 1; `grep -c 'applyFronting' $file` ≥ 1.
    - File `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CloudflareAdapter.swift` exists; `grep -c 'CDNProviderAdapter' $file` ≥ 1; `grep -c 'xtls-rprx-vision\|reality' $file` ≥ 1 (blacklist check).
    - File `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FastlyAdapter.swift` exists.
    - File `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CustomCDNAdapter.swift` exists.
    - File `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingError.swift` exists; `grep -c 'malformedJSON\|fallbackExhausted' $file` ≥ 1.
    - `grep -c 'cdnFailureCacheURL' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/AppGroupContainer.swift` ≥ 1.
    - `grep -c 'Library/Caches/cdn' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/AppGroupContainer.swift` ≥ 1.
    - `cd BBTB && swift build --package-path Packages/FrontingEngine` exits 0.
    - `cd BBTB && swift test --package-path Packages/FrontingEngine --filter FrontingProfileTests` exits 0; ≥ 4 tests PASS.
  </acceptance_criteria>
  <done>Новый SwiftPM пакет FrontingEngine компилируется; 7 source files + 1 modified AppGroupContainer.swift; ≥ 4 unit-теста на Codable / adapter identification зелёные.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FrontingConfigApplier + FrontingFailureCache + FrontingFallbackChain + ≥ 8 tests across 2 test files</name>
  <files>BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingConfigApplier.swift, BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingFailureCache.swift, BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingFallbackChain.swift, BBTB/Packages/FrontingEngine/Tests/FrontingEngineTests/FrontingConfigApplierTests.swift, BBTB/Packages/FrontingEngine/Tests/FrontingEngineTests/FrontingFallbackChainTests.swift</files>
  <read_first>
    - BBTB/Packages/FrontingEngine/Sources/FrontingEngine/CloudflareAdapter.swift (Task 1 output — для applyFronting reference)
    - BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingProfile.swift (Task 1 output)
    - BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift (Phase 8 rule_set injection lines 270-323 — JSON dict mutation pattern reference; same paradigm для FrontingConfigApplier)
    - BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesEngineCoordinator.swift (actor pattern, re-entry guard)
    - BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/AppGroupContainer.swift (Task 1 updated — cdnFailureCacheURL)
    - .planning/phases/10-advanced-settings-security-polish/10-RESEARCH.md §Code Examples → CDN-фронтинг overlay (lines 716-774 — complete FrontingConfigApplier snippet) + §Pitfall 3 (Cloudflare classic fronting blocked) + §Pitfall 6 (NEVPNStatusDidChange XPC — НЕ subscribe из fallback chain)
    - .planning/phases/10-advanced-settings-security-polish/10-CONTEXT.md D-05..D-07
  </read_first>
  <behavior>
    - Test 1 (FrontingConfigApplier): `test_apply_ws_overrides_host_header` — input JSON с outbound type=vless + tls + transport.type=ws + transport.headers.Host="original.com". Profile: sniHost="cdn.example.com", httpHost="cdn.example.com". After apply: outbound.server == "cdn.example.com" (либо connectHost); outbound.tls.server_name == "cdn.example.com"; outbound.transport.headers.Host == "cdn.example.com".
    - Test 2: `test_apply_httpupgrade_overrides_host` — transport.type=httpupgrade + transport.host="orig". After apply: transport.host == profile.httpHost.
    - Test 3: `test_apply_grpc_overrides_sni_only` — transport.type=grpc + transport.service_name="svc". After apply: tls.server_name изменён; transport.service_name UNchanged.
    - Test 4: `test_apply_noop_for_reality` — outbound type=vless + tls.reality.enabled=true. After apply: outbound unchanged; applyFronting returns false.
    - Test 5: `test_apply_noop_for_tuic` — outbound type=tuic. After apply: outbound unchanged; returns false.
    - Test 6: `test_apply_noop_for_hysteria2` — outbound type=hysteria2. After apply: unchanged.
    - Test 7: `test_apply_noop_for_vision_flow` — outbound type=vless + flow="xtls-rprx-vision". After apply: unchanged.
    - Test 8 (FrontingFailureCache): `test_failure_cache_records_score_and_cooldown` — actor.recordFailure(provider: .cloudflare, ip: "1.2.3.4", networkType: "wifi"). После: actor.shouldSkip(...) returns true; persistsтся в AppGroupContainer.cdnFailureCacheURL.
    - Test 9 (FrontingFailureCache): `test_failure_cache_cooldown_respected` — recordFailure с custom клоком. clock advances < cooldown → shouldSkip == true. clock advances > cooldown → shouldSkip == false (score reset либо moved).
    - Test 10 (FrontingFallbackChain): `test_fallback_chain_advances_on_failure` — chain с 3 profiles [profile1, profile2, profile3]. First nextEndpoint returns profile1. После reportFailure → next returns profile2. После всех failed → next returns nil + exhausted=true.
    - Test 11 (FrontingFallbackChain): `test_fallback_chain_sequential_concurrency_1` — два concurrent task'а вызывают nextEndpoint одновременно (TaskGroup). Verify через actor isolation: возвращаемые profiles разные (cursor advance) либо thread-safe access. DEC-06d-04 paradigm.
  </behavior>
  <action>
**File 1**: `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingConfigApplier.swift`:
- Doc-comment: «DPI-06 / D-05 — JSON overlay над expandConfigForTunnel output. Меняет outbound.server / tls.server_name / transport.headers.Host (WS) / transport.host (HTTPUpgrade). Skip Reality/TUIC/Hysteria2/Vision (D-05 blacklist enforced через CloudflareAdapter.applyFronting).»
- `public enum FrontingConfigApplier`:
  - `public static func apply(json: String, profile: FrontingProfile, adapter: any CDNProviderAdapter.Type) throws -> String`:
    1. `guard let data = json.data(using: .utf8), var root = try JSONSerialization.jsonObject(with: data) as? [String: Any] else { throw FrontingError.malformedJSON }`.
    2. `var outbounds = (root["outbounds"] as? [[String: Any]]) ?? []`.
    3. For each i in outbounds.indices:
       - `var ob = outbounds[i]`.
       - `_ = adapter.applyFronting(to: &ob, profile: profile)`. (Return value игнорируется — каждый outbound решает self; logging вне scope этого helper'а.)
       - `outbounds[i] = ob`.
    4. `root["outbounds"] = outbounds`.
    5. `let modified = try JSONSerialization.data(withJSONObject: root, options: [])`.
    6. `guard let result = String(data: modified, encoding: .utf8) else { throw FrontingError.malformedJSON }`.
    7. `return result`.
  - `public static func apply(outbound: inout [String: Any], profile: FrontingProfile, adapter: any CDNProviderAdapter.Type) -> Bool`:
    - Single-outbound вариант (для использования инлайн без JSON roundtrip).
    - `return adapter.applyFronting(to: &outbound, profile: profile)`.

**File 2**: `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingFailureCache.swift`:
- Doc-comment: «DPI-06 / D-06 — failure score + cooldown cache. Persistence: App Group cdnFailureCacheURL. Cooldown 6-24ч в зависимости от score level.»
- `public actor FrontingFailureCache`:
  - Codable internal struct:
    - `private struct FailureRecord: Codable { let score: Int; let cooldownUntil: Date }`.
    - `private struct CacheKey: Hashable, Codable { let provider: String; let ip: String; let networkType: String }`.
  - State:
    - `private var records: [String: FailureRecord]` (key = `"<provider>|<ip>|<networkType>"`).
    - `private let cacheURL: URL` (default `AppGroupContainer.cdnFailureCacheURL`).
    - `private let clock: () -> Date` (default Date.init; injectable для tests).
  - `public init(cacheURL: URL = AppGroupContainer.cdnFailureCacheURL, clock: @escaping () -> Date = Date.init)`:
    - Load existing records from disk (try? JSONDecoder().decode([String: FailureRecord].self, from: Data(contentsOf:))).
  - `public func recordFailure(provider: CDNProvider, ip: String, networkType: String)`:
    - Compose key string.
    - `let oldScore = records[key]?.score ?? 0`.
    - `let newScore = min(oldScore + 1, 10)` (cap at 10).
    - Cooldown ladder: 1 → 6h, 2-3 → 12h, ≥4 → 24h.
    - `let cooldownUntil = clock().addingTimeInterval(cooldownSeconds(newScore))`.
    - `records[key] = FailureRecord(score: newScore, cooldownUntil: cooldownUntil)`.
    - Persist через background JSON write.
  - `public func shouldSkip(provider: CDNProvider, ip: String, networkType: String) -> Bool`:
    - Compose key. `guard let r = records[key] else { return false }`.
    - `return clock() < r.cooldownUntil`.
  - `public func recordSuccess(provider: CDNProvider, ip: String, networkType: String)`:
    - Reset record: `records[key] = nil` (либо decrement score).
    - Persist.
  - `private func cooldownSeconds(_ score: Int) -> TimeInterval`: 1 → 6*3600, 2..3 → 12*3600, else 24*3600.
  - `private func persist()`: encode records → atomically write to cacheURL. Best-effort (try?).

**File 3**: `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingFallbackChain.swift`:
- Doc-comment: «DPI-06 / D-06 — sequential CDN fallback chain. DEC-06d-04 concurrency=1.»
- `public actor FrontingFallbackChain`:
  - State:
    - `private let profiles: [FrontingProfile]` — статический список (admin populates через subscription либо bundle config).
    - `private var cursor: Int = 0` — текущий index.
    - `private let cache: FrontingFailureCache`.
  - `public init(profiles: [FrontingProfile], cache: FrontingFailureCache)`.
  - `public func nextEndpoint(networkType: String) async -> (FrontingProfile?, exhausted: Bool)`:
    - Перебор от cursor до конца (single pass):
      - `let p = profiles[i]`.
      - Если `await cache.shouldSkip(provider: p.provider, ip: p.connectHost, networkType: networkType)` → continue (advance cursor).
      - Иначе → `cursor = i + 1` (advance для next call); return (p, exhausted: false).
    - Если все profiles iterated → return (nil, exhausted: true).
  - `public func reportFailure(profile: FrontingProfile, networkType: String) async`:
    - `await cache.recordFailure(provider: profile.provider, ip: profile.connectHost, networkType: networkType)`.
  - `public func reportSuccess(profile: FrontingProfile, networkType: String) async`:
    - `await cache.recordSuccess(provider: profile.provider, ip: profile.connectHost, networkType: networkType)`.
  - `public func reset()` — `cursor = 0`.

**File 4**: `BBTB/Packages/FrontingEngine/Tests/FrontingEngineTests/FrontingConfigApplierTests.swift`:
- ≥ 7 tests из behavior list (Tests 1-7).
- Helpers:
  - `func makeOutboundJSON(type: String, extras: [String: Any] = [:]) -> String` — wraps в minimal root structure.
  - `func parseOutbound(from json: String) -> [String: Any]` — decode + extract outbounds[0].
- Each test: build fixture → apply → parse → assert на конкретных JSON keys.

**File 5**: `BBTB/Packages/FrontingEngine/Tests/FrontingEngineTests/FrontingFallbackChainTests.swift`:
- ≥ 4 tests из behavior list (Tests 8-11).
- Helpers: temp directory для cache (init FrontingFailureCache(cacheURL: tempPath, clock: testClock)).
- Mocked clock: `class MutableClock { var now: Date; func tick(_ s: TimeInterval) { now.addTimeInterval(s) } }` либо closure capture pattern.
  </action>
  <verify>
    <automated>cd BBTB && swift test --package-path Packages/FrontingEngine 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingConfigApplier.swift` exists; `grep -c 'public enum FrontingConfigApplier' $file` ≥ 1; `grep -c 'JSONSerialization' $file` ≥ 1.
    - File `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingFailureCache.swift` exists; `grep -c 'public actor FrontingFailureCache' $file` ≥ 1; `grep -c 'cooldownUntil' $file` ≥ 1.
    - File `BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingFallbackChain.swift` exists; `grep -c 'public actor FrontingFallbackChain' $file` ≥ 1; `grep -c 'nextEndpoint' $file` ≥ 1.
    - `grep -c 'AppGroupContainer.cdnFailureCacheURL' BBTB/Packages/FrontingEngine/Sources/FrontingEngine/FrontingFailureCache.swift` ≥ 1.
    - `grep -c 'test_apply_' BBTB/Packages/FrontingEngine/Tests/FrontingEngineTests/FrontingConfigApplierTests.swift` ≥ 7.
    - `grep -c 'test_failure_cache\|test_fallback_chain' BBTB/Packages/FrontingEngine/Tests/FrontingEngineTests/FrontingFallbackChainTests.swift` ≥ 4.
    - `cd BBTB && swift test --package-path Packages/FrontingEngine` exits 0; ≥ 11 tests PASS total (4 от Task 1 + 7+ от Task 2).
  </acceptance_criteria>
  <done>FrontingConfigApplier + FrontingFailureCache + FrontingFallbackChain реализованы; ≥ 11 unit-тестов across 3 test файлов зелёные; пакет полностью self-contained — wiring через ConfigImporter в Plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Subscription server → app (frontingProfile blob) | Admin доставляет CDN config — должен быть trusted (Marzban subscription авторизован) |
| App → CDN target (TCP/TLS) | dial target меняется на CDN IP/domain — TLS handshake идёт к CDN, не к origin |
| FrontingFailureCache ↔ App Group disk | JSON cache файл в App Group |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-W5-01 | Tampering | Subscription server подменяет frontingProfile на враждебный CDN endpoint | accept | Admin trust model — same Marzban auth + Phase 8 Ed25519 rules.json не covers subscription JSON. Risk = scope subscription endpoint trust (DPI-08 cert pinning Plan 04 покрывает MITM). |
| T-10-W5-02 | Tampering | CDN provider (Cloudflare/Fastly) логирует traffic либо подменяет content | accept | Public CDN — out-of-scope для client. Admin choice of CDN. |
| T-10-W5-03 | DoS | CDN blackholes traffic → user отрезан от VPN | mitigate | FrontingFallbackChain (D-06) — sequential provider advance + cooldown 6-24ч. После exhaustion → fallback на direct Reality/Vision profile (Plan 06 wiring). |
| T-10-W5-04 | Information Disclosure | FrontingProfile содержит admin-side IP pool → доступен в App Group | accept | IP pool — public CDN edges (e.g. Cloudflare anycast). Не secret. |
| T-10-W5-05 | DoS (self) | FrontingConfigApplier применён к Reality outbound → handshake fails | mitigate | D-05 blacklist в CloudflareAdapter/FastlyAdapter/CustomCDNAdapter.applyFronting. Tests 4-7 покрывают. |
| T-10-W5-06 | Misuse | FrontingFailureCache infinite cooldown держит CDN заблокированным forever | mitigate | Cooldown ladder caps at 24ч. После cooldown — score не растёт автоматически, требует новый failure. |
| T-10-W5-07 | Tampering | App Group cache JSON tampered → spurious cooldown | accept | App Group sandbox (Phase 1 R6). Worst case — пользователь видит CDN-фронтинг inactive когда должен работать; manual recovery через app restart либо clear-cache action (Phase 11 backlog). |
| T-10-W5-08 | Race condition | Two ConfigImporter паралельно вызывают FrontingFallbackChain.nextEndpoint | mitigate | actor isolation Swift. Test 11 покрывает concurrent access. |
| T-10-W5-09 | Configuration drift | FrontingProfile fields расходятся с admin subscription schema | accept | Codable JSON tolerance — additional fields игнорируются; missing optional fields → use defaults. Admin документация (wiki/cdn-fronting-server-handoff.md создаётся в Plan 06). |
| T-10-W5-10 | NEVPNStatusDidChange XPC storm trigger | accept | RESEARCH.md §Pitfall 6: FrontingFallbackChain — actor, observes ТОЛЬКО connect() result (не subscribe to NEVPNStatusDidChange). Plan 06 wiring уважает этот invariant. |
</threat_model>

<verification>
Wave 2 Plan 05 verification:
1. `swift build --package-path BBTB/Packages/FrontingEngine` clean.
2. `swift test --package-path BBTB/Packages/FrontingEngine` ≥ 11 tests PASS.
3. `swift build --package-path BBTB/Packages/PacketTunnelKit` clean (AppGroupContainer.swift extension).
4. Manual sanity: создать `FrontingProfile(provider: .cloudflare, connectHost: "1.1.1.1", connectPort: 443, sniHost: "example.com", httpHost: "example.com", mode: .domain)` и вызвать `FrontingConfigApplier.apply(json: fixture_ws, profile:, adapter: CloudflareAdapter.self)` → verify transport.headers.Host подменён.
</verification>

<success_criteria>
- DPI-06 CDN-фронтинг architecture реализована как отдельный пакет FrontingEngine.
- 3 CDN provider adapters (Cloudflare/Fastly/Custom) делятся одной D-05 mapping логикой.
- FrontingFailureCache + FrontingFallbackChain — actors с App Group persistence + DEC-06d-04 sequential concurrency.
- ≥ 11 unit-тестов across 3 test файлов зелёные.
- Пакет НЕ wired в production yet — wiring через ConfigImporter в Plan 06 (Wave 3).
</success_criteria>

<output>
After completion, create `.planning/phases/10-advanced-settings-security-polish/10-05-SUMMARY.md` со следующими полями:
- DPI-06 FrontingEngine пакет создан.
- New SwiftPM package: 10 source files + 3 test files.
- AppGroupContainer.cdnFailureCacheURL added.
- Tests added count.
- Open follow-up (Plan 06): Tuist Project.swift wire + ConfigImporter integration + admin handoff doc wiki/cdn-fronting-server-handoff.md.
- Commit SHA(s).
</output>
