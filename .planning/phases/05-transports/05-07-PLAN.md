---
phase: 05-transports
plan: 07
type: execute
wave: 7
depends_on: [05-06]
files_modified:
  - BBTB/Packages/ConfigParser/Package.swift
  - BBTB/Packages/Protocols/VLESSReality/Package.swift
  - BBTB/Packages/Protocols/VLESSTLS/Package.swift
  - BBTB/Packages/Protocols/Trojan/Package.swift
  - BBTB/Packages/Protocols/Shadowsocks/Package.swift
  - BBTB/Packages/Protocols/Hysteria2/Package.swift
  - BBTB/Packages/Protocols/VLESSReality/Sources/VLESSReality/ConfigBuilder.swift
  - BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift
  - BBTB/Packages/Protocols/Trojan/Sources/Trojan/ConfigBuilder.swift
  - BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/ConfigBuilder.swift
  - BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/ConfigBuilder.swift
  - BBTB/Packages/Protocols/VLESSTLS/Tests/VLESSTLSTests/BuildOutboundTests.swift
  - BBTB/Packages/Protocols/Trojan/Tests/TrojanTests/BuildOutboundTests.swift
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/TransportOverride.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/TransportOverrideTests.swift
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift
  - BBTB/App/iOSApp/BBTB_iOSApp.swift
  - BBTB/App/macOSApp/BBTB_macOSApp.swift
autonomous: true
requirements: [CORE-03, TRANSP-02, TRANSP-03, TRANSP-04]

must_haves:
  truths:
    - "Каждый из 5 protocol packages имеет public static buildOutbound(from:transport:tag:) метод"
    - "VLESSTLS.ConfigBuilder.buildOutbound для transport=.ws(/buy, h) возвращает outbound dict с transport block {type:ws, path:/buy, headers:{Host:h}}"
    - "VLESSTLS.ConfigBuilder.buildOutbound для transport=.tcp возвращает outbound dict БЕЗ ключа transport"
    - "Trojan.ConfigBuilder.buildOutbound для transport=.ws сохраняет ALPN h2-strip invariant (Phase 2 W4)"
    - "PoolBuilder.buildSingBoxJSON содержит switch с 5 case'ами, каждый — однострочный вызов ProtocolPackage.buildOutbound"
    - "Private buildVLESSOutbound/buildVLESSTLSOutbound/buildTrojanOutbound/buildShadowsocksOutbound/buildHysteria2Outbound удалены из PoolBuilder.swift"
    - "TransportRegistry.shared.register вызывается для всех 5 handler-ов (TCP/WS/HTTP/HTTPUpgrade/gRPC) в BBTB_iOSApp.swift и BBTB_macOSApp.swift"
    - "R1 invariant test test_nonHy2_outbounds_neverHaveInsecureTrue по-прежнему PASSes после refactor"
    - "ConfigImporter применяет ServerConfig.transportOverride к ParsedVLESSTLS/ParsedTrojan перед PoolBuilder (mutation в reparseFromKeychain или после)"
  artifacts:
    - path: BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift
      provides: "public static func buildOutbound(from:transport:tag:) -> [String: Any] producing VLESS+TLS outbound with optional transport block from TransportRegistry"
      contains: "public static func buildOutbound"
    - path: BBTB/Packages/Protocols/Trojan/Sources/Trojan/ConfigBuilder.swift
      provides: "public static func buildOutbound(from:transport:tag:) preserving ALPN h2-strip for WS"
      contains: "public static func buildOutbound"
    - path: BBTB/Packages/ConfigParser/Sources/ConfigParser/TransportOverride.swift
      provides: "applyTransportOverride(_:_:) helper for mutating AnyParsedConfig"
      contains: "func applyTransportOverride"
    - path: BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift
      provides: "Coordinator: 5-case switch → 5 one-liner calls to ProtocolPackage.buildOutbound"
      contains: "VLESSTLS.ConfigBuilder.buildOutbound"
  key_links:
    - from: BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift
      to: BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift
      via: "VLESSTLS.ConfigBuilder.buildOutbound call inside switch"
      pattern: "VLESSTLS\\.ConfigBuilder\\.buildOutbound"
    - from: BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift
      to: BBTB/Packages/TransportRegistry/Sources/TransportRegistry/TransportRegistry.swift
      via: "TransportRegistry.shared.handler(for: transport.identifier)?.buildTransportBlock(for: transport)"
      pattern: "TransportRegistry\\.shared\\.handler"
    - from: BBTB/App/iOSApp/BBTB_iOSApp.swift
      to: BBTB/Packages/TransportRegistry/Sources/TransportRegistry/TransportRegistry.swift
      via: "TransportRegistry.shared.register(WSTransportHandler.self)..."
      pattern: "TransportRegistry\\.shared\\.register"
---

<objective>
Wave 7 — integration вершина Phase 5. После Wave 6 типы готовы; теперь каждый protocol package
получает свой `buildOutbound`, PoolBuilder становится thin coordinator, App startup регистрирует
все transport handlers, и ConfigImporter применяет per-server `transportOverride`.

**Эта плана — самая тяжёлая в Phase 5** (~17 файлов модифицируется). Минимизируем риск:
1. Sequential Task 1-3 — каждая может independently verified `swift test` per package.
2. Task 4 (PoolBuilder coordinator) — последний, после того как все protocol packages готовы;
   compile gate проверяет всю pipeline.
3. Task 5 (App registration + ConfigImporter override) — financial integration, requires Task 4.

R1 invariant (`insecure: false` для всех TLS блоков кроме Hy2 D-08) сохраняется через:
- Hardcoded `insecure: false` в каждом protocol package buildOutbound (НЕ читаем из parsed).
- `test_nonHy2_outbounds_neverHaveInsecureTrue` invariant test в PoolBuilderTests preserved.

Output: 17 файлов (5 Package.swift modified, 5 protocol ConfigBuilder.swift modified, 2 new
BuildOutboundTests, PoolBuilder + new TransportOverride.swift + tests, ConfigImporter,
iOS/macOS App startup files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-transports/05-CONTEXT.md
@.planning/phases/05-transports/05-RESEARCH.md
@.planning/phases/05-transports/05-PATTERNS.md
@.planning/phases/05-transports/05-06-PLAN.md

# Source-of-truth references — must read for context
@BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift
@BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift
@BBTB/Packages/VPNCore/Sources/VPNCore/TransportConfig.swift
@BBTB/Packages/TransportRegistry/Sources/TransportRegistry/TransportRegistry.swift
@BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift
@BBTB/Packages/Protocols/Trojan/Sources/Trojan/ConfigBuilder.swift
@BBTB/Packages/Protocols/VLESSReality/Sources/VLESSReality/ConfigBuilder.swift
@BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/ConfigBuilder.swift
@BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/ConfigBuilder.swift
@BBTB/App/iOSApp/BBTB_iOSApp.swift
@BBTB/App/macOSApp/BBTB_macOSApp.swift
@BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift

<interfaces>
**Per-protocol buildOutbound signature (D-14):**
```swift
// Inside each Protocols/X/Sources/X/ConfigBuilder.swift, as extension or static method:
extension ConfigBuilder {
    public static func buildOutbound(
        from parsed: ParsedX,           // ParsedVLESSTLS / ParsedTrojan / ParsedVLESS / ParsedShadowsocks / ParsedHysteria2
        transport: TransportConfig,
        tag: String
    ) -> [String: Any]
}
```

**PoolBuilder coordinator pattern (D-15) — exact form after refactor:**
```swift
for (index, parsed) in truncated.enumerated() {
    let tag: String
    let outbound: [String: Any]
    switch parsed {
    case .vlessReality(let v):
        tag = "vless-\(index)"
        outbound = VLESSReality.ConfigBuilder.buildOutbound(from: v, transport: .tcp, tag: tag)
    case .vlessTLS(let v):
        tag = "vless-tls-\(index)"
        outbound = VLESSTLS.ConfigBuilder.buildOutbound(from: v, transport: v.transport, tag: tag)
    case .trojan(let t):
        tag = "trojan-\(index)"
        outbound = Trojan.ConfigBuilder.buildOutbound(from: t, transport: t.transport, tag: tag)
    case .shadowsocks(let s):
        tag = "ss-\(index)"
        outbound = Shadowsocks.ConfigBuilder.buildOutbound(from: s, transport: .tcp, tag: tag)
    case .hysteria2(let h):
        tag = "hy2-\(index)"
        outbound = Hysteria2.ConfigBuilder.buildOutbound(from: h, transport: .tcp, tag: tag)
    }
    outbounds.append(outbound)
    tags.append(tag)
}
```

**applyTransportOverride helper (new in ConfigParser):**
```swift
public func applyTransportOverride(_ parsed: AnyParsedConfig, _ override: TransportConfig?) -> AnyParsedConfig {
    guard let override = override else { return parsed }
    switch parsed {
    case .vlessTLS(var v): v.transport = override; return .vlessTLS(v)
    case .trojan(var t):   t.transport = override; return .trojan(t)
    case .vlessReality, .shadowsocks, .hysteria2:
        return parsed   // D-03/D-16 — override игнорируется для этих протоколов
    }
}
```
Note: ParsedVLESSTLS and ParsedTrojan must be marked `mutating` capable for `.transport` reassignment — `var v` in pattern binding works only if struct fields are `var`, not `let`. **Phase 5 may require relaxing fields from `let` to `var`** (in ParsedConfigs.swift, after Wave 6 migration). Alternative: build a fresh struct via init. See Task 4 action notes.

**App startup registration (Pitfall 8 — order matters but only relative to "before first connect"):**
```swift
// Inside BBTB_iOSApp.init() and BBTB_macOSApp.init(), AFTER ProtocolRegistry registration:
TransportRegistry.shared.register(TCPTransportHandler.self)
TransportRegistry.shared.register(WSTransportHandler.self)
TransportRegistry.shared.register(HTTPTransportHandler.self)
TransportRegistry.shared.register(HTTPUpgradeTransportHandler.self)
TransportRegistry.shared.register(GRPCTransportHandler.self)
```

**ConfigImporter mutation hook (Pitfall 5 — apply override by ServerConfig.id, NOT by identity key):**
In `provisionTunnelProfile(for: selectedID)` method, after reparseFromKeychain returns
ParsedXxx, immediately wrap:
```swift
let withOverride = applyTransportOverride(parsed, cfg.transportOverride)
parsedList = [withOverride]   // or .append for auto-mode
```
ServerConfig.transportOverride field will be added in Wave 8 (UI plan). For Wave 7 — ConfigImporter
just reads the field which may not exist yet, so we add a placeholder: in this plan we assume
the field exists OR is `nil`. **Order constraint:** This plan implements the IMPORT side
(applyTransportOverride helper + ConfigImporter call), but the SwiftData field itself comes in
Wave 8 (UI plan). Use feature-flag approach: ConfigImporter passes `cfg.transportOverride` which
Wave 8 adds as optional Codable enum.

**Important:** Wave 7 must NOT modify ServerConfig.swift in VPNCore. That belongs to Wave 8.
For Wave 7 ConfigImporter, use a typed accessor:
```swift
// Helper inside ConfigImporter, until Wave 8 adds the field:
private func transportOverride(for cfg: ServerConfig) -> TransportConfig? {
    return nil  // Wave 8 will replace with cfg.transportOverride
}
```
This stub allows Wave 7 to integrate end-to-end without depending on Wave 8.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adjust Package.swift dependencies (Protocols/* depend on TransportRegistry; ConfigParser depends on Protocols/*)</name>
  <files>BBTB/Packages/Protocols/VLESSReality/Package.swift, BBTB/Packages/Protocols/VLESSTLS/Package.swift, BBTB/Packages/Protocols/Trojan/Package.swift, BBTB/Packages/Protocols/Shadowsocks/Package.swift, BBTB/Packages/Protocols/Hysteria2/Package.swift, BBTB/Packages/ConfigParser/Package.swift</files>
  <read_first>
    BBTB/Packages/Protocols/VLESSReality/Package.swift,
    BBTB/Packages/Protocols/VLESSTLS/Package.swift,
    BBTB/Packages/Protocols/Trojan/Package.swift,
    BBTB/Packages/Protocols/Shadowsocks/Package.swift,
    BBTB/Packages/Protocols/Hysteria2/Package.swift,
    BBTB/Packages/ConfigParser/Package.swift,
    BBTB/Packages/TransportRegistry/Package.swift
  </read_first>
  <action>
    1. **Каждый `Protocols/X/Package.swift`** — добавить `.package(path: "../../TransportRegistry")` в `dependencies` (если ещё нет) и `"TransportRegistry"` в `dependencies` массив `.target(...)`. Каждый protocol package будет вызывать `TransportRegistry.shared` для построения transport block. (Reality, Shadowsocks, Hysteria2 — формально не используют transport overlay, но Package.swift unification стоит дёшево и упрощает PoolBuilder pattern — РЕКОМЕНДУЕТСЯ. Альтернатива: добавить TransportRegistry только в VLESSTLS + Trojan; Reality/Shadowsocks/Hysteria2 buildOutbound игнорирует transport param. Выбор для конкретного executor-а в Task 2 — main constraint: VLESSTLS + Trojan ДОЛЖНЫ зависеть.)
    2. **`ConfigParser/Package.swift`** — добавить 5 protocol packages в `dependencies`:
       ```
       .package(path: "../Protocols/VLESSReality"),
       .package(path: "../Protocols/VLESSTLS"),
       .package(path: "../Protocols/Trojan"),
       .package(path: "../Protocols/Shadowsocks"),
       .package(path: "../Protocols/Hysteria2"),
       ```
       И в `.target(name: "ConfigParser", dependencies: ...)` добавить `"VLESSReality", "VLESSTLS", "Trojan", "Shadowsocks", "Hysteria2"`. PoolBuilder будет их вызывать.
    3. **Compile gate**: `cd BBTB/Packages/ConfigParser && swift build`. Если SwiftPM ругается на cyclic deps (не должен — Wave 6 уже устранил cycle перемещением ParsedXxx в VPNCore) — проверить, что:
       - VPNCore не зависит ни от ConfigParser, ни от Protocols/*.
       - TransportRegistry не зависит от ConfigParser или Protocols/*.
       - Protocols/* зависят только от VPNCore + TransportRegistry.
       - ConfigParser зависит от VPNCore + (теперь) Protocols/* + TransportRegistry (опционально — если PoolBuilder напрямую вызывает TransportRegistry.shared, что НЕ рекомендуется per D-13 — но Protocols/* делегируют).
    4. После compile success — run tests: `swift test` per package. Никаких regressions expected (Task 1 — pure dependency-graph changes без code modifications).
  </action>
  <verify>
    <automated>cd BBTB/Packages/ConfigParser && swift build 2>&1 | tail -10 && cd BBTB/Packages/Protocols/VLESSTLS && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TransportRegistry" BBTB/Packages/Protocols/VLESSTLS/Package.swift` ≥ 2 (path + target dep).
    - `grep -c "TransportRegistry" BBTB/Packages/Protocols/Trojan/Package.swift` ≥ 2.
    - `grep -c "VLESSTLS\|Trojan\|VLESSReality\|Shadowsocks\|Hysteria2" BBTB/Packages/ConfigParser/Package.swift` ≥ 10 (each protocol referenced as both path and target dep).
    - `swift build` in BBTB/Packages/ConfigParser exits 0.
    - `swift build` in each Protocols/X exits 0.
    - SwiftPM resolves without cyclic dependency errors.
  </acceptance_criteria>
  <done>Package graph allows PoolBuilder → Protocols/X → TransportRegistry; no cycles; existing tests still build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement buildOutbound in VLESSTLS + Trojan (transport-aware protocols) with full test coverage</name>
  <files>BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift, BBTB/Packages/Protocols/Trojan/Sources/Trojan/ConfigBuilder.swift, BBTB/Packages/Protocols/VLESSTLS/Tests/VLESSTLSTests/BuildOutboundTests.swift, BBTB/Packages/Protocols/Trojan/Tests/TrojanTests/BuildOutboundTests.swift</files>
  <read_first>
    BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift,
    BBTB/Packages/Protocols/Trojan/Sources/Trojan/ConfigBuilder.swift,
    BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift (lines 140-272 — existing buildVLESSTLSOutbound + buildTrojanOutbound),
    BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift,
    BBTB/Packages/VPNCore/Sources/VPNCore/TransportConfig.swift,
    BBTB/Packages/TransportRegistry/Sources/TransportRegistry/TransportRegistry.swift,
    BBTB/Packages/TransportRegistry/Sources/TransportRegistry/Handlers/WSTransportHandler.swift,
    .planning/phases/05-transports/05-RESEARCH.md (Pattern 2 lines 360-414 — exact buildOutbound code, Pitfall 1 ALPN h2 strip),
    .planning/phases/05-transports/05-PATTERNS.md
  </read_first>
  <behavior>
    - **VLESSTLS.ConfigBuilder.buildOutbound(from: parsed, transport: .tcp, tag: "vless-tls-0"):** возвращает dict с keys "type"="vless", "tag", "server", "server_port", "uuid", "flow", "network"="tcp", "tls" (с insecure=false, alpn=parsed.alpn, utls), БЕЗ "transport" key
    - **VLESSTLS.ConfigBuilder.buildOutbound(from: parsed, transport: .ws("/buy","cdn"), tag: ...):** dict содержит "transport" key с value {type:ws, path:/buy, headers:{Host:cdn}}; ALPN h2-strip applied (если parsed.alpn содержит h2 → h2 removed, или → ["http/1.1"] если list empty)
    - **VLESSTLS.ConfigBuilder.buildOutbound(from: parsed, transport: .grpc("svc"), tag: ...):** dict содержит "transport"={type:grpc, service_name:svc}; ALPN осталось parsed.alpn (NO h2-strip для не-WS transport)
    - **Trojan.ConfigBuilder.buildOutbound(from: parsed, transport: .tcp, tag:):** dict без "transport" key, type="trojan", network="tcp", insecure=false (R1 invariant)
    - **Trojan.ConfigBuilder.buildOutbound(from: parsed, transport: .ws("/x", ""), tag:):** "transport"={type:ws, path:/x} (host empty → headers omitted); ALPN h2-strip applied
    - **R1 invariant**: для обоих protocol packages, любой transport, любой parsed — `outbound["tls"]["insecure"]` всегда `false`. Один из тестов — explicit assertion.
    - Backward compat: PoolBuilder тесты PASS (Phase 4 baseline) — PoolBuilder ещё не вызывает эти buildOutbound в Task 2; coordinator refactor — Task 4.
  </behavior>
  <action>
    1. **VLESSTLS/ConfigBuilder.swift** — добавить новый extension/method:
       - Сохранить existing `public enum ConfigBuilder` + existing `buildSingBoxJSON(from inputs:)` метод нетронутыми (single-server template path, используется в Phase 4 в `ConfigImporter` — backward compat).
       - **Добавить** `extension ConfigBuilder { ... }` или вынести в тот же enum:
         - `import VPNCore` (для TransportConfig + ParsedVLESSTLS).
         - `import TransportRegistry` (для TransportRegistry.shared).
         - Метод:
           ```swift
           public static func buildOutbound(
               from parsed: ParsedVLESSTLS,
               transport: TransportConfig,
               tag: String
           ) -> [String: Any] {
               // Pitfall 1 — ALPN h2 strip для WS (Phase 2 W4 invariant).
               let alpn: [String]
               if case .ws = transport {
                   let filtered = parsed.alpn.filter { $0 != "h2" }
                   alpn = filtered.isEmpty ? ["http/1.1"] : filtered
               } else {
                   alpn = parsed.alpn
               }

               var outbound: [String: Any] = [
                   "type": "vless",
                   "tag": tag,
                   "server": parsed.host,
                   "server_port": parsed.port,
                   "uuid": parsed.uuid.uuidString.lowercased(),
                   "flow": parsed.flow ?? "",
                   "network": "tcp",  // VLESS+TLS over transport overlay always network=tcp
                   "tls": [
                       "enabled": true,
                       "server_name": parsed.sni,
                       "insecure": false,    // R1 invariant — hardcoded, never reads from parsed
                       "alpn": alpn,
                       "utls": ["enabled": true, "fingerprint": parsed.fingerprint],
                   ] as [String: Any],
               ]

               // D-13 — delegate transport block to TransportRegistry.
               if let block = TransportRegistry.shared.handler(for: transport.identifier)?
                   .buildTransportBlock(for: transport) {
                   outbound["transport"] = block
               }
               // If block is nil (TCP or unregistered) — no transport block, sing-box uses defaults.

               return outbound
           }
           ```

    2. **Trojan/ConfigBuilder.swift** — аналогично, но для ParsedTrojan:
       - Сохранить `TrojanInputs` struct + `TransportType` nested enum + existing `buildSingBoxJSON(from inputs:)` для single-server template path (используется в Phase 2 single-server). **Не удалять**, чтобы не сломать Phase 2 path. (D-06 удалил `ParsedTrojan.TransportType` — это другой enum, jeg, in TrojanURIParser. `Trojan.ConfigBuilder.TransportType` — local to this file's TrojanInputs struct, осталось as-is.)
       - **Добавить** новый метод:
         ```swift
         public static func buildOutbound(
             from parsed: ParsedTrojan,
             transport: TransportConfig,
             tag: String
         ) -> [String: Any] {
             // Pitfall 1 ALPN h2-strip
             let alpn: [String]
             if case .ws = transport {
                 let filtered = parsed.alpn.filter { $0 != "h2" }
                 alpn = filtered.isEmpty ? ["http/1.1"] : filtered
             } else {
                 alpn = parsed.alpn
             }

             var outbound: [String: Any] = [
                 "type": "trojan",
                 "tag": tag,
                 "server": parsed.host,
                 "server_port": parsed.port,
                 "password": parsed.password,
                 "network": "tcp",
                 "tls": [
                     "enabled": true,
                     "server_name": parsed.sni,
                     "insecure": false,
                     "alpn": alpn,
                     "utls": ["enabled": true, "fingerprint": parsed.fingerprint],
                 ] as [String: Any],
             ]

             // Special-case: legacy Trojan-WS block. If transport == .ws AND host empty, use SNI as Host header.
             if case let .ws(path, wsHost) = transport, wsHost.isEmpty {
                 // Legacy Phase 2 behavior — substitute SNI when host empty.
                 outbound["transport"] = [
                     "type": "ws",
                     "path": path,
                     "headers": ["Host": parsed.sni],
                 ] as [String: Any]
             } else if let block = TransportRegistry.shared.handler(for: transport.identifier)?
                 .buildTransportBlock(for: transport) {
                 outbound["transport"] = block
             }

             return outbound
         }
         ```
         Note: SNI fallback для empty host оставляем в Trojan package — это Phase 2 backward-compat invariant. Альтернативно — fallback на caller side, но Phase 2 fixtures ожидают именно эту семантику для Trojan-WS.

    3. **Tests — `BuildOutboundTests.swift` в VLESSTLS package:**
       - `import XCTest; @testable import VLESSTLS; import VPNCore; import TransportRegistry`.
       - **Setup**: в `override class func setUp()` зарегистрировать ВСЕ 5 transport handlers (TCP, WS, HTTP, HTTPUpgrade, gRPC) в `TransportRegistry.shared` — иначе lookup вернёт nil и transport block не добавится. Это test-level wiring; в production wiring — Task 4 (App startup).
       - Tests:
         - `test_vlessTLS_tcp_outbound_hasNoTransportKey` — transport=.tcp → result dict не содержит "transport".
         - `test_vlessTLS_ws_outbound_hasWsTransportBlock` — transport=.ws("/buy","cdn") → dict["transport"]["type"]=="ws", path, headers.
         - `test_vlessTLS_ws_alpn_excludes_h2` — parsed.alpn=["h2","http/1.1"], transport=.ws(...) → outbound["tls"]["alpn"] не содержит "h2" (Pitfall 1 invariant).
         - `test_vlessTLS_ws_alpn_emptyAfterFilter_fallbackToHttp1` — parsed.alpn=["h2"], transport=.ws(...) → alpn==["http/1.1"].
         - `test_vlessTLS_grpc_outbound_hasGrpcBlock` — transport=.grpc("svc") → dict["transport"]["service_name"]=="svc".
         - `test_vlessTLS_http_outbound_hasHttpBlock` — transport=.http("/api") → dict["transport"]["type"]=="http", path.
         - `test_vlessTLS_httpUpgrade_outbound_hasHttpUpgradeBlock` — transport=.httpUpgrade("/u","h") → dict["transport"]["host"] is String "h".
         - `test_vlessTLS_R1_insecure_false_invariant` — для каждого из 5 transport вариантов, assert `outbound["tls"]["insecure"] as? Bool == false`. (Loop over 5 cases.)
         - `test_vlessTLS_uuid_lowercased` — passed UUID uppercase → uuidString.lowercased() in output.
         - `test_vlessTLS_flow_emptyString_when_nil` — parsed.flow=nil → outbound["flow"]=="".

    4. **Tests — `BuildOutboundTests.swift` в Trojan package:** аналогичные тесты для Trojan:
       - `test_trojan_tcp_outbound_hasNoTransportKey`.
       - `test_trojan_ws_outbound_hasWsTransportBlock_withCustomHost`.
       - `test_trojan_ws_outbound_emptyHost_usesSNI` — parsed.sni="foo.com", transport=.ws("/x", "") → outbound["transport"]["headers"]["Host"]=="foo.com" (SNI fallback per Phase 2 invariant).
       - `test_trojan_ws_alpn_excludes_h2` — Phase 2 W4 invariant.
       - `test_trojan_grpc_outbound_hasGrpcBlock`.
       - `test_trojan_http_outbound`.
       - `test_trojan_httpUpgrade_outbound`.
       - `test_trojan_R1_insecure_false_invariant`.

    5. **Run tests:**
       - `cd BBTB/Packages/Protocols/VLESSTLS && swift test --filter BuildOutboundTests` — ≥ 10 tests PASS.
       - `cd BBTB/Packages/Protocols/Trojan && swift test --filter BuildOutboundTests` — ≥ 8 tests PASS.
       - **Existing Phase 1-4 tests** в этих packages — должны PASS unchanged (`swift test` без filter).
  </action>
  <verify>
    <automated>cd BBTB/Packages/Protocols/VLESSTLS && swift test 2>&1 | tail -20 && cd ../Trojan && swift test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public static func buildOutbound" BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift` == 1.
    - `grep -c "public static func buildOutbound" BBTB/Packages/Protocols/Trojan/Sources/Trojan/ConfigBuilder.swift` == 1.
    - `grep -c "TransportRegistry.shared.handler" BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift` == 1.
    - `grep -c "TransportRegistry.shared.handler" BBTB/Packages/Protocols/Trojan/Sources/Trojan/ConfigBuilder.swift` == 1.
    - `grep -c "\"insecure\": false" BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift` ≥ 1 (R1 hardcoded literal in buildOutbound).
    - `grep -c "\"insecure\": false" BBTB/Packages/Protocols/Trojan/Sources/Trojan/ConfigBuilder.swift` ≥ 1.
    - `grep -c "filter { \\\$0 != \"h2\" }" BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift` ≥ 1 (ALPN h2-strip preserved).
    - `swift test --filter BuildOutboundTests` in VLESSTLS exits 0 with ≥ 10 tests.
    - `swift test --filter BuildOutboundTests` in Trojan exits 0 with ≥ 8 tests.
    - Existing tests in both packages still PASS.
  </acceptance_criteria>
  <done>VLESSTLS + Trojan packages produce correct outbound dicts with transport block injection via TransportRegistry; R1 + ALPN h2-strip invariants preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Implement buildOutbound in VLESSReality + Shadowsocks + Hysteria2 (transport-ignoring protocols)</name>
  <files>BBTB/Packages/Protocols/VLESSReality/Sources/VLESSReality/ConfigBuilder.swift, BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/ConfigBuilder.swift, BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/ConfigBuilder.swift</files>
  <read_first>
    BBTB/Packages/Protocols/VLESSReality/Sources/VLESSReality/ConfigBuilder.swift,
    BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/ConfigBuilder.swift,
    BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/ConfigBuilder.swift,
    BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift (lines 114-233 — existing private builders for VLESS Reality, Shadowsocks, Hysteria2),
    BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift,
    .planning/phases/05-transports/05-CONTEXT.md (D-03, D-08, D-16),
    .planning/phases/05-transports/05-RESEARCH.md (R1 invariant for Hysteria2 D-08 exception, R1 invariant for Shadowsocks no TLS)
  </read_first>
  <behavior>
    - VLESSReality.ConfigBuilder.buildOutbound(from: v, transport: .tcp, tag:) produces outbound with type="vless", network="tcp", tls with insecure=false, server_name, utls, reality block (publicKey + shortId), NO transport key
    - VLESSReality.ConfigBuilder.buildOutbound с transport=.ws(...) **игнорирует transport** (D-03 — Reality only TCP) и возвращает то же что для transport=.tcp
    - Shadowsocks.ConfigBuilder.buildOutbound возвращает dict БЕЗ TLS блока (D-16 — SS shifrutes на уровне протокола), без "transport" key
    - Hysteria2.ConfigBuilder.buildOutbound сохраняет R1 EXCEPTION (D-08 — insecure: parsed.allowInsecure); содержит alpn=["h3"], optional obfs salamander
    - Все три protocol package builders НЕ читают TransportRegistry (D-16 — transport overlay не применяется)
  </behavior>
  <action>
    1. **VLESSReality/ConfigBuilder.swift:**
       - Сохранить existing template-based `buildSingBoxJSON(from inputs:)` (Phase 1 path).
       - Добавить `import VPNCore` (для ParsedVLESS + TransportConfig).
       - Новый метод:
         ```swift
         public static func buildOutbound(
             from parsed: ParsedVLESS,
             transport: TransportConfig,    // D-03: ignored — Reality only TCP
             tag: String
         ) -> [String: Any] {
             // D-03: Reality is XTLS-incompatible with transport overlay. transport param accepted
             // for API consistency (CORE-03) but always treated as .tcp internally.
             // Copy verbatim from PoolBuilder.buildVLESSOutbound (lines 114-138).

             var tls: [String: Any] = [
                 "enabled": true,
                 "server_name": parsed.sni,
                 "utls": ["enabled": true, "fingerprint": parsed.fingerprint],
             ]
             if !parsed.publicKey.isEmpty {
                 tls["reality"] = [
                     "enabled": true,
                     "public_key": parsed.publicKey,
                     "short_id": parsed.shortId,
                 ] as [String: Any]
             }
             // Reality: no insecure field — TLS handled by Reality embedded in client config.
             // (Verify: existing PoolBuilder.buildVLESSOutbound also omits insecure.)
             return [
                 "type": "vless",
                 "tag": tag,
                 "server": parsed.host,
                 "server_port": parsed.port,
                 "uuid": parsed.uuid.uuidString.lowercased(),
                 "flow": parsed.flow.isEmpty ? "" : parsed.flow,
                 "network": "tcp",
                 "tls": tls,
             ]
         }
         ```
         **Critical**: Скопировать ТОЧНО existing PoolBuilder.buildVLESSOutbound поведение (lines 114-138). Никаких новых fields, никаких новых invariants — мы переносим, не редактируем.

    2. **Shadowsocks/ConfigBuilder.swift:**
       - Сохранить existing `buildSingBoxJSON(from inputs:)`.
       - Добавить `import VPNCore`.
       - Новый метод — copy verbatim из PoolBuilder.buildShadowsocksOutbound (lines 173-183):
         ```swift
         public static func buildOutbound(
             from parsed: ParsedShadowsocks,
             transport: TransportConfig,    // D-16: ignored — Shadowsocks has no transport layer
             tag: String
         ) -> [String: Any] {
             // R1 invariant trivial: no TLS block — encryption is at protocol layer.
             return [
                 "type": "shadowsocks",
                 "tag": tag,
                 "server": parsed.host,
                 "server_port": parsed.port,
                 "method": parsed.method,
                 "password": parsed.password,
                 "network": "tcp",
             ]
         }
         ```

    3. **Hysteria2/ConfigBuilder.swift:**
       - Сохранить existing `buildSingBoxJSON(from inputs:)`.
       - Добавить `import VPNCore`.
       - Новый метод — copy verbatim из PoolBuilder.buildHysteria2Outbound (lines 201-233), **сохраняя весь R1 EXCEPTION comment block** (D-08 invariant):
         ```swift
         // ============================================================
         // R1 EXCEPTION — ONLY Hysteria2 (D-08).
         // This is the ONLY buildOutbound in BBTB where tls.insecure
         // may legitimately be set to true. Copying this pattern to any
         // other protocol builder is a security bug (Pitfall 2 RESEARCH).
         //
         // Mitigation layers:
         //   1. This comment block (code-level marker for PR review).
         //   2. test_nonHy2_outbounds_neverHaveInsecureTrue invariant test.
         //   3. ParsedShadowsocks/ParsedVLESSTLS/ParsedTrojan structs do NOT
         //      have an allowInsecure field (type-level by design).
         //
         // See: wiki/security-gaps.md R17,
         //      .planning/phases/04-protocol-expansion/04-CONTEXT.md D-08.
         // ============================================================
         public static func buildOutbound(
             from parsed: ParsedHysteria2,
             transport: TransportConfig,    // D-16: ignored — Hy2 is QUIC, no transport layer
             tag: String
         ) -> [String: Any] {
             // R1 EXCEPTION — only Hy2.
             var tls: [String: Any] = [
                 "enabled": true,
                 "server_name": parsed.sni,
                 "insecure": parsed.allowInsecure,    // D-08 EXCEPTION
                 "alpn": ["h3"],
             ]
             if let fp = parsed.fingerprint, !fp.isEmpty {
                 tls["utls"] = ["enabled": true, "fingerprint": fp]
             } else {
                 tls["utls"] = ["enabled": true, "fingerprint": "chrome"]
             }
             if let pin = parsed.pinSHA256, !pin.isEmpty {
                 tls["certificate_public_key_sha256"] = [pin]
             }

             var outbound: [String: Any] = [
                 "type": "hysteria2",
                 "tag": tag,
                 "server": parsed.host,
                 "server_port": parsed.port,
                 "password": parsed.auth,
                 "tls": tls,
             ]
             if let obfs = parsed.obfs, obfs == "salamander",
                let obfsPwd = parsed.obfsPassword, !obfsPwd.isEmpty {
                 outbound["obfs"] = ["type": "salamander", "password": obfsPwd]
             }
             return outbound
         }
         ```

    4. **No new test files** для этих 3 packages — copy verbatim семантики means existing PoolBuilderTests + R1 invariant test (test_nonHy2_outbounds_neverHaveInsecureTrue) уже покрывают их при Task 4 PoolBuilder coordinator refactor.

    5. **Compile gate**: `swift build` per package.
       - `cd BBTB/Packages/Protocols/VLESSReality && swift build` — 0 errors.
       - `cd BBTB/Packages/Protocols/Shadowsocks && swift build` — 0 errors.
       - `cd BBTB/Packages/Protocols/Hysteria2 && swift build` — 0 errors.
  </action>
  <verify>
    <automated>cd BBTB/Packages/Protocols/VLESSReality && swift build 2>&1 | tail -5 && cd ../Shadowsocks && swift build 2>&1 | tail -5 && cd ../Hysteria2 && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public static func buildOutbound" BBTB/Packages/Protocols/VLESSReality/Sources/VLESSReality/ConfigBuilder.swift` == 1.
    - `grep -c "public static func buildOutbound" BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/ConfigBuilder.swift` == 1.
    - `grep -c "public static func buildOutbound" BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/ConfigBuilder.swift` == 1.
    - `grep -c "R1 EXCEPTION" BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/ConfigBuilder.swift` ≥ 1 (D-08 comment block preserved).
    - `grep -c "insecure.*parsed.allowInsecure" BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/ConfigBuilder.swift` ≥ 1 (D-08 exception applied).
    - `grep -c "TransportRegistry" BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/ConfigBuilder.swift` == 0 (D-16 — does NOT use TransportRegistry).
    - `grep -c "TransportRegistry" BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/ConfigBuilder.swift` == 0.
    - `swift build` in each Protocols/X package exits 0.
  </acceptance_criteria>
  <done>VLESSReality + Shadowsocks + Hysteria2 packages have buildOutbound static methods identical in semantics to PoolBuilder's existing private builders; D-08 R1 exception isolated to Hysteria2 ONLY.</done>
</task>

<task type="auto">
  <name>Task 4: PoolBuilder coordinator refactor + applyTransportOverride helper + tests</name>
  <files>BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift, BBTB/Packages/ConfigParser/Sources/ConfigParser/TransportOverride.swift, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/TransportOverrideTests.swift</files>
  <read_first>
    BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift (current state after Wave 6),
    BBTB/Packages/Protocols/VLESSReality/Sources/VLESSReality/ConfigBuilder.swift (Task 3 output),
    BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift (Task 2 output),
    BBTB/Packages/Protocols/Trojan/Sources/Trojan/ConfigBuilder.swift (Task 2 output),
    BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/ConfigBuilder.swift (Task 3 output),
    BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/ConfigBuilder.swift (Task 3 output),
    BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift,
    .planning/phases/05-transports/05-RESEARCH.md (Pattern 3 lines 416-456, Pitfall 5),
    BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift,
    BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderSingleOutboundTests.swift
  </read_first>
  <behavior>
    - PoolBuilder.buildSingBoxJSON switch — каждый из 5 case'ов — ОДНА строка вызывающая ProtocolX.ConfigBuilder.buildOutbound(...)
    - Private functions buildVLESSOutbound/buildVLESSTLSOutbound/buildTrojanOutbound/buildShadowsocksOutbound/buildHysteria2Outbound — УДАЛЕНЫ
    - dnsBlock / urltest / direct / route assembly — БЕЗ ИЗМЕНЕНИЙ
    - applyTransportOverride(parsed:override:) — для .vlessTLS/.trojan mutate transport; для .vlessReality/.shadowsocks/.hysteria2 — return parsed unchanged
    - Existing tests: PoolBuilderTests, PoolBuilderSingleOutboundTests, DualProtocolSmokeTests, test_nonHy2_outbounds_neverHaveInsecureTrue — все PASS после refactor (ZERO regressions)
    - Новые тесты: TransportOverrideTests покрывают 5 cases AnyParsedConfig × override scenarios
    - Built JSON output для VLESS+TLS+WS должен содержать "transport":{"type":"ws"...} block (новое поведение Phase 5)
  </behavior>
  <action>
    1. **PoolBuilder.swift coordinator refactor:**
       - Добавить imports: `import VLESSReality, import VLESSTLS, import Trojan, import Shadowsocks, import Hysteria2`.
       - `import VPNCore` уже есть с Wave 6.
       - В `buildSingBoxJSON` найти switch по `parsed` (lines 42-61 per current state). Заменить каждый case на ONE-LINE call:
         ```swift
         switch parsed {
         case .vlessReality(let v):
             tag = "vless-\(index)"
             outbound = VLESSReality.ConfigBuilder.buildOutbound(from: v, transport: .tcp, tag: tag)
         case .vlessTLS(let v):
             tag = "vless-tls-\(index)"
             outbound = VLESSTLS.ConfigBuilder.buildOutbound(from: v, transport: v.transport, tag: tag)
         case .trojan(let t):
             tag = "trojan-\(index)"
             outbound = Trojan.ConfigBuilder.buildOutbound(from: t, transport: t.transport, tag: tag)
         case .shadowsocks(let s):
             tag = "ss-\(index)"
             outbound = Shadowsocks.ConfigBuilder.buildOutbound(from: s, transport: .tcp, tag: tag)
         case .hysteria2(let h):
             tag = "hy2-\(index)"
             outbound = Hysteria2.ConfigBuilder.buildOutbound(from: h, transport: .tcp, tag: tag)
         }
         ```
       - **Удалить** все 5 private static methods: `buildVLESSOutbound`, `buildVLESSTLSOutbound`, `buildTrojanOutbound`, `buildShadowsocksOutbound`, `buildHysteria2Outbound` (lines ~114-272 в текущем PoolBuilder.swift). Эта логика переехала в protocol packages в Tasks 2-3.
       - **Сохранить нетронутыми**: `dnsBlock` (private static), `buildSingleOutboundJSON` (public static — thin wrapper), `PoolError` enum, top-level `buildSingBoxJSON` signature.
       - **R1 invariant comment block** (lines 185-200 — R1 EXCEPTION для Hy2) — УДАЛИТЬ из PoolBuilder; оно теперь живёт в Hysteria2/ConfigBuilder.swift (Task 3 verified).

    2. **TransportOverride.swift — applyTransportOverride helper:**
       - Создать `BBTB/Packages/ConfigParser/Sources/ConfigParser/TransportOverride.swift`:
         - `import Foundation; import VPNCore`.
         - Public function:
           ```swift
           /// Phase 5 D-19 / Pitfall 5 — apply per-server transport override to ParsedXxx.
           /// Only `.vlessTLS` and `.trojan` accept overrides (D-03/D-16: Reality/SS/Hy2 ignore).
           /// Called by ConfigImporter before PoolBuilder.buildSingBoxJSON.
           public func applyTransportOverride(
               _ parsed: AnyParsedConfig,
               _ override: TransportConfig?
           ) -> AnyParsedConfig {
               guard let override = override else { return parsed }
               switch parsed {
               case .vlessTLS(let v):
                   let mutated = ParsedVLESSTLS(
                       uuid: v.uuid, host: v.host, port: v.port,
                       flow: v.flow, sni: v.sni, fingerprint: v.fingerprint,
                       alpn: v.alpn, transport: override, remarks: v.remarks
                   )
                   return .vlessTLS(mutated)
               case .trojan(let t):
                   let mutated = ParsedTrojan(
                       password: t.password, host: t.host, port: t.port,
                       security: t.security, sni: t.sni, fingerprint: t.fingerprint,
                       alpn: t.alpn, transport: override, remarks: t.remarks
                   )
                   return .trojan(mutated)
               case .vlessReality, .shadowsocks, .hysteria2:
                   return parsed  // D-03/D-16: override ignored
               }
           }
           ```
         **Note**: Using fresh struct construction (not `var v` rebind) avoids requiring `let` → `var` field migration in Wave 6's ParsedConfigs.swift. This keeps Wave 6 fully backward-compatible.

    3. **Tests — TransportOverrideTests.swift:**
       - `import XCTest; @testable import ConfigParser; import VPNCore`.
       - Tests:
         - `test_applyOverride_nilOverride_returnsUnchanged` — все 5 case'ов с override=nil → identity-equal output.
         - `test_applyOverride_vlessTLS_replacesTransport` — original .vlessTLS(transport: .tcp), override .ws("/x","h") → result .vlessTLS(transport: .ws("/x","h")).
         - `test_applyOverride_trojan_replacesTransport` — same pattern для Trojan.
         - `test_applyOverride_vlessReality_ignoresOverride` — original .vlessReality, override .ws(...) → result .vlessReality unchanged. (D-03 invariant.)
         - `test_applyOverride_shadowsocks_ignoresOverride` — D-16 invariant.
         - `test_applyOverride_hysteria2_ignoresOverride` — D-16 invariant.

    4. **Run всю ConfigParser тест-сюиту**:
       - `cd BBTB/Packages/ConfigParser && swift test` — все ≥ 164 теста (Wave 5 baseline) + 6 новых TransportOverride = ≥ 170 PASS.
       - Existing PoolBuilderTests, DualProtocolSmokeTests, R1 invariant test — все PASS (refactor preserved semantics).
       - Если какой-либо тест fails — diagnose: чаще всего mismatch в output dict ключах. Verify Tasks 2-3 buildOutbound вывод matches existing private builders byte-for-byte.

    5. **VLESS+TLS+WS integration smoke test** (new test или extension существующего PoolBuilderTests):
       - Загрузить fixture `vless-tls-ws.txt`, парсить, передать в PoolBuilder.buildSingBoxJSON.
       - Parse JSON output, найти outbound[0]["transport"], assert это dict с type="ws", path="/buy", headers["Host"]="cdn.example".
       - Это first end-to-end test показывающий Phase 5 new behavior — VLESS+TLS получает transport block через TransportRegistry.
       - **Setup constraint**: тест должен register WSTransportHandler в TransportRegistry.shared **до** вызова PoolBuilder. Использовать `override class func setUp()` для one-time registration в test class.
  </action>
  <verify>
    <automated>cd BBTB/Packages/ConfigParser && swift test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "VLESSReality.ConfigBuilder.buildOutbound\|VLESSTLS.ConfigBuilder.buildOutbound\|Trojan.ConfigBuilder.buildOutbound\|Shadowsocks.ConfigBuilder.buildOutbound\|Hysteria2.ConfigBuilder.buildOutbound" BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift` == 5 (each protocol called exactly once).
    - `grep -c "private static func build" BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift` ≤ 1 (only dnsBlock remains; all 5 outbound builders REMOVED).
    - File `BBTB/Packages/ConfigParser/Sources/ConfigParser/TransportOverride.swift` exists with `public func applyTransportOverride`.
    - `cd BBTB/Packages/ConfigParser && swift test` exits 0 with ≥ 170 tests run (Wave 5 baseline 164 + 6 TransportOverride).
    - test_nonHy2_outbounds_neverHaveInsecureTrue (existing invariant test in PoolBuilderTests) PASSes — refactor did not introduce R1 leak.
    - VLESS+TLS+WS end-to-end test: built JSON contains `"transport":{"type":"ws"` block.
  </acceptance_criteria>
  <done>PoolBuilder is now thin coordinator (5 one-liners + dnsBlock + assembly); each protocol package owns its outbound assembly; applyTransportOverride helper covers 5 case'ов correctly.</done>
</task>

<task type="auto">
  <name>Task 5: TransportRegistry registration in App startup + ConfigImporter applies transportOverride</name>
  <files>BBTB/App/iOSApp/BBTB_iOSApp.swift, BBTB/App/macOSApp/BBTB_macOSApp.swift, BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift</files>
  <read_first>
    BBTB/App/iOSApp/BBTB_iOSApp.swift,
    BBTB/App/macOSApp/BBTB_macOSApp.swift,
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift (focus on provisionTunnelProfile method around line 455 + reparseFromKeychain around line 538),
    BBTB/Packages/ConfigParser/Sources/ConfigParser/TransportOverride.swift (Task 4 output),
    BBTB/Packages/TransportRegistry/Sources/TransportRegistry/TransportRegistry.swift,
    BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift,
    .planning/phases/05-transports/05-RESEARCH.md (Pitfall 8 lines 587-602)
  </read_first>
  <behavior>
    - BBTB_iOSApp.swift и BBTB_macOSApp.swift содержат 5 строк регистрации TransportRegistry handler-ов (TCP, WS, HTTP, HTTPUpgrade, gRPC) после ProtocolRegistry block
    - TransportRegistry.shared.registeredIdentifiers после bootstrap содержит set {"tcp", "ws", "http", "httpupgrade", "grpc"} — smoke test verify
    - ConfigImporter.provisionTunnelProfile применяет applyTransportOverride к каждому parsed config перед PoolBuilder.buildSingBoxJSON
    - Phase 5 переходный момент: до Wave 8 (UI) ServerConfig.transportOverride не существует как SwiftData field; ConfigImporter использует stub `transportOverride(for: cfg) -> TransportConfig?` возвращающий nil. Wave 8 заменит на чтение реального поля.
    - Existing Phase 1-4 connect flow работает unchanged (все existing tests + integration scenarios PASS)
  </behavior>
  <action>
    1. **BBTB/App/iOSApp/BBTB_iOSApp.swift:**
       - Найти existing `ProtocolRegistry.shared.register(...)` block (lines 37-41).
       - Добавить imports в file header: `import TransportRegistry`. Verify, что existing imports (ProtocolRegistry, etc.) уже есть.
       - Сразу после `ProtocolRegistry.shared.register(Hysteria2Handler.self)` line добавить 5 строк:
         ```swift
         // CORE-03 (Phase 5) — register transport handlers.
         // Pitfall 8: registration must happen before any provisionTunnelProfile call.
         TransportRegistry.shared.register(TCPTransportHandler.self)
         TransportRegistry.shared.register(WSTransportHandler.self)
         TransportRegistry.shared.register(HTTPTransportHandler.self)
         TransportRegistry.shared.register(HTTPUpgradeTransportHandler.self)
         TransportRegistry.shared.register(GRPCTransportHandler.self)
         ```
    2. **BBTB/App/macOSApp/BBTB_macOSApp.swift:** идентично — добавить тот же `import TransportRegistry` + те же 5 строк registration.

    3. **ConfigImporter.swift — applyTransportOverride wiring:**
       - Найти метод `provisionTunnelProfile(for selectedID: UUID?)` (line ~455).
       - Найти где `parsedList` собирается (lines ~487, ~493). После `let parsed = try? reparseFromKeychain(cfg, tag: tag)` (или соответствующий line) — wrap parsed в applyTransportOverride:
         ```swift
         // Phase 5 D-19 — apply per-server transport override (currently always nil; Wave 8 wires real field).
         let withOverride = applyTransportOverride(parsed, transportOverride(for: cfg))
         parsedList = [withOverride]   // for explicit-selection branch
         // (and parsedList.append(withOverride) for auto-mode branch)
         ```
       - Добавить private helper в конец class:
         ```swift
         /// Phase 5 placeholder — Wave 8 replaces with `cfg.transportOverride` (SwiftData field).
         /// Returns nil until ServerDetailView + SwiftData migration land in Wave 8.
         private func transportOverride(for cfg: ServerConfig) -> TransportConfig? {
             return nil
         }
         ```
       - Добавить `import ConfigParser` (если не импортирован — но он уже импортирован для PoolBuilder access). Verify через `grep -n "import ConfigParser" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift`.
       - **applyTransportOverride** — top-level function в ConfigParser, не метод; доступен через `import ConfigParser` без префикса. Verify в Task 4 что она объявлена `public func` (top-level), не как метод enum.

    4. **Smoke test для bootstrap:**
       - Добавить test в `BBTB/Packages/TransportRegistry/Tests/TransportRegistryTests/TransportRegistryTests.swift` (Wave 0 file):
         ```swift
         func test_bootstrap_smoke_allFiveHandlersIdentifiable() {
             // This test does NOT register handlers — it verifies the API surface.
             // Production bootstrap (BBTB_iOSApp / BBTB_macOSApp) registers all 5.
             let expectedIdentifiers: Set<String> = ["tcp", "ws", "http", "httpupgrade", "grpc"]
             let handlerTypes: [any TransportHandler.Type] = [
                 TCPTransportHandler.self,
                 WSTransportHandler.self,
                 HTTPTransportHandler.self,
                 HTTPUpgradeTransportHandler.self,
                 GRPCTransportHandler.self,
             ]
             let actualIdentifiers = Set(handlerTypes.map { $0.identifier })
             XCTAssertEqual(actualIdentifiers, expectedIdentifiers)
         }
         ```
         Этот тест verify что API names handler-ов совпадают с TransportConfig identifier-ами — это catch typo в Wave 0-5.

    5. **Run integration tests:**
       - `cd BBTB && swift build` (workspace-level) — должен пройти; Xcode build также должен пройти, если doable.
       - `cd BBTB/Packages/AppFeatures && swift test` — все existing tests PASS.
       - `cd BBTB/Packages/TransportRegistry && swift test --filter test_bootstrap_smoke_allFiveHandlersIdentifiable` — PASS.
       - `cd BBTB/Packages/ConfigParser && swift test` — все ≥ 170 tests PASS (Wave 7 baseline preserved).
  </action>
  <verify>
    <automated>cd BBTB/App/iOSApp && grep -c "TransportRegistry.shared.register" BBTB_iOSApp.swift; cd /Users/vergevsky/ClaudeProjects/VPN/BBTB/App/macOSApp && grep -c "TransportRegistry.shared.register" BBTB_macOSApp.swift; cd /Users/vergevsky/ClaudeProjects/VPN/BBTB/Packages/AppFeatures && swift build 2>&1 | tail -10 && cd ../TransportRegistry && swift test 2>&1 | tail -10    <automated>cd BBTB/App && grep -c "TransportRegistry.shared.register" iOSApp/BBTB_iOSApp.swift macOSApp/BBTB_macOSApp.swift 2>&1 | tail -5 && cd /Users/vergevsky/ClaudeProjects/VPN/BBTB/Packages/AppFeatures && swift build 2>&1 | tail -10 && cd ../TransportRegistry && swift test 2>&1 | tail -10 && cd ../ConfigParser && swift test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TransportRegistry.shared.register" BBTB/App/iOSApp/BBTB_iOSApp.swift` == 5 (TCP + WS + HTTP + HTTPUpgrade + gRPC).
    - `grep -c "TransportRegistry.shared.register" BBTB/App/macOSApp/BBTB_macOSApp.swift` == 5.
    - `grep -c "import TransportRegistry" BBTB/App/iOSApp/BBTB_iOSApp.swift` ≥ 1.
    - `grep -c "import TransportRegistry" BBTB/App/macOSApp/BBTB_macOSApp.swift` ≥ 1.
    - `grep -c "applyTransportOverride" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift` ≥ 1 (at least one call site).
    - `grep -c "private func transportOverride(for cfg: ServerConfig)" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift` == 1 (stub placeholder added).
    - `cd BBTB/Packages/AppFeatures && swift build` exits 0.
    - `cd BBTB/Packages/TransportRegistry && swift test` exits 0 with the new bootstrap smoke test PASSing.
    - `cd BBTB/Packages/ConfigParser && swift test` ≥ 170 tests PASS (Wave 7 baseline).
  </acceptance_criteria>
  <done>Both iOS and macOS App entry points register all 5 transport handlers; ConfigImporter wires applyTransportOverride with stub helper ready for Wave 8 SwiftData field; full integration test suite green.</done>
</task>

</tasks>

<verification>
**Full Wave 7 verification:**

```bash
# 1. Package builds — every layer compiles after dependency restructure
cd BBTB/Packages/VPNCore && swift build 2>&1 | tail -5
cd BBTB/Packages/TransportRegistry && swift build 2>&1 | tail -5
for pkg in VLESSReality VLESSTLS Trojan Shadowsocks Hysteria2; do
  cd "BBTB/Packages/Protocols/$pkg" && swift build 2>&1 | tail -3
done
cd BBTB/Packages/ConfigParser && swift build 2>&1 | tail -5
cd BBTB/Packages/AppFeatures && swift build 2>&1 | tail -5
# Expected: all exit 0

# 2. Tests — no regressions, new tests pass
cd BBTB/Packages/VPNCore && swift test 2>&1 | tail -10        # ≥ Wave 6 baseline
cd BBTB/Packages/TransportRegistry && swift test 2>&1 | tail -10  # ≥ 41 (Wave 4: 40 + bootstrap smoke)
cd BBTB/Packages/Protocols/VLESSTLS && swift test 2>&1 | tail -10  # existing + ≥ 10 BuildOutboundTests
cd BBTB/Packages/Protocols/Trojan && swift test 2>&1 | tail -10    # existing + ≥ 8 BuildOutboundTests
cd BBTB/Packages/ConfigParser && swift test 2>&1 | tail -15        # ≥ 170 (Wave 6 + 6 TransportOverride)
cd BBTB/Packages/AppFeatures && swift test 2>&1 | tail -10

# 3. R1 security invariant — most important regression check
cd BBTB/Packages/ConfigParser && swift test --filter "test_nonHy2_outbounds_neverHaveInsecureTrue" 2>&1 | tail -10
# Expected: PASS — refactor preserved R1
```

**Если хоть один тест fails в этом списке — Wave 7 не готов. Fix immediately, не proceed to Wave 8.**
</verification>

<success_criteria>
- [ ] Package.swift dependencies updated: Protocols/* depend on TransportRegistry + VPNCore; ConfigParser depends on all 5 Protocols/*
- [ ] All 5 protocol packages have `public static func buildOutbound(from:transport:tag:)` method
- [ ] VLESSTLS + Trojan buildOutbound call `TransportRegistry.shared.handler(for:)?.buildTransportBlock` for transport overlay
- [ ] VLESSReality + Shadowsocks + Hysteria2 buildOutbound ignore transport param (D-03/D-16); D-08 R1 EXCEPTION isolated to Hysteria2
- [ ] PoolBuilder.swift has 5 one-line case calls; private buildXxxOutbound methods REMOVED
- [ ] TransportOverride.swift created with applyTransportOverride helper covering 5 case'ов
- [ ] BBTB_iOSApp.swift + BBTB_macOSApp.swift register all 5 TransportHandlers
- [ ] ConfigImporter.provisionTunnelProfile applies applyTransportOverride before PoolBuilder
- [ ] R1 invariant test (`test_nonHy2_outbounds_neverHaveInsecureTrue`) PASSes
- [ ] ALPN h2-strip invariant preserved для Trojan-WS (Phase 2 W4 backward compat)
- [ ] Full ConfigParser suite ≥ 170 tests PASS (Wave 6 baseline + 6 TransportOverride tests)
- [ ] All ≥ 18 new BuildOutboundTests PASS in VLESSTLS + Trojan packages
- [ ] No regressions in any pre-Phase-5 test
</success_criteria>

<output>
After completion, create `.planning/phases/05-transports/05-07-SUMMARY.md` documenting:
- Per-protocol buildOutbound methods added (5 packages, with R1 / D-08 / ALPN invariants noted)
- PoolBuilder coordinator size delta (lines before vs after — should be ~150 lines smaller)
- Bootstrap registration confirmed for both iOS + macOS App entry points
- Pitfall 8 mitigation: register BEFORE first connect — verified via test
- Test counts: Protocols/VLESSTLS +10, Protocols/Trojan +8, ConfigParser +6 TransportOverride + integration smoke
- Stub `transportOverride(for: cfg) -> TransportConfig?` returning nil in ConfigImporter — replaced by Wave 8 with real `cfg.transportOverride` SwiftData field read
- Next: Wave 8 — UI plan: ServerDetailView + ServerListSheet chevron + ServerConfig.transportOverride SwiftData lightweight migration + Picker wiring
</output>
