---
phase: 05-transports
plan: 08
type: execute
wave: 8
depends_on: [05-07]
files_modified:
  - BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift
  - BBTB/Packages/VPNCore/Tests/VPNCoreTests/ServerConfigTransportOverrideTests.swift
  - BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerRow.swift
  - BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift
  - BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListViewModel.swift
  - BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerDetailView.swift
  - BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerDetailViewModel.swift
  - BBTB/Packages/AppFeatures/Sources/ServerListFeature/TransportPicker.swift
  - BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/ServerDetailViewModelTests.swift
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift
  - BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings
autonomous: false
requirements: [TRANSP-05]

must_haves:
  truths:
    - "ServerConfig имеет optional поле transportOverride: TransportConfig? с default nil (SwiftData lightweight migration)"
    - "Открытие ServerListSheet после установки Phase 5 build с pre-Phase-5 data — не крашит, все сервера видны, transportOverride == nil для всех"
    - "ServerRow содержит шеврон-кнопку › справа, тап на неё → push на ServerDetailView через NavigationLink"
    - "ServerDetailView показывает: name, host, port, protocolDisplayName, sni, lastLatencyMs, countryCode (из ServerConfig); + flow, fingerprint, UUID, ALPN, publicKey, shortId (re-parsed из rawURI или Keychain via ConfigImporter)"
    - "ServerDetailView содержит Transport Picker с опциями: Авто, TCP, WebSocket, gRPC, HTTP/2, HTTPUpgrade — selected отображает текущее значение transportOverride (или Авто если nil)"
    - "Изменение Picker сохраняет ServerConfig.transportOverride в SwiftData через ModelContext.save() — fetch-all + Swift filter, НЕ #Predicate (Pitfall 4)"
    - "ConfigImporter.transportOverride(for:) теперь возвращает cfg.transportOverride (не nil placeholder)"
    - "Connect после выбора WS override — sing-box config содержит transport block ws (verified via ConfigImporter test or UAT)"
  artifacts:
    - path: BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift
      provides: "ServerConfig.transportOverride: TransportConfig? added with SwiftData lightweight migration"
      contains: "transportOverride: TransportConfig?"
    - path: BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerDetailView.swift
      provides: "Server detail screen with read-only fields + transport Picker"
      contains: "Picker"
    - path: BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerDetailViewModel.swift
      provides: "Re-parses rawURI/Keychain on appear; persists transportOverride on Picker change"
      contains: "applyTransportSelection"
    - path: BBTB/Packages/AppFeatures/Sources/ServerListFeature/TransportPicker.swift
      provides: "DesignSystem-styled Picker view"
      contains: "TransportSelection"
  key_links:
    - from: BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift
      to: BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerDetailView.swift
      via: "NavigationLink / navigationDestination от ServerRow chevron tap"
      pattern: "ServerDetailView"
    - from: BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerDetailViewModel.swift
      to: BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift
      via: "server.transportOverride = override; try context.save()"
      pattern: "transportOverride = "
    - from: BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift
      to: BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift
      via: "transportOverride(for cfg: ServerConfig) returns cfg.transportOverride"
      pattern: "cfg.transportOverride"
---

<objective>
Wave 8 — финальный UI + SwiftData миграция. Закрывает TRANSP-05 ("В Расширенных можно вручную
выбрать транспорт для дебага") через push-navigation от ServerListSheet к ServerDetailView,
с persisted per-server Picker.

**Главные риски Phase 5 концентрируются здесь:**
1. **SwiftData lightweight migration** добавления optional Codable enum (Pitfall 3) — может крашить
   на pre-Phase-5 install при первом запуске.
2. **NavigationStack внутри sheet** на iOS 18 — UX-вопрос (Open Q3): push может collapse-ить
   sheet detents.
3. **Pitfall 4** — SwiftData #Predicate с Codable enum возвращает empty; нужен fetch-all + filter.

Мы митигируем (1) через optional с default nil — auto-migration работает; UAT verify
на pre-Phase-5 device.
Мы митигируем (2) через NavigationStack + force `.large` detent при open detail (Open Q3
recommendation).
Мы митигируем (3) через явный fetch-all + Swift filter в ServerDetailViewModel.saveTransportOverride
(D-19 + Pitfall 4 invariant).

**Один human-verify checkpoint** в конце — пользователь подтверждает на iPhone, что:
- Шеврон tap открывает экран.
- Выбор WS persists после re-open.
- Connect после override работает (transport block присутствует в logs).

Output: 7 файлов модифицировано (ServerConfig + ServerRow + ServerListSheet + ViewModel +
ConfigImporter + Localizable), 4 файла создано (ServerDetailView, ViewModel, Picker, tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-transports/05-CONTEXT.md
@.planning/phases/05-transports/05-RESEARCH.md
@.planning/phases/05-transports/05-PATTERNS.md
@.planning/phases/05-transports/05-07-PLAN.md

# Source-of-truth references
@BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift
@BBTB/Packages/VPNCore/Sources/VPNCore/TransportConfig.swift
@BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerRow.swift
@BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift
@BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListViewModel.swift
@BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift
@BBTB/Packages/DesignSystem/Sources/DesignSystem/DesignSystem.swift

<interfaces>
**ServerConfig.transportOverride field (D-19):**
```swift
// In ServerConfig @Model, after `missingFromLastFetch`:
/// Phase 5 D-19 — manual transport override (TRANSP-05).
/// nil = use URI-derived transport (Auto in Picker).
/// non-nil = пользователь выбрал транспорт в ServerDetailView.
/// SwiftData lightweight migration: optional Codable enum auto-migrated (Pitfall 3).
public var transportOverride: TransportConfig?
```

**TransportSelection enum для Picker** (per RESEARCH Example 5 lines 918-925):
```swift
public enum TransportSelection: Hashable {
    case auto   // == transportOverride == nil
    case tcp
    case ws
    case grpc
    case http
    case httpUpgrade

    public static func from(_ override: TransportConfig?) -> TransportSelection
    public func toOverride() -> TransportConfig?  // .auto → nil; others → TransportConfig case with defaults
}
```

**ServerDetailViewModel API:**
```swift
@MainActor
public final class ServerDetailViewModel: ObservableObject {
    @Published public private(set) var parsedDetails: ParsedDetails?
    @Published public var selectedTransport: TransportSelection

    public init(server: ServerConfig, modelContainer: ModelContainer, configImporter: ConfigImporting)

    public func onAppear() async                            // re-parse rawURI / Keychain
    public func applyTransportSelection(_ new: TransportSelection) async  // persist via ModelContext
}

public struct ParsedDetails: Equatable {
    public let uuid: UUID?
    public let flow: String?
    public let fingerprint: String
    public let alpn: [String]
    public let publicKey: String?
    public let shortId: String?
    public let currentTransport: TransportConfig
}
```

**Localization keys to add** (per RESEARCH Example 5):
- `serverDetailGeneralSection` = "Общие" / "General"
- `serverDetailParsedSection` = "Параметры протокола" / "Protocol parameters"
- `serverDetailTransportSection` = "Транспорт" / "Transport"
- `serverDetailTransport` = "Транспорт" / "Transport"
- `serverDetailTransportAuto` = "Авто" / "Auto"
- `serverDetailTransportFooter` = "Авто использует транспорт из ссылки сервера" / "Auto uses transport from server URL"
- `serverDetailName` = "Имя" / "Name"
- `serverDetailHost` = "Хост" / "Host"
- `serverDetailPort` = "Порт" / "Port"
- `serverDetailProtocol` = "Протокол" / "Protocol"
- `serverDetailLatency` = "Задержка" / "Latency"
- `serverDetailFlow` = "Flow"
- `serverDetailFingerprint` = "Fingerprint"
- `serverDetailPublicKey` = "Public key"
- `serverDetailShortId` = "Short ID"
- `serverDetailAccessibilityHint` = "Детали сервера" / "Server details"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ServerConfig.transportOverride SwiftData field + migration smoke test</name>
  <files>BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift, BBTB/Packages/VPNCore/Tests/VPNCoreTests/ServerConfigTransportOverrideTests.swift</files>
  <read_first>
    BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift,
    BBTB/Packages/VPNCore/Sources/VPNCore/TransportConfig.swift,
    BBTB/Packages/VPNCore/Sources/VPNCore/SwiftDataContainer.swift,
    .planning/phases/05-transports/05-CONTEXT.md (D-19),
    .planning/phases/05-transports/05-RESEARCH.md (Pitfall 3 SwiftData Codable enum migration lines 509-519, Example 7 lines 989-1011)
  </read_first>
  <action>
    1. **ServerConfig.swift** — добавить новое optional поле + init parameter:
       - Найти место в @Model class после `public var missingFromLastFetch: Bool = false` (line 58):
         ```swift
         // Phase 5 D-19 — TRANSP-05 manual transport override.
         /// nil = use URI-derived transport (Auto in Picker).
         /// non-nil = user-selected transport in ServerDetailView.
         /// SwiftData lightweight migration: optional Codable enum auto-migrated (Pitfall 3 RESEARCH).
         public var transportOverride: TransportConfig?
         ```
         Без default value — для optional полей SwiftData lightweight migration работает (default = nil implicitly).
       - В init: добавить parameter `transportOverride: TransportConfig? = nil` в конец списка; в body — `self.transportOverride = transportOverride`.
       - **Не модифицировать SwiftDataContainer.swift** schema registration — `@Model ServerConfig` уже зарегистрирован; добавление optional field не требует schema version bump (lightweight migration).

    2. **Migration smoke test — ServerConfigTransportOverrideTests.swift:**
       - `import XCTest; @testable import VPNCore; import SwiftData`.
       - Tests:
         - `test_serverConfig_init_transportOverride_defaultsNil` — создать ServerConfig без указания transportOverride, assert `result.transportOverride == nil`.
         - `test_serverConfig_init_transportOverride_storesValue` — создать ServerConfig с `transportOverride: .ws(path: "/x", host: "h")`, assert поле == указанное.
         - `test_serverConfig_swiftData_roundtrip` — создать in-memory ModelContainer; insert ServerConfig с transportOverride=.ws("/x", "h"); save; fetch via FetchDescriptor; assert retrieved.transportOverride == .ws("/x", "h"). Verify SwiftData round-trip через JSON encoding works.
         - `test_serverConfig_swiftData_nilOverride_roundtrip` — то же но transportOverride=nil; assert retrieved.transportOverride == nil.
       - **In-memory ModelContainer setup** (по образцу existing SwiftData tests в VPNCore — search via `grep -rn "ModelContainer\|inMemory" BBTB/Packages/VPNCore/Tests/`):
         ```swift
         let config = ModelConfiguration(isStoredInMemoryOnly: true)
         let container = try ModelContainer(for: ServerConfig.self, configurations: config)
         let context = ModelContext(container)
         ```

    3. **Compile + tests:**
       - `cd BBTB/Packages/VPNCore && swift build` — 0 errors.
       - `cd BBTB/Packages/VPNCore && swift test --filter ServerConfigTransportOverrideTests` — 4 PASS.
       - `cd BBTB/Packages/VPNCore && swift test` — all existing tests + 4 new PASS.

    4. **Downstream compile check:**
       - `cd BBTB/Packages/ConfigParser && swift build` — должен пройти (ServerConfig в VPNCore; ConfigParser теперь видит новое поле автоматически).
       - `cd BBTB/Packages/AppFeatures && swift build` — должен пройти.
  </action>
  <verify>
    <automated>cd BBTB/Packages/VPNCore && swift test --filter ServerConfigTransportOverrideTests 2>&1 | tail -10 && cd BBTB/Packages/AppFeatures && swift build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public var transportOverride: TransportConfig?" BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift` == 1.
    - `grep -c "transportOverride: TransportConfig? = nil" BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift` ≥ 1 (init parameter).
    - `swift test --filter ServerConfigTransportOverrideTests` exits 0 with 4 tests PASS.
    - SwiftData round-trip test PASSes (verifies Codable enum persists).
    - Downstream packages still compile (ConfigParser, AppFeatures).
  </acceptance_criteria>
  <done>ServerConfig.transportOverride field added with SwiftData lightweight migration; in-memory round-trip verified for nil + non-nil values.</done>
</task>

<task type="auto">
  <name>Task 2: ServerDetailView + ViewModel + TransportPicker (UI components)</name>
  <files>BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerDetailView.swift, BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerDetailViewModel.swift, BBTB/Packages/AppFeatures/Sources/ServerListFeature/TransportPicker.swift, BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/ServerDetailViewModelTests.swift, BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings</files>
  <read_first>
    BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift,
    BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListViewModel.swift,
    BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerRow.swift,
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift (reparseFromKeychain method around line 538),
    BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift (with transportOverride from Task 1),
    BBTB/Packages/VPNCore/Sources/VPNCore/TransportConfig.swift,
    BBTB/Packages/DesignSystem/Sources/DesignSystem/DesignSystem.swift,
    BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings,
    .planning/phases/05-transports/05-RESEARCH.md (Example 5 ServerDetailView lines 846-916, Pitfall 4 SwiftData filter, Pitfall 5 override by ID, Pitfall 9 re-parse rawURI/Keychain)
  </read_first>
  <action>
    1. **TransportPicker.swift** — отдельный View компонент:
       - `import SwiftUI; import VPNCore; import DesignSystem; import Localization`.
       - Объявить `public enum TransportSelection: Hashable, Sendable` per RESEARCH Example 5 lines 918-925.
         Cases: `.auto, .tcp, .ws, .grpc, .http, .httpUpgrade`.
       - Добавить static helper `public static func from(_ override: TransportConfig?) -> TransportSelection`:
         ```swift
         guard let override else { return .auto }
         switch override {
         case .tcp: return .tcp
         case .ws: return .ws
         case .grpc: return .grpc
         case .http: return .http
         case .httpUpgrade: return .httpUpgrade
         }
         ```
       - Добавить instance method `public func toOverride() -> TransportConfig?`:
         ```swift
         switch self {
         case .auto: return nil
         case .tcp: return .tcp
         case .ws: return .ws(path: "/", host: "")             // user-edited path/host — future; defaults for Phase 5
         case .grpc: return .grpc(serviceName: "TunService")   // sing-box default
         case .http: return .http(path: "/")
         case .httpUpgrade: return .httpUpgrade(path: "/", host: "")
         }
         ```
         **Important UX note in comment**: "Phase 5 Picker provides COARSE override — user selects transport type, but does NOT edit path/host/serviceName. URI-derived values lost when override applied. Wave 10 (Advanced settings) will expose per-field editing."
       - Объявить `public struct TransportPicker: View`:
         ```swift
         public struct TransportPicker: View {
             @Binding public var selection: TransportSelection
             public init(selection: Binding<TransportSelection>) { self._selection = selection }
             public var body: some View {
                 Picker(selection: $selection) {
                     Text(L10n.serverDetailTransportAuto).tag(TransportSelection.auto)
                     Text("TCP").tag(TransportSelection.tcp)
                     Text("WebSocket").tag(TransportSelection.ws)
                     Text("gRPC").tag(TransportSelection.grpc)
                     Text("HTTP/2").tag(TransportSelection.http)
                     Text("HTTPUpgrade").tag(TransportSelection.httpUpgrade)
                 } label: {
                     Text(L10n.serverDetailTransport)
                 }
                 .pickerStyle(.menu)
                 .accessibilityIdentifier("BBTB.ServerDetail.TransportPicker")
             }
         }
         ```
         Сохранить `import Localization`; использовать `L10n.serverDetailTransportAuto` etc.

    2. **ServerDetailViewModel.swift:**
       - `import Foundation; import SwiftUI; import SwiftData; import OSLog; import VPNCore; import ConfigParser`.
       - `import MainScreenFeature` (для access к ConfigImporting protocol) — но ConfigImporting в ConfigParser, не MainScreenFeature; verify через `grep -n "public protocol ConfigImporting" BBTB/Packages/ConfigParser/Sources/ConfigParser/ConfigImporting.swift`. **Используй ConfigImporting protocol из ConfigParser** (уже импортирован выше).
       - Тип `ParsedDetails` (struct):
         ```swift
         public struct ParsedDetails: Equatable, Sendable {
             public let uuid: UUID?
             public let flow: String?
             public let fingerprint: String
             public let alpn: [String]
             public let publicKey: String?
             public let shortId: String?
             public let currentTransport: TransportConfig
         }
         ```
       - `@MainActor public final class ServerDetailViewModel: ObservableObject`:
         - private static let log = `Logger(subsystem: "app.bbtb.server-list", category: "detail")`.
         - `@Published public private(set) var parsedDetails: ParsedDetails?`.
         - `@Published public var selectedTransport: TransportSelection` — initialized from `TransportSelection.from(server.transportOverride)` in init.
         - Stored: `public let server: ServerConfig` (для read-only UI fields), `private let modelContainer: ModelContainer`, `private let configImporter: ConfigImporting`.
         - `public init(server: ServerConfig, modelContainer: ModelContainer, configImporter: ConfigImporting)`.
         - `public func onAppear() async`:
           - Извлечь `tag = server.keychainTag` если есть.
           - Если `tag != nil` — попытаться re-parse из Keychain через ConfigImporter (Pitfall 9: для supported servers, rawURI = nil per T-02-04 invariant — нужно идти через Keychain). **Note:** existing `reparseFromKeychain` в ConfigImporter — private. Решение: добавить публичный метод в ConfigImporting protocol `reparseAnyParsedConfig(from cfg: ServerConfig) async -> AnyParsedConfig?` (в Task 4 ниже) ИЛИ временно использовать `server.rawURI` где есть. Для Wave 8 простейший путь: если `server.rawURI != nil` — парсить через UniversalImportParser; иначе показать "(secrets in Keychain — re-parse unavailable)" в parsedDetails.
           - Из parsed AnyParsedConfig extract: uuid, flow, fingerprint, alpn, publicKey (Reality only), shortId (Reality only), currentTransport.
           - Assign to `parsedDetails`.
         - `public func applyTransportSelection(_ new: TransportSelection) async`:
           - Pitfall 4 (SwiftData #Predicate) + Pitfall 5 (apply by ServerConfig.id):
             ```swift
             let context = ModelContext(modelContainer)
             let allServers = (try? context.fetch(FetchDescriptor<ServerConfig>())) ?? []
             guard let cfg = allServers.first(where: { $0.id == server.id }) else {
                 Self.log.warning("ServerDetailVM: server \(server.id) not found in store")
                 return
             }
             let newOverride = new.toOverride()
             cfg.transportOverride = newOverride
             do {
                 try context.save()
                 selectedTransport = new
                 Self.log.info("ServerDetailVM: persisted transportOverride=\(String(describing: newOverride)) for \(cfg.id)")
             } catch {
                 Self.log.error("ServerDetailVM: save failed: \(error.localizedDescription)")
             }
             ```

    3. **ServerDetailView.swift** — SwiftUI screen per RESEARCH Example 5:
       - `import SwiftUI; import VPNCore; import DesignSystem; import Localization; import ConfigParser`.
       - `public struct ServerDetailView: View`:
         - `@ObservedObject public var viewModel: ServerDetailViewModel`.
         - `public init(viewModel: ServerDetailViewModel) { self.viewModel = viewModel }`.
         - body — Form с 3 секциями (per Example 5 lines 864-915):
           - **Section "General" (L10n.serverDetailGeneralSection)**: name, host, port, protocolDisplayName, sni (если есть), latency (если есть), countryCode (если есть).
           - **Section "Parsed" (L10n.serverDetailParsedSection)** (if viewModel.parsedDetails != nil): uuid, flow, fingerprint, alpn, publicKey, shortId.
           - **Section "Transport" (L10n.serverDetailTransportSection)**: `TransportPicker(selection: $viewModel.selectedTransport)`. + `.onChange(of: viewModel.selectedTransport)` → `Task { await viewModel.applyTransportSelection(new) }`. + Footer Text L10n.serverDetailTransportFooter.
         - `.navigationTitle(viewModel.server.name)` + `.navigationBarTitleDisplayMode(.inline)` (iOS only).
         - `.task { await viewModel.onAppear() }`.
       - `private struct LabeledRow: View` per Example 5 lines 927-937 — HStack с label (secondary) + value (primary, textSelection enabled).

    4. **Localization keys** — `BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings`:
       - Добавить keys list из <interfaces> выше (15 keys × 2 locales ru + en).
       - Use existing JSON structure of `.xcstrings`; добавить новые entries по образцу existing keys.
       - L10n constants — Localization автогенерирует из xcstrings; verify имена.

    5. **ServerDetailViewModelTests.swift:**
       - `import XCTest; @testable import ServerListFeature; import VPNCore; import SwiftData; import ConfigParser`.
       - Tests:
         - `test_init_selectedTransport_fromOverride_auto` — server with nil override → selectedTransport == .auto.
         - `test_init_selectedTransport_fromOverride_ws` — server with .ws("/x","h") override → selectedTransport == .ws.
         - `test_applyTransportSelection_ws_persists` — call applyTransportSelection(.ws), verify server.transportOverride == .ws(path:"/", host:""), verify SwiftData save called (in-memory store, refetch, check value).
         - `test_applyTransportSelection_auto_clearsOverride` — start with .ws override, apply .auto → server.transportOverride == nil.
         - `test_applyTransportSelection_usesFetchAllNotPredicate` — verify the code path doesn't use #Predicate (Pitfall 4) — this is a "white-box" test or just trust the implementation review.
       - Mock ConfigImporting via simple struct conforming to protocol (return canned ParsedXxx).
  </action>
  <verify>
    <automated>cd BBTB/Packages/AppFeatures && swift test --filter ServerDetailViewModelTests 2>&1 | tail -15 && swift build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Files `ServerDetailView.swift`, `ServerDetailViewModel.swift`, `TransportPicker.swift` exist in ServerListFeature.
    - `grep -c "public enum TransportSelection" BBTB/Packages/AppFeatures/Sources/ServerListFeature/TransportPicker.swift` == 1.
    - `grep -c "case .auto\|case .tcp\|case .ws\|case .grpc\|case .http\|case .httpUpgrade" BBTB/Packages/AppFeatures/Sources/ServerListFeature/TransportPicker.swift` ≥ 6 (all 6 TransportSelection cases referenced).
    - `grep -c "applyTransportSelection" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerDetailViewModel.swift` ≥ 1.
    - `grep -c "fetch(FetchDescriptor<ServerConfig>())" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerDetailViewModel.swift` ≥ 1 (Pitfall 4 — fetch-all not predicate).
    - `grep -c "#Predicate" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerDetailViewModel.swift` == 0 (Pitfall 4 — must NOT use #Predicate for transportOverride filter).
    - Localizable.xcstrings contains keys for serverDetailGeneralSection, serverDetailTransport, serverDetailTransportAuto, etc.
    - `swift test --filter ServerDetailViewModelTests` exits 0 with ≥ 5 tests.
    - `swift build` for AppFeatures exits 0.
  </acceptance_criteria>
  <done>ServerDetailView screen + ViewModel + TransportPicker view exist; ViewModel persists transportOverride correctly via fetch-all+filter; localizations added; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: ServerListSheet chevron NavigationLink + ServerRow modification + ServerListViewModel.openDetail</name>
  <files>BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerRow.swift, BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift, BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListViewModel.swift</files>
  <read_first>
    BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerRow.swift,
    BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift,
    BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListViewModel.swift,
    BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerDetailView.swift (Task 2 output),
    .planning/phases/05-transports/05-RESEARCH.md (Example 6 ServerListSheet chevron lines 942-986, Open Question 3 NavigationStack-в-sheet)
  </read_first>
  <action>
    1. **ServerRow.swift:**
       - Найти existing struct (line ~19 per grep). Сейчас row — `Button(action: onTap)` обёрнут в `.buttonStyle(.plain)`.
       - Modify init и body:
         - Init: добавить `let onDetailTap: () -> Void` parameter.
         - В body: HStack content (после `LatencyBadge` / selected checkmark, перед закрывающей `.padding`) добавить trailing `Button` с шевроном:
           ```swift
           Button(action: onDetailTap) {
               Image(systemName: "chevron.right")
                   .imageScale(.small)
                   .foregroundStyle(.tertiary)
                   .padding(.leading, DS.Spacing.sm)
           }
           .buttonStyle(.plain)
           .accessibilityIdentifier("BBTB.ServerListSheet.ServerRow.Detail.\(server.id.uuidString)")
           .accessibilityLabel(L10n.serverDetailAccessibilityHint)
           ```
         - Existing `Button(action: onTap)` (вся row body) — оставить как селектор; шеврон-кнопка вложена в HStack и **должна перехватывать тап** (SwiftUI: nested Button перехватывает touch). Verify в UAT.

    2. **ServerListSheet.swift:**
       - Найти existing `sheetContent` (line ~106).
       - Wrap content в `NavigationStack` (per RESEARCH Example 6 line 947):
         ```swift
         NavigationStack {
             ScrollView {
                 // existing LazyVStack + ForEach
             }
             .navigationDestination(item: $viewModel.openServerDetail) { server in
                 ServerDetailView(viewModel: viewModel.makeDetailViewModel(for: server))
             }
         }
         ```
       - **Open Question 3 mitigation**: При open detail force `.large` detent. Это можно сделать через `@State private var detents: Set<PresentationDetent> = ...` reactive switching, или `.presentationDetents([.large])` фиксированно когда openServerDetail != nil. **Минимальный риск path**: оставить existing `sheetDetents` (current var sheetDetents per line ~67), и НЕ force `.large` — verify в UAT, и если глючит — Wave 8 follow-up. (Open Question 3: recommendation — force .large; здесь мы оставляем default, отмечая в SUMMARY как Phase 11 follow-up если глитч обнаружен.)
       - Pass new `onDetailTap` closure в ServerRow callsite (current ServerRow constructor call в LazyVStack — нужно дополнить closure parameter): `onDetailTap: { viewModel.openDetail(for: server) }`.

    3. **ServerListViewModel.swift:**
       - Найти existing вершину class (line ~44).
       - Добавить published state:
         ```swift
         /// Phase 5 D-17 — non-nil when user tapped chevron; triggers .navigationDestination push.
         @Published public var openServerDetail: ServerConfig? = nil
         ```
       - Добавить public method:
         ```swift
         public func openDetail(for server: ServerConfig) {
             openServerDetail = server
         }
         ```
       - Добавить factory method:
         ```swift
         public func makeDetailViewModel(for server: ServerConfig) -> ServerDetailViewModel {
             return ServerDetailViewModel(
                 server: server,
                 modelContainer: modelContainer,
                 configImporter: importer
             )
         }
         ```
         (`importer` — existing private property `ConfigImporting`; verify via existing init signature — `ServerListViewModel` constructor включает importer на Plan 04 baseline.)

    4. **Compile + smoke tests:**
       - `cd BBTB/Packages/AppFeatures && swift build` — 0 errors.
       - `cd BBTB/Packages/AppFeatures && swift test` — existing tests + Task 2 tests PASS.
  </action>
  <verify>
    <automated>cd BBTB/Packages/AppFeatures && swift build 2>&1 | tail -10 && swift test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onDetailTap" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerRow.swift` ≥ 2 (parameter + Button action).
    - `grep -c "chevron.right" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerRow.swift` == 1.
    - `grep -c "NavigationStack" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift` ≥ 1.
    - `grep -c "navigationDestination" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift` ≥ 1.
    - `grep -c "openServerDetail" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListViewModel.swift` ≥ 2 (property + method).
    - `grep -c "makeDetailViewModel" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListViewModel.swift` ≥ 1.
    - `swift build` for AppFeatures exits 0.
    - All ServerListFeature tests still pass (existing + Task 2 new).
  </acceptance_criteria>
  <done>Chevron in ServerRow → NavigationLink to ServerDetailView; ServerListViewModel exposes openDetail + makeDetailViewModel; sheet wraps content in NavigationStack.</done>
</task>

<task type="auto">
  <name>Task 4: ConfigImporter wires real transportOverride read + reparseAnyParsedConfig public method</name>
  <files>BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift</files>
  <read_first>
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift (focus reparseFromKeychain ~538 + transportOverride stub helper from Wave 7),
    BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift (with new transportOverride field),
    BBTB/Packages/ConfigParser/Sources/ConfigParser/ConfigImporting.swift,
    BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerDetailViewModel.swift (Task 2)
  </read_first>
  <action>
    1. **Replace placeholder helper:** В ConfigImporter.swift найти Wave 7 stub:
       ```swift
       private func transportOverride(for cfg: ServerConfig) -> TransportConfig? {
           return nil
       }
       ```
       Заменить на:
       ```swift
       private func transportOverride(for cfg: ServerConfig) -> TransportConfig? {
           return cfg.transportOverride
       }
       ```
    2. **Expose reparseAnyParsedConfig для ServerDetailViewModel:**
       - В `ConfigImporting` protocol (`BBTB/Packages/ConfigParser/Sources/ConfigParser/ConfigImporting.swift`) добавить:
         ```swift
         /// Phase 5 — re-parse AnyParsedConfig от ServerConfig (Keychain или rawURI).
         /// Используется ServerDetailViewModel для отображения detail fields (flow, fingerprint, etc).
         /// Возвращает nil если parsing fails (corrupted Keychain или missing fields).
         func reparseAnyParsedConfig(from cfg: ServerConfig) async -> AnyParsedConfig?
         ```
       - В ConfigImporter concrete implementation:
         ```swift
         public func reparseAnyParsedConfig(from cfg: ServerConfig) async -> AnyParsedConfig? {
             // Strategy: prefer Keychain (supported servers had rawURI cleared per T-02-04 invariant);
             // fallback to rawURI for unsupported / Phase-4-upgraded servers.
             if let tag = cfg.keychainTag, let parsed = try? reparseFromKeychain(cfg, tag: tag) {
                 return parsed
             }
             if let rawURI = cfg.rawURI {
                 // Universal parser path — same as ConfigImporter.importFromRawInput pipeline for one URI.
                 // For Phase 5 simplicity, we don't run full pipeline — just call the URI parser directly
                 // based on cfg.protocolID. (Wave 11 may unify these paths.)
                 // Phase 5 acceptable: return nil if Keychain fails AND rawURI parse non-trivial.
                 // ServerDetailView shows whatever fields are available from ServerConfig alone.
                 return nil
             }
             return nil
         }
         ```
         Это minimal implementation — для UI it's enough showing fields when Keychain succeeds. Расширение (rawURI parse path) — Wave 11 (UX polish).
    3. **ServerDetailViewModel.onAppear использование:**
       - В Task 2 onAppear method заменить TODO/stub на:
         ```swift
         public func onAppear() async {
             let parsed = await configImporter.reparseAnyParsedConfig(from: server)
             self.parsedDetails = parsed.map { extractDetails(from: $0) }
         }

         private func extractDetails(from parsed: AnyParsedConfig) -> ParsedDetails {
             switch parsed {
             case .vlessReality(let v):
                 return ParsedDetails(
                     uuid: v.uuid, flow: v.flow.isEmpty ? nil : v.flow,
                     fingerprint: v.fingerprint, alpn: [], publicKey: v.publicKey,
                     shortId: v.shortId, currentTransport: .tcp
                 )
             case .vlessTLS(let v):
                 return ParsedDetails(
                     uuid: v.uuid, flow: v.flow, fingerprint: v.fingerprint,
                     alpn: v.alpn, publicKey: nil, shortId: nil,
                     currentTransport: v.transport
                 )
             case .trojan(let t):
                 return ParsedDetails(
                     uuid: nil, flow: nil, fingerprint: t.fingerprint,
                     alpn: t.alpn, publicKey: nil, shortId: nil,
                     currentTransport: t.transport
                 )
             case .shadowsocks(let s):
                 return ParsedDetails(
                     uuid: nil, flow: nil, fingerprint: "—",
                     alpn: [], publicKey: nil, shortId: nil,
                     currentTransport: .tcp
                 )
             case .hysteria2(let h):
                 return ParsedDetails(
                     uuid: nil, flow: nil, fingerprint: h.fingerprint ?? "—",
                     alpn: ["h3"], publicKey: nil, shortId: nil,
                     currentTransport: .tcp
                 )
             }
         }
         ```
    4. **Compile + run tests**:
       - `cd BBTB/Packages/ConfigParser && swift build` — 0 errors (protocol addition).
       - `cd BBTB/Packages/AppFeatures && swift build` — 0 errors.
       - `cd BBTB/Packages/AppFeatures && swift test` — все existing tests + Task 2 new tests PASS.
  </action>
  <verify>
    <automated>cd BBTB/Packages/AppFeatures && swift build 2>&1 | tail -10 && swift test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "return cfg.transportOverride" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift` ≥ 1 (replaced stub).
    - `grep -c "return nil$" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift | head -1` shows fewer matches than Wave 7 baseline (stub removed).
    - `grep -c "reparseAnyParsedConfig" BBTB/Packages/ConfigParser/Sources/ConfigParser/ConfigImporting.swift` ≥ 1 (protocol method).
    - `grep -c "reparseAnyParsedConfig" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift` ≥ 1 (concrete impl).
    - `grep -c "reparseAnyParsedConfig" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerDetailViewModel.swift` ≥ 1 (call site).
    - `swift build` for AppFeatures + ConfigParser exit 0.
    - All AppFeatures tests PASS.
  </acceptance_criteria>
  <done>ConfigImporter reads real transportOverride from ServerConfig; reparseAnyParsedConfig public API exposed for ServerDetailView; ParsedDetails extracted correctly from all 5 protocol variants.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human verification of UI + SwiftData migration on real device</name>
  <what-built>
    Phase 5 complete: 4 transport handlers (WS, gRPC, HTTP, HTTPUpgrade) registered in TransportRegistry,
    PoolBuilder coordinator delegates to protocol packages, ServerDetailView accessible via chevron
    в ServerListSheet, ServerConfig.transportOverride persists Picker selection, ConfigImporter
    applies override before PoolBuilder.

    Verified автоматически:
    - 230+ unit tests PASS (TransportRegistry: 40, ConfigParser: 170+, VPNCore: 10+, Protocols/VLESSTLS+Trojan: 18+)
    - R1 invariant test (test_nonHy2_outbounds_neverHaveInsecureTrue) PASSes
    - SwiftData round-trip test для transportOverride field PASSes (in-memory)
    - Build success на iOS + macOS targets
  </what-built>
  <how-to-verify>
    **Critical UAT checks (выполнить НА устройстве — iPhone preferred):**

    1. **SwiftData migration safety** (Pitfall 3 — самый высокий риск):
       - Установить Phase 4 build (предыдущая версия) → импортировать ≥ 1 VLESS+TLS сервер → дождаться completion.
       - Установить Phase 5 build поверх (БЕЗ delete data).
       - Открыть приложение. **Ожидание**: app не крашит на launch; ServerListSheet показывает existing сервера; transportOverride для всех == nil (Auto).
       - Если приложение крашит → fallback к split-fields workaround (RESEARCH Pitfall 3 footer); это блокер.

    2. **Chevron → push navigation (Open Q3 — NavigationStack-в-sheet):**
       - Открыть ServerListSheet (тап на server-line в main screen).
       - Тап на шеврон `›` справа любой row.
       - **Ожидание**: push на ServerDetailView; navigation back button работает; sheet detents не collapse.
       - Если sheet "схлопывается" в height 0 при push → fallback к full-screen modal (Phase 11 follow-up).

    3. **Picker persistence:**
       - Открыть ServerDetailView для любого VLESS+TLS сервера.
       - Тап на Picker «Транспорт» → выбрать «WebSocket».
       - Close detail view (back button).
       - Open detail view снова (тот же сервер).
       - **Ожидание**: Picker shows «WebSocket» (persisted).

    4. **End-to-end connect с override:**
       - Установить override = «WebSocket» для VLESS+TLS сервера (если subscription содержит WS-capable VLESS — server side must support WS).
       - Close detail view.
       - Toggle connect (большая кнопка main screen).
       - **Ожидание**: connection succeeds; IP проверка via `https://api.ipify.org` показывает VPN IP. Логи sing-box (если access via Xcode console) содержат "ws transport" в outbound config.
       - Если сервер НЕ поддерживает WS — connection fails (это expected; override не magically работает с TCP-only server). User должен вернуть Picker в «Авто».

    5. **Backward compat — Trojan-WS из Phase 2 fixture:**
       - Использовать Trojan-WS subscription URL (если есть) — re-import.
       - Подключиться. **Ожидание**: connection works идентично Phase 2 (no regression).

    **NOT required for human-verify (deferred to manual testing per Phase 4 precedent):**
    - All 4 transports × 2 protocols (8 combinations) end-to-end UAT — manual user testing post-merge.
    - macOS UAT — same flow на Mac.

    Если **любая из 5 checks fails** — Phase 5 NOT shippable. Создать gap-closure issue.
  </how-to-verify>
  <resume-signal>Type "approved — Phase 5 complete" or describe failures.</resume-signal>
</task>

</tasks>

<verification>
**Final Phase 5 verification gate:**

```bash
# 1. Full test suite
cd BBTB/Packages/VPNCore && swift test 2>&1 | tail -10
cd BBTB/Packages/TransportRegistry && swift test 2>&1 | tail -10
cd BBTB/Packages/Protocols/VLESSTLS && swift test 2>&1 | tail -10
cd BBTB/Packages/Protocols/Trojan && swift test 2>&1 | tail -10
cd BBTB/Packages/Protocols/VLESSReality && swift test 2>&1 | tail -10
cd BBTB/Packages/Protocols/Shadowsocks && swift test 2>&1 | tail -10
cd BBTB/Packages/Protocols/Hysteria2 && swift test 2>&1 | tail -10
cd BBTB/Packages/ConfigParser && swift test 2>&1 | tail -15
cd BBTB/Packages/AppFeatures && swift test 2>&1 | tail -10

# 2. Build all packages
for pkg in VPNCore TransportRegistry ConfigParser AppFeatures; do
  cd BBTB/Packages/$pkg && swift build 2>&1 | tail -3
done
for pkg in VLESSReality VLESSTLS Trojan Shadowsocks Hysteria2; do
  cd BBTB/Packages/Protocols/$pkg && swift build 2>&1 | tail -3
done

# 3. Workspace build (Xcode-level)
cd BBTB && xcodebuild -workspace BBTB.xcworkspace -scheme BBTB-iOS -configuration Debug build 2>&1 | tail -10
```

Expected: ALL exits 0, all test counts at or above per-wave baselines.
</verification>

<success_criteria>
- [ ] `ServerConfig.transportOverride: TransportConfig?` field added with SwiftData lightweight migration
- [ ] ServerConfigTransportOverrideTests — 4 tests PASS (init defaults, init stores, swiftdata roundtrip nil + value)
- [ ] ServerDetailView + ServerDetailViewModel + TransportPicker created in ServerListFeature
- [ ] ServerDetailViewModelTests — ≥ 5 tests PASS
- [ ] ServerRow has chevron Button; ServerListSheet wraps content in NavigationStack with navigationDestination
- [ ] ServerListViewModel exposes openServerDetail + openDetail + makeDetailViewModel
- [ ] Localizable.xcstrings contains 15 new keys (ru + en)
- [ ] ConfigImporter.transportOverride(for:) returns cfg.transportOverride (no longer stub nil)
- [ ] ConfigImporting protocol gains reparseAnyParsedConfig method
- [ ] ServerDetailViewModel uses fetch-all + filter (NOT #Predicate) for transportOverride save (Pitfall 4)
- [ ] All Wave 0-7 tests still PASS — no regressions
- [ ] Human verify checkpoint PASSED — 5 UAT checks confirmed on iPhone
- [ ] All 5 phase requirements (CORE-03, TRANSP-01..05) satisfied (TRANSP-01 XHTTP deferred to backlog per CONTEXT.md)
</success_criteria>

<output>
After completion, create `.planning/phases/05-transports/05-08-SUMMARY.md` documenting:
- ServerConfig.transportOverride SwiftData migration confirmed safe on iPhone (no pre-Phase-5 data crash)
- ServerDetailView screen flow: tap chevron → push → Picker → persists → connect respects override
- 15 new localization keys (ru + en)
- ConfigImporter wire-up: stub → real read of cfg.transportOverride
- Test counts: VPNCore +4, ServerListFeature +5, ConfigParser baseline preserved
- UAT outcome: PASS / FAIL details from Task 5 checkpoint
- Phase 5 architecture decisions for `wiki/security-gaps.md` / new `wiki/transports.md`:
  - TransportRegistry pattern (CORE-03)
  - Shared TransportConfig в VPNCore
  - PoolBuilder coordinator after D-15
  - Per-protocol buildOutbound + R1 / ALPN h2 / D-08 EXCEPTION invariants preserved
  - SwiftData lightweight Codable enum migration verified
- Carry-forwards / Phase 11 follow-ups:
  - NavigationStack-в-sheet UX (Open Q3) — if collapses, force `.large` detent
  - ServerDetailView field editing (SNI, publicKey override) — Phase 10 Advanced Settings
- Next: Phase 6 — Network resilience.
</output>
