---
phase: 11-onboarding-ux-polish
plan: 02
type: execute
wave: 2
depends_on: [11-01-PLAN.md]
files_modified:
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift
  - BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenView.swift
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift
  - BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/FileImporterTests.swift
autonomous: true
requirements:
  - IMP-03
user_setup: []

must_haves:
  truths:
    - "В меню «+» на главном экране есть третья кнопка «Импортировать из файла»"
    - "Tap на эту кнопку открывает системный document picker с фильтром .json/.yaml/.yml"
    - "После выбора файла приложение читает его содержимое и импортирует через тот же pipeline, что и буфер/QR"
    - "Файлы из iCloud Drive и Files.app доступны через security-scoped resource API"
    - "Ошибки доступа/чтения файла показываются через тот же alert механизм что и pasteboard errors"
    - "ImportSource имеет новый case .file для трассировки источника импорта"
  artifacts:
    - path: "BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift"
      provides: "ImportSource.file case (4th case после .pasteboard / .qrCode / .deepLink)"
      contains: "case file"
    - path: "BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenView.swift"
      provides: ".fileImporter modifier и третья кнопка в addMenu"
      contains: ".fileImporter(isPresented:"
    - path: "BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift"
      provides: "importFromFile(_ url: URL) async public метод"
      exports: ["importFromFile"]
    - path: "BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/FileImporterTests.swift"
      provides: "Unit-тесты на UTType фильтрацию + .file case в ImportSource + masking"
  key_links:
    - from: "MainScreenView.addMenu"
      to: "MainScreenView.fileImporter"
      via: "showFileImporter @State Bool toggle"
      pattern: "showFileImporter\\s*=\\s*true"
    - from: "fileImporter callback"
      to: "viewModel.importFromFile(url:)"
      via: "URL после security-scoped read"
      pattern: "viewModel\\.importFromFile"
    - from: "importFromFile"
      to: "importer.importFromRawInput(_:source:)"
      via: ".file source case"
      pattern: "importFromRawInput.*source:\\s*\\.file"
---

<objective>
IMP-03 vertical slice: добавить пункт «Импортировать из файла» в существующее меню «+» на главном экране, поддержать `.json`/`.yaml`/`.yml` через SwiftUI `.fileImporter`, прочитать выбранный файл через security-scoped resource API, и передать содержимое в существующий `importFromRawInput` pipeline с новым `ImportSource.file` case.

Purpose: D-04 — file picker доступен только через меню «+» на главном экране (НЕ в Onboarding). Пользователи, у которых есть `.json` или `.yaml` конфигурация (Clash YAML / sing-box JSON), могут добавить её одним тапом вместо копирования содержимого в буфер. Это завершает IMP-03 (последний оставшийся пункт IMP-* для v1).

Output: Кнопка-меню с системным document picker, обработка security-scoped URL, error path через `lastError`, новый `.file` source case для analytics traceability, и 3 unit-теста.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-onboarding-ux-polish/11-CONTEXT.md
@.planning/phases/11-onboarding-ux-polish/11-RESEARCH.md
@.planning/phases/11-onboarding-ux-polish/11-PATTERNS.md
@.planning/phases/11-onboarding-ux-polish/11-01-PLAN.md

<interfaces>
<!-- Existing ImportSource enum (VPNCore/Sources/VPNCore/ParsedConfigs.swift ~line 289) -->
public enum ImportSource: Sendable, Equatable {
    case pasteboard
    case qrCode
    case deepLink
    // <— добавить: case file
}

<!-- Existing ConfigImporting protocol method (ConfigParser/ConfigImporting.swift) -->
func importFromRawInput(_ raw: String, source: ImportSource) async throws -> ImportResult

<!-- Existing MainScreenView addMenu (MainScreenView.swift ~lines 151-167) — нужно расширить третьим Button -->
private var addMenu: some View {
    Menu {
        Button { showQRScanner = true } label: { Label(L10n.menuScanQR, ...) }
        Button(action: viewModel.importFromPasteboard) { Label(L10n.menuImportFromClipboard, ...) }
        // <— добавить: Button { showFileImporter = true } label: { Label(L10n.menuImportFromFile, systemImage: "doc") }
    } label: {
        Image(systemName: "plus").font(.title3)
    }
    .accessibilityIdentifier("BBTB.AddButton")
}

<!-- Existing MainScreenViewModel.performImport (MainScreenViewModel.swift ~line 675) -->
private func performImport(_ source: ImportSource, raw: String?) async {
    importInProgress = true
    defer { importInProgress = false }
    lastError = nil
    do {
        let result: ImportResult
        switch source {
        case .qrCode where raw != nil:
            result = try await importer.importFromQRCode(raw!)
        default:
            result = try await importer.importFromPasteboard()
        }
        // ... existing handling
    } catch {
        lastError = error.localizedDescription
        // ...
    }
}

<!-- L10n ключи добавлены в Plan 01: -->
- L10n.menuImportFromFile
- L10n.importErrorFileAccessDenied
- L10n.importErrorFileReadFailed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Добавить ImportSource.file case + расширить performImport</name>
  <files>
    BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift,
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift
  </files>
  <read_first>
    BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift (только enum ImportSource — около line 289),
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift (методы importFromPasteboard, importFromQRString, performImport),
    BBTB/Packages/ConfigParser/Sources/ConfigParser/ConfigImporting.swift (protocol surface),
    .planning/phases/11-onboarding-ux-polish/11-PATTERNS.md (раздел S10 «Async + defer для security-scoped resource»)
  </read_first>
  <action>
    **Файл 1: `BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift`** — добавить `case file` к enum `ImportSource`. Расположить ПОСЛЕ существующего `case deepLink` (порядок: pasteboard, qrCode, deepLink, file). Не менять Equatable/Sendable conformance (синтезируется автоматически).

    **CRITICAL:** Поиск call-site exhaustiveness:
    После добавления нового case, выполнить `grep -RIn "switch.*[Ii]mportSource\|case .pasteboard\|case .qrCode\|case .deepLink" BBTB/Packages/ --include="*.swift"` и убедиться, что ВСЕ switch'и над `ImportSource`:
    - Либо имеют `default:` ветку (тогда `.file` попадёт под default — OK для performImport).
    - Либо exhaustively добавить `case .file:` ветку.

    Известные switch sites (по результатам RESEARCH grep'а): только `MainScreenViewModel.performImport` использует exhaustive switch с `case .qrCode where raw != nil` + `default`. Остальные usage'и — только pattern-matching на specific case'ы (.pasteboard в xcstrings header label). Если CI compile fails — пройтись по compiler errors и добавить case в каждый.

    **Файл 2: `BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift`** — добавить два публичных метода и расширить performImport:

    1. Расширить `performImport(_ source:, raw:)` switch — добавить ветку:
       ```
       case .file where raw != nil:
           result = try await importer.importFromRawInput(raw!, source: .file)
       ```
       Расположить ПЕРЕД `default:` (иначе никогда не сработает). НЕ менять остальные ветки.

    2. Добавить public method (после `importFromQRString(_ raw:)`):
       ```
       /// IMP-03 — async импорт содержимого файла, выбранного через .fileImporter.
       /// Caller (MainScreenView) ОБЯЗАН уже сделал startAccessingSecurityScopedResource()
       /// и сделает defer stop ПОСЛЕ того, как этот метод вернётся.
       public func importFromFile(rawContents: String) {
           Task { @MainActor in await performImport(.file, raw: rawContents) }
       }
       ```

    Не добавлять других методов. Не менять existing public API.

    **CONSTRAINTS:**
    - НЕ изменять signature `performImport(_ source: ImportSource, raw: String?)` — только добавить ветку в switch.
    - НЕ переиспользовать `.pasteboard` для file flow — нам нужен `.file` для аналитики (Phase 12 TELEM-04) и для error-path differentiation.
    - НЕ читать файл внутри ViewModel — это делает View (см. Task 2.2). VM только принимает строку с содержимым.
  </action>
  <verify>
    <automated>
      cd BBTB/Packages/AppFeatures && swift build 2>&1 | tail -20 && \
      cd BBTB/Packages/VPNCore && swift build 2>&1 | tail -10 && \
      grep -n "case file" BBTB/Packages/VPNCore/Sources/VPNCore/ParsedConfigs.swift && \
      grep -n "public func importFromFile" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift
    </automated>
  </verify>
  <acceptance_criteria>
    - `cd BBTB/Packages/VPNCore && swift build` exit 0.
    - `cd BBTB/Packages/AppFeatures && swift build` exit 0 (нет switch-exhaustiveness compile errors).
    - `grep -n "case file" ParsedConfigs.swift` возвращает ровно 1 строку внутри `enum ImportSource`.
    - `grep -n "public func importFromFile" MainScreenViewModel.swift` возвращает ровно 1 строку.
    - `grep -n "case .file" MainScreenViewModel.swift` возвращает ровно 1 строку (внутри performImport switch).
    - Существующие public API `importFromPasteboard()` и `importFromQRString(_:)` остаются с теми же signature'ами (grep verification).
  </acceptance_criteria>
  <done>
    `ImportSource.file` доступен как 4-й case. `MainScreenViewModel.importFromFile(rawContents:)` принимает string с содержимым файла и направляет в pipeline через `performImport(.file, raw:)`. Компиляция чистая, всё existing API сохранено.
  </done>
</task>

<task type="auto">
  <name>Task 2.2: UI-интеграция fileImporter в MainScreenView.addMenu + тесты</name>
  <files>
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenView.swift,
    BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/FileImporterTests.swift
  </files>
  <read_first>
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenView.swift (полностью — body + addMenu, особенно lines 114-167),
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/TopBar.swift (ОБЯЗАТЕЛЬНО прочитать — если addMenu живёт здесь, а не в MainScreenView, то fileImporter добавляется в TopBar.swift),
    BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/ConnectionTimerTests.swift (test pattern),
    .planning/phases/11-onboarding-ux-polish/11-PATTERNS.md (Pattern 2 «SwiftUI fileImporter» и раздел `MainScreenView.swift (MODIFY — host integration)»),
    .planning/phases/11-onboarding-ux-polish/11-RESEARCH.md (Pitfall 5 «iCloud-located file blocking read»)
  </read_first>
  <action>
    **Файл 1: `BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenView.swift`** — three additive changes:

    1. **Импорт:** в начале файла добавить `import UniformTypeIdentifiers` (после `import ServerListFeature`).

    2. **@State variable** — после `@State private var showQRScanner = false` (line 13) добавить:
       ```
       @State private var showFileImporter = false
       ```

    3. **addMenu расширение** — в `Menu { ... }` блоке (около line 152) ПОСЛЕ существующего `Button(action: viewModel.importFromPasteboard) { Label(L10n.menuImportFromClipboard, systemImage: "doc.on.clipboard") }` добавить третью кнопку:
       ```
       Button {
           showFileImporter = true
       } label: {
           Label(L10n.menuImportFromFile, systemImage: "doc")
       }
       ```

    4. **`.fileImporter` modifier** — добавить новый chain modifier к outer `VStack` сразу ПОСЛЕ существующего `.fullScreenCover/.sheet(QRScanner)` блока (после line 135 для iOS, после соответствующей macOS ветки). На обеих платформах одинаковый код (`.fileImporter` cross-platform). Логика callback:
       - На `.success(let urls)`: взять `urls.first`, вызвать `url.startAccessingSecurityScopedResource()`. Если false → `viewModel.lastError = L10n.importErrorFileAccessDenied; return`. Иначе `defer { url.stopAccessingSecurityScopedResource() }`. Прочитать `String(contentsOf: url, encoding: .utf8)` в `do/catch`; on success → `viewModel.importFromFile(rawContents: text)`; on catch → `viewModel.lastError = L10n.importErrorFileReadFailed`.
       - На `.failure(let error)`: `viewModel.lastError = error.localizedDescription`.

       Вся callback логика — обёрнута в `Task { ... }` (чтобы IO не блокировал main, см. RESEARCH Pitfall 5).

       Параметры modifier:
       - `isPresented: $showFileImporter`
       - `allowedContentTypes: [.json, UTType(filenameExtension: "yaml") ?? .data, UTType(filenameExtension: "yml") ?? .data]` (inline, без extension UTType — keep file simple)
       - `allowsMultipleSelection: false`

    5. **Accessibility identifier** — на новый Button добавить `.accessibilityIdentifier("BBTB.AddMenu.ImportFromFile")` (см. Pattern S7).

    **CONSTRAINTS:**
    - НЕ заменять existing `Menu { ... }` целиком — только append третью Button.
    - НЕ менять accessibility identifier на addMenu (`BBTB.AddButton`) — он используется в существующих тестах.
    - НЕ trying to add Onboarding integration в этом файле — это Plan 03.
    - НЕ inline-extension `UTType.yaml` — используем inline `UTType(filenameExtension: "yaml") ?? .data` (single-use, defensive nil-coalesce).
    - `.fileImporter` modifier применять КАК один блок на оба `#if os(iOS)` и `#elseif os(macOS)` ветки — modifier cross-platform с iOS 14+/macOS 11+, наш минимум выше.

    **Файл 2 (NEW): `BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/FileImporterTests.swift`** — XCTestCase с тремя тестами:
    1. `test_importSource_fileCase_exists` — `let s: ImportSource = .file; XCTAssertEqual(s, ImportSource.file)`. Smoke gate, ловит regression если кто-то удалит case.
    2. `test_importFromFile_routesToFileSource` — создать stub `ConfigImporting` (вариант: переиспользовать существующий stub из `ConfigImporterSubscriptionTests.swift` если он публичный, иначе inline) который записывает `(raw, source)` пары; инициализировать `MainScreenViewModel` с этим stub'ом; вызвать `vm.importFromFile(rawContents: "vless://test")`; через small await (`Task.yield()` × 3 или `for await _ in vm.$importInProgress.values.dropFirst().prefix(2)`) убедиться, что importer получил `(raw: "vless://test", source: .file)`. Если построение полного VM-стенда сложно — упрощённая версия: проверить только `ImportSource.file != .pasteboard && != .qrCode && != .deepLink`.
    3. `test_uttype_yaml_resolvable` — `XCTAssertNotNil(UTType(filenameExtension: "yaml")); XCTAssertNotNil(UTType(filenameExtension: "yml"))`. Проверяет, что наш inline UTType lookup не вернёт nil на test runtime.

    Test target — `MainScreenFeatureTests` (existing — в Package.swift testTarget). Import: `@testable import MainScreenFeature`, `import VPNCore` (для ImportSource), `import UniformTypeIdentifiers`.
  </action>
  <verify>
    <automated>
      cd BBTB/Packages/AppFeatures && swift test --filter FileImporterTests 2>&1 | tail -20 && \
      grep -c "import UniformTypeIdentifiers\|showFileImporter\|\.fileImporter\|L10n\.menuImportFromFile" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenView.swift
    </automated>
  </verify>
  <acceptance_criteria>
    - `swift test --filter FileImporterTests` PASS (минимум 2 из 3 тестов — третий test_importFromFile_routesToFileSource может быть downgraded до smoke если VM init слишком complex).
    - `grep -c` на 4 sentinel'ах в `MainScreenView.swift` возвращает ≥4 (`import UniformTypeIdentifiers`, `showFileImporter`, `.fileImporter`, `L10n.menuImportFromFile`).
    - `swift build` для AppFeatures зелёный.
    - `cd BBTB/Packages/AppFeatures && swift test` (full suite) зелёный — никаких regressions в существующих 143/143 тестах.
    - Существующий identifier `BBTB.ConnectionButton` и `BBTB.AddButton` сохранены (grep).
    - `xcodebuild -workspace BBTB.xcworkspace -scheme BBTB -destination 'generic/platform=iOS Simulator' -quiet build 2>&1 | tail -5` — exit 0 (требует Tuist generate — выполнить если xcworkspace отсутствует: `cd BBTB && tuist generate --no-open`).
  </acceptance_criteria>
  <done>
    Меню «+» содержит третью кнопку «Импортировать из файла»; `.fileImporter` modifier открывает системный picker; security-scoped resource обрабатывается с try/defer; ошибки доступа/чтения через `lastError → .alert`; new ImportSource.file traces file flow в pipeline. iOS + macOS xcodebuild зелёный.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → app | user-selected file URL (потенциально malicious содержимое) |
| App sandbox → external file system | `fileImporter` обходит sandbox via security-scoped resource permission |
| iCloud Drive → local | downloaded file читается синхронно `String(contentsOf:)` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | malicious .yaml file → injection в Clash YAML parser | mitigate | Существующий `UniversalImportParser` уже валидирует через ConfigParser pipeline (Phase 4 verified); IMP-03 НЕ создаёт новый parser path — переиспользует `importFromRawInput`. Defense-in-depth: Phase 4 R1 invariant test (`test_nonHy2_outbounds_neverHaveInsecureTrue`) применяется к любому источнику. |
| T-11-02-02 | Information Disclosure | Path traversal через выбранный URL | mitigate | Apple-managed: `fileImporter` возвращает только user-selected URLs, security-scoped permission system enforced ОС; нет access к paths не разрешённым пользователем. ASVS V4 covered. |
| T-11-02-03 | DoS | huge .json/.yaml (100+ MB) → memory exhaustion | accept | Realistic config files <1MB; iOS будет killer process на memory pressure. Phase 12 может добавить size cap если потребуется. |
| T-11-02-04 | DoS | iCloud Drive file download блокирует main thread | mitigate | Чтение завёрнуто в `Task { ... }` — выполняется на cooperative pool, main thread свободен. Long-download UX: пользователь видит import spinner (`importInProgress`) — это acceptable. |
| T-11-02-05 | Privilege Escalation | пропуск startAccessingSecurityScopedResource() → read fails | mitigate | Task 2.2 enforces try/defer pattern; acceptance_criteria включает grep на `startAccessingSecurityScopedResource`. |
| T-11-02-06 | Repudiation | Импорт через файл не отличается от pasteboard в логах | mitigate | `.file` ImportSource case записывается в analytics (Phase 12) и в logger при performImport entry. |

</threat_model>

<verification>
- AppFeatures + VPNCore swift build green.
- Full AppFeatures swift test green (143/143 + 3 new tests).
- iOS + macOS xcodebuild green: `cd BBTB && tuist generate --no-open && xcodebuild -workspace BBTB.xcworkspace -scheme BBTB -destination 'generic/platform=iOS Simulator' -quiet build && xcodebuild -workspace BBTB.xcworkspace -scheme BBTB-macOS -quiet build`.
- Manual smoke (Phase 11 UAT) — выбрать `.json` и `.yaml` тестовые конфиги через picker → импорт успешен → серверы появляются в pool.
- Grep `case .file:` или `case file` в `ParsedConfigs.swift` = 1.
</verification>

<success_criteria>
IMP-03 ✅ Validated: Импорт через файл (`.json`/`.yaml`/`.yml`) работает через меню «+». Pipeline идентичен pasteboard/QR (через `importFromRawInput`). Security-scoped resource correctly handled (Apple-mandated). Error path consistent через `lastError → .alert`. Никаких regression на existing tests.
</success_criteria>

<output>
После completion создать `.planning/phases/11-onboarding-ux-polish/11-02-SUMMARY.md` с: enum diff (ImportSource case file), MainScreenView add-locations с line numbers, тестовая стратегия (что покрывают 3 new тесты, что — manual UAT), статус iOS + macOS xcodebuild.
</output>
