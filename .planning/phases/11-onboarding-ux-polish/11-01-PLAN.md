---
phase: 11-onboarding-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BBTB/Packages/Localization/Sources/Localization/L10n.swift
  - BBTB/Packages/Localization/Sources/Localization/Resources/Localizable.xcstrings
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift
  - BBTB/Packages/AppFeatures/Sources/ServerListFeature/TransportPicker.swift
  - BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/TransportPickerLabelsTests.swift
autonomous: true
requirements:
  - LOC-02
user_setup: []

must_haves:
  truths:
    - "ConfigImporter не содержит hardcoded русских строк (line 42 и line 984 переведены на L10n keys)"
    - "TransportPicker все пять protocol labels (TCP/WebSocket/gRPC/HTTP/2/HTTPUpgrade) идут через L10n keys"
    - "L10n.swift содержит все ~30 ключей для Phase 11 (onboarding, help, diagnostics, import-file, transport, subscription)"
    - "Localizable.xcstrings содержит ru/en переводы для всех новых ключей"
    - "grep по 'русским буквам в Text/строках' в shipping packages = 0"
    - "AppFeatures swift test green throughout (≥143/143)"
  artifacts:
    - path: "BBTB/Packages/Localization/Sources/Localization/L10n.swift"
      provides: "Type-safe accessors для всех Phase 11 L10n ключей"
      contains: "onboardingTitle, helpFaq1Question, diagnosticsSection, menuImportFromFile, transportLabelTcp, subscriptionFallbackName"
    - path: "BBTB/Packages/Localization/Sources/Localization/Resources/Localizable.xcstrings"
      provides: "ru/en переводы Phase 11 strings"
      contains: "onboarding.title, help.faq1.question, diagnostics.section, transport.label_tcp"
    - path: "BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift"
      provides: "ImporterError.noSupportedServers и subscription fallback name через L10n"
    - path: "BBTB/Packages/AppFeatures/Sources/ServerListFeature/TransportPicker.swift"
      provides: "Все 5 transport tags через L10n"
    - path: "BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/TransportPickerLabelsTests.swift"
      provides: "Unit-тест на то что TransportPicker labels не содержат hardcoded строк"
  key_links:
    - from: "ConfigImporter.swift line 42"
      to: "L10n.importErrorNoSupportedConfigs"
      via: "уже-существующий L10n key"
      pattern: "case .noSupportedServers: return L10n.importErrorNoSupportedConfigs"
    - from: "ConfigImporter.swift line ~984"
      to: "L10n.subscriptionFallbackName"
      via: "новый L10n key"
      pattern: "?? L10n.subscriptionFallbackName"
    - from: "TransportPicker.swift body"
      to: "L10n.transportLabel*"
      via: "Text(L10n.transportLabelXxx)"
      pattern: "Text\\(L10n\\.transportLabel"
---

<objective>
Wave 1 foundation: добавить ~30 новых L10n ключей в Localization пакет (Onboarding, Help/FAQ, Diagnostics, file import, transport labels, subscription fallback), реализовать ru/en переводы в xcstrings, и закрыть LOC-02 cleanup для двух hardcoded строк в ConfigImporter и пяти protocol-label строк в TransportPicker.

Purpose: Это foundation-wave. Все остальные планы Phase 11 (Wave 2+) обращаются к L10n keys, добавляемым здесь. Если ключи существуют, downstream-планы пишут UI без блокировки. Один общий L10n-commit вместо распыления ключей по 5+ feature-планам.

Output: Все Phase 11 L10n ключи (с placeholder ru/en значениями для UI-полировки в Wave 4), два cleanup-фикса hardcoded строк, и один unit-тест на TransportPicker labels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-onboarding-ux-polish/11-CONTEXT.md
@.planning/phases/11-onboarding-ux-polish/11-RESEARCH.md
@.planning/phases/11-onboarding-ux-polish/11-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing L10n pattern (BBTB/Packages/Localization/Sources/Localization/L10n.swift) -->

Launch-critical keys используют `static let = tr("key")` (eager, на первой кадре).
Non-launch keys используют `static var key: String { tr("key") }` (lazy).

Существующие relevant ключи (НЕ дублировать):
- L10n.importErrorNoSupportedConfigs — уже есть (line 88) — ConfigImporter line 42 fix
- L10n.actionImportFromClipboard, L10n.actionScanQR — для Onboarding CTA fallback
- L10n.serverDetailTransport, L10n.serverDetailTransportAuto — TransportPicker уже использует

`tr()` хелпер:
private static func tr(_ key: String) -> String {
    NSLocalizedString(key, bundle: Bundle.module, comment: "")
}

<!-- xcstrings entry format (BBTB/Packages/Localization/Sources/Localization/Resources/Localizable.xcstrings) -->

JSON-структура каждого ключа:
"onboarding.title": {
  "localizations": {
    "en": { "stringUnit": { "state": "translated", "value": "Bring back the bug" } },
    "ru": { "stringUnit": { "state": "translated", "value": "Верни жука" } }
  }
}

Файл UTF-8 JSON; редактируется как обычный JSON — Xcode потом нормализует на следующем open.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Добавить все Phase 11 L10n ключи в L10n.swift и xcstrings</name>
  <files>
    BBTB/Packages/Localization/Sources/Localization/L10n.swift,
    BBTB/Packages/Localization/Sources/Localization/Resources/Localizable.xcstrings
  </files>
  <read_first>
    BBTB/Packages/Localization/Sources/Localization/L10n.swift,
    BBTB/Packages/Localization/Sources/Localization/Resources/Localizable.xcstrings,
    .planning/phases/11-onboarding-ux-polish/11-PATTERNS.md (раздел «L10n.swift + Localizable.xcstrings (MODIFY)»),
    .planning/phases/11-onboarding-ux-polish/11-CONTEXT.md (D-09, D-10, D-11)
  </read_first>
  <action>
    Расширить L10n.swift секцию «Non-launch (static var)» новыми ключами. Использовать паттерн `public static var camelName: String { tr("dot.notation.key") }` (lazy `static var`, НЕ `static let` — это не launch-critical).

    Минимально обязательный набор ключей (точные id ниже — НЕ переименовывать; они захардкожены в downstream планах):

    Onboarding (5):
    - `onboardingTitle` ← `onboarding.title`
    - `onboardingSubtitle` ← `onboarding.subtitle`
    - `onboardingPaste` ← `onboarding.cta_paste`
    - `onboardingScanQR` ← `onboarding.cta_qr`
    - `onboardingAccessibilityHint` ← `onboarding.a11y_hint` (для VoiceOver на CTA)

    Help / FAQ (12):
    - `helpTitle` ← `help.title`
    - `helpEntryLabel` ← `help.entry.label` (строка в SettingsView)
    - `helpFooter` ← `help.footer`
    - `helpFaq1Question` / `helpFaq1Answer` — «Как добавить сервер?»
    - `helpFaq2Question` / `helpFaq2Answer` — «Что делать если не подключается?»
    - `helpFaq3Question` / `helpFaq3Answer` — «WebRTC (веб-RTC утечка) — что это?»
    - `helpFaq4Question` / `helpFaq4Answer` — «Приложения которые видят VPN (22 приложения из РФ)» (LOC-04 обязательно)
    - `helpFaq5Question` / `helpFaq5Answer` — «Известные ограничения детектирования»

    Diagnostics (8):
    - `diagnosticsSection` ← `diagnostics.section` (header «Диагностика»)
    - `diagnosticsExportLog` ← `diagnostics.export_log` (button «Подготовить лог»)
    - `diagnosticsShareLog` ← `diagnostics.share_log` (ShareLink label «Отправить лог разработчику»)
    - `diagnosticsLast24h` ← `diagnostics.last_24h` (footer строка)
    - `diagnosticsPreparing` ← `diagnostics.preparing` (label пока идёт сбор)
    - `diagnosticsNoLogsTitle` ← `diagnostics.no_logs.title` (alert)
    - `diagnosticsNoLogsMessage` ← `diagnostics.no_logs.message`
    - `diagnosticsVersionFormat` ← `diagnostics.version_format` (формат `v%@ (%@)` с двумя `%@` для app version и OS version; использовать `tr(_:_:)` варианта с CVarArg)

    Import — file picker (3):
    - `menuImportFromFile` ← `menu.import_from_file`
    - `importErrorFileAccessDenied` ← `import.error.file_access_denied`
    - `importErrorFileReadFailed` ← `import.error.file_read_failed`

    Transport labels (5) — для LOC-02 cleanup TransportPicker:
    - `transportLabelTcp` ← `transport.label_tcp`
    - `transportLabelWebSocket` ← `transport.label_websocket`
    - `transportLabelGrpc` ← `transport.label_grpc`
    - `transportLabelHttp2` ← `transport.label_http2`
    - `transportLabelHttpUpgrade` ← `transport.label_http_upgrade`

    Subscription fallback (1) — для ConfigImporter line ~984:
    - `subscriptionFallbackName` ← `subscription.fallback_name`

    **xcstrings:** для каждого нового ключа добавить блок ru+en в Localizable.xcstrings.

    Значения (рекомендованные, planner-discretion):
    - `onboarding.title`: ru «Верни жука» / en «Bring back the bug»
    - `onboarding.subtitle`: ru «Один тап для свободного интернета без ТСПУ» / en «One-tap freedom from Russian DPI»
    - `onboarding.cta_paste`: ru «Вставить из буфера» / en «Paste from clipboard»
    - `onboarding.cta_qr`: ru «Сканировать QR» / en «Scan QR code»
    - `help.title`: ru «Помощь» / en «Help»
    - `help.entry.label`: ru «Помощь» / en «Help»
    - `diagnostics.section`: ru «Диагностика» / en «Diagnostics»
    - `diagnostics.export_log`: ru «Подготовить лог» / en «Prepare log»
    - `diagnostics.share_log`: ru «Отправить лог разработчику» / en «Send log to developer»
    - `diagnostics.last_24h`: ru «Последние 24 часа. IP-адреса маскируются.» / en «Last 24 hours. IP addresses are masked.»
    - `diagnostics.version_format`: ru `v%@ (%@)` / en `v%@ (%@)` (positional %@ для app version и OS version)
    - `diagnostics.no_logs.title`: ru «Нет данных» / en «No data»
    - `diagnostics.no_logs.message`: ru «Подключитесь к VPN хотя бы раз, чтобы появился лог.» / en «Connect to VPN at least once to generate a log.»
    - `menu.import_from_file`: ru «Импортировать из файла» / en «Import from file»
    - `import.error.file_access_denied`: ru «Не удалось получить доступ к файлу.» / en «Could not access file.»
    - `import.error.file_read_failed`: ru «Не удалось прочитать файл.» / en «Could not read file.»
    - `transport.label_tcp`: ru `TCP` / en `TCP` (нерасшифровываемый аббревиатура)
    - `transport.label_websocket`: ru `WebSocket` / en `WebSocket`
    - `transport.label_grpc`: ru `gRPC` / en `gRPC`
    - `transport.label_http2`: ru `HTTP/2` / en `HTTP/2`
    - `transport.label_http_upgrade`: ru `HTTPUpgrade` / en `HTTPUpgrade`
    - `subscription.fallback_name`: ru «Подписка» / en «Subscription»

    Содержательные FAQ-ответы (3 коротких параграфа в каждом, ru+en) — planner-discretion с soft-cap 600 символов на ответ. Темы зафиксированы в `11-CONTEXT.md` D-09 и `11-RESEARCH.md` §LOC-03/04. Обязательно: FAQ4 (22 приложения) ДОЛЖЕН содержать ссылку «см. wiki/vpn-detection-by-apps.md» внутри ru-варианта.

    **CRITICAL constraints:**
    - НЕ переименовывать ни одно из существующих имён `static var`/`static let`. Только add.
    - НЕ использовать `static let` для новых ключей — все non-launch-critical (`static var`).
    - В xcstrings все 30 новых ключей в state="translated".
    - Не вставлять fenced code blocks в этот action — только описание.
    - Сохранить файл xcstrings как valid JSON (после правки прогнать `python3 -c "import json; json.load(open('...'))"` или `jq empty < ...xcstrings`).
  </action>
  <verify>
    <automated>
      cd BBTB/Packages/Localization && swift build && \
      jq empty Sources/Localization/Resources/Localizable.xcstrings && \
      grep -c "onboardingTitle\|helpFaq1Question\|diagnosticsSection\|menuImportFromFile\|transportLabelTcp\|subscriptionFallbackName" Sources/Localization/L10n.swift
    </automated>
  </verify>
  <acceptance_criteria>
    - `swift build` зелёный для Localization пакета.
    - `jq empty` на xcstrings возвращает exit 0 (valid JSON).
    - `grep -c` на 6 ключевых identifier'ах вернёт ≥6 (каждый встречается минимум один раз).
    - Файл `L10n.swift` содержит как минимум 27 новых строк с `public static var` (5 onboarding + 12 help + 8 diagnostics + 3 import + 5 transport + 1 subscription — суммарно 34, минимум 27 для прохождения).
    - Файл `Localizable.xcstrings` содержит как минимум 27 новых top-level ключей с обоими `en` и `ru` блоками в `localizations`.
    - НИ ОДИН существующий ключ не удалён — `grep -c "appDisplayName\|statusEmpty\|menuScanQR\|settingsKillSwitchFooter" L10n.swift` возвращает то же число что и до правки.
  </acceptance_criteria>
  <done>
    Все Phase 11 L10n ключи существуют в обоих файлах (L10n.swift accessor + xcstrings entry) с ru+en переводами; `swift build` зелёный; downstream-планы могут безопасно ссылаться на `L10n.onboardingTitle` etc без missing-symbol ошибки.
  </done>
</task>

<task type="auto">
  <name>Task 1.2: LOC-02 cleanup — ConfigImporter + TransportPicker + lint-тест</name>
  <files>
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift,
    BBTB/Packages/AppFeatures/Sources/ServerListFeature/TransportPicker.swift,
    BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/TransportPickerLabelsTests.swift
  </files>
  <read_first>
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift,
    BBTB/Packages/AppFeatures/Sources/ServerListFeature/TransportPicker.swift,
    BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/ConnectionTimerTests.swift (как pattern для test),
    .planning/phases/11-onboarding-ux-polish/11-PATTERNS.md (разделы «ConfigImporter.swift lines 42, 984» и «TransportPicker.swift lines 79-83»),
    .planning/phases/11-onboarding-ux-polish/11-RESEARCH.md (Pitfall 6 «Localization key naming collision»)
  </read_first>
  <action>
    **Файл 1: `BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift`** — два точечных замены:
    1. `case .noSupportedServers: return "В источнике нет поддерживаемых конфигураций."` → `case .noSupportedServers: return L10n.importErrorNoSupportedConfigs` (ключ УЖЕ существует, не дублировать).
    2. Строка `let derived = sanitized ?? (URL(string: url)?.host) ?? "Подписка"` (примерно line 984) → `let derived = sanitized ?? (URL(string: url)?.host) ?? L10n.subscriptionFallbackName` (новый ключ из Task 1.1).

    Не менять ничего другого в файле — только два literal-replace.

    **Файл 2: `BBTB/Packages/AppFeatures/Sources/ServerListFeature/TransportPicker.swift`** body — заменить пять `Text("...")` literal'ов на `Text(L10n.transportLabelXxx)`:
    - `Text("TCP")` → `Text(L10n.transportLabelTcp)`
    - `Text("WebSocket")` → `Text(L10n.transportLabelWebSocket)`
    - `Text("gRPC")` → `Text(L10n.transportLabelGrpc)`
    - `Text("HTTP/2")` → `Text(L10n.transportLabelHttp2)`
    - `Text("HTTPUpgrade")` → `Text(L10n.transportLabelHttpUpgrade)`
    Сохранить `.tag(TransportSelection.xxx)` неизменными.

    **Файл 3 (NEW): `BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/TransportPickerLabelsTests.swift`** — единичный XCTestCase класс `TransportPickerLabelsTests: XCTestCase` с двумя тестами:
    - `test_transportLabels_resolveViaL10n` — для каждого из 5 идентификаторов (`L10n.transportLabelTcp`, `transportLabelWebSocket`, `transportLabelGrpc`, `transportLabelHttp2`, `transportLabelHttpUpgrade`) проверить `XCTAssertFalse(string.isEmpty)` и `XCTAssertFalse(string.contains("transport.label_"))` (последнее гарантирует, что ключ резолвится, а не возвращается raw fallback).
    - `test_transportLabels_areUnique` — собрать пять значений в `Set<String>`, проверить `XCTAssertEqual(set.count, 5)`.

    Тест-файл должен `@testable import ServerListFeature` и `import Localization` (см. pattern в SettingsViewModelTests).

    **CRITICAL:**
    - НЕ менять enum cases TransportSelection (`.tcp`, `.ws`, `.grpc`, `.http`, `.httpUpgrade`) — только display labels.
    - НЕ удалять/менять existing `Text(L10n.serverDetailTransportAuto)` для случая `.auto` (уже через L10n).
    - НЕ touch accessibility identifier `BBTB.ServerDetail.TransportPicker`.
    - Action: per LOC-02 «никаких hardcoded English strings» — даже identical strings типа "TCP" идут через L10n для consistency, как зафиксировано в 11-RESEARCH «Don't Hand-Roll» таблице.
  </action>
  <verify>
    <automated>
      cd BBTB/Packages/AppFeatures && swift test --filter TransportPickerLabelsTests 2>&1 | tail -20 && \
      grep -nE '"[А-Яа-яЁё]' BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift | grep -vE '//|/\*' | grep -v 'L10n\.' | wc -l && \
      grep -nE '^\s*Text\("[A-Z]' BBTB/Packages/AppFeatures/Sources/ServerListFeature/TransportPicker.swift | wc -l
    </automated>
  </verify>
  <acceptance_criteria>
    - `swift test --filter TransportPickerLabelsTests` PASS (2 теста).
    - `grep -nE '"[А-Яа-яЁё]' ConfigImporter.swift | grep -vE '//|/\*' | grep -v 'L10n\.'` возвращает **0** (нет hardcoded русских в non-comment коде; L10n-обёрнутые отфильтровываются).
    - `grep -nE '^\s*Text\("[A-Z]' TransportPicker.swift` возвращает **0** (нет `Text("TCP")` и аналогов в исходнике).
    - Full AppFeatures test suite зелёный: `cd BBTB/Packages/AppFeatures && swift test` exit code 0.
    - Identifier `BBTB.ServerDetail.TransportPicker` присутствует в TransportPicker.swift (не удалён).
  </acceptance_criteria>
  <done>
    Все hardcoded русские строки в ConfigImporter.swift line 42 + line ~984 переведены на L10n keys. Все пять transport labels в TransportPicker через L10n. Lint-тест `TransportPickerLabelsTests` гарантирует невозможность регрессии. AppFeatures swift test зелёный.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Code → user-visible UI | L10n strings отображаются пользователю; injection-vector если ключ резолвится в untrusted bundle |
| Resource bundle → app process | xcstrings JSON parsed at runtime; malformed file = crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | xcstrings JSON resource | accept | Bundle.module resource подписан вместе с app; tampering требует jailbreak → out of scope per Phase 1 R6 model |
| T-11-01-02 | Information Disclosure | L10n keys в logs | mitigate | L10n values — static UI text, no PII; не логируются |
| T-11-01-03 | Denial of Service | malformed xcstrings → crash на launch | mitigate | Task 1.1 verify `jq empty` гарантирует valid JSON перед commit; CI рендерит xcstrings в .stringsdict через xcassetcatalog → compile fails при malformed |
| T-11-01-04 | Spoofing | L10n-injection через subscription Title | accept | Уже mitigated Phase 3 (`sanitizeSubscriptionName` strips control chars + 100-char clamp); Phase 11 ключи — compile-time, нет dynamic injection |
| T-11-01-05 | Information Disclosure | L10n FAQ4 контент про 22 приложения раскрывает known detection apps | accept | Информация уже public в wiki/vpn-detection-by-apps.md; FAQ просто surfaces. Не security risk — это user education. |

</threat_model>

<verification>
- AppFeatures swift test green: `cd BBTB/Packages/AppFeatures && swift test`
- Localization swift build green: `cd BBTB/Packages/Localization && swift build`
- xcstrings valid JSON: `jq empty BBTB/Packages/Localization/Sources/Localization/Resources/Localizable.xcstrings`
- Grep gate: hardcoded Russian strings в ConfigImporter и TransportPicker = 0 (см. acceptance_criteria Task 1.2).
- Все 6 sentinel identifier'ов резолвятся через grep в L10n.swift (Task 1.1 verify).
</verification>

<success_criteria>
LOC-02 ✅ (ConfigImporter + TransportPicker hardcoded strings cleared). L10n foundation для Wave 2-7 ready: 30 ключей доступны через `L10n.onboardingTitle`, `L10n.helpFaq1Question`, `L10n.diagnosticsSection`, etc. Lint-тест `TransportPickerLabelsTests` зафиксирован как regression-guard. AppFeatures swift test зелёный, нет regressions.
</success_criteria>

<output>
После completion создать `.planning/phases/11-onboarding-ux-polish/11-01-SUMMARY.md` с: список добавленных L10n ключей (по группам), точные line-replace diffs ConfigImporter + TransportPicker, статус swift test, и `grep -c` подтверждения cleanup.
</output>
