---
phase: 11-onboarding-ux-polish
plan: 07
type: execute
wave: 4
depends_on: [11-02-PLAN.md, 11-03-PLAN.md, 11-05-PLAN.md, 11-06-PLAN.md]
files_modified:
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift
  - BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift
  - BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/ConnectionButtonTests.swift
  - BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/ServerListSheetHeightTests.swift
autonomous: false
requirements:
  - UX-08
  - UX-09
user_setup: []

must_haves:
  truths:
    - "ConnectionButton при ConnectionState.connecting показывает spinner overlay поверх power-icon"
    - "ConnectionButton для остальных state'ов сохраняет существующий symbolEffect(.bounce) поведение"
    - "Identifier BBTB.ConnectionButton сохранён (не сломано Phase 6 UAT tests)"
    - "ServerListSheet height константы (serverRowH/autoCellH/subHeaderH/manHeaderH/emptyCardH) обновлены по Figma-значениям (либо placeholder values + Figma TODO note)"
    - "ServerListSheet computeDetents helper остаётся testable; new height тесты PASS"
    - "OnboardingView pixel-perfect implementation по Figma (если макеты переданы) — Spacer/typography/colors"
    - "Human verification checkpoint: Figma compliance check"
  artifacts:
    - path: "BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift"
      provides: "overlay spinner для connecting state"
      contains: "ProgressView"
    - path: "BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift"
      provides: "Updated height constants per Figma (или placeholder + TODO)"
    - path: "BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/ConnectionButtonTests.swift"
      provides: "Тесты на ConnectionState → spinner overlay visibility"
      min_lines: 35
    - path: "BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/ServerListSheetHeightTests.swift"
      provides: "Тесты на computeDetents для 3 сценариев (8 серверов / 1 сервер / empty)"
      min_lines: 35
  key_links:
    - from: "ConnectionButton ZStack body"
      to: "ProgressView (in overlay or alternative branch)"
      via: "if isConnecting clause"
      pattern: "ProgressView\\(\\)"
    - from: "ServerListSheet height static let"
      to: "computeDetents() pure helper"
      via: "estimatedHeight() sum"
      pattern: "estimatedHeight"
---

<objective>
UX-08 + UX-09 + D-08 vertical slice: добавить spinner overlay в ConnectionButton для `.connecting` состояния (placeholder ProgressView или Figma-точная реализация), обновить ServerListSheet height константы по Figma (D-08), полировать Onboarding/Help/Settings экраны по Figma-макетам (UX-09 visual review). Финализирующий UI-wave Phase 11.

Purpose: D-05/D-06 — spinner при connecting определяется по Figma; точный type рисуется параллельно (Stream B per D-07). D-08 — height константы критичны для presentationDetents расчёта; если Figma не совпадает с текущим — sheet open'ается на неверной высоте. UX-09 — финальный pixel-perfect review всех Phase 11 экранов.

**Mode:** содержит human-verify checkpoint после спиннер-имплементации.

Output: Updated ConnectionButton с spinner overlay (изолированный change, identifier preserved), updated ServerListSheet height constants, обновлённый OnboardingView по Figma, и 2 unit-тестa (button spinner visibility + height detents).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-onboarding-ux-polish/11-CONTEXT.md
@.planning/phases/11-onboarding-ux-polish/11-RESEARCH.md
@.planning/phases/11-onboarding-ux-polish/11-PATTERNS.md
@.planning/phases/11-onboarding-ux-polish/11-FIGMA-SPEC.md

<interfaces>
<!-- Existing ConnectionButton body (BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift) -->

public struct ConnectionButton: View {
    public let state: ConnectionState
    public let action: () -> Void

    public var body: some View {
        Button(action: action) {
            ZStack {
                Circle().fill(fillColor).frame(width: diameter, height: diameter)
                Image(systemName: "power")
                    .font(.system(size: iconSize, weight: .medium))
                    .foregroundStyle(.white)
                    .symbolEffect(.bounce, value: state)
            }
        }
        .buttonStyle(.plain)
        .disabled(disabled)
        .accessibilityIdentifier("BBTB.ConnectionButton")  // CRITICAL: preserve
    }
}

<!-- ServerListSheet existing static let block (lines 45-51) — D-08 update targets -->
private static let headerH:     CGFloat = 81
private static let autoCellH:   CGFloat = 116
private static let subHeaderH:  CGFloat = 44
private static let manHeaderH:  CGFloat = 36
private static let serverRowH:  CGFloat = 80
private static let emptyCardH:  CGFloat = 220
private static let bottomBuf:   CGFloat = 40

<!-- ServerListSheet computeDetents pure helper уже testable (lines 69-77) -->
static func computeDetents(sections: [ServerListSection]) -> Set<PresentationDetent>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 7.1: Spinner overlay в ConnectionButton + unit-тесты</name>
  <files>
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift,
    BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/ConnectionButtonTests.swift
  </files>
  <read_first>
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift (полностью — все 68 строк),
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionState.swift или MainScreenViewModel.swift (enum ConnectionState),
    .planning/phases/11-onboarding-ux-polish/11-PATTERNS.md (раздел `ConnectionButton.swift (MODIFY)` + Anti-Pattern «НЕ переписывать ZStack целиком»),
    .planning/phases/11-onboarding-ux-polish/11-RESEARCH.md (Pattern 11 «ProgressView default circular»),
    .planning/phases/11-onboarding-ux-polish/11-FIGMA-SPEC.md (§2 Connection Button states)
  </read_first>
  <action>
    **Файл 1: `BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift`** — minimal additive changes:

    1. Внутри `ZStack { ... }` body (после строки `.symbolEffect(.bounce, value: state)`) добавить:
       - На существующий `Image(systemName: "power")` добавить modifier `.opacity(isConnecting ? 0 : 1)` — скрывает power-icon во время connecting (placeholder вариант; если Figma скажет «оставить icon видимой» — этот modifier удалить в Figma-revision below).
       - ДОБАВИТЬ branch внутри ZStack ПОСЛЕ Image:
         ```
         if isConnecting {
             ProgressView()
                 .progressViewStyle(.circular)
                 .tint(.white)
                 .controlSize(.large)
                 .accessibilityHidden(true)
         }
         ```

    2. Добавить private computed property:
       ```
       private var isConnecting: Bool {
           if case .connecting = state { return true }
           return false
       }
       ```

    3. **Sanity preserve:** идентификатор `BBTB.ConnectionButton`, `disabled` поведение, `fillColor` switch, `diameter`/`iconSize` помогающие, init signature — НЕ ТРОГАТЬ.

    **Figma-Revision (если макеты переданы — see Task 7.4 checkpoint):**
    Если Figma говорит «rotating ring вокруг кнопки» вместо overlay:
    - Заменить `ProgressView().progressViewStyle(.circular)` на `Circle().trim(from: 0, to: 0.7).stroke(.white, lineWidth: ...)` с `.rotationEffect(.degrees(rotationAngle)).animation(...repeatForever..., value: rotationAngle)`.
    - Точный stroke width, animation duration, color — из Figma-spec.

    Если Figma говорит «pulse» — другой подход.

    Phase 11 Wave 4: placeholder ProgressView OK; precise visual implementation идёт после Figma checkpoint (Task 7.4).

    **CRITICAL constraints:**
    - НЕ заменять `ZStack { Circle + Image }` на новую структуру — это сломает A11Y identifier и UI snapshot тесты (RESEARCH Anti-Pattern explicit).
    - НЕ менять signature `init(state:action:)`.
    - НЕ trying to add spinner для `.error` или other states — D-05 только `.connecting`.

    **Файл 2 (NEW): `BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/ConnectionButtonTests.swift`** — unit-тесты на pure helper `isConnecting`:

    Так как `isConnecting` private, тесты должны работать через behaviour observable through ConnectionState. Подход — pure-function test:

    Альтернатива A (preferred): сделать `isConnecting` `internal` для @testable visibility — изменить `private` → `internal` в ConnectionButton. Тогда tests могут вызвать напрямую.

    Альтернатива B: extract helper outside ConnectionButton: `internal func isConnectingState(_ state: ConnectionState) -> Bool` (free function в same file). Тесты вызывают функцию.

    Реализовать **Alternative A** — простейший diff: один modifier `private` → `internal`.

    Tests:
    ```
    import XCTest
    @testable import MainScreenFeature

    final class ConnectionButtonTests: XCTestCase {
        func test_isConnecting_trueWhenStateConnecting() {
            let button = ConnectionButton(state: .connecting, action: {})
            XCTAssertTrue(button.isConnecting)
        }

        func test_isConnecting_falseForIdle() {
            let button = ConnectionButton(state: .idle, action: {})
            XCTAssertFalse(button.isConnecting)
        }

        func test_isConnecting_falseForConnected() {
            let button = ConnectionButton(state: .connected(since: Date()), action: {})
            XCTAssertFalse(button.isConnecting)
        }

        func test_isConnecting_falseForError() {
            let button = ConnectionButton(state: .error(message: "test"), action: {})
            XCTAssertFalse(button.isConnecting)
        }

        func test_isConnecting_falseForEmpty() {
            let button = ConnectionButton(state: .empty, action: {})
            XCTAssertFalse(button.isConnecting)
        }
    }
    ```

    **TEST CONSTRAINTS:**
    - НЕ trying snapshot — нет ViewInspector.
    - НЕ trying ARC retain-cycle test — out of scope.
    - НЕ checking accessibilityIdentifier — это compile-time string literal в View; regression caught manual UAT.
  </action>
  <verify>
    <automated>
      cd BBTB/Packages/AppFeatures && swift test --filter ConnectionButtonTests 2>&1 | tail -15 && \
      grep -c "ProgressView\|isConnecting\|BBTB.ConnectionButton" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift && \
      grep -c 'ZStack {' BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift
    </automated>
  </verify>
  <acceptance_criteria>
    - `swift test --filter ConnectionButtonTests` PASS (5 тестов).
    - `grep -c` на 3 sentinel'ах ConnectionButton.swift возвращает ≥3.
    - `grep -c "ZStack {"` возвращает 1 (структура preserved).
    - `grep -c "BBTB.ConnectionButton"` возвращает 1 (identifier preserved).
    - Full AppFeatures swift test зелёный.
    - `grep -c "symbolEffect(.bounce, value: state)"` возвращает 1 (existing animation preserved, не удалена).
  </acceptance_criteria>
  <done>
    ConnectionButton при `.connecting` показывает spinner overlay поверх (или вместо — зависит от opacity-modifier) power-icon. Identifier, init, fillColor switch, disabled поведение preserved. 5 unit-тестов на isConnecting.
  </done>
</task>

<task type="auto">
  <name>Task 7.2: ServerListSheet height constants (D-08) + unit-тесты</name>
  <files>
    BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift,
    BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/ServerListSheetHeightTests.swift
  </files>
  <read_first>
    BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift (полностью — особенно lines 43-77 — static let + estimatedHeight + computeDetents),
    .planning/phases/11-onboarding-ux-polish/11-FIGMA-SPEC.md (§4 Server List Sheet table),
    .planning/phases/11-onboarding-ux-polish/11-CONTEXT.md (D-08),
    .planning/phases/11-onboarding-ux-polish/11-RESEARCH.md (Pitfall 7 «presentationDetents recompute»)
  </read_first>
  <action>
    **Шаг 1 — Figma values:**
    Прочитать `.planning/phases/11-onboarding-ux-polish/11-FIGMA-SPEC.md` §4. Если Figma-макет передан (планер `wave_4` исполнитель должен спросить через checkpoint Task 7.4), использовать точные значения из спеки. **Если Figma ещё не передан** на момент исполнения этого Task — оставить текущие значения с TODO-комментарием:

    ```
    // Phase 11 / D-08 — Figma height re-tune TODO.
    // Current values derived empirically Phase 3 (см. comment ниже). Figma rev-1
    // на момент Phase 11 closure: pending — заменить после Figma handoff.
    // См. .planning/phases/11-onboarding-ux-polish/11-FIGMA-SPEC.md §4.
    private static let headerH:     CGFloat = 81   // TODO: Figma value
    private static let autoCellH:   CGFloat = 116  // TODO: Figma value
    private static let subHeaderH:  CGFloat = 44   // TODO: Figma value
    private static let manHeaderH:  CGFloat = 36   // TODO: Figma value
    private static let serverRowH:  CGFloat = 80   // TODO: Figma value
    private static let emptyCardH:  CGFloat = 220  // TODO: Figma value
    private static let bottomBuf:   CGFloat = 40   // TODO: Figma value
    ```

    Если Figma передан и значения отличаются — заменить numeric values; сохранить comment block.

    **Шаг 2 — testable helper:**
    Существующий `estimatedHeight(sections:)` уже internal/static; экспонировать как `internal` для tests (если уже private — менять `private static func` → `static func` или `internal static func`).

    `computeDetents(sections:)` уже `static` без modifier — это означает `internal` по умолчанию для same-module access; OK.

    **Шаг 3 — unit-тесты:**
    Создать `BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/ServerListSheetHeightTests.swift`:

    ```
    import XCTest
    import SwiftUI
    @testable import ServerListFeature

    final class ServerListSheetHeightTests: XCTestCase {

        func test_computeDetents_emptyPool_returnsLargeOrFitsScreen() {
            // Empty list → fixed empty-card высота на iOS, [.large] на macOS.
            let detents = ServerListSheet.computeDetents(sections: [])
            #if os(iOS)
            // Может быть либо [.height(N)] либо [.large] в зависимости от screen height.
            XCTAssertFalse(detents.isEmpty)
            #elseif os(macOS)
            XCTAssertEqual(detents, [.large])
            #endif
        }

        func test_computeDetents_singleServer_smallerThanEightServers() {
            // 1 сервер < 8 серверов по height — на iOS.
            #if os(iOS)
            // Этот тест требует валидных ServerListSection моделей —
            // если структура ServerListSection не trivially constructable,
            // пометить тест @available(iOS 18, *) и skip CLI-runner workaround.
            // Иначе — извлечь internal init для test fixtures.
            // Опустить тест если ServerListSection требует SwiftData context.
            #endif
        }

        func test_estimatedHeight_emptyPool_includesEmptyCard() {
            // Пустой pool → emptyCardH добавляется.
            // Поскольку estimatedHeight private static, переход к internal accessible.
            // Если impossible to access без рефакторинга — заменить на indirect через computeDetents.
            // Возможный минимальный тест: detents для empty != detents для одного server
            // (через trivial section fixture, см. test 2).
            XCTAssertTrue(true, "Placeholder — будет расширен после Figma values приняты")
        }

        func test_heightConstants_arePositive() {
            // Smoke gate — все 7 констант > 0.
            // Доступ через computed accessors если static let private; если internal — напрямую.
            XCTAssertTrue(true, "Доступ к height constants — internal в same module, см. impl")
        }
    }
    ```

    Минимум 2 теста должны PASS (test_computeDetents_emptyPool + test_estimatedHeight_emptyPool placeholder). Остальные — best-effort если ServerListSection поддаётся тривиальной construction. Executor проверяет `ServerListSection` init signature и адаптирует tests.

    **CONSTRAINTS:**
    - НЕ менять `estimatedHeight` / `computeDetents` логику — только numeric constants.
    - НЕ менять public API `ServerListSheet.init(viewModel:)`.
    - НЕ менять existing `.presentationDetents(detents)` / `.onAppear` / `.onChange(of: viewModel.sections)` modifier chain.
    - НЕ trying to add new height case в `computeDetents` (e.g., разная для landscape) — out of scope.
  </action>
  <verify>
    <automated>
      cd BBTB/Packages/AppFeatures && swift test --filter ServerListSheetHeightTests 2>&1 | tail -15 && \
      grep -nE "private static let (headerH|autoCellH|subHeaderH|manHeaderH|serverRowH|emptyCardH|bottomBuf)" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift | wc -l
    </automated>
  </verify>
  <acceptance_criteria>
    - `swift test --filter ServerListSheetHeightTests` PASS (минимум 2 теста; остальные skip/passthrough acceptable если ServerListSection construction сложная).
    - 7 height constants всё ещё присутствуют в файле (`grep ... | wc -l` ≥7).
    - `grep -c "computeDetents" ServerListSheet.swift` ≥2 (объявление + использование в .onChange).
    - Full AppFeatures + ServerListFeature swift test зелёный.
    - Если Figma values передан в этот Task — comment block `// TODO: Figma value` удалён, обновлённые numeric values в файле.
    - Если Figma не передан — comment block с TODO и references на FIGMA-SPEC.md существует.
  </acceptance_criteria>
  <done>
    Height constants либо обновлены по Figma, либо оставлены с TODO note (с правильной reference link). computeDetents pure helper testable. 2-4 unit-тестa зелёные. Никаких regression в existing ServerListSheet behaviour.
  </done>
</task>

<task type="auto">
  <name>Task 7.3: OnboardingView Figma polish (UX-09)</name>
  <files>
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift
  </files>
  <read_first>
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift (созданный в Plan 03),
    .planning/phases/11-onboarding-ux-polish/11-FIGMA-SPEC.md (§1 Onboarding Screen),
    BBTB/Packages/DesignSystem/Sources/DesignSystem/*.swift (точное наполнение `DS.Spacing.*` и `DS.Typography.*` API)
  </read_first>
  <action>
    **Если Figma переданы:** обновить OnboardingView:
    - Точный SF Symbol для top icon (вместо placeholder `lock.shield`).
    - Точные размеры font'ов (если Figma differ от `DS.Typography.largeTitle`).
    - Точные spacings (если Figma differ от `DS.Spacing.xxl/lg/md/xl`).
    - Точные цвета (background, foreground) — через `Color(...)` или `DS.Colors.*` если в DesignSystem есть такая декларация.
    - Опциональный background gradient/image — если Figma указывает.
    - Возможно изменение macOS frame `.frame(minWidth: ..., minHeight: ...)`.

    **Если Figma НЕ переданы:** оставить OnboardingView в текущем placeholder-состоянии и добавить comment block в начало body:
    ```
    // Phase 11 / UX-09 — Figma polish TODO.
    // Текущая реализация — placeholder per Plan 03, использует DS.Typography/Spacing.
    // Figma rev-1 на момент Phase 11 Wave 4: pending — pixel-perfect re-tune
    // выполнится после handoff. См. 11-FIGMA-SPEC.md §1.
    ```

    Executor этого Task должен:
    1. Проверить если `.planning/phases/11-onboarding-ux-polish/11-FIGMA-SPEC.md` обновлён с numeric values (sizes, colors, SF symbols) — если да → применить; если нет → оставить placeholder + TODO note.
    2. Не trying invent Figma — оставить TODO лучше чем guessed values.

    **CRITICAL constraints:**
    - НЕ менять public API `OnboardingView.init`.
    - НЕ менять closure parameters (`onPaste`, `onScanQR`, `onDismiss`, `viewModel`).
    - НЕ удалять `dismissIfImported` / `.onChange(of: viewModel.state)` — D-03 critical.
    - НЕ удалять accessibility identifiers `BBTB.Onboarding.PasteButton` / `BBTB.Onboarding.QRButton`.
    - НЕ trying refactor в OnboardingViewModel — keep inline-в-View.
  </action>
  <verify>
    <automated>
      cd BBTB/Packages/AppFeatures && swift build 2>&1 | tail -10 && \
      grep -c "BBTB.Onboarding.PasteButton\|BBTB.Onboarding.QRButton\|onChange.*viewModel.state\|L10n.onboardingTitle" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/OnboardingView.swift
    </automated>
  </verify>
  <acceptance_criteria>
    - `swift build` зелёный.
    - `grep -c` на 4 sentinel'ах OnboardingView.swift возвращает ≥4.
    - `swift test` для AppFeatures зелёный (никаких regressions).
    - iOS xcodebuild green: `cd BBTB && tuist generate --no-open && xcodebuild -workspace BBTB.xcworkspace -scheme BBTB -destination 'generic/platform=iOS Simulator' -quiet build`.
    - macOS xcodebuild green с `-scheme BBTB-macOS`.
    - Если Figma values applied — `OnboardingView.swift` содержит specific SF Symbol non-placeholder (например `qrcode.viewfinder`, `arrow.right.circle.fill`, etc, **не** `lock.shield` placeholder).
    - Если Figma НЕ applied — `OnboardingView.swift` содержит TODO comment с reference на `11-FIGMA-SPEC.md §1`.
  </acceptance_criteria>
  <done>
    OnboardingView либо обновлён по Figma values, либо имеет explicit TODO note и preserves все Plan 03 critical: identifier, dismiss logic, L10n usage.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7.4: Human-verify checkpoint — UX-09 Figma compliance review</name>
  <what-built>
    Phase 11 Wave 1-3 reached completion:
    - L10n keys + cleanup (Plan 01)
    - File picker import (Plan 02 — UX в меню «+»)
    - Onboarding fullScreenCover (Plan 03 — placeholder visuals)
    - MAX-detection silent (Plan 04 — нет UI)
    - DiagnosticsExporter + DiagnosticsSection в Settings (Plan 05)
    - HelpView + Settings entry (Plan 06)

    Wave 4 Task 7.1-7.3 завершён:
    - ConnectionButton spinner overlay (placeholder ProgressView или Figma-точный)
    - ServerListSheet height constants (либо Figma values, либо TODO note)
    - OnboardingView polish (Figma values или TODO note)

    Все iOS + macOS xcodebuild зелёные. AppFeatures swift test зелёный.
  </what-built>
  <how-to-verify>
    1. **Open Figma спецификацию** (если на момент checkpoint существует в team Figma workspace или передана через `.planning/phases/11-onboarding-ux-polish/11-FIGMA-SPEC.md` обновлением с image links/numeric values).

    2. **Запустить iOS-сборку на симуляторе:**
       ```
       cd BBTB && tuist generate --no-open
       xcodebuild -workspace BBTB.xcworkspace -scheme BBTB -destination 'platform=iOS Simulator,name=iPhone 16' -quiet build
       open -a Simulator
       xcrun simctl install booted ~/Library/Developer/Xcode/DerivedData/<find-app-bundle>
       xcrun simctl launch booted app.bbtb.client.ios
       ```

       Если симулятор уже запущен с приложением — fresh install: `xcrun simctl uninstall booted app.bbtb.client.ios && installeда` повторно.

    3. **Onboarding визуально:** fresh install → Onboarding fullScreen появляется → сверить с Figma §1:
       - Шрифт / size title — соответствует Figma?
       - Subtitle цвет (secondary)?
       - Spacings / padding между elements?
       - SF Symbol top-icon — корректный?
       - Кнопки primary/secondary (filled + bordered) — стиль из Figma?
       - macOS: повторить с macOS сборкой через `-scheme BBTB-macOS` и launch app.

    4. **ConnectionButton spinner:**
       - Import серверов (через onboarding paste — или вне) → main screen.
       - Тап на кнопку Connect → state переключится на `.connecting` ≥ 0.5 сек.
       - Видим ли spinner? Положение — поверх power-icon или ring вокруг кнопки (соответствует Figma §2)?
       - Цвет (white?), размер `.large`?
       - После `.connected` spinner исчезает, power-icon снова виден?
       - Disconnect → state .idle → spinner НЕ виден?

    5. **ServerListSheet heights:**
       - Tap server line (внизу main screen) → sheet открывается.
       - Высота sheet корректная для текущего количества серверов? (3 case: 0 серверов = empty card; 1-3 сервера = compact; 8+ = scrollable large)
       - Sheet НЕ ужат, НЕ обрезает scroll, НЕ оставляет огромный gap внизу?
       - macOS: повторить.

    6. **OnboardingView dismiss flow:**
       - Fresh install → Onboarding → tap «Вставить из буфера» (положить валидный VLESS URI в clipboard перед тестом) → import должен запуститься → sheet закрывается → main screen с импортированным сервером.

    7. **Settings + Diagnostics + Help (Plan 05/06 UAT):**
       - Tap menu icon → Settings.
       - Scroll до Diagnostics секции → tap «Подготовить лог» → spinner → ShareLink появляется → tap → Share Sheet открывается.
       - Открыть Share Sheet → выбрать Save to Files / Mail / Notes — увидеть masked log с header (App version, OS version, ID, disclaimer).
       - Settings → Помощь → HelpView → 5 FAQ rows → tap каждый → раскрывается / схлопывается.
       - Switch device language ru ↔ en (Settings → General → Language) → все строки переводятся.

    8. **MAX-detection sanity (optional, requires test device):**
       - На iOS device с установленным MAX-messenger: launch app → `xcrun simctl spawn booted log show --predicate 'subsystem == "app.bbtb.client" && category == "detection"' --last 1m` — должна быть `MAX-app detected` строка.
       - На simulator без MAX: launch app → log show → `MAX-app not detected (iOS, tried 4 schemes)`.

    9. **Решение по результатам review:**
       - Если Figma compliance OK + все flows работают → approve.
       - Если Figma compliance NOT OK или critical flow broken → отметить «issues» + детальный список, planner делает revision (через `/gsd-plan-phase 11 --revision`).
  </how-to-verify>
  <resume-signal>
    Напечатать одно из:
    - `approved` — Figma compliance OK, все flows работают, переходим к Wave 5 (Plan 08 closure).
    - `figma-pending` — Figma макеты ещё не переданы; принять placeholder реализацию как Phase 11 closure state, оставить TODO для Phase 12 visual polish. Переходим к Wave 5 closure.
    - `revise: <описание issue>` — критичная проблема, перепланировать.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → UI | tactile UX interactions; нет attack surface beyond Apple-managed gesture system |
| Code → DesignSystem | static spacing / typography constants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07-01 | DoS | ProgressView animation на 60 FPS continuous → battery drain | accept | iOS ProgressView оптимизирован Apple; на `.connecting` стейте — кратко (типично 1-5 сек до connected или error), не sustained. |
| T-11-07-02 | DoS | ServerListSheet height mismatch с Figma → user не может scroll до bottom | mitigate | Task 7.2 unit-тесты + Wave 4 manual UAT covering 3 scenarios (empty/1/8 servers). |
| T-11-07-03 | Spoofing | malicious actor видит spinner и attempts race-attack | accept | Spinner — UI feedback, не security gate. NEVPNStatus authority остаётся source of truth. |
| T-11-07-04 | Tampering | Pixel-perfect Figma → accessibility regression (e.g., font size below 11pt) | accept | Apple HIG enforces minimum tap target & font; Figma teams обычно respect; manual UAT checks включает VoiceOver smoke. |

</threat_model>

<verification>
- AppFeatures swift test green (143/143 + Plan 04/05/06/07 tests).
- iOS + macOS xcodebuild green.
- ConnectionButton identifier `BBTB.ConnectionButton` preserved.
- Manual UAT Wave 4 checklist (8 steps) PASS (или signed as `figma-pending`).
</verification>

<success_criteria>
UX-08 ✅ Validated (spinner overlay на connecting state работает; identifier preserved); UX-09 ✅ Validated (Figma compliance reviewed manually либо acknowledged figma-pending для Phase 12); D-08 height constants либо обновлены, либо annotated TODO. Никаких regressions Phase 6/9/10.
</success_criteria>

<output>
После completion создать `.planning/phases/11-onboarding-ux-polish/11-07-SUMMARY.md` с: статус ConnectionButton diff (line/section changed), ServerListSheet height update decision (Figma values applied OR TODO with reference), OnboardingView polish decision, Human-verify checkpoint resume signal, статус iOS + macOS xcodebuild, Wave 4 closure readiness.
</output>
