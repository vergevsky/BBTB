---
phase: 08-rules-engine-split-tunneling
plan: W0
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - BBTB/Project.swift
  - BBTB/App/macOSApp/BBTB-macOS.entitlements
  - BBTB/App/AppProxyExtension-macOS/AppProxyProvider.swift
  - BBTB/App/AppProxyExtension-macOS/Info.plist
  - BBTB/App/AppProxyExtension-macOS/AppProxyExtension-macOS.entitlements
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionURLFetcher.swift
  - wiki/appproxy-deferral-2026.md
autonomous: true
requirements: [RULES-11]
out_of_scope: [RULES-11]
mvp_slice: "RULES-11 + Phase 8 SC #3 формально carve-out: ROADMAP/REQUIREMENTS обновлены, AppProxyExtension-macOS target/files удалены, isBlockedHost SSRF helper повышен до public — фундамент готов для W1 vertical slice."
user_setup:
  - service: apple-developer-portal
    why: "Phase 8 D-09 revisit AppProxy capability на macOS App ID (после code DELETE)"
    dashboard_config:
      - task: "В Apple Developer Portal 2026: Identifiers → app.bbtb.client.macos → Edit → Network Extensions capability → снять галочку 'App Proxy Provider' (оставить 'Packet Tunnel Provider' включённым). Сохранить. Если был отдельный App ID app.bbtb.client.macos.appproxy (Phase 1 likely didn't register it поскольку он никогда не reach TestFlight upload) — удалить или disable."
        location: "https://developer.apple.com/account/resources/identifiers/list"

must_haves:
  truths:
    - "ROADMAP.md Phase 8 entry: SC #3 (macOS AppProxy) переведён в backlog v0.10+ с D-08 rationale ссылкой"
    - "REQUIREMENTS.md RULES-11 row помечен Out of Scope, v0.10+ conditional, со strikethrough оформлением аналогично PROTO-06/07/09"
    - "Tuist Project.swift не содержит target 'BBTB-AppProxy-macOS' и dependency reference '.target(name: \"BBTB-AppProxy-macOS\")' в BBTB-macOS блоке"
    - "Директория BBTB/App/AppProxyExtension-macOS/ физически удалена из репозитория"
    - "BBTB-macOS.entitlements массив networking.networkextension содержит только 'packet-tunnel-provider' (без 'app-proxy-provider')"
    - "SubscriptionURLFetcher.isBlockedHost(_:) и normalizeHostForLog(_:) helpers повышены до public для reuse из нового RulesEngine пакета"
    - "wiki/appproxy-deferral-2026.md существует, ссылается на Codex thread 019e284c, описывает D-08 rationale и условия возврата в v0.10+"
    - "tuist generate проходит без ошибок; xcodebuild BBTB-macOS scheme собирается без AppProxyProvider.swift references"
    - "grep -rE 'NEAppProxyProvider|app-proxy-provider' BBTB/App/macOSApp/ BBTB/Packages/AppFeatures/Sources/ возвращает 0 matches"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 8 SC #3 → deferred v0.10+; RULES-11 marked Out of Scope с reference на wiki/appproxy-deferral-2026.md"
      contains: "Out of Scope per Phase 8 D-08/D-09 2026-05-15"
    - path: ".planning/REQUIREMENTS.md"
      provides: "RULES-11 row strikethrough + rationale block"
      contains: "~~**RULES-11**"
    - path: "BBTB/Project.swift"
      provides: "Tuist manifest without AppProxy target"
      min_lines: 200
    - path: "BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionURLFetcher.swift"
      provides: "isBlockedHost(_:) + normalizeHostForLog(_:) promoted public для RulesEngine reuse"
      contains: "public static func isBlockedHost"
    - path: "wiki/appproxy-deferral-2026.md"
      provides: "Long-term decision log для AppProxy deferral"
      min_lines: 40
  key_links:
    - from: "BBTB/Project.swift"
      to: "macOS app target (BBTB-macOS) dependencies"
      via: "удалена строка `.target(name: \"BBTB-AppProxy-macOS\")`"
      pattern: "BBTB-AppProxy-macOS"
    - from: ".planning/ROADMAP.md Phase 8 entry"
      to: "wiki/appproxy-deferral-2026.md"
      via: "markdown link под success criterion #3"
      pattern: "appproxy-deferral-2026"
    - from: "Packages/RulesEngine (future W1)"
      to: "ConfigParser.SubscriptionURLFetcher.isBlockedHost"
      via: "import + public static call"
      pattern: "SubscriptionURLFetcher.isBlockedHost"
---

<objective>
Plan W0 — фундамент Phase 8: формально carve-out RULES-11 + Phase 8 SC #3 (macOS AppProxy data plane) согласно CONTEXT.md D-08/D-09 + Codex architectural review 019e284c. Удалить весь связанный код, обновить документацию (ROADMAP/REQUIREMENTS/wiki long-term memory), promote `SubscriptionURLFetcher.isBlockedHost` helper до public для будущего reuse из пакета `RulesEngine` (W1).

Purpose: Phase 8 не может начинать имплементацию RulesEngine пока в репозитории остаётся «зомби»-target `BBTB-AppProxy-macOS` с пустым `AppProxyProvider.swift`. Это nuance important потому что (1) Tuist generate включает его в workspace; (2) macOS entitlements декларируют capability которая никогда не будет использоваться; (3) Apple Developer Portal capability должна быть disabled до publication; (4) downstream waves (W1 RulesFetcher) полагаются на reuse существующего SSRF blocklist из ConfigParser — helper должен быть public до того, как новый пакет начнёт его импортировать.

Output: чистая baseline для всех downstream wave; нет breaking changes для существующих iOS+macOS targets (только удаление dead code + visibility расширение).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-rules-engine-split-tunneling/08-CONTEXT.md
@.planning/phases/08-rules-engine-split-tunneling/08-RESEARCH.md
@.planning/phases/08-rules-engine-split-tunneling/08-PATTERNS.md
@.planning/phases/08-rules-engine-split-tunneling/08-VALIDATION.md
@BBTB/Project.swift
@BBTB/App/macOSApp/BBTB-macOS.entitlements
@BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionURLFetcher.swift
@wiki/wireguard-deferral-2026.md
@wiki/openvpn-deferral-2026.md
@wiki/amneziawg-deferral-2026.md

<interfaces>
<!-- W0 establishes following public surface для downstream RulesEngine consumer -->

From BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionURLFetcher.swift:
- public static func isBlockedHost(_ host: String) -> Bool   // SSRF blocklist (loopback / RFC-1918 / link-local / ULA / multicast)
- public static func normalizeHostForLog(_ host: String) -> String   // PII-safe host display

The two helpers MUST keep идентичную semantics (Phase 2 audit signed off на этот blocklist). W0 task #4 — pure visibility change `internal → public`, ноль изменения logic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task W0.1: Promote SubscriptionURLFetcher SSRF helpers to public</name>
  <files>BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionURLFetcher.swift</files>
  <read_first>
    BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionURLFetcher.swift,
    BBTB/Packages/ConfigParser/Tests/ConfigParserTests/SubscriptionURLFetcherTests.swift,
    .planning/phases/08-rules-engine-split-tunneling/08-RESEARCH.md (§ Code Examples → RulesFetcher lines 622-654 — calls out этот helper для reuse),
    .planning/phases/08-rules-engine-split-tunneling/08-PATTERNS.md (§ Risks Surfaced #1 — точная причина promotion)
  </read_first>
  <action>В `SubscriptionURLFetcher.swift` повысить access level helper-функций `isBlockedHost(_ host: String) -> Bool` и `normalizeHostForLog(_ host: String) -> String` с `internal` (default) на `public`. Чистый visibility change — НЕ менять body, НЕ менять signature, НЕ переименовывать. Если helper'ы являются `private` (а не `internal`) — также повысить до `public`. Сохранить существующие doc-comments + добавить однострочный комментарий выше каждого helper'а: «Phase 8 W0 — promoted public для reuse из RulesEngine.RulesFetcher (см. .planning/phases/08-rules-engine-split-tunneling/08-RESEARCH.md § Validation Architecture Risk #1)». ConfigParser package version bump НЕ нужен (SwiftPM не использует semver в monorepo).</action>
  <verify>
    <automated>cd BBTB/Packages/ConfigParser && swift test 2>&1 | tail -20 | grep -E 'Test Suite.*passed|failed'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -E 'public static func (isBlockedHost|normalizeHostForLog)' BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionURLFetcher.swift | wc -l` equals 2
    - Test command: `cd BBTB/Packages/ConfigParser && swift test` exits 0
    - Source: `grep -c 'internal static func isBlockedHost\|private static func isBlockedHost' BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionURLFetcher.swift` equals 0
    - Source: doc-comment «Phase 8 W0 — promoted public для reuse из RulesEngine.RulesFetcher» присутствует выше обеих функций (grep returns 2)
  </acceptance_criteria>
  <done>SubscriptionURLFetcher exposes isBlockedHost + normalizeHostForLog как public API; все существующие тесты ConfigParser проходят; никакой regression в Phase 2-7 кода.</done>
</task>

<task type="auto">
  <name>Task W0.2: Delete AppProxyExtension-macOS code + entitlements (D-09)</name>
  <files>BBTB/App/AppProxyExtension-macOS/AppProxyProvider.swift, BBTB/App/AppProxyExtension-macOS/Info.plist, BBTB/App/AppProxyExtension-macOS/AppProxyExtension-macOS.entitlements, BBTB/App/macOSApp/BBTB-macOS.entitlements</files>
  <read_first>
    BBTB/App/AppProxyExtension-macOS/AppProxyProvider.swift,
    BBTB/App/AppProxyExtension-macOS/Info.plist,
    BBTB/App/AppProxyExtension-macOS/AppProxyExtension-macOS.entitlements,
    BBTB/App/macOSApp/BBTB-macOS.entitlements,
    .planning/phases/08-rules-engine-split-tunneling/08-CONTEXT.md (D-09 точная спецификация),
    .planning/phases/08-rules-engine-split-tunneling/08-RESEARCH.md (§ Runtime State Inventory steps 5-7)
  </read_first>
  <action>(1) Удалить через `git rm -r BBTB/App/AppProxyExtension-macOS/` всю директорию (3 файла: AppProxyProvider.swift, Info.plist, AppProxyExtension-macOS.entitlements). (2) Открыть `BBTB/App/macOSApp/BBTB-macOS.entitlements` и в массиве `<key>com.apple.developer.networking.networkextension</key><array>...</array>` УДАЛИТЬ строку `<string>app-proxy-provider</string>` если она там есть; сохранить только `<string>packet-tunnel-provider</string>`. Не трогать другие entitlements (app-sandbox, network.client, App Group identifier, etc.). NO Tuist regenerate в этой задаче — Project.swift всё ещё содержит target reference, regenerate сделает Task W0.3. (3) Никаких commit hooks / pre-commit изменений — пользователь не запросил commit, just delete + edit.</action>
  <verify>
    <automated>test ! -d BBTB/App/AppProxyExtension-macOS/ && ! grep -q 'app-proxy-provider' BBTB/App/macOSApp/BBTB-macOS.entitlements && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `test ! -d BBTB/App/AppProxyExtension-macOS/` exits 0 (директория удалена)
    - CLI: `git status --short BBTB/App/AppProxyExtension-macOS/ 2>&1 | grep -E '^ D|^D '` shows 3 deleted files (AppProxyProvider.swift, Info.plist, .entitlements)
    - Source: `grep -c 'app-proxy-provider' BBTB/App/macOSApp/BBTB-macOS.entitlements` equals 0
    - Source: `grep -c 'packet-tunnel-provider' BBTB/App/macOSApp/BBTB-macOS.entitlements` equals 1 (preserved)
  </acceptance_criteria>
  <done>3 AppProxy файла физически удалены из репозитория; macOS app entitlements объявляют только packet-tunnel-provider capability; Tuist Project.swift всё ещё содержит target reference (будет удалено в W0.3) — на этом этапе `tuist generate` ожидаемо сломается, что приемлемо т.к. следующая задача (W0.3) сразу чинит manifest.</done>
</task>

<task type="auto">
  <name>Task W0.3: Remove BBTB-AppProxy-macOS target from Tuist manifest + regenerate</name>
  <files>BBTB/Project.swift</files>
  <read_first>
    BBTB/Project.swift (особенно lines 105-145 и 205-220 — BBTB-macOS dependencies блок + AppProxy target block),
    .planning/phases/08-rules-engine-split-tunneling/08-RESEARCH.md (§ Runtime State Inventory steps 1-4 — точная последовательность),
    .planning/phases/08-rules-engine-split-tunneling/08-PATTERNS.md (§ 23 — точные строки удаления + § Risks Surfaced #6 — Tuist regenerate risk)
  </read_first>
  <action>В `BBTB/Project.swift` выполнить ДВА удаления: (a) В блоке target `BBTB-macOS` (~line 105-145) в массиве `dependencies:` удалить строку `.target(name: "BBTB-AppProxy-macOS"),`. (b) Удалить весь блок target `.target(name: "BBTB-AppProxy-macOS", destinations: [.mac], product: .appExtension, bundleId: "\(bundlePrefix).macos.appproxy", ...)` целиком (вместе с MARK comment `// MARK: macOS AppProxy Extension (placeholder под Phase 8)` если она есть прямо перед блоком — он становится stale). Все остальные targets (BBTB iOS app, BBTB-macOS, BBTB-Tunnel-iOS, BBTB-Tunnel-macOS) остаются нетронутыми. После edit выполнить `cd BBTB && tuist generate` чтобы regenerate workspace. Если Xcode держит workspace открытым — закрыть его до regenerate (`osascript -e 'quit app "Xcode"' || true`). После regenerate выполнить smoke check `xcodebuild -workspace BBTB/BBTB.xcworkspace -scheme BBTB-macOS -configuration Debug -destination 'platform=macOS' build -quiet 2>&1 | tail -5` чтобы убедиться что macOS app компилируется без AppProxy references.</action>
  <verify>
    <automated>! grep -q 'BBTB-AppProxy-macOS' BBTB/Project.swift && cd BBTB && tuist generate 2>&1 | tail -3 | grep -qE 'Project generated|generated successfully|Generated project'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'BBTB-AppProxy-macOS' BBTB/Project.swift` equals 0
    - Source: `grep -c 'app.bbtb.client.macos.appproxy' BBTB/Project.swift` equals 0
    - Source: `grep -c 'BBTB-Tunnel-macOS' BBTB/Project.swift` equals 2 or more (preserved — target definition + dependency reference)
    - CLI: `cd BBTB && tuist generate` exits 0
    - CLI: After regenerate, `xcodebuild -workspace BBTB/BBTB.xcworkspace -scheme BBTB-macOS -configuration Debug -destination 'platform=macOS' -showBuildSettings 2>&1 | grep -E 'PRODUCT_NAME = BBTB-macOS'` returns at least one match (target exists, builds)
  </acceptance_criteria>
  <done>Tuist manifest очищен от AppProxy target + dependency reference; `tuist generate` clean; macOS xcodebuild schema BBTB-macOS существует и собирается; iOS app не затронут.</done>
</task>

<task type="auto">
  <name>Task W0.4: Update ROADMAP.md + REQUIREMENTS.md (RULES-11 → Out of Scope)</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/ROADMAP.md (Phase 8 entry lines 323-336),
    .planning/REQUIREMENTS.md (RULES section + Out of Scope section patterns Phase 7 PROTO-06/07/09 — образец strikethrough оформления),
    .planning/phases/08-rules-engine-split-tunneling/08-CONTEXT.md (D-08 + D-09 точные формулировки),
    .planning/phases/08-rules-engine-split-tunneling/08-RESEARCH.md (§ Why RULES-11 carve-out — copy-paste рационейл)
  </read_first>
  <action>ROADMAP.md edit Phase 8 entry:
(a) В разделе **Success Criteria** для Phase 8: SC #3 («На macOS AppProxyProvider позволяет роутить отдельные приложения через VPN») заменить на: «3. ~~На macOS AppProxyProvider позволяет роутить отдельные приложения через VPN~~ → **Out of Scope v0.8**, перенесено в v0.10+ conditional. См. `wiki/appproxy-deferral-2026.md` для rationale + Codex thread `019e284c-4bf6-7f91-ada7-7e679692b5fb`.»
(b) В строке **Requirements:** Phase 8 НЕ удалять RULES-11 из списка (для preserve coverage gate), но добавить инлайновую пометку: `~~RULES-11~~ (Out of Scope per D-08)`. То есть формат строки: `CORE-05, RULES-01, RULES-02, ..., RULES-10, ~~RULES-11~~ (Out of Scope per D-08)`.

REQUIREMENTS.md edit Rules Engine section:
(a) Строку RULES-11 заменить со strikethrough оформлением аналогично PROTO-06/07/09. Точный текст:
`- [ ] ~~**RULES-11**: AppProxyProvider таргет на macOS для per-app routing~~ → **Out of Scope** _(Phase 8 D-08/D-09 carve-out 2026-05-15. Architectural mismatch: sing-box L3 TUN inbound vs NEAppProxy L4 flows; NETunnelProviderManager и NEAppProxyProviderManager mutually exclusive; bridging либо ломает R1 invariant (SOCKS5 inbound), либо bypass-ит Reality (теряем anti-DPI). Workaround: never_through_vpn через rule_set domain/IP matching покрывает 95% friends-and-family TestFlight scenarios. Возврат — v0.10+ conditional при 3+ TestFlight запросов на per-app routing. См. wiki/appproxy-deferral-2026.md + Codex thread 019e284c.)_`
(b) Аналогично подкорректировать CORE-05 если ROADMAP/REQUIREMENTS amendment per 08-RESEARCH.md § Phase Requirements line 841 требуется — CORE-05 НЕ удалять, но добавить inline пометку: «**CORE-05**: ~~AppProxyExtension таргет на macOS~~ → Split-tunneling routing via sing-box rule_set (D-08 carve-out 2026-05-15)»
(c) Last updated footer внизу REQUIREMENTS.md: «Last updated: 2026-05-15 — Phase 8 W0 amendment: RULES-11 + CORE-05 (AppProxy сторона) carved out per D-08/D-09. Split-tunnel data plane перенесён на sing-box rule_set, см. wiki/appproxy-deferral-2026.md.»</action>
  <verify>
    <automated>grep -c 'Out of Scope per D-08' .planning/ROADMAP.md | xargs test 1 -le && grep -c 'wiki/appproxy-deferral-2026.md' .planning/REQUIREMENTS.md | xargs test 1 -le && grep -c '~~\*\*RULES-11\*\*' .planning/REQUIREMENTS.md | xargs test 1 -le && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.planning/ROADMAP.md` содержит подстроку «Out of Scope per D-08» (grep returns ≥ 1)
    - Source: `.planning/ROADMAP.md` Phase 8 Requirements строка содержит `~~RULES-11~~` markup (grep returns ≥ 1)
    - Source: `.planning/REQUIREMENTS.md` содержит строку `~~**RULES-11**: AppProxyProvider таргет на macOS` (grep returns 1)
    - Source: `.planning/REQUIREMENTS.md` содержит ссылку `wiki/appproxy-deferral-2026.md` (grep returns ≥ 1)
    - Source: `.planning/REQUIREMENTS.md` footer обновлён с датой `2026-05-15` (grep returns ≥ 1)
  </acceptance_criteria>
  <done>ROADMAP/REQUIREMENTS отражают формальный carve-out RULES-11 + Phase 8 SC #3; strikethrough оформление консистентно с Phase 7 PROTO-06/07/09 паттерном; rationale ссылается на wiki page (which W0.5 создаст) + Codex thread ID.</done>
</task>

<task type="auto">
  <name>Task W0.5: Create wiki/appproxy-deferral-2026.md long-term decision log</name>
  <files>wiki/appproxy-deferral-2026.md, wiki/index.md, wiki/log.md</files>
  <read_first>
    wiki/wireguard-deferral-2026.md (closest analog — same deferral genre),
    wiki/amneziawg-deferral-2026.md (same genre),
    wiki/openvpn-deferral-2026.md (same genre),
    wiki/index.md (для добавления нового entry),
    wiki/log.md (для append daily entry),
    .planning/phases/08-rules-engine-split-tunneling/08-CONTEXT.md (D-08/D-09 wording),
    .planning/phases/08-rules-engine-split-tunneling/08-RESEARCH.md (§ Why RULES-11 carve-out — copy-paste source)
  </read_first>
  <action>Создать `wiki/appproxy-deferral-2026.md` со структурой совпадающей с wireguard-deferral-2026.md (page format из CLAUDE.md: Title / Summary / Sources / Last updated / horizontal rule / Main content / Related pages). Контент должен покрывать:
- **Что отложено:** RULES-11 (AppProxyProvider target на macOS) + Phase 8 ROADMAP SC #3.
- **Когда:** 2026-05-15 в `/gsd-discuss-phase 8` после Codex architectural review (thread `019e284c-4bf6-7f91-ada7-7e679692b5fb`).
- **Architectural mismatch:** L3 sing-box TUN inbound vs L4 NEAppProxyFlow; NETunnelProviderManager и NEAppProxyProviderManager mutually exclusive; iOS support отсутствует (AppProxy только macOS); per-app filter granularity vs domain/IP matching.
- **Почему bridging не работает:** SOCKS5 inbound ломает R1 invariant; multi-instance sing-box без documented support; AppProxy bypass-ит Reality (теряем anti-DPI).
- **Workaround в v0.8:** never_through_vpn / always_through_vpn / block_completely через sing-box rule_set domain/IP matching покрывает 95% friends-and-family TestFlight use-cases. Что теряем — per-bundle-ID granularity (e.g. «route Telegram через VPN, all other messengers — direct»).
- **Условие возврата (v0.10+ conditional):** 3+ независимых TestFlight запроса на per-app routing функцию, ИЛИ ТСПУ behavioral break domain-based split-tunnel (маловероятно).
- **Cost возврата:** ~15 min Tuist target re-add + ~30 min Apple Developer Portal capability re-enable + TBD AppProxy data plane implementation (depends на chosen approach — fresh design, NOT bundle-IDs but Apple-canonical signing_identifier + designated_requirement per HIG).
- **Связанные wiki pages:** [[security-gaps]] R1 invariant, [[engine-abstraction-decision-2026]] mono-engine baseline, [[architecture]] current architecture diagram.

После создания файла:
- Добавить запись в `wiki/index.md` в подходящем разделе (deferral decisions / out-of-scope) — следовать существующему formatting для wireguard-deferral / openvpn-deferral / amneziawg-deferral entries.
- Append в `wiki/log.md`: `2026-05-15 — Phase 8 W0 — Created appproxy-deferral-2026.md (RULES-11 + SC #3 carve-out per D-08/D-09; Codex thread 019e284c). Updated ROADMAP.md Phase 8 entry + REQUIREMENTS.md RULES-11 row + Tuist Project.swift (deleted BBTB-AppProxy-macOS target) + macOS entitlements (removed app-proxy-provider value).`</action>
  <verify>
    <automated>test -f wiki/appproxy-deferral-2026.md && wc -l wiki/appproxy-deferral-2026.md | awk '$1 > 40' | grep -q . && grep -q '019e284c' wiki/appproxy-deferral-2026.md && grep -q 'appproxy-deferral' wiki/index.md && grep -q '2026-05-15.*appproxy-deferral' wiki/log.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `test -f wiki/appproxy-deferral-2026.md` exits 0
    - CLI: `wc -l wiki/appproxy-deferral-2026.md` returns ≥ 40
    - Source: `grep -c '019e284c-4bf6-7f91-ada7-7e679692b5fb' wiki/appproxy-deferral-2026.md` equals 1
    - Source: `grep -c 'Last updated' wiki/appproxy-deferral-2026.md` equals 1
    - Source: `grep -c 'v0.10+' wiki/appproxy-deferral-2026.md` ≥ 1
    - Source: `grep -c 'appproxy-deferral' wiki/index.md` ≥ 1
    - Source: `tail -5 wiki/log.md | grep '2026-05-15.*appproxy'` returns ≥ 1 match
  </acceptance_criteria>
  <done>Long-term decision log создан и связан с wiki/index + wiki/log; downstream waves могут ссылаться на этот файл для rationale контекста; страница соблюдает CLAUDE.md page format.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-local → repo | Удаление файлов и Tuist regenerate происходят локально; pre-commit hook должен пройти |
| repo → Apple Developer Portal | Entitlements remove требует параллельного capability disable в Portal (manual step in user_setup) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-W0-01 | Tampering | Tuist manifest edit | mitigate | Изменения покрыты git diff review + `tuist generate` must succeed; smoke `xcodebuild BBTB-macOS` confirms нет stale references |
| T-08-W0-02 | Information disclosure | macOS entitlements file | accept | `app-proxy-provider` value — public-by-design capability declaration; removal только убирает unused capability, не exposes secrets |
| T-08-W0-03 | Elevation of privilege | SubscriptionURLFetcher visibility promotion | mitigate | Promotion internal → public НЕ меняет behavior; SSRF blocklist logic preserved verbatim; Phase 2 audit signed off на existing implementation; W0.1 task explicit «no body changes» |
| T-08-W0-04 | DoS | tuist regenerate failure breaks dev environment | mitigate | `tuist generate` запускается inline в W0.3 task verify; если падает — task fails, downstream wave не стартует |
| T-08-W0-05 | Repudiation | wiki long-term memory missing для RULES-11 deferral | mitigate | W0.5 создаёт `wiki/appproxy-deferral-2026.md` с явной cross-reference на Codex thread ID + ROADMAP + REQUIREMENTS — заpisь не теряется |
| T-08-W0-06 | Spoofing | Apple Developer Portal manual step skipped | accept | user_setup frontmatter explicit; если admin забывает disable capability в Portal — реального security impact нет (capability не используется в shipped binary; entitlement removed в Task W0.2). Documented в wiki page для долговременной памяти. |
</threat_model>

<verification>
- ✓ Все 5 tasks выполнены с автоматическим verify
- ✓ `git status` показывает 3 удалённых файла под AppProxyExtension-macOS/ + edits в 5 файлах (Project.swift, BBTB-macOS.entitlements, ROADMAP.md, REQUIREMENTS.md, SubscriptionURLFetcher.swift) + 3 новых wiki файла (appproxy-deferral-2026.md created, index.md + log.md edited)
- ✓ `tuist generate` exits 0
- ✓ `xcodebuild` BBTB-macOS scheme собирается без AppProxy references
- ✓ ConfigParser tests passing (no regression от visibility promotion)
- ✓ grep search для `NEAppProxyProvider|app-proxy-provider` в main app sources returns 0
</verification>

<success_criteria>
1. **AppProxy code physically removed:** `BBTB/App/AppProxyExtension-macOS/` директория не существует; macOS entitlements объявляют только packet-tunnel-provider.
2. **Tuist clean:** Project.swift не содержит `BBTB-AppProxy-macOS` target или dependency reference; `tuist generate` succeeds; BBTB-macOS schema продолжает building.
3. **ROADMAP + REQUIREMENTS reflect carve-out:** Phase 8 SC #3 → strikethrough + reference на wiki page; RULES-11 row strikethrough + rationale; CORE-05 wording adjusted; footer обновлён датой 2026-05-15.
4. **SSRF helper public:** `SubscriptionURLFetcher.isBlockedHost` + `normalizeHostForLog` доступны как `public` API; ConfigParser tests passing.
5. **Wiki long-term memory landed:** `wiki/appproxy-deferral-2026.md` создан, page format соблюдён, ссылается на Codex thread 019e284c + Codex Q architecture review; добавлен в wiki/index.md; log.md содержит daily entry.
6. **Phase 8 baseline готов для W1:** все downstream waves могут полагаться на (a) clean Tuist manifest, (b) public SSRF helper, (c) documented carve-out.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rules-engine-split-tunneling/08-W0-SUMMARY.md` со списком всех изменений:
- Files deleted (3 under AppProxyExtension-macOS/)
- Files edited (Project.swift, BBTB-macOS.entitlements, ROADMAP.md, REQUIREMENTS.md, SubscriptionURLFetcher.swift, wiki/index.md, wiki/log.md)
- Files created (wiki/appproxy-deferral-2026.md)
- Tuist generate result
- xcodebuild smoke result
- ConfigParser test result
- Apple Developer Portal user_setup reminder
</output>
