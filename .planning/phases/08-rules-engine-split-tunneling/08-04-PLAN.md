---
phase: 08-rules-engine-split-tunneling
plan: W3
type: execute
wave: 4
depends_on: [W2]
files_modified:
  - BBTB/Packages/AppFeatures/Sources/SettingsFeature/RulesViewerSection.swift
  - BBTB/Packages/AppFeatures/Sources/SettingsFeature/ForceUpdateRulesButton.swift
  - BBTB/Packages/AppFeatures/Sources/SettingsFeature/MinAppVersionBanner.swift
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MinAppVersionSheet.swift
  - BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdvancedSettingsView.swift
  - BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenView.swift
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift
  - BBTB/Packages/AppFeatures/Package.swift
  - BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings
  - BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/SettingsViewModelTests.swift
  - BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/MinAppVersionTests.swift
  - BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/ForceUpdateButtonStateTests.swift
autonomous: true
requirements: [RULES-08, RULES-09, RULES-10]
mvp_slice: "Vertical slice #3 — user-visible UI поверх W2 pipeline. После W3 пользователь видит: (a) текущие правила в Advanced Settings (read-only viewer); (b) force-update button с 60s cooldown countdown + inline status; (c) min_app_version modal sheet + persistent banner. Wired via late-bind setter pattern к RulesEngineCoordinator (W2)."
user_setup:
  - service: testflight-invite-url
    why: "min_app_version sheet primary button открывает TestFlight invite — URL надо заменить с placeholder на real invite token в Phase 12"
    dashboard_config:
      - task: "Phase 8 hardcodes URL constant `RulesEngineConstants.testFlightInviteURL = URL(string: \"https://testflight.apple.com/join/PLACEHOLDER\")!` в SettingsFeature. Phase 12 заменит на реальный invite token из App Store Connect → My Apps → BBTB → TestFlight → Public Link. До замены: button открывает placeholder URL что приведёт к TestFlight 404 — приемлемо для v0.8 development. См. memory project_phase12_distribution_creds_prerequisite.md."
        location: "App Store Connect → TestFlight → Public Link"

must_haves:
  truths:
    - "RulesViewerSection отображает 3 категории (block/never/always) с 3 sub-matchers каждая (domains/ip_cidrs/countries) внутри DisclosureGroup; entries .textSelection(.enabled); badge показывает count"
    - "ForceUpdateRulesButton имеет state machine .idle / .inProgress / .cooldown(seconds); UI обновляется каждую секунду через Timer.publish; на tap → haptic feedback (iOS)"
    - "ForceUpdateRulesButton inline status row показывает success/alreadyLatest/networkFailure/signatureFailure outcome с auto-dismiss 4s; не используется alert"
    - "MinAppVersionBanner — orange-tinted row внутри Form (top-most secцiя), tap → openTestFlight"
    - "MinAppVersionSheet — modal с icon arrow.up.app.fill (56pt accent) + title + body + 2 buttons (Open TestFlight / Later); presentationDetents .medium на iOS; 440x320 на macOS"
    - "Sheet dismissal persists через @AppStorage('app.bbtb.minAppVersion.dismissed') = <version string>; sheet НЕ показывается повторно для same version; banner все равно visible в Settings"
    - "AdvancedSettingsView Form содержит 4 sections в order: MinAppVersionBanner (conditional) → DNS (existing) → Rules viewer → Force-update"
    - "SettingsViewModel published properties: rulesSnapshot, rulesVersion, rulesLastFetchedAt, forceUpdateButtonState, forceUpdateStatusOutcome, showMinAppVersionBanner"
    - "MainScreenViewModel published showMinAppVersionSheet; observer для bbtbRulesEngineDidUpdate (queue: nil); handleMinAppVersionCheck async method"
    - "Two-phase init: SettingsViewModel + RulesEngineCoordinator wired через late-bind setter; observer registered после wire-up"
    - "L10n keys: ~30 новых добавлены в Localizable.xcstrings (ru + en) per UI-SPEC §Copywriting"
    - "Unit tests: SettingsViewModelTests covers force-update + cooldown publishing; MinAppVersionTests covers numeric semver comparison; ForceUpdateButtonStateTests covers state machine transitions"
  artifacts:
    - path: "BBTB/Packages/AppFeatures/Sources/SettingsFeature/RulesViewerSection.swift"
      provides: "Read-only SwiftUI viewer для RulesSnapshot"
      contains: "public struct RulesViewerSection"
    - path: "BBTB/Packages/AppFeatures/Sources/SettingsFeature/ForceUpdateRulesButton.swift"
      provides: "Force-update button + inline status row component"
      contains: "public struct ForceUpdateRulesButton"
    - path: "BBTB/Packages/AppFeatures/Sources/SettingsFeature/MinAppVersionBanner.swift"
      provides: "Orange-tinted persistent banner для Advanced Settings"
      contains: "public struct MinAppVersionBanner"
    - path: "BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MinAppVersionSheet.swift"
      provides: "Modal sheet для upgrade prompt"
      contains: "public struct MinAppVersionSheet"
    - path: "BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift"
      provides: "Extended VM bindings для rules state + force-update + banner"
      contains: "rulesSnapshot\\|forceUpdateButtonState"
    - path: "BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings"
      provides: "~30 новых L10n keys для Phase 8 UI"
      contains: "rules.forceUpdate"
  key_links:
    - from: "SettingsViewModel.swift"
      to: "RulesEngineCoordinator (W2)"
      via: "weak coordinator reference + late-bind setter + NotificationCenter observer"
      pattern: "rulesEngineCoordinator"
    - from: "MainScreenViewModel.swift"
      to: "Notification.Name.bbtbRulesEngineDidUpdate"
      via: "observer для notification + handleMinAppVersionCheck"
      pattern: "bbtbRulesEngineDidUpdate"
    - from: "AdvancedSettingsView.swift"
      to: "RulesViewerSection + ForceUpdateRulesButton + MinAppVersionBanner"
      via: "Form Section composition"
      pattern: "RulesViewerSection\\|ForceUpdateRulesButton"
    - from: "MinAppVersionSheet primary button"
      to: "TestFlight URL (placeholder в W3, real в Phase 12)"
      via: "UIApplication.shared.open / NSWorkspace.shared.open"
      pattern: "testflight.apple.com"
---

<objective>
Plan W3 — vertical slice #3: переводит W2 pipeline в видимый user experience. Пользователь в Settings → Расширенные видит current rules snapshot, может нажать «Принудительно обновить правила» (60s cooldown), и если admin поднял min_app_version выше current — видит modal sheet + persistent orange banner. Это ПЕРВАЯ wave где пользователь может физически взаимодействовать с Phase 8 функциональностью.

Purpose: separate UI shells (W3) от platform schedulers (W4) и engine integration (W5). Phase 8 W3 — SwiftUI surface, ViewModel state, L10n keys, observer wiring; никакого touch tunnel'у, никакого BGTaskScheduler — это все W4-W5. После W3 user can manually trigger rules-engine workflow через force-update button; auto-fetch на cold-start (BGAppRefreshTask schedule) приходит в W4.

Output: 4 new SwiftUI components + 2 modified views + 2 modified ViewModels + extended L10n + 3 test files. AdvancedSettingsView rebuilt с правильным section order; MainScreenView adds sheet modifier; observer wiring через NotificationCenter Apple-pattern queue=nil per feedback_nevpn_observer_queue_main.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-rules-engine-split-tunneling/08-CONTEXT.md
@.planning/phases/08-rules-engine-split-tunneling/08-UI-SPEC.md
@.planning/phases/08-rules-engine-split-tunneling/08-PATTERNS.md
@.planning/phases/08-rules-engine-split-tunneling/08-VALIDATION.md
@.planning/phases/08-rules-engine-split-tunneling/08-W2-PLAN.md
@BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdvancedSettingsView.swift
@BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift
@BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdBlockToggleSection.swift
@BBTB/Packages/AppFeatures/Sources/SettingsFeature/AutoReconnectToggleSection.swift
@BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenView.swift
@BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift
@BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ReconnectBanner.swift
@BBTB/Packages/AppFeatures/Sources/MainScreenFeature/EmptyStateCard.swift
@BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift
@BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings

<interfaces>
<!-- W3 declares following public surface для downstream W4 host bootstrap -->

From BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift (extended):
- @Published public private(set) var rulesSnapshot: RulesSnapshot?
- @Published public private(set) var rulesVersion: Int = 0
- @Published public private(set) var rulesLastFetchedAt: Date?
- @Published public private(set) var forceUpdateButtonState: ForceUpdateButtonState
- @Published public private(set) var forceUpdateStatusOutcome: ForceUpdateOutcome?
- @Published public private(set) var showMinAppVersionBanner: Bool
- public weak var rulesEngineCoordinator: RulesEngineCoordinator?   // late-bound (Phase 6c pattern)
- public func wireRulesCoordinator(_ coordinator: RulesEngineCoordinator) async   // late-bind setter
- public func triggerForceUpdate() async
- public func openTestFlight()

From BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift (extended):
- @Published public private(set) var showMinAppVersionSheet: Bool
- public weak var rulesEngineCoordinator: RulesEngineCoordinator?
- public func wireRulesCoordinator(_ coordinator: RulesEngineCoordinator) async
- public func handleMinAppVersionCheck() async
- public func dismissMinAppVersionSheet()

From SettingsFeature:
- public enum ForceUpdateButtonState: Equatable, Sendable { case idle; case inProgress; case cooldown(secondsRemaining: Int) }
- public struct RulesViewerSection: View — init(snapshot: RulesSnapshot?)
- public struct ForceUpdateRulesButton: View — init(buttonState, statusOutcome, onTap)
- public struct MinAppVersionBanner: View — init(currentVersion, onTap)

From MainScreenFeature:
- public struct MinAppVersionSheet: View — init(currentVersion, onOpenTestFlight, onDismiss)

L10n keys (~30 total) per UI-SPEC §Copywriting:
- rules.section.{block,never,always}
- rules.section.{block,never,always}.footer
- rules.matcher.{domains,ipcidrs,countries}
- rules.count.{domains,ipcidrs,countries}   // plural-aware
- rules.header.version
- rules.empty.{category,title,subtitle}
- rules.forceUpdate.{button,inProgress,cooldown,success,noChange,network,signature,footer}
- rules.forceUpdate.section
- minAppVersion.sheet.{title,body,primary,secondary}
- minAppVersion.banner.{text,cta}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task W3.1: ForceUpdateButtonState type + L10n keys + 3 atomic SwiftUI components</name>
  <files>BBTB/Packages/AppFeatures/Sources/SettingsFeature/ForceUpdateRulesButton.swift, BBTB/Packages/AppFeatures/Sources/SettingsFeature/RulesViewerSection.swift, BBTB/Packages/AppFeatures/Sources/SettingsFeature/MinAppVersionBanner.swift, BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MinAppVersionSheet.swift, BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings, BBTB/Packages/AppFeatures/Package.swift</files>
  <read_first>
    BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdBlockToggleSection.swift (public Section component pattern для RulesViewerSection sub-rows),
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ReconnectBanner.swift (lines 1-53 — exact orange-tint banner skeleton для MinAppVersionBanner),
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/EmptyStateCard.swift (lines 6-49 — icon + title + 2 buttons pattern для MinAppVersionSheet),
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionButton.swift (state-machine button pattern для ForceUpdateRulesButton),
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionTimer.swift (Timer.publish pattern для countdown tick),
    BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings (existing L10n format),
    BBTB/Packages/AppFeatures/Package.swift (нужно добавить RulesEngine как dep),
    .planning/phases/08-rules-engine-split-tunneling/08-UI-SPEC.md (§Component Inventory + §Copywriting + §Layout + §Color + §Typography для финального дизайн контракта),
    .planning/phases/08-rules-engine-split-tunneling/08-PATTERNS.md (§ 12, 13, 14, 15)
  </read_first>
  <action>(1) Edit `BBTB/Packages/AppFeatures/Package.swift` — добавить `.package(path: "../RulesEngine")` в dependencies + `"RulesEngine"` в SettingsFeature target dependencies + в MainScreenFeature target dependencies.

(2) В SettingsFeature module создать `ForceUpdateRulesButton.swift` containing:
- `public enum ForceUpdateButtonState: Equatable, Sendable { case idle; case inProgress; case cooldown(secondsRemaining: Int) }`
- `public struct ForceUpdateRulesButton: View` per UI-SPEC §Layout + §Interaction State Machine:
  - init(buttonState: ForceUpdateButtonState, statusOutcome: ForceUpdateOutcome?, onTap: @escaping () -> Void) where ForceUpdateOutcome imported from RulesEngine
  - body: VStack(spacing: 8) { Button(action: onTap) { stateLabel }.buttonStyle(.borderedProminent).tint(.accentColor).disabled(buttonState != .idle).frame(maxWidth: .infinity); статусRow }
  - private var stateLabel: View — switch buttonState: .idle → Text(L10n.rulesForceUpdateButton); .inProgress → HStack { ProgressView().tint(.white); Text(L10n.rulesForceUpdateInProgress) }; .cooldown(sec) → Text(L10n.rulesForceUpdateCooldown(sec)).monospacedDigit()
  - private func statusRow(_ outcome: ForceUpdateOutcome) -> View — HStack { icon + text }; icon/color/text per UI-SPEC §Color category-warning + §Copywriting (success=green/checkmark.circle.fill, alreadyLatest=green/checkmark.circle, networkFailure=orange/exclamationmark.triangle.fill, signatureFailure=orange/lock.slash.fill); text uses L10n.rulesForceUpdateSuccess/.noChange/.network/.signature
  - .transition(.opacity) на status row + .animation(.easeInOut(duration: 0.2)) on outcome change

(3) В SettingsFeature module создать `RulesViewerSection.swift` per UI-SPEC §Layout:
- public struct RulesViewerSection: View — init(snapshot: RulesSnapshot?)
- body: Group { if let snap = snapshot { content(snap) } else { emptyCard } }
- private func content(_ s: RulesSnapshot) -> View — VStack(spacing: 0) { Text(L10n.rulesHeaderVersion(s.version, RelativeDateTimeFormatter().localizedString(for: s.lastFetchedAt ?? Date(), relativeTo: Date()))).font(.caption).textCase(.uppercase).foregroundStyle(.secondary); CategoryGroup(category: .block, entries: s.block); CategoryGroup(category: .never, entries: s.never); CategoryGroup(category: .always, entries: s.always) }
- private struct CategoryGroup: View — internal sub-component encapsulating one category Section header + 3 DisclosureGroup (domains/ip_cidrs/countries) + per-category footer; category color per UI-SPEC §Color (block = red.opacity(0.10), never = orange, always = green)
- private struct MatcherDisclosure: View — DisclosureGroup label HStack { icon + Text(matcher name) + Spacer + count badge}; expanded content LazyVStack of entries с Text(entry).font(.body.monospaced()).textSelection(.enabled)
- emptyCard — small inline card with SF Symbol doc.text.magnifyingglass + L10n.rulesEmptyTitle + L10n.rulesEmptySubtitle (per A-17)

(4) В SettingsFeature module создать `MinAppVersionBanner.swift` modeled после `ReconnectBanner` from MainScreenFeature (but NEW file in SettingsFeature target):
- public struct MinAppVersionBanner: View — init(currentVersion: String, onTap: @escaping () -> Void)
- body: Button(action: onTap) { HStack { Image(systemName: "arrow.up.circle.fill").foregroundColor(.orange); VStack(alignment: .leading) { Text(L10n.minAppVersionBannerText); Text(L10n.minAppVersionBannerCta).font(.caption).foregroundStyle(.secondary) }; Spacer; Image(systemName: "chevron.right").foregroundStyle(.secondary).font(.caption) } }.buttonStyle(.plain) + accessibility per UI-SPEC §A-20

(5) В MainScreenFeature module создать `MinAppVersionSheet.swift` modeled после `EmptyStateCard`:
- public struct MinAppVersionSheet: View — init(currentVersion: String, onOpenTestFlight: @escaping () -> Void, onDismiss: @escaping () -> Void)
- body: VStack(spacing: 0) per UI-SPEC §Layout Specifications D-11 sheet section (icon 56pt + title + body + 2 buttons + padding 24 horizontal / 32 vertical; #if os(macOS) .frame(width: 440, height: 320))

(6) Localizable.xcstrings — добавить ~30 новых ключей per UI-SPEC §Copywriting tables. Russian и English переводы для каждого. Plural forms для `rules.count.domains` (`%lld доменов / %lld доменов / %lld доменов` для Russian few-forms + `%lld domain / %lld domains` для English).

(7) Все компоненты должны компилироваться `cd BBTB/Packages/AppFeatures && swift build`. UI preview опционально через `#Preview` block.</action>
  <verify>
    <automated>cd BBTB/Packages/AppFeatures && swift build 2>&1 | tail -5 | grep -qE 'Build complete|finished compiling' && grep -c 'public struct ForceUpdateRulesButton\|public struct RulesViewerSection\|public struct MinAppVersionBanner\|public struct MinAppVersionSheet' BBTB/Packages/AppFeatures/Sources/{SettingsFeature,MainScreenFeature}/*.swift | xargs test 4 -le && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'public struct ForceUpdateRulesButton' BBTB/Packages/AppFeatures/Sources/SettingsFeature/ForceUpdateRulesButton.swift` equals 1
    - Source: `grep -c 'public struct RulesViewerSection' BBTB/Packages/AppFeatures/Sources/SettingsFeature/RulesViewerSection.swift` equals 1
    - Source: `grep -c 'public struct MinAppVersionBanner' BBTB/Packages/AppFeatures/Sources/SettingsFeature/MinAppVersionBanner.swift` equals 1
    - Source: `grep -c 'public struct MinAppVersionSheet' BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MinAppVersionSheet.swift` equals 1
    - Source: `grep -c 'public enum ForceUpdateButtonState' BBTB/Packages/AppFeatures/Sources/SettingsFeature/ForceUpdateRulesButton.swift` equals 1
    - Source: `grep -c 'RulesEngine' BBTB/Packages/AppFeatures/Package.swift` ≥ 2 (dep + target deps)
    - Source: `grep -c 'rules.forceUpdate\|minAppVersion.sheet\|rules.matcher\|rules.section' BBTB/Packages/Localization/Sources/Localization/Localizable.xcstrings` ≥ 20 (subset of ~30 keys)
    - Source: `grep -c 'arrow.up.app.fill\|arrow.up.circle.fill' BBTB/Packages/AppFeatures/Sources/{SettingsFeature,MainScreenFeature}/*.swift` ≥ 2 (sheet icon + banner icon)
    - Source: `grep -c 'textSelection(.enabled)' BBTB/Packages/AppFeatures/Sources/SettingsFeature/RulesViewerSection.swift` ≥ 1 (UI-SPEC A-07 invariant)
    - CLI: `cd BBTB/Packages/AppFeatures && swift build` exits 0
  </acceptance_criteria>
  <done>4 новых SwiftUI компонента созданы, скомпилированы, L10n keys в xcstrings, package dependencies в AppFeatures обновлены. Никаких ViewModel/wiring изменений в этой задаче.</done>
</task>

<task type="auto">
  <name>Task W3.2: Extend SettingsViewModel + MainScreenViewModel + wire AdvancedSettingsView + MainScreenView</name>
  <files>BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift, BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdvancedSettingsView.swift, BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift, BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenView.swift</files>
  <read_first>
    BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift (Phase 6 @Published + @AppStorage extension pattern для extension; lines 181-214 applyAutoReconnectToManager late-bind pattern),
    BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdvancedSettingsView.swift (lines 19-37 — existing Form Section pattern),
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift (lines 205-221 — NEVPNStatusDidChange observer queue=nil pattern),
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenView.swift (existing .sheet modifier pattern),
    BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesEngineCoordinator.swift (W2 — exact API),
    .planning/phases/08-rules-engine-split-tunneling/08-UI-SPEC.md (§Interaction Patterns 3, 4, 5; §Edge Cases для cooldown wallclock),
    .planning/phases/08-rules-engine-split-tunneling/08-PATTERNS.md (§ 18 + § 19 + § S-1 + § S-2)
  </read_first>
  <action>(1) Extend SettingsViewModel.swift. Добавить (in same class, после существующих @AppStorage props):
- `@Published public private(set) var rulesSnapshot: RulesSnapshot?`
- `@Published public private(set) var rulesVersion: Int = 0`
- `@Published public private(set) var rulesLastFetchedAt: Date?`
- `@Published public private(set) var forceUpdateButtonState: ForceUpdateButtonState = .idle`
- `@Published public private(set) var forceUpdateStatusOutcome: ForceUpdateOutcome?` (RulesEngine.ForceUpdateOutcome)
- `@Published public private(set) var showMinAppVersionBanner: Bool = false`
- `public weak var rulesEngineCoordinator: RulesEngineCoordinator?` — late-bound
- `@AppStorage("app.bbtb.minAppVersion.dismissed") private var dismissedMinAppVersion: String = ""` — per-version dismissal flag
- `private var cooldownExpiresAt: Date?`
- `private var cooldownTimer: Timer?`
- `private var statusOutcomeAutoDismissTask: Task<Void, Never>?`
- `private var rulesUpdateObserver: NSObjectProtocol?`
- `public var currentAppVersion: String { Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String ?? "0.0.0" }`

Add methods:
- `public func wireRulesCoordinator(_ coordinator: RulesEngineCoordinator) async`:
  - self.rulesEngineCoordinator = coordinator
  - if let snap = await coordinator.currentSnapshot() { await applySnapshot(snap) }
  - register observer (queue: nil per S-1) for `.bbtbRulesEngineDidUpdate`; on fire → Task { @MainActor in self.refreshFromCoordinator() }
- `private func refreshFromCoordinator() async`:
  - guard let coord = rulesEngineCoordinator else { return }
  - if let snap = await coord.currentSnapshot() { await applySnapshot(snap) }
- `private func applySnapshot(_ snapshot: RulesSnapshot) async` (@MainActor):
  - self.rulesSnapshot = snapshot
  - self.rulesVersion = snapshot.version
  - self.rulesLastFetchedAt = snapshot.lastFetchedAt
  - self.showMinAppVersionBanner = (snapshot.minAppVersion.compare(currentAppVersion, options: .numeric) == .orderedDescending)
- `public func triggerForceUpdate() async`:
  - guard forceUpdateButtonState == .idle else { return }   // race guard
  - #if os(iOS) UIImpactFeedbackGenerator(style: .light).impactOccurred() #endif
  - self.forceUpdateButtonState = .inProgress
  - let outcome = await rulesEngineCoordinator?.forceUpdate() ?? .networkFailure
  - await applyForceUpdateOutcome(outcome)
- `private func applyForceUpdateOutcome(_ outcome: ForceUpdateOutcome) async` (@MainActor):
  - self.forceUpdateStatusOutcome = outcome
  - switch outcome:
    - .cooldownActive(secondsRemaining: sec): self.forceUpdateButtonState = .cooldown(secondsRemaining: sec); cooldownExpiresAt = Date().addingTimeInterval(TimeInterval(sec)); startCooldownTimer()
    - other: cooldownExpiresAt = Date().addingTimeInterval(60); self.forceUpdateButtonState = .cooldown(secondsRemaining: 60); startCooldownTimer()
  - schedule statusOutcomeAutoDismissTask = Task { try? await Task.sleep(for: .seconds(4)); await MainActor.run { self.forceUpdateStatusOutcome = nil } }
  - на refresh (success outcome) → call refreshFromCoordinator() for fresh snapshot
- `private func startCooldownTimer()`:
  - cooldownTimer?.invalidate()
  - cooldownTimer = Timer.scheduledTimer(withTimeInterval: 1.0, repeats: true) { [weak self] _ in
        Task { @MainActor in self?.cooldownTick() }
    }
- `private func cooldownTick()` (@MainActor):
  - guard let expiresAt = cooldownExpiresAt else { return }
  - let remaining = Int(expiresAt.timeIntervalSince(Date()))
  - if remaining <= 0 { cooldownTimer?.invalidate(); cooldownTimer = nil; cooldownExpiresAt = nil; self.forceUpdateButtonState = .idle }
  - else { self.forceUpdateButtonState = .cooldown(secondsRemaining: remaining) }
- `public func openTestFlight()`:
  - let url = URL(string: "https://testflight.apple.com/join/PLACEHOLDER")!
  - #if os(iOS) UIApplication.shared.open(url) #else NSWorkspace.shared.open(url) #endif
  - if let v = self.rulesSnapshot?.minAppVersion { self.dismissedMinAppVersion = v }
- `deinit { rulesUpdateObserver.map { NotificationCenter.default.removeObserver($0) }; cooldownTimer?.invalidate(); statusOutcomeAutoDismissTask?.cancel() }`

(2) Rewrite AdvancedSettingsView.swift body — Form with 4 sections в order per UI-SPEC §Layout:
```
Form {
    // 1. Persistent MinAppVersion banner (conditional, top of Form)
    if viewModel.showMinAppVersionBanner {
        Section {
            MinAppVersionBanner(currentVersion: viewModel.currentAppVersion, onTap: viewModel.openTestFlight)
        }
        .listRowBackground(Color.orange.opacity(0.15))
    }

    // 2. DNS section (existing — unchanged)
    Section { AdBlockToggleSection(isOn: $viewModel.adBlockEnabled, footerText: L10n.settingsDnsAdblockFooter); CustomDNSField(text: $viewModel.customDNS) }
    header: { Text(L10n.settingsDnsSection) } footer: { Text(L10n.settingsDnsCustomFooter) }

    // 3. Rules viewer (NEW)
    Section { RulesViewerSection(snapshot: viewModel.rulesSnapshot) }
    header: { Text(L10n.rulesSection(viewModel.rulesVersion, viewModel.rulesLastFetchedAtRelative)).textCase(.uppercase) }

    // 4. Force-update button + footer (NEW)
    Section {
        ForceUpdateRulesButton(buttonState: viewModel.forceUpdateButtonState, statusOutcome: viewModel.forceUpdateStatusOutcome,
                               onTap: { Task { await viewModel.triggerForceUpdate() } })
    }
    header: { Text(L10n.rulesForceUpdateSection).textCase(.uppercase) }
    footer: { Text(L10n.rulesForceUpdateFooter) }
}
.navigationTitle(L10n.settingsAdvancedTitle)
```

(3) Extend MainScreenViewModel.swift:
- `@Published public private(set) var showMinAppVersionSheet: Bool = false`
- `public weak var rulesEngineCoordinator: RulesEngineCoordinator?`
- `@AppStorage("app.bbtb.minAppVersion.dismissed") private var dismissedMinAppVersion: String = ""`
- `private var rulesUpdateObserver: NSObjectProtocol?`
- `public func wireRulesCoordinator(_ coordinator: RulesEngineCoordinator) async`:
  - self.rulesEngineCoordinator = coordinator
  - register observer for `.bbtbRulesEngineDidUpdate` queue: nil → Task { @MainActor in self.handleMinAppVersionCheck() }
- `public func handleMinAppVersionCheck() async`:
  - guard let coord = rulesEngineCoordinator, let snap = await coord.currentSnapshot() else { return }
  - let current = Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String ?? "0.0.0"
  - let needsUpgrade = current.compare(snap.minAppVersion, options: .numeric) == .orderedAscending
  - let alreadyDismissed = dismissedMinAppVersion == snap.minAppVersion
  - if needsUpgrade && !alreadyDismissed { await MainActor.run { self.showMinAppVersionSheet = true } }
- `public func dismissMinAppVersionSheet()`:
  - self.showMinAppVersionSheet = false
  - if let v = (Task { await rulesEngineCoordinator?.currentSnapshot()?.minAppVersion })... — упрощённо: фиксировать current snapshot.minAppVersion в @AppStorage dismissedMinAppVersion в момент dismiss. Используем computed @MainActor sync: `if let snap = currentLastObservedSnapshot { dismissedMinAppVersion = snap.minAppVersion }`. Можно хранить last observed как private @MainActor var.

(4) Extend MainScreenView.swift:
- Добавить `.sheet(isPresented: $viewModel.showMinAppVersionSheet) { MinAppVersionSheet(currentVersion: viewModel.currentAppVersion, onOpenTestFlight: { viewModel.dismissMinAppVersionSheet(); viewModel.openTestFlight() }, onDismiss: viewModel.dismissMinAppVersionSheet) }`
- На sheet добавить `.presentationDetents([.medium]).presentationDragIndicator(.visible).presentationBackgroundInteraction(.disabled)` — per UI-SPEC §Layout
- Добавить `.task { await viewModel.handleMinAppVersionCheck() }` — Phase 6d pattern (НЕ .onAppear для cold-start)

(5) Все edits должны компилироваться. swift build green для AppFeatures.</action>
  <verify>
    <automated>cd BBTB/Packages/AppFeatures && swift build 2>&1 | tail -5 | grep -qE 'Build complete|finished compiling' && grep -c 'wireRulesCoordinator\|triggerForceUpdate\|handleMinAppVersionCheck' BBTB/Packages/AppFeatures/Sources/{SettingsFeature,MainScreenFeature}/*.swift | xargs test 4 -le && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'public func wireRulesCoordinator' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift` equals 1
    - Source: `grep -c 'public func wireRulesCoordinator' BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift` equals 1
    - Source: `grep -c 'public func triggerForceUpdate' BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift` equals 1
    - Source: `grep -c 'public func handleMinAppVersionCheck' BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift` equals 1
    - Source: `grep -c 'queue: nil' BBTB/Packages/AppFeatures/Sources/{SettingsFeature,MainScreenFeature}/*ViewModel.swift` ≥ 2 (per S-1)
    - Source: `grep -c 'RulesViewerSection\|ForceUpdateRulesButton\|MinAppVersionBanner' BBTB/Packages/AppFeatures/Sources/SettingsFeature/AdvancedSettingsView.swift` ≥ 3
    - Source: `grep -c 'MinAppVersionSheet' BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenView.swift` equals 1
    - Source: `grep -c 'presentationDetents\|presentationDragIndicator' BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenView.swift` ≥ 2
    - Source: `grep -c 'dismissedMinAppVersion' BBTB/Packages/AppFeatures/Sources/{SettingsFeature,MainScreenFeature}/*ViewModel.swift` ≥ 2
    - CLI: `cd BBTB/Packages/AppFeatures && swift build` exits 0
  </acceptance_criteria>
  <done>ViewModels extended with late-bind setters + force-update flow + min_app_version check; AdvancedSettingsView и MainScreenView wired to new components; Observer pattern уважает queue=nil invariant из Phase 6c.</done>
</task>

<task type="auto">
  <name>Task W3.3: ViewModel unit tests — SettingsViewModelTests + MinAppVersionTests + ForceUpdateButtonStateTests</name>
  <files>BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/SettingsViewModelTests.swift, BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/MinAppVersionTests.swift, BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/ForceUpdateButtonStateTests.swift</files>
  <read_first>
    BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/SettingsViewModelDNSTests.swift (existing pattern для @MainActor XCTest + UserDefaults reset),
    BBTB/Packages/RulesEngine/Tests/RulesEngineTests/RulesEngineCoordinatorTests.swift (W2 FakeFetcher/FixedClock mock pattern для reuse),
    BBTB/Packages/AppFeatures/Sources/SettingsFeature/SettingsViewModel.swift (just-extended — exact API),
    BBTB/Packages/AppFeatures/Sources/SettingsFeature/ForceUpdateRulesButton.swift (state enum cases)
  </read_first>
  <action>(1) `ForceUpdateButtonStateTests.swift` — pure unit tests на state enum + cooldown logic без VM:
- test_initialState_isIdle
- test_inProgress_blocksAdditionalTaps — assert .inProgress != .idle (Equatable check)
- test_cooldown_60s_isNotIdle — assert .cooldown(secondsRemaining: 60) != .idle
- test_cooldown_decrements — given cooldownExpiresAt = Date() + 60s, simulate Timer fire 5x at 1s intervals → state should be .cooldown(55..59)
- test_cooldown_zeroSeconds_transitionsToIdle — cooldownExpiresAt в прошлом → next tick → state == .idle
- test_wallclock_resumption — cooldownExpiresAt: Date в future + 30s; suspend (no ticks); resume + check → remaining ≈ 30 (within ±2s grace)

(2) `MinAppVersionTests.swift` — Comparison + dismissal logic:
- test_currentBelowMin_returnsTrue — "1.0.5".compare("1.2.0", options: .numeric) == .orderedAscending (needsUpgrade=true)
- test_currentEqualToMin_returnsFalse — "1.2.0".compare("1.2.0", .numeric) == .orderedSame
- test_currentAboveMin_returnsFalse — "1.2.10".compare("1.2.0", .numeric) == .orderedDescending
- test_numericSemverComparison_handles_1.2.10_greater_than_1.2.9 — critical: "1.2.10".compare("1.2.9", .numeric) == .orderedDescending (NOT orderedAscending — that's lex sort)
- test_numericSemverComparison_handles_1.10.0_greater_than_1.9.9 — same lexicographic trap
- test_dismissedVersion_skipsSheet — @AppStorage dismissedMinAppVersion == "1.2.0"; current snapshot.minAppVersion == "1.2.0" → showSheet stays false

(3) `SettingsViewModelTests.swift` — VM integration tests (@MainActor):
- test_wireCoordinator_initializesSnapshotPublishing — wire fake coordinator с pre-loaded snapshot → assert vm.rulesSnapshot != nil, vm.rulesVersion == fake snapshot version
- test_notificationFires_refreshesSnapshot — wire; fake coord swap snapshot internally; post .bbtbRulesEngineDidUpdate → assert vm.rulesSnapshot updated (через expectation/await fulfillment)
- test_triggerForceUpdate_idleToInProgress_toCooldown — wire fake; vm.forceUpdateButtonState == .idle initially; await vm.triggerForceUpdate() → fake returns .success(42) → state should be .cooldown(60) + statusOutcome == .success(42)
- test_triggerForceUpdate_raceGuard_secondCallIsNoop — wire fake; trigger twice rapidly; assert fake.forceUpdate invoked ровно 1 раз
- test_showMinAppVersionBanner_setWhenMinExceedsCurrent — wire; snapshot.minAppVersion = "99.0.0" (future) > current "0.8.0" → showMinAppVersionBanner == true
- test_showMinAppVersionBanner_falseWhenCurrentMeetsMin — snapshot.minAppVersion = "0.8.0" == current → false
- test_statusOutcome_autoDismisses_after_4_seconds — trigger; assert statusOutcome != nil; wait 5s; assert statusOutcome == nil (use try await Task.sleep(for: .seconds(5)))

Use FakeRulesEngineCoordinator stub — implement minimal subset that matches actor RulesEngineCoordinator's public API (currentSnapshot / forceUpdate / performBackgroundRefresh / bootstrap). Может быть actor итак, либо class with @unchecked Sendable wrapping. Reuse from RulesEngineTests если возможно (test target dependency).</action>
  <verify>
    <automated>cd BBTB/Packages/AppFeatures && swift test --filter SettingsFeatureTests 2>&1 | tail -10 | grep -E "Executed [0-9]+ tests, with 0 failures"</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `cd BBTB/Packages/AppFeatures && swift test --filter SettingsFeatureTests` exits 0 with ≥ 17 tests passed (6 ForceUpdate + 6 MinAppVersion + 7 SettingsViewModel — ≥ 17 total) and 0 failures
    - Source: `grep -cE 'func test_(initialState|inProgress|cooldown|wallclock)' BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/ForceUpdateButtonStateTests.swift` ≥ 6
    - Source: `grep -cE 'func test_(currentBelowMin|currentEqualToMin|currentAboveMin|numericSemver|dismissedVersion)' BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/MinAppVersionTests.swift` ≥ 5
    - Source: `grep -cE 'func test_(wireCoordinator|notificationFires|triggerForceUpdate|showMinAppVersionBanner|statusOutcome_autoDismisses)' BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/SettingsViewModelTests.swift` ≥ 6
    - Source: `grep -c '1.2.10' BBTB/Packages/AppFeatures/Tests/SettingsFeatureTests/MinAppVersionTests.swift` ≥ 2 (critical semver edge case covered)
  </acceptance_criteria>
  <done>≥ 17 new tests covering force-update state machine, semver numeric comparison (включая ловушки 1.2.10 > 1.2.9), ViewModel late-bind + observer integration, race guard, auto-dismiss timing. All SettingsFeatureTests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ViewModel state → UI render | @Published + @MainActor isolation prevents render-time data race |
| User tap → coordinator action | Race guard `guard buttonState == .idle` blocks repeated taps; cooldown enforced server-side too |
| TestFlight URL constant | Hardcoded placeholder в W3; Phase 12 substitutes real invite token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-W3-01 | DoS | User mass-taps force-update button | mitigate | Race guard `guard buttonState == .idle else { return }` (UI-SPEC §Edge Cases) + coordinator cooldown 60s + UI shows disabled state with countdown |
| T-08-W3-02 | Spoofing | Malicious rules.json injecting HTML/JS into entry rendering | mitigate | Все entry rendering через SwiftUI `Text(entry)` — auto-escapes; NEVER `Text(LocalizedStringKey(entry))` который интерпретировал бы markdown; UI-SPEC §Security explicitly states this |
| T-08-W3-03 | DoS | Extremely long domain string breaks layout | accept | `.monospaced()` + wraps по default; `LazyVStack` для 10000+ entries; no overflow crash |
| T-08-W3-04 | Tampering | User dismisses min_app_version sheet & coordinator changes minAppVersion to even higher value next refresh | mitigate | `dismissedMinAppVersion` @AppStorage is per-version; новый increase → sheet re-presents because dismissedMinAppVersion != new snapshot.minAppVersion |
| T-08-W3-05 | Information disclosure | NotificationCenter observer queue=.main loses notifications when app suspended | mitigate | Observer queue=nil + Task @MainActor hop inside callback — per Phase 6c memory feedback_nevpn_observer_queue_main.md |
| T-08-W3-06 | Spoofing | Wrong actor → ViewModel mutation race | mitigate | RulesEngineCoordinator is actor (W2); cross-actor calls inside `await coord.method()`; ViewModel @MainActor isolated; explicit Task { @MainActor } hops |
| T-08-W3-07 | Information disclosure | TestFlight invite URL leaked в open-source repo | accept | Phase 8 W3 hardcodes PLACEHOLDER; Phase 12 substitutes real URL — at that point repo may go non-public, либо invite URL is meant to be public (TestFlight semantics) |
| T-08-W3-08 | Repudiation | User unsure if force-update succeeded | mitigate | Inline status row с auto-dismiss 4s + persistent badge updates с new version в Rules viewer; haptic feedback на tap; ProgressView during in-flight |
</threat_model>

<verification>
- ✓ 4 SwiftUI компонента созданы, скомпилированы, layout match UI-SPEC
- ✓ ~30 L10n keys добавлены в Localizable.xcstrings (ru + en)
- ✓ AdvancedSettingsView перестроен: 4 секции в order Banner→DNS→Rules→Force-update
- ✓ MainScreenView добавил sheet modifier
- ✓ ViewModels с late-bind setters, observer queue=nil, race guards
- ✓ ≥ 17 new unit tests passing
- ✓ swift build + swift test AppFeatures green
</verification>

<success_criteria>
1. **Rules viewer functional:** Пользователь видит current rules version + last-fetched relative timestamp + 3 категории с DisclosureGroup expand/collapse + entry counts + .textSelection(.enabled) для copy support tickets.
2. **Force-update button functional:** state machine .idle/.inProgress/.cooldown wires correctly; cooldown countdown ticks каждую секунду + survives background-foreground re-entry через wallclock comparison; haptic feedback iOS only.
3. **Inline status feedback:** success/alreadyLatest/networkFailure/signatureFailure outcomes displayed с appropriate icons + auto-dismiss 4s; никаких alerts.
4. **Min app version flow:** modal sheet + persistent banner; sheet dismisses → @AppStorage persists dismissal per-version; banner stays.
5. **Wiring complete:** SettingsViewModel + MainScreenViewModel связаны с RulesEngineCoordinator через late-bind setter; observer queue=nil pattern respected.
6. **Tests green:** ≥ 17 new tests + все existing AppFeatures tests passing (regression preserved).
7. **L10n contract:** ~30 новых keys в xcstrings с Russian + English переводами; plural forms для count badges.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rules-engine-split-tunneling/08-W3-SUMMARY.md` со:
- New files list (4 SwiftUI components + 3 test files)
- Modified files (2 ViewModels + 2 Views + xcstrings + Package.swift)
- L10n key count added
- TestFlight URL TODO reminder для Phase 12
- Test count breakdown
- Manual UAT instructions (preview AdvancedSettingsView на iOS Simulator)
</output>
