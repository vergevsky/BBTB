---
phase: 08-rules-engine-split-tunneling
plan: W5
type: execute
wave: 6
depends_on: [W4]
files_modified:
  - BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift
  - BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift
autonomous: true
requirements: [RULES-05, RULES-06, RULES-07]
mvp_slice: "Vertical slice #5 — sing-box engine физически читает rules cache из App Group. После W5 user видит реальный split-tunnel поведение: домены из block_completely → reject в tunnel; домены из never_through_vpn → direct outbound; домены из always_through_vpn → urltest VPN outbound. Это THE moment где Phase 8 становится functional product (W2 pipeline → W3 UI → W4 BG-fetch → W5 engine apply)."
user_setup: []

must_haves:
  truths:
    - "SingBoxConfigLoader.expandConfigForTunnel injects 3 `route.rule_set` entries (block / never / always) с `type: \"local\"`, `format: \"binary\"`, `path: AppGroupContainer.rulesCacheDirectory/<filename>.srs`"
    - "expandConfigForTunnel injects 3 `route.rules` priority rules: block → action: reject; never → outbound: direct; always → outbound: <urltest-tag from existing config>"
    - "Priority order top-down: 1. block (reject) → 2. never (direct) → 3. always (urltest) — sing-box matches first-hit, никакого ambiguity"
    - "Rule_set inject ИДЕМПОТЕНТЕН: повторный вызов expandConfigForTunnel не добавляет duplicate rule_sets (existing tags filtered)"
    - "Post-expand `validate(json:)` passes — R10 invariant preserved (rule_set entries не открывают forbidden inbounds, action 'reject' допустим)"
    - "R1 invariant preserved — template файлы (`SingBoxConfigTemplate.vless-reality.json` etc.) не содержат inline `rule_set` keys (только runtime expansion)"
    - "При absent SRS files (cache не bootstrapped — edge case) rule_set entries injected с paths которые resolveable но sing-box graceful-fails reload (libbox documented behavior: missing file → warning log, не tunnel kill)"
    - "Unit tests: 6 new tests в SingBoxConfigLoaderTests — injection / ordering / idempotency / validate-after / firstProxyTag-reuse / templateBareCheck"
  artifacts:
    - path: "BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift"
      provides: "Extended expandConfigForTunnel с Phase 8 rule_set + priority rules injection"
      contains: "rule_set"
    - path: "BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift"
      provides: "6 new tests covering RULES-05/06/07 + R10 invariant preservation"
      contains: "testRulesetInjection"
  key_links:
    - from: "SingBoxConfigLoader.expandConfigForTunnel"
      to: "AppGroupContainer.rulesCacheDirectory"
      via: "URL path construction для rule_set entries"
      pattern: "AppGroupContainer.rulesCacheDirectory"
    - from: "route.rules priority order"
      to: "RulesManifest.Category enum (block/never/always)"
      via: "tag matching bbtb-block / bbtb-never / bbtb-always"
      pattern: "bbtb-(block|never|always)"
    - from: "always rule outbound"
      to: "first proxy outbound tag (urltest-auto for protocol failover)"
      via: "reuse existing logic at line 219-223 SingBoxConfigLoader"
      pattern: "firstProxyTag"
---

<objective>
Plan W5 — vertical slice #5: финальный кусок — sing-box engine видит и применяет cache. Cyber-route flow: VPS rules → main app fetches (W2) → user opens app (W3+W4) → cache landed at App Group → extension starts tunnel → SingBoxConfigLoader.expandConfigForTunnel injects rule_set entries pointing к cache files → sing-box parses .srs → libbox fswatch.Watcher (1.10+) auto-reloads на mtime change. Это завершает «end-to-end working» MVP loop.

Purpose: separate engine integration (W5) от everything else. Это самый rigid invariant-controlled change в Phase 8: SingBoxConfigLoader — R1/R10-critical path; tests должны verify что expansion не открывает SOCKS5 inbound (R1) и validate() passes после inject (R10). Все priority rules идут в правильном порядке (sing-box matches top-down).

Output: ONE source file modification + ONE test file extension. По выходу из W5 — на reality test device с baseline-rules-manifest.json в bundle → connect tunnel → wireshark/console показывает domain matching working (M-05 manual UAT в W7).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-rules-engine-split-tunneling/08-CONTEXT.md
@.planning/phases/08-rules-engine-split-tunneling/08-RESEARCH.md
@.planning/phases/08-rules-engine-split-tunneling/08-PATTERNS.md
@.planning/phases/08-rules-engine-split-tunneling/08-W2-PLAN.md
@BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift
@BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/Resources/SingBoxConfigTemplate.vless-reality.json
@BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/AppGroupContainer.swift
@BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift
@BBTB/scripts/validate-r1-r6.sh

<interfaces>
<!-- W5 modifies existing SingBoxConfigLoader public API surface non-breaking -->
- public static func expandConfigForTunnel(_ json: String) throws -> String
  — same signature; behaviorally extended:
    + adds route.rule_set entries (bbtb-block / bbtb-never / bbtb-always) с type:"local" format:"binary" path:"AppGroupContainer.rulesCacheDirectory/<file>.srs"
    + adds 3 route.rules priority entries (in this order):
      1. {"rule_set": "bbtb-block",  "action": "reject"}
      2. {"rule_set": "bbtb-never",  "outbound": "direct"}
      3. {"rule_set": "bbtb-always", "outbound": "<firstProxyTag>"}
    + idempotent: повторный вызов не дублирует entries (check existing tags before append)

Required existing infrastructure:
- AppGroupContainer.rulesCacheDirectory (W2.1) — provides path
- Template `SingBoxConfigTemplate.vless-reality.json` — should NOT have inline rule_set (W7 invariant check)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task W5.1: Extend SingBoxConfigLoader.expandConfigForTunnel — inject rule_set + priority rules</name>
  <files>BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift</files>
  <read_first>
    BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift (full file — особенно lines 136-243 — existing expandConfigForTunnel chain; lines 219-223 — firstProxyTag resolution logic для reuse; lines 228-237 — sniff insertion pattern для inject location reference),
    BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/Resources/SingBoxConfigTemplate.vless-reality.json (full file — для understanding existing route block structure),
    BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/AppGroupContainer.swift (W2.1 — rulesCacheDirectory property),
    .planning/phases/08-rules-engine-split-tunneling/08-RESEARCH.md (§ Pattern 1 — точный inject JSON structure; § Pitfall 4-7),
    .planning/phases/08-rules-engine-split-tunneling/08-PATTERNS.md (§ 16 — точный inject pseudo-code template)
  </read_first>
  <action>В `SingBoxConfigLoader.swift` add NEW step в `expandConfigForTunnel(_:)` chain ПОСЛЕ existing sniff insertion (~line 237) и ПЕРЕД final serialization (~line 239). Step 5:

```swift
// 5. Phase 8 D-01 — inject 3 route.rule_set entries + 3 priority rules
//    Idempotent: проверяет existing tags перед append.
//    Order сохраняется: block (reject) → never (direct) → always (urltest).
//    AppGroupContainer.rulesCacheDirectory резолвится в extension process — same path как main app писатель.
if var route = root["route"] as? [String: Any] {
    // 5a. Inject rule_set declarations
    var ruleSets = (route["rule_set"] as? [[String: Any]]) ?? []
    let existingTags: Set<String> = Set(ruleSets.compactMap { $0["tag"] as? String })
    let rulesDir = AppGroupContainer.rulesCacheDirectory.path
    let categories: [(tag: String, file: String)] = [
        ("bbtb-block",  "bbtb-block.srs"),
        ("bbtb-never",  "bbtb-never.srs"),
        ("bbtb-always", "bbtb-always.srs"),
    ]
    for (tag, file) in categories where !existingTags.contains(tag) {
        ruleSets.append([
            "tag": tag,
            "type": "local",
            "format": "binary",
            "path": "\(rulesDir)/\(file)"
        ])
    }
    route["rule_set"] = ruleSets

    // 5b. Inject priority rules (top-down match order)
    var rules = (route["rules"] as? [[String: Any]]) ?? []
    let existingRuleSetRefs: Set<String> = Set(rules.compactMap { $0["rule_set"] as? String })

    // Resolve firstProxyTag via existing logic (мирится с line 219-223 reuse).
    let outbounds = (root["outbounds"] as? [[String: Any]]) ?? []
    let firstProxyTag: String = outbounds.first { o in
        guard let t = o["type"] as? String else { return false }
        return proxyOutboundTypes.contains(t)
    }?["tag"] as? String ?? "vless-out"

    // Insertion idx — after sniff + hijack-dns rules (typically idx 2).
    let insertIdx = rules.firstIndex { ($0["action"] as? String) == "hijack-dns" }.map { $0 + 1 } ?? 0
    var newRules: [[String: Any]] = []
    if !existingRuleSetRefs.contains("bbtb-block")  { newRules.append(["rule_set": "bbtb-block",  "action": "reject"]) }
    if !existingRuleSetRefs.contains("bbtb-never")  { newRules.append(["rule_set": "bbtb-never",  "outbound": "direct"]) }
    if !existingRuleSetRefs.contains("bbtb-always") { newRules.append(["rule_set": "bbtb-always", "outbound": firstProxyTag]) }
    rules.insert(contentsOf: newRules, at: insertIdx)
    route["rules"] = rules
    root["route"] = route
}
```

Key implementation notes:
- `AppGroupContainer.rulesCacheDirectory` — must be imported via `import PacketTunnelKit` (но это SAM modular — SingBoxConfigLoader живёт в PacketTunnelKit и AppGroupContainer тоже там, no import needed).
- Идемпотентность — фундаментально. `expandConfigForTunnel` called дважды (W3.1 R10 invariant test уже проверяет) — second call MUST be no-op.
- `firstProxyTag` reuse уже existing logic — don't duplicate `proxyOutboundTypes` set; reference existing `private static let proxyOutboundTypes: Set<String>` (lines 50-75).
- Post-expand `validate(json:)` (called from BaseSingBoxTunnel.startTunnel after expand) уже не пропускает forbidden inbounds. action:"reject" — это action, не inbound type. AppGroupContainer path — resolveable на extension side. R1 preserved.
- Doc-comment поверх `expandConfigForTunnel` (если existing comment блок есть) — добавить line «Phase 8: also injects route.rule_set + 3 priority rules pointing to AppGroup SRS cache.»

Add helper test-only computed property (if useful для tests — optional):
- `internal static var phase8InjectedRuleTags: [String] { ["bbtb-block", "bbtb-never", "bbtb-always"] }` — для test assertion reuse</action>
  <verify>
    <automated>cd BBTB/Packages/PacketTunnelKit && swift build 2>&1 | tail -3 | grep -qE 'Build complete|finished compiling' && grep -c 'rule_set' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift | xargs test 3 -le && grep -c 'AppGroupContainer.rulesCacheDirectory' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift | xargs test 1 -eq && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'rule_set' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift` ≥ 3 (key in declaration + reference in rules)
    - Source: `grep -c 'bbtb-block\|bbtb-never\|bbtb-always' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift` ≥ 3
    - Source: `grep -c 'AppGroupContainer.rulesCacheDirectory' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift` equals 1
    - Source: `grep -c '"action": "reject"' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift` ≥ 1
    - Source: `grep -c '"outbound": "direct"' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift` ≥ 1
    - Source: `grep -c 'firstProxyTag\|firstProxyTag = outbounds' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift` ≥ 1
    - Source: `grep -c 'existingTags\|existingRuleSetRefs' BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift` ≥ 2 (idempotency guards)
    - CLI: `cd BBTB/Packages/PacketTunnelKit && swift build` exits 0
  </acceptance_criteria>
  <done>SingBoxConfigLoader.expandConfigForTunnel extended; injection idempotent; firstProxyTag reused от existing logic; build green. Tests follow in next task.</done>
</task>

<task type="auto">
  <name>Task W5.2: Add 6 SingBoxConfigLoaderTests covering RULES-05/06/07 + R10 invariant + idempotency</name>
  <files>BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift</files>
  <read_first>
    BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift (existing tests file — pure-function XCTest pattern; particularly existing testValidateConfig... tests для structure reference),
    BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift (just-updated в W5.1 — exact behavior reference),
    BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/Resources/SingBoxConfigTemplate.vless-reality.json (template для loadTemplate-based test fixture),
    .planning/phases/08-rules-engine-split-tunneling/08-VALIDATION.md (§ Per-Task Verification Map — R10 invariant test command + RULES-05/06/07 test names)
  </read_first>
  <action>В существующий `SingBoxConfigLoaderTests.swift` добавить 6 новых XCTest method:

```swift
// MARK: - Phase 8 — rule_set injection tests (RULES-05/06/07 + R10)

func test_expandConfigForTunnel_injectsThreeRuleSetEntries() throws {
    let baseTemplate = try SingBoxConfigLoader.loadVLESSRealityTemplate()
    let expanded = try SingBoxConfigLoader.expandConfigForTunnel(baseTemplate)
    let parsed = try XCTUnwrap(JSONSerialization.jsonObject(with: Data(expanded.utf8)) as? [String: Any])
    let route = try XCTUnwrap(parsed["route"] as? [String: Any])
    let ruleSets = try XCTUnwrap(route["rule_set"] as? [[String: Any]])
    let tags = ruleSets.compactMap { $0["tag"] as? String }
    XCTAssertEqual(Set(tags), Set(["bbtb-block", "bbtb-never", "bbtb-always"]))
    for rs in ruleSets {
        XCTAssertEqual(rs["type"] as? String, "local")
        XCTAssertEqual(rs["format"] as? String, "binary")
        let path = try XCTUnwrap(rs["path"] as? String)
        XCTAssertTrue(path.contains("Library/Caches/rules/"), "Expected AppGroup path, got \(path)")
        XCTAssertTrue(path.hasSuffix(".srs"))
    }
}

func test_expandConfigForTunnel_priorityOrderIsBlockThenNeverThenAlways() throws {
    let template = try SingBoxConfigLoader.loadVLESSRealityTemplate()
    let expanded = try SingBoxConfigLoader.expandConfigForTunnel(template)
    let parsed = try XCTUnwrap(JSONSerialization.jsonObject(with: Data(expanded.utf8)) as? [String: Any])
    let route = try XCTUnwrap(parsed["route"] as? [String: Any])
    let rules = try XCTUnwrap(route["rules"] as? [[String: Any]])
    // Extract just our 3 rule_set references in their order
    let phase8 = rules.compactMap { $0["rule_set"] as? String }
    XCTAssertEqual(phase8, ["bbtb-block", "bbtb-never", "bbtb-always"], "Phase 8 priority order is block > never > always")
    // Block uses reject
    let blockRule = try XCTUnwrap(rules.first { ($0["rule_set"] as? String) == "bbtb-block" })
    XCTAssertEqual(blockRule["action"] as? String, "reject")
    // Never uses direct
    let neverRule = try XCTUnwrap(rules.first { ($0["rule_set"] as? String) == "bbtb-never" })
    XCTAssertEqual(neverRule["outbound"] as? String, "direct")
    // Always uses urltest proxy tag
    let alwaysRule = try XCTUnwrap(rules.first { ($0["rule_set"] as? String) == "bbtb-always" })
    let alwaysOutbound = try XCTUnwrap(alwaysRule["outbound"] as? String)
    XCTAssertNotEqual(alwaysOutbound, "direct")  // must NOT be direct
    XCTAssertNotEqual(alwaysOutbound, "block")
}

func test_expandConfigForTunnel_alwaysCategoryUsesSameProxyTagAsFinal() throws {
    let template = try SingBoxConfigLoader.loadVLESSRealityTemplate()
    let expanded = try SingBoxConfigLoader.expandConfigForTunnel(template)
    let parsed = try XCTUnwrap(JSONSerialization.jsonObject(with: Data(expanded.utf8)) as? [String: Any])
    let route = try XCTUnwrap(parsed["route"] as? [String: Any])
    let rules = try XCTUnwrap(route["rules"] as? [[String: Any]])
    let alwaysRule = try XCTUnwrap(rules.first { ($0["rule_set"] as? String) == "bbtb-always" })
    let alwaysOutbound = try XCTUnwrap(alwaysRule["outbound"] as? String)
    // resolved firstProxyTag must match Phase 1+ outbound naming convention
    let outbounds = try XCTUnwrap(parsed["outbounds"] as? [[String: Any]])
    let proxyTags = outbounds.compactMap { o -> String? in
        guard let type = o["type"] as? String, ["vless", "trojan", "shadowsocks", "hysteria2", "tuic", "urltest", "selector"].contains(type) else { return nil }
        return o["tag"] as? String
    }
    XCTAssertTrue(proxyTags.contains(alwaysOutbound), "always outbound \(alwaysOutbound) should be one of \(proxyTags)")
}

func test_expandConfigForTunnel_idempotent_secondCallNoDuplication() throws {
    let template = try SingBoxConfigLoader.loadVLESSRealityTemplate()
    let firstExpand = try SingBoxConfigLoader.expandConfigForTunnel(template)
    let secondExpand = try SingBoxConfigLoader.expandConfigForTunnel(firstExpand)
    let parsed = try XCTUnwrap(JSONSerialization.jsonObject(with: Data(secondExpand.utf8)) as? [String: Any])
    let route = try XCTUnwrap(parsed["route"] as? [String: Any])
    let ruleSets = try XCTUnwrap(route["rule_set"] as? [[String: Any]])
    XCTAssertEqual(ruleSets.count, 3, "rule_set entries deduped — exactly 3, not 6")
    let rules = try XCTUnwrap(route["rules"] as? [[String: Any]])
    let bbtbRefs = rules.compactMap { $0["rule_set"] as? String }
    XCTAssertEqual(bbtbRefs.count, 3, "priority rules deduped — exactly 3, not 6")
}

func test_validate_passesAfterRulesetExpansion_R10invariant() throws {
    // R10 invariant: post-expand validate(json:) MUST pass.
    let template = try SingBoxConfigLoader.loadVLESSRealityTemplate()
    let expanded = try SingBoxConfigLoader.expandConfigForTunnel(template)
    // Must NOT throw — validate gates on whitelisted inbounds + outbounds only
    XCTAssertNoThrow(try SingBoxConfigLoader.validate(json: expanded))
}

func test_template_doesNotContainInlineRuleSetBlock_R1invariant() throws {
    // R1 (Phase 8 extension): template stays bare, no embedded rule_set keys
    let template = try SingBoxConfigLoader.loadVLESSRealityTemplate()
    XCTAssertFalse(template.contains("\"rule_set\""), "Template must not embed inline rule_set; runtime expansion is single source")
    let parsed = try XCTUnwrap(JSONSerialization.jsonObject(with: Data(template.utf8)) as? [String: Any])
    if let route = parsed["route"] as? [String: Any] {
        XCTAssertNil(route["rule_set"], "Route block in template must not contain rule_set key")
    }
}
```

Note: если `loadVLESSRealityTemplate()` already public (per W2 read_first SingBoxConfigLoader.swift line 246-256) — use directly. Если internal — promote к public либо use `@testable import PacketTunnelKit`.</action>
  <verify>
    <automated>cd BBTB/Packages/PacketTunnelKit && swift test --filter SingBoxConfigLoaderTests 2>&1 | tail -10 | grep -E "Executed [0-9]+ tests, with 0 failures"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -cE 'func test_(expandConfigForTunnel|validate_passesAfterRulesetExpansion|template_doesNotContainInlineRuleSet)' BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift` ≥ 6
    - Source: `grep -c 'bbtb-block.*bbtb-never.*bbtb-always\|"bbtb-block", "bbtb-never", "bbtb-always"' BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift` ≥ 1 (priority order assertion)
    - Source: `grep -c 'action.*reject\|"reject"' BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift` ≥ 1
    - Source: `grep -c 'idempotent\|secondCallNoDuplication' BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift` ≥ 1
    - Source: `grep -c 'R10invariant\|R1invariant' BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift` ≥ 2
    - CLI: `cd BBTB/Packages/PacketTunnelKit && swift test --filter SingBoxConfigLoaderTests` exits 0 with ALL tests passed (existing + ≥ 6 new)
  </acceptance_criteria>
  <done>6 new tests cover injection / priority order / firstProxyTag reuse / idempotency / R10 post-expand validate / R1 template-bare; entire PacketTunnelKit test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SingBoxConfigLoader runtime → libbox parser | JSON serialized from in-process [String: Any]; libbox parses |
| AppGroupContainer path → libbox rule_set local file load | path resolved at runtime; extension sandbox + entitlement gates filesystem |
| Idempotency invariant → repeated expand calls | пометка: SingBoxConfigLoader.expandConfigForTunnel может вызываться multiple раз (Phase 1 R10 — uses validate() defensively after expand always) — test_idempotent guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-W5-01 | Tampering | rule_set entries open new inbound surface (R1 invariant break) | mitigate | `action: "reject"` — это outbound action, не inbound type; post-expand `validate()` gates на inbound whitelist `{tun, direct}` (unchanged); test_validate_passesAfterRulesetExpansion confirms invariant preserved |
| T-08-W5-02 | DoS | Idempotency break — multiple expand calls duplicate rules → libbox crash on parse | mitigate | `existingTags` + `existingRuleSetRefs` sets gate appends; test_idempotent verifies ровно 3 entries after second expand |
| T-08-W5-03 | Tampering | Path traversal attack via crafted manifest filename | accept | filenames hardcoded (bbtb-block.srs etc.) в SingBoxConfigLoader — не управляется server-side rules.json content; manifest содержит filenames для verification но coordinator writes под hardcoded names |
| T-08-W5-04 | Information disclosure | rule_set path leaks App Group container UUID в logs | accept | App Group path = `<UUID>/Library/Caches/rules/...`; UUID — random per-install — не PII; libbox internal logs may show full path но `RulesEngineLogger.privacy: .public` controls exposure в OSLog |
| T-08-W5-05 | DoS | Missing .srs file → libbox rule_set load fails → tunnel may not start | mitigate | (a) bootstrap (W2.3) copies baseline на first launch; (b) libbox documented behavior: missing local file → warning log + skip rule_set, tunnel continues (RESEARCH Open Question A2 — empirical UAT validates в W7); (c) AppGroupContainer.rulesCacheDirectory idempotent createDirectory |
| T-08-W5-06 | Tampering | firstProxyTag resolution wrong → always category routes к non-VPN outbound | mitigate | test_alwaysCategoryUsesSameProxyTagAsFinal verifies alwaysOutbound resolves к valid proxy tag; reuses same resolution logic как existing `final` outbound (line 219-223) — same source of truth |
| T-08-W5-07 | Spoofing | Attacker crafts JSON config containing `rule_set` with malicious local path | mitigate | expandConfigForTunnel is called on internal generated config (PoolBuilder output) — user-supplied config never reaches here without pass через ConfigImporter (Phase 4); R1 validate gates on inbound types regardless |
| T-08-W5-08 | Repudiation | Engine apply без log trace | mitigate | RulesEngineLogger.coordinator logs each write event; libbox internal logging configured via tunnel.log; PerfSignposter span "RulesRefresh" tracks lifetime |
</threat_model>

<verification>
- ✓ SingBoxConfigLoader.expandConfigForTunnel injects 3 rule_set + 3 priority rules
- ✓ Priority order: block(reject) → never(direct) → always(urltest-from-firstProxyTag)
- ✓ Idempotent: second invoke не duplicate
- ✓ R10 invariant: post-expand validate() passes
- ✓ R1 invariant: template bare (no inline rule_set)
- ✓ firstProxyTag reuse от existing logic
- ✓ PacketTunnelKit swift test green (existing + 6 new)
- ✓ Path = AppGroupContainer.rulesCacheDirectory + filename — resolveable on extension side
</verification>

<success_criteria>
1. **rule_set injection working:** expandConfigForTunnel adds 3 entries (block/never/always) с type:"local" format:"binary" pointing к AppGroup paths.
2. **Priority hierarchy enforced:** block (reject) > never (direct) > always (urltest) > default — exact order in rules array via insertion index logic.
3. **firstProxyTag reuse:** always category outbound = same proxy tag что existing `final` route uses — reused from line 219-223 logic.
4. **Idempotency proven:** second expandConfigForTunnel call no-ops; test verifies ровно 3 rule_set + 3 priority rules after multiple invocations.
5. **R10 invariant preserved:** post-expand validate() passes без throw.
6. **R1 invariant preserved:** template `SingBoxConfigTemplate.vless-reality.json` остаётся bare (no inline rule_set keys) — W7 invariant gate verifies dynamically.
7. **6 new tests passing:** SingBoxConfigLoaderTests adds RULES-05/06/07 coverage + R10/R1 invariant tests; full PacketTunnelKit suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rules-engine-split-tunneling/08-W5-SUMMARY.md` со:
- Exact diff of expandConfigForTunnel addition (line range)
- Test names + count
- R1/R10 invariant confirmation
- Open Question A2 (fswatch in iOS NE sandbox) — note for W7 UAT validation
- Manual UAT preview (M-05 — real domain blocking on device — пройти в W7)
</output>
