---
phase: 08-rules-engine-split-tunneling
plan: W7
type: execute
wave: 8
depends_on: [W6]
files_modified:
  - BBTB/scripts/validate-r1-r6.sh
  - wiki/rules-engine.md
  - wiki/architecture.md
  - wiki/security-gaps.md
  - wiki/index.md
  - wiki/log.md
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [CORE-05, RULES-01, RULES-02, RULES-03, RULES-04, RULES-05, RULES-06, RULES-07, RULES-08, RULES-09, RULES-10]
mvp_slice: "Vertical slice #7 — closure: extend validate-r1-r6.sh с Phase 8 invariants (R8 / RULES-02 / D-08), run full regression gate, sync long-term wiki memory (architectural decisions D-01..D-13 в wiki/rules-engine.md per CLAUDE.md «обязательно фиксируется в wiki»), update STATE/PROJECT planning artifacts. После W7 Phase 8 готов к /gsd-verify-work 8 — все 11 in-scope RULES requirements в state «implementation complete», UAT pending (M-04/M-05/M-07/M-08 manual on device)."
user_setup:
  - service: manual-uat-iphone
    why: "Phase 8 имеет 4 manual-only UAT scenarios per 08-VALIDATION.md что нельзя cover automated tests"
    dashboard_config:
      - task: "Manual UAT после W7 commit (если developer запускает /gsd-verify-work 8): M-04 (BGAppRefreshTask 6h real wall-time — alt: iOS Simulator Debug → Simulate Background Fetch); M-05 (real domain blocking — connect tunnel + curl на max.ru → connection reset); M-07 (split-tunnel country resolve — admin packs RU CIDRs → yandex.ru goes direct); M-08 (min_app_version sheet UX — admin publishes min=99.0.0 → sheet appears, dismiss persists через kill). Все scenarios — на iPhone iOS 18.x test device c активной active subscription."
        location: "iPhone test device + admin VPS pipeline (if available)"

must_haves:
  truths:
    - "validate-r1-r6.sh extended с 5 новыми Phase 8 invariant checks (R8 template-bare / R8 AppGroupContainer-path-usage / RULES-02 32-byte pubkey / D-08 no-NEAppProxyProvider / R12 PublicKey не shipping placeholder Ed25519 bytes 0x00 0x01 0x02 0x03)"
    - "validate-r1-r6.sh extended с run_pkg_tests for RulesEngine package — runs swift test recursively по packages/"
    - "Full regression gate pass: bash BBTB/scripts/validate-r1-r6.sh exits 0; iOS + macOS xcodebuild SUCCEEDED; all packages swift test green"
    - "wiki/rules-engine.md updated с post-Phase-8 final state: D-01..D-13 decision summary, architectural diagram, file layout, ed25519 rotation strategy, return condition для RULES-11 + Phase 11/12 follow-ups"
    - "wiki/architecture.md updated с RulesEngine package introduction в architecture diagram"
    - "wiki/security-gaps.md updated с Phase 8 R8 invariant + Ed25519 signature trust path + 8 STRIDE threats summary"
    - "wiki/index.md updated с new wiki entries (если any new создан) + entry для rules-engine refreshed"
    - "wiki/log.md daily entry для 2026-05-15 (либо closure date) describing Phase 8 completion"
    - ".planning/STATE.md updated: Phase 8 moved к Status «Implementation complete YYYY-MM-DD, UAT pending», Phase 9 next; Recent decisions section augmented с D-01..D-13"
    - ".planning/PROJECT.md updated: 11 RULES-* requirements moved к Implementation Complete column (или equivalent); RULES-11 marked Out of Scope; CORE-05 reframed"
    - "Phase 8 final commit совершён с tag/message «Phase 8 implementation complete: Rules Engine + Split tunneling v0.8»"
  artifacts:
    - path: "BBTB/scripts/validate-r1-r6.sh"
      provides: "Extended invariant gate с Phase 8 checks"
      contains: "R8: vless-reality template has no inline rule_set"
    - path: "wiki/rules-engine.md"
      provides: "Long-term decision log Phase 8 — D-01..D-13 + architecture + rotation strategy"
      min_lines: 100
    - path: ".planning/STATE.md"
      provides: "Updated active phase + recent decisions"
      contains: "Phase 8 — Rules Engine + Split tunneling"
    - path: ".planning/PROJECT.md"
      provides: "11 RULES requirements + CORE-05 status updated"
  key_links:
    - from: "validate-r1-r6.sh"
      to: "BBTB/Packages/RulesEngine/Sources/RulesEngine/PublicKey.swift"
      via: "grep 32-byte pubkey assertion"
      pattern: "32 -eq"
    - from: "validate-r1-r6.sh"
      to: "main app sources"
      via: "grep '-rE' для NEAppProxyProvider absence"
      pattern: "NEAppProxyProvider"
    - from: "wiki/rules-engine.md"
      to: "wiki/appproxy-deferral-2026.md (W0)"
      via: "markdown link [[appproxy-deferral-2026]]"
      pattern: "appproxy-deferral-2026"
    - from: ".planning/STATE.md"
      to: "wiki/rules-engine.md"
      via: "reference в Recent decisions section"
      pattern: "wiki/rules-engine.md"
---

<objective>
Plan W7 — closure: фиксация invariant gates, long-term wiki memory, planning artifact sync, и final regression check. После W7 Phase 8 — implementation complete. `/gsd-verify-work 8` runs manual UAT (M-04/M-05/M-07/M-08) + signs off на closure.

Purpose: Phase 8 затрагивает security-critical surface (Ed25519 + sing-box engine config). Invariant gates через validate-r1-r6.sh — automation backstop для catch regression в future phases. CLAUDE.md правило «каждое архитектурное решение обязательно фиксируется в wiki» — W7 syncs D-01..D-13 решения в wiki/rules-engine.md.

Output: 1 modified shell script (validate-r1-r6.sh) + 3 modified wiki files (rules-engine + architecture + security-gaps) + 2 modified wiki indexing files (index + log) + 2 modified planning files (STATE + PROJECT). Полный регрессионный gate executed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-rules-engine-split-tunneling/08-CONTEXT.md
@.planning/phases/08-rules-engine-split-tunneling/08-RESEARCH.md
@.planning/phases/08-rules-engine-split-tunneling/08-VALIDATION.md
@.planning/phases/08-rules-engine-split-tunneling/08-W0-PLAN.md
@.planning/phases/08-rules-engine-split-tunneling/08-W1-PLAN.md
@.planning/phases/08-rules-engine-split-tunneling/08-W2-PLAN.md
@.planning/phases/08-rules-engine-split-tunneling/08-W3-PLAN.md
@.planning/phases/08-rules-engine-split-tunneling/08-W4-PLAN.md
@.planning/phases/08-rules-engine-split-tunneling/08-W5-PLAN.md
@.planning/phases/08-rules-engine-split-tunneling/08-W6-PLAN.md
@BBTB/scripts/validate-r1-r6.sh
@wiki/rules-engine.md
@wiki/architecture.md
@wiki/security-gaps.md
@wiki/index.md
@wiki/log.md
@.planning/STATE.md
@.planning/PROJECT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task W7.1: Extend validate-r1-r6.sh с Phase 8 invariant checks</name>
  <files>BBTB/scripts/validate-r1-r6.sh</files>
  <read_first>
    BBTB/scripts/validate-r1-r6.sh (full file — existing check function pattern + run_pkg_tests helper if exists),
    BBTB/Packages/RulesEngine/Sources/RulesEngine/PublicKey.swift (W1 — 32-byte pubkey literal),
    BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift (W5 — AppGroupContainer.rulesCacheDirectory usage),
    BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/Resources/SingBoxConfigTemplate.vless-reality.json (template — must stay bare),
    .planning/phases/08-rules-engine-split-tunneling/08-VALIDATION.md (§ validate-r1-r6.sh Phase 8 extension — exact checks),
    .planning/phases/08-rules-engine-split-tunneling/08-PATTERNS.md (§ 24 — existing check framework + 4 Phase 8 invariants)
  </read_first>
  <action>В `BBTB/scripts/validate-r1-r6.sh` добавить ПОСЛЕ существующих R1-R6 checks и ПЕРЕД unit-test loop block:

```bash
# ============================================================================
# Phase 8 — rule_set integrity & RULES-02 / D-08 invariants
# ============================================================================

# R8: vless-reality template stays bare (no inline rule_set block)
check "R8: vless-reality template has no inline rule_set block" \
    bash -c '! grep -q "\"rule_set\"" BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/Resources/SingBoxConfigTemplate.vless-reality.json'

# R8: SingBoxConfigLoader uses AppGroupContainer для rule_set paths (no /tmp, no relative)
check "R8: SingBoxConfigLoader uses AppGroupContainer for rule_set paths" \
    grep -q "AppGroupContainer.rulesCacheDirectory" \
    BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift

# RULES-02: RulesEngine PublicKey.swift has exactly 32-byte Ed25519 pubkey literal
check "RULES-02: PublicKey.swift has 32-byte pubkey constant" \
    bash -c 'grep -v "^[[:space:]]*//" BBTB/Packages/RulesEngine/Sources/RulesEngine/PublicKey.swift | grep -oE "0x[0-9a-fA-F]+" | wc -l | awk "{ exit !(\$1 == 32) }"'

# R12: PublicKey.swift does not ship placeholder Ed25519 bytes (0x00, 0x01, 0x02, 0x03 — first 4 bytes
# of W1 placeholder sequence). Если этот pattern в production binary — trust chain broken: signatures
# подписанные real private key не verify против placeholder pubkey. Converts WARNING 3 в hard gate так
# что producting binary не может ship с trust-chain-broken state.
check "R12: PublicKey.swift does not ship placeholder Ed25519 bytes" \
    bash -c '! grep -E "0x00,\s*0x01,\s*0x02,\s*0x03" BBTB/Packages/RulesEngine/Sources/RulesEngine/PublicKey.swift'

# D-08: No NEAppProxyProvider imports in main app sources после W0 carve-out
check "D-08: No NEAppProxyProvider import in main app sources" \
    bash -c '! grep -rE "NEAppProxyProvider|app-proxy-provider" BBTB/App/macOSApp/ BBTB/Packages/AppFeatures/Sources/ 2>/dev/null'

# RULES-02: swift-crypto dependency declared
check "RULES-02: swift-crypto dependency in RulesEngine/Package.swift" \
    grep -q "swift-crypto" BBTB/Packages/RulesEngine/Package.swift

# RULES-04: BGTaskSchedulerPermittedIdentifiers + UIBackgroundModes fetch
check "RULES-04: iOS Info.plist declares rules-refresh task identifier" \
    grep -q "app.bbtb.client.ios.rules-refresh" BBTB/App/iOSApp/Info.plist

# Baseline integrity (W6 outputs)
check "RULES-05: 3 baseline SRS files committed" \
    bash -c 'test $(ls BBTB/Packages/RulesEngine/Sources/RulesEngine/Resources/bbtb-baseline-*.srs 2>/dev/null | wc -l) -eq 3'

check "RULES-05: 4 baseline signature files committed (3 srs.sig + manifest.json.sig)" \
    bash -c 'test $(ls BBTB/Packages/RulesEngine/Sources/RulesEngine/Resources/*.sig 2>/dev/null | wc -l) -eq 4'

check "RULES-05: baseline-rules-manifest.json.sig is exactly 64 bytes" \
    bash -c 'test $(wc -c < BBTB/Packages/RulesEngine/Sources/RulesEngine/Resources/baseline-rules-manifest.json.sig 2>/dev/null) -eq 64'
```

Также — extend `run_pkg_tests` invocation list (если такая function existings) с `run_pkg_tests "RulesEngine" "BBTB/Packages/RulesEngine"` чтобы RulesEngine tests запускались в общем CI run.

Если в существующем validate-r1-r6.sh нет `run_pkg_tests` framework — add at end:
```bash
echo "--- Package tests ---"
for pkg in RulesEngine PacketTunnelKit AppFeatures ConfigParser VPNCore; do
    echo "Testing $pkg..."
    (cd BBTB/Packages/$pkg && swift test 2>&1 | tail -5 | grep -E "Executed [0-9]+ tests" | head -1) || FAIL=$((FAIL+1))
done

if [ "$FAIL" -gt 0 ]; then
    echo "❌ $FAIL invariant(s) failed"
    exit 1
fi
echo "✅ All invariants passed"
exit 0
```

Run script: `bash BBTB/scripts/validate-r1-r6.sh` — должен exit 0 with всеми PASS messages.</action>
  <verify>
    <automated>bash BBTB/scripts/validate-r1-r6.sh 2>&1 | tail -25 | tee /tmp/r1r6.log; grep -q 'FAIL\|❌' /tmp/r1r6.log && exit 1 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'R8: vless-reality template has no inline rule_set' BBTB/scripts/validate-r1-r6.sh` equals 1
    - Source: `grep -c 'R8: SingBoxConfigLoader uses AppGroupContainer' BBTB/scripts/validate-r1-r6.sh` equals 1
    - Source: `grep -c 'RULES-02: PublicKey.swift has 32-byte' BBTB/scripts/validate-r1-r6.sh` equals 1
    - Source: `grep -c 'R12: PublicKey.swift does not ship placeholder Ed25519 bytes' BBTB/scripts/validate-r1-r6.sh` equals 1
    - Source: `grep -c 'D-08: No NEAppProxyProvider' BBTB/scripts/validate-r1-r6.sh` equals 1
    - Source: `grep -c 'RULES-05.*baseline\|RULES-04.*Info.plist' BBTB/scripts/validate-r1-r6.sh` ≥ 3
    - CLI: `bash BBTB/scripts/validate-r1-r6.sh` exits 0
    - CLI: `bash BBTB/scripts/validate-r1-r6.sh 2>&1 | grep -c 'PASS\|✓'` ≥ 12 (existing R1-R6 + R8 + R12 + RULES-02 + RULES-04 + RULES-05 + D-08)
    - CLI: `bash BBTB/scripts/validate-r1-r6.sh 2>&1 | grep -q 'R12: PublicKey.swift does not ship placeholder Ed25519 bytes ✓\|R12: PublicKey.swift does not ship placeholder Ed25519 bytes PASS'` returns success after W6.2 pubkey derivation
    - CLI: `bash BBTB/scripts/validate-r1-r6.sh 2>&1 | grep -c 'FAIL'` equals 0
  </acceptance_criteria>
  <done>validate-r1-r6.sh extended (R8 / R12 / RULES-02 / RULES-04 / RULES-05 / D-08); bash exits 0; ≥ 12 PASS messages including R12 placeholder-pubkey guard (converts WARNING 3 в hard gate — production binary не может ship с placeholder Ed25519 bytes); 0 FAIL.</done>
</task>

<task type="auto">
  <name>Task W7.2: Sync wiki long-term memory — rules-engine.md, architecture.md, security-gaps.md, index.md, log.md</name>
  <files>wiki/rules-engine.md, wiki/architecture.md, wiki/security-gaps.md, wiki/index.md, wiki/log.md</files>
  <read_first>
    wiki/rules-engine.md (current draft — needs full Phase 8 closure rewrite),
    wiki/architecture.md (architectural diagram — нужно update с RulesEngine package),
    wiki/security-gaps.md (R1-R7 + R10-R17 entries — нужно add R18 Rules Engine trust path entry),
    wiki/index.md (для проверки/добавления entries),
    wiki/log.md (для append daily entry),
    wiki/wireguard-deferral-2026.md (closure log format reference),
    wiki/performance-baseline.md (long-term decision log format reference),
    .planning/phases/08-rules-engine-split-tunneling/08-CONTEXT.md (D-01..D-13 source-of-truth — copy в wiki),
    .planning/phases/08-rules-engine-split-tunneling/08-RESEARCH.md (architectural diagram + Open Questions для wiki),
    CLAUDE.md (rule about wiki long-term memory)
  </read_first>
  <action>(1) **wiki/rules-engine.md** — полная rewrite (existing content draft may be stale). New content covers:
- Title + Summary (CLAUDE.md page format)
- Sources: D-01..D-13 from 08-CONTEXT.md, RESEARCH.md, Codex thread 019e2841 (Area A) + 019e284c (Area D)
- Last updated: 2026-05-15
- **Architecture diagram** (ascii) — copy from 08-RESEARCH.md § Architecture Patterns (VPS → main app → extension flow)
- **D-01..D-13 decision summary** — для каждой decision: contextual rationale (1-2 строки)
  - D-01: server-side SRS pipeline через sing-box route.rule_set
  - D-02: domain/IP/country mapping в SRS
  - D-03: DNS sniffing requirement
  - D-04: server-side country resolve через MaxMind GeoLite2
  - D-05: embedded baseline в .app bundle (signed)
  - D-07: two-file signature (.sig sidecar)
  - D-08: RULES-11 + Phase 8 SC #3 → Out of Scope (link к [[appproxy-deferral-2026]])
  - D-09: AppProxyExtension-macOS target → DELETED в W0
  - D-10: force-update cooldown 60s
  - D-11: min_app_version modal sheet dismissible + persistent banner
  - D-12: rules.json не блокирует cold start
  - D-13: failover concurrency=1 sequential
- **File layout** — BBTB/Packages/RulesEngine package structure
- **Ed25519 rotation strategy** — copy from RESEARCH § Pitfall 5 (v1.x plan: support two keys → migration → drop old)
- **Open Questions** (4 from RESEARCH) — what was validated в W7 manual UAT, what remains для future
- **Return condition для RULES-11 (AppProxy)** — 3+ TestFlight requests на per-app routing, см. [[appproxy-deferral-2026]]
- **Phase 11/12 follow-ups** — TestFlight invite URL substitution, real production VPS-pipeline build, Numerical Instruments baseline
- **Related pages:** [[appproxy-deferral-2026]] [[max-messenger]] [[security-gaps]] [[engine-abstraction-decision-2026]] [[performance-baseline]] [[geoip-detection]]

(2) **wiki/architecture.md** — update existing architectural diagram block с RulesEngine layer:
- Add RulesEngine как new SwiftPM package в `Packages/` listing
- Update component diagram (если exists) с three new arrows:
  - main app → RulesEngineCoordinator (W2)
  - RulesEngineCoordinator → SRSCacheStore → App Group filesystem
  - PacketTunnelExtension (SingBoxConfigLoader) ← App Group filesystem ← read .srs
- Update «Engines» section: mono-engine sing-box остаётся (per [[engine-abstraction-decision-2026]]); rule_set — sing-box feature; RulesEngine package — orchestrator только, не second engine.

(3) **wiki/security-gaps.md** — add new entry «R18 Rules Engine trust path»:
```
### R18 Rules Engine signature trust path (Phase 8 — 2026-05-15)

**Threat model:** server-distributed rules.json mutated by attacker → client applies malicious rules.
**Mitigation:** Ed25519 detached signature verify via swift-crypto/CryptoKit. Hardcoded 32-byte public key в `BBTB/Packages/RulesEngine/Sources/RulesEngine/PublicKey.swift`. Manifest contains per-file sha256 + sig_path; coordinator verifies each .srs sig before atomic write.

**Invariants (enforced via validate-r1-r6.sh):**
- Public key constant = exactly 32 bytes
- swift-crypto dependency declared в RulesEngine/Package.swift
- No NEAppProxyProvider imports (RULES-11 carve-out per D-08, см. [[appproxy-deferral-2026]])

**Known limitations:**
- v0.8 hardcoded pubkey не rotatable; v1.x plan: dual-key support → migration → drop old (см. [[rules-engine]] § Rotation strategy)
- min_app_version field может lock-out legitimate users если admin error — accept; mitigation = admin operational care + TestFlight invite revisit

**Cross-references:** [[rules-engine]], [[appproxy-deferral-2026]], R1 invariant (sing-box no SOCKS5), R10 invariant (post-expand validate)
```

(4) **wiki/index.md** — update Phase 8 entries:
- Если wiki/rules-engine.md already listed — refresh description «Phase 8 final state (2026-05-15) — Ed25519 signed rules pipeline + split-tunnel через sing-box rule_set»
- Если new pages (например, нет — W0 уже создал appproxy-deferral-2026.md) — ensure listed

(5) **wiki/log.md** — append entry:
```
## 2026-05-15 — Phase 8 closure

- **Rules Engine + Split tunneling (v0.8) implementation complete.**
- New SwiftPM package `RulesEngine` (5 source + 3 test files): swift-crypto Ed25519 verify + HTTPS+SSRF mirror failover + SRS atomic write + manifest decode + bootstrap from bundle baseline.
- New SwiftUI components в SettingsFeature: `RulesViewerSection`, `ForceUpdateRulesButton`, `MinAppVersionBanner`. In MainScreenFeature: `MinAppVersionSheet`.
- SingBoxConfigLoader.expandConfigForTunnel extended — injects 3 route.rule_set entries + priority rules (block→reject; never→direct; always→urltest).
- iOS host registers BGAppRefreshTask 6h; macOS host registers NSBackgroundActivityScheduler 6h tolerance 10min.
- Build script `BBTB/scripts/build-baseline-rules.sh` — developer-managed asset workflow (Pitfall 6 Option A); committed real signed baseline (max.ru / mssgr.tatar.ru в block_completely).
- **Wiki updates:** `wiki/rules-engine.md` final state, `wiki/architecture.md` RulesEngine layer added, `wiki/security-gaps.md` R18 entry added.
- **RULES-11 + Phase 8 SC #3 carved out** в W0 per D-08/D-09 (см. [[appproxy-deferral-2026]] — created W0 2026-05-15).
- **Codex consultations:** thread `019e2841` (Area A architectural review — sing-box rule_set strategy), thread `019e284c` (Area D AppProxy review).
- **Manual UAT pending:** M-04/M-05/M-07/M-08 on iPhone device (BG-refresh wall-time, real domain block, country resolve, min_app_version sheet UX).
- **Next:** `/gsd-verify-work 8` для UAT signoff + Phase 9 Deep Links discuss.
```</action>
  <verify>
    <automated>test -f wiki/rules-engine.md && wc -l wiki/rules-engine.md | awk '$1 > 100' | grep -q . && grep -q '2026-05-15' wiki/rules-engine.md && grep -q 'D-01\|D-08' wiki/rules-engine.md && grep -q 'R18 Rules Engine' wiki/security-gaps.md && grep -q '2026-05-15.*Phase 8 closure' wiki/log.md && grep -q 'RulesEngine' wiki/architecture.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `wc -l wiki/rules-engine.md` returns ≥ 100
    - Source: `grep -c 'D-01\|D-02\|D-03\|D-04\|D-05\|D-07\|D-08\|D-09\|D-10\|D-11\|D-12\|D-13' wiki/rules-engine.md` ≥ 12 (all 12 decisions listed; D-06 omitted intentionally — was internal placeholder)
    - Source: `grep -c '\[\[appproxy-deferral-2026\]\]\|appproxy-deferral-2026' wiki/rules-engine.md` ≥ 1 (cross-reference)
    - Source: `grep -c 'Ed25519\|swift-crypto' wiki/rules-engine.md` ≥ 2
    - Source: `grep -c 'Last updated' wiki/rules-engine.md` ≥ 1
    - Source: `grep -c 'RulesEngine' wiki/architecture.md` ≥ 1
    - Source: `grep -c 'R18 Rules Engine' wiki/security-gaps.md` equals 1
    - Source: `tail -50 wiki/log.md | grep '2026-05-15.*Phase 8 closure'` returns ≥ 1
  </acceptance_criteria>
  <done>wiki/rules-engine.md полностью rewritten (≥ 100 lines covering D-01..D-13 + architecture + rotation + open questions + return conditions); wiki/architecture.md acknowledges RulesEngine package; wiki/security-gaps.md adds R18 entry; wiki/log.md daily entry committed; CLAUDE.md «wiki long-term memory» правило выполнено.</done>
</task>

<task type="auto">
  <name>Task W7.3: Update planning artifacts (STATE.md + PROJECT.md) + final regression gate</name>
  <files>.planning/STATE.md, .planning/PROJECT.md</files>
  <read_first>
    .planning/STATE.md (current — Phase 8 active section + Recent decisions + Progress table),
    .planning/PROJECT.md (current — requirements status table + Key Decisions table),
    .planning/phases/08-rules-engine-split-tunneling/08-CONTEXT.md (D-01..D-13 source),
    .planning/phases/08-rules-engine-split-tunneling/08-W0-PLAN.md (CORE-05 + RULES-11 carve-out details),
    .planning/STATE.md (для existing Phase 7 closure format reference)
  </read_first>
  <action>(1) **.planning/STATE.md** edit:
- Update header `status:` line: "Phase 8 — Rules Engine + Split tunneling: Implementation complete 2026-05-15. UAT pending (M-04/M-05/M-07/M-08 manual on iPhone). Closure SUMMARY в .planning/phases/08-rules-engine-split-tunneling/08-Final-SUMMARY.md. Next: `/gsd-verify-work 8`."
- Update `last_updated: "2026-05-15T..."` (use current ISO timestamp)
- Update `progress.completed_phases` += 1 (если Phase 8 implementation complete counts as +1 by convention; либо keep как in-progress until UAT signoff per Phase 6c precedent)
- **Active Phase** section — describe Phase 8 outcome:
```
### Phase 8 implementation summary

- **All 7 waves complete** (W0..W7): 
  - W0 ✓ — RULES-11 + Phase 8 SC #3 carve-out (Tuist target deleted, ROADMAP/REQUIREMENTS amended, `wiki/appproxy-deferral-2026.md` created)
  - W1 ✓ — RulesEngine SwiftPM пакет (swift-crypto 4.x + RulesSigner + RulesFetcher + RulesManifest + 9 unit tests)
  - W2 ✓ — RulesEngineCoordinator pipeline (bootstrap + performBackgroundRefresh + forceUpdate + SRSCacheStore actor + BaselineRulesLoader + 13 tests)
  - W3 ✓ — UI components (RulesViewerSection, ForceUpdateRulesButton, MinAppVersionBanner, MinAppVersionSheet) + ViewModel wiring + ~30 L10n keys + 17 tests
  - W4 ✓ — iOS BGAppRefreshTask + macOS NSBackgroundActivityScheduler + host wire-up
  - W5 ✓ — SingBoxConfigLoader.expandConfigForTunnel rule_set injection + 6 tests (R1/R10 invariants preserved)
  - W6 ✓ — scripts/build-baseline-rules.sh + real signed baseline files committed
  - W7 ✓ — validate-r1-r6.sh extended (R8 + RULES-02 + D-08 invariants) + wiki sync + this STATE update
- **Test totals (Phase 8):** RulesEngine ≥ 22 + PacketTunnelKit 66+6 + AppFeatures 143+17 + existing packages baseline — all green.
- **Build:** iOS + macOS xcodebuild SUCCEEDED.
- **UAT pending:** M-04 (BGAppRefreshTask wall-time), M-05 (real domain block on device), M-07 (split-tunnel country resolve admin scenario), M-08 (min_app_version sheet UX) — все на iPhone iOS 18+ test device.
```

- **Recent decisions (Phase 8)** — append секция со всеми D-01..D-13 (one-line each) + Codex thread refs.
- **Progress table** — Phase 8 row update: «Implementation complete 2026-05-15 — UAT pending (M-04/M-05/M-07/M-08 manual)»
- **Next Action** section — update: «Manual UAT 4 scenarios on iPhone, then `/gsd-verify-work 8` → Phase 9 (Deep Links v0.9) `/gsd-discuss-phase 9`».

(2) **.planning/PROJECT.md** edit:
- В section «Requirements status» (если exists table format — refresh; otherwise — list update):
  - 11 RULES requirements (RULES-01..10, исключая RULES-11) → status «Implementation Complete YYYY-MM-DD, UAT pending»
  - RULES-11 → status «Out of Scope per D-08» с link к wiki/appproxy-deferral-2026.md
  - CORE-05 → status «Reframed (D-08 carve-out)» — original «AppProxyExtension target» → effective Phase 8 deliverable = «Split-tunneling routing via sing-box rule_set»
- В section «Key Decisions» (если exists — append):
  - Phase 8: 13 decisions D-01..D-13 (concise table summary, full list в wiki/rules-engine.md)
  - Phase 8 closure commit reference (filled после commit в W7.4 либо leave placeholder «commit TBD»)

(3) **Final regression gate**:
- `bash BBTB/scripts/validate-r1-r6.sh` — must exit 0
- `cd BBTB && xcodebuild -workspace BBTB.xcworkspace -scheme BBTB -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 16 Pro' build -quiet 2>&1 | tail -3 | grep BUILD` — must show SUCCEEDED
- `cd BBTB && xcodebuild -workspace BBTB.xcworkspace -scheme BBTB-macOS -configuration Debug -destination 'platform=macOS' build -quiet 2>&1 | tail -3 | grep BUILD` — must show SUCCEEDED
- Per-package swift test summary:
  - `cd BBTB/Packages/RulesEngine && swift test 2>&1 | grep 'Executed.*0 failures'`
  - `cd BBTB/Packages/PacketTunnelKit && swift test 2>&1 | grep 'Executed.*0 failures'`
  - `cd BBTB/Packages/AppFeatures && swift test 2>&1 | grep 'Executed.*0 failures'`
  - `cd BBTB/Packages/ConfigParser && swift test 2>&1 | grep 'Executed.*0 failures'`

Capture all gate outputs в logs для inclusion в 08-Final-SUMMARY.md (which will be generated при /gsd-verify-work).</action>
  <verify>
    <automated>grep -q 'Phase 8.*Implementation complete\|Phase 8 — Rules Engine.*complete' .planning/STATE.md && grep -q 'D-01\|D-08' .planning/STATE.md && grep -q 'Out of Scope per D-08' .planning/PROJECT.md && bash BBTB/scripts/validate-r1-r6.sh 2>&1 | tail -5 | grep -qv FAIL && cd BBTB && xcodebuild -workspace BBTB.xcworkspace -scheme BBTB -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 16 Pro' build -quiet 2>&1 | tail -3 | grep -qE 'BUILD SUCCEEDED|build succeeded' && xcodebuild -workspace BBTB.xcworkspace -scheme BBTB-macOS -configuration Debug -destination 'platform=macOS' build -quiet 2>&1 | tail -3 | grep -qE 'BUILD SUCCEEDED|build succeeded' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'Phase 8.*Implementation complete\|Rules Engine.*complete' .planning/STATE.md` ≥ 1
    - Source: `grep -c '08-rules-engine-split-tunneling\|Rules Engine + Split tunneling' .planning/STATE.md` ≥ 1
    - Source: `grep -cE 'D-0[1-9]|D-1[0-3]' .planning/STATE.md` ≥ 10 (most D-XX decisions listed)
    - Source: `grep -c 'Out of Scope per D-08\|RULES-11.*Out of Scope' .planning/PROJECT.md` ≥ 1
    - Source: `grep -c 'RULES-01\|RULES-02\|RULES-03\|RULES-04\|RULES-05\|RULES-06\|RULES-07\|RULES-08\|RULES-09\|RULES-10' .planning/PROJECT.md` ≥ 10
    - CLI: `bash BBTB/scripts/validate-r1-r6.sh` exits 0 with 0 FAIL
    - CLI: iOS xcodebuild SUCCEEDED for BBTB scheme
    - CLI: macOS xcodebuild SUCCEEDED for BBTB-macOS scheme
    - CLI: `cd BBTB/Packages/RulesEngine && swift test 2>&1 | grep -c 'Executed.*0 failures'` ≥ 1
    - CLI: `cd BBTB/Packages/PacketTunnelKit && swift test 2>&1 | grep -c 'Executed.*0 failures'` ≥ 1
  </acceptance_criteria>
  <done>STATE.md + PROJECT.md reflect Phase 8 implementation complete; 11 RULES requirements + CORE-05 status updated; final regression gate green (validate-r1-r6.sh + iOS xcodebuild + macOS xcodebuild + all package swift test).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validate-r1-r6.sh → invariant assertions | CI gate; must catch regressions in future phases |
| wiki long-term memory → future developer decisions | Wiki entries preserve rationale to prevent re-litigation |
| .planning state → workflow continuity | STATE.md drives /gsd-progress and /gsd-verify-work tooling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-W7-01 | Tampering | Future phase removes Phase 8 invariant check from validate-r1-r6.sh inadvertently | mitigate | Each new invariant has explicit «check» label с phase tag (e.g. «R8: ...») — code review responsibility flags removal; commits diff readable |
| T-08-W7-02 | Information disclosure | wiki page leaks signing key bytes accidentally | mitigate | wiki/rules-engine.md explicitly states «pubkey is public-by-design»; никакого private key bytes анywhere в wiki/source — only file paths; secure storage external |
| T-08-W7-03 | Repudiation | Decisions D-01..D-13 lost из long-term memory | mitigate | W7.2 task explicitly logs all 12 decisions (D-06 omitted intentionally — placeholder) в wiki/rules-engine.md; CLAUDE.md rule enforced |
| T-08-W7-04 | DoS | validate-r1-r6.sh hangs CI на missing tools | mitigate | Existing script uses fast `grep -q` / `test` operations; no network / no external CLI; новые checks same pattern |
| T-08-W7-05 | Tampering | Wiki cross-reference link rot когда appproxy-deferral page renames | accept | Single relative reference [[appproxy-deferral-2026]] — if renamed, грep finds + fixes; low likelihood для long-term decision pages |
| T-08-W7-06 | Spoofing | Phase 9+ accidentally re-enables NEAppProxy capability | mitigate | D-08 invariant в validate-r1-r6.sh catches `grep -rE "NEAppProxyProvider"`; commit gate forces explicit acknowledge if re-enabled |
| T-08-W7-07 | Information disclosure | STATE.md decision summary surfaces sensitive details | accept | STATE.md содержит только D-XX references + one-liners; full details в CONTEXT.md / wiki — separation already enforced |
| T-08-W7-08 | DoS | Regression gate too slow for routine use | accept | swift test per-package ~10-30s; validate-r1-r6.sh ~5s; xcodebuild ~60-90s — total ~5min full gate; acceptable for end-of-phase / pre-merge |
</threat_model>

<verification>
- ✓ validate-r1-r6.sh extended с 4+ Phase 8 invariant checks + per-package test loop
- ✓ bash BBTB/scripts/validate-r1-r6.sh exits 0 (all PASS, 0 FAIL)
- ✓ wiki/rules-engine.md fully written ≥ 100 lines covering D-01..D-13 + architecture + rotation + return conditions
- ✓ wiki/architecture.md acknowledges RulesEngine package
- ✓ wiki/security-gaps.md adds R18 trust path entry
- ✓ wiki/log.md daily entry committed
- ✓ STATE.md + PROJECT.md reflect Phase 8 implementation complete
- ✓ iOS xcodebuild SUCCEEDED + macOS xcodebuild SUCCEEDED
- ✓ all package swift test pass (RulesEngine ≥ 22 + PacketTunnelKit + AppFeatures + ConfigParser)
- ✓ CLAUDE.md «wiki long-term memory» правило fulfilled
</verification>

<success_criteria>
1. **validate-r1-r6.sh extended:** 4+ new Phase 8 invariants codified; per-package swift test loop runs RulesEngine; full gate exits 0.
2. **Long-term wiki memory landed:** wiki/rules-engine.md ≥ 100 lines, D-01..D-13 documented, architecture diagram preserved, rotation strategy noted, return conditions для RULES-11 referenced.
3. **Cross-cutting wiki sync:** architecture.md acknowledges RulesEngine layer; security-gaps.md adds R18 entry; index.md + log.md updated с daily entries.
4. **Planning artifacts current:** STATE.md describes Phase 8 implementation complete + 7 waves summary + 13 decisions; PROJECT.md requirements status updated.
5. **Full regression green:** validate-r1-r6.sh PASS; iOS + macOS xcodebuild SUCCEEDED; all package swift test 0 failures.
6. **CLAUDE.md compliance:** Каждое архитектурное решение Phase 8 зафиксировано в wiki — D-01..D-13 + Codex thread refs + open questions + future paths.
7. **Phase 8 ready для /gsd-verify-work 8:** UAT scenarios documented в user_setup; closure SUMMARY emerged from W7 commit.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rules-engine-split-tunneling/08-Final-SUMMARY.md` со:
- Phase 8 overview (7 waves complete)
- Test totals breakdown per package
- Build matrix (iOS + macOS xcodebuild status)
- Invariant gate output (validate-r1-r6.sh log)
- Wiki sync confirmation
- STATE/PROJECT update confirmation
- Manual UAT scenarios checklist для /gsd-verify-work
- Files created/modified count (cumulative across W0-W7)
- Final commit message recommendation
- Next step recommendation (manual UAT → /gsd-verify-work 8 → /gsd-discuss-phase 9)
</output>
