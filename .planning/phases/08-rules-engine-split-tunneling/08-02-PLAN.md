---
phase: 08-rules-engine-split-tunneling
plan: W1
type: execute
wave: 2
depends_on: [W0]
files_modified:
  - BBTB/Packages/RulesEngine/Package.swift
  - BBTB/Packages/RulesEngine/Sources/RulesEngine/PublicKey.swift
  - BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesSigner.swift
  - BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesFetcher.swift
  - BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesManifest.swift
  - BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesEngineLogger.swift
  - BBTB/Packages/RulesEngine/Tests/RulesEngineTests/RulesSignerTests.swift
  - BBTB/Packages/RulesEngine/Tests/RulesEngineTests/RulesFetcherTests.swift
  - BBTB/Packages/RulesEngine/Tests/RulesEngineTests/Fixtures/MockURLProtocol.swift
  - BBTB/Project.swift
autonomous: true
requirements: [RULES-01, RULES-02]
mvp_slice: "Vertical slice #1 — RulesEngine пакет может FETCH с HTTPS+SSRF mirror failover, VERIFY Ed25519 detached signature через swift-crypto. End-to-end test: mock URLSession → RulesFetcher fetches → RulesSigner.verify(message, signature) returns true для valid pair, false для tampered."
user_setup:
  - service: ed25519-keypair-generation
    why: "v0.8 hardcoded Ed25519 public key должен быть сгенерирован один раз и зафиксирован в PublicKey.swift как [UInt8] литерал; private key хранится на VPS вне репозитория"
    dashboard_config:
      - task: "Developer (admin) генерирует Ed25519 ключевую пару локально: `openssl genpkey -algorithm ed25519 -out /tmp/bbtb-rules-private.pem && openssl pkey -in /tmp/bbtb-rules-private.pem -pubout -out /tmp/bbtb-rules-public.pem`. Затем извлекает 32 raw байта public key: `openssl pkey -in /tmp/bbtb-rules-public.pem -pubin -outform DER | tail -c 32 | xxd -i` — output копируется в PublicKey.swift как `static let publicKeyBytes: [UInt8] = [0xXX, 0xYY, ...]` (32 элемента). Private key уносится на VPS в зашифрованном виде (например через SecureKeep / 1Password), НЕ commit в репозиторий. Для тестов W1 — generates test keypair (test_pubkey_bytes) который попадает в тестовые fixtures only."
        location: "Developer machine + VPS admin storage"

must_haves:
  truths:
    - "Пакет `RulesEngine` создан как локальный SwiftPM в BBTB/Packages/RulesEngine, добавлен в Tuist BBTB iOS+macOS app target dependencies"
    - "swift-crypto 4.0.0..<5.0.0 добавлен как external dependency в Package.swift"
    - "PublicKey.swift содержит 32-байтный Ed25519 public key как `[UInt8]` Swift literal (32 элемента 0xNN)"
    - "RulesSigner.verify(message:signature:) возвращает true для valid pair, false для tampered signature, false для wrong-length signature"
    - "RulesFetcher.fetch(url:) enforces HTTPS scheme, applies isBlockedHost SSRF blocklist (reused public helper из ConfigParser W0), timeout 10s, User-Agent 'BBTB-Rules/0.8 (iOS / macOS)'"
    - "RulesFetcher.fetchWithFailover(urls:) пытается mirrors sequentially (concurrency=1 per DEC-06d-04), возвращает first success, throws aggregated error на all-fail"
    - "RulesManifest Codable struct decode-able из rules-manifest.json с полями version (Int), minAppVersion (String), srsFormatVersion (Int), totalSizeBytes (Int), files ([FileEntry]), и optional CategoryBodies полями block_completely / never_through_vpn / always_through_vpn (consumed by W2.3 currentSnapshot для read-only Rules viewer)"
    - "Все unit-тесты RulesEngineTests passing: ≥ 3 RulesSigner tests (valid/tampered/wrong-length) + ≥ 4 RulesFetcher tests (https-required/ssrf-blocked/mirror-failover/all-fail)"
  artifacts:
    - path: "BBTB/Packages/RulesEngine/Package.swift"
      provides: "Package manifest со swift-crypto 4.x dep и VPNCore + ConfigParser local deps"
      contains: ".package(url: \"https://github.com/apple/swift-crypto"
    - path: "BBTB/Packages/RulesEngine/Sources/RulesEngine/PublicKey.swift"
      provides: "Hardcoded Ed25519 public key как 32-byte Swift literal"
      contains: "publicKeyBytes"
    - path: "BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesSigner.swift"
      provides: "Ed25519 signature verify wrapper"
      exports: ["verify"]
      contains: "Curve25519.Signing.PublicKey"
    - path: "BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesFetcher.swift"
      provides: "HTTPS fetcher с SSRF blocklist + mirror failover"
      exports: ["fetch", "fetchWithFailover", "FetchError"]
    - path: "BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesManifest.swift"
      provides: "Codable manifest model"
      contains: "public struct RulesManifest"
    - path: "BBTB/Packages/RulesEngine/Tests/RulesEngineTests/Fixtures/MockURLProtocol.swift"
      provides: "Test helper для URLProtocol stubbing"
      contains: "class MockURLProtocol"
  key_links:
    - from: "RulesFetcher.swift"
      to: "ConfigParser.SubscriptionURLFetcher.isBlockedHost"
      via: "import ConfigParser + public static call"
      pattern: "SubscriptionURLFetcher.isBlockedHost"
    - from: "RulesSigner.swift"
      to: "swift-crypto Curve25519.Signing.PublicKey"
      via: "import Crypto + isValidSignature(_:for:)"
      pattern: "isValidSignature"
    - from: "BBTB/Project.swift"
      to: "RulesEngine Swift package"
      via: ".package(path: ...Packages/RulesEngine)"
      pattern: "RulesEngine"
---

<objective>
Plan W1 — vertical slice #1: создать новый SwiftPM пакет `RulesEngine` со swift-crypto dependency, hardcoded Ed25519 public key, и двумя ключевыми primitives (`RulesSigner.verify` + `RulesFetcher.fetch/fetchWithFailover`). Это foundation — без верификации подписи и mirror failover ни одна последующая waveh не может безопасно apply server rules. Также solidify test infrastructure (MockURLProtocol, fixture pattern) — downstream waves добавят тесты на coordinator, integration, и SingBoxConfigLoader injection.

Purpose: separate verification logic (W1) от orchestration (W2) — позволяет independently тестировать crypto + network primitives. RulesSigner — pure function, RulesFetcher — single-responsibility HTTPS+SSRF wrapper. Обе testable изолированно через mock URLSession.

Output: новый пакет `RulesEngine` (5 source files + 3 test files + 1 fixture helper) который проходит `swift test` локально и подключён через Tuist в обе платформы.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-rules-engine-split-tunneling/08-CONTEXT.md
@.planning/phases/08-rules-engine-split-tunneling/08-RESEARCH.md
@.planning/phases/08-rules-engine-split-tunneling/08-PATTERNS.md
@.planning/phases/08-rules-engine-split-tunneling/08-VALIDATION.md
@.planning/phases/08-rules-engine-split-tunneling/08-W0-PLAN.md
@BBTB/Packages/ConfigParser/Package.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionURLFetcher.swift
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/SubscriptionURLFetcherTests.swift
@BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift
@BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/AppGroupContainer.swift
@BBTB/Project.swift

<interfaces>
<!-- W1 declares following public surface для downstream W2 consumer (RulesEngineCoordinator) -->

From BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesSigner.swift:
- public enum RulesSigner
  - public static func verify(message: Data, signature: Data) -> Bool

From BBTB/Packages/RulesEngine/Sources/RulesEngine/PublicKey.swift:
- enum PublicKey   // internal — RulesSigner consumes
  - static let publicKey: Curve25519.Signing.PublicKey

From BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesFetcher.swift:
- public enum RulesFetcher
  - public struct FetchResult { public let body: Data; public let etag: String?; public let mirrorURL: URL }
  - public enum FetchError: Error, Equatable {
      case nonHTTPS(String); case malformedURL; case blockedHost(String);
      case notHTTPResponse; case httpStatusError(Int); case timeout;
      case allMirrorsFailed([FetchError])
    }
  - public static func fetch(url: URL, session: URLSession = .shared) async throws -> FetchResult
  - public static func fetchWithFailover(urls: [URL], session: URLSession = .shared) async throws -> FetchResult

From BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesManifest.swift:
- public struct RulesManifest: Codable, Sendable, Equatable
  - public let version: Int
  - public let minAppVersion: String
  - public let srsFormatVersion: Int
  - public let totalSizeBytes: Int
  - public let files: [FileEntry]
  - public let blockCompletely: CategoryBodies?    // CodingKey "block_completely" — populated in baseline + final manifests; consumed by W2.3 currentSnapshot()
  - public let neverThroughVpn: CategoryBodies?   // CodingKey "never_through_vpn"
  - public let alwaysThroughVpn: CategoryBodies?  // CodingKey "always_through_vpn"
  - public enum Category: String, Codable, Sendable { case block, never, always }
  - public struct FileEntry: Codable, Sendable, Equatable { public let name, sha256, sigPath: String; public let category: Category }
  - public struct CategoryBodies: Codable, Sendable, Equatable { public let domains: [String]?; public let ipCidrs: [String]?; public let countries: [String]? }   // CodingKey "ip_cidrs" for ipCidrs

From BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesEngineLogger.swift:
- enum RulesEngineLogger   // internal — used inside package
  - static let coordinator: Logger
  - static let fetcher: Logger
  - static let signer: Logger
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task W1.1: Create RulesEngine Package.swift + register in Tuist</name>
  <files>BBTB/Packages/RulesEngine/Package.swift, BBTB/Project.swift</files>
  <read_first>
    BBTB/Packages/ConfigParser/Package.swift (точный pattern — same Swift 6 / iOS 18 / macOS 15 / external dep pattern с Yams),
    BBTB/Project.swift (lines 35-55 для localPackages array + lines 75-100 для BBTB iOS app dependencies + lines 110-140 для BBTB-macOS dependencies),
    .planning/phases/08-rules-engine-split-tunneling/08-RESEARCH.md (§ Standard Stack installation block lines 72-90 — swift-crypto version range),
    .planning/phases/08-rules-engine-split-tunneling/08-PATTERNS.md (§ 1 — точный manifest skeleton mapping)
  </read_first>
  <action>(1) Создать `BBTB/Packages/RulesEngine/Package.swift` со structure совпадающей с ConfigParser/Package.swift. Конкретика:
- `// swift-tools-version: 6.0` header
- platforms `.iOS(.v18), .macOS(.v15)`
- products: `.library(name: "RulesEngine", targets: ["RulesEngine"])`
- dependencies:
    - `.package(path: "../VPNCore")` — local sibling
    - `.package(path: "../ConfigParser")` — local sibling (для reuse public isBlockedHost)
    - `.package(url: "https://github.com/apple/swift-crypto.git", "4.0.0"..<"5.0.0")` — external
- targets:
    - `.target(name: "RulesEngine", dependencies: ["VPNCore", "ConfigParser", .product(name: "Crypto", package: "swift-crypto")], resources: [.process("Resources")])` — resources directory будет создан в W6 для baseline SRS, но declarative path заявлен сейчас
    - `.testTarget(name: "RulesEngineTests", dependencies: ["RulesEngine", "ConfigParser"], resources: [.process("Fixtures")])`
- Создать пустую placeholder директорию `Sources/RulesEngine/Resources/.gitkeep` чтобы SwiftPM не падал на missing resources path
- Создать пустую `Tests/RulesEngineTests/Fixtures/.gitkeep`

(2) Edit `BBTB/Project.swift`:
- В `localPackages: [Package]` array (lines ~36-55) добавить `.package(path: .relativeToManifest("Packages/RulesEngine")),`
- В блоке target `BBTB` (iOS app, lines ~75-100) в dependencies массиве добавить `.package(product: "RulesEngine"),` после `.package(product: "ConfigParser"),`
- В блоке target `BBTB-macOS` (lines ~110-140) в dependencies массиве аналогично добавить `.package(product: "RulesEngine"),`

(3) Запустить `cd BBTB && tuist generate` чтобы regenerate workspace.

(4) `cd BBTB/Packages/RulesEngine && swift build` чтобы убедиться что Package.swift валиден и swift-crypto резолвится. Build пока пустой (нет source files), но resolution + target declaration должны работать.</action>
  <verify>
    <automated>test -f BBTB/Packages/RulesEngine/Package.swift && grep -q 'swift-crypto' BBTB/Packages/RulesEngine/Package.swift && grep -q 'RulesEngine' BBTB/Project.swift && cd BBTB/Packages/RulesEngine && swift package resolve 2>&1 | tail -3 | grep -qE 'finished|Resolved|cached' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `test -f BBTB/Packages/RulesEngine/Package.swift` exits 0
    - Source: `grep -c 'swift-crypto' BBTB/Packages/RulesEngine/Package.swift` ≥ 1
    - Source: `grep -c '"4.0.0"..<"5.0.0"' BBTB/Packages/RulesEngine/Package.swift` equals 1
    - Source: `grep -c 'product: "Crypto"' BBTB/Packages/RulesEngine/Package.swift` equals 1
    - Source: `grep -c 'Packages/RulesEngine' BBTB/Project.swift` ≥ 1
    - Source: `grep -c 'product: "RulesEngine"' BBTB/Project.swift` equals 2 (iOS app + macOS app)
    - CLI: `cd BBTB/Packages/RulesEngine && swift package resolve` exits 0
    - CLI: `cd BBTB && tuist generate` exits 0
  </acceptance_criteria>
  <done>Пакет существует, swift-crypto разрешён, Tuist знает о нём, оба app target декларируют dependency. Source файлы будут наполнены в следующих tasks.</done>
</task>

<task type="auto">
  <name>Task W1.2: Implement PublicKey.swift + RulesSigner.swift + Logger (Ed25519 verify)</name>
  <files>BBTB/Packages/RulesEngine/Sources/RulesEngine/PublicKey.swift, BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesSigner.swift, BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesEngineLogger.swift</files>
  <read_first>
    BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/AppGroupContainer.swift (enum-namespace pattern для PublicKey),
    BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBox/SingBoxConfigLoader.swift (public enum static helper pattern для RulesSigner),
    BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/TunnelLogger.swift (Logger subsystem/category pattern),
    BBTB/Packages/AppFeatures/Sources/MainScreenFeature/PerfSignposter.swift (Signposter subsystem pattern lines 22-49),
    .planning/phases/08-rules-engine-split-tunneling/08-RESEARCH.md (§ Pattern 2 — exact CryptoKit verify call lines 254-275),
    .planning/phases/08-rules-engine-split-tunneling/08-PATTERNS.md (§ 3 + § 4 + § 10 + § S-3)
  </read_first>
  <action>(1) `PublicKey.swift` (internal access — only RulesSigner uses):
- `import Foundation` + `import Crypto`
- `enum PublicKey { private static let publicKeyBytes: [UInt8] = [...] }`
- Bytes = 32-element placeholder литерал из 0x00..0x1F (TODO comment: «PHASE 8 W1 — populated с REAL public key bytes from `openssl pkey -pubin -outform DER | tail -c 32 | xxd -i` per user_setup в 08-W1-PLAN.md. До замены real bytes — production builds НЕ shipping; placeholder допустим только для CI tests с test-only fixtures»).
- `static let publicKey: Curve25519.Signing.PublicKey = { try! Curve25519.Signing.PublicKey(rawRepresentation: Data(publicKeyBytes)) }()` — try! justified, build bug if fails
- doc-comment объясняет: «Ed25519 public key — НЕ secret; rotation strategy deferred to v1.x (см. Pitfall 5 в 08-RESEARCH.md). См. wiki/rules-engine.md для rotation roadmap.»

(2) `RulesEngineLogger.swift` (internal):
- `import OSLog`
- `enum RulesEngineLogger { static let coordinator = Logger(subsystem: "app.bbtb.client", category: "rules-engine.coordinator"); static let fetcher = Logger(subsystem: "app.bbtb.client", category: "rules-engine.fetcher"); static let signer = Logger(subsystem: "app.bbtb.client", category: "rules-engine.signer") }`

(3) `RulesSigner.swift` (public namespace):
- `import Foundation` + `import Crypto`
- `public enum RulesSigner { public static func verify(message: Data, signature: Data) -> Bool }`
- Body: `guard signature.count == 64 else { RulesEngineLogger.signer.error("RulesSigner.verify rejected: signature.count=\(signature.count) ≠ 64"); return false }; let result = PublicKey.publicKey.isValidSignature(signature, for: message); if !result { RulesEngineLogger.signer.warning("RulesSigner.verify failed for message.count=\(message.count)") }; return result`
- Возвращаемый тип — `Bool` (НЕ throws); `false` на любую неудачу включая wrong-length signature; explicit early-return для wrong length чтобы не давать CryptoKit пытаться парсить garbage.
- doc-comment: «Ed25519 detached signature verify через swift-crypto CryptoKit re-export. < 5ms на A13+ hardware. NEVER throws; returns false на любую invalid input (wrong length / tampered / unrelated key).»</action>
  <verify>
    <automated>cd BBTB/Packages/RulesEngine && swift build 2>&1 | tail -3 | grep -qE 'Build complete|finished compiling' && grep -c '0x[0-9a-fA-F]\{1,2\}' BBTB/Packages/RulesEngine/Sources/RulesEngine/PublicKey.swift | xargs test 32 -le && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `cd BBTB/Packages/RulesEngine && swift build` exits 0
    - Source: `grep -c 'publicKeyBytes' BBTB/Packages/RulesEngine/Sources/RulesEngine/PublicKey.swift` ≥ 1
    - Source: `grep -oE '0x[0-9a-fA-F]+' BBTB/Packages/RulesEngine/Sources/RulesEngine/PublicKey.swift | wc -l` ≥ 32 (32 hex byte literals — может быть >32 если в comments есть hex; main check: exactly 32 в `publicKeyBytes` array)
    - Source: `grep -c 'Curve25519.Signing.PublicKey' BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesSigner.swift` ≥ 1
    - Source: `grep -c 'signature.count == 64' BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesSigner.swift` equals 1
    - Source: `grep -c 'subsystem: "app.bbtb.client", category: "rules-engine' BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesEngineLogger.swift` ≥ 3 (coordinator, fetcher, signer)
  </acceptance_criteria>
  <done>RulesSigner.verify полностью функционален; компилируется + swift build green; placeholder pubkey зафиксирован с TODO для real-bytes replacement (user_setup). RulesEngineLogger готов для использования из downstream coordinator W2.</done>
</task>

<task type="auto">
  <name>Task W1.3: Implement RulesFetcher.swift + RulesManifest.swift</name>
  <files>BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesFetcher.swift, BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesManifest.swift</files>
  <read_first>
    BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionURLFetcher.swift (lines 80-107 точный fetch pattern для копирования signature + error handling),
    BBTB/Packages/VPNCore/Sources/VPNCore/Subscription.swift (Codable struct pattern для Manifest),
    .planning/phases/08-rules-engine-split-tunneling/08-RESEARCH.md (§ Code Examples → RulesFetcher lines 622-654 + § Pattern §S-6 bounded concurrency),
    .planning/phases/08-rules-engine-split-tunneling/08-PATTERNS.md (§ 2 + § 5 + § S-6)
  </read_first>
  <action>(1) `RulesManifest.swift`:
- `import Foundation`
- `public struct RulesManifest: Codable, Sendable, Equatable { public let version: Int; public let minAppVersion: String; public let srsFormatVersion: Int; public let totalSizeBytes: Int; public let files: [FileEntry]; public let blockCompletely: CategoryBodies?; public let neverThroughVpn: CategoryBodies?; public let alwaysThroughVpn: CategoryBodies? }`
- `public enum Category: String, Codable, Sendable { case block = "block_completely"; case never = "never_through_vpn"; case always = "always_through_vpn" }`
- `public struct FileEntry: Codable, Sendable, Equatable { public let name: String; public let sha256: String; public let sigPath: String; public let category: Category }`
- `public struct CategoryBodies: Codable, Sendable, Equatable { public let domains: [String]?; public let ipCidrs: [String]?; public let countries: [String]? }` — nested type holding raw domain/CIDR/country arrays per category; consumed by W2.3 `RulesEngineCoordinator.currentSnapshot()` для materialization в `RulesSnapshot.CategoryEntries` (без которого RULES-09 read-only viewer навсегда empty)
- Custom `CodingKeys` (snake_case mapping — Codex thread 019e2841 confirmed naming convention):
  - На `RulesManifest`: `minAppVersion` → `"min_app_version"`, `srsFormatVersion` → `"srs_format_version"`, `totalSizeBytes` → `"total_size_bytes"`, `blockCompletely` → `"block_completely"`, `neverThroughVpn` → `"never_through_vpn"`, `alwaysThroughVpn` → `"always_through_vpn"`
  - На `FileEntry`: `sigPath` → `"sig_path"`
  - На `CategoryBodies`: `ipCidrs` → `"ip_cidrs"`
- `blockCompletely / neverThroughVpn / alwaysThroughVpn` все optional (могут отсутствовать в minimal admin manifests, должны decode-ить даже когда server-side payload содержит только `files` references); `CategoryBodies` inner arrays тоже optional для same reason (empty admin category → `{}` valid)

(2) `RulesFetcher.swift`:
- `import Foundation` + `import ConfigParser` (для reuse `SubscriptionURLFetcher.isBlockedHost` public helper from W0)
- `public enum RulesFetcher`
- `public struct FetchResult { public let body: Data; public let etag: String?; public let mirrorURL: URL }`
- `public enum FetchError: Error, Equatable, LocalizedError { case nonHTTPS(String); case malformedURL; case blockedHost(String); case notHTTPResponse; case httpStatusError(Int); case timeout; case payloadTooLarge(Int); case allMirrorsFailed([FetchError]) }` + `errorDescription` switch для каждого
- `public static func fetch(url: URL, session: URLSession = .shared, maxBytes: Int = 5 * 1024 * 1024) async throws -> FetchResult`:
    - guard scheme == "https" else throw nonHTTPS
    - guard host non-empty else throw malformedURL
    - guard !SubscriptionURLFetcher.isBlockedHost(host) else throw blockedHost (using public helper from W0)
    - URLRequest: timeoutInterval = 10, User-Agent "BBTB-Rules/0.8 (iOS / macOS)", Accept "application/json, application/octet-stream", cachePolicy .reloadIgnoringLocalCacheData
    - `let (data, response) = try await session.data(for: request)` — wrap в Task with cancellation handling, but URLSession уже async-aware
    - guard let http = response as? HTTPURLResponse else throw notHTTPResponse
    - guard 200..<300 contains http.statusCode else throw httpStatusError(http.statusCode)
    - guard data.count <= maxBytes else throw payloadTooLarge(data.count) — Pitfall 3 defense
    - return FetchResult(body: data, etag: http.value(forHTTPHeaderField: "ETag"), mirrorURL: url)
- `public static func fetchWithFailover(urls: [URL], session: URLSession = .shared, maxBytes: Int = 5 * 1024 * 1024) async throws -> FetchResult`:
    - guard !urls.isEmpty else throw allMirrorsFailed([])
    - sequential loop (concurrency=1 per DEC-06d-04): для каждого url пробуем fetch, на success — return; на failure — collect error и continue; после loop — throw allMirrorsFailed(collected)
    - Logging через RulesEngineLogger.fetcher: на каждый mirror attempt — `.info("Trying mirror \(idx+1)/\(urls.count): \(url.absoluteString, privacy: .public)")`; на failure — `.warning("Mirror \(idx+1) failed: \(error.localizedDescription, privacy: .public)")`; на success — `.notice("Mirror \(idx+1) succeeded: \(data.count) bytes, etag=\(etag ?? \"none\", privacy: .public)")`</action>
  <verify>
    <automated>cd BBTB/Packages/RulesEngine && swift build 2>&1 | tail -3 | grep -qE 'Build complete|finished compiling' && grep -c 'isBlockedHost' BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesFetcher.swift | xargs test 1 -eq && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `cd BBTB/Packages/RulesEngine && swift build` exits 0
    - Source: `grep -c 'public enum FetchError' BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesFetcher.swift` equals 1
    - Source: `grep -c 'SubscriptionURLFetcher.isBlockedHost' BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesFetcher.swift` equals 1
    - Source: `grep -c 'fetchWithFailover' BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesFetcher.swift` ≥ 2 (signature + implementation)
    - Source: `grep -c 'BBTB-Rules/0.8' BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesFetcher.swift` equals 1
    - Source: `grep -c 'public struct RulesManifest' BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesManifest.swift` equals 1
    - Source: `grep -c 'block_completely\|never_through_vpn\|always_through_vpn' BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesManifest.swift` ≥ 3
    - Source: `grep -c 'public struct CategoryBodies' BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesManifest.swift` equals 1
    - Source: `grep -c 'blockCompletely\|neverThroughVpn\|alwaysThroughVpn' BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesManifest.swift` ≥ 3 (three optional category-body fields declared)
    - Source: `grep -c '"ip_cidrs"' BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesManifest.swift` ≥ 1 (CodingKey для CategoryBodies.ipCidrs)
    - Source: `grep -c 'payloadTooLarge' BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesFetcher.swift` ≥ 2 (case definition + check)
  </acceptance_criteria>
  <done>RulesFetcher выполняет single-URL fetch + sequential mirror failover; RulesManifest decode-able с optional `CategoryBodies` block_completely / never_through_vpn / always_through_vpn (foundation для RULES-09 read-only viewer через W2.3 currentSnapshot); pre-flight max-size cap защищает от Pitfall 3 oversized payloads; SSRF reused через public W0 helper; all paths logged.</done>
</task>

<task type="auto">
  <name>Task W1.4: MockURLProtocol fixture + RulesSignerTests + RulesFetcherTests</name>
  <files>BBTB/Packages/RulesEngine/Tests/RulesEngineTests/Fixtures/MockURLProtocol.swift, BBTB/Packages/RulesEngine/Tests/RulesEngineTests/RulesSignerTests.swift, BBTB/Packages/RulesEngine/Tests/RulesEngineTests/RulesFetcherTests.swift</files>
  <read_first>
    BBTB/Packages/ConfigParser/Tests/ConfigParserTests/SubscriptionURLFetcherTests.swift (existing pattern — copy MockURLProtocol verbatim если он там есть либо реализовать с нуля по тому же recipe),
    BBTB/Packages/PacketTunnelKit/Tests/PacketTunnelKitTests/SingBoxConfigLoaderTests.swift (pure-function XCTest pattern для RulesSignerTests),
    BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesSigner.swift (just authored — exact API surface),
    BBTB/Packages/RulesEngine/Sources/RulesEngine/RulesFetcher.swift (just authored — exact API + error cases),
    .planning/phases/08-rules-engine-split-tunneling/08-PATTERNS.md (§ 25 + § 26 + Risks Surfaced #5)
  </read_first>
  <action>(1) `MockURLProtocol.swift` (test target only):
- `import XCTest` + `@testable import RulesEngine`
- `final class MockURLProtocol: URLProtocol { nonisolated(unsafe) static var requestHandler: ((URLRequest) throws -> (HTTPURLResponse, Data))?; override class func canInit(with request: URLRequest) -> Bool { return true }; override class func canonicalRequest(for request: URLRequest) -> URLRequest { return request }; override func startLoading() { ... uses requestHandler to return mock response or error }; override func stopLoading() {} }`
- Helper `static func makeSession() -> URLSession { let config = URLSessionConfiguration.ephemeral; config.protocolClasses = [MockURLProtocol.self]; return URLSession(configuration: config) }`

(2) `RulesSignerTests.swift` — minimum 3 тестов:
- `test_verify_acceptsValidSignature` — генерирует test Ed25519 keypair через CryptoKit, подписывает known message, делает temporary subclass или test-only function `withTestPublicKey` через `@testable import` чтобы swap PublicKey для теста (либо использует фиксированный hardcoded test pubkey-bytes + corresponding signature от known test private key, baked в test). Cleanest path: добавить в RulesSigner `internal static func verify(message: Data, signature: Data, key: Curve25519.Signing.PublicKey)` overload для injection — test calls overload, production calls original. Use this approach.
- `test_verify_rejectsTamperedSignature` — берёт valid signature, flip последний bit, expect verify returns false
- `test_verify_rejectsWrongLengthSignature` — 63-byte signature → false; 65-byte → false; empty → false
- (bonus) `test_verify_rejectsWrongMessage` — valid signature for message1, verify against message2 → false

(3) `RulesFetcherTests.swift` — minimum 6 тестов:
- `test_fetch_rejectsNonHTTPSScheme` — `RulesFetcher.fetch(url: URL(string: "http://example.com")!)` throws nonHTTPS("http")
- `test_fetch_rejectsBlockedHost` — `RulesFetcher.fetch(url: URL(string: "https://127.0.0.1/rules")!)` throws blockedHost
- `test_fetch_returnsBodyOn200` — MockURLProtocol returns 200 + JSON body; expect FetchResult.body matches
- `test_fetch_throwsOn500` — Mock returns 500; expect httpStatusError(500)
- `test_fetch_rejectsPayloadAboveMaxBytes` — Mock returns 10MB body, fetch с maxBytes=5MB → payloadTooLarge(10485760)
- `test_fetchWithFailover_succeedsOnSecondMirror` — Mock fails first URL with 503, succeeds on second; expect FetchResult.mirrorURL == second URL
- `test_fetchWithFailover_throwsAllFailedWhenAllMirrorsDown` — Mock returns 500 for all; expect allMirrorsFailed([httpStatusError(500), httpStatusError(500), httpStatusError(500)])
- `test_fetchWithFailover_emptyURLsThrowsAllFailed` — empty array → allMirrorsFailed([])</action>
  <verify>
    <automated>cd BBTB/Packages/RulesEngine && swift test 2>&1 | tail -15 | grep -E "Executed [0-9]+ tests" | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `cd BBTB/Packages/RulesEngine && swift test` exits 0
    - CLI: `cd BBTB/Packages/RulesEngine && swift test 2>&1 | tail -20 | grep -E "Test Suite 'RulesSignerTests' passed"` returns match
    - CLI: `cd BBTB/Packages/RulesEngine && swift test 2>&1 | tail -20 | grep -E "Test Suite 'RulesFetcherTests' passed"` returns match
    - CLI: `cd BBTB/Packages/RulesEngine && swift test 2>&1 | grep -E "Executed [0-9]+ tests, with 0 failures"` returns match
    - Source: `grep -c 'class MockURLProtocol' BBTB/Packages/RulesEngine/Tests/RulesEngineTests/Fixtures/MockURLProtocol.swift` equals 1
    - Source: `grep -cE 'func test_(verify|fetch)' BBTB/Packages/RulesEngine/Tests/RulesEngineTests/*.swift` ≥ 9 (3 signer + ≥ 6 fetcher)
  </acceptance_criteria>
  <done>Full RulesEngine test suite passing (≥ 9 tests, 0 failures); MockURLProtocol готов для reuse в W2 coordinator tests; RulesSigner и RulesFetcher proven correct in isolation. Ready for W2 integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → main app | RulesFetcher принимает untrusted HTTPS responses; tamper risk до RulesSigner verify |
| compile-time → runtime | Hardcoded Ed25519 pubkey зашит в binary; rotation deferred to v1.x |
| test target → production target | MockURLProtocol — test-only; production code никогда не registers MockURLProtocol |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-W1-01 | Tampering | RulesFetcher HTTPS downgrade attack | mitigate | `guard url.scheme == "https"` early return; explicit `nonHTTPS` error case; no follow-redirect to http в URLSession default |
| T-08-W1-02 | Spoofing | SSRF к localhost / RFC-1918 / link-local | mitigate | Reuse `SubscriptionURLFetcher.isBlockedHost` (Phase 2 audited blocklist) через public W0 helper; new package НЕ дублирует blocklist |
| T-08-W1-03 | Tampering | Forged manifest with manipulated `min_app_version` lock-out | mitigate (partial) | RulesSigner verify gates apply; W2 coordinator gates write. W1 layer не нужно — это W2 responsibility. Здесь mitigation = signature primitive correctness (≥ 4 unit-tests assert valid/tampered/wrong-length/wrong-message rejection) |
| T-08-W1-04 | DoS | Oversized payload exhausts memory (Pitfall 3 / NE 50MB limit) | mitigate | `maxBytes: Int = 5 * 1024 * 1024` parameter default + `payloadTooLarge` error case rejects up-front; W2 coordinator может tighten cap до 1MB если manifest size_bytes < 1MB known |
| T-08-W1-05 | Information disclosure | Hardcoded private key в repo accidentally | mitigate | user_setup explicit: «private key хранится на VPS, НЕ commit»; PublicKey.swift содержит ТОЛЬКО public bytes; .gitignore can include /tmp/bbtb-rules-private.pem pattern; pre-commit hook (recommended Phase 12) может grep for "-----BEGIN PRIVATE KEY-----" в diff |
| T-08-W1-06 | Tampering | swift-crypto version drift introduces bug в Ed25519 verify | accept | swift-crypto = Apple-supported library; tracking major-line 4.x bounded `..<5.0.0`; CryptoKit re-export на Apple platforms gives hardware-accelerated impl; test suite catches API regressions |
| T-08-W1-07 | Spoofing | Wrong-length signature passed to CryptoKit causes crash | mitigate | Explicit `guard signature.count == 64` before calling `isValidSignature`; test `test_verify_rejectsWrongLengthSignature` covers 63/65/0 byte cases |
| T-08-W1-08 | DoS | Mirror failover sequential exhausts 30s BGAppRefreshTask budget | mitigate | Per-mirror timeout = 10s; 3 mirrors × 10s = 30s worst-case, equal to BGAppRefreshTask budget; if budget exceeded, BGAppRefreshTask.expirationHandler cancels Task gracefully (W4 wiring); cooldown for force-update (W3) ensures user не triggers more than once per 60s |
</threat_model>

<verification>
- ✓ Package.swift валиден, swift-crypto резолвится, Tuist знает о пакете
- ✓ swift build green для library target
- ✓ swift test green (≥ 9 tests, 0 failures) — RulesSigner valid/tampered/wrong-length + RulesFetcher 6 paths
- ✓ Logger subsystems корректно объявлены
- ✓ PublicKey содержит ровно 32 byte literals (R8 invariant — будет финально assert в W7 через validate-r1-r6.sh extension)
- ✓ MockURLProtocol готов для W2 reuse
</verification>

<success_criteria>
1. **RulesEngine package exists и building:** SwiftPM манифест валиден, swift-crypto 4.x резолвится, package linked в Tuist iOS+macOS app targets.
2. **Ed25519 verify primitive correct:** RulesSigner.verify returns правильные результаты для valid/tampered/wrong-length/wrong-message inputs; signed off через ≥ 3 unit-tests.
3. **HTTPS fetcher с SSRF + size cap:** RulesFetcher.fetch enforces https-only, reuses W0 public isBlockedHost helper, 10s timeout, 5MB body cap, ≥ 6 unit-tests cover edge cases.
4. **Mirror failover sequential per DEC-06d-04:** fetchWithFailover пытается mirrors один за другим, aggregates errors, не делает parallel requests.
5. **Manifest model decodes:** RulesManifest Codable struct parses canonical server JSON с snake_case fields (`min_app_version`, `srs_format_version`, `total_size_bytes`, `sig_path`).
6. **Foundation laid для W2:** все public API surface declared в `<interfaces>` block; downstream coordinator может consume без дальнейших visibility-promotions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rules-engine-split-tunneling/08-W1-SUMMARY.md` со:
- Package structure listing
- swift-crypto version actually resolved (`Package.resolved` snippet)
- Test count + pass status
- PublicKey placeholder TODO reminder
- Files created list
- Tuist regenerate confirmation
</output>
