---
phase: 04-protocol-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BBTB/Packages/ConfigParser/Package.swift
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/StubParsers.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ShadowsocksURIParserTests.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Hysteria2URIParserTests.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ClashYAMLParserTests.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/VLESSURIParserTLSTests.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/ss-2022-aes-128-gcm.txt
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/ss-2022-percent-encoded.txt
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/ss-legacy-chacha20.txt
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/outline-access-key.txt
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/hy2-with-obfs.txt
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/hy2-insecure.txt
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/hy2-multi-port.txt
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/vless-tls-no-flow.txt
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/vless-tls-vision.txt
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/clash-mixed-proxies.yaml
autonomous: true
requirements:
  - PROTO-03
  - PROTO-04
  - PROTO-05
  - IMP-04
  - IMP-05
requirements_addressed:
  - PROTO-03
  - PROTO-04
  - PROTO-05
  - IMP-04
  - IMP-05
must_haves:
  truths:
    - "ConfigParser компилируется с Yams 6.2.1 dependency"
    - "AnyParsedConfig содержит 5 case'ов (vlessReality, vlessTLS, trojan, shadowsocks, hysteria2)"
    - "Test bundle содержит fixtures для всех 5 новых сценариев (SS-2022 b64, SS-2022 percent, SS legacy, Outline, Hy2 obfs, Hy2 insecure, Hy2 multi-port, VLESS-TLS no-flow, VLESS-TLS vision, Clash mixed)"
    - "Wave 0 test файлы существуют и failed-падают (RED) — Nyquist gate"
  artifacts:
    - path: "BBTB/Packages/ConfigParser/Package.swift"
      provides: "Yams 6.2.1 SPM dependency для ClashYAMLParser"
      contains: ".package(url: \"https://github.com/jpsim/Yams.git\", from: \"6.2.1\")"
    - path: "BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift"
      provides: "AnyParsedConfig + ParsedVLESSTLS + ParsedShadowsocks + ParsedHysteria2 structs + UnsupportedReason расширение"
      contains: "case vlessTLS"
    - path: "BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ShadowsocksURIParserTests.swift"
      provides: "Wave 0 RED stubs для PROTO-04 dual-decoder + outline + unsupported method"
    - path: "BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Hysteria2URIParserTests.swift"
      provides: "Wave 0 RED stubs для PROTO-05 (both schemes + D-08 + multi-port + obfs)"
    - path: "BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ClashYAMLParserTests.swift"
      provides: "Wave 0 RED stubs для IMP-05 (extractsProxies + mixedProxies + brokenYAML + alpnStringVsArray)"
    - path: "BBTB/Packages/ConfigParser/Tests/ConfigParserTests/VLESSURIParserTLSTests.swift"
      provides: "Wave 0 RED stubs для PROTO-03 (security=tls + Vision flow + Reality precedence)"
  key_links:
    - from: "AnyParsedConfig enum"
      to: "downstream switches (PoolBuilder, ConfigImporter)"
      via: "Swift exhaustiveness check"
      pattern: "case \\.(vlessTLS|shadowsocks|hysteria2)"
    - from: "Test fixtures Bundle.module"
      to: "Wave 0 test files"
      via: "Bundle resource loading из subdirectory Fixtures"
      pattern: "loadFixture\\("
---

## Phase Goal

**As a** BBTB-разработчик, **I want to** иметь test scaffold + enum scaffold + Yams dependency для Phase 4 новых протоколов, **so that** последующие vertical-slice планы могут реализовывать VLESS+TLS / SS / Hy2 / Clash YAML с TDD-циклами RED→GREEN.

<objective>
Foundation wave: добавить Yams 6.2.1 в ConfigParser SPM, расширить `AnyParsedConfig` enum на 3 новых case (vlessTLS, shadowsocks, hysteria2) + новые `Parsed*` structs, обновить `UnsupportedReason` и `StubParsers.supportedSchemesInPhase4`, создать пустые/failing test файлы и fixtures для всех Wave 0 gaps из 04-VALIDATION.md.

Purpose: Wave 0 — Nyquist gate. Все Wave 1+ tasks ссылаются на `swift test --filter <X>` команды; без существующих test файлов эти команды дают "no such test class" и не падают, что нарушает RED→GREEN. Этот план создаёт scaffold, который Wave 1+ заполняют.

Output:
- `BBTB/Packages/ConfigParser/Package.swift` с Yams 6.2.1.
- `ImportedServer.swift` с 5-case enum + 3 новых struct.
- 11 fixture файлов в `Tests/.../Fixtures/`.
- 4 test файла со skeleton tests (минимум 1 failing `XCTAssertTrue(false)` placeholder на класс).
- `StubParsers.supportedSchemesInPhase4: Set<String>` определён.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-protocol-expansion/04-CONTEXT.md
@.planning/phases/04-protocol-expansion/04-RESEARCH.md
@.planning/phases/04-protocol-expansion/04-PATTERNS.md
@.planning/phases/04-protocol-expansion/04-VALIDATION.md
@BBTB/Packages/ConfigParser/Package.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/StubParsers.swift
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/TrojanURIParserTests.swift
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures

<interfaces>
<!-- Current AnyParsedConfig — ImportedServer.swift:9-12 -->
Existing (Phase 1/2):
- public enum AnyParsedConfig: Sendable, Equatable { case vlessReality(ParsedVLESS); case trojan(ParsedTrojan) }
- public enum UnsupportedReason: String, Sendable, Equatable { case schemaUnsupportedInPhase2; case transportUnsupported; case malformedURI; ... }

Existing (Phase 2):
- public struct ParsedVLESS — uuid, host, port, sni, publicKey, shortId, fingerprint, alpn, flow, transport, remarks
- public struct ParsedTrojan — password, host, port, security, sni, fingerprint, alpn, transport, remarks

Existing (StubParsers.swift):
- public static let supportedSchemesInPhase2: Set<String> = ["vless", "trojan"]
- public static let knownSchemes: Set<String> = [...] (содержит "ss", "hy2", "hysteria2", "vmess", "wireguard", "ssh", "socks5", "socks")
- private static func defaultPortForScheme(_ s: String) -> Int { /* "ss":8388, "hy2","hysteria2":443, ... */ }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Yams 6.2.1 SPM dependency + AnyParsedConfig + новые Parsed* structs + UnsupportedReason расширение</name>
  <files>BBTB/Packages/ConfigParser/Package.swift, BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift, BBTB/Packages/ConfigParser/Sources/ConfigParser/StubParsers.swift</files>
  <read_first>
    - BBTB/Packages/ConfigParser/Package.swift (current dependencies/target setup)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift (AnyParsedConfig + UnsupportedReason existing pattern lines 9-13)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/StubParsers.swift (supportedSchemesInPhase2 lines 12-13, knownSchemes lines 16-)
    - .planning/phases/04-protocol-expansion/04-CONTEXT.md (D-01, D-03, D-05, D-07 — точный состав новых structs)
    - .planning/phases/04-protocol-expansion/04-PATTERNS.md (раздел 16 ImportedServer expansion lines 917-949; раздел 13 ConfigParser Package.swift lines 770-796)
    - .planning/phases/04-protocol-expansion/04-RESEARCH.md (Standard Stack — Yams 6.2.1 verified 2026-02-05 from jpsim/Yams)
  </read_first>
  <behavior>
    - Package.swift содержит .package(url: "https://github.com/jpsim/Yams.git", from: "6.2.1") в dependencies
    - ConfigParser target dependencies включает .product(name: "Yams", package: "Yams")
    - swift package resolve проходит без ошибок (Yams 6.2.1 разрешается)
    - swift build для ConfigParser package проходит
    - AnyParsedConfig содержит 5 case'ов: vlessReality, vlessTLS, trojan, shadowsocks, hysteria2
    - ParsedVLESSTLS struct: uuid (UUID), host (String), port (Int), flow (String?), sni (String), fingerprint (String), alpn ([String]), networkType (String), remarks (String?), Sendable + Equatable + public init
    - ParsedShadowsocks struct: host (String), port (Int), method (String), password (String), remarks (String?), Sendable + Equatable + public init
    - ParsedHysteria2 struct: host (String), port (Int), auth (String), sni (String), fingerprint (String?), obfs (String?), obfsPassword (String?), allowInsecure (Bool), pinSHA256 (String?), remarks (String?), Sendable + Equatable + public init
    - UnsupportedReason содержит существующие case'ы + новые: schemaUnsupportedInPhase4, unsupportedSSMethod, multiPortNotSupported
    - StubParsers.supportedSchemesInPhase4: Set<String> = ["vless", "trojan", "ss", "hy2", "hysteria2"] (новая константа; supportedSchemesInPhase2 остаётся для backward compat)
  </behavior>
  <action>
    Расширь ConfigParser SPM package по образцу D-01/D-03/D-05/D-07 из 04-CONTEXT.md.

    В `Package.swift` добавь `.package(url: "https://github.com/jpsim/Yams.git", from: "6.2.1")` в массив dependencies; в target `ConfigParser` `dependencies` массив добавь `.product(name: "Yams", package: "Yams")`. testTarget остаётся без изменений.

    В `ImportedServer.swift` расширь enum `AnyParsedConfig` добавлением трёх case: `vlessTLS(ParsedVLESSTLS)`, `shadowsocks(ParsedShadowsocks)`, `hysteria2(ParsedHysteria2)`. Сохрани существующие case'ы `vlessReality(ParsedVLESS)` и `trojan(ParsedTrojan)`. Добавь три новые struct `ParsedVLESSTLS`, `ParsedShadowsocks`, `ParsedHysteria2` с полями из D-03 / D-05 / D-07; все три — public, Sendable, Equatable, с public memberwise init (явный init с параметрами в порядке полей; default'ов нет — все required). В enum `UnsupportedReason` добавь три новых rawValue case: `schemaUnsupportedInPhase4`, `unsupportedSSMethod`, `multiPortNotSupported` (рядом с существующими).

    В `StubParsers.swift` добавь новую public константу `supportedSchemesInPhase4: Set<String> = ["vless", "trojan", "ss", "hy2", "hysteria2"]` рядом с `supportedSchemesInPhase2` (последнюю не удалять — break существующих StubParsersTests). knownSchemes не трогать (уже содержит все нужные схемы).

    Реализация по D-01 (D-02 откладывается на Plan 02), D-05, D-07 в `.planning/phases/04-protocol-expansion/04-CONTEXT.md`.
  </action>
  <verify>
    <automated>cd BBTB/Packages/ConfigParser && swift package resolve && swift build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File BBTB/Packages/ConfigParser/Package.swift contains literal string `jpsim/Yams.git`
    - File BBTB/Packages/ConfigParser/Package.swift contains literal string `from: "6.2.1"`
    - File BBTB/Packages/ConfigParser/Package.swift contains literal string `.product(name: "Yams", package: "Yams")`
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift contains literal `case vlessTLS(ParsedVLESSTLS)`
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift contains literal `case shadowsocks(ParsedShadowsocks)`
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift contains literal `case hysteria2(ParsedHysteria2)`
    - File contains literal `public struct ParsedVLESSTLS:` followed within 30 lines by `flow:` and `networkType:`
    - File contains literal `public struct ParsedShadowsocks:` followed within 20 lines by `method:` and `password:`
    - File contains literal `public struct ParsedHysteria2:` followed within 40 lines by `allowInsecure:` and `pinSHA256:`
    - File contains literal `case schemaUnsupportedInPhase4`
    - File contains literal `case unsupportedSSMethod`
    - File contains literal `case multiPortNotSupported`
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/StubParsers.swift contains literal `supportedSchemesInPhase4`
    - `swift package resolve` exits 0 в BBTB/Packages/ConfigParser
    - `swift build` exits 0 в BBTB/Packages/ConfigParser (компилируется без ошибок)
  </acceptance_criteria>
  <done>AnyParsedConfig enum имеет 5 case'ов, три новых Parsed* struct определены с правильными полями, Yams 6.2.1 разрешён и пакет компилируется.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 test fixtures (11 файлов)</name>
  <files>BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/ss-2022-aes-128-gcm.txt, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/ss-2022-percent-encoded.txt, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/ss-legacy-chacha20.txt, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/outline-access-key.txt, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/hy2-with-obfs.txt, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/hy2-insecure.txt, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/hy2-multi-port.txt, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/vless-tls-no-flow.txt, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/vless-tls-vision.txt, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/clash-mixed-proxies.yaml</files>
  <read_first>
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures (current Fixtures contents — ls of directory)
    - .planning/phases/04-protocol-expansion/04-RESEARCH.md (Code Examples — SIP002 spec для SS-2022 method/password; Hysteria2 URI Scheme; Clash YAML structure)
    - .planning/phases/04-protocol-expansion/04-VALIDATION.md (Wave 0 Requirements lines 60-66)
    - .planning/phases/04-protocol-expansion/04-PATTERNS.md (Fixtures classification lines 51-63)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/unsupported-ss-uri.txt (если есть — как референсный формат)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/trojan-tcp-uri.txt (если есть — как референсный URI fixture)
  </read_first>
  <behavior>
    - ss-2022-aes-128-gcm.txt содержит SS-2022 URI с base64-encoded userinfo (legacy SIP002 form) для method "2022-blake3-aes-128-gcm"
    - ss-2022-percent-encoded.txt содержит SS-2022 URI с percent-encoded userinfo (SIP022 form) для method "2022-blake3-aes-256-gcm"
    - ss-legacy-chacha20.txt содержит legacy SS URI с base64url userinfo для method "chacha20-ietf-poly1305"
    - outline-access-key.txt содержит SIP002 ss:// URI типичной формы из Outline (base64url userinfo + #tag fragment)
    - hy2-with-obfs.txt содержит hy2:// URI с obfs=salamander, obfs-password=<value>, sni=<value>
    - hy2-insecure.txt содержит hy2:// URI с insecure=1 (для D-08 теста)
    - hy2-multi-port.txt содержит hy2:// URI с портом в формате 443,8443 (для D-09 reject теста)
    - vless-tls-no-flow.txt содержит vless:// URI с security=tls без flow
    - vless-tls-vision.txt содержит vless:// URI с security=tls и flow=xtls-rprx-vision (без pbk/sid → не Reality)
    - clash-mixed-proxies.yaml содержит valid YAML с секцией proxies, минимум: 1 ss (cipher 2022-blake3-aes-256-gcm), 1 trojan, 1 hysteria2 (skip-cert-verify: true), 1 vmess (unsupported), 1 vless с reality-opts (Reality), 1 vless без reality (TLS). Минимум 6 proxies.
    - Все файлы non-empty, single-line URI files оканчиваются newline, YAML файл валиден (`python3 -c "import yaml; yaml.safe_load(open('...'))"` парсит)
  </behavior>
  <action>
    Создай 11 fixture файлов в `BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/`.

    Для каждого `.txt` файла — одна строка URI с финальным newline. Конкретные значения хостов/паролей — любые валидные literals (например `example.com`, `vpn.test`, password32bytesbase64encodedstringhere); полностью fictional, не реальные сервера. Hysteria2 fixtures должны иметь `auth@host:port?sni=...` структуру + специфичные query params; SS fixtures — `ss://userinfo@host:port#tag` с userinfo либо base64url(method:password) (legacy), либо percent-encoded(method:password) (2022). VLESS-TLS — `vless://uuid@host:443?security=tls&...&fp=chrome` (для vision — добавить `&flow=xtls-rprx-vision`).

    Для `clash-mixed-proxies.yaml`: top-level ключ `proxies:` со списком 6+ объектов. Каждый proxy объект — `name`, `type`, `server`, `port` + type-specific поля (`cipher` + `password` для ss; `password` + `sni` + `network` для trojan; `password` + `sni` + `skip-cert-verify` для hysteria2; `uuid` + `network` + `tls`+ `servername` для vless без Reality; `uuid` + `reality-opts` с `public-key` + `short-id` для vless+reality; `uuid` для vmess). Все значения fictional. Файл должен быть валиден YAML (test тек 4 проверит парсинг через Yams в Plan 05).

    Использовать literal спеки из 04-RESEARCH.md `## Code Examples` (Example 2 SS userinfo, Pattern 4 Clash YAML structure, Hysteria2 URI Scheme) и 04-PATTERNS.md раздел `## File Classification`.
  </action>
  <verify>
    <automated>cd BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures && test -s ss-2022-aes-128-gcm.txt && test -s ss-2022-percent-encoded.txt && test -s ss-legacy-chacha20.txt && test -s outline-access-key.txt && test -s hy2-with-obfs.txt && test -s hy2-insecure.txt && test -s hy2-multi-port.txt && test -s vless-tls-no-flow.txt && test -s vless-tls-vision.txt && test -s clash-mixed-proxies.yaml && python3 -c "import yaml,sys; yaml.safe_load(open('clash-mixed-proxies.yaml'))" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 10 fixture file paths exist and are non-empty (`test -s` exits 0 for each)
    - ss-2022-aes-128-gcm.txt content starts with `ss://`
    - ss-2022-percent-encoded.txt content starts with `ss://`
    - ss-legacy-chacha20.txt content starts with `ss://`
    - outline-access-key.txt content starts with `ss://`
    - hy2-with-obfs.txt content matches regex `^hy(steria)?2://.*obfs=salamander`
    - hy2-insecure.txt content matches regex `^hy(steria)?2://.*insecure=1`
    - hy2-multi-port.txt content contains substring `,` between port digits (multi-port form)
    - vless-tls-no-flow.txt content starts with `vless://` AND contains `security=tls` AND does NOT contain `flow=`
    - vless-tls-vision.txt content starts with `vless://` AND contains `security=tls` AND contains `flow=xtls-rprx-vision` AND does NOT contain `pbk=`
    - clash-mixed-proxies.yaml: `python3 -c "import yaml; d=yaml.safe_load(open('...')); assert 'proxies' in d; assert len(d['proxies']) >= 6"` exits 0
    - clash-mixed-proxies.yaml content contains types: `ss`, `trojan`, `hysteria2`, `vmess`, `vless`
  </acceptance_criteria>
  <done>10 fixture файлов созданы, валидные по формату; clash YAML парсится Python yaml.safe_load и содержит ≥6 proxies со всеми требуемыми типами.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 test scaffolds — 4 новых test файла (RED stubs)</name>
  <files>BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ShadowsocksURIParserTests.swift, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Hysteria2URIParserTests.swift, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ClashYAMLParserTests.swift, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/VLESSURIParserTLSTests.swift</files>
  <read_first>
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/TrojanURIParserTests.swift (loadFixture helper, test method pattern, negative test pattern — lines 6-77)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/VLESSURIParserTests.swift (existing VLESS test pattern; new TLS tests должны coexist без regressions)
    - .planning/phases/04-protocol-expansion/04-PATTERNS.md (раздел 14 URI Parser Tests lines 798-849 — per-parser test coverage)
    - .planning/phases/04-protocol-expansion/04-VALIDATION.md (Per-Task Verification Map для VLESS-TLS, SS, Hy2, Clash YAML test classes)
    - .planning/phases/04-protocol-expansion/04-RESEARCH.md (Phase Requirements → Test Map lines 875-902 — список конкретных test method names per parser)
  </read_first>
  <behavior>
    - Каждый test файл компилируется (даже если parser type ещё не существует — используется only `XCTFail("not implemented yet")` или `XCTAssertTrue(false)` placeholder)
    - Каждый файл содержит final class <Name>Tests: XCTestCase с минимум 1 test method, который failed-падает (RED state)
    - ShadowsocksURIParserTests включает заглушки минимум для: test_2022_base64_parses, test_2022_percentEncoded_parses, test_legacy_chacha20_parses, test_unknownMethod_unsupported, test_outlineAccessKey_parses, test_malformedURI_throws
    - Hysteria2URIParserTests включает заглушки для: test_bothSchemes_parse, test_insecureFlag_setsAllowInsecure, test_multiPort_rejects, test_obfsSalamander_parses, test_obfsNotSalamander_throws
    - ClashYAMLParserTests включает заглушки для: test_extractsProxies, test_mixedProxies_classifiedCorrectly, test_brokenYAML_returnsEmpty, test_alpnStringVsArray_handled
    - VLESSURIParserTLSTests включает заглушки для: test_securityTLS_returnsVlessTLS, test_visionFlow_preserved, test_noFlow_nilField, test_realityWithExtraTLS_returnsReality
    - loadFixture helper доступен (либо локальный copy из TrojanURIParserTests, либо общий)
    - Если тип парсера ещё не существует (например ShadowsocksURIParser) — тест НЕ обращается к нему напрямую (использует XCTFail/XCTAssertTrue(false) placeholder), чтобы файл компилировался
    - swift test --filter ShadowsocksURIParserTests завершается с failure exit code (хотя бы 1 fail, но НЕ "no such test class") — RED gate
    - Аналогично для остальных трёх классов
  </behavior>
  <action>
    Создай четыре новых XCTest файла по образцу `BBTB/Packages/ConfigParser/Tests/ConfigParserTests/TrojanURIParserTests.swift`.

    Каждый файл: `import XCTest`, `@testable import ConfigParser`, `final class <Name>Tests: XCTestCase { ... }`. Включи приватный `loadFixture(_:ext:)` helper (copy от TrojanURIParserTests) для последующих green-фаз. Все test methods — placeholder заглушки: `XCTFail("Pending Plan 0X: <test description>")` (с указанием в каком Plan это станет green: Plan 02 для VLESS-TLS, Plan 03 для SS, Plan 04 для Hy2, Plan 05 для Clash YAML).

    Конкретный список test method names для каждого файла — взять из .planning/phases/04-protocol-expansion/04-RESEARCH.md раздел `## Validation Architecture` → `Phase Requirements → Test Map`. Не вызывать пока несуществующие типы (`ShadowsocksURIParser`, `Hysteria2URIParser`, `ClashYAMLParser`) — placeholder только через XCTFail. ParsedVLESSTLS уже определён в Task 1, поэтому в `VLESSURIParserTLSTests` placeholder может ссылаться на тип, но not вызывать parser (parser modify — Plan 02).

    Цель — RED-стейт: `swift test --filter <Class>` должен завершиться failure (а не compile error, не "no test class"). Это Nyquist gate для Wave 1.
  </action>
  <verify>
    <automated>cd BBTB/Packages/ConfigParser && swift test --filter "ShadowsocksURIParserTests|Hysteria2URIParserTests|ClashYAMLParserTests|VLESSURIParserTLSTests" 2>&1 | tee /tmp/wave0.log | tail -50; grep -E "Test Case|failed" /tmp/wave0.log | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ShadowsocksURIParserTests.swift exists and contains `final class ShadowsocksURIParserTests: XCTestCase`
    - File contains literal `test_2022_base64`, `test_2022_percentEncoded`, `test_legacy_chacha20`, `test_unknownMethod_unsupported`, `test_outlineAccessKey`, `test_malformedURI_throws`
    - File BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Hysteria2URIParserTests.swift exists and contains `final class Hysteria2URIParserTests: XCTestCase`
    - File contains literal `test_bothSchemes`, `test_insecureFlag`, `test_multiPort_rejects`, `test_obfsSalamander`
    - File BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ClashYAMLParserTests.swift exists and contains `final class ClashYAMLParserTests: XCTestCase`
    - File contains literal `test_extractsProxies`, `test_mixedProxies`, `test_brokenYAML_returnsEmpty`, `test_alpnStringVsArray`
    - File BBTB/Packages/ConfigParser/Tests/ConfigParserTests/VLESSURIParserTLSTests.swift exists and contains `final class VLESSURIParserTLSTests: XCTestCase`
    - File contains literal `test_securityTLS`, `test_visionFlow`, `test_noFlow`, `test_realityWithExtraTLS`
    - `swift test --filter ShadowsocksURIParserTests` exits non-zero (test failed, not compile error)
    - `swift test --filter Hysteria2URIParserTests` exits non-zero
    - `swift test --filter ClashYAMLParserTests` exits non-zero
    - `swift test --filter VLESSURIParserTLSTests` exits non-zero
    - In /tmp/wave0.log грep matches "Test Case" lines (не "error: no such test")
  </acceptance_criteria>
  <done>4 test файла существуют, компилируются, содержат test method скелеты с XCTFail placeholders. swift test --filter завершается non-zero (RED) — готов RED gate для Wave 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External SPM (jpsim/Yams) → build | Untrusted SPM dependency pulled into ConfigParser; build resolves transitively |
| Test fixtures → Bundle.module | Static resources, no runtime mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W0-01 | Tampering | Yams SPM dependency | mitigate | Pin to `from: "6.2.1"` (semver-major lock); jpsim/Yams — 1.2k stars, MIT, 5+ лет на рынке (verified 04-RESEARCH.md Security Domain) |
| T-04-W0-02 | DoS | Future ClashYAMLParser parsing malicious YAML (anchors/recursion) | accept | LibYAML built-in depth limits; will be tested in Plan 05 test_brokenYAML_returnsEmpty |
| T-04-W0-03 | Information disclosure | Test fixtures committed in repo | accept | Все fixture credentials fictional (`example.com`, fictional UUID/passwords); проверено acceptance_criteria |
| T-04-W0-04 | Tampering | AnyParsedConfig enum exhaustiveness | mitigate | Public enum — Swift compiler enforce exhaustive switch downstream; pattern verified в Pitfall 7 (04-RESEARCH.md) — добавление case'ов вызовет compile errors в существующих switches, что обеспечит coverage Plan 06 |

R1 invariant в этом плане НЕ применим (нет JSON outbound generation). R1 enforcement будет в Plan 02/03/04 (templates + builders) и Plan 04 specific (Hy2 R1 EXCEPTION + invariant test).
</threat_model>

<verification>
- `swift package resolve && swift build` в BBTB/Packages/ConfigParser проходит (Yams resolved).
- `swift test --filter "ShadowsocksURIParserTests|Hysteria2URIParserTests|ClashYAMLParserTests|VLESSURIParserTLSTests"` падает с failures (не compile errors) — RED gate.
- Существующие test классы (TrojanURIParserTests, VLESSURIParserTests, StubParsersTests) не сломаны: `swift test --filter "TrojanURIParserTests|VLESSURIParserTests|StubParsersTests"` exit 0.
- Все 11 fixture файлов созданы; Clash YAML валиден.
</verification>

<success_criteria>
- AnyParsedConfig содержит 5 case'ов с правильными struct'ами.
- Yams 6.2.1 разрешена и компилируется.
- 4 test файла существуют с placeholder XCTFail заглушками; падают (RED) при запуске.
- 11 fixture файлов созданы с валидным содержимым.
- StubParsers.supportedSchemesInPhase4 определена.
- Существующие Phase 1/2/3 тесты не сломаны.
</success_criteria>

<output>
After completion, create `.planning/phases/04-protocol-expansion/04-01-SUMMARY.md`
</output>
