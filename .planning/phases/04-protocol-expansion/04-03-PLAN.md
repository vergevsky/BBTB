---
phase: 04-protocol-expansion
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/ShadowsocksURIParser.swift
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ShadowsocksURIParserTests.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift
  - BBTB/Packages/Protocols/Shadowsocks/Package.swift
  - BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/ShadowsocksHandler.swift
  - BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/ConfigBuilder.swift
  - BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/Resources/SingBoxConfigTemplate.shadowsocks.json
  - BBTB/Packages/Protocols/Shadowsocks/Tests/ShadowsocksTests/ConfigBuilderTests.swift
autonomous: true
requirements:
  - PROTO-04
requirements_addressed:
  - PROTO-04
must_haves:
  truths:
    - "ShadowsocksURIParser парсит SIP002 ss:// в obоих формах: percent-encoded (AEAD-2022) и base64url (legacy)"
    - "Поддерживаемые методы: 2022-blake3-aes-128-gcm/aes-256-gcm/chacha20-poly1305 + aes-128-gcm/aes-192-gcm/aes-256-gcm/chacha20-ietf-poly1305/xchacha20-ietf-poly1305"
    - "Неизвестный метод → throws ShadowsocksURIError.unsupportedMethod(String)"
    - "Outline access keys парсятся (это SIP002 ss:// формат — same parser)"
    - "Protocols/Shadowsocks package компилируется: handler identifier=\"shadowsocks\", ConfigBuilder генерирует sing-box JSON"
    - "Shadowsocks sing-box JSON БЕЗ TLS блока (SS encrypted на уровне протокола), проходит SingBoxConfigLoader.validate"
    - "PoolBuilder.buildSingBoxJSON для pool с .shadowsocks — генерирует outbound type=\"shadowsocks\" с method/password/network tcp"
    - "UniversalImportParser routes ss:// → supported (.shadowsocks)"
  artifacts:
    - path: "BBTB/Packages/ConfigParser/Sources/ConfigParser/ShadowsocksURIParser.swift"
      provides: "public static func parse(_ uri: String) throws -> ParsedShadowsocks (dual-decoder для userinfo)"
      contains: "supportedSSMethods"
    - path: "BBTB/Packages/Protocols/Shadowsocks/Package.swift"
      provides: "SPM package Shadowsocks"
      contains: "name: \"Shadowsocks\""
    - path: "BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/ShadowsocksHandler.swift"
      provides: "VPNProtocolHandler identifier=shadowsocks"
      contains: "identifier = \"shadowsocks\""
    - path: "BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/Resources/SingBoxConfigTemplate.shadowsocks.json"
      provides: "sing-box outbound type=shadowsocks БЕЗ tls block"
      contains: "\"type\": \"shadowsocks\""
  key_links:
    - from: "ShadowsocksURIParser.decodeUserinfo"
      to: "supportedSSMethods Set"
      via: "method whitelist check"
      pattern: "supportedSSMethods\\.contains"
    - from: "PoolBuilder switch parsed"
      to: "buildShadowsocksOutbound"
      via: "case .shadowsocks(let s)"
      pattern: "buildShadowsocksOutbound"
    - from: "UniversalImportParser.parseSingleURI"
      to: "ShadowsocksURIParser.parse"
      via: "case \"ss\":"
      pattern: "ShadowsocksURIParser\\.parse"
---

## Phase Goal

**As a** user, **I want to** импортировать Shadowsocks-2022 URI или Outline access key и подключиться, **so that** мой Outline-сгенерированный конфиг или SS-2022 сервер работает в BBTB.

<objective>
Vertical slice для PROTO-04 (Shadowsocks-2022 + legacy SS + Outline access keys):
1. Создать `ShadowsocksURIParser.swift` с dual-decoder userinfo (Pitfall 1 mitigation): percent-encoded path (SS-2022/SIP022) первый, base64url fallback (legacy SIP002) второй; whitelist методов (2022-blake3-* + legacy AEAD).
2. Создать SPM package `BBTB/Packages/Protocols/Shadowsocks/` с handler, ConfigBuilder (без TLS блока — SS encrypted на уровне протокола), sing-box JSON template.
3. Расширить `PoolBuilder` функцией `buildShadowsocksOutbound` и case `.shadowsocks` в switch.
4. Обновить `UniversalImportParser.parseSingleURI` case "ss" → ShadowsocksURIParser.
5. Заменить XCTFail-placeholders в `ShadowsocksURIParserTests` на полноценные GREEN-тесты.

Purpose: PROTO-04 + Outline access keys (IMP-05 часть — D-11 единственный parser покрывает оба).

Output: импорт `ss://userinfo@host:port#tag` (включая Outline-генерированные access keys) → AnyParsedConfig.shadowsocks(ParsedShadowsocks) → sing-box outbound type="shadowsocks" с method/password/network=tcp; validate OK.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protocol-expansion/04-CONTEXT.md
@.planning/phases/04-protocol-expansion/04-RESEARCH.md
@.planning/phases/04-protocol-expansion/04-PATTERNS.md
@.planning/phases/04-01-SUMMARY.md
@.planning/phases/04-02-SUMMARY.md
@BBTB/Packages/ConfigParser/Sources/ConfigParser/TrojanURIParser.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift
@BBTB/Packages/Protocols/Trojan/Package.swift
@BBTB/Packages/Protocols/Trojan/Sources/Trojan/TrojanHandler.swift
@BBTB/Packages/Protocols/Trojan/Sources/Trojan/ConfigBuilder.swift
@BBTB/Packages/Protocols/Trojan/Sources/Trojan/Resources/SingBoxConfigTemplate.trojan-tcp.json
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ShadowsocksURIParserTests.swift
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/TrojanURIParserTests.swift
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures

<interfaces>
Existing (Plan 01):
- public struct ParsedShadowsocks — host (String), port (Int), method (String), password (String), remarks (String?), Sendable + Equatable
- public enum UnsupportedReason — includes unsupportedSSMethod, schemaUnsupportedInPhase4

Existing (Plan 02 — VLESSTLS pattern):
- BBTB/Packages/Protocols/VLESSTLS/ — SPM package structure (Package.swift + Sources + Tests + Resources)

SIP002 reference:
- URI form: ss://userinfo@host:port#tag (userinfo либо base64url(method:password), либо percent-encoded(method:password))
- SIP022 (AEAD-2022): userinfo MUST be percent-encoded (NOT base64url)
- key length per method: 16 bytes для aes-128-gcm, 32 bytes для aes-256-gcm / chacha20-poly1305 / 2022-blake3-*

sing-box outbound shadowsocks fields:
- type: "shadowsocks", tag, server, server_port, method, password, network ("tcp" | "udp")
- NO tls block
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ShadowsocksURIParser.swift с dual-decoder + полные tests</name>
  <files>BBTB/Packages/ConfigParser/Sources/ConfigParser/ShadowsocksURIParser.swift, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ShadowsocksURIParserTests.swift, BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift</files>
  <read_first>
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/TrojanURIParser.swift (паттерн URI parser: ParsedX struct + XXXURIError enum + enum XXXURIParser с public static parse, lines 1-127)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift (после Plan 01 — ParsedShadowsocks struct fields)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift (parseSingleURI switch — найти grep'ом `case "vless"` и `case "trojan"` для расположения нового case "ss")
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ShadowsocksURIParserTests.swift (Plan 01 RED placeholders — заменить на GREEN tests)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/TrojanURIParserTests.swift (loadFixture helper, negative test pattern lines 6-77)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionURLFetcher.swift (если содержит base64-decode helper с padding tolerance — переиспользовать)
    - .planning/phases/04-protocol-expansion/04-RESEARCH.md (Example 2 lines 619-680 — точный код decodeUserinfo dual-path; Pitfall 1 lines 503-512)
    - .planning/phases/04-protocol-expansion/04-CONTEXT.md (D-04 — методы whitelist; D-05 — поля)
    - .planning/phases/04-protocol-expansion/04-PATTERNS.md (раздел 5 lines 335-398 для parser pattern)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/ss-2022-aes-128-gcm.txt
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/ss-2022-percent-encoded.txt
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/ss-legacy-chacha20.txt
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/outline-access-key.txt
  </read_first>
  <behavior>
    - ShadowsocksURIParser имеет public static let `supportedSSMethods: Set<String>` = ["2022-blake3-aes-128-gcm", "2022-blake3-aes-256-gcm", "2022-blake3-chacha20-poly1305", "aes-128-gcm", "aes-192-gcm", "aes-256-gcm", "chacha20-ietf-poly1305", "xchacha20-ietf-poly1305"]
    - ShadowsocksURIError enum: malformedURI, missingPort, malformedUserinfo, unsupportedMethod(String), missingHost
    - public static func parse(_ uri: String) throws -> ParsedShadowsocks
    - Parse pipeline: trim whitespace → URLComponents → scheme=="ss" check → host non-empty → port non-nil → user (userinfo) extraction → decodeUserinfo dual-path → method whitelist check → ParsedShadowsocks return
    - decodeUserinfo dual-path: Path 1 = `user.removingPercentEncoding`.split(":") — если method ∈ supportedSSMethods → return (method, password); Path 2 = base64url decode (с padding adjustment '=' до length%4==0, '-'→'+' '_'→'/') → utf8 decode → split(":") — если method ∈ supportedSSMethods → return; иначе throw malformedUserinfo
    - Test cases (GREEN заместо Plan 01 XCTFail): test_2022_base64_parses (fixture ss-2022-aes-128-gcm.txt → assert parsed.method == "2022-blake3-aes-128-gcm", parsed.host/port корректны), test_2022_percentEncoded_parses (fixture ss-2022-percent-encoded.txt → assert parsed.method == "2022-blake3-aes-256-gcm"), test_legacy_chacha20_parses (fixture ss-legacy-chacha20.txt → assert parsed.method == "chacha20-ietf-poly1305"), test_outlineAccessKey_parses (fixture outline-access-key.txt → должен парситься в supported, проверить host/port), test_unknownMethod_unsupported (URI с method "rc4-md5" → throws unsupportedMethod("rc4-md5")), test_malformedURI_throws (URI без @ или без host → throws malformedURI), test_emptyPassword_handled (URI с method:'' password — допустимо или throws — verify behavior)
    - UniversalImportParser.parseSingleURI case "ss": → wrap ShadowsocksURIParser.parse → .supported(name, parsed: .shadowsocks(parsed), rawURI: trimmed); на throw → .failed.invalid (но если throws unsupportedMethod → .unsupported с reason .unsupportedSSMethod вместо .failed)
    - swift test --filter ShadowsocksURIParserTests exits 0
    - Existing UniversalImportParser tests для ss:// (если присутствуют как unsupported stub) — обновить ожидания: ss:// с supported method теперь → supported (не unsupported)
  </behavior>
  <action>
    Создай `BBTB/Packages/ConfigParser/Sources/ConfigParser/ShadowsocksURIParser.swift` по образцу `TrojanURIParser.swift`:

    Файл содержит: `import Foundation`. Public enum `ShadowsocksURIError: Error, LocalizedError, Equatable` с case'ами malformedURI, missingPort, malformedUserinfo, unsupportedMethod(String), missingHost; localizedDescription для каждого. Public enum `ShadowsocksURIParser` со static private константой `supportedSSMethods: Set<String>` (точный список из 04-RESEARCH.md Example 2 — 8 методов: 3 SS-2022-blake3-* + 5 legacy AEAD).

    Public `static func parse(_ uri: String) throws -> ParsedShadowsocks`:
    1. trim whitespace.
    2. URLComponents(string: trimmed); guard scheme?.lowercased() == "ss", host non-empty, port != nil, user != nil else throw malformedURI.
    3. let (method, password) = try decodeUserinfo(user).
    4. guard supportedSSMethods.contains(method) else throw unsupportedMethod(method).
    5. return ParsedShadowsocks(host: host, port: port, method: method, password: password, remarks: comps.fragment?.removingPercentEncoding).

    Private static func `decodeUserinfo(_ user: String) throws -> (method: String, password: String)`:
    - Path 1: let decoded = user.removingPercentEncoding ?? user; if let colonIdx = decoded.firstIndex(of: ":"), method = String(decoded[..<colonIdx]), password = String(decoded[decoded.index(after: colonIdx)...]); если supportedSSMethods.contains(method) → return (method, password).
    - Path 2 (fallback): var padded = user; while padded.count % 4 != 0 { padded.append("=") }; let base64Std = padded.replacingOccurrences(of: "-", with: "+").replacingOccurrences(of: "_", with: "/"); if let data = Data(base64Encoded: base64Std), let s = String(data: data, encoding: .utf8), let colonIdx = s.firstIndex(of: ":") → return (method, password) безусловно (без method whitelist check тут, чтобы parse функция могла различить malformedUserinfo от unsupportedMethod).
    - else throw malformedUserinfo.

    Замени XCTFail placeholders в `ShadowsocksURIParserTests.swift` на полноценные тесты (см. behavior). Каждый test использует `loadFixture` helper. Для test_2022_base64_parses — fixture ss-2022-aes-128-gcm.txt; для test_2022_percentEncoded_parses — fixture ss-2022-percent-encoded.txt; для test_legacy_chacha20_parses — ss-legacy-chacha20.txt; для test_outlineAccessKey_parses — outline-access-key.txt. Для negative tests — inline URI strings (не fixtures).

    В `UniversalImportParser.swift` найди switch `parseSingleURI` (grep `case "trojan"`) и добавь `case "ss":` блок (структурно идентично case "trojan"). На ShadowsocksURIError.unsupportedMethod(let m) — возвращай `.unsupported(name: ..., scheme: "ss", host: ..., port: ..., rawURI: trimmed, reason: .unsupportedSSMethod)` через intermediate catch. На остальные ошибки — `.failed(.invalid(rawURI: trimmed, error: error.localizedDescription))`. На success — `.supported(name: parsed.remarks ?? "\(parsed.host):\(parsed.port)", parsed: .shadowsocks(parsed), rawURI: trimmed)`.

    Реализация по D-04 / D-05 / D-11 + 04-RESEARCH.md Example 2 + Pitfall 1 mitigation.
  </action>
  <verify>
    <automated>cd BBTB/Packages/ConfigParser && swift test --filter "ShadowsocksURIParserTests|UniversalImportParserTests" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/ShadowsocksURIParser.swift exists
    - File contains literal `public static func parse(_ uri: String) throws -> ParsedShadowsocks`
    - File contains literal `supportedSSMethods` AND `"2022-blake3-aes-128-gcm"` AND `"2022-blake3-aes-256-gcm"` AND `"chacha20-ietf-poly1305"`
    - File contains literal `decodeUserinfo` AND literal `removingPercentEncoding` AND literal `Data(base64Encoded:`
    - File contains literal `case unsupportedMethod(String)`
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift contains literal `case "ss":` AND literal `ShadowsocksURIParser.parse`
    - File contains literal `.shadowsocks(` (case construction)
    - File BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ShadowsocksURIParserTests.swift does NOT contain literal `XCTFail("Pending Plan 03`
    - File contains literal `test_2022_base64`, `test_2022_percentEncoded`, `test_legacy_chacha20`, `test_outlineAccessKey`, `test_unknownMethod_unsupported`, `test_malformedURI_throws`
    - File contains literal `XCTAssertEqual` или `XCTAssertNotNil` (actual assertions)
    - `swift test --filter ShadowsocksURIParserTests` exits 0
    - `swift test --filter UniversalImportParserTests` exits 0 (regression — ss:// import flow обновлён, остальные форматы остаются working)
  </acceptance_criteria>
  <done>ShadowsocksURIParser реализован с dual-decoder; supported methods whitelist enforced; SIP002 + SIP022 + Outline формы парсятся; UniversalImportParser routes ss:// в supported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать Protocols/Shadowsocks SPM package (handler + ConfigBuilder + template без TLS + tests)</name>
  <files>BBTB/Packages/Protocols/Shadowsocks/Package.swift, BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/ShadowsocksHandler.swift, BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/ConfigBuilder.swift, BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/Resources/SingBoxConfigTemplate.shadowsocks.json, BBTB/Packages/Protocols/Shadowsocks/Tests/ShadowsocksTests/ConfigBuilderTests.swift</files>
  <read_first>
    - BBTB/Packages/Protocols/Trojan/Package.swift (полностью — образец)
    - BBTB/Packages/Protocols/Trojan/Sources/Trojan/TrojanHandler.swift
    - BBTB/Packages/Protocols/Trojan/Sources/Trojan/ConfigBuilder.swift
    - BBTB/Packages/Protocols/Trojan/Sources/Trojan/Resources/SingBoxConfigTemplate.trojan-tcp.json (для общей структуры — log/dns/route — копируется; outbound type+fields меняются)
    - BBTB/Packages/Protocols/Trojan/Tests/TrojanTests/ConfigBuilderTests.swift (паттерн tests)
    - BBTB/Packages/Protocols/VLESSTLS/Package.swift (если Plan 02 ушёл первым — близкий аналог)
    - .planning/phases/04-protocol-expansion/04-PATTERNS.md (раздел 4 lines 269-333 — template per-protocol modifications; раздел 2/3 для handler/builder shape; раздел 15 для tests)
    - .planning/phases/04-protocol-expansion/04-RESEARCH.md (sing-box shadowsocks outbound fields, Standard Stack)
    - .planning/phases/04-protocol-expansion/04-CONTEXT.md (D-06 — sing-box outbound структура; D-04 — нет TLS блока)
  </read_first>
  <behavior>
    - Package.swift: name "Shadowsocks", platforms iOS .v18 / macOS .v15, library product "Shadowsocks", dependencies path-based на ../../VPNCore + ../../PacketTunnelKit, resources [.process("Resources/SingBoxConfigTemplate.shadowsocks.json")], testTarget linkerSettings как в Trojan
    - ShadowsocksHandler: VPNProtocolHandler, identifier="shadowsocks", displayName="Shadowsocks", остальные методы stub как Trojan
    - ConfigBuilder.ShadowsocksInputs: host (String), port (Int), method (String), password (String), remark (String?)
    - BuilderError enum: templateLoadFailed(String), invalidPort(Int), missingPassword, missingMethod
    - buildSingBoxJSON: validate port range + non-empty method + non-empty password; loadTemplate "SingBoxConfigTemplate.shadowsocks" из Bundle.module; replacingOccurrences для ${SERVER_HOST}, ${SS_METHOD}, ${SS_PASSWORD}, ${DNS_DETOUR} ← "shadowsocks-out"; if port != 8388 (или другой default — verify в template) → mutatePort
    - SingBoxConfigTemplate.shadowsocks.json: top-level log/dns/outbounds/route/experimental структура как trojan-tcp.json; outbounds[0] = type "shadowsocks", tag "shadowsocks-out", server "${SERVER_HOST}", server_port 8388 (или default — выбрать обоснованный default), method "${SS_METHOD}", password "${SS_PASSWORD}", network "tcp". БЕЗ tls блока. route.final "shadowsocks-out". DNS секция с ${DNS_DETOUR}.
    - Sing-box JSON НЕ содержит "tls" в outbound[0] (это намеренно — SS encrypted на уровне протокола, не TLS)
    - ConfigBuilderTests: test_buildsConfigWithoutPlaceholders (no ${X} substrings), test_validateRoundTrip (SingBoxConfigLoader.validate passes), test_methodSubstituted ("aes-256-gcm" из inputs → присутствует в JSON), test_passwordSubstituted, test_customPort (non-8388 mutated), test_invalidPort_throws
    - swift build для Shadowsocks package проходит
    - swift test --filter ShadowsocksTests exits 0
  </behavior>
  <action>
    Создай SPM package `BBTB/Packages/Protocols/Shadowsocks/` по образцу `BBTB/Packages/Protocols/Trojan/`.

    - `Package.swift` — копировать Trojan/Package.swift, name "Trojan" → "Shadowsocks", paths Sources/Shadowsocks + Tests/ShadowsocksTests, resources list single entry `.process("Resources/SingBoxConfigTemplate.shadowsocks.json")`. Зависимости и linkerSettings — identical.
    - `Sources/Shadowsocks/ShadowsocksHandler.swift` — копировать TrojanHandler.swift, переименовать struct → ShadowsocksHandler, identifier "trojan" → "shadowsocks", displayName "Trojan" → "Shadowsocks". HandlerError структура та же.
    - `Sources/Shadowsocks/ConfigBuilder.swift` — копировать Trojan/ConfigBuilder.swift. Переименовать TrojanInputs → ShadowsocksInputs с полями (host: String, port: Int, method: String, password: String, remark: String?). BuilderError (templateLoadFailed, invalidPort, missingPassword, missingMethod). buildSingBoxJSON: validate (port range 1-65535 + non-empty method + non-empty password) → loadTemplate "SingBoxConfigTemplate.shadowsocks" → replacingOccurrences для ${SERVER_HOST} (host), ${SS_METHOD} (method), ${SS_PASSWORD} (password), ${DNS_DETOUR} ("shadowsocks-out") → mutatePort если необходимо. mutatePort + loadTemplate — копировать из TrojanConfigBuilder.
    - `Sources/Shadowsocks/Resources/SingBoxConfigTemplate.shadowsocks.json` — копировать `BBTB/Packages/Protocols/Trojan/Sources/Trojan/Resources/SingBoxConfigTemplate.trojan-tcp.json` как базу; заменить outbounds[0] на: { "type": "shadowsocks", "tag": "shadowsocks-out", "server": "${SERVER_HOST}", "server_port": 8388, "method": "${SS_METHOD}", "password": "${SS_PASSWORD}", "network": "tcp" }. УБРАТЬ tls block. route.final → "shadowsocks-out". DNS секция с ${DNS_DETOUR}.
    - `Tests/ShadowsocksTests/ConfigBuilderTests.swift` — копировать Trojan tests, factory `defaultInputs` = ShadowsocksInputs(host: "example.com", port: 8388, method: "2022-blake3-aes-256-gcm", password: "32bytespasswordstringforss2022test", remark: nil). Тесты: test_buildsConfigWithoutPlaceholders (no ${SS_METHOD}, ${SS_PASSWORD}, ${SERVER_HOST}, ${DNS_DETOUR} в результате; assert "2022-blake3-aes-256-gcm" в JSON; assert "shadowsocks-out" в JSON; XCTAssertNoThrow(try SingBoxConfigLoader.validate(json: json)) — R1 self-test), test_legacyMethod (method="aes-256-gcm" → builds), test_customPort_mutated, test_invalidPort_throws, test_emptyMethod_throws.

    Хорошо проверить — в полученном JSON НЕТ `"tls"` ключа в outbound[0]. Это критично — SS работает БЕЗ TLS.
  </action>
  <verify>
    <automated>cd BBTB/Packages/Protocols/Shadowsocks && swift build 2>&1 | tail -10 && swift test --filter ShadowsocksTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File BBTB/Packages/Protocols/Shadowsocks/Package.swift exists, contains literal `name: "Shadowsocks"` AND `library(name: "Shadowsocks"`
    - File contains literal `.process("Resources/SingBoxConfigTemplate.shadowsocks.json")`
    - File BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/ShadowsocksHandler.swift contains literal `identifier = "shadowsocks"` AND `displayName = "Shadowsocks"`
    - File BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/ConfigBuilder.swift contains literal `ShadowsocksInputs` AND `${SS_METHOD}` AND `${SS_PASSWORD}` AND `shadowsocks-out`
    - File BBTB/Packages/Protocols/Shadowsocks/Sources/Shadowsocks/Resources/SingBoxConfigTemplate.shadowsocks.json exists, is valid JSON
    - JSON contains literal `"type": "shadowsocks"` AND `${SS_METHOD}` AND `${SS_PASSWORD}` AND `"network": "tcp"`
    - JSON outbounds[0] does NOT contain literal `"tls"` key (e.g., `grep -A 20 '"type": "shadowsocks"' SingBoxConfigTemplate.shadowsocks.json | grep -c '"tls"'` returns 0; comments/headers excluded)
    - `swift build` in BBTB/Packages/Protocols/Shadowsocks exits 0
    - `swift test --filter ShadowsocksTests` in BBTB/Packages/Protocols/Shadowsocks exits 0
    - ConfigBuilderTests.swift contains literal `SingBoxConfigLoader.validate` (R1 self-test for the new outbound)
  </acceptance_criteria>
  <done>SPM package Protocols/Shadowsocks существует, компилируется, тесты PASS; sing-box JSON без TLS блока, R1-compliant (нет insecure flag вовсе).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PoolBuilder — buildShadowsocksOutbound + case .shadowsocks в switch + integration tests</name>
  <files>BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift</files>
  <read_first>
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift (после Plan 02 — switch parsed уже содержит .vlessTLS; добавь .shadowsocks)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift (после Plan 02 — содержит test_vlessTLS_*)
    - BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBoxConfigLoader.swift (validate function, proxyOutboundTypes contains "shadowsocks" — verified, не менять)
    - .planning/phases/04-protocol-expansion/04-PATTERNS.md (раздел 7 lines 519-532 — buildShadowsocksOutbound minimal pattern)
    - .planning/phases/04-protocol-expansion/04-RESEARCH.md (Pattern 2 lines 354-364 — точный outbound структура)
  </read_first>
  <behavior>
    - PoolBuilder.swift switch parsed дополнен case `.shadowsocks(let s):` → tag = "ss-\(index)", outbound = buildShadowsocksOutbound(parsed: s, tag: tag)
    - Новая private static func buildShadowsocksOutbound(parsed: ParsedShadowsocks, tag: String) -> [String: Any] возвращает: type="shadowsocks", tag, server=parsed.host, server_port=parsed.port, method=parsed.method, password=parsed.password, network="tcp"
    - НЕТ "tls" ключа в outbound dictionary (R1 invariant trivial — SS не TLS)
    - PoolBuilderTests: test_shadowsocks_buildsValidOutbound (pool=[.shadowsocks(makeShadowsocks())] → assert outbound type=="shadowsocks", tag starts "ss-", method/password присутствуют, "tls" key absent, SingBoxConfigLoader.validate passes), test_shadowsocks_inMultiOutboundPool (pool с .shadowsocks + .trojan + .vlessTLS → urltest selector содержит все три tag'а)
    - swift test --filter PoolBuilderTests exits 0
  </behavior>
  <action>
    Расширь `PoolBuilder.swift`:

    1. В `switch parsed` добавь после .vlessTLS case'а: `case .shadowsocks(let s): tag = "ss-\(index)"; outbound = buildShadowsocksOutbound(parsed: s, tag: tag)`.
    2. Добавь private static func `buildShadowsocksOutbound(parsed: ParsedShadowsocks, tag: String) -> [String: Any]` (точная implementation — 04-PATTERNS.md раздел 7 lines 521-532): возвращает `["type": "shadowsocks", "tag": tag, "server": parsed.host, "server_port": parsed.port, "method": parsed.method, "password": parsed.password, "network": "tcp"]`.

    Расширь `PoolBuilderTests.swift`:
    - helper `makeShadowsocks(...)` factory (host="example.com", port=8388, method="2022-blake3-aes-256-gcm", password="testpasswordhere32bytesbase64encoded=", remarks=nil).
    - test_shadowsocks_buildsValidOutbound: pool с одним .shadowsocks; let json = try PoolBuilder.buildSingBoxJSON(from: pool); decode JSON; assert первый non-direct outbound type=="shadowsocks", tag starts with "ss-", method/password совпадают, в outbound dictionary НЕТ key "tls" (XCTAssertNil((outbound["tls"] as Any?))); XCTAssertNoThrow(try SingBoxConfigLoader.validate(json: json)).
    - test_shadowsocks_inMultiOutboundPool: pool с .shadowsocks + .trojan; assert urltest selector outbound list содержит и "ss-0" и "trojan-1" (или whatever ordering produces).

    Не изменять SingBoxConfigLoader (proxyOutboundTypes уже содержит "shadowsocks" — verified в Phase 2 W0.T4).
  </action>
  <verify>
    <automated>cd BBTB/Packages/ConfigParser && swift test --filter "PoolBuilderTests|PoolBuilderSingleOutboundTests" 2>&1 | tail -30 && swift test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift contains literal `case .shadowsocks(let`
    - File contains literal `buildShadowsocksOutbound`
    - File contains literal `"type": "shadowsocks"` AND `"network": "tcp"` AND `parsed.method` AND `parsed.password`
    - buildShadowsocksOutbound function body does NOT contain literal `"tls"` (grep -A 15 'func buildShadowsocksOutbound' PoolBuilder.swift | grep -v '^#' | grep -c '"tls"' returns 0)
    - File BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift contains literal `test_shadowsocks_buildsValidOutbound` or similar test name
    - File contains literal `XCTAssertNoThrow(try SingBoxConfigLoader.validate` (R1 self-test)
    - `swift test --filter PoolBuilderTests` exits 0
    - `swift test` (full ConfigParser suite) exits 0 (regression — Plan 02 VLESS-TLS tests still pass, all Plan 01/02/03 tests green)
  </acceptance_criteria>
  <done>PoolBuilder.buildShadowsocksOutbound реализован, switch расширен, integration tests PASS, ConfigParser suite зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User ss:// URI (paste/QR/subscription) → ShadowsocksURIParser | Untrusted input; dual-decoder может вести себя по-разному для legacy vs SS-2022 |
| Outline access key (внешний QR) → ShadowsocksURIParser | Same — Outline = SIP002 ss:// |
| ParsedShadowsocks.method whitelist → PoolBuilder | Method controlled by parser; sing-box validates again at handshake |
| Bundle.module template → ConfigBuilder | Static, build-time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | Malicious ss:// с deprecated stream cipher (rc4-md5, aes-256-cfb) | mitigate | ShadowsocksURIParser.supportedSSMethods НЕ включает stream ciphers (выкошены в 2017 как небезопасные); unsupportedMethod throws → UniversalImportParser routes в .unsupported (не connect) |
| T-04-03-02 | Cryptographic | SS-2022 password короче required key length (16/32 bytes) | accept | sing-box engine validates key length при handshake → fail connection (fail-safe); parser НЕ validate'ит длину (полагаемся на engine как documented в 04-RESEARCH.md Security Domain) |
| T-04-03-03 | Tampering | base64url userinfo с inject'ом control chars | mitigate | Data(base64Encoded:) Foundation API проверяет base64 alphabet; non-UTF8 results → return nil → throw malformedUserinfo; no shell exec path |
| T-04-03-04 | Information disclosure | SS password в outboundJSON | accept | SS password — это credentials (как Trojan password в Phase 2); хранится через Keychain в Plan 06; sing-box JSON генерируется только для активного туннеля, не сохраняется в plaintext (T-02-04 invariant) |
| T-04-03-05 | DoS | Recursion в base64url decode при extreme padding | accept | Foundation Data(base64Encoded:) — встроенный лимит длины строки, no recursion |
| T-04-03-06 | Spoofing | ss:// URI с неверным fingerprint типичного Outline ключа | accept | Outline UX — пользователь копирует свой ключ; нет centralized service для подмены |

R1 invariant: SS не имеет TLS блока (encrypted на уровне протокола). Plan 03 не вводит `insecure` toggle ни для SS, ни для других протоколов (только Plan 04 для Hy2 — D-08).
</threat_model>

<verification>
- `swift test` в BBTB/Packages/ConfigParser passes (включая SS-2022 tests + Outline + Plan 02 VLESS-TLS, plan 01 baseline).
- `swift test` в BBTB/Packages/Protocols/Shadowsocks passes.
- `swift build` обоих пакетов проходит.
- ConfigParser test fixture outline-access-key.txt парсится как valid ParsedShadowsocks (verified в test_outlineAccessKey_parses).
- Unsupported methods (e.g., rc4-md5) бросают unsupportedMethod, не передаются в pool builder.
</verification>

<success_criteria>
- ss:// SIP002 URI (legacy base64url) парсится → ParsedShadowsocks → sing-box outbound type=shadowsocks без TLS block.
- ss:// SIP022 URI (percent-encoded) парсится — same flow.
- Outline access key (= SIP002 ss://) парсится — covered by ShadowsocksURIParser without dedicated branch.
- 8 method whitelist enforced; unknown method → unsupported.
- PoolBuilder integration: pool с .shadowsocks → sing-box JSON validates.
- ShadowsocksURIParserTests все PASS (RED→GREEN transition).
</success_criteria>

<output>
After completion, create `.planning/phases/04-protocol-expansion/04-03-SUMMARY.md`
</output>
