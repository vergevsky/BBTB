---
phase: 04-protocol-expansion
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/Hysteria2URIParser.swift
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Hysteria2URIParserTests.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift
  - BBTB/Packages/Protocols/Hysteria2/Package.swift
  - BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/Hysteria2Handler.swift
  - BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/ConfigBuilder.swift
  - BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/Resources/SingBoxConfigTemplate.hysteria2.json
  - BBTB/Packages/Protocols/Hysteria2/Tests/Hysteria2Tests/ConfigBuilderTests.swift
autonomous: true
requirements:
  - PROTO-05
requirements_addressed:
  - PROTO-05
must_haves:
  truths:
    - "Hysteria2URIParser парсит hy2:// И hysteria2:// схемы (D-09 dual scheme)"
    - "insecure=1 / allowInsecure=1 / skip-cert-verify=1 в URI → ParsedHysteria2.allowInsecure=true (D-08 — ТРИ синонима)"
    - "Hysteria2 — ЕДИНСТВЕННЫЙ протокол где tls.insecure: true в sing-box JSON legitimate (D-08)"
    - "Multi-port URI (port=123,5000-6000) → throws multiPortNotSupported (D-09 Phase 4 reject; Pitfall 6)"
    - "obfs=salamander handled correctly; obfs ≠ salamander → throws unsupportedObfs"
    - "Protocols/Hysteria2 package компилируется: handler identifier=\"hysteria2\""
    - "Sing-box JSON Hy2 с allowInsecure=true → tls.insecure: true; Hy2 с allowInsecure=false → tls.insecure: false"
    - "PoolBuilder buildHysteria2Outbound — единственный builder с пропускной tls.insecure (помечен комментарием // R1 EXCEPTION — only Hysteria2 (D-08))"
    - "Invariant test: non-hy2 outbounds в multi-protocol pool НИКОГДА не имеют tls.insecure=true (Pitfall 2 mitigation)"
  artifacts:
    - path: "BBTB/Packages/ConfigParser/Sources/ConfigParser/Hysteria2URIParser.swift"
      provides: "Hysteria2URIParser.parse + ParsedHysteria2; D-08 collapse трёх синонимов в allowInsecure; D-09 dual-scheme + multi-port reject"
      contains: "multiPortNotSupported"
    - path: "BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/Resources/SingBoxConfigTemplate.hysteria2.json"
      provides: "sing-box outbound type=hysteria2 с ${ALLOW_INSECURE} placeholder (только Hy2 имеет этот placeholder)"
      contains: "${ALLOW_INSECURE}"
    - path: "BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift"
      provides: "R1 invariant test: test_nonHy2_outbounds_neverHaveInsecureTrue"
      contains: "test_nonHy2_outbounds_neverHaveInsecureTrue"
  key_links:
    - from: "Hysteria2URIParser.parse"
      to: "ParsedHysteria2.allowInsecure"
      via: "synonyms collapse insecure/allowInsecure/skip-cert-verify"
      pattern: "allowInsecure ="
    - from: "PoolBuilder.buildHysteria2Outbound"
      to: "tls.insecure: parsed.allowInsecure"
      via: "ТОЛЬКО Hy2 builder — отмечено комментарием"
      pattern: "R1 EXCEPTION"
    - from: "PoolBuilderTests.test_nonHy2_outbounds_neverHaveInsecureTrue"
      to: "multi-protocol pool"
      via: "iterate outbounds, assert non-hy2 tag → tls.insecure==false"
      pattern: "test_nonHy2_outbounds_neverHaveInsecureTrue"
---

## Phase Goal

**As a** user, **I want to** импортировать hy2:// или hysteria2:// URI (включая self-hosted с self-signed cert) и подключиться по QUIC/UDP, **so that** мой Hysteria2 сервер работает в BBTB даже с insecure=1 (что норма для self-hosted setups).

<objective>
Vertical slice для PROTO-05 (Hysteria2 — единственное R1 exception):
1. Создать `Hysteria2URIParser.swift` с D-09 dual scheme (hy2/hysteria2), multi-port reject (Pitfall 6), D-08 collapse трёх синонимов insecure/allowInsecure/skip-cert-verify в Bool, obfs=salamander whitelist.
2. Создать SPM package `BBTB/Packages/Protocols/Hysteria2/` с template содержащим `${ALLOW_INSECURE}` placeholder (единственный template с таким placeholder).
3. Расширить `PoolBuilder` с `buildHysteria2Outbound` (R1 EXCEPTION — единственный outbound builder с `tls.insecure: parsed.allowInsecure`, помечен явным комментарием).
4. Добавить R1 invariant test `test_nonHy2_outbounds_neverHaveInsecureTrue` (Pitfall 2 mitigation) — итерирует outbounds в multi-protocol pool, assert'ит что только tag начинающийся "hy2-" может иметь tls.insecure=true.
5. Обновить `UniversalImportParser.parseSingleURI` case "hy2"/"hysteria2".
6. Заменить XCTFail-placeholders в `Hysteria2URIParserTests` на GREEN tests.

Purpose: PROTO-05 — закрыть R1 одним намеренным исключением (D-08), реализованным с максимальной видимостью (комментарии + invariant test + изолирована в одном builder).

Output: импорт `hy2://auth@host:443?sni=...&insecure=1&obfs=salamander&obfs-password=...` → ParsedHysteria2 → sing-box outbound type=hysteria2 с tls.insecure=true legitimately; non-hy2 outbounds в том же pool — все имеют tls.insecure=false.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-protocol-expansion/04-CONTEXT.md
@.planning/phases/04-protocol-expansion/04-RESEARCH.md
@.planning/phases/04-protocol-expansion/04-PATTERNS.md
@.planning/phases/04-01-SUMMARY.md
@.planning/phases/04-02-SUMMARY.md
@.planning/phases/04-03-SUMMARY.md
@BBTB/Packages/ConfigParser/Sources/ConfigParser/TrojanURIParser.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift
@BBTB/Packages/Protocols/Trojan/Package.swift
@BBTB/Packages/Protocols/Trojan/Sources/Trojan/TrojanHandler.swift
@BBTB/Packages/Protocols/Trojan/Sources/Trojan/ConfigBuilder.swift
@BBTB/Packages/Protocols/Trojan/Sources/Trojan/Resources/SingBoxConfigTemplate.trojan-tcp.json
@BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBoxConfigLoader.swift
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Hysteria2URIParserTests.swift
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures

<interfaces>
Existing (Plan 01):
- public struct ParsedHysteria2 — host, port, auth, sni, fingerprint (String?), obfs (String?), obfsPassword (String?), allowInsecure (Bool), pinSHA256 (String?), remarks (String?)
- public enum UnsupportedReason — includes multiPortNotSupported

Existing (Plan 02/03):
- AnyParsedConfig: .hysteria2(ParsedHysteria2) case
- PoolBuilder switch already has .vlessReality, .vlessTLS, .trojan, .shadowsocks cases
- StubParsers.defaultPortForScheme: "hy2","hysteria2" → 443

Hysteria2 URI scheme spec (04-RESEARCH.md sources):
- hy2://auth@host:port?sni=...&obfs=salamander&obfs-password=...&insecure=1&pinSHA256=...&fp=...#name
- Scheme aliases: hy2 OR hysteria2 (D-09 — support both)
- Multi-port port forms: "443,8443" or "443-8443" — D-09 reject
- obfs: only "salamander" supported by sing-box (other values → throw)

sing-box outbound hysteria2:
- type: "hysteria2", tag, server, server_port, password
- tls { enabled, server_name, insecure (D-08 exception), utls, certificate_public_key_sha256 }
- optional obfs { type: "salamander", password }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hysteria2URIParser.swift с D-08+D-09 + полные tests</name>
  <files>BBTB/Packages/ConfigParser/Sources/ConfigParser/Hysteria2URIParser.swift, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Hysteria2URIParserTests.swift, BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift</files>
  <read_first>
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/TrojanURIParser.swift (паттерн, lines 1-127)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/ShadowsocksURIParser.swift (Plan 03 output — близкий аналог)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift (Plan 01 — ParsedHysteria2 struct fields)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift (Plan 03 added case "ss"; добавь case "hy2","hysteria2" рядом)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Hysteria2URIParserTests.swift (Plan 01 RED placeholders)
    - .planning/phases/04-protocol-expansion/04-RESEARCH.md (Pattern 1 lines 266-339 — точный код Hysteria2URIParser с multi-port regex; Pitfall 6 lines 562-567)
    - .planning/phases/04-protocol-expansion/04-CONTEXT.md (D-07 — поля; D-08 — три синонима; D-09 — dual scheme + multi-port reject)
    - .planning/phases/04-protocol-expansion/04-PATTERNS.md (раздел 5 lines 391-398; раздел E lines 1053-1071 — default port)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/hy2-with-obfs.txt
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/hy2-insecure.txt
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/hy2-multi-port.txt
  </read_first>
  <behavior>
    - Hysteria2URIError enum: malformedURI, missingAuth, multiPortNotSupported(String), unsupportedObfs(String)
    - public static func parse(_ uri: String) throws -> ParsedHysteria2
    - Scheme check: comps.scheme?.lowercased() ∈ {"hy2", "hysteria2"} (D-09 dual scheme; иначе throw malformedURI)
    - Multi-port check: ДО URLComponents parse, скан port-части строки (substring after "@" и before "/" или "?") — если содержит "," или "-" → throw multiPortNotSupported(portStr) (Pitfall 6 — URLComponents.port == nil silently swallow'ит multi-port)
    - port = comps.port ?? 443 (default port 443 — RESEARCH §pattern-E)
    - auth = user.removingPercentEncoding ?? user; non-empty
    - allowInsecure collapse: ["1","true","yes"].contains((q["insecure"] ?? q["allowInsecure"] ?? q["skip-cert-verify"] ?? "0").lowercased()) — D-08
    - obfs check: if q["obfs"] non-empty && != "salamander" → throw unsupportedObfs(value)
    - Поля результата: host, port, auth, sni=q["sni"] ?? host, fingerprint=q["fingerprint"] ?? q["fp"] (может быть nil), obfs=q["obfs"] (nil если empty), obfsPassword=q["obfs-password"], allowInsecure, pinSHA256=q["pinSHA256"], remarks=comps.fragment?.removingPercentEncoding
    - Test cases GREEN: test_bothSchemes_parse (hy2://... и hysteria2://... оба парсятся), test_insecureFlag_setsAllowInsecure (три параметра insecure=1, allowInsecure=1, skip-cert-verify=1 — все три варианта → allowInsecure==true; ничего → false), test_multiPort_rejects (fixture hy2-multi-port.txt → throws multiPortNotSupported), test_obfsSalamander_parses (fixture hy2-with-obfs.txt → assert obfs=="salamander" + obfsPassword non-empty), test_obfsNotSalamander_throws (URI с obfs=plain → throws unsupportedObfs), test_defaultPort (URI без явного port → port==443), test_malformedURI_throws (URI без @ → throws malformedURI)
    - UniversalImportParser.parseSingleURI case "hy2", "hysteria2" → ShadowsocksURIParser-style wrap, supported / unsupported (multiPortNotSupported) / failed routing
    - swift test --filter Hysteria2URIParserTests exits 0
  </behavior>
  <action>
    Создай `BBTB/Packages/ConfigParser/Sources/ConfigParser/Hysteria2URIParser.swift` по образцу `TrojanURIParser.swift` + точный код из 04-RESEARCH.md Pattern 1 (lines 266-339).

    Файл: `import Foundation`. Public enum `Hysteria2URIError: Error, LocalizedError, Equatable` с case'ами malformedURI, missingAuth, multiPortNotSupported(String), unsupportedObfs(String); localizedDescription для каждого.

    Public enum `Hysteria2URIParser`:
    ```
    public static func parse(_ uri: String) throws -> ParsedHysteria2 {
      let trimmed = uri.trimmingCharacters(in: .whitespacesAndNewlines)
      guard let comps = URLComponents(string: trimmed),
            let scheme = comps.scheme?.lowercased(),
            scheme == "hy2" || scheme == "hysteria2",
            let host = comps.host, !host.isEmpty,
            let user = comps.user
      else { throw Hysteria2URIError.malformedURI }
      // D-09 multi-port reject (Pitfall 6) — выполняется ДО comps.port чтения, через substring анализ
      let afterAt = String(trimmed.split(separator: "@", maxSplits: 1).last ?? "")
      let portCandidate = afterAt.split(maxSplits: 1, whereSeparator: { $0 == "/" || $0 == "?" || $0 == "#" }).first.map(String.init) ?? ""
      let portPart = portCandidate.split(separator: ":", maxSplits: 1).last.map(String.init) ?? ""
      if portPart.contains(",") || portPart.contains("-") {
        throw Hysteria2URIError.multiPortNotSupported(portPart)
      }
      let port = comps.port ?? 443
      let auth = user.removingPercentEncoding ?? user
      guard !auth.isEmpty else { throw Hysteria2URIError.missingAuth }
      var q: [String: String] = [:]
      for item in comps.queryItems ?? [] { if let v = item.value { q[item.name] = v } }
      // D-08 — three synonyms collapse
      let allowInsecure = ["1","true","yes"].contains((q["insecure"] ?? q["allowInsecure"] ?? q["skip-cert-verify"] ?? "0").lowercased())
      // obfs whitelist
      if let obfs = q["obfs"], !obfs.isEmpty, obfs != "salamander" {
        throw Hysteria2URIError.unsupportedObfs(obfs)
      }
      return ParsedHysteria2(
        host: host, port: port, auth: auth,
        sni: q["sni"] ?? host,
        fingerprint: q["fingerprint"] ?? q["fp"],
        obfs: (q["obfs"]?.isEmpty == false) ? q["obfs"] : nil,
        obfsPassword: q["obfs-password"],
        allowInsecure: allowInsecure,
        pinSHA256: q["pinSHA256"],
        remarks: comps.fragment?.removingPercentEncoding
      )
    }
    ```

    Замени XCTFail placeholders в `Hysteria2URIParserTests.swift` на GREEN tests:
    - test_bothSchemes_parse: вызвать parse дважды — один раз с hy2://, один раз с hysteria2:// той же auth/host/port; assert results semantically equal (auth, host, port совпадают).
    - test_insecureFlag_setsAllowInsecure: проверить три URI с insecure=1, allowInsecure=1, skip-cert-verify=1 — все три → allowInsecure==true; URI без этих flags → allowInsecure==false.
    - test_multiPort_rejects: loadFixture("hy2-multi-port") → XCTAssertThrowsError { try Hysteria2URIParser.parse(uri) } { err in XCTAssertEqual(err as? Hysteria2URIError, .multiPortNotSupported(...)) } (или partial match).
    - test_obfsSalamander_parses: loadFixture("hy2-with-obfs") → assert parsed.obfs == "salamander", parsed.obfsPassword non-nil non-empty.
    - test_obfsNotSalamander_throws: inline URI `hy2://auth@host:443?obfs=plain&sni=host` → throws unsupportedObfs("plain").
    - test_defaultPort: URI без `:port` → parsed.port == 443.
    - test_insecureFromFixture: loadFixture("hy2-insecure") → assert parsed.allowInsecure == true.

    В `UniversalImportParser.swift` добавь case "hy2", "hysteria2": к switch parseSingleURI (по образцу Plan 03 case "ss"). На Hysteria2URIError.multiPortNotSupported(let p) → routing в `.unsupported` с reason `.multiPortNotSupported` (новый UnsupportedReason из Plan 01); на остальные errors → `.failed.invalid`; на success → `.supported(name: parsed.remarks ?? "\(parsed.host):\(parsed.port)", parsed: .hysteria2(parsed), rawURI: trimmed)`.

    Реализация по D-07, D-08, D-09 + 04-RESEARCH.md Pattern 1 + Pitfall 6 mitigation.
  </action>
  <verify>
    <automated>cd BBTB/Packages/ConfigParser && swift test --filter "Hysteria2URIParserTests|UniversalImportParserTests" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/Hysteria2URIParser.swift exists
    - File contains literal `scheme == "hy2" || scheme == "hysteria2"` (D-09 dual scheme)
    - File contains literal `multiPortNotSupported` AND literal `portPart.contains(",")` AND literal `portPart.contains("-")`
    - File contains literal `insecure` AND `allowInsecure` AND `skip-cert-verify` (D-08 three synonyms)
    - File contains literal `obfs != "salamander"` (whitelist check)
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift contains literal `case "hy2", "hysteria2":` или `case "hy2":` together with `case "hysteria2":`
    - File contains literal `Hysteria2URIParser.parse`
    - File contains literal `.hysteria2(` (case construction)
    - File BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Hysteria2URIParserTests.swift does NOT contain `XCTFail("Pending Plan 04`
    - File contains literal `test_bothSchemes`, `test_insecureFlag`, `test_multiPort_rejects`, `test_obfsSalamander`
    - File contains literal `XCTAssertEqual` или `XCTAssertTrue`
    - `swift test --filter Hysteria2URIParserTests` exits 0
    - `swift test --filter UniversalImportParserTests` exits 0 (regression — vless/trojan/ss routes остаются working)
  </acceptance_criteria>
  <done>Hysteria2URIParser реализован с D-08 (три синонима), D-09 (dual scheme + multi-port reject), obfs whitelist. UniversalImportParser routes hy2/hysteria2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать Protocols/Hysteria2 SPM package — handler + ConfigBuilder + template с ${ALLOW_INSECURE} placeholder + tests</name>
  <files>BBTB/Packages/Protocols/Hysteria2/Package.swift, BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/Hysteria2Handler.swift, BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/ConfigBuilder.swift, BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/Resources/SingBoxConfigTemplate.hysteria2.json, BBTB/Packages/Protocols/Hysteria2/Tests/Hysteria2Tests/ConfigBuilderTests.swift</files>
  <read_first>
    - BBTB/Packages/Protocols/Trojan/Package.swift (образец)
    - BBTB/Packages/Protocols/Trojan/Sources/Trojan/TrojanHandler.swift
    - BBTB/Packages/Protocols/Trojan/Sources/Trojan/ConfigBuilder.swift
    - BBTB/Packages/Protocols/Trojan/Sources/Trojan/Resources/SingBoxConfigTemplate.trojan-tcp.json (общая структура log/dns/route)
    - BBTB/Packages/Protocols/VLESSTLS/Package.swift (Plan 02 output — недавний близкий аналог)
    - BBTB/Packages/Protocols/Shadowsocks/Package.swift (Plan 03 output)
    - BBTB/Packages/Protocols/Trojan/Tests/TrojanTests/ConfigBuilderTests.swift
    - .planning/phases/04-protocol-expansion/04-PATTERNS.md (раздел 4 lines 269-333 — Hysteria2 template specifics; раздел 15 lines 853-915 — Hy2 R1 EXCEPTION tests)
    - .planning/phases/04-protocol-expansion/04-RESEARCH.md (Pattern 3 lines 367-397 — Hy2 outbound builder; Code Examples)
    - .planning/phases/04-protocol-expansion/04-CONTEXT.md (D-08, D-10)
  </read_first>
  <behavior>
    - Package.swift: name "Hysteria2", platforms iOS .v18 / macOS .v15, library "Hysteria2", dependencies на ../../VPNCore + ../../PacketTunnelKit, resources [.process("Resources/SingBoxConfigTemplate.hysteria2.json")], testTarget с linkerSettings как Trojan
    - Hysteria2Handler: VPNProtocolHandler, identifier="hysteria2", displayName="Hysteria2"
    - ConfigBuilder.Hysteria2Inputs: host (String), port (Int), password (String), sni (String), fingerprint (String?), obfs (String?), obfsPassword (String?), allowInsecure (Bool), pinSHA256 (String?), remark (String?)
    - BuilderError enum: templateLoadFailed(String), invalidPort(Int), missingPassword, missingSNI
    - buildSingBoxJSON: validate port + non-empty password/sni → loadTemplate "SingBoxConfigTemplate.hysteria2" → replacingOccurrences: ${SERVER_HOST} ← host, ${HY2_PASSWORD} ← password, ${SNI_DOMAIN} ← sni, ${ALLOW_INSECURE} ← (allowInsecure ? "true" : "false"), ${DNS_DETOUR} ← "hysteria2-out"
    - Если obfs != nil && obfsPassword != nil — после template substitution мутировать JSON добавлением obfs ключа в outbound[0]: {"type":"salamander","password":"<obfsPassword>"} (через JSONSerialization round-trip + dictionary mutation)
    - Если pinSHA256 != nil — мутировать tls.certificate_public_key_sha256 в outbound[0].tls (array of strings — [pinSHA256])
    - Если fingerprint != nil — мутировать outbound[0].tls.utls.fingerprint (вместо default chrome)
    - mutatePort работает как в других builder'ах
    - SingBoxConfigTemplate.hysteria2.json: outbounds[0] = type "hysteria2", tag "hysteria2-out", server "${SERVER_HOST}", server_port 443, password "${HY2_PASSWORD}", tls { enabled: true, server_name "${SNI_DOMAIN}", insecure: ${ALLOW_INSECURE} (literally — без кавычек вокруг placeholder!), alpn: ["h3"] (Hysteria2 = QUIC, H3 ALPN), utls { enabled: true, fingerprint: "chrome" } }. route.final "hysteria2-out". DNS секция с ${DNS_DETOUR}.
    - **КРИТИЧНО**: В template `${ALLOW_INSECURE}` — placeholder ДЛЯ JSON Boolean literal (НЕ строка). После replacingOccurrences значение "true" или "false" должно быть valid JSON boolean. Это значит placeholder в template — `"insecure": ${ALLOW_INSECURE}` (без кавычек вокруг placeholder); после replace становится `"insecure": true` или `"insecure": false`. Это валидный JSON.
    - Над строкой `"insecure": ${ALLOW_INSECURE}` в template добавь комментарий-маркер (в template невозможно — JSON не поддерживает comments) — поэтому маркер должен быть в Swift коде ConfigBuilder.swift: над строкой replacingOccurrences `${ALLOW_INSECURE}` — комментарий `// R1 EXCEPTION — only Hysteria2 (D-08). See SingBoxConfigTemplate.hysteria2.json: `"insecure": ${ALLOW_INSECURE}` is a JSON boolean literal placeholder, NOT a string.`
    - ConfigBuilderTests: test_buildsConfigWithoutPlaceholders, test_validateRoundTrip (SingBoxConfigLoader.validate passes), test_insecureTrue (inputs.allowInsecure=true → JSON содержит "insecure": true; Bool из JSON == true), test_insecureFalse_default (inputs.allowInsecure=false → "insecure": false), test_obfsSalamander_present (inputs.obfs="salamander" + obfsPassword=non-empty → outbound содержит "obfs": {"type":"salamander","password":"..."}), test_obfsAbsent_omitted (inputs.obfs=nil → нет obfs ключа в outbound), test_pinSHA256_present (inputs.pinSHA256 != nil → tls.certificate_public_key_sha256 содержит value), test_customPort_mutated, test_invalidPort_throws
    - swift build для Hysteria2 package проходит; swift test --filter Hysteria2Tests exits 0
  </behavior>
  <action>
    Создай SPM package `BBTB/Packages/Protocols/Hysteria2/` по образцу Trojan/VLESSTLS/Shadowsocks (Plan 02/03 outputs).

    - `Package.swift` — копировать Trojan pattern, name → "Hysteria2", paths Sources/Hysteria2 + Tests/Hysteria2Tests, resources [.process("Resources/SingBoxConfigTemplate.hysteria2.json")]. linkerSettings + dependencies identical.
    - `Sources/Hysteria2/Hysteria2Handler.swift` — копировать TrojanHandler, переименовать → Hysteria2Handler, identifier "hysteria2", displayName "Hysteria2".
    - `Sources/Hysteria2/ConfigBuilder.swift` — ConfigBuilder enum с Hysteria2Inputs struct (поля как описано в behavior), BuilderError (templateLoadFailed, invalidPort, missingPassword, missingSNI), buildSingBoxJSON pipeline:
      1. validate (port 1-65535, non-empty password, non-empty sni).
      2. loadTemplate "SingBoxConfigTemplate.hysteria2".
      3. **R1 EXCEPTION comment block** (важно для review hygiene — Pitfall 2 mitigation):
         ```
         // ============================================================
         // R1 EXCEPTION — ONLY Hysteria2 (D-08).
         // ${ALLOW_INSECURE} placeholder is replaced with `true` or `false`
         // — JSON boolean literal, NOT a string. This is the ONLY place
         // in the entire codebase where tls.insecure may legitimately be true.
         // Any copy of this pattern to other protocol builders = security bug.
         // ============================================================
         ```
      4. replacingOccurrences: ${SERVER_HOST} ← inputs.host, ${HY2_PASSWORD} ← inputs.password, ${SNI_DOMAIN} ← inputs.sni, ${ALLOW_INSECURE} ← (inputs.allowInsecure ? "true" : "false"), ${DNS_DETOUR} ← "hysteria2-out".
      5. mutatePort если port != 443.
      6. Если inputs.fingerprint != nil — JSONSerialization round-trip + mutate outbound[0].tls.utls.fingerprint.
      7. Если inputs.pinSHA256 != nil — mutate outbound[0].tls.certificate_public_key_sha256 = [pinSHA256].
      8. Если inputs.obfs != nil + non-empty obfsPassword — mutate outbound[0].obfs = ["type": "salamander", "password": obfsPassword!].
      9. return final JSON string.
      loadTemplate + mutatePort — копировать из Trojan/ConfigBuilder.
    - `Sources/Hysteria2/Resources/SingBoxConfigTemplate.hysteria2.json` — копировать общую структуру trojan-tcp.json (log/dns/route/experimental); outbounds[0] = `{"type": "hysteria2", "tag": "hysteria2-out", "server": "${SERVER_HOST}", "server_port": 443, "password": "${HY2_PASSWORD}", "tls": { "enabled": true, "server_name": "${SNI_DOMAIN}", "insecure": ${ALLOW_INSECURE}, "alpn": ["h3"], "utls": { "enabled": true, "fingerprint": "chrome" } } }`. ВНИМАНИЕ: `"insecure": ${ALLOW_INSECURE}` — без кавычек вокруг placeholder, чтобы после replace получился JSON boolean (`true`/`false`). route.final → "hysteria2-out". DNS секция с ${DNS_DETOUR}.
    - `Tests/Hysteria2Tests/ConfigBuilderTests.swift` — копировать Trojan tests, factory `defaultInputs` = Hysteria2Inputs(host: "example.com", port: 443, password: "hy2password32bytesfictional", sni: "vpn.example.com", fingerprint: nil, obfs: nil, obfsPassword: nil, allowInsecure: false, pinSHA256: nil, remark: nil). Тесты: test_buildsConfigWithoutPlaceholders, test_validate_strictDefault (allowInsecure=false → JSON contains "insecure" : false, validate passes), test_insecureTrue (allowInsecure=true → JSON.outbounds[0].tls["insecure"] as Bool == true), test_obfsSalamander_added (obfs="salamander", obfsPassword="obfspass" → outbound["obfs"] as [String:Any] equals expected dict), test_obfsAbsent (obfs=nil → outbound["obfs"] == nil), test_customFingerprint_mutated, test_pinSHA256_added, test_customPort_mutated, test_invalidPort_throws, test_emptySNI_throws.

    Реализация по D-08, D-10 + 04-RESEARCH.md Pattern 3 + Pitfall 2 mitigation.
  </action>
  <verify>
    <automated>cd BBTB/Packages/Protocols/Hysteria2 && swift build 2>&1 | tail -10 && swift test --filter Hysteria2Tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File BBTB/Packages/Protocols/Hysteria2/Package.swift exists, contains `name: "Hysteria2"` AND library product Hysteria2
    - File contains `.process("Resources/SingBoxConfigTemplate.hysteria2.json")`
    - File BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/Hysteria2Handler.swift contains `identifier = "hysteria2"` AND `displayName = "Hysteria2"`
    - File BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/ConfigBuilder.swift contains literal `Hysteria2Inputs` AND `${ALLOW_INSECURE}` AND `${HY2_PASSWORD}` AND `hysteria2-out`
    - File contains literal `R1 EXCEPTION` (comment marker)
    - File BBTB/Packages/Protocols/Hysteria2/Sources/Hysteria2/Resources/SingBoxConfigTemplate.hysteria2.json exists
    - File contains literal `${ALLOW_INSECURE}` AND `"type": "hysteria2"` AND `"alpn": ["h3"]`
    - In the JSON, the substring `"insecure": ${ALLOW_INSECURE}` appears (no quotes around placeholder; JSON boolean substitution)
    - `swift build` in BBTB/Packages/Protocols/Hysteria2 exits 0
    - `swift test --filter Hysteria2Tests` in BBTB/Packages/Protocols/Hysteria2 exits 0
    - ConfigBuilderTests file contains literal `XCTAssertEqual(tls["insecure"] as? Bool, true)` или семантический эквивалент для allowInsecure=true case
    - ConfigBuilderTests file contains literal `XCTAssertEqual(tls["insecure"] as? Bool, false)` для default case
    - ConfigBuilderTests file contains literal `SingBoxConfigLoader.validate` (R1 self-test)
  </acceptance_criteria>
  <done>SPM package Protocols/Hysteria2 существует, компилируется, тесты PASS включая insecure=true/false и obfs/pin/fingerprint mutations. R1 EXCEPTION помечен явно в Swift коде.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PoolBuilder buildHysteria2Outbound + invariant test test_nonHy2_outbounds_neverHaveInsecureTrue (R1 mitigation)</name>
  <files>BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift</files>
  <read_first>
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift (после Plan 03 — switch parsed содержит .vlessReality/.vlessTLS/.trojan/.shadowsocks; добавь .hysteria2 case)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift (после Plan 02/03 — содержит test_vlessTLS_* + test_shadowsocks_*)
    - .planning/phases/04-protocol-expansion/04-PATTERNS.md (раздел 7 lines 534-565 — buildHysteria2Outbound с R1 EXCEPTION; раздел Shared Pattern A lines 953-983 — invariant test)
    - .planning/phases/04-protocol-expansion/04-RESEARCH.md (Pattern 3 lines 370-397; Pitfall 2 lines 514-522)
  </read_first>
  <behavior>
    - PoolBuilder.swift switch parsed дополнен case `.hysteria2(let h):` → tag = "hy2-\(index)", outbound = buildHysteria2Outbound(parsed: h, tag: tag)
    - Новая private static func buildHysteria2Outbound(parsed: ParsedHysteria2, tag: String) -> [String: Any]:
      - Над function definition — comment block `// R1 EXCEPTION — only Hysteria2 (D-08). См. wiki R17.`
      - Build tls dictionary: `var tls: [String: Any] = ["enabled": true, "server_name": parsed.sni, "insecure": parsed.allowInsecure]`
      - Над строкой `"insecure": parsed.allowInsecure` — комментарий `// R1 EXCEPTION — only Hysteria2 (D-08). Любое появление этого поля в другом builder'е = bug.`
      - Если parsed.fingerprint != nil → tls["utls"] = ["enabled": true, "fingerprint": parsed.fingerprint!]
      - Если parsed.pinSHA256 != nil → tls["certificate_public_key_sha256"] = [parsed.pinSHA256!]
      - Build outbound: `["type": "hysteria2", "tag": tag, "server": parsed.host, "server_port": parsed.port, "password": parsed.auth, "tls": tls]`
      - Если parsed.obfs == "salamander" && parsed.obfsPassword non-nil non-empty → outbound["obfs"] = ["type": "salamander", "password": parsed.obfsPassword!]
      - return outbound
    - PoolBuilderTests: test_hysteria2_buildsValidOutbound (assert type=="hysteria2", tag starts "hy2-", validate passes), test_hysteria2_insecureTrue (parsed.allowInsecure=true → outbound.tls.insecure==true), test_hysteria2_obfsSalamander, test_nonHy2_outbounds_neverHaveInsecureTrue (Pitfall 2 invariant — multi-protocol pool [.vlessReality, .vlessTLS, .trojan, .shadowsocks, .hysteria2(allowInsecure:true)]; iterate outbounds; для каждого outbound с tag НЕ начинающимся "hy2-" assert tls["insecure"] != true OR tls absent — это R1 invariant)
    - swift test --filter PoolBuilderTests exits 0
  </behavior>
  <action>
    Расширь `PoolBuilder.swift`:

    1. В `switch parsed` после .shadowsocks case'а добавь:
       ```
       case .hysteria2(let h):
         tag = "hy2-\(index)"
         outbound = buildHysteria2Outbound(parsed: h, tag: tag)
       ```
    2. Добавь private static func `buildHysteria2Outbound(parsed: ParsedHysteria2, tag: String) -> [String: Any]`. Над function — большой комментарий-маркер:
       ```
       // ============================================================
       // R1 EXCEPTION — ONLY Hysteria2 (D-08).
       // This is the ONLY outbound builder in BBTB where tls.insecure
       // may legitimately be set to true. Copying this pattern to any
       // other protocol builder is a security bug.
       // See: wiki/security-gaps.md R17, .planning/phases/04-protocol-expansion/04-CONTEXT.md D-08
       // ============================================================
       ```
       Тело function — по 04-PATTERNS.md раздел 7 lines 535-564.

    Расширь `PoolBuilderTests.swift`:
    - helper `makeHysteria2(allowInsecure: Bool = false, obfs: String? = nil, obfsPassword: String? = nil, pinSHA256: String? = nil, fingerprint: String? = nil)` factory.
    - test_hysteria2_buildsValidOutbound: pool=[.hysteria2(makeHysteria2())]; build JSON; assert outbound type=="hysteria2", tag starts "hy2-", validate passes.
    - test_hysteria2_insecureTrue: pool=[.hysteria2(makeHysteria2(allowInsecure: true))]; build JSON; decode; assert tls["insecure"] as? Bool == true.
    - test_hysteria2_insecureFalse_default: pool=[.hysteria2(makeHysteria2(allowInsecure: false))]; assert tls["insecure"] as? Bool == false.
    - test_hysteria2_obfsSalamander_present: obfs="salamander", obfsPassword="obfspass" → outbound["obfs"] dictionary equals expected.
    - test_hysteria2_pinSHA256: pinSHA256="abc" → outbound["tls"]["certificate_public_key_sha256"] as? [String] == ["abc"].
    - **test_nonHy2_outbounds_neverHaveInsecureTrue** (КРИТИЧНО — Pitfall 2 invariant):
      ```
      let pool: [AnyParsedConfig] = [
        .vlessReality(makeVLESSReality()),
        .vlessTLS(makeVLESSTLS()),
        .trojan(makeTrojan()),
        .shadowsocks(makeShadowsocks()),
        .hysteria2(makeHysteria2(allowInsecure: true))  // D-08 exception — legitimate true
      ]
      let json = try PoolBuilder.buildSingBoxJSON(from: pool)
      let root = try JSONSerialization.jsonObject(with: json.data(using: .utf8)!) as! [String: Any]
      let outbounds = root["outbounds"] as! [[String: Any]]
      for outbound in outbounds {
        guard let tag = outbound["tag"] as? String, !tag.hasPrefix("hy2-") else { continue }
        if let tls = outbound["tls"] as? [String: Any], let insecure = tls["insecure"] as? Bool {
          XCTAssertFalse(insecure, "R1 violation: outbound \(tag) has tls.insecure=true (only hy2 allowed)")
        }
      }
      ```

    Не изменять SingBoxConfigLoader (proxyOutboundTypes уже содержит "hysteria2" — verified в Phase 2 W0.T4).
  </action>
  <verify>
    <automated>cd BBTB/Packages/ConfigParser && swift test --filter "PoolBuilderTests|PoolBuilderSingleOutboundTests" 2>&1 | tail -30 && swift test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift contains literal `case .hysteria2(let`
    - File contains literal `buildHysteria2Outbound`
    - File contains literal `R1 EXCEPTION` (comment marker — Pitfall 2 mitigation)
    - File contains literal `"insecure": parsed.allowInsecure` (только в buildHysteria2Outbound — verify единственное вхождение `parsed.allowInsecure` в Swift коде PoolBuilder.swift)
    - File BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift contains literal `test_nonHy2_outbounds_neverHaveInsecureTrue`
    - File contains literal `R1 violation` (assertion message)
    - File contains literal `test_hysteria2_insecureTrue` AND literal `test_hysteria2_insecureFalse_default`
    - `swift test --filter PoolBuilderTests` exits 0
    - `swift test --filter PoolBuilderSingleOutboundTests` exits 0 (regression — existing degenerate-path tests still pass)
    - `swift test` (full ConfigParser suite) exits 0 (all Plan 01-04 tests green)
  </acceptance_criteria>
  <done>PoolBuilder.buildHysteria2Outbound реализован с R1 EXCEPTION маркером; invariant test test_nonHy2_outbounds_neverHaveInsecureTrue passes — R1 enforcement через test gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User hy2:// URI → Hysteria2URIParser | Untrusted input; multi-port detection — pre-URLComponents string scan |
| ParsedHysteria2.allowInsecure → PoolBuilder.buildHysteria2Outbound | ONLY place where tls.insecure may be true |
| sing-box Hysteria2 outbound → external server | QUIC handshake validates self-signed cert if insecure=true; pin check via certificate_public_key_sha256 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Spoofing | hy2:// URI с insecure=1 → man-in-the-middle с self-signed cert | accept (D-08) | Documented user-trust model: пользователь сам выбирает self-hosted server, accepting trust on first use; mitigation в Phase 11 — UI warning при импорте Hy2-insecure config (deferred) |
| T-04-04-02 | Tampering | Программист copy-paste'ит buildHysteria2Outbound → R1 violation в другом протоколе | mitigate | (1) Большой comment block с R1 EXCEPTION marker над function и over `"insecure": parsed.allowInsecure` line; (2) **invariant test test_nonHy2_outbounds_neverHaveInsecureTrue** — CI gate на любое regression; (3) ParsedShadowsocks/ParsedVLESSTLS/ParsedTrojan structs by design не содержат allowInsecure field |
| T-04-04-03 | DoS | Hysteria2 URI с port "1-65535" (full range) | mitigate | D-09 Pitfall 6: pre-URLComponents string scan на `,` или `-` в port-части → throw multiPortNotSupported; test_multiPort_rejects fixture coverage |
| T-04-04-04 | Tampering | malicious obfs value (не "salamander") | mitigate | obfs whitelist в parser: != "salamander" → throw unsupportedObfs; test_obfsNotSalamander_throws coverage |
| T-04-04-05 | Cryptographic | pinSHA256 значение неверное → false sense of security | accept | invalid pin → sing-box handshake fails → no connection (fail-safe); user видит connection error, не молчаливый MITM |
| T-04-04-06 | Information disclosure | Hy2 password в outbound JSON | accept | Same as Trojan/SS — credentials хранятся через Keychain в Plan 06; sing-box JSON генерируется для активного туннеля; T-02-04 invariant — rawURI=nil для supported |
| T-04-04-07 | Repudiation | sing-box принимает unknown obfs type через JSON edit | accept | parser whitelist на client side; даже если bypass — sing-box engine validates obfs.type в handshake (verified A7 — sing-box supports только salamander) |

R1 invariant ENFORCEMENT: (1) Code-level — комментарий-маркер `R1 EXCEPTION` в Swift и в JSON (через comment в Swift outside JSON file); (2) Test-level — `test_nonHy2_outbounds_neverHaveInsecureTrue` итерирует pool и assert'ит invariant; (3) Type-level — ParsedShadowsocks/ParsedVLESSTLS/ParsedTrojan structs не содержат `allowInsecure: Bool` field by design.
</threat_model>

<verification>
- `swift test` в BBTB/Packages/ConfigParser PASS (включая Hy2 tests + Plan 01-03 baseline + invariant test).
- `swift test` в BBTB/Packages/Protocols/Hysteria2 PASS.
- `swift build` обоих packages проходит.
- test_nonHy2_outbounds_neverHaveInsecureTrue is green (доказывает: после добавления .hysteria2 case к pool, non-hy2 outbounds сохраняют R1 strict TLS).
- Manual check: prepare hy2:// URI с insecure=1; load через UniversalImportParser; verify result.supported.first parsed.allowInsecure == true.
- Manual check: prepare hy2:// URI без insecure=1; verify parsed.allowInsecure == false; sing-box JSON Hy2 outbound содержит "insecure": false.
</verification>

<success_criteria>
- Hysteria2 URI парсится с D-09 dual scheme и D-08 трёх синонимов для allowInsecure.
- Multi-port URI отвергнут с понятной ошибкой.
- Sing-box JSON Hy2 outbound содержит tls.insecure согласно parsed.allowInsecure.
- R1 invariant enforced: test_nonHy2_outbounds_neverHaveInsecureTrue PASS — non-hy2 outbounds в multi-protocol pool НИКОГДА не имеют insecure=true.
- Hysteria2URIParserTests все PASS (RED→GREEN transition complete).
- Phase 4 base достигнут: все 3 новых протокола (VLESS+TLS, SS, Hy2) полностью реализованы pipeline-end-to-end (parser → pool → builder → JSON → validate).
</success_criteria>

<output>
After completion, create `.planning/phases/04-protocol-expansion/04-04-SUMMARY.md`
</output>
