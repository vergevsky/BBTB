---
phase: 04-protocol-expansion
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/VLESSURIParser.swift
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/VLESSURIParserTests.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/VLESSURIParserTLSTests.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift
  - BBTB/Packages/Protocols/VLESSTLS/Package.swift
  - BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/VLESSTLSHandler.swift
  - BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift
  - BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/Resources/SingBoxConfigTemplate.vless-tls.json
  - BBTB/Packages/Protocols/VLESSTLS/Tests/VLESSTLSTests/ConfigBuilderTests.swift
autonomous: true
requirements:
  - PROTO-03
requirements_addressed:
  - PROTO-03
must_haves:
  truths:
    - "VLESSURIParser.parse возвращает AnyParsedConfig (breaking change со старой ParsedVLESS)"
    - "vless:// URI с security=tls без pbk/sid → .vlessTLS case"
    - "vless:// URI с pbk OR security=reality → .vlessReality (существующее поведение сохранено, Pitfall 3 mitigated)"
    - "vless:// URI с security=none или missing → throw VLESSURIError.unsupportedSecurity"
    - "vless:// URI с pbk + дополнительным security=tls → .vlessReality (Reality precedence)"
    - "Vision flow (xtls-rprx-vision) сохраняется в ParsedVLESSTLS.flow для подходящих URI"
    - "Protocols/VLESSTLS package компилируется: handler identifier=\"vless-tls\", ConfigBuilder генерирует sing-box JSON через template"
    - "VLESSTLS sing-box JSON проходит SingBoxConfigLoader.validate (R1 strict: tls.insecure=false)"
    - "PoolBuilder.buildSingBoxJSON для pool с .vlessTLS — генерирует outbound type=\"vless\" с tls.insecure=false и без reality block"
    - "UniversalImportParser routes vless:// security=tls в supported (.vlessTLS) — не fail.invalid"
  artifacts:
    - path: "BBTB/Packages/Protocols/VLESSTLS/Package.swift"
      provides: "SPM package VLESSTLS"
      contains: "name: \"VLESSTLS\""
    - path: "BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/VLESSTLSHandler.swift"
      provides: "VPNProtocolHandler с identifier vless-tls"
      contains: "identifier = \"vless-tls\""
    - path: "BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift"
      provides: "buildSingBoxJSON(from: VLESSTLSInputs) -> String"
      contains: "buildSingBoxJSON"
    - path: "BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/Resources/SingBoxConfigTemplate.vless-tls.json"
      provides: "sing-box outbound type=vless с TLS, без reality block, с ${VLESS_FLOW} placeholder"
      contains: "\"insecure\": false"
    - path: "BBTB/Packages/ConfigParser/Sources/ConfigParser/VLESSURIParser.swift"
      provides: "parse(_:) throws -> AnyParsedConfig с двойной веткой Reality/TLS (D-02)"
  key_links:
    - from: "VLESSURIParser.parse"
      to: ".vlessReality OR .vlessTLS"
      via: "Reality detection через q[\"pbk\"] != nil OR q[\"security\"] == \"reality\""
      pattern: "case vlessTLS"
    - from: "PoolBuilder.buildSingBoxJSON"
      to: "buildVLESSTLSOutbound"
      via: "switch parsed case .vlessTLS"
      pattern: "buildVLESSTLSOutbound"
    - from: "VLESSTLS template ${VLESS_FLOW}"
      to: "ConfigBuilder.buildSingBoxJSON"
      via: "replacingOccurrences"
      pattern: "\\$\\{VLESS_FLOW\\}"
---

## Phase Goal

**As a** user, **I want to** импортировать vless:// URI с security=tls (без Reality) и сразу подключиться, **so that** мои VLESS-серверы без Reality keypair работают в BBTB (а не отвергаются как Reality-only).

<objective>
Vertical slice для PROTO-03 (VLESS+TLS без Reality):
1. Расширить `VLESSURIParser.parse` сигнатуру до `throws -> AnyParsedConfig` (D-02) с двойной веткой — Reality precedence сначала, TLS branch потом.
2. Создать новый SPM package `BBTB/Packages/Protocols/VLESSTLS/` с handler, ConfigBuilder, sing-box JSON template (без Reality block, с ${VLESS_FLOW} placeholder который уже работает в Phase 1 после commit 9aa3e93).
3. Расширить `PoolBuilder` новой `buildVLESSTLSOutbound` функцией и case `.vlessTLS` в switch.
4. Обновить `UniversalImportParser.parseSingleURI` case "vless" — после breaking change parser возвращает AnyParsedConfig напрямую (не оборачивать в `.vlessReality`).
5. Заменить XCTFail-placeholders в `VLESSURIParserTLSTests` на полноценные GREEN-тесты; обновить существующие VLESS-Reality тесты на новую сигнатуру.

Purpose: PROTO-03 — расширить покрытие VLESS для серверов без Reality keypair. Vision (xtls-rprx-vision) — частный случай (D-01 максимальное покрытие, не ограничиваться Vision).

Output: импорт `vless://uuid@host:443?security=tls&flow=xtls-rprx-vision&sni=...&fp=chrome#name` (или без flow) проходит → `AnyParsedConfig.vlessTLS(ParsedVLESSTLS)` → ServerConfig → sing-box JSON pool с outbound type=vless без reality block → validate OK.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protocol-expansion/04-CONTEXT.md
@.planning/phases/04-protocol-expansion/04-RESEARCH.md
@.planning/phases/04-protocol-expansion/04-PATTERNS.md
@.planning/phases/04-01-SUMMARY.md
@BBTB/Packages/ConfigParser/Sources/ConfigParser/VLESSURIParser.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift
@BBTB/Packages/Protocols/VLESSReality/Package.swift
@BBTB/Packages/Protocols/Trojan/Package.swift
@BBTB/Packages/Protocols/Trojan/Sources/Trojan/TrojanHandler.swift
@BBTB/Packages/Protocols/Trojan/Sources/Trojan/ConfigBuilder.swift
@BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/Resources/SingBoxConfigTemplate.vless-reality.json
@BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBoxConfigLoader.swift
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/VLESSURIParserTests.swift
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/VLESSURIParserTLSTests.swift
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift
@BBTB/Packages/Protocols/Trojan/Tests/TrojanTests/ConfigBuilderTests.swift

<interfaces>
Existing (Plan 01 created):
- public enum AnyParsedConfig — 5 cases including .vlessTLS(ParsedVLESSTLS)
- public struct ParsedVLESSTLS — uuid, host, port, flow (String?), sni, fingerprint, alpn, networkType, remarks
- public enum UnsupportedReason — includes schemaUnsupportedInPhase4

Current (will change in Task 1):
- public static func VLESSURIParser.parse(_ uri: String) throws -> ParsedVLESS

After Task 1:
- public static func VLESSURIParser.parse(_ uri: String) throws -> AnyParsedConfig

VLESSReality template excerpt — used as base for vless-tls template:
- ${SERVER_HOST}, ${VLESS_UUID}, ${VLESS_FLOW}, ${SNI_DOMAIN}, ${UTLS_FINGERPRINT}, ${DNS_DETOUR}

Trojan package structure (analog):
- Package.swift declares name + library product + path dependencies (../../VPNCore, ../../PacketTunnelKit) + resources processing
- TrojanHandler: VPNProtocolHandler — static identifier, displayName, isAvailable, validate, connect (stub), disconnect (no-op), diagnostics
- ConfigBuilder.buildSingBoxJSON(from:) — loads template via Bundle.module, applies replacingOccurrences, returns String

SingBoxConfigLoader.validate (PacketTunnelKit):
- public static func validate(json: String) throws — accepts outbound types: vless, trojan, urltest, selector, shadowsocks, vmess, hysteria2, wireguard, tuic (verified line 69-73 — НЕ менять)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VLESSURIParser breaking change + D-02 двойная ветка (Reality precedence + TLS branch)</name>
  <files>BBTB/Packages/ConfigParser/Sources/ConfigParser/VLESSURIParser.swift, BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/VLESSURIParserTests.swift, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/VLESSURIParserTLSTests.swift</files>
  <read_first>
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/VLESSURIParser.swift (current parse signature line 44, full body lines 44-end)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift (parseSingleURI case "vless" — найти grep'ом литерал `case "vless"`)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift (после Plan 01 — ParsedVLESSTLS struct shape)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/VLESSURIParserTests.swift (все existing test methods — должны быть обновлены на новую сигнатуру)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/VLESSURIParserTLSTests.swift (Plan 01 placeholders — заменить на real tests)
    - .planning/phases/04-protocol-expansion/04-RESEARCH.md (Example 3 lines 683-728 — точный D-02 branching pattern)
    - .planning/phases/04-protocol-expansion/04-RESEARCH.md (Pitfall 3 lines 524-532 — Reality precedence order)
    - .planning/phases/04-protocol-expansion/04-CONTEXT.md (D-02 — точная логика; D-03 — поля ParsedVLESSTLS)
    - .planning/phases/04-protocol-expansion/04-PATTERNS.md (раздел 6 lines 400-451 — VLESSURIParser MODIFY pattern)
  </read_first>
  <behavior>
    - VLESSURIParser.parse сигнатура: `public static func parse(_ uri: String) throws -> AnyParsedConfig`
    - Reality detection порядок (Pitfall 3): СНАЧАЛА `if q["pbk"] != nil && !(q["pbk"] ?? "").isEmpty || q["security"] == "reality"` → возвращает `.vlessReality(parsed)` со существующей ParsedVLESS construction логикой
    - Иначе `if q["security"] == "tls"` → возвращает `.vlessTLS(ParsedVLESSTLS(...))` со полями: uuid, host, port, flow=q["flow"] (nil если отсутствует или пустая строка), sni=q["sni"] ?? host, fingerprint=q["fp"] ?? "chrome", alpn=parse q["alpn"] (CSV split) ?? ["h2","http/1.1"], networkType=q["type"] ?? "tcp", remarks=comps.fragment?.removingPercentEncoding
    - Иначе (security=none или missing или other) → throw VLESSURIError.unsupportedSecurity(security) — это уже существующий error case или добавь новый case в enum
    - VLESSURIError.unsupportedSecurity(String) — новый case в enum (или существующий — verify в исходнике)
    - UniversalImportParser.parseSingleURI case "vless" — обновлён: возврат `VLESSURIParser.parse(trimmed)` НАПРЯМУЮ в supported (не оборачивать в .vlessReality)
    - В UniversalImportParser помощник `extractServerName(from: AnyParsedConfig) -> String` либо inline — для генерации name из parsed (host:port если remarks nil)
    - Существующие VLESSURIParserTests (Phase 1) обновлены: вместо `let p = try VLESSURIParser.parse(...)` теперь `guard case let .vlessReality(p) = try VLESSURIParser.parse(...) else { XCTFail(); return }` — pattern для Reality assertions
    - VLESSURIParserTLSTests заменён GREEN: test_securityTLS_returnsVlessTLS, test_visionFlow_preserved, test_noFlow_nilField, test_realityWithExtraTLS_returnsReality (Pitfall 3)
    - Существующие VLESS-Reality тесты остаются PASS (regression-free)
    - Существующие импорты vless+reality в UniversalImportParserTests остаются PASS
  </behavior>
  <action>
    Изменить сигнатуру `VLESSURIParser.parse` на `throws -> AnyParsedConfig`. Извлечение URLComponents / host / port / uuid / queryItems → оставить без изменений (lines ~45-60 текущего файла). После сборки `q: [String: String]` добавить D-02 branching:

    1. Сохрани `let security = q["security"] ?? ""`.
    2. `let hasReality = (q["pbk"] != nil && !(q["pbk"] ?? "").isEmpty) || security == "reality"`.
    3. `if hasReality` — выполни существующую ParsedVLESS-construction (копировать оригинальное тело текущей parse функции после security check), верни `.vlessReality(parsed)`.
    4. `else if security == "tls"` — построй `ParsedVLESSTLS(uuid: uuid, host: host, port: port, flow: <q["flow"] non-empty или nil>, sni: q["sni"] ?? host, fingerprint: q["fp"] ?? "chrome", alpn: <CSV split q["alpn"]> ?? ["h2","http/1.1"], networkType: q["type"] ?? "tcp", remarks: comps.fragment?.removingPercentEncoding)`, верни `.vlessTLS(parsed)`.
    5. `else` — `throw VLESSURIError.unsupportedSecurity(security)`. Если case `unsupportedSecurity(String)` ещё не существует в `VLESSURIError` enum — добавь его (с localizedDescription).

    В `UniversalImportParser.swift`, найди `case "vless":` в `parseSingleURI` — текущее тело оборачивает результат в `.vlessReality(...)`. Замени на: `let parsedConfig = try VLESSURIParser.parse(trimmed)` и используй `parsedConfig` напрямую в `.supported(name: ..., parsed: parsedConfig, rawURI: trimmed)`. Извлечение name — host:port если remarks недоступны (для unified pattern — проверь как Trojan делает в case "trojan" и копируй).

    Обнови существующие тесты в `VLESSURIParserTests.swift` — каждый `let p = try VLESSURIParser.parse(...)` где результат раньше был ParsedVLESS, теперь должен распаковываться: `guard case let .vlessReality(p) = try VLESSURIParser.parse(...) else { XCTFail("expected vlessReality"); return }`. Сохрани assertions на p.uuid / p.host / etc. как было.

    Замени XCTFail-placeholders в `VLESSURIParserTLSTests.swift` на полноценные тесты — следуй RESEARCH §Example 3 + Pitfall 3 + 04-VALIDATION.md test names: test_securityTLS_returnsVlessTLS (URI с security=tls без pbk → assert case .vlessTLS, поля совпадают), test_visionFlow_preserved (URI с flow=xtls-rprx-vision → assert parsed.flow == "xtls-rprx-vision"), test_noFlow_nilField (URI без flow query → assert parsed.flow == nil), test_realityWithExtraTLS_returnsReality (URI с pbk + security=tls → assert case .vlessReality, НЕ .vlessTLS — Pitfall 3 invariant), test_securityNone_throws (URI с security=none → throw .unsupportedSecurity).

    Реализация D-02 по `.planning/phases/04-protocol-expansion/04-CONTEXT.md` + точный pattern из 04-RESEARCH.md Example 3.
  </action>
  <verify>
    <automated>cd BBTB/Packages/ConfigParser && swift test --filter "VLESSURIParserTests|VLESSURIParserTLSTests|UniversalImportParserTests" 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/VLESSURIParser.swift contains literal `throws -> AnyParsedConfig`
    - File contains literal `q["pbk"]` AND literal `q["security"] == "reality"` AND literal `q["security"] == "tls"` AND literal `.vlessReality(` AND literal `.vlessTLS(`
    - File contains literal `unsupportedSecurity` (error case)
    - Reality branch appears before TLS branch in source — `grep -n "vlessReality\\|vlessTLS" VLESSURIParser.swift` shows .vlessReality on smaller line number than .vlessTLS
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift case "vless" no longer wraps result in `.vlessReality(...)` (grep -c shows .vlessReality occurrences НЕ внутри switch case "vless" блока)
    - File BBTB/Packages/ConfigParser/Tests/ConfigParserTests/VLESSURIParserTLSTests.swift contains literal `XCTAssertNotNil` или `XCTAssertEqual` AND does NOT contain literal `XCTFail("Pending Plan 02`
    - `swift test --filter VLESSURIParserTLSTests` exits 0 (все tests PASS)
    - `swift test --filter VLESSURIParserTests` exits 0 (regression — Reality tests обновлены на новую сигнатуру и проходят)
    - `swift test --filter UniversalImportParserTests` exits 0 (import flow VLESS-Reality остаётся работающим, импорт vless-tls — новый тест должен PASS если уже добавлен)
  </acceptance_criteria>
  <done>VLESSURIParser возвращает AnyParsedConfig, D-02 двойная ветка работает, все VLESS тесты PASS, UniversalImportParser обновлён.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать Protocols/VLESSTLS SPM package (handler + ConfigBuilder + template + tests)</name>
  <files>BBTB/Packages/Protocols/VLESSTLS/Package.swift, BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/VLESSTLSHandler.swift, BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift, BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/Resources/SingBoxConfigTemplate.vless-tls.json, BBTB/Packages/Protocols/VLESSTLS/Tests/VLESSTLSTests/ConfigBuilderTests.swift</files>
  <read_first>
    - BBTB/Packages/Protocols/Trojan/Package.swift (полностью — точный образец structure, linkerSettings для test target lines 1-36)
    - BBTB/Packages/Protocols/Trojan/Sources/Trojan/TrojanHandler.swift (полностью — паттерн handler, lines 11-49)
    - BBTB/Packages/Protocols/Trojan/Sources/Trojan/ConfigBuilder.swift (полностью — Inputs struct + buildSingBoxJSON + mutatePort + loadTemplate, lines 1-81)
    - BBTB/Packages/Protocols/Trojan/Sources/Trojan/Resources/SingBoxConfigTemplate.trojan-tcp.json (полностью — структура JSON template)
    - BBTB/Packages/Protocols/Trojan/Tests/TrojanTests/ConfigBuilderTests.swift (полностью — тестовый паттерн, validate self-test lines 34-45)
    - BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/Resources/SingBoxConfigTemplate.vless-reality.json (полностью — точная база для vless-tls.json минус reality block)
    - BBTB/Packages/VPNCore/Sources/VPNCore/VPNProtocolHandler.swift (контракт protocol)
    - .planning/phases/04-protocol-expansion/04-PATTERNS.md (разделы 1-4 lines 90-330; раздел 15 lines 851-915 для ConfigBuilder tests)
    - .planning/phases/04-protocol-expansion/04-CONTEXT.md (D-03 — поля ParsedVLESSTLS; D-01 — handler identifier vless-tls)
  </read_first>
  <behavior>
    - Package.swift: name "VLESSTLS", platforms iOS .v18 / macOS .v15, library product "VLESSTLS", dependencies = [.package(path: "../../VPNCore"), .package(path: "../../PacketTunnelKit")], target "VLESSTLS" с resources [.process("Resources/SingBoxConfigTemplate.vless-tls.json")], testTarget "VLESSTLSTests" с linkerSettings как в Trojan (resolv, bsm на macOS, SystemConfiguration/AppKit/UIKit)
    - VLESSTLSHandler.swift: public struct VLESSTLSHandler: VPNProtocolHandler, static identifier = "vless-tls", static displayName = "VLESS + TLS", isAvailable=true, validate(config:) throws (mismatch error case), connect(config:) async throws -> TunnelHandle (returns stub), disconnect (no-op), diagnostics () -> ProtocolDiagnostics, public init()
    - ConfigBuilder.swift: enum ConfigBuilder { public struct VLESSTLSInputs (uuid: UUID, host, port, flow: String?, sni, fingerprint, alpn: [String], remark: String?) + BuilderError enum + buildSingBoxJSON(from: VLESSTLSInputs) throws -> String + private mutatePort + private loadTemplate }
    - buildSingBoxJSON: validate port range 1-65535 (BuilderError.invalidPort), validate non-empty uuid/sni; load template "SingBoxConfigTemplate.vless-tls" из Bundle.module; replacingOccurrences для ${SERVER_HOST} (inputs.host), ${VLESS_UUID} (inputs.uuid.uuidString), ${VLESS_FLOW} (inputs.flow ?? ""), ${SNI_DOMAIN} (inputs.sni), ${UTLS_FINGERPRINT} (inputs.fingerprint), ${DNS_DETOUR} ("vless-out"); if port != 443 → mutatePort
    - SingBoxConfigTemplate.vless-tls.json: точная копия vless-reality.json минус блок reality в tls. outbound type "vless", tag "vless-out", server "${SERVER_HOST}", server_port 443, uuid "${VLESS_UUID}", flow "${VLESS_FLOW}", network "tcp", tls { enabled true, server_name "${SNI_DOMAIN}", insecure false, alpn ["h2","http/1.1"], utls { enabled true, fingerprint "${UTLS_FINGERPRINT}" } }. route.final "vless-out". dns секция с ${DNS_DETOUR}.
    - tls.insecure = false hardcoded — R1 invariant
    - ConfigBuilderTests: test_buildsConfigWithoutPlaceholders (все ${X} substrings absent в результате), test_validateRoundTrip (XCTAssertNoThrow(try SingBoxConfigLoader.validate(json: json))), test_visionFlowSet (если inputs.flow = "xtls-rprx-vision" → json содержит этот flow), test_emptyFlowOmitted (если inputs.flow == nil → json содержит flow placeholder заменённый на "" — verified не падает), test_customPort (non-443 port мутирован), test_invalidPort_throws
    - swift build для VLESSTLS package проходит
    - swift test --filter VLESSTLSTests exits 0
  </behavior>
  <action>
    Создать новый SPM package `BBTB/Packages/Protocols/VLESSTLS/` по точному образцу `BBTB/Packages/Protocols/Trojan/`.

    Структура файлов:
    - `Package.swift` — копировать Trojan/Package.swift и заменить "Trojan" → "VLESSTLS"; resources list = single entry `.process("Resources/SingBoxConfigTemplate.vless-tls.json")`; пути sources `Sources/VLESSTLS`, tests `Tests/VLESSTLSTests`; dependencies остаются path-based на ../../VPNCore и ../../PacketTunnelKit; testTarget linkerSettings — копировать exactly (resolv, bsm на macOS, SystemConfiguration на macOS, AppKit на macOS, UIKit на iOS).
    - `Sources/VLESSTLS/VLESSTLSHandler.swift` — копировать TrojanHandler.swift; заменить `Trojan` → `VLESSTLS` в имени struct и error type, заменить identifier "trojan" → "vless-tls", displayName "Trojan" → "VLESS + TLS". HandlerError структура та же (identifierMismatch case).
    - `Sources/VLESSTLS/ConfigBuilder.swift` — копировать Trojan/ConfigBuilder.swift; переименовать TrojanInputs → VLESSTLSInputs с полями (uuid: UUID, host: String, port: Int, flow: String?, sni: String, fingerprint: String, alpn: [String], remark: String?); BuilderError enum (templateLoadFailed(String), invalidPort(Int), missingUUID, missingSNI); buildSingBoxJSON pipeline: validate → loadTemplate(named: "SingBoxConfigTemplate.vless-tls") → replacingOccurrences для всех 5 placeholders + ${DNS_DETOUR} ← "vless-out" → if port != 443 mutatePort. mutatePort и loadTemplate — копировать exactly из TrojanConfigBuilder.
    - `Sources/VLESSTLS/Resources/SingBoxConfigTemplate.vless-tls.json` — копировать `BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/Resources/SingBoxConfigTemplate.vless-reality.json`, удалить блок `"reality": { ... }` из outbound[0].tls, оставить `"insecure": false`, `"alpn": ["h2","http/1.1"]`, `"utls"`. Сохранить `"flow": "${VLESS_FLOW}"` placeholder и DNS секцию с `${DNS_DETOUR}`. route.final = "vless-out".
    - `Tests/VLESSTLSTests/ConfigBuilderTests.swift` — копировать Trojan/Tests/TrojanTests/ConfigBuilderTests.swift, переименовать factory `tcpInputs` → `defaultInputs` с VLESSTLSInputs (uuid: UUID(uuidString: "00000000-0000-0000-0000-00000000ABCD")!, host: "example.com", port: 443, flow: nil, sni: "vpn.example.ru", fingerprint: "chrome", alpn: ["h2","http/1.1"], remark: nil); тесты: test_buildsConfigWithoutPlaceholders (assert no ${X} в JSON, assert "example.com" в JSON, assert "vpn.example.ru" в JSON, assert "vless-out" в JSON, XCTAssertNoThrow(try SingBoxConfigLoader.validate(json: json)) — R1 self-test), test_visionFlowSet (flow="xtls-rprx-vision" → assert json содержит этот flow), test_nilFlow_handled (flow=nil → ${VLESS_FLOW} replaced with "", validate всё равно passes — A1 assumption check), test_customPort (port=8443 → assert json содержит "server_port" : 8443), test_invalidPort_throws (port=0 → XCTAssertThrowsError BuilderError.invalidPort).

    Не создавать Reality outbound block в template; не использовать insecure=true; не добавлять `${ALLOW_INSECURE}` placeholder. Это критично для R1 invariant (VLESS-TLS — strict TLS, не Hy2 exception).
  </action>
  <verify>
    <automated>cd BBTB/Packages/Protocols/VLESSTLS && swift build 2>&1 | tail -10 && swift test --filter VLESSTLSTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File BBTB/Packages/Protocols/VLESSTLS/Package.swift exists and contains literal `name: "VLESSTLS"` AND literal `library(name: "VLESSTLS"`
    - File contains literal `.package(path: "../../VPNCore")` AND literal `.package(path: "../../PacketTunnelKit")`
    - File contains literal `.process("Resources/SingBoxConfigTemplate.vless-tls.json")`
    - File contains linker settings — literal `.linkedLibrary("resolv")` AND `.linkedFramework("UIKit", .when(platforms: [.iOS]))`
    - File BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/VLESSTLSHandler.swift exists and contains literal `identifier = "vless-tls"` AND literal `displayName = "VLESS + TLS"` AND literal `: VPNProtocolHandler`
    - File BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/ConfigBuilder.swift exists and contains literal `VLESSTLSInputs` AND `buildSingBoxJSON` AND `${VLESS_FLOW}` AND `vless-out`
    - File BBTB/Packages/Protocols/VLESSTLS/Sources/VLESSTLS/Resources/SingBoxConfigTemplate.vless-tls.json exists, is valid JSON (`python3 -c "import json; json.load(open('...'))"` exits 0)
    - JSON contains literal `"type": "vless"` AND `"insecure": false` AND `${VLESS_FLOW}` placeholder
    - JSON does NOT contain literal `"reality"` (no reality block at all)
    - `swift build` in BBTB/Packages/Protocols/VLESSTLS exits 0
    - `swift test --filter VLESSTLSTests` in BBTB/Packages/Protocols/VLESSTLS exits 0 (все builder tests PASS)
    - ConfigBuilderTests.swift contains literal `XCTAssertNoThrow(try SingBoxConfigLoader.validate` (R1 self-test)
  </acceptance_criteria>
  <done>SPM package Protocols/VLESSTLS существует, компилируется, тесты PASS, sing-box JSON template без reality block с R1 strict TLS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PoolBuilder — buildVLESSTLSOutbound + case .vlessTLS в switch + integration tests</name>
  <files>BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift</files>
  <read_first>
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift (полностью — switch parsed lines ~42-49, buildVLESSOutbound lines ~102-125, buildTrojanOutbound lines ~127-164)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift (existing test patterns)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderSingleOutboundTests.swift (single-outbound degenerate path patterns)
    - BBTB/Packages/PacketTunnelKit/Sources/PacketTunnelKit/SingBoxConfigLoader.swift (validate function — НЕ менять, только использовать в self-tests)
    - .planning/phases/04-protocol-expansion/04-PATTERNS.md (раздел 7 lines 453-518 — PoolBuilder MODIFY pattern + buildVLESSTLSOutbound spec)
    - .planning/phases/04-protocol-expansion/04-RESEARCH.md (Pattern 2 lines 342-365 для outbound builder shape)
    - .planning/phases/04-protocol-expansion/04-CONTEXT.md (D-01 — Vision flow optional)
  </read_first>
  <behavior>
    - PoolBuilder.swift switch parsed дополнен case `.vlessTLS(let v):` → tag = "vless-tls-\(index)", outbound = buildVLESSTLSOutbound(parsed: v, tag: tag)
    - Новая private static func buildVLESSTLSOutbound(parsed: ParsedVLESSTLS, tag: String) -> [String: Any] возвращает: type="vless", tag, server=parsed.host, server_port=parsed.port, uuid=parsed.uuid.uuidString, flow=parsed.flow ?? "" (или omit если nil — выбрать вариант, который проходит SingBoxConfigLoader.validate; verified A1 assumption — sing-box принимает empty flow), network="tcp", tls={ enabled: true, server_name: parsed.sni, insecure: false (R1 invariant — НЕ из parsed), alpn: parsed.alpn, utls: { enabled: true, fingerprint: parsed.fingerprint } }
    - Pool JSON с .vlessTLS outbound НЕ содержит "reality" под tls
    - Pool JSON с .vlessTLS outbound содержит "insecure": false (R1)
    - Pool JSON с .vlessTLS outbound через SingBoxConfigLoader.validate проходит без throw
    - PoolBuilderTests: test_vlessTLS_buildsValidOutbound (single .vlessTLS → assert outbound[0].type=="vless", tag начинается "vless-tls-", insecure==false, нет reality ключа в tls), test_vlessTLS_inPool_withTrojan (multi-outbound pool [.vlessTLS, .trojan] → assert urltest selector упоминает оба tag'а), test_vlessTLS_visionFlow_preserved (parsed.flow="xtls-rprx-vision" → assert outbound["flow"] == "xtls-rprx-vision"), test_vlessTLS_nilFlow_handled (parsed.flow=nil → assert outbound flow либо отсутствует, либо пустая строка, и validate passes)
    - swift test --filter PoolBuilderTests exits 0
  </behavior>
  <action>
    Расширь `BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift`:

    1. В `switch parsed` (около lines 42-49) добавь после существующих case'ов: `case .vlessTLS(let v): tag = "vless-tls-\(index)"; outbound = buildVLESSTLSOutbound(parsed: v, tag: tag)`.
    2. Добавь private static func `buildVLESSTLSOutbound(parsed: ParsedVLESSTLS, tag: String) -> [String: Any]`. Используй паттерн buildVLESSOutbound (lines 102-125) как базу, но без `reality` блока в tls. Конкретные ключи outbound: "type": "vless", "tag": tag, "server": parsed.host, "server_port": parsed.port, "uuid": parsed.uuid.uuidString, "flow": parsed.flow ?? "", "network": "tcp", "tls": [ "enabled": true, "server_name": parsed.sni, "insecure": false, "alpn": parsed.alpn, "utls": [ "enabled": true, "fingerprint": parsed.fingerprint ] ].
    3. ВАЖНО: `insecure` хардкодим `false` (НЕ из parsed). Это R1 invariant — VLESS-TLS не имеет D-08 exception (только Hy2).
    4. Закомментировать рядом с `"insecure": false` — `// R1 invariant — VLESS+TLS strict TLS (no Hy2-style exception)`.

    Расширь `BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift`:
    - Добавь helper `makeVLESSTLS(...)` factory с дефолтами (uuid, host="vpn.test", port=443, flow=nil, sni="vpn.test", fingerprint="chrome", alpn=["h2","http/1.1"], networkType="tcp", remarks=nil).
    - test_vlessTLS_buildsValidOutbound: pool = [.vlessTLS(makeVLESSTLS())]; let json = try PoolBuilder.buildSingBoxJSON(from: pool); decode JSON; assert outbounds[0]["type"] as? String == "vless", outbounds[0]["tag"] as? String starts with "vless-tls-", tls["insecure"] as? Bool == false, tls["reality"] == nil; XCTAssertNoThrow(try SingBoxConfigLoader.validate(json: json)) — R1 self-test.
    - test_vlessTLS_visionFlow: parsed.flow = "xtls-rprx-vision" → assert outbound["flow"] as? String == "xtls-rprx-vision".
    - test_vlessTLS_nilFlow: parsed.flow = nil → assert flow либо отсутствует, либо "" (verify работает с SingBoxConfigLoader.validate).
    - test_pool_vlessTLS_plus_trojan: multi-outbound pool — assert обе tag'а присутствуют в urltest outbound селекторе.

    Не изменять existing Reality/Trojan branches. Не изменять SingBoxConfigLoader (proxyOutboundTypes уже содержит "vless").
  </action>
  <verify>
    <automated>cd BBTB/Packages/ConfigParser && swift test --filter "PoolBuilderTests|PoolBuilderSingleOutboundTests" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift contains literal `case .vlessTLS(let`
    - File contains literal `buildVLESSTLSOutbound`
    - File contains literal `"insecure": false` (R1 invariant for VLESS-TLS)
    - File contains comment literal `R1 invariant` somewhere near buildVLESSTLSOutbound
    - File BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderTests.swift contains literal `test_vlessTLS_buildsValidOutbound` (or similar test name with vlessTLS)
    - PoolBuilderTests file contains literal `XCTAssertNoThrow(try SingBoxConfigLoader.validate` (R1 self-test for new outbound)
    - `swift test --filter PoolBuilderTests` exits 0
    - `swift test --filter PoolBuilderSingleOutboundTests` exits 0 (regression — existing tests for single outbound остаются working)
    - `swift test --filter UniversalImportParserTests` exits 0 (regression — VLESS-Reality import flow still works)
    - `swift test` (full ConfigParser test suite) exits 0
  </acceptance_criteria>
  <done>PoolBuilder.buildVLESSTLSOutbound реализован с R1 strict TLS, switch расширен, тесты PASS, integration с SingBoxConfigLoader.validate работает.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User pastebuf vless:// URI → VLESSURIParser | Untrusted input crosses; URI parsed as Foundation URLComponents |
| ParsedVLESSTLS → PoolBuilder JSON → sing-box | Generated JSON validated by SingBoxConfigLoader (R1 inbound whitelist) |
| Bundle.module template → ConfigBuilder | Static resource, build-time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Spoofing | Reality URI с подставленным `security=tls` → ошибочная классификация как vlessTLS (потеря Reality crypto) | mitigate | D-02 Pitfall 3: branch order `hasReality` BEFORE `security=="tls"`; test `test_realityWithExtraTLS_returnsReality` обеспечивает invariant |
| T-04-02-02 | Tampering | vless:// URI с `security=none` или missing — потенциальный downgrade на plain TCP | mitigate | D-02: security=none → throws VLESSURIError.unsupportedSecurity; UniversalImportParser маршрутизирует в failed.invalid; R1 invariant сохранён |
| T-04-02-03 | Information disclosure | VLESS UUID в outboundJSON (logged?) | accept | UUID — это credentials sing-box JSON; не пишется в логи приложения; хранится через Keychain тоже (Phase 1/2 pattern); risk не повышен Phase 4 |
| T-04-02-04 | Tampering | Подмена insecure=true в VLESS-TLS outbound (R1 violation) | mitigate | PoolBuilder.buildVLESSTLSOutbound хардкодит `"insecure": false`; не читает из ParsedVLESSTLS (тот не содержит allowInsecure поля по дизайну D-03); test_vlessTLS_buildsValidOutbound assert'ит tls.insecure == false |
| T-04-02-05 | DoS | Malformed vless:// URI с recursion или extreme size | accept | URLComponents имеет встроенный лимит длины; vless URI <2KB realistically; no parser amplification path |
| T-04-02-06 | Information disclosure | Vision flow value утечка | accept | `xtls-rprx-vision` — это public Xray spec, не credentials; flow поле в outboundJSON безопасно |

R1 invariant: VLESS+TLS template + builder hardcode `tls.insecure: false`. ParsedVLESSTLS struct **намеренно** не содержит `allowInsecure: Bool` поля — это design-time enforcement (D-08 exception только для Hy2).
</threat_model>

<verification>
- `swift test` в BBTB/Packages/ConfigParser passes полностью (включая обновлённые VLESS-Reality тесты + новые VLESS-TLS тесты).
- `swift test` в BBTB/Packages/Protocols/VLESSTLS passes.
- `swift build` обоих пакетов проходит.
- Manual: загрузить vless-tls-vision.txt fixture в UniversalImportParser → result.supported.first → AnyParsedConfig.vlessTLS — verify via test.
- Manual: загрузить vless-reality fixture → result.supported.first → AnyParsedConfig.vlessReality — verify regression.
</verification>

<success_criteria>
- VLESS+TLS URI (vision и без vision) парсится в .vlessTLS, проходит через PoolBuilder, генерирует валидный sing-box JSON.
- Reality URI продолжает парситься в .vlessReality (Pitfall 3 enforced, regression-free).
- VLESS-TLS sing-box JSON content проходит SingBoxConfigLoader.validate (R1).
- Protocols/VLESSTLS package компилируется и тесты PASS.
- VLESSURIParserTLSTests все PASS (RED→GREEN transition complete).
</success_criteria>

<output>
After completion, create `.planning/phases/04-protocol-expansion/04-02-SUMMARY.md`
</output>
