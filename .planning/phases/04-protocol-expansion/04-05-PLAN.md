---
phase: 04-protocol-expansion
plan: 05
type: execute
wave: 5
depends_on: [04-01, 04-02, 04-03, 04-04]
files_modified:
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/ClashYAMLParser.swift
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/StubParsers.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ClashYAMLParserTests.swift
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/UniversalImportParserTests.swift
autonomous: true
requirements:
  - IMP-04
  - IMP-05
requirements_addressed:
  - IMP-04
  - IMP-05
must_haves:
  truths:
    - "ClashYAMLParser парсит секцию proxies: через Yams.load → [String: Any] → mapping в [ImportedServer]"
    - "Маппит типы: ss → .shadowsocks, trojan → .trojan, vless (без reality-opts) → .vlessTLS, vless (с reality-opts) → .vlessReality, hysteria2/hy2 → .hysteria2, vmess → unsupported, неизвестное → unsupported"
    - "alpn поле обрабатывается dual-type: string CSV или array (Pitfall 4 mitigation)"
    - "skip-cert-verify (Clash YAML) → allowInsecure (для hysteria2; для других ignored — R1)"
    - "Битый YAML → ClashYAMLParser возвращает empty results (не throws на весь импорт)"
    - "Per-proxy ошибки изолированы — bad proxy → .unsupported, не throws"
    - "UniversalImportParser.classify обнаруживает Clash YAML format: input starts with proxies: OR contains mixed-port:/allow-lan:"
    - "UniversalImportParser routes detected Clash YAML → ClashYAMLParser.parse"
    - "UniversalImportParser routes все 5 URI схем правильно: vless / trojan / ss / hy2 / hysteria2 + subscription URL + JSON endpoint + Clash YAML"
    - "Outline access keys работают через ss:// path (Plan 03 покрытие) — IMP-05 закрыт"
  artifacts:
    - path: "BBTB/Packages/ConfigParser/Sources/ConfigParser/ClashYAMLParser.swift"
      provides: "ClashYAMLParser.parse(_ body: String) throws -> [ImportedServer]; isSupportedSSMethod helper; parseALPN helper"
      contains: "Yams.load"
    - path: "BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift"
      provides: "classify branch для Clash YAML; case .clashYAML в InputClass enum; routing"
      contains: "ClashYAMLParser.parse"
  key_links:
    - from: "UniversalImportParser.classify"
      to: "ClashYAMLParser.parse"
      via: "InputClass.clashYAML(body)"
      pattern: "ClashYAMLParser"
    - from: "ClashYAMLParser per-proxy switch"
      to: "AnyParsedConfig (5 cases)"
      via: "type field mapping"
      pattern: "case \"ss\"|case \"trojan\"|case \"vless\"|case \"hysteria2\""
---

## Phase Goal

**As a** user, **I want to** импортировать Clash YAML subscription (paste весь YAML или fetch через subscription URL возвращающий YAML), **so that** мой Clash subscription панель работает в BBTB без manual conversion.

<objective>
Финализация import pipeline:
1. Создать `ClashYAMLParser.swift` (новый Swift файл в ConfigParser) — единственный файл Phase 4 без точного in-repo analog'а. Использует Yams 6.2.1 (Plan 01 dependency) для YAML→[String: Any]. Маппит секцию `proxies:` в [ImportedServer].
2. Расширить `UniversalImportParser.classify` детектированием Clash YAML format — input starts with `proxies:` OR contains `mixed-port:` / `allow-lan:` YAML markers.
3. Расширить `UniversalImportParser` routing — детектированный Clash YAML → ClashYAMLParser.parse → результат как ImportResult.
4. Закрыть IMP-04 finish: убедиться что UniversalImportParser routes все 5 URI схем (vless / trojan / ss / hy2 / hysteria2) — все 5 case'ов уже добавлены в Plan 02-04, этот план верифицирует комплексным integration test.
5. Закрыть IMP-05 finish: Outline access keys (через ss:// path Plan 03 — D-11 — единственный parser покрывает) + Clash YAML (Plan 05 main feature).
6. Заменить XCTFail-placeholders в `ClashYAMLParserTests` на GREEN tests.
7. Обновить `StubParsers.supportedSchemesInPhase4` (Plan 01 уже создан с 5 схемами) — здесь финализация displayName messages, чтобы unsupported items показывали "v0.4" вместо "v0.2".

Purpose: IMP-04 / IMP-05 — IMP-04 = parsing всех URI handler'ов; IMP-05 = Outline + Clash YAML.

Output: paste Clash YAML с 6 proxies → ImportResult со supported.count == 5 (5 поддерживаемых типов: ss, trojan, vless-tls, vless-reality, hysteria2) и unsupported.count == 1 (vmess). swift test --filter ClashYAMLParserTests + UniversalImportParserTests PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-protocol-expansion/04-CONTEXT.md
@.planning/phases/04-protocol-expansion/04-RESEARCH.md
@.planning/phases/04-protocol-expansion/04-PATTERNS.md
@.planning/phases/04-01-SUMMARY.md
@.planning/phases/04-02-SUMMARY.md
@.planning/phases/04-03-SUMMARY.md
@.planning/phases/04-04-SUMMARY.md
@BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/ShadowsocksURIParser.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/Hysteria2URIParser.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/StubParsers.swift
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ClashYAMLParserTests.swift
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/UniversalImportParserTests.swift
@BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/clash-mixed-proxies.yaml

<interfaces>
Existing (Plan 01-04):
- AnyParsedConfig: .vlessReality, .vlessTLS, .trojan, .shadowsocks, .hysteria2
- ParsedVLESSTLS, ParsedShadowsocks, ParsedHysteria2 — все доступны
- ParsedVLESS (existing) — для Reality из Clash reality-opts
- ParsedTrojan (existing) — для Trojan из Clash
- UnsupportedReason: schemaUnsupportedInPhase4, unsupportedSSMethod, multiPortNotSupported, ... existing
- ShadowsocksURIParser.supportedSSMethods — Set<String> с 8 методами; reuse для Clash YAML cipher check
- UniversalImportParser.parseSingleURI: уже содержит case "vless"/"trojan"/"ss"/"hy2"/"hysteria2" (Plan 02-04)
- import Yams (Plan 01 dependency)

Clash YAML proxy fields per type (04-RESEARCH.md Sources):
- common: name, type, server, port
- ss: cipher, password
- trojan: password, sni, alpn?, network?, ws-opts?, skip-cert-verify?
- vless: uuid, network, tls?, servername?, flow?, alpn?, ws-opts? (если есть reality-opts с public-key/short-id → Reality)
- vless reality-opts: public-key, short-id, fingerprint? (under reality-opts.client-fingerprint)
- hysteria2/hy2: password, sni?, skip-cert-verify?, obfs?, obfs-password?, ports? (multi-port)
- vmess: uuid, alterId, cipher, network — Phase 4 NOT SUPPORTED

UniversalImportParser.classify (existing pattern):
- Returns InputClass enum (single URI, multi-URI block, subscription URL, JSON endpoint, base64 block). Add: `.clashYAML(body: String)` case.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClashYAMLParser.swift — Yams.load + per-proxy mapping в AnyParsedConfig (6 типов)</name>
  <files>BBTB/Packages/ConfigParser/Sources/ConfigParser/ClashYAMLParser.swift, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ClashYAMLParserTests.swift</files>
  <read_first>
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift (parseSingBoxJSON pattern — multi-result accumulation; existing extractParsed* helpers)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift (ImportedServer enum — .supported / .unsupported case constructors)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/ShadowsocksURIParser.swift (Plan 03 — supportedSSMethods reuse)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ClashYAMLParserTests.swift (Plan 01 placeholders)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/Fixtures/clash-mixed-proxies.yaml (полностью — основная test fixture)
    - .planning/phases/04-protocol-expansion/04-RESEARCH.md (Pattern 4 lines 400-462; Example 5 lines 802-826 — Yams usage; Pitfall 4 lines 533-548 — alpn dual-type)
    - .planning/phases/04-protocol-expansion/04-CONTEXT.md (D-12 — точный mapping per-type; D-13 — detection)
    - .planning/phases/04-protocol-expansion/04-PATTERNS.md (раздел "No Analog Found" lines 1072-1076 — почему этот файл уникальный)
  </read_first>
  <behavior>
    - import Yams + Foundation
    - public enum ClashYAMLParser
    - public static func parse(_ body: String) throws -> [ImportedServer]
      Реализация: guard let root = try Yams.load(yaml: body) as? [String: Any], let proxies = root["proxies"] as? [[String: Any]] else { return [] }; iterate per-proxy; на каждый — try cast required fields (name, type, server, port — все 4 mandatory; иначе continue); внутри per-proxy try/catch — bad proxy → .unsupported entry, не throws на весь YAML
    - Type mapping switch:
      - case "ss": cipher + password required; if ShadowsocksURIParser.supportedSSMethods.contains(cipher) → .supported(.shadowsocks(ParsedShadowsocks(host: server, port: port, method: cipher, password: password, remarks: name))); иначе .unsupported(scheme:"ss", reason: .unsupportedSSMethod)
      - case "trojan": password + sni? required → .supported(.trojan(ParsedTrojan(password: password, host: server, port: port, security: "tls", sni: sni ?? server, fingerprint: "chrome", alpn: parseALPN(proxy["alpn"]), transport: .tcp, remarks: name))); если network=="ws" — пробросить ws-opts (path, host) в .ws transport
      - case "vless": uuid required; check `reality-opts` под proxy — если есть public-key non-empty → .vlessReality (build ParsedVLESS из reality-opts.public-key + reality-opts.short-id + servername + reality-opts.client-fingerprint? ?? "chrome"); иначе если tls == true → .vlessTLS (ParsedVLESSTLS из servername, fingerprint, alpn, flow); иначе → .unsupported(reason: .schemaUnsupportedInPhase4) (vless без TLS — same как URI security=none)
      - case "hysteria2", "hy2": password + sni? required → .supported(.hysteria2(ParsedHysteria2(host: server, port: port, auth: password, sni: sni ?? server, fingerprint: nil, obfs: obfs (если == "salamander" иначе nil), obfsPassword: obfs-password, allowInsecure: (skip-cert-verify == true || skip-cert-verify == "1"), pinSHA256: nil, remarks: name))); если ports field имеется (multi-port) → .unsupported(reason: .multiPortNotSupported)
      - case "vmess": → .unsupported(scheme: "vmess", reason: .schemaUnsupportedInPhase4)
      - default: → .unsupported(scheme: type, reason: .schemaUnsupportedInPhase4)
    - Helper private static func parseALPN(_ raw: Any?) -> [String]:
      if let arr = raw as? [String] { return arr }
      if let s = raw as? String { return s.split(separator: ",").map { $0.trimmingCharacters(in: .whitespaces) } }
      return ["h2", "http/1.1"]  // default
    - Helper private static func parseBool(_ raw: Any?) -> Bool:
      if let b = raw as? Bool { return b }
      if let s = raw as? String { return ["true","1","yes"].contains(s.lowercased()) }
      if let i = raw as? Int { return i != 0 }
      return false
    - On Yams.load throw → throw rethrow; caller handles
    - Pitfall 4: alpn может быть string CSV или array — parseALPN handles
    - rawURI для каждого ImportedServer — `(try? Yams.dump(object: proxy)) ?? ""` (одна YAML proxy entry serialized back — для potential re-parse в auto-upgrade later)
    - Test cases GREEN: test_extractsProxies (clash-mixed-proxies.yaml → result.count >= 6, supported.count == 5, unsupported.count == 1 (vmess)), test_mixedProxies_classifiedCorrectly (assert types map: ss→shadowsocks, trojan→trojan, hysteria2→hysteria2, vless с reality-opts → vlessReality, vless без → vlessTLS, vmess → unsupported), test_brokenYAML_returnsEmpty (input "this is not yaml: [unclosed" → empty results OR throws — выбрать поведение, документировать), test_alpnStringVsArray_handled (YAML с alpn: "h2,http/1.1" vs alpn: ["h2","http/1.1"] — оба должны парситься), test_perProxyErrorIsolation (one bad proxy в YAML не блокирует остальные)
    - swift test --filter ClashYAMLParserTests exits 0
  </behavior>
  <action>
    Создай `BBTB/Packages/ConfigParser/Sources/ConfigParser/ClashYAMLParser.swift`. Imports: `import Foundation`, `import Yams` (Plan 01 dependency available).

    Реализуй public enum ClashYAMLParser с public static func `parse(_ body: String) throws -> [ImportedServer]`. Алгоритм — точно по 04-RESEARCH.md Pattern 4 lines 408-461 + Pitfall 4 alpn handling + Pitfall 5 для bad proxy isolation:

    ```
    public static func parse(_ body: String) throws -> [ImportedServer] {
        guard let root = try Yams.load(yaml: body) as? [String: Any],
              let proxies = root["proxies"] as? [[String: Any]]
        else { return [] }
        var results: [ImportedServer] = []
        for proxy in proxies {
            guard let type = (proxy["type"] as? String)?.lowercased(),
                  let name = proxy["name"] as? String,
                  let server = proxy["server"] as? String,
                  let port = proxy["port"] as? Int
            else { continue }
            let raw = (try? Yams.dump(object: proxy)) ?? ""
            switch type {
            case "ss":
                // map в .shadowsocks или .unsupported(unsupportedSSMethod)
                ...
            case "trojan":
                // map в .trojan
                ...
            case "vless":
                // если proxy["reality-opts"] dictionary с non-empty public-key → .vlessReality
                // иначе если proxy["tls"] == true → .vlessTLS
                // иначе → .unsupported(reason: .schemaUnsupportedInPhase4)
                ...
            case "hysteria2", "hy2":
                // если есть proxy["ports"] (multi-port) → .unsupported(reason: .multiPortNotSupported)
                // иначе map в .hysteria2 с skip-cert-verify → allowInsecure
                ...
            case "vmess":
                results.append(.unsupported(name: name, scheme: "vmess", host: server, port: port, rawURI: raw, reason: .schemaUnsupportedInPhase4))
            default:
                results.append(.unsupported(name: name, scheme: type, host: server, port: port, rawURI: raw, reason: .schemaUnsupportedInPhase4))
            }
        }
        return results
    }
    ```

    Private helpers: parseALPN, parseBool — точные сигнатуры в behavior блоке. parseALPN из 04-RESEARCH.md Pitfall 4 lines 538-548.

    Замени XCTFail placeholders в `ClashYAMLParserTests.swift`:
    - test_extractsProxies: loadFixture("clash-mixed-proxies", ext: "yaml") → ClashYAMLParser.parse(body) → assert results.count == total proxies в fixture; count supported items >= 5; count unsupported >= 1 (vmess).
    - test_mixedProxies_classifiedCorrectly: parse same fixture; assert конкретные типы (используй switch на .supported / .unsupported + extracted AnyParsedConfig case'ы; для каждой proxy с known type — check correct AnyParsedConfig case mapping).
    - test_brokenYAML_returnsEmpty: ClashYAMLParser.parse("this is not yaml: [unclosed") → assert results.isEmpty (OR XCTAssertThrowsError если выбран throw-путь — выбрать один и документировать в комментарии теста).
    - test_alpnStringVsArray_handled: inline YAML body с двумя trojan proxies — один с alpn: "h2,http/1.1", второй с alpn: ["h2","http/1.1"]; assert оба парсятся в .trojan со совпадающим alpn содержимым [String].
    - test_perProxyError_isolation: inline YAML с тремя proxies: один валидный ss, один невалидный (пропущено password), один валидный trojan; assert results.count == 3 (с одной .unsupported или skipped); не throws на весь YAML.

    Реализация по D-12 / D-13 + 04-RESEARCH.md Pattern 4 + Pitfall 4 + Pitfall 5.
  </action>
  <verify>
    <automated>cd BBTB/Packages/ConfigParser && swift test --filter ClashYAMLParserTests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/ClashYAMLParser.swift exists
    - File contains literal `import Yams`
    - File contains literal `Yams.load(yaml:` AND literal `proxies` AND literal `[String: Any]`
    - File contains literal `case "ss":` AND `case "trojan":` AND `case "vless":` AND `case "hysteria2", "hy2":` (or similar grouping) AND `case "vmess":`
    - File contains literal `parseALPN` AND literal `parseBool` (helper functions)
    - File contains literal `reality-opts` (Reality detection логика для VLESS)
    - File contains literal `skip-cert-verify` (Hysteria2 D-08 mapping)
    - File BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ClashYAMLParserTests.swift does NOT contain `XCTFail("Pending Plan 05`
    - File contains literal `test_extractsProxies`, `test_mixedProxies`, `test_brokenYAML`, `test_alpnStringVsArray`
    - File contains literal `XCTAssertEqual` или `XCTAssertGreaterThanOrEqual` (actual assertions)
    - `swift test --filter ClashYAMLParserTests` exits 0
  </acceptance_criteria>
  <done>ClashYAMLParser реализован — парсит секцию proxies в [ImportedServer] с правильным mapping 5+ типов; per-proxy error isolation; Pitfall 4 alpn handling; tests PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UniversalImportParser — Clash YAML detection + routing + IMP-04 integration test (все 5 URI схем)</name>
  <files>BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift, BBTB/Packages/ConfigParser/Sources/ConfigParser/StubParsers.swift, BBTB/Packages/ConfigParser/Tests/ConfigParserTests/UniversalImportParserTests.swift</files>
  <read_first>
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift (классификация InputClass — найди grep `enum InputClass` и `func classify`; рассмотри всю функцию; найди где routing executes)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/StubParsers.swift (после Plan 01 — supportedSchemesInPhase4 уже определена; displayName messages могут упоминать "v0.2" — обнови на "v0.4")
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/UniversalImportParserTests.swift (existing test patterns; добавить новые tests для Clash YAML + 5-schemes coverage)
    - .planning/phases/04-protocol-expansion/04-CONTEXT.md (D-13 — Clash YAML detection: prefix `proxies:` OR yaml-markers `mixed-port:` / `allow-lan:`)
    - .planning/phases/04-protocol-expansion/04-PATTERNS.md (раздел 8 lines 567-594 — UniversalImportParser MODIFY)
    - .planning/phases/04-protocol-expansion/04-RESEARCH.md (System Architecture Diagram lines 145-194)
  </read_first>
  <behavior>
    - UniversalImportParser InputClass enum дополнен case `.clashYAML(body: String)` (или эквивалент)
    - classify() метод дополнен Clash YAML detection branch — выполняется ДО subscription URL check:
      ```
      let trimmedLower = trimmed.lowercased()
      if trimmedLower.hasPrefix("proxies:") || trimmedLower.contains("mixed-port:") || trimmedLower.contains("allow-lan:") {
          return .clashYAML(body: trimmed)
      }
      ```
    - Routing — на InputClass.clashYAML(body) → call ClashYAMLParser.parse(body) → wrap results в ImportResult(supported: [...], unsupported: [...], failed: [], subscriptionURL: nil, source: source, metadata: ["format": "clash-yaml"])
    - Если ClashYAMLParser.parse throws (broken YAML) → ImportResult с empty supported/unsupported и `.failed(.invalid(rawURI: trimmed prefix, error: ...))`
    - StubParsers: supportedSchemesInPhase4 уже определена в Plan 01; обнови displayName-related messages в StubParsers (если такие есть упоминающие "v0.2") на "v0.4" — найди grep `v0\\.2` в StubParsers.swift; если найдены — поменяй на v0.4
    - UniversalImportParserTests новые тесты:
      - test_classify_clashYAML: input "proxies:\n  - name: test\n    type: ss\n    server: ..." → classify returns .clashYAML branch
      - test_classify_yamlMarkers: input "mixed-port: 7890\nallow-lan: false\nproxies:\n..." → .clashYAML
      - test_import_clashYAML_endToEnd: loadFixture clash-mixed-proxies.yaml → importer.import(rawInput: body) → result.supported.count >= 5, result.unsupported.count >= 1
      - test_routes_phase4_protocols (IMP-04 integration test): для каждого из 5 schemes (vless, trojan, ss, hy2, hysteria2) — fixture → importer.import → result.supported.count == 1 + parsed case correctness
      - test_outlineAccessKey_routes_ss: loadFixture outline-access-key.txt → importer.import → result.supported.first parsed == .shadowsocks(...) (IMP-05 integration test для Outline)
      - test_clashYAML_with_brokenYAML: входной body "proxies:\n  -<broken yaml content with bad indentation@@" → failed != empty OR supported.isEmpty (документировать поведение)
    - swift test --filter UniversalImportParserTests exits 0
  </behavior>
  <action>
    Расширь `BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift`:

    1. В существующий InputClass enum (найди grep'ом `enum InputClass`) добавь case `clashYAML(body: String)`. Если enum приватный/internal — оставить тот же visibility.
    2. В функции classify() (найди grep'ом `func classify` или anchor `subscriptionURL` check) добавь Clash YAML detection branch ПЕРЕД subscription URL check:
       ```
       let trimmedLower = trimmed.lowercased()
       if trimmedLower.hasPrefix("proxies:") || trimmedLower.contains("\nproxies:") || trimmedLower.contains("mixed-port:") || trimmedLower.contains("allow-lan:") {
           return .clashYAML(body: trimmed)
       }
       ```
       (включить также вариант с переводом строки чтобы YAML файлы со starting blank line или комментарием тоже детектились — `trimmedLower.contains("\nproxies:")`).
    3. В switch routing на InputClass result (найди grep'ом `switch class` или routing logic после classify call) добавь case `.clashYAML(let body)`:
       ```
       case .clashYAML(let body):
         do {
           let items = try ClashYAMLParser.parse(body)
           var supported: [ImportedServer] = []
           var unsupported: [ImportedServer] = []
           for item in items {
             switch item {
             case .supported: supported.append(item)
             case .unsupported: unsupported.append(item)
             }
           }
           return ImportResult(supported: supported, unsupported: unsupported, failed: [], subscriptionURL: nil, source: source, metadata: ["format": "clash-yaml"])
         } catch {
           return ImportResult(supported: [], unsupported: [], failed: [.invalid(rawURI: String(body.prefix(120)), error: error.localizedDescription)], subscriptionURL: nil, source: source, metadata: nil)
         }
       ```
       (точный синтаксис — adjust под существующую сигнатуру ImportResult в текущей кодовой базе).

    В `StubParsers.swift` — найди литералы "v0.2" или "Phase 2" в displayName/error message формах; если есть upgrade-path messages — поменяй на "v0.4" / "Phase 4". supportedSchemesInPhase4 уже из Plan 01.

    Расширь `UniversalImportParserTests.swift`:
    - test_classify_clashYAML — input "proxies:\n  - name: T\n    type: ss\n    server: e.com\n    port: 8388" → call classifier (если функция internal — @testable import уже даёт доступ); assert .clashYAML branch returned.
    - test_classify_yamlMarkers — input с `mixed-port:` → .clashYAML.
    - test_import_clashYAML_endToEnd — let body = loadFixture("clash-mixed-proxies", ext: "yaml"); let importer = UniversalImportParser(); let result = try await importer.import(rawInput: body, source: .pasteboard); assert result.supported.count >= 5, result.unsupported.count >= 1.
    - test_routes_all_phase4_protocols: для каждого из 5 fixture-схем (vless-tls-no-flow.txt, trojan fixture из Phase 2, ss-2022-aes-128-gcm.txt, hy2-with-obfs.txt — и `hysteria2://...` inline string) — call importer.import → assert result.supported.count == 1 + извлечь supported[0] → check `case let .supported(_, parsed, _)` и assert правильный case .vlessTLS / .trojan / .shadowsocks / .hysteria2 / .vlessReality (для VLESS-Reality fixture если есть).
    - test_outlineAccessKey_routes_ss — loadFixture outline-access-key.txt → import → supported.first parsed case .shadowsocks (IMP-05 integration).

    Не сломай существующие UniversalImportParser tests (regression).
  </action>
  <verify>
    <automated>cd BBTB/Packages/ConfigParser && swift test --filter UniversalImportParserTests 2>&1 | tail -40 && swift test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift contains literal `clashYAML` (InputClass case)
    - File contains literal `ClashYAMLParser.parse`
    - File contains literal `proxies:` (detection branch)
    - File contains literal `mixed-port:` OR `allow-lan:` (yaml-marker detection)
    - File contains literal `"format": "clash-yaml"` (metadata) OR similar metadata marker
    - File BBTB/Packages/ConfigParser/Tests/ConfigParserTests/UniversalImportParserTests.swift contains literal `test_import_clashYAML` OR `test_classify_clashYAML`
    - File contains literal `test_routes_all_phase4_protocols` OR `test_routes_phase4_protocols` (IMP-04 integration test)
    - File contains literal `test_outlineAccessKey` (IMP-05 integration)
    - `swift test --filter UniversalImportParserTests` exits 0
    - `swift test` (full ConfigParser suite) exits 0 — все Plan 01-05 tests green
  </acceptance_criteria>
  <done>UniversalImportParser детектирует и маршрутизирует Clash YAML; 5 URI схем routes verified integration-test'ом; Outline access keys работают через ss:// path; IMP-04 + IMP-05 закрыты.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Subscription URL fetch → YAML body → ClashYAMLParser | YAML body untrusted; from remote subscription server |
| Yams.load → [String: Any] → switch type | Type casts могут fail → per-proxy isolation |
| Clash YAML proxy entries → AnyParsedConfig | Each proxy independently mapped; bad one не аффектит остальные |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | DoS | Malicious Clash YAML с recursion (anchors/aliases) или extreme size | mitigate | LibYAML (под капотом Yams) имеет встроенный depth limit; subscription fetch body-size cap deferred to Phase 7 (W-02-09 carry-forward — не Phase 4 scope) |
| T-04-05-02 | Tampering | YAML с embedded shell strings в proxy fields (e.g., name: "`rm -rf`") | mitigate | Yams.load возвращает `[String: Any]`, не Codable; no code execution path; URLs/paths from YAML никогда не передаются в Process.run или shell |
| T-04-05-03 | Spoofing | Subscription claims `type: vless` но `reality-opts` malformed (missing public-key) → fall-through на vlessTLS (потеря Reality crypto) | mitigate | reality-opts detection: проверяет `public-key` non-empty AND short-id non-empty; ноль обоих → fall to TLS branch (valid intent — пользователь именно так хочет); один из двух — could be misconfiguration → log warning (deferred Phase 11 UX) |
| T-04-05-04 | DoS | Single proxy throws → drops весь YAML | mitigate | Per-proxy try/catch (или continue на guard let fail) — bad entry → .unsupported, не throws ClashYAMLParser.parse целиком; test_perProxyError_isolation enforces |
| T-04-05-05 | Information disclosure | YAML body в logs | accept | UniversalImportParser logs только short prefix (120 chars) в error message; full YAML body не пишется в TunnelLogger |
| T-04-05-06 | Spoofing | Clash YAML с trojan + skip-cert-verify: true | mitigate (R1) | parseBool(skip-cert-verify) для Trojan игнорируется (ParsedTrojan не имеет allowInsecure field — R1 invariant; same как Phase 2 URI parser); только Hysteria2 honors skip-cert-verify (D-08) |
| T-04-05-07 | Tampering | ClashYAMLParser маршрутизирует unknown type как .unsupported → fall-through на other parsers | accept | UniversalImportParser routing: detected Clash YAML → ТОЛЬКО ClashYAMLParser, не fall-through на URI parser; .unsupported entries сохраняются в SwiftData с rawURI=YAML proxy serialized, для potential future auto-upgrade |

R1 invariant: Clash YAML trojan/vless-tls/ss никогда не получают allowInsecure=true даже если YAML содержит skip-cert-verify: true. Только hysteria2/hy2 honor skip-cert-verify (D-08). Type-level enforcement через ParsedTrojan/ParsedShadowsocks/ParsedVLESSTLS structs не имеющих allowInsecure field.
</threat_model>

<verification>
- `swift test` в BBTB/Packages/ConfigParser passes (Plan 01-05 cumulative green).
- ClashYAMLParser fixture round-trip: clash-mixed-proxies.yaml → parse → results.count == fixture proxies count.
- UniversalImportParser classify detects YAML body — verified test_classify_clashYAML / test_classify_yamlMarkers.
- IMP-04 integration test test_routes_all_phase4_protocols PASS — 5 schemes correctly mapped.
- IMP-05 integration test test_outlineAccessKey_routes_ss PASS.
- No regression в Phase 1/2/3 import flows.
</verification>

<success_criteria>
- ClashYAMLParser реализован через Yams; маппит 6 типов; per-proxy error isolation; alpn dual-type handling.
- UniversalImportParser детектирует Clash YAML format и маршрутизирует ClashYAMLParser.
- IMP-04 finish: все 5 URI схем (vless / trojan / ss / hy2 / hysteria2) routes verified.
- IMP-05 finish: Outline access keys работают через ss:// path + Clash YAML работает.
- ClashYAMLParserTests + новые UniversalImportParserTests все PASS.
- ConfigParser test suite полностью зелёный.
</success_criteria>

<output>
After completion, create `.planning/phases/04-protocol-expansion/04-05-SUMMARY.md`
</output>
