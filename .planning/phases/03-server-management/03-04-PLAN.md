---
phase: 03-server-management
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionMergeService.swift
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift
  - BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListViewModel.swift
  - BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift
  - BBTB/Packages/AppFeatures/Sources/ServerListFeature/SubscriptionHeader.swift
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenView.swift
  - BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift
  - BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/PullToRefreshTests.swift
  - BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/MergeStrategyTests.swift
  - BBTB/Packages/VPNCore/Tests/VPNCoreTests/CascadeDeleteTests.swift
autonomous: true
requirements: [SRV-02, SRV-03, UX-04]
must_haves:
  truths:
    - "Pull-to-refresh выполняет 2 фазы последовательно: fetch all Subscription URLs → merge ServerConfig → пинг всех supported"
    - "Re-fetch той же подписки сохраняет lastLatencyMs существующих серверов (server-identity: host+port+protocolID+sni)"
    - "Серверы, исчезнувшие из последнего fetch, помечаются missingFromLastFetch=true (НЕ удаляются — D-14)"
    - "Swipe delete на ServerRow удаляет один ServerConfig + Keychain entry; coordinator.selectedServerID сбрасывается если был этим id"
    - "Swipe + confirm на SubscriptionHeader выполняет cascade delete (Subscription + все её ServerConfig + Keychain entries)"
    - "App foreground (scenePhase .active) триггерит silent refresh без UI spinner"
    - "ConfigImporting protocol расширен публичными методами persistKeychainSecret(for:) и buildServerConfig(from:subscriptionID:keychainTag:); ServerListViewModel вызывает их через protocol reference (без force-cast к concrete ConfigImporter)"
  artifacts:
    - path: "BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionMergeService.swift"
      provides: "Merge fetched ImportedServer by server-identity into existing pool"
      contains: "SubscriptionMergeService"
    - path: "BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListViewModel.swift"
      provides: "pullToRefresh, silentForegroundRefresh, deleteServer, confirmDeleteSubscription"
      contains: "pullToRefresh"
    - path: "BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenView.swift"
      provides: "scenePhase observer triggering silent refresh"
      contains: "scenePhase"
    - path: "BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift"
      provides: "ConfigImporting protocol с public методами persistKeychainSecret(for:) и buildServerConfig(from:subscriptionID:keychainTag:); concrete ConfigImporter implements + extracts из persistSupported"
      contains: "persistKeychainSecret"
  key_links:
    - from: "ServerListSheet pull gesture"
      to: "ServerListViewModel.pullToRefresh"
      via: ".refreshable { await viewModel.pullToRefresh() }"
      pattern: "pullToRefresh"
    - from: "MainScreenView scenePhase .active"
      to: "ServerListViewModel.silentForegroundRefresh"
      via: ".onChange(of: scenePhase)"
      pattern: "scenePhase"
    - from: "ServerListViewModel.confirmDeleteSubscription"
      to: "Keychain cleanup + cascade delete + selection reset"
      via: "KeychainStore.delete + context.delete + coordinator.applySelection(nil)"
      pattern: "KeychainStore.delete"
    - from: "ServerListViewModel.fetchAndMerge"
      to: "ConfigImporter.persistKeychainSecret + buildServerConfig (через ConfigImporting protocol)"
      via: "importer.persistKeychainSecret(for:) / importer.buildServerConfig(from:subscriptionID:keychainTag:) closures"
      pattern: "ConfigImporting"
---

<objective>
Четвёртый вертикальный слайс — pull-to-refresh + foreground refresh + cascade delete (D-07, D-12, D-13, D-14). После выполнения: pull-down в server list sheet → fetch всех subscription URLs → merge by identity (preserve lastLatencyMs) → ping all → UI обновляется. App foreground (return from background) → silent refresh без UI spinner. Swipe delete на row/header → удаление с cascade + Keychain cleanup.

Phase Goal (user story):

**As a** пользователь BBTB, **I want to** видеть свои серверы сгруппированными по подпискам, переключаться между ними и обновлять список одним жестом, **so that** я мог подключаться к лучшему доступному серверу без ручной настройки.

Покрытие в этом плане: «обновлять список одним жестом» — yes (pull + foreground); merge сохраняет latency history; удаление работает консистентно. Pre-connect auto-select + reconnect-on-active — Plan 05.

Purpose: Без этого плана server list — статичный snapshot at sheet open. Pull-to-refresh + merge — главное user-facing affordance подписок (UX-04 требование). Cascade delete — единственный путь удалить подписку (D-07).

Output:
- SubscriptionMergeService.swift NEW
- ConfigImporting protocol — расширен `persistKeychainSecret(for:)` и `buildServerConfig(from:subscriptionID:keychainTag:)`, чтобы ServerListViewModel мог вызвать их через protocol reference (без typing к concrete ConfigImporter).
- ConfigImporter — extract helpers из persistSupported, expose как public methods, заменить deleteExistingPool на merge при .httpURL branch.
- ServerListViewModel — implement pullToRefresh, silentForegroundRefresh, deleteServer, confirmDeleteSubscription
- ServerListSheet — wire .refreshable + .confirmationDialog
- SubscriptionHeader — inline fetch-error indicator
- MainScreenView — .onChange(of: scenePhase)
- ServerConfig — computed identity property
- 3 test файла
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-server-management/03-CONTEXT.md
@.planning/phases/03-server-management/03-RESEARCH.md
@.planning/phases/03-server-management/03-PATTERNS.md
@.planning/phases/03-server-management/03-UI-SPEC.md
@BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/UniversalImportParser.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionURLFetcher.swift
@BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift
@BBTB/Packages/VPNCore/Sources/VPNCore/KeychainStore.swift

<interfaces>
Plan 01 contracts:
- @Model Subscription + ServerConfig.subscriptionID + Phase-3 fields.
- ConfigImporter.getOrCreateSubscription(url:name:in:) returns Subscription.
- ConfigImporter.persistSupported(_:subscriptionID:in:) returns ServerConfig.

Plan 02 contracts:
- ServerProbeService.probeAll(_: [(UUID, String, Int)]) -> AsyncStream<(UUID, ProbeAggregate)>.

Plan 03 contracts:
- ServerListViewModel — @MainActor с @Published state, sections, pingStates, refreshError, pendingDeleteSubscription, weak coordinator. Plan 03 содержит stub bodies for pullToRefresh, deleteServer, confirmDeleteSubscription. Plan 04 заполняет.

ConfigImporting protocol expansion (новое в этом плане — критично для compile при вызове через protocol reference):
- Существующий protocol (см. ConfigImporter.swift строки 19-37) НЕ имеет `persistKeychainSecret` / `buildServerConfig`. Plan 04 расширяет:
  ```
  public protocol ConfigImporting: AnyObject, Sendable {
      // ... existing members (importFromRawInput, importFromPasteboard, importFromQRCode, loadActiveServer, countSupportedConfigs) сохраняются неизменными ...

      /// Persist Keychain secret для supported ImportedServer; возвращает keychainTag (или nil для unsupported).
      /// Используется SubscriptionMergeService через closure injection при pull-to-refresh.
      func persistKeychainSecret(for server: ImportedServer) throws -> String?

      /// Построить ServerConfig из ImportedServer + subscriptionID + keychainTag.
      /// НЕ insert'ит в context — caller (SubscriptionMergeService) делает context.insert.
      func buildServerConfig(from server: ImportedServer,
                             subscriptionID: UUID,
                             keychainTag: String?) -> ServerConfig
  }
  ```
- concrete `ConfigImporter` class implements оба метода (extract логики из существующего persistSupported / persistUnsupported в строках 215-313). Существующий persistSupported wrapper остаётся для single-paste branch (вызывает новые helpers внутри).
- ServerListViewModel хранит `private let importer: any ConfigImporting` (или `ConfigImporting`-typed property — Swift 5.9+ allows existential without `any` для AnyObject protocol). При вызове через protocol reference compile passes.
- WHY not subclass / sub-protocol: ConfigImporting и так single-protocol abstraction для всего import surface; добавление двух methods — наименее инвазивно. Альтернатива «sub-protocol ServerConfigPersisting» рассмотрена и отброшена — лишний layer без выгоды (всегда один и тот же conformer).

Phase 1/2 carry-forward:
- KeychainStore.delete(tag:) throws — Keychain cleanup при cascade delete.
- ConfigImporter.deleteExistingPool(subscriptionURL:in:) Phase 2 helper — заменяется merge logic в subscription URL branch (single-paste branch продолжает использовать deleteAllExistingConfigs).
- SubscriptionURLFetcher.fetch(url:session:) — Phase 2 fetcher (W-02-09 carry-forward — без body cap, accepted risk до Phase 7).
- UniversalImportParser — actor для parse fetched body.

Server identity для D-14 dedup:
- Composite key: host:port:protocolID:sni. ServerConfig extension computed property `var identity: String { "\(host):\(port):\(protocolID):\(sni ?? "")" }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing tests — merge identity dedup + pull-to-refresh 2-phase order + cascade delete</name>
  <files>BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/MergeStrategyTests.swift, BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/PullToRefreshTests.swift, BBTB/Packages/VPNCore/Tests/VPNCoreTests/CascadeDeleteTests.swift</files>
  <read_first>
    - .planning/phases/03-server-management/03-CONTEXT.md (D-07, D-12, D-13, D-14)
    - .planning/phases/03-server-management/03-RESEARCH.md секции «Code Examples — Example 5, Example 7», «Common Pitfalls — Pitfall 5, Pitfall 9, Pitfall 10», «Open Questions — 2»
    - .planning/phases/03-server-management/03-PATTERNS.md секции «SubscriptionMergeService.swift», «ConfigImporter.swift — MODIFIED»
    - .planning/phases/03-server-management/03-UI-SPEC.md §3 + §6
    - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift (deleteExistingPool строки 315-326 как pattern Keychain cleanup; existing ConfigImporting protocol строки 19-37)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift (ImportedServer fields)
    - BBTB/Packages/VPNCore/Sources/VPNCore/KeychainStore.swift (API delete(tag:))
    - BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListViewModel.swift (Plan 03 stubs)
  </read_first>
  <behavior>
    MergeStrategyTests (`SubscriptionMergeService.merge(fetchedSupported:fetchedUnsupported:into:context:persistKeychain:buildServerConfig:)`):
    - test_merge_new_uris_inserts_serverconfig — fetched 2 supported, existing пустой; assert 2 ServerConfig в store с subscriptionID == sub.id и missingFromLastFetch == false
    - test_merge_existing_identity_preserves_latency — existing ServerConfig (host:port:protocolID:sni совпадает с fetched) с lastLatencyMs == 42; merge → lastLatencyMs остался 42, name обновлён на fetched.displayName, missingFromLastFetch == false
    - test_merge_disappeared_uris_marks_missing — existing identity X не в fetched → row.missingFromLastFetch == true, row НЕ удалён
    - test_merge_updates_subscription_lastFetched — после merge subscription.lastFetched в пределах 1 sec от .now
    - test_merge_isolated_per_subscription — две Subscription А и Б, merge А не трогает rows Б (Б.missingFromLastFetch == false для всех)
    - test_merge_unsupported_servers_persist_with_subscriptionID — fetched включает unsupported (isSupported=false) → ServerConfig инсерт с subscriptionID == sub.id, keychainTag == nil

    PullToRefreshTests (`ServerListViewModel.pullToRefresh()`):
    - test_pull_to_refresh_two_phase_order — mock fetcher и mock probeService записывают timestamps; assert: ALL fetch events завершились ДО первого probe event (recorded events ordered list — fetch markers перед probe markers)
    - test_pull_to_refresh_partial_fetch_failure — две Subscription; first fetch throws, second ok; assert viewModel.subscriptionFetchErrors содержит entry для first.id, второй sub.lastFetched обновлён, ping вышел на existing servers
    - test_pull_to_refresh_all_fetch_fail — все fetch throw; assert state == .refreshError(...), refreshError != nil (UI-SPEC §3.4 — full failure → alert)
    - test_pull_to_refresh_state_transitions — start state .loaded → start pullToRefresh → state .refreshing → completion → state .loaded
    - test_silent_foreground_refresh_does_not_set_refreshing_state — start state .loaded → silentForegroundRefresh → state остаётся .loaded throughout (badges progressively updated через pingStates но not state)

    CascadeDeleteTests (ServerListViewModel.deleteServer + confirmDeleteSubscription через minimal in-memory ModelContainer + mock coordinator):
    - test_delete_serverconfig_removes_keychain — Insert ServerConfig с keychainTag "test-tag-1"; store Keychain entry с этим tag; deleteServer(id) → ServerConfig удалён + Keychain entry отсутствует (KeychainStore.fetch returns error / nil)
    - test_delete_subscription_cascades_to_serverconfigs — Subscription А + 3 ServerConfig.subscriptionID = А.id с keychain tags; confirmDeleteSubscription(А) → Subscription удалена + 3 ServerConfig удалены + 3 Keychain entries cleaned
    - test_delete_subscription_does_not_touch_orphan_servers — orphan (subscriptionID=nil) + 2 серверов в А; confirmDeleteSubscription(А) → orphan остался
    - test_delete_subscription_clears_selected_when_selected — mock coordinator.selectedServerID == server-in-А.id; confirmDeleteSubscription(А) → mock.applySelection called с nil
    - test_delete_single_server_clears_selected_when_selected — mock coordinator.selectedServerID == server.id; deleteServer(id) → mock.applySelection called с nil
  </behavior>
  <action>
    1. Создать MergeStrategyTests.swift в BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/. Imports {Foundation, SwiftData, XCTest, @testable ConfigParser, @testable VPNCore}. Setup in-memory ModelContainer для (ServerConfig.self, Subscription.self). Helper для создания mock ImportedServer factory.
    2. Создать PullToRefreshTests.swift в BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/. Imports @testable ServerListFeature. Mock SubscriptionURLFetcher через protocol injection (Plan 04 GREEN-task сделает fetcher injectable через ViewModel init). Mock ServerProbeService через subclassing OR через injectable probeService protocol. Mock ConfigImporting через `final class MockImporter: ConfigImporting { ... }` (реализует все protocol members, в т.ч. новые persistKeychainSecret/buildServerConfig — для compile необходимо чтобы protocol уже был расширен в Task 2). Записывать events в shared `[(timestamp: Date, kind: String)]` для assertion on ordering.
    3. Создать CascadeDeleteTests.swift в BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/CascadeDeleteTests.swift. ВНИМАНИЕ: cascade-delete fitting в VPNCore tests — но ServerListViewModel живёт в ServerListFeature. Решение — файл создаётся в ServerListFeatureTests, frontmatter обновляется при коммите (replace BBTB/Packages/VPNCore/Tests/VPNCoreTests/CascadeDeleteTests.swift → BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/CascadeDeleteTests.swift). Mock coordinator: `final class MockCoordinator: ServerSelectionCoordinating { var selectedServerID: UUID?; func applySelection(_ id: UUID?) { selectedServerID = id; appliedCalls.append(id) }; var appliedCalls: [UUID?] = []; func dismissServerList() {} }`. Tests verify KeychainStore tags через `try? KeychainStore.fetch(tag: ...)` returns nil after delete.
    4. Запустить тесты; assert RED.
    5. Commit: `test(03-04): add failing tests for merge identity + pull-to-refresh order + cascade delete`.

    NOTE по frontmatter: при создании CascadeDeleteTests.swift внутри ServerListFeatureTests — обновить files_modified в frontmatter этого PLAN.md до правильного пути перед commit (replace BBTB/Packages/VPNCore/Tests/VPNCoreTests/CascadeDeleteTests.swift на BBTB/Packages/AppFeatures/Tests/ServerListFeatureTests/CascadeDeleteTests.swift).
  </action>
  <verify>
    <automated>cd /Users/vergevsky/ClaudeProjects/VPN && swift test --package-path BBTB/Packages/AppFeatures --filter "MergeStrategyTests|PullToRefreshTests|CascadeDeleteTests" 2>&1 | grep -E "error:|FAILED|Cannot find" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File MergeStrategyTests.swift exists in MainScreenFeatureTests with test_merge_existing_identity_preserves_latency, test_merge_disappeared_uris_marks_missing
    - File PullToRefreshTests.swift exists in ServerListFeatureTests with test_pull_to_refresh_two_phase_order, test_pull_to_refresh_partial_fetch_failure, test_silent_foreground_refresh_does_not_set_refreshing_state
    - File CascadeDeleteTests.swift exists (either path) with test_delete_subscription_cascades_to_serverconfigs, test_delete_subscription_clears_selected_when_selected
    - Command `swift test --package-path BBTB/Packages/AppFeatures --filter MergeStrategyTests` exits non-zero (RED)
    - Command `swift test --package-path BBTB/Packages/AppFeatures --filter PullToRefreshTests` exits non-zero (RED)
    - Command `swift test --package-path BBTB/Packages/AppFeatures --filter CascadeDeleteTests` exits non-zero (RED)
    - Git log shows commit "test(03-04): add failing tests for merge identity + pull-to-refresh order + cascade delete"
  </acceptance_criteria>
  <done>3 test файла существуют, все RED, commit на RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConfigImporting protocol expansion + SubscriptionMergeService + ConfigImporter rewrite + ServerListViewModel implementations (GREEN)</name>
  <files>BBTB/Packages/VPNCore/Sources/VPNCore/ServerConfig.swift, BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionMergeService.swift, BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift, BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListViewModel.swift, BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift, BBTB/Packages/AppFeatures/Sources/ServerListFeature/SubscriptionHeader.swift, BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenView.swift</files>
  <read_first>
    - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift (existing ConfigImporting protocol строки 19-37, persistSupported строки 215-292, persistUnsupported строки 294-313, deleteExistingPool строки 315-326)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/ImportedServer.swift (fields доступные для identity computation)
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionURLFetcher.swift (Phase 2 fetcher API)
    - BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListViewModel.swift (Plan 03 — где находятся stub methods)
    - BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListSheet.swift (Plan 03 — где находится .refreshable stub)
    - .planning/phases/03-server-management/03-RESEARCH.md «Code Examples — Example 5, Example 7», «Common Pitfalls — Pitfall 5, 9, 10»
    - .planning/phases/03-server-management/03-UI-SPEC.md §3 + §6
  </read_first>
  <action>
    Размер таска большой; следовать порядку строго.

    A. ServerConfig identity computed:
    1. В ServerConfig.swift добавить:
       public extension ServerConfig {
           var identity: String { "\(host):\(port):\(protocolID):\(sni ?? "")" }
       }

    B. SubscriptionMergeService:
    2. Создать BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionMergeService.swift. Imports: Foundation, SwiftData, VPNCore. Декларация:

       public enum SubscriptionMergeService {
           public static func merge(
               fetchedSupported: [ImportedServer],
               fetchedUnsupported: [ImportedServer],
               into subscription: Subscription,
               context: ModelContext,
               persistKeychain: (ImportedServer) throws -> String?,
               buildServerConfig: (ImportedServer, UUID, String?) -> ServerConfig
           ) throws
       }

       Логика per RESEARCH Example 7 строки 1086-1112:
       (1) FetchDescriptor<ServerConfig>(predicate: #Predicate { $0.subscriptionID == subscription.id }) → existing;
       (2) existingByIdentity: [String: ServerConfig] dictionary;
       (3) newIdentities: Set<String>;
       (4) объединить supported + unsupported в один цикл (хранить isSupported из ImportedServer): для каждого fetched — compute identity через `"\(server.host):\(server.port):\(server.protocolID):\(server.sni ?? "")"`; newIdentities.insert; если existingByIdentity[identity] != nil → row.name = server.displayName, row.missingFromLastFetch = false, preserve lastLatencyMs/lastPingedAt/failedProbeCount (НЕ переписывать); если новый → tag = try persistKeychain(fetched) (для unsupported persistKeychain возвращает nil); newConfig = buildServerConfig(fetched, subscription.id, tag); context.insert(newConfig);
       (5) для каждого row in existing where !newIdentities.contains(row.identity) → row.missingFromLastFetch = true;
       (6) subscription.lastFetched = .now.

       НЕ вызывать context.save() — caller контролирует точку сохранения (atomic transaction).

       OSLog: subsystem "app.bbtb.subscription-merge", category "merge".

    C. ConfigImporting protocol expansion + ConfigImporter rewire (BLOCKER fix per planner-checker):

    C.0. **Расширить ConfigImporting protocol** (в начале ConfigImporter.swift строки 19-37). Добавить два метода ПОСЛЕ существующих (важно: сохранить existing members неизменными — Phase 1/2 callers продолжают работать):
       ```
       public protocol ConfigImporting: AnyObject, Sendable {
           // ... existing 5 members сохраняются БЕЗ ИЗМЕНЕНИЙ ...

           /// Plan 04 — persist Keychain secret for an ImportedServer.
           /// Returns keychainTag (or nil if unsupported / no secret to persist).
           /// Used by SubscriptionMergeService via closure injection during pull-to-refresh.
           func persistKeychainSecret(for server: ImportedServer) throws -> String?

           /// Plan 04 — build ServerConfig from ImportedServer + subscriptionID + keychainTag.
           /// Does NOT insert into context — caller (SubscriptionMergeService) handles insert.
           func buildServerConfig(from server: ImportedServer,
                                  subscriptionID: UUID,
                                  keychainTag: String?) -> ServerConfig
       }
       ```
       Verification: после Шага C.0 `grep -c "func persistKeychainSecret" ConfigImporter.swift` должно быть ≥ 2 (1 декларация в protocol + 1 implementation в class) и grep'ом не должно быть других protocols с тем же именем.

    C.1. **Implement методы в concrete ConfigImporter class** — extract из существующего persistSupported (строки 215-292). Принцип:
       - `persistKeychainSecret(for: server)` — берёт case .supported / .unsupported, для supported собирает payload dictionary (см. строки 231-262), вызывает JSONSerialization + KeychainStore.save с tag `"bbtb-config-\(id.uuidString)"`. Returns tag. Для .unsupported — returns nil. Throws ImporterError.keychainSaveFailed на ошибку. ВНИМАНИЕ к UUID: tag должен быть deterministic от server identity / random UUID — НО для нового сервера всегда генерируется new UUID. Решение: persistKeychainSecret внутри генерирует UUID, который потом будет use'нут в buildServerConfig (передаётся через возвращаемый tag — caller извлекает UUID из tag для buildServerConfig). Альтернатива: возвращать tuple (UUID, String) или хранить state. Рекомендация: hostage UUID hidden в tag format `bbtb-config-{uuid}` — buildServerConfig может пропарсить UUID из tag. ЕЩЁ ПРОЩЕ: вызвать `let id = UUID()` внутри persistKeychainSecret, return tag; buildServerConfig получает tag и парсит UUID из него. ИЛИ — реструктурировать API: возвращать struct `KeychainPersistResult { let tag: String; let id: UUID }`. Принять последний вариант:
         ```
         public struct KeychainPersistResult: Sendable {
             public let id: UUID
             public let tag: String
         }
         public func persistKeychainSecret(for server: ImportedServer) throws -> KeychainPersistResult?
         ```
         НО это ломает существующее SubscriptionMergeService.merge signature `persistKeychain: (ImportedServer) throws -> String?`. Согласовать:
         - Изменить signature SubscriptionMergeService.merge на `persistKeychain: (ImportedServer) throws -> KeychainPersistResult?` (Шаг B обновляется соответственно).
         - `buildServerConfig` принимает `id: UUID` дополнительно: `buildServerConfig: (ImportedServer, UUID /* id */, UUID /* subscriptionID */, String? /* keychainTag */) -> ServerConfig`.
         FINAL DECISION: signature
         ```
         public func persistKeychainSecret(for server: ImportedServer) throws -> KeychainPersistResult?
         public func buildServerConfig(from server: ImportedServer,
                                        id: UUID,
                                        subscriptionID: UUID,
                                        keychainTag: String?) -> ServerConfig
         ```
         (Шаг B SubscriptionMergeService.merge signature должен использовать эти signature'ы — Шаг C.0 protocol тоже обновить с `id: UUID` параметром в buildServerConfig.)
       - `buildServerConfig(from:id:subscriptionID:keychainTag:)` — собирает ServerConfig per существующий блок строки 273-289. Принимает UUID id (из KeychainPersistResult), subscriptionID, keychainTag. НЕ insert'ит в context.

    C.2. **Обновить protocol declaration C.0** под FINAL DECISION signature (для compile-time consistency):
       ```
       public protocol ConfigImporting: AnyObject, Sendable {
           // existing members ...
           func persistKeychainSecret(for server: ImportedServer) throws -> KeychainPersistResult?
           func buildServerConfig(from server: ImportedServer,
                                   id: UUID,
                                   subscriptionID: UUID,
                                   keychainTag: String?) -> ServerConfig
       }
       ```

    C.3. **Обновить SubscriptionMergeService.merge signature** (Шаг B пересмотр):
       ```
       public static func merge(
           fetchedSupported: [ImportedServer],
           fetchedUnsupported: [ImportedServer],
           into subscription: Subscription,
           context: ModelContext,
           persistKeychain: (ImportedServer) throws -> KeychainPersistResult?,
           buildServerConfig: (ImportedServer, UUID /* id */, UUID /* subscriptionID */, String? /* keychainTag */) -> ServerConfig
       ) throws
       ```
       (KeychainPersistResult объявлен в VPNCore или ConfigParser — обе options приемлемы; рекомендация — рядом с ConfigImporting в ConfigImporter.swift или в отдельном PublicTypes file in VPNCore. Выбрать так, чтобы оба ConfigParser и AppFeatures могли импортировать. Final: place struct in VPNCore.)
       Логика merge при new identity:
       ```
       guard let persistResult = try persistKeychain(fetched) else {
           // unsupported case — build ServerConfig без keychainTag
           let newID = UUID()
           let newCfg = buildServerConfig(fetched, newID, subscription.id, nil)
           context.insert(newCfg)
           return
       }
       let newCfg = buildServerConfig(fetched, persistResult.id, subscription.id, persistResult.tag)
       context.insert(newCfg)
       ```

    C.4. **ConfigImporter.importFromRawInput — переписать subscription URL branch** (строки 122-133 в существующем коде):
       - удалить `try deleteExistingPool(subscriptionURL: subURL, in: context)` callsite в branch `if let subURL = result.subscriptionURL`.
       - заменить body на:
         a) `let sub = try getOrCreateSubscription(url: subURL, name: result.metadata?.title, in: context)` (Plan 01 helper)
         b) `try SubscriptionMergeService.merge(fetchedSupported: result.supported, fetchedUnsupported: result.unsupported, into: sub, context: context, persistKeychain: { [self] server in try self.persistKeychainSecret(for: server) }, buildServerConfig: { [self] server, id, subID, tag in self.buildServerConfig(from: server, id: id, subscriptionID: subID, keychainTag: tag) })`
         c) `try context.save()` (atomic после всего merge)
       - existing persistSupported wrapper — обновить чтобы внутри вызывал новые helpers persistKeychainSecret + buildServerConfig (de-duplicate). Single-paste branch (result.subscriptionURL == nil) продолжает использовать persistSupported wrapper (поведение неизменно).
       - НЕ удалять `deleteExistingPool` helper полностью — он может использоваться в single-paste branch (поведение неизменно). Grep'нуть; если нигде больше — удалить.
       - Phase 2 ConfigImporter test suite — verify нет regressions: tests от Phase 2 которые проверяли `deleteExistingPool вызвался для subscription URL` могут провалиться — обновить эти tests на новую expectation «merge вызвался» (если такие unit-tests существуют — обновить, не пропускать).

    D. ServerListViewModel — fill Plan 03 stubs:
    4. Add @Published var subscriptionFetchErrors: [UUID: String] = [:] для UI-SPEC §3.4 inline indicator on SubscriptionHeader.
    5. Add inject в ServerListViewModel.init:
       - `private let parser: UniversalImportParser` (или injectable protocol для testability) — default `UniversalImportParser()`. PullToRefreshTests требует injection для mock.
       - `private let importer: ConfigImporting` — injected через init. Используется в fetchAndMerge для closure delegation.
       Init signature становится:
       ```
       public init(modelContainer: ModelContainer,
                   probeService: ServerProbeService,
                   importer: ConfigImporting,
                   parser: UniversalImportParser = UniversalImportParser())
       ```
       Plan 03 init без importer/parser — Plan 04 расширяет init signature. Все Plan 03 callsites (MainScreenViewModel создаёт ServerListViewModel) обновить — Шаг F.
    6. public func pullToRefresh() async — per RESEARCH Example 5 + UI-SPEC §3:
       - `state = .refreshing`; в конце функции — `state = .loaded` если не `.refreshError`. defer-free (явный if-блок).
       - subscriptionFetchErrors = [:]; refreshError = nil
       - Phase 1 (sequential D-13): получить context = ModelContext(modelContainer); fetch all Subscription rows; для каждого sub — try-catch внутри loop. ИСПОЛЬЗОВАТЬ только structured concurrency (RESEARCH Pitfall 5): for sub in subscriptions { do { try await fetchAndMerge(sub: sub, in: context) } catch { subscriptionFetchErrors[sub.id] = error.localizedDescription } }. После loop — `try? context.save()`. Если subscriptionFetchErrors.count == subscriptions.count → refreshError = L10n.serverListRefreshErrorMessage + state = .refreshError(...).
       - Phase 2: await pingAllServers() (Plan 03 method) — независимо от fetch outcome (даже если все fetch failed, существующие servers перепиниговываются).
    7. private func fetchAndMerge(sub: Subscription, in context: ModelContext) async throws:
       - `guard let url = URL(string: sub.url) else { throw ImporterError.malformedURI(...) }`
       - `let fetched = try await SubscriptionURLFetcher.fetch(url: url, session: .shared)` — Phase 2 carry-forward; throws на network failures.
       - parse через parser: `let result = try await parser.import(rawInput: fetched.body, source: .pasteboard)` (или dedicated source — Phase 4 может добавить .subscriptionRefresh).
       - **CRITICAL — вызывает через ConfigImporting protocol reference** (NOT cast к concrete ConfigImporter):
         ```
         try SubscriptionMergeService.merge(
             fetchedSupported: result.supported,
             fetchedUnsupported: result.unsupported,
             into: sub,
             context: context,
             persistKeychain: { [importer] server in try importer.persistKeychainSecret(for: server) },
             buildServerConfig: { [importer] server, id, subID, tag in
                 importer.buildServerConfig(from: server, id: id, subscriptionID: subID, keychainTag: tag)
             }
         )
         ```
         Это compile-time гарантия что protocol expose'ит оба метода (Шаг C.0/C.2 обеспечил).
       - НЕ throw из этой функции если merge ok — throw только при fetch/parse failure. Caller-loop в pullToRefresh catches.

    8. public func silentForegroundRefresh() async — copy pullToRefresh logic, но НЕ менять state (start .loaded → end .loaded). Логи через OSLog. Ошибки fetch не отображать на UI (subscriptionFetchErrors не set, refreshError не set) — silent failure.

    9. public func deleteServer(id: UUID) async — per RESEARCH Pitfall 10, UI-SPEC §6.2:
       - context = ModelContext(modelContainer); FetchDescriptor<ServerConfig>(predicate: #Predicate { $0.id == id }); fetch first.
       - if let tag = srv.keychainTag, !tag.isEmpty { try? KeychainStore.delete(tag: tag) }
       - context.delete(srv); try? context.save()
       - if coordinator?.selectedServerID == id { coordinator?.applySelection(nil) }
       - await loadFromStore() (Plan 03 method — refresh sections).

    10. public func confirmDeleteSubscription(_ sub: Subscription) async — per UI-SPEC §6.1:
        - context = ModelContext(modelContainer); FetchDescriptor<ServerConfig>(predicate: #Predicate { $0.subscriptionID == sub.id }); fetch
        - для каждого server in linked: if tag = server.keychainTag, !tag.isEmpty { try? KeychainStore.delete(tag: tag) }; context.delete(server)
        - context.delete(sub); try? context.save()
        - if let selected = coordinator?.selectedServerID, linked.contains(where: { $0.id == selected }) { coordinator?.applySelection(nil) }
        - pendingDeleteSubscription = nil
        - await loadFromStore()

    E. MainScreenView scenePhase observer:
    11. В MainScreenView.swift добавить `@Environment(\.scenePhase) private var scenePhase` (если ещё нет). После .sheet modifier на root body добавить `.onChange(of: scenePhase) { _, newPhase in if newPhase == .active, let vm = viewModel.serverListViewModel { Task { @MainActor in await vm.silentForegroundRefresh() } } }`.

    F. Wire ServerListSheet → pullToRefresh + confirmationDialog + ServerListViewModel construction update:
    12. В ServerListSheet.swift заменить `.refreshable { /* Plan 04 */ }` на `.refreshable { await viewModel.pullToRefresh() }`.
    13. Добавить .confirmationDialog после .refreshable:
        `.confirmationDialog(L10n.serverListDeleteSubscriptionConfirmMessage(viewModel.pendingDeleteSubscription?.name ?? "", viewModel.pendingDeleteSubscriptionServerCount), isPresented: Binding(get: { viewModel.pendingDeleteSubscription != nil }, set: { if !$0 { viewModel.pendingDeleteSubscription = nil } }), titleVisibility: .visible) { Button(L10n.actionDelete, role: .destructive) { if let sub = viewModel.pendingDeleteSubscription { Task { await viewModel.confirmDeleteSubscription(sub) } } } Button(L10n.actionCancel, role: .cancel) { viewModel.pendingDeleteSubscription = nil } }`
        Computed `var pendingDeleteSubscriptionServerCount: Int` на ViewModel.
    14. ServerListSheet передаёт viewModel.subscriptionFetchErrors[sub.id] в SubscriptionHeader через `fetchError:` argument.
    15. MainScreenViewModel (Plan 03 wiring) — обновить construction ServerListViewModel чтобы передать `importer`: `ServerListViewModel(modelContainer: ..., probeService: ..., importer: importer)` (где `importer` — уже DI-injected `ConfigImporting` в MainScreenViewModel.init из Phase 2 baseline).

    G. SubscriptionHeader inline fetch-error:
    16. SubscriptionHeader init signature расширить: `public init(subscription: Subscription, fetchError: String? = nil, onDelete: @escaping () -> Void)`. Если fetchError != nil — рядом с lastFetched timestamp показывать `Image(systemName: "exclamationmark.triangle").foregroundStyle(.orange).help(fetchError)` (per UI-SPEC §3.4).

    H. Run tests + commit:
    17. swift test --package-path BBTB/Packages/AppFeatures --filter "MergeStrategyTests|PullToRefreshTests|CascadeDeleteTests" — все GREEN.
    18. swift test --package-path BBTB/Packages/AppFeatures — no regressions.
    19. swift test --package-path BBTB/Packages/ConfigParser — no regressions.
    20. swift test --package-path BBTB/Packages/VPNCore — no regressions.
    21. Commit: `feat(03-04): SubscriptionMergeService + ConfigImporting protocol expansion + pull-to-refresh + cascade delete + foreground refresh`.

    HARD constraints:
    - Pull-to-refresh использует ТОЛЬКО structured concurrency — НЕ unstructured Task { ... }.
    - merge-by-identity сохраняет lastLatencyMs/lastPingedAt/failedProbeCount — НЕ переписывать на nil/0 при update.
    - cascade delete clear coordinator.selectedServerID если был среди удалённых.
    - foreground refresh — silent (state не меняется на .refreshing).
    - НЕТ scope-reduction language — confirmDeleteSubscription выполняется fully (Subscription + ServerConfigs + Keychain + selection reset).
    - ServerListViewModel.fetchAndMerge вызывает persistKeychainSecret / buildServerConfig ТОЛЬКО через ConfigImporting protocol reference — НЕ через cast `(importer as! ConfigImporter)`. Если такой cast обнаружится — это violation; compile должен проходить без cast'ов.
  </action>
  <verify>
    <automated>cd /Users/vergevsky/ClaudeProjects/VPN && swift test --package-path BBTB/Packages/AppFeatures --filter "MergeStrategyTests|PullToRefreshTests|CascadeDeleteTests" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File BBTB/Packages/ConfigParser/Sources/ConfigParser/SubscriptionMergeService.swift exists with `public enum SubscriptionMergeService`
    - SubscriptionMergeService.merge signature includes fetchedSupported, fetchedUnsupported, into subscription, context, persistKeychain, buildServerConfig parameters
    - ServerConfig.swift contains computed `var identity: String` via extension
    - **BLOCKER FIX — ConfigImporting protocol expansion verified**:
      - `grep -E "func persistKeychainSecret\\(for" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift | wc -l` returns ≥ 2 (1 в protocol declaration + 1 в class implementation)
      - `grep -E "func buildServerConfig\\(from" BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift | wc -l` returns ≥ 2
      - Protocol block (between `public protocol ConfigImporting` и closing `}`) содержит `persistKeychainSecret` AND `buildServerConfig` — verify через `awk '/public protocol ConfigImporting/,/^}/' ConfigImporter.swift | grep -E "persistKeychainSecret|buildServerConfig" | wc -l` returns ≥ 2
      - KeychainPersistResult struct declared (в ConfigImporter.swift или VPNCore) — `grep -rE "struct KeychainPersistResult" BBTB/Packages/` returns ≥ 1
    - ConfigImporter.swift no longer calls `deleteExistingPool(subscriptionURL:` inside subscription URL branch (`grep -v '^//' BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift | grep -c "deleteExistingPool(subscriptionURL"` returns 0)
    - ConfigImporter.swift calls `SubscriptionMergeService.merge` in subscription URL branch
    - ServerListViewModel.swift contains public func `pullToRefresh()`, `silentForegroundRefresh()`, `deleteServer(id:)`, `confirmDeleteSubscription(_:)` with non-stub bodies (length >= 8 lines each — `awk` body check)
    - ServerListViewModel.swift contains `@Published var subscriptionFetchErrors`
    - ServerListViewModel.swift calls `importer.persistKeychainSecret` AND `importer.buildServerConfig` — `grep -E "importer\\.persistKeychainSecret|importer\\.buildServerConfig" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListViewModel.swift | wc -l` returns ≥ 2
    - ServerListViewModel.swift НЕ содержит cast'ов `as! ConfigImporter` или `as? ConfigImporter` — `grep -E "as[!?] ConfigImporter" BBTB/Packages/AppFeatures/Sources/ServerListFeature/ServerListViewModel.swift | wc -l` returns 0
    - ServerListSheet.swift contains `.refreshable { await viewModel.pullToRefresh() }` (NOT `/* Plan 04 */` comment)
    - ServerListSheet.swift contains `.confirmationDialog`
    - SubscriptionHeader.swift init signature includes `fetchError: String?`
    - MainScreenView.swift contains `.onChange(of: scenePhase)` AND `silentForegroundRefresh`
    - Command `swift test --package-path BBTB/Packages/AppFeatures --filter MergeStrategyTests` exits 0
    - Command `swift test --package-path BBTB/Packages/AppFeatures --filter PullToRefreshTests` exits 0
    - Command `swift test --package-path BBTB/Packages/AppFeatures --filter CascadeDeleteTests` exits 0
    - Command `swift test --package-path BBTB/Packages/AppFeatures` exits 0 (no Phase 2/3 regressions)
    - Command `swift test --package-path BBTB/Packages/ConfigParser` exits 0
    - Git log shows commit "feat(03-04): SubscriptionMergeService + ConfigImporting protocol expansion + pull-to-refresh + cascade delete + foreground refresh"
  </acceptance_criteria>
  <done>Все GREEN; ConfigImporting protocol expose'ит persistKeychainSecret + buildServerConfig (verified grep); ServerListViewModel вызывает их через protocol reference без cast'ов; merge сохраняет latency; pull-to-refresh sequential 2-phase; cascade delete с Keychain cleanup и selection reset; foreground silent refresh wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Subscription URL response → SubscriptionMergeService → SwiftData | External server bytes пересекают в локальное хранилище; merge по identity сохраняет существующие latency metrics — может позволить attacker отслеживать что user уже использовал тех же серверов раньше (на сервере известно что user уже подключался через identity matching) |
| Keychain → cascade delete | При удалении подписки Keychain entries cleanup'ятся; если cleanup failed, остаются orphan secrets |
| coordinator weak reference | Если MainScreenViewModel deinit'ится во время pendingDeleteSubscription dialog — confirmation handler пытается update nil coordinator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | T (Tampering) | merge logic: «existing identity» совпадение overwrite'ит name на server-controlled fetched.displayName без sanitization — поле может содержать unicode RTL override или control chars | mitigate | SubscriptionMergeService при `row.name = fetched.displayName` применять ту же sanitization что в Plan 01 T-03-01: clamp ≤100 chars + strip \\n\\r\\t. Implement как helper `sanitize(displayName:)` reused. |
| T-03-18 | I (Information Disclosure) | Keychain cleanup failure при cascade delete не отображает error — orphan secrets остаются на устройстве после удаления подписки | accept | KeychainStore.delete уже идемпотент (Phase 2 Keychain wrapper). Если specific tag не найден — silent ok. Acceptable risk; orphan secrets изолированы App-bundle scope. Documenting trade-off в SECURITY.md. |
| T-03-19 | D (DoS) | Pull-to-refresh DoS: пользователь спамит pulls → много concurrent subscription fetches → backend overwhelmed | accept | .refreshable system handles debounce (system spinner blocks subsequent pulls until current completes) — RESEARCH Security Domain. Acceptable risk. |
| T-03-20 | T (Tampering — data integrity) | Race condition: silent foreground refresh runs во время pull-to-refresh user-инициированного → две параллельные refresh sequence'и могут конфликтовать на context.save() | mitigate | ServerListViewModel — @MainActor; все state mutations serialized. fetchAndMerge sequential per subscription; context.save() в конце. Если две refresh sequences пересекутся — SwiftData ModelContext не Sendable, должен быть один context per call (создаётся локально в каждой). Документировано в Open Question 3 acceptable. |
| T-03-21 | I (Information Disclosure) | Pull-to-refresh идёт по системному интернету (не VPN tunnel) для fetch subscription URL → reveals subscription URL host к ISP/DNS resolver | accept | Phase 2 baseline: SubscriptionURLFetcher HTTPS-only enforced; URL host visible через TLS SNI. Pre-tunnel необходимо для fetch — нельзя получить актуальный pool через downed tunnel. Plan 07 cert pinning + alt-routes mitigation. |
| T-03-22 | T (Tampering — invariant) | confirmDeleteSubscription clears selection — если несколько servers в подписке + selected был среди них, coordinator.applySelection(nil) переключает на Auto. User может не понять что произошло. | accept | UX-SPEC §6.1 explicitly: «если selectedServerID был одним из удалённых → fallback к Auto». Plan 05 reconnect-on-active с новой Auto-select score добавит explicit user feedback. Документировано в UI-SPEC. |
| T-03-23 | E (Elevation of Privilege — API surface) | Расширение публичного ConfigImporting protocol persistKeychainSecret + buildServerConfig увеличивает attack surface — любой conformer может теперь exfiltrate Keychain payload или mint arbitrary ServerConfig | mitigate | Methods callable только из ServerListFeature (текущий single consumer); Sendable-protocol требует thread-safety; KeychainStore.save уже использует kSecAttrAccessibleWhenUnlockedThisDeviceOnly (Phase 1 hardening). Никаких новых secrets через эти helpers не пересекает trust boundary — это refactor existing logic, не expand'ит её. |

**Carry-forward:**
- Plan 01 T-03-01 (Subscription.name sanitization) extended в T-03-17 для merge updates.
- Plan 02 T-03-09 (race condition probe-during-delete) — этот план реализует cascade-trigger reverse (delete cancels probes через structured concurrency Plan 03 stream cancel).
</threat_model>

<verification>
**Per-task:**
- Task 1 RED→Task 2 GREEN: все 3 test файла pass

**Plan-level:**
- swift test --package-path BBTB/Packages/AppFeatures exit 0 (no regressions Phase 2/3)
- Pull-to-refresh structured concurrency verified: grep -E "Task\\.detached|Task\\{" ServerListViewModel.swift в pullToRefresh/silentForegroundRefresh body → 0 unstructured tasks (allowing inner `Task {}` только в @MainActor context for SwiftUI events)
- Merge preserves lastLatencyMs verified в test
- Cascade delete with Keychain cleanup + selection reset verified
- SubscriptionHeader inline fetch-error UI compiles + macOS-compatible
- **ConfigImporting protocol expansion verified** (BLOCKER fix from planner-checker): protocol декларация содержит persistKeychainSecret + buildServerConfig (grep), ServerListViewModel вызывает их через protocol reference без cast'ов (grep returns 0 для `as! ConfigImporter`)
</verification>

<success_criteria>
1. ✅ SubscriptionMergeService.merge implementation per D-14: by-identity dedup, preserve latency, mark disappeared
2. ✅ ConfigImporting protocol expanded — `persistKeychainSecret(for:) -> KeychainPersistResult?` и `buildServerConfig(from:id:subscriptionID:keychainTag:) -> ServerConfig` доступны через protocol reference (BLOCKER fix)
3. ✅ ConfigImporter subscription URL branch заменён на merge (deleteExistingPool removed); single-paste branch неизменен
4. ✅ ServerListViewModel.pullToRefresh — 2-phase sequential (fetch all → merge → ping all), structured concurrency
5. ✅ ServerListViewModel.fetchAndMerge вызывает persistKeychainSecret / buildServerConfig через `importer` protocol reference (compile-time verified без cast'ов)
6. ✅ silentForegroundRefresh — silent (state не меняется), wired через scenePhase
7. ✅ deleteServer — Keychain cleanup + ServerConfig delete + selection reset if needed
8. ✅ confirmDeleteSubscription — cascade delete (Subscription + linked ServerConfigs + Keychain) + selection reset
9. ✅ ServerListSheet wires .refreshable + .confirmationDialog
10. ✅ SubscriptionHeader inline fetch-error indicator (UI-SPEC §3.4)
11. ✅ ServerConfig.identity computed property для D-14 dedup
12. ✅ Все Phase 1/2/3 tests green; нет regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-server-management/03-04-SUMMARY.md`. Особое внимание:
- ConfigImporting protocol expansion rationale + KeychainPersistResult struct decision (carried struct between persistKeychain closure return и buildServerConfig invocation для consistency UUID).
- Open Question 2 resolution (server-identity при изменённом password — merge сохраняет existing keychainTag или upsert? Document decision based on implementation)
- Open Question 4 — pre-connect timeout — НЕ в Plan 04 scope, отложено в Plan 05
- Carry-forward: scenePhase observer работает в MainScreenView (а не в ServerListSheet — потому что sheet может быть закрыт), но он отвечает за foreground refresh даже когда sheet закрыт
- Carry-forward: SubscriptionMergeService.merge sanitize'ит name (T-03-17 mitigation)
- T-03-23 — API surface accept rationale
</output>
