---
phase: 03-server-management
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift
  - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift
  - BBTB/Packages/Localization/Sources/Localization/L10n.swift
  - BBTB/Packages/Localization/Sources/Localization/Resources/Localizable.xcstrings
  - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderSingleOutboundTests.swift
  - BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/AutoSelectIntegrationTests.swift
autonomous: false
requirements: [SRV-01]
must_haves:
  truths:
    - "Tap Connect при selectedServerID == nil → pre-connect ping всех supported → выбор min score → connect через 1-outbound pool"
    - "Tap Connect при selectedServerID != nil → connect через 1-outbound pool без urltest (только этот сервер)"
    - "Если все supported серверы 3/3 timeout → state = .error с сообщением 'Все серверы недоступны' (Pitfall 8)"
    - "Smena selectedServerID при активном tunnel → reconnect через stopVPNTunnel + provisionTunnelProfile + startVPNTunnel (без алерта, D-09)"
    - "selectedServerID персистится в UserDefaults и восстанавливается на следующий launch"
    - "PoolBuilder.buildSingleOutboundJSON принимает один AnyParsedConfig и строит pool degenerate-case (без urltest, route.final = единственный outbound)"
  artifacts:
    - path: "BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift"
      provides: "buildSingleOutboundJSON для 1-outbound pool (degenerate-case)"
      contains: "buildSingleOutboundJSON"
    - path: "BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift"
      provides: "pre-connect auto-select + reconnect-on-selection-change + UserDefaults persist"
      contains: "performPreConnectAutoSelect"
    - path: "BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift"
      provides: "provisionTunnelProfile(for: selectedID:) поддержка single-outbound path"
      contains: "provisionTunnelProfile"
  key_links:
    - from: "MainScreenViewModel.performToggle"
      to: "ServerProbeService.probeAll + ServerScore.autoSelect + ConfigImporter.provisionTunnelProfile"
      via: "pre-connect ping → min-score → 1-outbound pool"
      pattern: "performPreConnectAutoSelect"
    - from: "MainScreenViewModel.applySelection"
      to: "TunnelController.disconnect + provisionTunnelProfile + TunnelController.connect"
      via: "reconnect when .connected state (D-09)"
      pattern: "reconnectIfActive"
---

<objective>
Пятый и финальный вертикальный слайс — pre-connect auto-select + reconnect-on-selection (D-04, D-09, SRV-01 finish). После выполнения: tap Connect runs ping всех supported серверов (≈1.5 сек) → выбирает min score → строит 1-outbound pool → connects. Если manual server selected — connects через same 1-outbound path без urltest. Смена сервера во время активного туннеля → автоматический reconnect без алерта. UserDefaults сохраняет selectedServerID между запусками. Финальный checkpoint:human-verify подтверждает full UAT на iPhone + macOS.

Phase Goal (user story):

**As a** пользователь BBTB, **I want to** видеть свои серверы сгруппированными по подпискам, переключаться между ними и обновлять список одним жестом, **so that** я мог подключаться к лучшему доступному серверу без ручной настройки.

Покрытие в этом плане: «подключаться к лучшему доступному серверу без ручной настройки» — auto-select с score-based winner; «переключаться между ними» — reconnect-on-selection без алерта. **Phase 3 goal полностью покрыт после Plan 05.**

Purpose: Без этого плана previous 4 plans — UI shell с probe и persistence, но connect flow остаётся Phase 2 (urltest internal failover, без user choice). Plan 05 связывает selectedServerID с actual NETunnelProviderManager runtime — превращает Phase 3 в working product.

Output:
- PoolBuilder.buildSingleOutboundJSON (NEW public method, reuses existing degenerate path)
- MainScreenViewModel — performPreConnectAutoSelect + reconnect-on-selection + UserDefaults persist
- ConfigImporter.provisionTunnelProfile(for:) — поддержка selectedID для 1-outbound build
- 2 test файла (PoolBuilderSingleOutboundTests, AutoSelectIntegrationTests)
- L10n.serverListNoReachableServers (новый ключ)
- checkpoint:human-verify task для UAT
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-server-management/03-CONTEXT.md
@.planning/phases/03-server-management/03-RESEARCH.md
@.planning/phases/03-server-management/03-PATTERNS.md
@.planning/phases/03-server-management/03-UI-SPEC.md
@BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift
@BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift
@BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift
@BBTB/Packages/AppFeatures/Sources/MainScreenFeature/TunnelController.swift
@BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ReconnectBanner.swift

<interfaces>
Plan 02 contracts:
- ServerProbeService.probeAll(_: [(UUID, String, Int)]) -> AsyncStream<(UUID, ProbeAggregate)>
- ServerScore.autoSelect(_: [(UUID, Double?)]) -> UUID?

Plan 03 contracts:
- MainScreenViewModel.selectedServerID: UUID? @Published (Plan 03 in-memory; Plan 05 persist в UserDefaults)
- MainScreenViewModel conforms ServerSelectionCoordinating: applySelection(_:) → пишет selectedServerID

PoolBuilder Phase 2 baseline (RESEARCH Example 6 строки 1028-1034):
- Existing public static func buildSingBoxJSON(from parsed: [AnyParsedConfig]) throws -> String — degenerate 1-outbound case уже корректно работает: tags.count == 1 → finalTag = tags[0] (без urltest selector).
- Phase 3 публикует thin wrapper buildSingleOutboundJSON(from parsed: AnyParsedConfig) → buildSingBoxJSON(from: [parsed]).

Phase 2 reconnect pattern (ReconnectBanner.swift, TunnelController.swift):
- TunnelController.disconnect() throws → останавливает tunnel
- ConfigImporter.provisionTunnelProfile или эквивалент — обновляет NETunnelProviderManager.providerConfiguration["configJSON"] + save/load
- TunnelController.connect() throws → start

Phase 1 carry-forward (sing-box):
- One NETunnelProviderManager, providerBundleIdentifier "app.bbtb.client.{ios,macos}.tunnel"
- Phase 2 urltest remains в runtime для live failover; Plan 05 bypassит urltest на pre-connect выборе через 1-outbound pool — sing-box runtime urltest НЕ нужен потому что в pool только 1 outbound.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing tests — PoolBuilder.buildSingleOutboundJSON + MainScreenViewModel auto-select integration</name>
  <files>BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderSingleOutboundTests.swift, BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/AutoSelectIntegrationTests.swift</files>
  <read_first>
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift (existing buildSingBoxJSON — degenerate case строки 54-56)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/ (existing tests за образец)
    - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift (Phase 2 performToggleImpl + ConnectionState)
    - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConnectionState.swift (state enum cases — нужны для assertions)
    - .planning/phases/03-server-management/03-CONTEXT.md (D-04, D-09)
    - .planning/phases/03-server-management/03-RESEARCH.md «Code Examples — Example 6», «Common Pitfalls — Pitfall 8»
    - .planning/phases/03-server-management/03-PATTERNS.md «PoolBuilder.swift — MODIFIED», «MainScreenViewModel.swift — MODIFIED»
    - .planning/phases/03-server-management/03-UI-SPEC.md §3.1 «Auto-select pre-connect» + §14 «Удаление активного сервера»
  </read_first>
  <behavior>
    PoolBuilderSingleOutboundTests:
    - test_buildSingleOutboundJSON_returns_pool_with_no_urltest — pass один AnyParsedConfig (e.g. parsed Trojan-WS); decode resulting JSON; assert outbounds.count == 1 (или 2 если direct included, but tags не должны включать "urltest-out"), route.final == outbound tag (НЕ "urltest-out")
    - test_buildSingleOutboundJSON_equals_buildSingBoxJSON_with_one_element_array — equivalence: buildSingleOutboundJSON(p) == buildSingBoxJSON([p]) (string equality, или structural decode-and-compare)
    - test_buildSingleOutboundJSON_preserves_protocol_specific_settings — для VLESS-Reality config (с publicKey, shortId) или Trojan-WS (с path, host) — assert все protocol-specific fields preserved в outputs JSON

    AutoSelectIntegrationTests:
    - test_pre_connect_auto_select_picks_min_score_server — setup: insert 3 supported ServerConfig (с разными host:port); mock ServerProbeService returning controlled ProbeAggregate: A→100ms/0%, B→50ms/0%, C→200ms/0%; mock TunnelController.connect tracks providerConfiguration["configJSON"]; mock ConfigImporter.provisionTunnelProfile(for: id?) captures parameter; call vm.performToggle() (idle→connecting); assert: provisionTunnelProfile called с id == B (min score)
    - test_pre_connect_auto_select_skips_unreachable — A→100ms/0%, B→3/3 timeout (score nil), C→150ms/0%; performToggle → provisionTunnelProfile called с A.id (B excluded)
    - test_pre_connect_all_unreachable_returns_error — все три server 3/3 timeout; performToggle → state == .error(message: содержит «недоступн» или эквивалент L10n.serverListNoReachableServers); НЕ connect (Pitfall 8 mitigation)
    - test_manual_selected_server_skips_auto_select — vm.selectedServerID = serverX.id; performToggle → provisionTunnelProfile called с serverX.id (НЕ runs probeAll on all supported)
    - test_selection_change_during_active_tunnel_reconnects — vm.state = .connected(since:); applySelection(otherID); assert: TunnelController.disconnect called → provisionTunnelProfile(for: otherID) called → TunnelController.connect called; state переходит .connected → .connecting → .connected without alert (D-09)
    - test_selectedServerID_persists_via_user_defaults — set vm.selectedServerID = X; create new MainScreenViewModel instance (with same UserDefaults); selectedServerID == X на новом instance.
    - test_deleted_selected_server_falls_back_to_auto — set vm.selectedServerID = X; outside delete X from store; vm should observe и reset selectedServerID = nil (RESEARCH Pitfall 10). Realization: либо через NotificationCenter ModelDidChange observer, либо при applySelection валидация existsInStore — выбрать через test contract: applySelection(_) ignores stale IDs OR explicit `await reconcileSelectionWithStore()`.
  </behavior>
  <action>
    1. Создать PoolBuilderSingleOutboundTests.swift в BBTB/Packages/ConfigParser/Tests/ConfigParserTests/. Imports: Foundation, XCTest, @testable ConfigParser. Tests декларированы в behavior. Подавать существующие parsed configs из ConfigParserTests/fixtures (есть Phase 2 trojan/VLESS-Reality samples). JSON decode через JSONSerialization.jsonObject(with:options:) → проверка fields.
    2. Создать AutoSelectIntegrationTests.swift в BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/. Imports @testable MainScreenFeature, @testable VPNCore. Mock infrastructure:
       - `MockProbeService: ServerProbeService` — override probeAll (или ввести injectable protocol-style). Поскольку Plan 02 ServerProbeService — concrete class — для testability ввести protocol-style: добавить `public protocol ProbeServicing { func probeAll(_: [(UUID, String, Int)]) -> AsyncStream<(UUID, ProbeAggregate)> }`. ServerProbeService conforms; mock также conforms. MainScreenViewModel получает ProbeServicing вместо concrete. ОЛИБО: оставить concrete ServerProbeService и тесты использовать live in-process listener — но D-04 «3 probes × 500ms» делает test slow. **Решение: ввести protocol ProbeServicing в Plan 05 Task 2 production-код**.
       - `MockTunnelController: TunnelControlling` — captures connect/disconnect calls (Phase 2 protocol existing).
       - `MockConfigImporter: ConfigImporting` — captures provisionTunnelProfile(for:) calls.
       - UserDefaults isolation: используется `UserDefaults(suiteName: "test-suite-\(UUID())")` в каждом тесте, MainScreenViewModel расширен с optional `userDefaults: UserDefaults` injection (default `.standard`).
    3. Запустить tests; assert RED (compile-fail на missing buildSingleOutboundJSON, performPreConnectAutoSelect, ProbeServicing).
    4. Commit: `test(03-05): add failing tests for PoolBuilder.buildSingleOutboundJSON + auto-select integration`.
  </action>
  <verify>
    <automated>cd /Users/vergevsky/ClaudeProjects/VPN && swift test --package-path BBTB/Packages/ConfigParser --filter PoolBuilderSingleOutboundTests 2>&1 | grep -E "error:|FAILED|Cannot find" | head -5; swift test --package-path BBTB/Packages/AppFeatures --filter AutoSelectIntegrationTests 2>&1 | grep -E "error:|FAILED|Cannot find" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderSingleOutboundTests.swift exists with test_buildSingleOutboundJSON_returns_pool_with_no_urltest, test_buildSingleOutboundJSON_equals_buildSingBoxJSON_with_one_element_array, test_buildSingleOutboundJSON_preserves_protocol_specific_settings
    - File BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/AutoSelectIntegrationTests.swift exists with test_pre_connect_auto_select_picks_min_score_server, test_pre_connect_all_unreachable_returns_error, test_manual_selected_server_skips_auto_select, test_selection_change_during_active_tunnel_reconnects, test_selectedServerID_persists_via_user_defaults, test_deleted_selected_server_falls_back_to_auto
    - Command `swift test --package-path BBTB/Packages/ConfigParser --filter PoolBuilderSingleOutboundTests` exits non-zero (RED)
    - Command `swift test --package-path BBTB/Packages/AppFeatures --filter AutoSelectIntegrationTests` exits non-zero (RED)
    - Git log shows commit "test(03-05): add failing tests for PoolBuilder.buildSingleOutboundJSON + auto-select integration"
  </acceptance_criteria>
  <done>2 test файла существуют, все RED, commit на RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PoolBuilder.buildSingleOutboundJSON + MainScreenViewModel auto-select + reconnect + UserDefaults persist + Pitfall-8/Pitfall-10 fallbacks</name>
  <files>BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift, BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift, BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift, BBTB/Packages/Localization/Sources/Localization/L10n.swift, BBTB/Packages/Localization/Sources/Localization/Resources/Localizable.xcstrings</files>
  <read_first>
    - BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift (existing buildSingBoxJSON — degenerate-case path строки 54-56)
    - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/MainScreenViewModel.swift (performToggleImpl pattern Phase 2)
    - BBTB/Packages/AppFeatures/Sources/MainScreenFeature/ConfigImporter.swift (existing provision flow — Phase 2 предусматривал один outbound из массива; Plan 05 добавит signature для selectedID)
    - BBTB/Packages/AppFeatures/Tests/MainScreenFeatureTests/AutoSelectIntegrationTests.swift (RED-тесты из Task 1)
    - BBTB/Packages/ConfigParser/Tests/ConfigParserTests/PoolBuilderSingleOutboundTests.swift
    - .planning/phases/03-server-management/03-RESEARCH.md «Code Examples — Example 6», «Common Pitfalls — Pitfall 8, Pitfall 10»
    - .planning/phases/03-server-management/03-UI-SPEC.md §3.1, §14
  </read_first>
  <action>
    A. PoolBuilder.buildSingleOutboundJSON:
    1. В BBTB/Packages/ConfigParser/Sources/ConfigParser/PoolBuilder.swift добавить:
       public extension PoolBuilder {
           static func buildSingleOutboundJSON(from parsed: AnyParsedConfig) throws -> String {
               return try buildSingBoxJSON(from: [parsed])
           }
       }
       Это thin wrapper — reuse degenerate-case existing logic (1 outbound, no urltest). RESEARCH Example 6 строки 1031-1034.
    2. Run swift test --package-path BBTB/Packages/ConfigParser --filter PoolBuilderSingleOutboundTests — GREEN.

    B. ProbeServicing protocol (testability):
    3. В BBTB/Packages/VPNCore/Sources/VPNCore/ServerProbeService.swift добавить:
       public protocol ProbeServicing: Sendable {
           func probeAll(_ servers: [(id: UUID, host: String, port: Int)]) -> AsyncStream<(UUID, ProbeAggregate)>
       }
       extension ServerProbeService: ProbeServicing {}
       
       Существующие callsites (Plan 03 ServerListViewModel) — MainScreenViewModel получают type-annotation расслабленную (ProbeServicing вместо concrete ServerProbeService) если необходимо, иначе остаются с concrete (тоже OK — concrete conforms).

    C. ConfigImporter.provisionTunnelProfile(for:) расширение:
    4. В ConfigImporter.swift расширить (или добавить) `public func provisionTunnelProfile(for selectedID: UUID? = nil) async throws`:
       - context = ModelContext(modelContainer)
       - FetchDescriptor<ServerConfig>(predicate: #Predicate { $0.isSupported == true }); fetch supported.
       - if supported.isEmpty → throw ImporterError.noSupportedServers (новый case если нет — добавить enum case).
       - parsed: [AnyParsedConfig]
       - if let id = selectedID, let one = supported.first(where: { $0.id == id }) {
             parsed = [try reparseFromKeychain(one)]
         } else if let id = selectedID {
             // selected ID gone (Pitfall 10 race condition) — fallback to full pool
             parsed = try supported.map { try reparseFromKeychain($0) }
         } else {
             parsed = try supported.map { try reparseFromKeychain($0) }
         }
       - if parsed.count == 1 → json = try PoolBuilder.buildSingleOutboundJSON(from: parsed[0])
       - else → json = try PoolBuilder.buildSingBoxJSON(from: parsed) — Phase 2 urltest pool.
       - existing Phase 2 NETunnelProviderManager save/load flow: загружать manager, обновить providerConfiguration["configJSON"] = json, save, load снова.
       — Существующий Phase 2 ConfigImporter имеет похожий метод; этот таск расширяет signature optional selectedID параметром в конец списка (back-compat).

    D. MainScreenViewModel implementation:
    5. В MainScreenViewModel.swift:
       - Заменить @Published var selectedServerID: UUID? Plan 03 in-memory на UserDefaults-mirrored: `@Published public var selectedServerID: UUID? { didSet { saveSelectedServerID() } }`. Реализовать:
         private let userDefaults: UserDefaults
         private static let selectedServerIDKey = "app.bbtb.selectedServerID"
         private func saveSelectedServerID() {
             if let id = selectedServerID { userDefaults.set(id.uuidString, forKey: Self.selectedServerIDKey) }
             else { userDefaults.removeObject(forKey: Self.selectedServerIDKey) }
         }
         В init: восстановить из UserDefaults — `if let stored = userDefaults.string(forKey: Self.selectedServerIDKey), let uuid = UUID(uuidString: stored) { selectedServerID = uuid }`.
         Extend init: `public init(importer: ConfigImporting, tunnel: TunnelControlling, modelContainer: ModelContainer? = nil, probeService: ProbeServicing = ServerProbeService(), userDefaults: UserDefaults = .standard)`. Back-compat Phase 2 callsites через default params.
       - Inject probeService через init.
       - performToggleImpl branching:
         case .idle, .error:
             state = .connecting
             do {
                 if let id = selectedServerID {
                     // Manual selection — connect directly (1-outbound pool)
                     try await importer.provisionTunnelProfile(for: id)
                 } else {
                     // Auto — pre-connect probe + winner
                     let winnerID = try await performPreConnectAutoSelect()
                     try await importer.provisionTunnelProfile(for: winnerID)
                 }
                 let since = try await tunnel.connect()
                 state = .connected(since: since)
             } catch let err as MainScreenError where err == .noReachableServers {
                 state = .error(message: L10n.serverListNoReachableServers)
             } catch {
                 state = .error(message: error.localizedDescription)
             }
       - performPreConnectAutoSelect() async throws -> UUID:
         a) fetch all supported ServerConfig via ModelContext(modelContainer).
         b) если supported.isEmpty → throw MainScreenError.noSupportedServers (новый enum case).
         c) payload tuple [(id, host, port)]
         d) пройти AsyncStream probeService.probeAll(payload), накопить results в `var aggregates: [UUID: ProbeAggregate] = [:]`. Использовать `for await (id, agg) in stream { aggregates[id] = agg }`.
         e) `let candidates = supported.map { (id: $0.id, score: aggregates[$0.id]?.score) }`. Если все nil → throw MainScreenError.noReachableServers (Pitfall 8). Если supported.count == 1 → допустимо вернуть единственный id даже без reachable (degenerate; UI-SPEC accept).
         f) `guard let winner = ServerScore.autoSelect(candidates) else { throw MainScreenError.noReachableServers }`; return winner.

    6. ServerSelectionCoordinating.applySelection — обновить (Plan 03 stub):
       public func applySelection(_ id: UUID?) {
           let previousID = selectedServerID
           selectedServerID = id   // didSet UserDefaults persist
           guard previousID != id else { return }
           // Reconnect-on-active per D-09
           if case .connected = state {
               Task { @MainActor in
                   state = .connecting
                   do {
                       try await tunnel.disconnect()
                       try await importer.provisionTunnelProfile(for: id)
                       let since = try await tunnel.connect()
                       state = .connected(since: since)
                   } catch {
                       state = .error(message: error.localizedDescription)
                   }
               }
           }
       }

    7. Pitfall 10 — reconcile selectedServerID при detection deleted server:
       Добавить public func `reconcileSelectionWithStore() async`. Логика: fetch supported ServerConfig; if let id = selectedServerID, !supported.contains(where: { $0.id == id }) → applySelection(nil) (но БЕЗ reconnect — server deleted, текущий connect через urltest fallback или disconnect — DECISION: текущий behavior: если deleted-server был активным и tunnel up — disconnect; реализация ниже).
       Вызывать reconcileSelectionWithStore() из:
       - MainScreenViewModel.refresh() (Phase 2 existing — добавляет вызов в конце)
       - ServerListViewModel.deleteServer/confirmDeleteSubscription (Plan 04 уже clear'ит coordinator.selectedServerID если был среди удалённых; этот reconcile — defensive для других путей, например NotificationCenter ModelDidChange).

    E. Errors enum:
    8. В MainScreenViewModel.swift добавить:
       public enum MainScreenError: Error, Equatable, LocalizedError {
           case noReachableServers
           case noSupportedServers
           public var errorDescription: String? {
               switch self {
               case .noReachableServers: return L10n.serverListNoReachableServers
               case .noSupportedServers: return L10n.serverListNoSupportedServers
               }
           }
       }

    F. L10n new keys:
    9. В Localizable.xcstrings добавить:
       - `serverList.noReachableServers` → RU «Все серверы недоступны. Проверьте подключение к интернету.» / EN «No reachable servers. Check your internet connection.»
       - `serverList.noSupportedServers` → RU «Нет поддерживаемых серверов» / EN «No supported servers»
    10. В L10n.swift добавить static let accessors.

    G. Run tests + commit:
    11. swift test --package-path BBTB/Packages/ConfigParser --filter PoolBuilderSingleOutboundTests — GREEN.
    12. swift test --package-path BBTB/Packages/AppFeatures --filter AutoSelectIntegrationTests — GREEN.
    13. swift test --package-path BBTB/Packages/AppFeatures — no regressions.
    14. swift test --package-path BBTB/Packages/VPNCore — no regressions (ProbeServicing protocol добавили, существующие tests должны работать).
    15. swift test --package-path BBTB/Packages/ConfigParser — no regressions.
    16. Commit: `feat(03-05): pre-connect auto-select + reconnect-on-selection + UserDefaults persist`.

    HARD constraints:
    - performPreConnectAutoSelect использует ServerProbeService.probeAll (Plan 02) → ServerScore.autoSelect (Plan 02). НЕ дублировать score logic.
    - reconnect-on-selection НЕТ алерта (D-09 explicit); UI sequence: .connected → .connecting → .connected, no banner.
    - Pitfall 8: всё-unreachable → .error состояние с L10n.serverListNoReachableServers — НЕ pre-connect-stuck в .connecting.
    - Pitfall 10: deleted selected server → graceful fallback (reconcile + Plan 04 clearance + this reconcile cross-check).
    - selectedServerID UserDefaults persist — не Keychain (не секрет, App-local).
    - НЕТ scope-reduction language — pre-connect runs полную D-04 семантику (parallel probes, score, 1-outbound pool, sing-box deployment).
  </action>
  <verify>
    <automated>cd /Users/vergevsky/ClaudeProjects/VPN && swift test --package-path BBTB/Packages/ConfigParser --filter PoolBuilderSingleOutboundTests 2>&1 | tail -10; swift test --package-path BBTB/Packages/AppFeatures --filter AutoSelectIntegrationTests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - PoolBuilder.swift declares `public static func buildSingleOutboundJSON(from parsed: AnyParsedConfig) throws -> String` (via extension)
    - MainScreenViewModel.swift contains `func performPreConnectAutoSelect()` 
    - MainScreenViewModel.swift contains `enum MainScreenError` with `case noReachableServers` AND `case noSupportedServers`
    - MainScreenViewModel.swift contains UserDefaults key `"app.bbtb.selectedServerID"` AND init parameter `userDefaults: UserDefaults`
    - MainScreenViewModel.swift `applySelection` body contains `if case .connected = state` AND calls `tunnel.disconnect` and `tunnel.connect` (reconnect logic)
    - ConfigImporter.swift `provisionTunnelProfile` accepts `for selectedID: UUID?` parameter
    - ConfigImporter.swift contains `PoolBuilder.buildSingleOutboundJSON` callsite (1-outbound path)
    - ServerProbeService.swift declares `public protocol ProbeServicing` AND `extension ServerProbeService: ProbeServicing`
    - Localizable.xcstrings contains key "serverList.noReachableServers"
    - L10n.swift declares `serverListNoReachableServers` static let accessor
    - Command `swift test --package-path BBTB/Packages/ConfigParser --filter PoolBuilderSingleOutboundTests` exits 0
    - Command `swift test --package-path BBTB/Packages/AppFeatures --filter AutoSelectIntegrationTests` exits 0
    - Command `swift test --package-path BBTB/Packages/AppFeatures` exits 0 (no regressions)
    - Command `swift test --package-path BBTB/Packages/VPNCore` exits 0
    - Command `swift test --package-path BBTB/Packages/ConfigParser` exits 0
    - Git log shows commit "feat(03-05): pre-connect auto-select + reconnect-on-selection + UserDefaults persist"
  </acceptance_criteria>
  <done>buildSingleOutboundJSON работает; performPreConnectAutoSelect → autoSelect → provisionTunnelProfile → connect chain работает; reconnect-on-selection без алерта; UserDefaults persist; Pitfall-8/10 fallbacks; все tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — Phase 3 full E2E на iPhone + macOS</name>
  <what-built>
    Plans 01-04 + Task 1-2 этого плана дали:
    1. SwiftData Subscription @Model + ServerConfig FK + idempotent migration
    2. ServerProbeService + ServerScore (TCP-probe core)
    3. ServerListSheet UI с sticky AutoCell + sections + ServerRow + LatencyBadge (UX-04 partial)
    4. Pull-to-refresh + foreground silent refresh + cascade delete + merge-by-identity
    5. Pre-connect auto-select + reconnect-on-selection + UserDefaults persist
  </what-built>
  <how-to-verify>
    **Подготовка**:
    1. Очистить state: на iPhone и macOS удалить старые BBTB конфигурации в Settings → VPN; уничтожить App Group store через TestFlight install (не upgrade): удалить app → reinstall свежий build.
    2. Иметь две тестовых subscription URL (например, два независимых operators) ИЛИ один с >=3 серверами разных протоколов.
    3. Иметь один URI для paste (single-paste path) — для Manual section.

    **iPhone UAT** (примерно 25 минут):

    T1 — **Server import создаёт Subscription**:
    1. Tap "+" → paste subscription URL № 1 → import. Verify: success alert с именем подписки и количеством серверов. Open server list sheet — Section с именем подписки в топе, AutoCell сверху с checkmark.
    2. Tap "+" → paste subscription URL № 2 → import. Open list — 2 секции (по lastFetched DESC).
    3. Tap "+" → paste single URI → import. Open list — Section «Добавлены вручную» в конце с одним сервером.

    T2 — **Sheet UI render correctness**:
    4. Open sheet. Verify: drag indicator показан; AutoCell sticky сверху; каждая Section имеет SubscriptionHeader (uppercase secondary text + last-fetched «только что»/«N мин назад»); ServerRow содержит флаг (или 🌐) + name + latency badge (initial spinner, progressively заменяется на ms).
    5. Verify Latency tier colors: <80ms green, 80-200ms yellow, 200-500ms orange, >500ms red. Unsupported серверы — opacity 0.4 + «не поддерживается» pill. Unreachable — «недоступен» red.

    T3 — **Pull-to-refresh 2-phase**:
    6. Pull-down в server list. Verify: system spinner показывается; sleep на 5 сек чтобы fetch+ping завершились; spinner dismisses; latency badges пересчитаны; «N мин назад» индикатор обновлён.
    7. **Negative path**: turn off Wi-Fi → pull-to-refresh. Verify: alert поверх sheet «Не удалось обновить подписки» (single OK button), sheet остаётся открытой. Turn Wi-Fi back on.

    T4 — **Foreground silent refresh**:
    8. Open server list, close (swipe down). Press home — app в background. Wait 30 sec. Re-open app. Verify: console (через Xcode connected device) показывает OSLog entries из `app.bbtb.server-list` subsystem indicating fetch + ping начались; UI не показывает spinner (silent). Открыть sheet — latency badges свежие.

    T5 — **Auto-select pre-connect**:
    9. Ensure AutoCell selected (checkmark on AutoCell, нет checkmark на отдельном сервере). Sheet dismiss.
    10. Tap power button на main screen. Verify: button → connecting state (animation bounce); ~1.5 sec → connected (timer запущен). Open sheet — check какой сервер selected (checkmark). Verify: это сервер с min latency среди supported.
    11. **Negative path**: turn off Wi-Fi → tap power. Verify: state → .error с user-visible message содержащим «Все серверы недоступны» (либо через alert). Turn Wi-Fi on, retry.

    T6 — **Manual selection + reconnect-on-active**:
    12. С активным tunnel (.connected): open sheet, select другой сервер. Verify: sheet dismiss; ConnectionButton bounce → .connecting → .connected без алерта; timer reset; новый server name в ServerLineView.
    13. Select Auto. Verify: pre-connect ping ~1.5 sec → reconnect → лучший сервер selected.

    T7 — **Swipe delete**:
    14. Open sheet. Swipe-left на ServerRow (single server) → tap «Удалить». Verify: server удалён немедленно (no confirm dialog), Keychain entry cleaned (нет orphan via Settings → Passwords? — нет user-visible UI; consider verified в unit-tests).
    15. Swipe-left на SubscriptionHeader → tap «Удалить» → confirm dialog «Удалить подписку "..." Будет удалено N серверов» → tap «Удалить». Verify: подписка + все её серверы удалены; AutoCell остаётся; если selected был среди удалённых — fallback to Auto (checkmark on AutoCell).

    T8 — **selectedServerID persist**:
    16. Select конкретный сервер (не Auto). Quit app (force-quit). Re-open. Verify: selected server тот же (UserDefaults restored).

    **macOS UAT** (примерно 10 минут):
    
    T9-T16 — repeat T1-T8 на macOS. Дополнительно verify:
    - Sheet размер: 480×720 default, resizable до min 400×600 (не collapse'ит).
    - .confirmationDialog renders как alert-style popover (not action sheet).
    - .swipeActions работают через right-click / context menu fallback (consider check).
    - scenePhase observer на macOS (Phase .active при window focus) — confirms console logs.

    Если ВСЕ tests PASS — type `approved`. Если есть failures — opisat`ьcheckpoint output: «UAT failed: T<N> — <description>».
  </how-to-verify>
  <resume-signal>Type "approved" if all 16 tests pass, or describe failures for gap-closure planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User selection (UserDefaults) → NETunnelProviderManager.providerConfiguration | UserDefaults писаны UI; считываются на следующий connect — между ними time gap; selected server может быть удалён в этот gap |
| pre-connect probe → real network | Все selectable серверы пингуются ДО tunnel; user's real IP видим всем выбираемым серверам (включая proverу) |
| Reconnect-on-selection-change → live tunnel state | Race: user меняет selection пока tunnel disconnect-in-progress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-23 | T (Tampering — data integrity) | UserDefaults selectedServerID не валидируется на ModelContainer existence — если server удалён вне ServerListFeature (например через context menu Settings — Phase 11), приложение пытается connect к non-existent server | mitigate | MainScreenViewModel.reconcileSelectionWithStore() в refresh()/onAppear; provisionTunnelProfile(for:) gracefully fallbacks на full pool если selectedID не найден в supported. test_deleted_selected_server_falls_back_to_auto verifies. |
| T-03-24 | I (Information Disclosure) | Pre-connect probe ВСЕХ supported серверов раскрывает client IP ВСЕМ серверам (не только тому к которому будет connect) | accept | RESEARCH Security Domain «TCP-probe leaks user IP». User не доверяет subscription operator? Скрытие IP требует proxy-pre-tunnel (Tor-style) — out of scope. Документировано wiki/security-gaps.md. |
| T-03-25 | D (DoS — own process) | Reconnect race: user быстро tap'ает разные servers в sheet → несколько disconnect/connect sequence'й конкурируют | mitigate | applySelection структурирована как Task { @MainActor in ... } — sequential because @MainActor. Однако если user tap'нет ДО предыдущего reconnect завершения — старый Task in-flight, новый запускается. Mitigation: проверять `if case .connecting = state { return }` в начале reconnect Task — игнорировать новые selection до завершения текущего. Document; UAT T6 verifies behavior. |
| T-03-26 | T (Tampering — invariant) | `.connecting` state stuck если provisionTunnelProfile throws мid-flow | mitigate | catch block в performToggleImpl и в reconnect Task устанавливает `state = .error(message:)` всегда. Test test_pre_connect_all_unreachable_returns_error verifies; UAT T5 negative path verifies. |
| T-03-27 | E (Elevation — privilege escalation in extension boundary) | NETunnelProviderManager updates providerConfiguration["configJSON"] — extension получает обновлённый config; если attacker инжектит malformed JSON через ConfigImporter path → extension может крашнуться или escalate | mitigate | Phase 1 SEC-06 carry-forward — ConfigImporter validates schema before persist. Plan 05 не добавляет нового parsing surface (только reuses Phase 2 parsed AnyParsedConfig). PoolBuilder.buildSingleOutboundJSON выдаёт same JSON structure что и Phase 2 (verified test_buildSingleOutboundJSON_preserves_protocol_specific_settings). |

**Carry-forward Phase 1/2:**
- R1 (no SOCKS inbound) — PoolBuilder.buildSingleOutboundJSON НЕ добавляет inbounds (Phase 1 carry-forward).
- R6 (P2P=false on NETunnelNetworkSettings) — Phase 2 baseline pre-tunnel applied; не trigger'ится этим planом.
- KILL-01/02 — kill switch toggle в Settings не меняется (Phase 2 carry-forward).
</threat_model>

<verification>
**Per-task automated:**
- Task 1 RED → Task 2 GREEN: PoolBuilderSingleOutboundTests + AutoSelectIntegrationTests pass
- Task 3 checkpoint:human-verify: 16 UAT subtests PASS на обеих platforms

**Plan-level:**
- swift test всех 3 пакетов (VPNCore, ConfigParser, AppFeatures) — exit 0
- ServerProbeService.ProbeServicing protocol адресуется в Plan 04 ServerListViewModel также (no double-injection conflict)
- ConfigImporter.provisionTunnelProfile(for:) — back-compat: Phase 2 callsites без selectedID компилируются (default nil)
- UserDefaults persist через app launch verified в UAT T8
- D-09 reconnect-without-alert verified в UAT T6
</verification>

<success_criteria>
1. ✅ PoolBuilder.buildSingleOutboundJSON работает — 1-outbound pool без urltest
2. ✅ MainScreenViewModel.performPreConnectAutoSelect — параллельный ping → min score → 1-outbound deployment
3. ✅ ConfigImporter.provisionTunnelProfile(for:) выбирает single-outbound path при selectedID, full urltest pool при nil
4. ✅ Auto-reconnect при applySelection во время .connected — без alert, через disconnect/provision/connect sequence
5. ✅ Pitfall 8: все 3/3 timeout → MainScreenError.noReachableServers → state .error с L10n.serverListNoReachableServers
6. ✅ Pitfall 10: deleted selected server → reconcile fallback to nil → graceful degradation
7. ✅ selectedServerID persists в UserDefaults; восстанавливается на новом launch
8. ✅ ServerProbeService conforms ProbeServicing protocol (testability)
9. ✅ ВСЕ tests green в трёх пакетах
10. ✅ UAT 16/16 subtests PASS на iPhone + macOS (после T7 если есть failures — gap-closure cycle)
11. ✅ Phase 3 goal полностью покрыт: import multi-subscription, view server list, pull-to-refresh, auto-select по score, переключение между серверами с reconnect, cascade delete
</success_criteria>

<output>
After completion, create `.planning/phases/03-server-management/03-05-SUMMARY.md`. Особое внимание:
- Open Question 4 resolution: pre-connect timeout — фактический wall-clock на устройстве (UAT T5 timing).
- Open Question 3 resolution: race condition pingAll-vs-pre-connect-auto-select (две параллельные probe sequence'и на одном actor; observed UAT поведение).
- UAT findings — все subtests + любые UX-полировки (deferred to Phase 11) — добавить в STATE.md Blockers section.
- Recommend: после UAT approval — обновить `wiki/server-management.md` (новая страница) с D-01..D-14 + implementation summary; обновить STATE.md «Phase 3 ✓ Complete».
</output>
